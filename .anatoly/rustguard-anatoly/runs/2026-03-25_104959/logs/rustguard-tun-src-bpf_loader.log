# Axis: utility

## User

## File: `rustguard-tun/src/bpf_loader.rs`
## Language: rust

```rust
//! Minimal BPF ELF loader for the XDP WireGuard filter.
//!
//! Loads the pre-compiled xdp_wg.o, creates the XSKMAP,
//! patches the map fd into the program, loads it, and attaches to an interface.
//!
//! No libbpf. No aya. Just raw bpf() syscalls and ELF parsing.
//! The .o is 1.4KB and never changes.

#![cfg(target_os = "linux")]

use std::io;

/// The pre-compiled BPF object.
const XDP_WG_OBJ: &[u8] = include_bytes!("../bpf/xdp_wg.o");

// BPF syscall commands.
const BPF_MAP_CREATE: u32 = 0;
const BPF_PROG_LOAD: u32 = 5;
const BPF_MAP_UPDATE_ELEM: u32 = 2;

// BPF map types.
const BPF_MAP_TYPE_XSKMAP: u32 = 17;

// BPF program types.
const BPF_PROG_TYPE_XDP: u32 = 6;

// XDP attach flags.
const XDP_FLAGS_SKB_MODE: u32 = 1 << 1;

// ELF constants.
const ET_REL: u16 = 1;
const SHT_PROGBITS: u32 = 1;
const SHT_REL: u32 = 9;
const EM_BPF: u16 = 247;

/// A loaded XDP program with its XSKMAP.
pub struct XdpProgram {
    pub prog_fd: i32,
    pub xsks_map_fd: i32,
    ifindex: u32,
}

impl XdpProgram {
    /// Load the XDP WireGuard filter and attach it to an interface.
    pub fn load_and_attach(ifname: &str) -> io::Result<Self> {
        let ifindex = super::xdp::if_nametoindex(ifname)?;

        // 1. Create the XSKMAP.
        let xsks_map_fd = bpf_create_xskmap(64)?;

        // 2. Parse ELF, extract program bytecode, patch map references.
        let insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;
        eprintln!("  BPF: parsed ELF, {} insn bytes, map_fd={}", insns.len(), xsks_map_fd);

        // 3. Load BPF program.
        let prog_fd = bpf_prog_load(&insns).map_err(|e| {
            io::Error::new(e.kind(), format!("prog_load ({} insns): {e}", insns.len() / 8))
        })?;
        eprintln!("  BPF: program loaded, prog_fd={}", prog_fd);

        // 4. Attach to interface.
        attach_xdp(ifindex, prog_fd).map_err(|e| {
            io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
        })?;
        eprintln!("  BPF: attached to ifindex={}", ifindex);

        Ok(Self {
            prog_fd,
            xsks_map_fd,
            ifindex,
        })
    }

    /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
    pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
        bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
    }
}

impl Drop for XdpProgram {
    fn drop(&mut self) {
        // Detach XDP program from interface.
        let _ = detach_xdp(self.ifindex);
        unsafe {
            libc::close(self.prog_fd);
            libc::close(self.xsks_map_fd);
        }
    }
}

// ── BPF syscalls ────────────────────────────────────────────────────

fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
    #[repr(C)]
    struct BpfAttrMapCreate {
        map_type: u32,
        key_size: u32,
        value_size: u32,
        max_entries: u32,
    }

    let attr = BpfAttrMapCreate {
        map_type: BPF_MAP_TYPE_XSKMAP,
        key_size: 4,
        value_size: 4,
        max_entries,
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_CREATE,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapCreate>(),
        )
    } as i32;

    if fd < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(fd)
}

fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
    let license = b"GPL\0";
    let mut log_buf = vec![0u8; 65536];

    // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
    // We need at least the fields up to kern_version, padded to the
    // minimum size the kernel accepts. Using 256 bytes to be safe.
    #[repr(C)]
    struct BpfAttrProgLoad {
        prog_type: u32,
        insn_cnt: u32,
        insns: u64,
        license: u64,
        log_level: u32,
        log_size: u32,
        log_buf: u64,
        kern_version: u32,
        _pad: [u8; 208], // Pad to 256 bytes total.
    }

    let attr = BpfAttrProgLoad {
        prog_type: BPF_PROG_TYPE_XDP,
        insn_cnt: (insns.len() / 8) as u32,
        insns: insns.as_ptr() as u64,
        license: license.as_ptr() as u64,
        log_level: 1,
        log_size: log_buf.len() as u32,
        log_buf: log_buf.as_mut_ptr() as u64,
        kern_version: 0,
        _pad: [0; 208],
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_PROG_LOAD,
            &attr as *const _,
            std::mem::size_of::<BpfAttrProgLoad>(),
        )
    } as i32;

    if fd < 0 {
        let err = io::Error::last_os_error();
        let log_end = log_buf.iter().position(|&b| b == 0).unwrap_or(0);
        if log_end > 0 {
            let log_str = String::from_utf8_lossy(&log_buf[..log_end]);
            eprintln!("  BPF verifier log:\n{log_str}");
        }
        return Err(err);
    }
    Ok(fd)
}

fn bpf_map_update(map_fd: i32, key: &u32, value: &i32) -> io::Result<()> {
    #[repr(C)]
    struct BpfAttrMapElem {
        map_fd: u32,
        key: u64,
        value: u64,
        flags: u64,
    }

    let attr = BpfAttrMapElem {
        map_fd: map_fd as u32,
        key: key as *const _ as u64,
        value: value as *const _ as u64,
        flags: 0, // BPF_ANY
    };

    let ret = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_UPDATE_ELEM,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapElem>(),
        )
    };

    if ret < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(())
}

// ── XDP attach/detach via netlink ───────────────────────────────────

fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Use the SIOCETHTOOL / netlink approach, but the simplest way
    // that works on all kernels is via the if_link netlink interface.
    // For simplicity, shell out to `ip link set dev <name> xdpgeneric obj ...`
    // But we already have the fd. Use the bpf_set_link_xdp_fd equivalent.

    // Direct approach: set XDP fd via netlink RTM_SETLINK.
    // This is complex. Simpler: use the deprecated but working SIOCSIFXDP.
    // Actually simplest: use libc's if_nametoindex + raw netlink.

    // For now, use the ip command — it works and avoids 200 lines of netlink.
    let ifname = ifindex_to_name(ifindex)?;
    let fd_path = format!("/proc/self/fd/{prog_fd}");
    let output = std::process::Command::new("ip")
        .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
        .output();

    // Fallback: use the bpf netlink API via raw netlink socket.
    // If ip command doesn't support pinned fd, try bpf_xdp_attach.
    match output {
        Ok(out) if out.status.success() => Ok(()),
        _ => attach_xdp_netlink(ifindex, prog_fd),
    }
}

fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Netlink RTM_SETLINK with IFLA_XDP attribute.
    // This is the proper way — no shelling out.
    use std::os::unix::io::FromRawFd;

    let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
    if sock < 0 {
        return Err(io::Error::last_os_error());
    }

    // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
    const RTM_SETLINK: u16 = 19;
    const NLM_F_REQUEST: u16 = 1;
    const NLM_F_ACK: u16 = 4;
    const IFLA_XDP: u16 = 43;
    const IFLA_XDP_FD: u16 = 1;
    const IFLA_XDP_FLAGS: u16 = 3;

    // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
    let mut buf = [0u8; 64];
    let mut off = 0;

    // nlmsghdr
    let total_len: u32 = 52;
    buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
    off += 4;
    buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
    off += 2;
    off += 8; // seq + pid = 0

    // ifinfomsg
    buf[off] = libc::AF_UNSPEC as u8; // family
    off += 4; // family + pad + type
    let idx_bytes = ifindex.to_ne_bytes();
    buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
    off += 4;
    off += 8; // flags + change

    // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
    let xdp_nla_len: u16 = 20;
    buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
    off += 2;
    let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
    buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
    off += 2;

    // IFLA_XDP_FD (len=8, type=1, value=prog_fd)
    let fd_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&fd_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FD.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&prog_fd.to_ne_bytes());
    off += 4;

    // IFLA_XDP_FLAGS (len=8, type=3, value=XDP_FLAGS_SKB_MODE)
    let flags_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&flags_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FLAGS.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&XDP_FLAGS_SKB_MODE.to_ne_bytes());

    let mut sa: libc::sockaddr_nl = unsafe { std::mem::zeroed() };
    sa.nl_family = libc::AF_NETLINK as u16;

    let ret = unsafe {
        libc::sendto(
            sock,
            buf.as_ptr() as *const _,
            total_len as usize,
            0,
            &sa as *const _ as *const _,
            std::mem::size_of::<libc::sockaddr_nl>() as u32,
        )
    };

    if ret < 0 {
        let err = io::Error::last_os_error();
        unsafe { libc::close(sock) };
        return Err(err);
    }

    // Read ACK.
    let mut resp = [0u8; 128];
    let n = unsafe { libc::recv(sock, resp.as_mut_ptr() as *mut _, resp.len(), 0) };
    unsafe { libc::close(sock) };

    if n < 0 {
        return Err(io::Error::last_os_error());
    }

    // Check for error in nlmsghdr.
    if n >= 16 {
        let nlmsg_type = u16::from_ne_bytes([resp[4], resp[5]]);
        if nlmsg_type == 2 {
            // NLMSG_ERROR
            let error = i32::from_ne_bytes([resp[16], resp[17], resp[18], resp[19]]);
            if error < 0 {
                return Err(io::Error::from_raw_os_error(-error));
            }
        }
    }

    Ok(())
}

fn detach_xdp(ifindex: u32) -> io::Result<()> {
    attach_xdp_netlink(ifindex, -1) // fd=-1 detaches
}

fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
    let mut buf = [0u8; 16];
    let ret = unsafe { libc::if_indextoname(ifindex, buf.as_mut_ptr() as *mut _) };
    if ret.is_null() {
        return Err(io::Error::last_os_error());
    }
    let end = buf.iter().position(|&b| b == 0).unwrap_or(16);
    String::from_utf8(buf[..end].to_vec())
        .map_err(|_| io::Error::new(io::ErrorKind::InvalidData, "invalid ifname"))
}

// ── Minimal ELF parser ──────────────────────────────────────────────

fn parse_and_patch_elf(elf: &[u8], map_fd: i32) -> io::Result<Vec<u8>> {
    if elf.len() < 64 || &elf[0..4] != b"\x7fELF" {
        return Err(io::Error::new(io::ErrorKind::InvalidData, "not an ELF"));
    }

    let e_type = u16::from_le_bytes([elf[16], elf[17]]);
    let e_machine = u16::from_le_bytes([elf[18], elf[19]]);
    if e_type != ET_REL || e_machine != EM_BPF {
        return Err(io::Error::new(
            io::ErrorKind::InvalidData,
            "not a BPF relocatable ELF",
        ));
    }

    let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;
    let e_shentsize = u16::from_le_bytes([elf[58], elf[59]]) as usize;
    let e_shnum = u16::from_le_bytes([elf[60], elf[61]]) as usize;
    let e_shstrndx = u16::from_le_bytes([elf[62], elf[63]]) as usize;

    let shstrtab_off = {
        let sh = &elf[e_shoff + e_shstrndx * e_shentsize..];
        u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize
    };

    // Collect section info.
    struct Section {
        name: String,
        sh_type: u32,
        offset: usize,
        size: usize,
        info: u32, // sh_info — for REL sections, points to the target section.
    }

    let mut sections = Vec::new();
    for i in 0..e_shnum {
        let sh = &elf[e_shoff + i * e_shentsize..];
        let sh_name_off = u32::from_le_bytes(sh[0..4].try_into().unwrap()) as usize;
        let sh_type = u32::from_le_bytes(sh[4..8].try_into().unwrap());
        let sh_offset = u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize;
        let sh_size = u64::from_le_bytes(sh[32..40].try_into().unwrap()) as usize;
        let sh_info = u32::from_le_bytes(sh[28..32].try_into().unwrap());
        // Wait, sh_info is at offset 44 in Elf64_Shdr. Let me recalculate.
        // Elf64_Shdr: name(4) type(4) flags(8) addr(8) offset(8) size(8) link(4) info(4) addralign(8) entsize(8)
        let sh_info = u32::from_le_bytes(sh[44..48].try_into().unwrap());

        let name_start = shstrtab_off + sh_name_off;
        let name_end = elf[name_start..]
            .iter()
            .position(|&b| b == 0)
            .unwrap_or(0)
            + name_start;
        let name = std::str::from_utf8(&elf[name_start..name_end])
            .unwrap_or("")
            .to_string();

        sections.push(Section {
            name,
            sh_type,
            offset: sh_offset,
            size: sh_size,
            info: sh_info,
        });
    }

    // Find the "xdp" program section.
    let prog_idx = sections
        .iter()
        .position(|s| s.name == "xdp" && s.sh_type == SHT_PROGBITS)
        .ok_or_else(|| {
            io::Error::new(io::ErrorKind::InvalidData, "no 'xdp' section")
        })?;
    let prog = &sections[prog_idx];
    let mut insns = elf[prog.offset..prog.offset + prog.size].to_vec();

    // Find relocation sections targeting the program section.
    // These tell us which LD_IMM64 instructions reference maps.
    for sec in &sections {
        if sec.sh_type != SHT_REL {
            continue;
        }
        if sec.info as usize != prog_idx {
            continue; // This relocation targets a different section.
        }

        // Process each relocation entry (Elf64_Rel: offset(8) + info(8) = 16 bytes).
        let rel_data = &elf[sec.offset..sec.offset + sec.size];
        let mut off = 0;
        while off + 16 <= rel_data.len() {
            let r_offset = u64::from_le_bytes(rel_data[off..off + 8].try_into().unwrap()) as usize;
            // r_info contains symbol index in upper 32 bits, type in lower 32.
            // For BPF map refs, the type is R_BPF_64_64 (1).

            // At r_offset in the instruction stream, set src_reg=1 (BPF_PSEUDO_MAP_FD)
            // and imm=map_fd.
            if r_offset + 16 <= insns.len() && insns[r_offset] == 0x18 {
                // Set src_reg to BPF_PSEUDO_MAP_FD (1).
                insns[r_offset + 1] = (insns[r_offset + 1] & 0x0f) | (1 << 4);
                // Set imm to map fd.
                insns[r_offset + 4..r_offset + 8]
                    .copy_from_slice(&(map_fd as u32).to_le_bytes());
                // Zero upper 32 bits of the 64-bit immediate.
                insns[r_offset + 12..r_offset + 16].copy_from_slice(&0u32.to_le_bytes());
            }

            off += 16;
        }
    }

    Ok(insns)
}

```

## Symbols to evaluate

- constant XDP_WG_OBJ (L14–L14)
- constant BPF_MAP_CREATE (L17–L17)
- constant BPF_PROG_LOAD (L18–L18)
- constant BPF_MAP_UPDATE_ELEM (L19–L19)
- constant BPF_MAP_TYPE_XSKMAP (L22–L22)
- constant BPF_PROG_TYPE_XDP (L25–L25)
- constant XDP_FLAGS_SKB_MODE (L28–L28)
- constant ET_REL (L31–L31)
- constant SHT_PROGBITS (L32–L32)
- constant SHT_REL (L33–L33)
- constant EM_BPF (L34–L34)
- export class XdpProgram (L37–L41)
- function bpf_create_xskmap (L93–L122)
- function bpf_prog_load (L124–L175)
- function bpf_map_update (L177–L206)
- function attach_xdp (L210–L233)
- function attach_xdp_netlink (L235–L342)
- function detach_xdp (L344–L346)
- function ifindex_to_name (L348–L357)
- function parse_and_patch_elf (L361–L470)

## Pre-computed Import Analysis

- XDP_WG_OBJ (not exported): internal only — check local usage in file
- BPF_MAP_CREATE (not exported): internal only — check local usage in file
- BPF_PROG_LOAD (not exported): internal only — check local usage in file
- BPF_MAP_UPDATE_ELEM (not exported): internal only — check local usage in file
- BPF_MAP_TYPE_XSKMAP (not exported): internal only — check local usage in file
- BPF_PROG_TYPE_XDP (not exported): internal only — check local usage in file
- XDP_FLAGS_SKB_MODE (not exported): internal only — check local usage in file
- ET_REL (not exported): internal only — check local usage in file
- SHT_PROGBITS (not exported): internal only — check local usage in file
- SHT_REL (not exported): internal only — check local usage in file
- EM_BPF (not exported): internal only — check local usage in file
- XdpProgram (exported): imported by 0 files — LIKELY DEAD
- bpf_create_xskmap (not exported): internal only — check local usage in file
- bpf_prog_load (not exported): internal only — check local usage in file
- bpf_map_update (not exported): internal only — check local usage in file
- attach_xdp (not exported): internal only — check local usage in file
- attach_xdp_netlink (not exported): internal only — check local usage in file
- detach_xdp (not exported): internal only — check local usage in file
- ifindex_to_name (not exported): internal only — check local usage in file
- parse_and_patch_elf (not exported): internal only — check local usage in file

Evaluate the utility of each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] TransportSession: utility DEAD → USED**: Deliberation reclassified: UTILITY DEAD→USED: This is a `pub struct` in a `-core` library crate (`rustguard-core`). Library crate public types are consumed by downstream crates in the workspace (e.g., `rustguard`). The utility evaluator likely only searched within the same crate and missed cross-crate imports, a classic false positive for workspace library crates. Reclassified to USED. TESTS NONE→WEAK: The merged field says NONE but the detail text itself says WEAK, and the source clearly contains 6 test functions in a `#[cfg(test)]` module covering encrypt/decrypt roundtrip, counter monotonicity, wrong-counter rejection, replay rejection, out-of-order delivery, and bidirectional use. The WEAK assessment is accurate: `encrypt_to` and `decrypt_in_place` (separate code paths using `seal_to`/`open_to`) have zero coverage, and nonce exhaustion / edge cases are untested. DOCUMENTATION UNDOCUMENTED→DOCUMENTED: The detail text itself concludes DOCUMENTED. The struct has a thorough `///` block (lines 7–11) explaining purpose, derivation, and key-role convention. Both public fields and all private fields carry `///` comments. Four of six methods have doc comments. Two public items (`new`, `send_counter`) lack doc comments (noted in best_practices rule 9), but the overall coverage is well above UNDOCUMENTED. The struct-level and field-level documentation is comprehensive.
- **[deliberation] COOKIE_LEN: utility DEAD → USED**: Deliberation reclassified: Utility DEAD reclassified → USED: this is a `pub const` in a library crate (`rustguard-core`). Downstream crates (e.g., message layout code) depend on it; pre-computed analysis likely doesn't capture cross-crate workspace imports. The entire cookie module is clearly used infrastructure for a WireGuard implementation. Tests NONE contradicts the detail text which says '[GOOD] Pure constant. Used throughout and indirectly validated in every roundtrip test.' For a pure constant with no runtime behavior, indirect validation is sufficient — reclassified to GOOD. Documentation UNDOCUMENTED is wrong — line 23 has `/// Cookie size.` which IS a doc comment, making this PARTIAL (brief but present).
- **[deliberation] CookieChecker: utility DEAD → USED**: Deliberation reclassified: Utility DEAD reclassified → USED: CookieChecker is the primary server-side public API of this module in a library crate. Pre-computed cross-crate analysis likely misses workspace-level imports. The entire module exists to support WireGuard cookie DoS protection — this struct is clearly intended for use by consuming crates. Tests NONE contradicts the detail text which says 'happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests' — reclassified to WEAK (tested but with gaps in verify_mac1, rotation, new_with). Documentation UNDOCUMENTED contradicts detail: 'Struct has a /// summary and all four fields have field-level /// comments.' Line 49 has a doc comment. Reclassified to PARTIAL (docs exist but missing examples and constructor docs).
- **[deliberation] CookieState: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX kept: unlike elapsed_since, the consequence here is that process_reply successfully decrypts a cookie but never marks it as fresh, making compute_mac2 silently return zeros on no_std. While the overall no_std design is intentional, this specific behavior is more insidious — a successful decryption followed by silent failure to use the cookie is a subtle trap with no comment explaining it. Utility DEAD → USED: same reasoning as CookieChecker — primary client-side public API in a library crate. Tests NONE → WEAK: detail says 'Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie.' Documentation UNDOCUMENTED → PARTIAL: detail says 'Struct has a /// summary and both private fields have doc comments.'
- **[deliberation] ReplayWindow: utility DEAD → USED**: Deliberation reclassified: CORRECTION ERROR confirmed at high confidence: shift_window's bit-shift block (L112-116) iterates in reverse with right-shift (>>), moving bits toward lower ages instead of higher ages. Verified with concrete example: top=0, bitmap[0]=1, then shift_window(1) right-shifts bitmap[0] to 0, losing the counter-0 mark. A subsequent check(0) would incorrectly accept the replay. The word-level copy_within is correct; only the sub-word bit-shift is inverted. Tests don't catch this because they never re-check a previously-seen counter after a shift has occurred. UTILITY reclassified DEAD→USED: this is a pub struct in a library crate (rustguard-core). Library public API types are not dead simply because no in-tree consumer is visible — the struct is the module's primary export and is extensively exercised in tests. TESTS reclassified NONE→WEAK: the detail text itself says '[WEAK]' and describes solid behavioral coverage via check_and_update (sequential, duplicate rejection, out-of-order, boundary, stress). However, check() and update() are never tested independently, and the critical shift_window bit-direction bug goes undetected due to test design gaps. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the struct's two fields have /// doc comments (L15, L17), and all three public methods have /// doc comments. Only the struct-level definition and new() constructor lack /// docs. This is clearly partial, not absent.
- **[deliberation] hash: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: this is a library crate's public API. Exported functions in a crypto library ARE the product — absence of in-workspace importers does not make them dead. The crate is designed to be consumed externally (rustguard-crypto). Tests NONE contradicts the detail text which describes three existing tests (hash_deterministic, hash_different_inputs, hash_multi_chunk); reclassified to WEAK since tests exist but lack reference vectors. Documentation UNDOCUMENTED contradicts the detail and source: line 8 has `/// BLAKE2s-256 hash.` — it's PARTIAL (brief but present, missing params/examples).
- **[deliberation] mac: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive for the same library-crate reason as hash. Tests NONE contradicts the detail text which describes mac_differs_with_key and indirect coverage via hmac_differs_from_keyed_blake2s; reclassified to WEAK (tests exist but no reference vectors). Documentation UNDOCUMENTED contradicts the source: lines 17-20 have a multi-line /// block explaining keyed MAC vs HMAC distinction. Reclassified to PARTIAL per the detail's own assessment.
- **[deliberation] hkdf: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: hkdf is a core public function of a crypto library crate, essential for the WireGuard KDF. Tests NONE contradicts the detail which describes two tests (hkdf_produces_different_outputs, hkdf_deterministic); reclassified to WEAK since no RFC 5869/WireGuard spec test vectors are present. Documentation UNDOCUMENTED contradicts source: lines 31-36 have a substantial /// block describing the algorithm and return semantics. Reclassified to PARTIAL.
- **[deliberation] AEAD_TAG_LEN: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: This is a `pub const` in a library crate (`rustguard-crypto`). It is the fundamental tag length constant meant for downstream consumers. The evaluator itself noted it matches Known False Positives. Zero in-crate importers is expected for a library public API constant. Tests NONE→GOOD: Pure compile-time constant with no runtime behavior; indirectly validated in `seal_then_open` assertion (`ciphertext.len() == plaintext.len() + AEAD_TAG_LEN`). No dedicated test needed. Documentation UNDOCUMENTED confirmed: line 7 has no `///` comment, so UNDOCUMENTED is correct.
- **[deliberation] MAX_PACKET_SIZE: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the doc comment on L9 says 'MTU 1420 + tag' but the expression computes `1500 + AEAD_TAG_LEN = 1516`, a clear mismatch (~80 bytes difference). Either the doc or the value is wrong. Utility DEAD→USED: library public constant for downstream consumption, same false positive pattern as AEAD_TAG_LEN. Tests NONE→GOOD: compile-time constant with no runtime behavior, no dedicated test required. Documentation UNDOCUMENTED→PARTIAL: line 9 has a `///` doc comment present (`/// Maximum WireGuard transport payload (MTU 1420 + tag).`), so UNDOCUMENTED is incorrect. Reclassified to PARTIAL due to the inaccurate MTU value in the doc.
- **[deliberation] seal: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: primary public encryption API of the library crate, consumed by downstream crates. Tests NONE→WEAK: `seal` is directly called in all 5 test functions (seal_then_open, wrong_key_fails, wrong_counter_fails, tampered_ciphertext_fails, aad_mismatch_fails), so NONE is factually incorrect. Reclassified to WEAK since edge cases (empty plaintext, large payloads, non-zero counter as primary input) are missing. Documentation UNDOCUMENTED→PARTIAL: has a multi-line `///` doc block on L12-15 describing purpose, nonce construction, and AAD semantics. Missing param-level docs and examples, hence PARTIAL not DOCUMENTED.
- **[deliberation] open: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: primary public decryption API of the library crate. Tests NONE→GOOD: five tests thoroughly exercise `open` — happy path, wrong key, wrong counter, tampered ciphertext, and AAD mismatch. All four primary authentication-failure modes are covered. NONE is clearly wrong. Documentation UNDOCUMENTED→PARTIAL: has a `///` block on L31-33 describing purpose and three failure conditions for `None` return. Missing param-level docs and examples, hence PARTIAL.
- **[deliberation] xseal: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: public API variant for WireGuard cookie encryption, consumed by downstream crates. Tests NONE confirmed: no test covers xseal — this is a genuine gap on a security-critical path. Documentation UNDOCUMENTED→PARTIAL: has a single-line `///` on L48 noting algorithm, nonce size, and use case. Minimal but present, so UNDOCUMENTED is incorrect.
- **[deliberation] xopen: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: public API variant for WireGuard cookie decryption, consumed by downstream crates. Tests NONE confirmed: no test covers xopen — roundtrip, wrong key, tampered data, and nonce mismatch all untested. Documentation UNDOCUMENTED→PARTIAL: has a single-line `///` on L57 noting algorithm, nonce size, and use case. Present but lacks param docs and return semantics.
- **[deliberation] seal_to: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: zero-allocation public API for hot-path transport encryption, consumed by downstream crates. Tests NONE confirmed: no test covers seal_to — output length, correct tag placement, and buffer semantics all unverified. Documentation UNDOCUMENTED→PARTIAL: has a three-line `///` block on L66-68 describing purpose, buffer precondition, and zero-allocation property. Missing `# Panics` section for `.expect()` and param-level docs.
- **[deliberation] open_to: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the function guards `ct_len < AEAD_TAG_LEN` (defensive intent) but never checks `ct_len > buf.len()`. Since `buf[..ct_len]` on L91 will panic if ct_len exceeds buf length, and this function processes potentially adversarial input while advertising Option return, the missing upper-bound guard is a genuine inconsistency. Utility DEAD→USED: zero-allocation public API for hot-path transport decryption. Tests NONE confirmed: completely untested. Documentation UNDOCUMENTED→PARTIAL: has a three-line `///` block on L81-83 describing purpose, buffer contract, and zero-allocation guarantee.
- **[deliberation] Tai64n: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: This is a `pub struct` in `rustguard-crypto`, a library crate whose entire purpose is to export types for cross-crate consumption. The evaluator itself noted this false-positive pattern and reduced confidence. Cross-crate imports were outside analysis scope, and similar symbols (TransportSession, CookieChecker) were previously misclassified. Reclassified with high confidence. Tests NONE → WEAK: The symbol field says NONE but the detail section itself begins with '[WEAK] Three inline tests cover monotonicity...' — there are clearly three tests in the `mod tests` block (L50-L80). They exist but only check roundtrip equality, never assert against known-correct byte encodings. WEAK is the accurate classification. Documentation UNDOCUMENTED → PARTIAL: Lines 4-7 contain four `///` doc comments on the struct covering purpose, byte layout, WireGuard use-case, and replay-monotonicity invariant. The detail section even states 'Struct carries four lines of /// doc comments — solid struct-level narrative.' The struct is documented; only `as_bytes` and `from_bytes` methods lack docs. PARTIAL is correct.
- **[deliberation] KEEPALIVE_TIMEOUT: utility DEAD → LOW_VALUE**: Deliberation reclassified: Utility DEAD → LOW_VALUE: this is an exported WireGuard protocol constant (§6.3). The needs_keepalive method deliberately takes keepalive_interval as an Option<Duration> parameter, making KEEPALIVE_TIMEOUT the canonical default value for external callers to pass in. For a library crate, exported protocol constants serve as public API even without internal usage — downstream consumers may use it. Removing it would break the API contract. Tests NONE: stands — no test references it. Documentation: the source clearly has '/// Keepalive interval when configured.' on L51, and the detail text itself says '[DOCUMENTED]'. The UNDOCUMENTED classification contradicts the evaluator's own detail — reclassify to DOCUMENTED.
- **[deliberation] Peer: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: The detail itself acknowledges this matches a known false-positive pattern for exported structs in workspace crates. `Peer` is the fundamental domain struct for a WireGuard daemon — it is inconceivable that a daemon named `rustguard-daemon` does not use its own Peer representation. The pre-analysis hedged with 'LIKELY DEAD', and the analysis simply lacked visibility into sibling modules (device.rs, handshake.rs, etc.) that inevitably consume this type. Reclassified with high confidence. Tests NONE: Agreed — no tests exist for this module or its methods (from_config, allows_ip, has_active_session). Kept as-is. Documentation UNDOCUMENTED → PARTIAL: The source clearly shows a struct-level `///` doc comment at L15, plus field-level `///` docs on `session` (L22) and `timers` (L24). Five fields remain undocumented and `from_config` lacks a doc comment, so coverage is incomplete but not absent. PARTIAL is the correct classification per the detail's own description.
- **[deliberation] run: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX confirmed: (1) MSG_RESPONSE peer-matching at L296-301 uses a heuristic that breaks with multiple peers — the first peer without an active session is selected regardless of which peer the handshake was for. (2) Timer keepalive nonce exhaustion at ~L340 continues without clearing the session, preventing rekeying. Both are real bugs visible in the source. Utility reclassified DEAD→USED: this is `pub fn run()`, the sole public entry point of the daemon tunnel module. It is almost certainly called from main.rs or a binary crate entry point. The pre-computed analysis likely did not trace binary crate call graphs. A pub fn named 'run' in a daemon module being dead code would be nonsensical. Overengineering ACCEPTABLE kept — large but justified orchestration function. Tests NONE accurate. Documentation reclassified UNDOCUMENTED→PARTIAL: the function does have a /// doc comment ('Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.') as visible at L48, so it is not fully undocumented; it is partial (missing parameter/error docs).
- **[deliberation] is_open: utility DEAD → USED**: Deliberation reclassified: Utility reclassified DEAD → USED. This is a library crate, and `is_open` is a natural part of the enrollment window API (`open_window`, `close_window`, `remaining`, `is_open`). The same zero-importer pattern applies to `send_command`, `start_listener`, and `cleanup` which are all classified USED under the library-crate pattern. Removing `is_open` would leave the API incomplete — callers would have to call `remaining() > 0` instead. Tests NONE and documentation PARTIAL kept as-is.
- **[deliberation] JoinConfig: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: JoinConfig is a public API struct serving as the sole parameter to the crate's primary entry point `run()`. The DEAD classification is a systematic false positive from cross-crate analysis not capturing workspace-internal consumers. This matches the documented false-positive pattern for library crate public types. Tests NONE → GOOD: The evaluator's own detail text says 'Per rule 6, types/interfaces/structs with no runtime behavior are GOOD by default' — this is a plain data struct with two fields and no methods, so GOOD is correct. Documentation UNDOCUMENTED kept: valid finding, public config type lacks /// doc comments and field descriptions.
- **[deliberation] ParsedUdp: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: This is a pub struct in a library crate returned by the public parse_eth_udp function. The original detail itself acknowledges this matches the Known False Positives pattern for library crate exports consumed by downstream crates. Tests NONE → WEAK: The detail text itself states 'The struct's two fields (src_addr, payload) are exercised by the single inline test parse_ipv4_udp_frame, which asserts both field values.' This is partial coverage (IPv4 path only, no IPv6), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Struct has a /// doc comment on L8' — the struct-level doc exists on line 8. Only the per-field docs are missing. That is PARTIAL, not UNDOCUMENTED.
- **[deliberation] parse_eth_udp: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: Public function in a library crate serving as the main entry point for Ethernet frame parsing. The original detail itself notes it matches the Known False Positives pattern for library crate public APIs consumed by downstream crates not visible in same-crate import analysis. Tests NONE → WEAK: The detail states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path through parse_eth_udp.' This is exercised but with limited coverage (no IPv6 branch, no error paths), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Has a two-line /// block (L14–L15) describing purpose and the None return condition.' A doc comment exists; it merely lacks #Examples and parameter descriptions. That is PARTIAL.
- **[deliberation] BATCH_SIZE: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: BATCH_SIZE is actively used within this file in RecvBatch struct field types (L20-22), for-loop bounds, and array initializations in recv_batch. It is part of the module's coherent public API. Static analysis missed intra-module usage. Documentation UNDOCUMENTED→PARTIAL: Source clearly shows `/// Maximum batch size for recvmmsg/sendmmsg.` at L13; the detail text itself states PARTIAL. The top-level UNDOCUMENTED classification contradicts the detail. Tests NONE kept: factual, though low concern for a compile-time constant.
- **[deliberation] PKT_BUF_SIZE: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: PKT_BUF_SIZE is used in RecvBatch struct definition (L20) and recv_batch buffer allocation (L50, L113). Actively consumed within the file. Documentation UNDOCUMENTED→PARTIAL: Source shows `/// Per-packet buffer size.` at L15; detail text confirms PARTIAL. Tests NONE kept: factual, low concern for a constant.
- **[deliberation] RecvBatch: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: RecvBatch is used as parameter type in both recv_batch function signatures (L40, L113). It is the core data structure of this module's API. Documentation UNDOCUMENTED→PARTIAL: Source has `/// A batch of received packets.` at L18; detail text confirms PARTIAL (struct doc exists but fields lack individual docs). Tests NONE kept: struct and constructor have zero coverage, which is a legitimate gap for a public type with unsafe-adjacent usage patterns.
- **[deliberation] recv_batch: utility DEAD → USED**: Deliberation reclassified: This is the macOS recv_batch at L112-L126. Utility DEAD→USED: macOS fallback is the platform-specific implementation consumers will call; cfg-gating makes static import analysis unreliable. Documentation UNDOCUMENTED→PARTIAL: Source shows `/// macOS fallback: receive one packet at a time.` at L110; detail text confirms PARTIAL. Tests NONE kept: WouldBlock and single-packet paths need coverage.
- **[deliberation] send_packet: utility DEAD → USED**: Deliberation reclassified: This is the macOS send_packet at L129-L131. Utility DEAD→USED: macOS platform counterpart, same reasoning as other cfg-gated functions. Overengineering ACCEPTABLE kept: API symmetry justified. Documentation UNDOCUMENTED kept: this is the one send_packet variant that truly has no doc comment (L128 is just #[cfg], no /// above it). Tests NONE kept: trivial wrapper.
- **[deliberation] PersistedPeer: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: PersistedPeer is a pub struct in a library crate (rustguard-enroll) intended to be consumed by other workspace crates. Zero in-file importers is expected for a public API type — the evaluator only searched within the crate boundary. Tests reclassified from NONE to GOOD: the detail text itself states 'Both inline tests directly construct and assert on PersistedPeer instances, exercising both public fields' and concludes GOOD for a plain data struct — the NONE field value contradicts the detail. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 11 clearly shows `/// A persisted peer — just enough to re-enroll on restart.` and best_practices rule 9 confirms 'All public items carry /// doc comments'. Field-level docs are absent but struct-level doc exists.
- **[deliberation] default_state_path: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate, designed for external consumers to call for the default path. No in-crate callers is expected — tests use temp dirs instead, and the binary crate would call this. Tests NONE is accurate: no test directly invokes this function, though for a trivial one-liner the risk is minimal. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 19 shows `/// Default state file path.` — the doc exists but is minimal (no examples, no platform notes).
- **[deliberation] save: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate meant for external consumers. Tests reclassified from NONE to WEAK: the detail text itself says 'Covered only by the save_and_load_roundtrip happy-path test with two peers' — the function IS called in the test at line 108, so NONE is factually incorrect. WEAK is appropriate given only the happy path is covered and error paths are untested. Documentation reclassified from UNDOCUMENTED to PARTIAL: lines 24-25 show a two-line /// doc comment describing purpose and format. It exists but lacks #Errors and parameter docs.
- **[deliberation] load: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate for external consumption. Tests reclassified from NONE to WEAK: both test functions call load() — save_and_load_roundtrip (line 109) and load_nonexistent_returns_empty (line 116). The detail text itself describes WEAK coverage with specific gaps (error paths untested). NONE is factually wrong. Documentation reclassified from UNDOCUMENTED to PARTIAL: line 48 shows `/// Load peers from the state file.` — the doc exists but is minimal, missing #Errors section and the important NotFound → Ok(vec![]) behavior.
- **[deliberation] icmp_checksum: utility LOW_VALUE → USED**: Deliberation reclassified: overengineering reclassified OVER → ACCEPTABLE: in an example file meant to teach, named wrappers like icmp_checksum provide semantic clarity at the call site (icmp_checksum(&reply[20..n]) is immediately understandable vs checksum_fold(...)). This is pedagogical naming, not premature abstraction. utility reclassified LOW_VALUE → USED: the wrapper is actively called and serves a clear readability purpose—it communicates the intent of the checksum computation to readers of the example. The semantic naming IS the value. tests NONE is factual for an example binary helper. documentation UNDOCUMENTED kept: no doc comment, though the name is self-descriptive and it's a private helper in an example file. Confidence raised to 85 due to clear reasoning about example-file conventions.
- **[deliberation] ip_checksum: utility LOW_VALUE → USED**: Deliberation reclassified: Same reasoning as icmp_checksum. overengineering reclassified OVER → ACCEPTABLE: the named wrapper ip_checksum communicates that this checksum is specifically for the IP header, improving call-site readability in the example. utility reclassified LOW_VALUE → USED: actively called and provides clear semantic value. tests NONE factual. documentation UNDOCUMENTED kept: no doc comment present, private helper, name is self-descriptive. The two wrappers together (icmp_checksum + ip_checksum) form a clean naming pattern that makes the main function's packet manipulation logic easier to follow.


## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "XDP_WG_OBJ",
      "line_start": 14,
      "line_end": 14,
      "utility": "USED",
      "confidence": 95,
      "detail": "Passed to parse_and_patch_elf at L68; included as pre-compiled BPF bytecode"
    },
    {
      "name": "BPF_MAP_CREATE",
      "line_start": 17,
      "line_end": 17,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in bpf_create_xskmap syscall at L109 as command argument"
    },
    {
      "name": "BPF_PROG_LOAD",
      "line_start": 18,
      "line_end": 18,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in bpf_prog_load syscall at L157 as command argument"
    },
    {
      "name": "BPF_MAP_UPDATE_ELEM",
      "line_start": 19,
      "line_end": 19,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in bpf_map_update syscall at L197 as command argument"
    },
    {
      "name": "BPF_MAP_TYPE_XSKMAP",
      "line_start": 22,
      "line_end": 22,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in BpfAttrMapCreate at L105 to specify map type"
    },
    {
      "name": "BPF_PROG_TYPE_XDP",
      "line_start": 25,
      "line_end": 25,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in BpfAttrProgLoad at L149 to specify program type"
    },
    {
      "name": "XDP_FLAGS_SKB_MODE",
      "line_start": 28,
      "line_end": 28,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in attach_xdp_netlink at L333 for XDP flags netlink attribute"
    },
    {
      "name": "ET_REL",
      "line_start": 31,
      "line_end": 31,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in parse_and_patch_elf at L390 to validate ELF type"
    },
    {
      "name": "SHT_PROGBITS",
      "line_start": 32,
      "line_end": 32,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in parse_and_patch_elf at L424 to identify program section"
    },
    {
      "name": "SHT_REL",
      "line_start": 33,
      "line_end": 33,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in parse_and_patch_elf at L448 to identify relocation sections"
    },
    {
      "name": "EM_BPF",
      "line_start": 34,
      "line_end": 34,
      "utility": "USED",
      "confidence": 95,
      "detail": "Used in parse_and_patch_elf at L391 to validate ELF machine type"
    },
    {
      "name": "XdpProgram",
      "line_start": 37,
      "line_end": 41,
      "utility": "DEAD",
      "confidence": 60,
      "detail": "Exported pub struct with 0 runtime importers per pre-computed analysis. Matches known false-positive pattern for library crate exports (cross-crate imports not visible in analysis scope), but no contradiction in provided context. Confidence lowered due to systematic pattern of missing workspace-level imports in earlier evaluations."
    },
    {
      "name": "bpf_create_xskmap",
      "line_start": 93,
      "line_end": 122,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in load_and_attach at L63 to create XSKMAP before loading program"
    },
    {
      "name": "bpf_prog_load",
      "line_start": 124,
      "line_end": 175,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in load_and_attach at L70 to load BPF bytecode into kernel"
    },
    {
      "name": "bpf_map_update",
      "line_start": 177,
      "line_end": 206,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in register_xsk at L90 to add AF_XDP socket fd to XSKMAP"
    },
    {
      "name": "attach_xdp",
      "line_start": 210,
      "line_end": 233,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in load_and_attach at L74 to attach program to interface"
    },
    {
      "name": "attach_xdp_netlink",
      "line_start": 235,
      "line_end": 342,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in attach_xdp at L231 and in detach_xdp at L345; core netlink-based XDP attach/detach implementation"
    },
    {
      "name": "detach_xdp",
      "line_start": 344,
      "line_end": 346,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in Drop impl for XdpProgram at L80 to clean up XDP attachment"
    },
    {
      "name": "ifindex_to_name",
      "line_start": 348,
      "line_end": 357,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in attach_xdp at L224 to resolve interface index to name for ip command"
    },
    {
      "name": "parse_and_patch_elf",
      "line_start": 361,
      "line_end": 470,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in load_and_attach at L68 to parse ELF, extract bytecode, and patch map references"
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.0746 | **Duration:** 80.8s


---

# Axis: duplication

## User

## File: `rustguard-tun/src/bpf_loader.rs`
## Language: rust

```rust
//! Minimal BPF ELF loader for the XDP WireGuard filter.
//!
//! Loads the pre-compiled xdp_wg.o, creates the XSKMAP,
//! patches the map fd into the program, loads it, and attaches to an interface.
//!
//! No libbpf. No aya. Just raw bpf() syscalls and ELF parsing.
//! The .o is 1.4KB and never changes.

#![cfg(target_os = "linux")]

use std::io;

/// The pre-compiled BPF object.
const XDP_WG_OBJ: &[u8] = include_bytes!("../bpf/xdp_wg.o");

// BPF syscall commands.
const BPF_MAP_CREATE: u32 = 0;
const BPF_PROG_LOAD: u32 = 5;
const BPF_MAP_UPDATE_ELEM: u32 = 2;

// BPF map types.
const BPF_MAP_TYPE_XSKMAP: u32 = 17;

// BPF program types.
const BPF_PROG_TYPE_XDP: u32 = 6;

// XDP attach flags.
const XDP_FLAGS_SKB_MODE: u32 = 1 << 1;

// ELF constants.
const ET_REL: u16 = 1;
const SHT_PROGBITS: u32 = 1;
const SHT_REL: u32 = 9;
const EM_BPF: u16 = 247;

/// A loaded XDP program with its XSKMAP.
pub struct XdpProgram {
    pub prog_fd: i32,
    pub xsks_map_fd: i32,
    ifindex: u32,
}

impl XdpProgram {
    /// Load the XDP WireGuard filter and attach it to an interface.
    pub fn load_and_attach(ifname: &str) -> io::Result<Self> {
        let ifindex = super::xdp::if_nametoindex(ifname)?;

        // 1. Create the XSKMAP.
        let xsks_map_fd = bpf_create_xskmap(64)?;

        // 2. Parse ELF, extract program bytecode, patch map references.
        let insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;
        eprintln!("  BPF: parsed ELF, {} insn bytes, map_fd={}", insns.len(), xsks_map_fd);

        // 3. Load BPF program.
        let prog_fd = bpf_prog_load(&insns).map_err(|e| {
            io::Error::new(e.kind(), format!("prog_load ({} insns): {e}", insns.len() / 8))
        })?;
        eprintln!("  BPF: program loaded, prog_fd={}", prog_fd);

        // 4. Attach to interface.
        attach_xdp(ifindex, prog_fd).map_err(|e| {
            io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
        })?;
        eprintln!("  BPF: attached to ifindex={}", ifindex);

        Ok(Self {
            prog_fd,
            xsks_map_fd,
            ifindex,
        })
    }

    /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
    pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
        bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
    }
}

impl Drop for XdpProgram {
    fn drop(&mut self) {
        // Detach XDP program from interface.
        let _ = detach_xdp(self.ifindex);
        unsafe {
            libc::close(self.prog_fd);
            libc::close(self.xsks_map_fd);
        }
    }
}

// ── BPF syscalls ────────────────────────────────────────────────────

fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
    #[repr(C)]
    struct BpfAttrMapCreate {
        map_type: u32,
        key_size: u32,
        value_size: u32,
        max_entries: u32,
    }

    let attr = BpfAttrMapCreate {
        map_type: BPF_MAP_TYPE_XSKMAP,
        key_size: 4,
        value_size: 4,
        max_entries,
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_CREATE,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapCreate>(),
        )
    } as i32;

    if fd < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(fd)
}

fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
    let license = b"GPL\0";
    let mut log_buf = vec![0u8; 65536];

    // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
    // We need at least the fields up to kern_version, padded to the
    // minimum size the kernel accepts. Using 256 bytes to be safe.
    #[repr(C)]
    struct BpfAttrProgLoad {
        prog_type: u32,
        insn_cnt: u32,
        insns: u64,
        license: u64,
        log_level: u32,
        log_size: u32,
        log_buf: u64,
        kern_version: u32,
        _pad: [u8; 208], // Pad to 256 bytes total.
    }

    let attr = BpfAttrProgLoad {
        prog_type: BPF_PROG_TYPE_XDP,
        insn_cnt: (insns.len() / 8) as u32,
        insns: insns.as_ptr() as u64,
        license: license.as_ptr() as u64,
        log_level: 1,
        log_size: log_buf.len() as u32,
        log_buf: log_buf.as_mut_ptr() as u64,
        kern_version: 0,
        _pad: [0; 208],
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_PROG_LOAD,
            &attr as *const _,
            std::mem::size_of::<BpfAttrProgLoad>(),
        )
    } as i32;

    if fd < 0 {
        let err = io::Error::last_os_error();
        let log_end = log_buf.iter().position(|&b| b == 0).unwrap_or(0);
        if log_end > 0 {
            let log_str = String::from_utf8_lossy(&log_buf[..log_end]);
            eprintln!("  BPF verifier log:\n{log_str}");
        }
        return Err(err);
    }
    Ok(fd)
}

fn bpf_map_update(map_fd: i32, key: &u32, value: &i32) -> io::Result<()> {
    #[repr(C)]
    struct BpfAttrMapElem {
        map_fd: u32,
        key: u64,
        value: u64,
        flags: u64,
    }

    let attr = BpfAttrMapElem {
        map_fd: map_fd as u32,
        key: key as *const _ as u64,
        value: value as *const _ as u64,
        flags: 0, // BPF_ANY
    };

    let ret = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_UPDATE_ELEM,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapElem>(),
        )
    };

    if ret < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(())
}

// ── XDP attach/detach via netlink ───────────────────────────────────

fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Use the SIOCETHTOOL / netlink approach, but the simplest way
    // that works on all kernels is via the if_link netlink interface.
    // For simplicity, shell out to `ip link set dev <name> xdpgeneric obj ...`
    // But we already have the fd. Use the bpf_set_link_xdp_fd equivalent.

    // Direct approach: set XDP fd via netlink RTM_SETLINK.
    // This is complex. Simpler: use the deprecated but working SIOCSIFXDP.
    // Actually simplest: use libc's if_nametoindex + raw netlink.

    // For now, use the ip command — it works and avoids 200 lines of netlink.
    let ifname = ifindex_to_name(ifindex)?;
    let fd_path = format!("/proc/self/fd/{prog_fd}");
    let output = std::process::Command::new("ip")
        .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
        .output();

    // Fallback: use the bpf netlink API via raw netlink socket.
    // If ip command doesn't support pinned fd, try bpf_xdp_attach.
    match output {
        Ok(out) if out.status.success() => Ok(()),
        _ => attach_xdp_netlink(ifindex, prog_fd),
    }
}

fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Netlink RTM_SETLINK with IFLA_XDP attribute.
    // This is the proper way — no shelling out.
    use std::os::unix::io::FromRawFd;

    let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
    if sock < 0 {
        return Err(io::Error::last_os_error());
    }

    // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
    const RTM_SETLINK: u16 = 19;
    const NLM_F_REQUEST: u16 = 1;
    const NLM_F_ACK: u16 = 4;
    const IFLA_XDP: u16 = 43;
    const IFLA_XDP_FD: u16 = 1;
    const IFLA_XDP_FLAGS: u16 = 3;

    // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
    let mut buf = [0u8; 64];
    let mut off = 0;

    // nlmsghdr
    let total_len: u32 = 52;
    buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
    off += 4;
    buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
    off += 2;
    off += 8; // seq + pid = 0

    // ifinfomsg
    buf[off] = libc::AF_UNSPEC as u8; // family
    off += 4; // family + pad + type
    let idx_bytes = ifindex.to_ne_bytes();
    buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
    off += 4;
    off += 8; // flags + change

    // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
    let xdp_nla_len: u16 = 20;
    buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
    off += 2;
    let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
    buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
    off += 2;

    // IFLA_XDP_FD (len=8, type=1, value=prog_fd)
    let fd_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&fd_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FD.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&prog_fd.to_ne_bytes());
    off += 4;

    // IFLA_XDP_FLAGS (len=8, type=3, value=XDP_FLAGS_SKB_MODE)
    let flags_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&flags_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FLAGS.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&XDP_FLAGS_SKB_MODE.to_ne_bytes());

    let mut sa: libc::sockaddr_nl = unsafe { std::mem::zeroed() };
    sa.nl_family = libc::AF_NETLINK as u16;

    let ret = unsafe {
        libc::sendto(
            sock,
            buf.as_ptr() as *const _,
            total_len as usize,
            0,
            &sa as *const _ as *const _,
            std::mem::size_of::<libc::sockaddr_nl>() as u32,
        )
    };

    if ret < 0 {
        let err = io::Error::last_os_error();
        unsafe { libc::close(sock) };
        return Err(err);
    }

    // Read ACK.
    let mut resp = [0u8; 128];
    let n = unsafe { libc::recv(sock, resp.as_mut_ptr() as *mut _, resp.len(), 0) };
    unsafe { libc::close(sock) };

    if n < 0 {
        return Err(io::Error::last_os_error());
    }

    // Check for error in nlmsghdr.
    if n >= 16 {
        let nlmsg_type = u16::from_ne_bytes([resp[4], resp[5]]);
        if nlmsg_type == 2 {
            // NLMSG_ERROR
            let error = i32::from_ne_bytes([resp[16], resp[17], resp[18], resp[19]]);
            if error < 0 {
                return Err(io::Error::from_raw_os_error(-error));
            }
        }
    }

    Ok(())
}

fn detach_xdp(ifindex: u32) -> io::Result<()> {
    attach_xdp_netlink(ifindex, -1) // fd=-1 detaches
}

fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
    let mut buf = [0u8; 16];
    let ret = unsafe { libc::if_indextoname(ifindex, buf.as_mut_ptr() as *mut _) };
    if ret.is_null() {
        return Err(io::Error::last_os_error());
    }
    let end = buf.iter().position(|&b| b == 0).unwrap_or(16);
    String::from_utf8(buf[..end].to_vec())
        .map_err(|_| io::Error::new(io::ErrorKind::InvalidData, "invalid ifname"))
}

// ── Minimal ELF parser ──────────────────────────────────────────────

fn parse_and_patch_elf(elf: &[u8], map_fd: i32) -> io::Result<Vec<u8>> {
    if elf.len() < 64 || &elf[0..4] != b"\x7fELF" {
        return Err(io::Error::new(io::ErrorKind::InvalidData, "not an ELF"));
    }

    let e_type = u16::from_le_bytes([elf[16], elf[17]]);
    let e_machine = u16::from_le_bytes([elf[18], elf[19]]);
    if e_type != ET_REL || e_machine != EM_BPF {
        return Err(io::Error::new(
            io::ErrorKind::InvalidData,
            "not a BPF relocatable ELF",
        ));
    }

    let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;
    let e_shentsize = u16::from_le_bytes([elf[58], elf[59]]) as usize;
    let e_shnum = u16::from_le_bytes([elf[60], elf[61]]) as usize;
    let e_shstrndx = u16::from_le_bytes([elf[62], elf[63]]) as usize;

    let shstrtab_off = {
        let sh = &elf[e_shoff + e_shstrndx * e_shentsize..];
        u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize
    };

    // Collect section info.
    struct Section {
        name: String,
        sh_type: u32,
        offset: usize,
        size: usize,
        info: u32, // sh_info — for REL sections, points to the target section.
    }

    let mut sections = Vec::new();
    for i in 0..e_shnum {
        let sh = &elf[e_shoff + i * e_shentsize..];
        let sh_name_off = u32::from_le_bytes(sh[0..4].try_into().unwrap()) as usize;
        let sh_type = u32::from_le_bytes(sh[4..8].try_into().unwrap());
        let sh_offset = u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize;
        let sh_size = u64::from_le_bytes(sh[32..40].try_into().unwrap()) as usize;
        let sh_info = u32::from_le_bytes(sh[28..32].try_into().unwrap());
        // Wait, sh_info is at offset 44 in Elf64_Shdr. Let me recalculate.
        // Elf64_Shdr: name(4) type(4) flags(8) addr(8) offset(8) size(8) link(4) info(4) addralign(8) entsize(8)
        let sh_info = u32::from_le_bytes(sh[44..48].try_into().unwrap());

        let name_start = shstrtab_off + sh_name_off;
        let name_end = elf[name_start..]
            .iter()
            .position(|&b| b == 0)
            .unwrap_or(0)
            + name_start;
        let name = std::str::from_utf8(&elf[name_start..name_end])
            .unwrap_or("")
            .to_string();

        sections.push(Section {
            name,
            sh_type,
            offset: sh_offset,
            size: sh_size,
            info: sh_info,
        });
    }

    // Find the "xdp" program section.
    let prog_idx = sections
        .iter()
        .position(|s| s.name == "xdp" && s.sh_type == SHT_PROGBITS)
        .ok_or_else(|| {
            io::Error::new(io::ErrorKind::InvalidData, "no 'xdp' section")
        })?;
    let prog = &sections[prog_idx];
    let mut insns = elf[prog.offset..prog.offset + prog.size].to_vec();

    // Find relocation sections targeting the program section.
    // These tell us which LD_IMM64 instructions reference maps.
    for sec in &sections {
        if sec.sh_type != SHT_REL {
            continue;
        }
        if sec.info as usize != prog_idx {
            continue; // This relocation targets a different section.
        }

        // Process each relocation entry (Elf64_Rel: offset(8) + info(8) = 16 bytes).
        let rel_data = &elf[sec.offset..sec.offset + sec.size];
        let mut off = 0;
        while off + 16 <= rel_data.len() {
            let r_offset = u64::from_le_bytes(rel_data[off..off + 8].try_into().unwrap()) as usize;
            // r_info contains symbol index in upper 32 bits, type in lower 32.
            // For BPF map refs, the type is R_BPF_64_64 (1).

            // At r_offset in the instruction stream, set src_reg=1 (BPF_PSEUDO_MAP_FD)
            // and imm=map_fd.
            if r_offset + 16 <= insns.len() && insns[r_offset] == 0x18 {
                // Set src_reg to BPF_PSEUDO_MAP_FD (1).
                insns[r_offset + 1] = (insns[r_offset + 1] & 0x0f) | (1 << 4);
                // Set imm to map fd.
                insns[r_offset + 4..r_offset + 8]
                    .copy_from_slice(&(map_fd as u32).to_le_bytes());
                // Zero upper 32 bits of the 64-bit immediate.
                insns[r_offset + 12..r_offset + 16].copy_from_slice(&0u32.to_le_bytes());
            }

            off += 16;
        }
    }

    Ok(insns)
}

```

## Symbols to evaluate

- constant XDP_WG_OBJ (L14–L14)
- constant BPF_MAP_CREATE (L17–L17)
- constant BPF_PROG_LOAD (L18–L18)
- constant BPF_MAP_UPDATE_ELEM (L19–L19)
- constant BPF_MAP_TYPE_XSKMAP (L22–L22)
- constant BPF_PROG_TYPE_XDP (L25–L25)
- constant XDP_FLAGS_SKB_MODE (L28–L28)
- constant ET_REL (L31–L31)
- constant SHT_PROGBITS (L32–L32)
- constant SHT_REL (L33–L33)
- constant EM_BPF (L34–L34)
- export class XdpProgram (L37–L41)
- function bpf_create_xskmap (L93–L122)
- function bpf_prog_load (L124–L175)
- function bpf_map_update (L177–L206)
- function attach_xdp (L210–L233)
- function attach_xdp_netlink (L235–L342)
- function detach_xdp (L344–L346)
- function ifindex_to_name (L348–L357)
- function parse_and_patch_elf (L361–L470)

## RAG — Semantic Duplication

### bpf_create_xskmap (L93–L122)
No similar functions found. Mark UNIQUE.

### bpf_prog_load (L124–L175)
No similar functions found. Mark UNIQUE.

### bpf_map_update (L177–L206)
No similar functions found. Mark UNIQUE.

### attach_xdp (L210–L233)
Similar functions found:
- **attach_xdp_netlink** in `rustguard-tun/src/bpf_loader.rs` (score: 0.841)
  Signature: fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
  Complexity: 1/5
  Source:
  ```rust
          _ => attach_xdp_netlink(ifindex, prog_fd),
      }
  }
  
  fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
      // Netlink RTM_SETLINK with IFLA_XDP attribute.
      // This is the proper way — no shelling out.
      use std::os::unix::io::FromRawFd;
  
      let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
      if sock < 0 {
          return Err(io::Error::last_os_error());
      }
  
      // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
      const RTM_SETLINK: u16 = 19;
      const NLM_F_REQUEST: u16 = 1;
      const NLM_F_ACK: u16 = 4;
      const IFLA_XDP: u16 = 43;
      const IFLA_XDP_FD: u16 = 1;
      const IFLA_XDP_FLAGS: u16 = 3;
  
      // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
      let mut buf = [0u8; 64];
      let mut off = 0;
  
      // nlmsghdr
      let total_len: u32 = 52;
      buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
      off += 4;
      buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
      off += 2;
      buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
      off += 2;
      off += 8; // seq + pid = 0
  
      // ifinfomsg
      buf[off] = libc::AF_UNSPEC as u8; // family
      off += 4; // family + pad + type
      let idx_bytes = ifindex.to_ne_bytes();
      buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
      off += 4;
      off += 8; // flags + change
  
      // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
      let xdp_nla_len: u16 = 20;
      buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
      off += 2;
      let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
      buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
  ```
- **detach_xdp** in `rustguard-tun/src/bpf_loader.rs` (score: 0.745)
  Signature: fn detach_xdp(ifindex: u32) -> io::Result<()> {
  Complexity: 1/5
  Calls: attach_xdp_netlink
  Source:
  ```rust
          let _ = detach_xdp(self.ifindex);
          unsafe {
              libc::close(self.prog_fd);
              libc::close(self.xsks_map_fd);
          }
      }
  }
  
  // ── BPF syscalls ────────────────────────────────────────────────────
  
  fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
      #[repr(C)]
      struct BpfAttrMapCreate {
          map_type: u32,
          key_size: u32,
          value_size: u32,
          max_entries: u32,
      }
  
      let attr = BpfAttrMapCreate {
          map_type: BPF_MAP_TYPE_XSKMAP,
          key_size: 4,
          value_size: 4,
          max_entries,
      };
  
      let fd = unsafe {
          libc::syscall(
              libc::SYS_bpf,
              BPF_MAP_CREATE,
              &attr as *const _,
              std::mem::size_of::<BpfAttrMapCreate>(),
          )
      } as i32;
  
      if fd < 0 {
          return Err(io::Error::last_os_error());
      }
      Ok(fd)
  }
  
  fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
      let license = b"GPL\0";
      let mut log_buf = vec![0u8; 65536];
  
      // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
      // We need at least the fields up to kern_version, padded to the
      // minimum size the kernel accepts. Using 256 bytes to be safe.
      #[repr(C)]
      struct BpfAttrProgLoad {
  ```

### attach_xdp_netlink (L235–L342)
Similar functions found:
- **attach_xdp** in `rustguard-tun/src/bpf_loader.rs` (score: 0.841)
  Signature: fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
  Complexity: 1/5
  Calls: attach_xdp_netlink, ifindex_to_name
  Source:
  ```rust
          attach_xdp(ifindex, prog_fd).map_err(|e| {
              io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
          })?;
          eprintln!("  BPF: attached to ifindex={}", ifindex);
  
          Ok(Self {
              prog_fd,
              xsks_map_fd,
              ifindex,
          })
      }
  
      /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
      pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
          bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
      }
  }
  
  impl Drop for XdpProgram {
      fn drop(&mut self) {
          // Detach XDP program from interface.
          let _ = detach_xdp(self.ifindex);
          unsafe {
              libc::close(self.prog_fd);
              libc::close(self.xsks_map_fd);
          }
      }
  }
  
  // ── BPF syscalls ────────────────────────────────────────────────────
  
  fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
      #[repr(C)]
      struct BpfAttrMapCreate {
          map_type: u32,
          key_size: u32,
          value_size: u32,
          max_entries: u32,
      }
  
      let attr = BpfAttrMapCreate {
          map_type: BPF_MAP_TYPE_XSKMAP,
          key_size: 4,
          value_size: 4,
          max_entries,
      };
  
      let fd = unsafe {
          libc::syscall(
              libc::SYS_bpf,
  ```
- **detach_xdp** in `rustguard-tun/src/bpf_loader.rs` (score: 0.716)
  Signature: fn detach_xdp(ifindex: u32) -> io::Result<()> {
  Complexity: 1/5
  Calls: attach_xdp_netlink
  Source:
  ```rust
          let _ = detach_xdp(self.ifindex);
          unsafe {
              libc::close(self.prog_fd);
              libc::close(self.xsks_map_fd);
          }
      }
  }
  
  // ── BPF syscalls ────────────────────────────────────────────────────
  
  fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
      #[repr(C)]
      struct BpfAttrMapCreate {
          map_type: u32,
          key_size: u32,
          value_size: u32,
          max_entries: u32,
      }
  
      let attr = BpfAttrMapCreate {
          map_type: BPF_MAP_TYPE_XSKMAP,
          key_size: 4,
          value_size: 4,
          max_entries,
      };
  
      let fd = unsafe {
          libc::syscall(
              libc::SYS_bpf,
              BPF_MAP_CREATE,
              &attr as *const _,
              std::mem::size_of::<BpfAttrMapCreate>(),
          )
      } as i32;
  
      if fd < 0 {
          return Err(io::Error::last_os_error());
      }
      Ok(fd)
  }
  
  fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
      let license = b"GPL\0";
      let mut log_buf = vec![0u8; 65536];
  
      // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
      // We need at least the fields up to kern_version, padded to the
      // minimum size the kernel accepts. Using 256 bytes to be safe.
      #[repr(C)]
      struct BpfAttrProgLoad {
  ```

### detach_xdp (L344–L346)
Trivial function (≤ 2 lines). Mark UNIQUE.

### ifindex_to_name (L348–L357)
Similar functions found:
- **if_nametoindex** in `rustguard-tun/src/xdp.rs` (score: 0.779)
  Signature: pub fn if_nametoindex(name: &str) -> io::Result<u32> {
  Complexity: 1/5
  Source:
  ```rust
          let ifindex = if_nametoindex(&config.ifname)?;
          let sxdp = SockaddrXdp {
              family: AF_XDP as u16,
              flags: XDP_USE_NEED_WAKEUP,
              ifindex,
              queue_id: config.queue_id,
              shared_umem_fd: 0,
          };
          if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
              let err = io::Error::last_os_error();
              libc::close(fd);
              libc::munmap(umem as *mut _, umem_size);
              return Err(err);
          }
  
          Ok(Self {
              fd,
              umem,
              umem_size,
              frame_size: config.frame_size,
              rx_ring,
              tx_ring,
              fill_ring,
              comp_ring,
              ring_size: config.ring_size,
              free_frames,
          })
      }
  
      /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
      /// Caller must call `rx_release` after processing to return frames to the fill ring.
      pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
          let prod = self.rx_ring.producer();
          let cons = self.rx_ring.consumer();
          let available = prod.wrapping_sub(cons);
  
          let mut packets = Vec::with_capacity(available as usize);
          for i in 0..available {
              let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
              let desc = unsafe {
                  &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
              };
              let data = unsafe {
                  std::slice::from_raw_parts(
                      self.umem.add(desc.addr as usize),
                      desc.len as usize,
                  )
              };
              packets.push((desc.addr, data));
          }
  ```

### parse_and_patch_elf (L361–L470)
No similar functions found. Mark UNIQUE.

Evaluate the duplication of each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] random_nonce: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: Duplication DUPLICATE reclassified → UNIQUE: the duplicate target is in `rustguard-enroll/src/protocol.rs`, a completely separate crate. These are trivial 4-line functions that fill a buffer with random bytes. Deduplicating them would require creating a shared utility crate or adding a cross-crate dependency for 4 lines of code — this would increase coupling disproportionately. Two crates independently needing to generate random nonces is normal, not a code smell. Tests WEAK and documentation UNDOCUMENTED kept.
- **[deliberation] mix_key: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: Duplication reclassified DUPLICATE → UNIQUE: rustguard-core (userspace, x25519-dalek types, SharedSecret wrapper, alloc-based) and rustguard-kmod (kernel module, raw byte arrays, C FFI) are fundamentally different compilation targets. Sharing code between a userspace Rust crate and a kernel module is architecturally infeasible — the type differences (SharedSecret vs raw bytes) reflect different abstraction layers, not accidental duplication. The parallel implementations are intentional. Tests WEAK and docs PARTIAL stand as originally assessed.
- **[deliberation] compute_mac1: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: Duplication reclassified DUPLICATE → UNIQUE for the same reason as mix_key: rustguard-core and rustguard-kmod target fundamentally different environments (userspace vs kernel). The parameter type difference (PublicKey wrapper vs raw [u8; 32]) is not superficial — it reflects different type systems mandated by the compilation target. Cross-crate deduplication into a shared dependency would force the kernel module to depend on x25519-dalek types, which is architecturally wrong. Tests WEAK valid — no known-vector test for the 16-byte output. Documentation PARTIAL correct — public function lacks #Parameters/#Returns sections.
- **[deliberation] elapsed_since: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: This is the no_std variant at L26-30. Correction NEEDS_FIX: the concern is legitimate — returning Duration::ZERO makes all time-based query methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake, handshake_timed_out) silently non-functional in no_std builds. While the inline comment acknowledges the design, no _at() query variants exist for downstream kernel callers, leaving the no_std time path incomplete. Keep NEEDS_FIX. Duplication: two private cfg-gated stubs returning Duration::ZERO in different modules is a standard Rust no_std pattern, not meaningful duplication. Extracting to a shared module would create unnecessary coupling → reclassify to UNIQUE. Tests NONE: correct, no_std path is never compiled in tests. Documentation UNDOCUMENTED: fair for a private function, inline comments partially explain intent.


## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "XDP_WG_OBJ",
      "line_start": 14,
      "line_end": 14,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Constant holding pre-compiled BPF object bytecode; no similar symbols found in RAG results",
      "duplicate_target": null
    },
    {
      "name": "BPF_MAP_CREATE",
      "line_start": 17,
      "line_end": 17,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Constant defining BPF syscall command; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "BPF_PROG_LOAD",
      "line_start": 18,
      "line_end": 18,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Constant defining BPF syscall command; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "BPF_MAP_UPDATE_ELEM",
      "line_start": 19,
      "line_end": 19,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Constant defining BPF syscall command; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "BPF_MAP_TYPE_XSKMAP",
      "line_start": 22,
      "line_end": 22,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Constant defining BPF map type; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "BPF_PROG_TYPE_XDP",
      "line_start": 25,
      "line_end": 25,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Constant defining BPF program type; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "XDP_FLAGS_SKB_MODE",
      "line_start": 28,
      "line_end": 28,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Constant defining XDP attach flag; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "ET_REL",
      "line_start": 31,
      "line_end": 31,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "ELF constant for relocatable file type; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "SHT_PROGBITS",
      "line_start": 32,
      "line_end": 32,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "ELF constant for section type; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "SHT_REL",
      "line_start": 33,
      "line_end": 33,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "ELF constant for relocation section type; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "EM_BPF",
      "line_start": 34,
      "line_end": 34,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "ELF constant for BPF machine type; no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "XdpProgram",
      "line_start": 37,
      "line_end": 41,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Struct wrapping loaded XDP program with fd pair; no similar types found",
      "duplicate_target": null
    },
    {
      "name": "bpf_create_xskmap",
      "line_start": 93,
      "line_end": 122,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Creates BPF XSKMAP via syscall; no similar functions found in RAG results",
      "duplicate_target": null
    },
    {
      "name": "bpf_prog_load",
      "line_start": 124,
      "line_end": 175,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Loads BPF program bytecode via syscall; no similar functions found",
      "duplicate_target": null
    },
    {
      "name": "bpf_map_update",
      "line_start": 177,
      "line_end": 206,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Updates BPF map element via syscall; no similar functions found",
      "duplicate_target": null
    },
    {
      "name": "attach_xdp",
      "line_start": 210,
      "line_end": 233,
      "duplication": "UNIQUE",
      "confidence": 85,
      "detail": "Wrapper that tries ip command first, then falls back to netlink. RAG score 0.841 with attach_xdp_netlink, but different semantic contracts: wrapper with fallback vs low-level implementation. Not interchangeable despite high similarity score.",
      "duplicate_target": null
    },
    {
      "name": "attach_xdp_netlink",
      "line_start": 235,
      "line_end": 342,
      "duplication": "UNIQUE",
      "confidence": 85,
      "detail": "Low-level netlink socket implementation for XDP attachment. RAG score 0.841 with attach_xdp, but attach_xdp calls this as fallback; different logical contracts and purposes. Not interchangeable.",
      "duplicate_target": null
    },
    {
      "name": "detach_xdp",
      "line_start": 344,
      "line_end": 346,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Trivial 1-line wrapper calling attach_xdp_netlink with -1 to detach; thin wrapper with distinct semantics",
      "duplicate_target": null
    },
    {
      "name": "ifindex_to_name",
      "line_start": 348,
      "line_end": 357,
      "duplication": "UNIQUE",
      "confidence": 85,
      "detail": "Converts interface index to name via libc. RAG score 0.779 with if_nametoindex from xdp.rs, but that function performs reverse operation (name→index). Opposite logic despite domain similarity.",
      "duplicate_target": null
    },
    {
      "name": "parse_and_patch_elf",
      "line_start": 361,
      "line_end": 470,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Parses ELF binary, extracts BPF program section, patches map fd references; no similar functions found",
      "duplicate_target": null
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.0719 | **Duration:** 70.5s


---

# Axis: correction

## User

## File: `rustguard-tun/src/bpf_loader.rs`
## Language: rust

```rust
//! Minimal BPF ELF loader for the XDP WireGuard filter.
//!
//! Loads the pre-compiled xdp_wg.o, creates the XSKMAP,
//! patches the map fd into the program, loads it, and attaches to an interface.
//!
//! No libbpf. No aya. Just raw bpf() syscalls and ELF parsing.
//! The .o is 1.4KB and never changes.

#![cfg(target_os = "linux")]

use std::io;

/// The pre-compiled BPF object.
const XDP_WG_OBJ: &[u8] = include_bytes!("../bpf/xdp_wg.o");

// BPF syscall commands.
const BPF_MAP_CREATE: u32 = 0;
const BPF_PROG_LOAD: u32 = 5;
const BPF_MAP_UPDATE_ELEM: u32 = 2;

// BPF map types.
const BPF_MAP_TYPE_XSKMAP: u32 = 17;

// BPF program types.
const BPF_PROG_TYPE_XDP: u32 = 6;

// XDP attach flags.
const XDP_FLAGS_SKB_MODE: u32 = 1 << 1;

// ELF constants.
const ET_REL: u16 = 1;
const SHT_PROGBITS: u32 = 1;
const SHT_REL: u32 = 9;
const EM_BPF: u16 = 247;

/// A loaded XDP program with its XSKMAP.
pub struct XdpProgram {
    pub prog_fd: i32,
    pub xsks_map_fd: i32,
    ifindex: u32,
}

impl XdpProgram {
    /// Load the XDP WireGuard filter and attach it to an interface.
    pub fn load_and_attach(ifname: &str) -> io::Result<Self> {
        let ifindex = super::xdp::if_nametoindex(ifname)?;

        // 1. Create the XSKMAP.
        let xsks_map_fd = bpf_create_xskmap(64)?;

        // 2. Parse ELF, extract program bytecode, patch map references.
        let insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;
        eprintln!("  BPF: parsed ELF, {} insn bytes, map_fd={}", insns.len(), xsks_map_fd);

        // 3. Load BPF program.
        let prog_fd = bpf_prog_load(&insns).map_err(|e| {
            io::Error::new(e.kind(), format!("prog_load ({} insns): {e}", insns.len() / 8))
        })?;
        eprintln!("  BPF: program loaded, prog_fd={}", prog_fd);

        // 4. Attach to interface.
        attach_xdp(ifindex, prog_fd).map_err(|e| {
            io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
        })?;
        eprintln!("  BPF: attached to ifindex={}", ifindex);

        Ok(Self {
            prog_fd,
            xsks_map_fd,
            ifindex,
        })
    }

    /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
    pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
        bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
    }
}

impl Drop for XdpProgram {
    fn drop(&mut self) {
        // Detach XDP program from interface.
        let _ = detach_xdp(self.ifindex);
        unsafe {
            libc::close(self.prog_fd);
            libc::close(self.xsks_map_fd);
        }
    }
}

// ── BPF syscalls ────────────────────────────────────────────────────

fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
    #[repr(C)]
    struct BpfAttrMapCreate {
        map_type: u32,
        key_size: u32,
        value_size: u32,
        max_entries: u32,
    }

    let attr = BpfAttrMapCreate {
        map_type: BPF_MAP_TYPE_XSKMAP,
        key_size: 4,
        value_size: 4,
        max_entries,
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_CREATE,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapCreate>(),
        )
    } as i32;

    if fd < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(fd)
}

fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
    let license = b"GPL\0";
    let mut log_buf = vec![0u8; 65536];

    // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
    // We need at least the fields up to kern_version, padded to the
    // minimum size the kernel accepts. Using 256 bytes to be safe.
    #[repr(C)]
    struct BpfAttrProgLoad {
        prog_type: u32,
        insn_cnt: u32,
        insns: u64,
        license: u64,
        log_level: u32,
        log_size: u32,
        log_buf: u64,
        kern_version: u32,
        _pad: [u8; 208], // Pad to 256 bytes total.
    }

    let attr = BpfAttrProgLoad {
        prog_type: BPF_PROG_TYPE_XDP,
        insn_cnt: (insns.len() / 8) as u32,
        insns: insns.as_ptr() as u64,
        license: license.as_ptr() as u64,
        log_level: 1,
        log_size: log_buf.len() as u32,
        log_buf: log_buf.as_mut_ptr() as u64,
        kern_version: 0,
        _pad: [0; 208],
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_PROG_LOAD,
            &attr as *const _,
            std::mem::size_of::<BpfAttrProgLoad>(),
        )
    } as i32;

    if fd < 0 {
        let err = io::Error::last_os_error();
        let log_end = log_buf.iter().position(|&b| b == 0).unwrap_or(0);
        if log_end > 0 {
            let log_str = String::from_utf8_lossy(&log_buf[..log_end]);
            eprintln!("  BPF verifier log:\n{log_str}");
        }
        return Err(err);
    }
    Ok(fd)
}

fn bpf_map_update(map_fd: i32, key: &u32, value: &i32) -> io::Result<()> {
    #[repr(C)]
    struct BpfAttrMapElem {
        map_fd: u32,
        key: u64,
        value: u64,
        flags: u64,
    }

    let attr = BpfAttrMapElem {
        map_fd: map_fd as u32,
        key: key as *const _ as u64,
        value: value as *const _ as u64,
        flags: 0, // BPF_ANY
    };

    let ret = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_UPDATE_ELEM,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapElem>(),
        )
    };

    if ret < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(())
}

// ── XDP attach/detach via netlink ───────────────────────────────────

fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Use the SIOCETHTOOL / netlink approach, but the simplest way
    // that works on all kernels is via the if_link netlink interface.
    // For simplicity, shell out to `ip link set dev <name> xdpgeneric obj ...`
    // But we already have the fd. Use the bpf_set_link_xdp_fd equivalent.

    // Direct approach: set XDP fd via netlink RTM_SETLINK.
    // This is complex. Simpler: use the deprecated but working SIOCSIFXDP.
    // Actually simplest: use libc's if_nametoindex + raw netlink.

    // For now, use the ip command — it works and avoids 200 lines of netlink.
    let ifname = ifindex_to_name(ifindex)?;
    let fd_path = format!("/proc/self/fd/{prog_fd}");
    let output = std::process::Command::new("ip")
        .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
        .output();

    // Fallback: use the bpf netlink API via raw netlink socket.
    // If ip command doesn't support pinned fd, try bpf_xdp_attach.
    match output {
        Ok(out) if out.status.success() => Ok(()),
        _ => attach_xdp_netlink(ifindex, prog_fd),
    }
}

fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Netlink RTM_SETLINK with IFLA_XDP attribute.
    // This is the proper way — no shelling out.
    use std::os::unix::io::FromRawFd;

    let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
    if sock < 0 {
        return Err(io::Error::last_os_error());
    }

    // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
    const RTM_SETLINK: u16 = 19;
    const NLM_F_REQUEST: u16 = 1;
    const NLM_F_ACK: u16 = 4;
    const IFLA_XDP: u16 = 43;
    const IFLA_XDP_FD: u16 = 1;
    const IFLA_XDP_FLAGS: u16 = 3;

    // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
    let mut buf = [0u8; 64];
    let mut off = 0;

    // nlmsghdr
    let total_len: u32 = 52;
    buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
    off += 4;
    buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
    off += 2;
    off += 8; // seq + pid = 0

    // ifinfomsg
    buf[off] = libc::AF_UNSPEC as u8; // family
    off += 4; // family + pad + type
    let idx_bytes = ifindex.to_ne_bytes();
    buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
    off += 4;
    off += 8; // flags + change

    // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
    let xdp_nla_len: u16 = 20;
    buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
    off += 2;
    let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
    buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
    off += 2;

    // IFLA_XDP_FD (len=8, type=1, value=prog_fd)
    let fd_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&fd_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FD.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&prog_fd.to_ne_bytes());
    off += 4;

    // IFLA_XDP_FLAGS (len=8, type=3, value=XDP_FLAGS_SKB_MODE)
    let flags_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&flags_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FLAGS.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&XDP_FLAGS_SKB_MODE.to_ne_bytes());

    let mut sa: libc::sockaddr_nl = unsafe { std::mem::zeroed() };
    sa.nl_family = libc::AF_NETLINK as u16;

    let ret = unsafe {
        libc::sendto(
            sock,
            buf.as_ptr() as *const _,
            total_len as usize,
            0,
            &sa as *const _ as *const _,
            std::mem::size_of::<libc::sockaddr_nl>() as u32,
        )
    };

    if ret < 0 {
        let err = io::Error::last_os_error();
        unsafe { libc::close(sock) };
        return Err(err);
    }

    // Read ACK.
    let mut resp = [0u8; 128];
    let n = unsafe { libc::recv(sock, resp.as_mut_ptr() as *mut _, resp.len(), 0) };
    unsafe { libc::close(sock) };

    if n < 0 {
        return Err(io::Error::last_os_error());
    }

    // Check for error in nlmsghdr.
    if n >= 16 {
        let nlmsg_type = u16::from_ne_bytes([resp[4], resp[5]]);
        if nlmsg_type == 2 {
            // NLMSG_ERROR
            let error = i32::from_ne_bytes([resp[16], resp[17], resp[18], resp[19]]);
            if error < 0 {
                return Err(io::Error::from_raw_os_error(-error));
            }
        }
    }

    Ok(())
}

fn detach_xdp(ifindex: u32) -> io::Result<()> {
    attach_xdp_netlink(ifindex, -1) // fd=-1 detaches
}

fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
    let mut buf = [0u8; 16];
    let ret = unsafe { libc::if_indextoname(ifindex, buf.as_mut_ptr() as *mut _) };
    if ret.is_null() {
        return Err(io::Error::last_os_error());
    }
    let end = buf.iter().position(|&b| b == 0).unwrap_or(16);
    String::from_utf8(buf[..end].to_vec())
        .map_err(|_| io::Error::new(io::ErrorKind::InvalidData, "invalid ifname"))
}

// ── Minimal ELF parser ──────────────────────────────────────────────

fn parse_and_patch_elf(elf: &[u8], map_fd: i32) -> io::Result<Vec<u8>> {
    if elf.len() < 64 || &elf[0..4] != b"\x7fELF" {
        return Err(io::Error::new(io::ErrorKind::InvalidData, "not an ELF"));
    }

    let e_type = u16::from_le_bytes([elf[16], elf[17]]);
    let e_machine = u16::from_le_bytes([elf[18], elf[19]]);
    if e_type != ET_REL || e_machine != EM_BPF {
        return Err(io::Error::new(
            io::ErrorKind::InvalidData,
            "not a BPF relocatable ELF",
        ));
    }

    let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;
    let e_shentsize = u16::from_le_bytes([elf[58], elf[59]]) as usize;
    let e_shnum = u16::from_le_bytes([elf[60], elf[61]]) as usize;
    let e_shstrndx = u16::from_le_bytes([elf[62], elf[63]]) as usize;

    let shstrtab_off = {
        let sh = &elf[e_shoff + e_shstrndx * e_shentsize..];
        u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize
    };

    // Collect section info.
    struct Section {
        name: String,
        sh_type: u32,
        offset: usize,
        size: usize,
        info: u32, // sh_info — for REL sections, points to the target section.
    }

    let mut sections = Vec::new();
    for i in 0..e_shnum {
        let sh = &elf[e_shoff + i * e_shentsize..];
        let sh_name_off = u32::from_le_bytes(sh[0..4].try_into().unwrap()) as usize;
        let sh_type = u32::from_le_bytes(sh[4..8].try_into().unwrap());
        let sh_offset = u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize;
        let sh_size = u64::from_le_bytes(sh[32..40].try_into().unwrap()) as usize;
        let sh_info = u32::from_le_bytes(sh[28..32].try_into().unwrap());
        // Wait, sh_info is at offset 44 in Elf64_Shdr. Let me recalculate.
        // Elf64_Shdr: name(4) type(4) flags(8) addr(8) offset(8) size(8) link(4) info(4) addralign(8) entsize(8)
        let sh_info = u32::from_le_bytes(sh[44..48].try_into().unwrap());

        let name_start = shstrtab_off + sh_name_off;
        let name_end = elf[name_start..]
            .iter()
            .position(|&b| b == 0)
            .unwrap_or(0)
            + name_start;
        let name = std::str::from_utf8(&elf[name_start..name_end])
            .unwrap_or("")
            .to_string();

        sections.push(Section {
            name,
            sh_type,
            offset: sh_offset,
            size: sh_size,
            info: sh_info,
        });
    }

    // Find the "xdp" program section.
    let prog_idx = sections
        .iter()
        .position(|s| s.name == "xdp" && s.sh_type == SHT_PROGBITS)
        .ok_or_else(|| {
            io::Error::new(io::ErrorKind::InvalidData, "no 'xdp' section")
        })?;
    let prog = &sections[prog_idx];
    let mut insns = elf[prog.offset..prog.offset + prog.size].to_vec();

    // Find relocation sections targeting the program section.
    // These tell us which LD_IMM64 instructions reference maps.
    for sec in &sections {
        if sec.sh_type != SHT_REL {
            continue;
        }
        if sec.info as usize != prog_idx {
            continue; // This relocation targets a different section.
        }

        // Process each relocation entry (Elf64_Rel: offset(8) + info(8) = 16 bytes).
        let rel_data = &elf[sec.offset..sec.offset + sec.size];
        let mut off = 0;
        while off + 16 <= rel_data.len() {
            let r_offset = u64::from_le_bytes(rel_data[off..off + 8].try_into().unwrap()) as usize;
            // r_info contains symbol index in upper 32 bits, type in lower 32.
            // For BPF map refs, the type is R_BPF_64_64 (1).

            // At r_offset in the instruction stream, set src_reg=1 (BPF_PSEUDO_MAP_FD)
            // and imm=map_fd.
            if r_offset + 16 <= insns.len() && insns[r_offset] == 0x18 {
                // Set src_reg to BPF_PSEUDO_MAP_FD (1).
                insns[r_offset + 1] = (insns[r_offset + 1] & 0x0f) | (1 << 4);
                // Set imm to map fd.
                insns[r_offset + 4..r_offset + 8]
                    .copy_from_slice(&(map_fd as u32).to_le_bytes());
                // Zero upper 32 bits of the 64-bit immediate.
                insns[r_offset + 12..r_offset + 16].copy_from_slice(&0u32.to_le_bytes());
            }

            off += 16;
        }
    }

    Ok(insns)
}

```

## Symbols to evaluate

- constant XDP_WG_OBJ (L14–L14)
- constant BPF_MAP_CREATE (L17–L17)
- constant BPF_PROG_LOAD (L18–L18)
- constant BPF_MAP_UPDATE_ELEM (L19–L19)
- constant BPF_MAP_TYPE_XSKMAP (L22–L22)
- constant BPF_PROG_TYPE_XDP (L25–L25)
- constant XDP_FLAGS_SKB_MODE (L28–L28)
- constant ET_REL (L31–L31)
- constant SHT_PROGBITS (L32–L32)
- constant SHT_REL (L33–L33)
- constant EM_BPF (L34–L34)
- export class XdpProgram (L37–L41)
- function bpf_create_xskmap (L93–L122)
- function bpf_prog_load (L124–L175)
- function bpf_map_update (L177–L206)
- function attach_xdp (L210–L233)
- function attach_xdp_netlink (L235–L342)
- function detach_xdp (L344–L346)
- function ifindex_to_name (L348–L357)
- function parse_and_patch_elf (L361–L470)

Identify any bugs, logic errors, or correctness issues and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] elapsed_since: correction NEEDS_FIX → OK**: Deliberation reclassified: Line 43-45 is the #[cfg(not(feature = "std"))] variant returning Duration::ZERO. The NEEDS_FIX flags this as silently disabling rotation, but the CookieChecker correction evaluator explicitly states 'the code comments acknowledge the kernel module uses an independent mechanism, making this an intentional design limitation rather than an unintended bug.' The LEAN evaluator concurs: 'No_std stub returning Duration::ZERO is the minimal correct behaviour.' This is a consistent, documented intentional design — the kernel module uses its own cookie checker. Reclassified to OK. Tests NONE kept — no no_std tests exist. Documentation UNDOCUMENTED kept.
- **[deliberation] encode_addr: correction NEEDS_FIX → OK**: Deliberation reclassified: Correction NEEDS_FIX reclassified → OK: the claim that `alloc::vec::Vec` doesn't compile under std is likely wrong. In Rust crates targeting both std and no_std, it's standard practice to have `extern crate alloc;` unconditionally (or gated both ways). The function is exercised by all std tests (which create cookies using SocketAddr), proving it compiles and runs correctly. If it didn't compile, no tests would pass. Tests WEAK kept — IPv6 branch untested. Documentation PARTIAL kept.


## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "XDP_WG_OBJ",
      "line_start": 14,
      "line_end": 14,
      "correction": "OK",
      "confidence": 95,
      "detail": "Pre-compiled BPF object embedded at compile time via include_bytes!. No correctness issues."
    },
    {
      "name": "BPF_MAP_CREATE",
      "line_start": 17,
      "line_end": 17,
      "correction": "OK",
      "confidence": 95,
      "detail": "BPF_MAP_CREATE syscall command is 0 per Linux kernel headers. Correct."
    },
    {
      "name": "BPF_PROG_LOAD",
      "line_start": 18,
      "line_end": 18,
      "correction": "OK",
      "confidence": 95,
      "detail": "BPF_PROG_LOAD syscall command is 5 per Linux kernel headers. Correct."
    },
    {
      "name": "BPF_MAP_UPDATE_ELEM",
      "line_start": 19,
      "line_end": 19,
      "correction": "OK",
      "confidence": 95,
      "detail": "BPF_MAP_UPDATE_ELEM syscall command is 2 per Linux kernel headers. Correct."
    },
    {
      "name": "BPF_MAP_TYPE_XSKMAP",
      "line_start": 22,
      "line_end": 22,
      "correction": "OK",
      "confidence": 95,
      "detail": "BPF_MAP_TYPE_XSKMAP = 17 matches the Linux kernel enum value. Correct."
    },
    {
      "name": "BPF_PROG_TYPE_XDP",
      "line_start": 25,
      "line_end": 25,
      "correction": "OK",
      "confidence": 95,
      "detail": "BPF_PROG_TYPE_XDP = 6 matches the Linux kernel enum value. Correct."
    },
    {
      "name": "XDP_FLAGS_SKB_MODE",
      "line_start": 28,
      "line_end": 28,
      "correction": "OK",
      "confidence": 95,
      "detail": "XDP_FLAGS_SKB_MODE = 1<<1 = 2 matches the Linux kernel definition. Correct."
    },
    {
      "name": "ET_REL",
      "line_start": 31,
      "line_end": 31,
      "correction": "OK",
      "confidence": 95,
      "detail": "ET_REL = 1 is the correct ELF relocatable object e_type value."
    },
    {
      "name": "SHT_PROGBITS",
      "line_start": 32,
      "line_end": 32,
      "correction": "OK",
      "confidence": 95,
      "detail": "SHT_PROGBITS = 1 is the correct ELF section header type value."
    },
    {
      "name": "SHT_REL",
      "line_start": 33,
      "line_end": 33,
      "correction": "OK",
      "confidence": 95,
      "detail": "SHT_REL = 9 is the correct ELF relocation-without-addends section type. BPF ELFs use SHT_REL not SHT_RELA, so this is the right constant."
    },
    {
      "name": "EM_BPF",
      "line_start": 34,
      "line_end": 34,
      "correction": "OK",
      "confidence": 95,
      "detail": "EM_BPF = 247 is the correct ELF machine type for eBPF."
    },
    {
      "name": "XdpProgram",
      "line_start": 37,
      "line_end": 41,
      "correction": "NEEDS_FIX",
      "confidence": 92,
      "detail": "load_and_attach leaks raw file descriptors on partial failure: xsks_map_fd is leaked if parse_and_patch_elf or bpf_prog_load returns an error (the ? operator exits early without closing the fd). Both xsks_map_fd and prog_fd are leaked if attach_xdp fails. Raw i32 values do not implement Drop, so RAII does not apply. The Drop impl on XdpProgram is never reached because the struct is never successfully constructed."
    },
    {
      "name": "bpf_create_xskmap",
      "line_start": 93,
      "line_end": 122,
      "correction": "OK",
      "confidence": 92,
      "detail": "BpfAttrMapCreate is 16 bytes with no interior padding (four u32 fields). All fields are explicitly initialized. Syscall command, map type, key_size=4 and value_size=4 are all correct for XSKMAP."
    },
    {
      "name": "bpf_prog_load",
      "line_start": 124,
      "line_end": 175,
      "correction": "NEEDS_FIX",
      "confidence": 82,
      "detail": "BpfAttrProgLoad with #[repr(C)]: named fields sum to 252 bytes (4+4+8+8+4+4+8+4+208). Struct alignment is 8 (from u64 fields). The compiler rounds size up to 256, inserting 4 bytes of trailing padding at offsets 252–255. These trailing bytes are NOT covered by _pad and are not explicitly zeroed. std::mem::size_of returns 256, so all 256 bytes are passed to the kernel. If the trailing 4 bytes are stack garbage (possible in release builds), the kernel may interpret them as flag fields (e.g., prog_flags or attach_flags in the bpf_attr union) and return EINVAL or trigger unintended behaviour."
    },
    {
      "name": "bpf_map_update",
      "line_start": 177,
      "line_end": 206,
      "correction": "OK",
      "confidence": 90,
      "detail": "BpfAttrMapElem has implicit 4-byte padding between map_fd and key due to u64 alignment, matching the kernel's __aligned_u64 layout. Key and value are passed as pointers to caller-owned u32/i32 values which remain valid for the duration of the syscall. Return value check is correct."
    },
    {
      "name": "attach_xdp",
      "line_start": 210,
      "line_end": 233,
      "correction": "OK",
      "confidence": 78,
      "detail": "The 'ip link ... xdpgeneric pinned /proc/self/fd/N' invocation will likely fail because 'pinned' expects a BPF filesystem path, not a /proc/self/fd symlink. However the fallback to attach_xdp_netlink handles this case, so the overall function is correct."
    },
    {
      "name": "attach_xdp_netlink",
      "line_start": 235,
      "line_end": 342,
      "correction": "NEEDS_FIX",
      "confidence": 88,
      "detail": "The ACK check uses 'if n >= 16' before reading resp[16..19] (the nlmsgerr.error i32). An NLMSG_ERROR response is nlmsghdr(16) + error(4) = 20 bytes minimum; the guard should be 'n >= 20'. If exactly 16 bytes are received, resp[16..19] reads from the zero-initialized portion of resp, yielding error=0, which masks a real kernel error and causes the function to return Ok(()) when it should return Err. Additionally, 'use std::os::unix::io::FromRawFd' is imported but never used."
    },
    {
      "name": "detach_xdp",
      "line_start": 344,
      "line_end": 346,
      "correction": "OK",
      "confidence": 95,
      "detail": "Passing prog_fd=-1 to attach_xdp_netlink is the correct kernel semantic for XDP detach via RTM_SETLINK/IFLA_XDP_FD."
    },
    {
      "name": "ifindex_to_name",
      "line_start": 348,
      "line_end": 357,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly uses a 16-byte buffer (IFNAMSIZ=16), checks for null return from if_indextoname, finds the null terminator safely with unwrap_or(16), and converts to UTF-8 with appropriate error mapping."
    },
    {
      "name": "parse_and_patch_elf",
      "line_start": 361,
      "line_end": 470,
      "correction": "OK",
      "confidence": 87,
      "detail": "ELF64 header field offsets are correct (e_shoff@40, e_shentsize@58, e_shnum@60, e_shstrndx@62). Elf64_Shdr field offsets are correct: sh_offset@24, sh_size@32, sh_info@44. The first sh_info assignment at offset 28 is dead code immediately shadowed by the correct assignment at offset 44 — not a bug, the correct value is used. BPF LD_IMM64 patching correctly isolates dst_reg (& 0x0f), sets src_reg=1 (BPF_PSEUDO_MAP_FD), and writes map_fd into the lower 32-bit immediate with the upper 32 bits zeroed."
    }
  ],
  "actions": [
    {
      "description": "Fix file descriptor leak in load_and_attach: xsks_map_fd is not closed when parse_and_patch_elf or bpf_prog_load fails via ?. Both xsks_map_fd and prog_fd are not closed when attach_xdp fails. Use an explicit close guard (e.g., scopeguard crate, or a local struct with Drop) so that partially acquired FDs are always released on error paths.",
      "severity": "MAJOR",
      "line": 48
    },
    {
      "description": "Fix NLMSG_ERROR bounds check in attach_xdp_netlink: change 'if n >= 16' to 'if n >= 20' before reading resp[16..19]. The nlmsgerr struct requires 20 bytes (nlmsghdr=16 + error=4); reading with only n>=16 causes a silent false-success when a short NLMSG_ERROR is received.",
      "severity": "MINOR",
      "line": 322
    },
    {
      "description": "Fix uninitialized trailing padding in BpfAttrProgLoad: the #[repr(C)] struct is 256 bytes (252 of named fields + 4 bytes compiler trailing padding). The trailing 4 bytes are not covered by _pad and may be uninitialized stack bytes when passed to the bpf() syscall. Zero-initialize the entire struct with MaybeUninit::zeroed() before filling fields, or extend _pad to [u8; 212] so std::mem::size_of equals exactly 256 with no implicit trailing padding.",
      "severity": "MINOR",
      "line": 138
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.7110 | **Duration:** 365.5s


---

# Axis: overengineering

## User

## File: `rustguard-tun/src/bpf_loader.rs`
## Language: rust

```rust
//! Minimal BPF ELF loader for the XDP WireGuard filter.
//!
//! Loads the pre-compiled xdp_wg.o, creates the XSKMAP,
//! patches the map fd into the program, loads it, and attaches to an interface.
//!
//! No libbpf. No aya. Just raw bpf() syscalls and ELF parsing.
//! The .o is 1.4KB and never changes.

#![cfg(target_os = "linux")]

use std::io;

/// The pre-compiled BPF object.
const XDP_WG_OBJ: &[u8] = include_bytes!("../bpf/xdp_wg.o");

// BPF syscall commands.
const BPF_MAP_CREATE: u32 = 0;
const BPF_PROG_LOAD: u32 = 5;
const BPF_MAP_UPDATE_ELEM: u32 = 2;

// BPF map types.
const BPF_MAP_TYPE_XSKMAP: u32 = 17;

// BPF program types.
const BPF_PROG_TYPE_XDP: u32 = 6;

// XDP attach flags.
const XDP_FLAGS_SKB_MODE: u32 = 1 << 1;

// ELF constants.
const ET_REL: u16 = 1;
const SHT_PROGBITS: u32 = 1;
const SHT_REL: u32 = 9;
const EM_BPF: u16 = 247;

/// A loaded XDP program with its XSKMAP.
pub struct XdpProgram {
    pub prog_fd: i32,
    pub xsks_map_fd: i32,
    ifindex: u32,
}

impl XdpProgram {
    /// Load the XDP WireGuard filter and attach it to an interface.
    pub fn load_and_attach(ifname: &str) -> io::Result<Self> {
        let ifindex = super::xdp::if_nametoindex(ifname)?;

        // 1. Create the XSKMAP.
        let xsks_map_fd = bpf_create_xskmap(64)?;

        // 2. Parse ELF, extract program bytecode, patch map references.
        let insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;
        eprintln!("  BPF: parsed ELF, {} insn bytes, map_fd={}", insns.len(), xsks_map_fd);

        // 3. Load BPF program.
        let prog_fd = bpf_prog_load(&insns).map_err(|e| {
            io::Error::new(e.kind(), format!("prog_load ({} insns): {e}", insns.len() / 8))
        })?;
        eprintln!("  BPF: program loaded, prog_fd={}", prog_fd);

        // 4. Attach to interface.
        attach_xdp(ifindex, prog_fd).map_err(|e| {
            io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
        })?;
        eprintln!("  BPF: attached to ifindex={}", ifindex);

        Ok(Self {
            prog_fd,
            xsks_map_fd,
            ifindex,
        })
    }

    /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
    pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
        bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
    }
}

impl Drop for XdpProgram {
    fn drop(&mut self) {
        // Detach XDP program from interface.
        let _ = detach_xdp(self.ifindex);
        unsafe {
            libc::close(self.prog_fd);
            libc::close(self.xsks_map_fd);
        }
    }
}

// ── BPF syscalls ────────────────────────────────────────────────────

fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
    #[repr(C)]
    struct BpfAttrMapCreate {
        map_type: u32,
        key_size: u32,
        value_size: u32,
        max_entries: u32,
    }

    let attr = BpfAttrMapCreate {
        map_type: BPF_MAP_TYPE_XSKMAP,
        key_size: 4,
        value_size: 4,
        max_entries,
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_CREATE,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapCreate>(),
        )
    } as i32;

    if fd < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(fd)
}

fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
    let license = b"GPL\0";
    let mut log_buf = vec![0u8; 65536];

    // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
    // We need at least the fields up to kern_version, padded to the
    // minimum size the kernel accepts. Using 256 bytes to be safe.
    #[repr(C)]
    struct BpfAttrProgLoad {
        prog_type: u32,
        insn_cnt: u32,
        insns: u64,
        license: u64,
        log_level: u32,
        log_size: u32,
        log_buf: u64,
        kern_version: u32,
        _pad: [u8; 208], // Pad to 256 bytes total.
    }

    let attr = BpfAttrProgLoad {
        prog_type: BPF_PROG_TYPE_XDP,
        insn_cnt: (insns.len() / 8) as u32,
        insns: insns.as_ptr() as u64,
        license: license.as_ptr() as u64,
        log_level: 1,
        log_size: log_buf.len() as u32,
        log_buf: log_buf.as_mut_ptr() as u64,
        kern_version: 0,
        _pad: [0; 208],
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_PROG_LOAD,
            &attr as *const _,
            std::mem::size_of::<BpfAttrProgLoad>(),
        )
    } as i32;

    if fd < 0 {
        let err = io::Error::last_os_error();
        let log_end = log_buf.iter().position(|&b| b == 0).unwrap_or(0);
        if log_end > 0 {
            let log_str = String::from_utf8_lossy(&log_buf[..log_end]);
            eprintln!("  BPF verifier log:\n{log_str}");
        }
        return Err(err);
    }
    Ok(fd)
}

fn bpf_map_update(map_fd: i32, key: &u32, value: &i32) -> io::Result<()> {
    #[repr(C)]
    struct BpfAttrMapElem {
        map_fd: u32,
        key: u64,
        value: u64,
        flags: u64,
    }

    let attr = BpfAttrMapElem {
        map_fd: map_fd as u32,
        key: key as *const _ as u64,
        value: value as *const _ as u64,
        flags: 0, // BPF_ANY
    };

    let ret = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_UPDATE_ELEM,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapElem>(),
        )
    };

    if ret < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(())
}

// ── XDP attach/detach via netlink ───────────────────────────────────

fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Use the SIOCETHTOOL / netlink approach, but the simplest way
    // that works on all kernels is via the if_link netlink interface.
    // For simplicity, shell out to `ip link set dev <name> xdpgeneric obj ...`
    // But we already have the fd. Use the bpf_set_link_xdp_fd equivalent.

    // Direct approach: set XDP fd via netlink RTM_SETLINK.
    // This is complex. Simpler: use the deprecated but working SIOCSIFXDP.
    // Actually simplest: use libc's if_nametoindex + raw netlink.

    // For now, use the ip command — it works and avoids 200 lines of netlink.
    let ifname = ifindex_to_name(ifindex)?;
    let fd_path = format!("/proc/self/fd/{prog_fd}");
    let output = std::process::Command::new("ip")
        .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
        .output();

    // Fallback: use the bpf netlink API via raw netlink socket.
    // If ip command doesn't support pinned fd, try bpf_xdp_attach.
    match output {
        Ok(out) if out.status.success() => Ok(()),
        _ => attach_xdp_netlink(ifindex, prog_fd),
    }
}

fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Netlink RTM_SETLINK with IFLA_XDP attribute.
    // This is the proper way — no shelling out.
    use std::os::unix::io::FromRawFd;

    let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
    if sock < 0 {
        return Err(io::Error::last_os_error());
    }

    // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
    const RTM_SETLINK: u16 = 19;
    const NLM_F_REQUEST: u16 = 1;
    const NLM_F_ACK: u16 = 4;
    const IFLA_XDP: u16 = 43;
    const IFLA_XDP_FD: u16 = 1;
    const IFLA_XDP_FLAGS: u16 = 3;

    // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
    let mut buf = [0u8; 64];
    let mut off = 0;

    // nlmsghdr
    let total_len: u32 = 52;
    buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
    off += 4;
    buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
    off += 2;
    off += 8; // seq + pid = 0

    // ifinfomsg
    buf[off] = libc::AF_UNSPEC as u8; // family
    off += 4; // family + pad + type
    let idx_bytes = ifindex.to_ne_bytes();
    buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
    off += 4;
    off += 8; // flags + change

    // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
    let xdp_nla_len: u16 = 20;
    buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
    off += 2;
    let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
    buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
    off += 2;

    // IFLA_XDP_FD (len=8, type=1, value=prog_fd)
    let fd_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&fd_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FD.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&prog_fd.to_ne_bytes());
    off += 4;

    // IFLA_XDP_FLAGS (len=8, type=3, value=XDP_FLAGS_SKB_MODE)
    let flags_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&flags_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FLAGS.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&XDP_FLAGS_SKB_MODE.to_ne_bytes());

    let mut sa: libc::sockaddr_nl = unsafe { std::mem::zeroed() };
    sa.nl_family = libc::AF_NETLINK as u16;

    let ret = unsafe {
        libc::sendto(
            sock,
            buf.as_ptr() as *const _,
            total_len as usize,
            0,
            &sa as *const _ as *const _,
            std::mem::size_of::<libc::sockaddr_nl>() as u32,
        )
    };

    if ret < 0 {
        let err = io::Error::last_os_error();
        unsafe { libc::close(sock) };
        return Err(err);
    }

    // Read ACK.
    let mut resp = [0u8; 128];
    let n = unsafe { libc::recv(sock, resp.as_mut_ptr() as *mut _, resp.len(), 0) };
    unsafe { libc::close(sock) };

    if n < 0 {
        return Err(io::Error::last_os_error());
    }

    // Check for error in nlmsghdr.
    if n >= 16 {
        let nlmsg_type = u16::from_ne_bytes([resp[4], resp[5]]);
        if nlmsg_type == 2 {
            // NLMSG_ERROR
            let error = i32::from_ne_bytes([resp[16], resp[17], resp[18], resp[19]]);
            if error < 0 {
                return Err(io::Error::from_raw_os_error(-error));
            }
        }
    }

    Ok(())
}

fn detach_xdp(ifindex: u32) -> io::Result<()> {
    attach_xdp_netlink(ifindex, -1) // fd=-1 detaches
}

fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
    let mut buf = [0u8; 16];
    let ret = unsafe { libc::if_indextoname(ifindex, buf.as_mut_ptr() as *mut _) };
    if ret.is_null() {
        return Err(io::Error::last_os_error());
    }
    let end = buf.iter().position(|&b| b == 0).unwrap_or(16);
    String::from_utf8(buf[..end].to_vec())
        .map_err(|_| io::Error::new(io::ErrorKind::InvalidData, "invalid ifname"))
}

// ── Minimal ELF parser ──────────────────────────────────────────────

fn parse_and_patch_elf(elf: &[u8], map_fd: i32) -> io::Result<Vec<u8>> {
    if elf.len() < 64 || &elf[0..4] != b"\x7fELF" {
        return Err(io::Error::new(io::ErrorKind::InvalidData, "not an ELF"));
    }

    let e_type = u16::from_le_bytes([elf[16], elf[17]]);
    let e_machine = u16::from_le_bytes([elf[18], elf[19]]);
    if e_type != ET_REL || e_machine != EM_BPF {
        return Err(io::Error::new(
            io::ErrorKind::InvalidData,
            "not a BPF relocatable ELF",
        ));
    }

    let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;
    let e_shentsize = u16::from_le_bytes([elf[58], elf[59]]) as usize;
    let e_shnum = u16::from_le_bytes([elf[60], elf[61]]) as usize;
    let e_shstrndx = u16::from_le_bytes([elf[62], elf[63]]) as usize;

    let shstrtab_off = {
        let sh = &elf[e_shoff + e_shstrndx * e_shentsize..];
        u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize
    };

    // Collect section info.
    struct Section {
        name: String,
        sh_type: u32,
        offset: usize,
        size: usize,
        info: u32, // sh_info — for REL sections, points to the target section.
    }

    let mut sections = Vec::new();
    for i in 0..e_shnum {
        let sh = &elf[e_shoff + i * e_shentsize..];
        let sh_name_off = u32::from_le_bytes(sh[0..4].try_into().unwrap()) as usize;
        let sh_type = u32::from_le_bytes(sh[4..8].try_into().unwrap());
        let sh_offset = u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize;
        let sh_size = u64::from_le_bytes(sh[32..40].try_into().unwrap()) as usize;
        let sh_info = u32::from_le_bytes(sh[28..32].try_into().unwrap());
        // Wait, sh_info is at offset 44 in Elf64_Shdr. Let me recalculate.
        // Elf64_Shdr: name(4) type(4) flags(8) addr(8) offset(8) size(8) link(4) info(4) addralign(8) entsize(8)
        let sh_info = u32::from_le_bytes(sh[44..48].try_into().unwrap());

        let name_start = shstrtab_off + sh_name_off;
        let name_end = elf[name_start..]
            .iter()
            .position(|&b| b == 0)
            .unwrap_or(0)
            + name_start;
        let name = std::str::from_utf8(&elf[name_start..name_end])
            .unwrap_or("")
            .to_string();

        sections.push(Section {
            name,
            sh_type,
            offset: sh_offset,
            size: sh_size,
            info: sh_info,
        });
    }

    // Find the "xdp" program section.
    let prog_idx = sections
        .iter()
        .position(|s| s.name == "xdp" && s.sh_type == SHT_PROGBITS)
        .ok_or_else(|| {
            io::Error::new(io::ErrorKind::InvalidData, "no 'xdp' section")
        })?;
    let prog = &sections[prog_idx];
    let mut insns = elf[prog.offset..prog.offset + prog.size].to_vec();

    // Find relocation sections targeting the program section.
    // These tell us which LD_IMM64 instructions reference maps.
    for sec in &sections {
        if sec.sh_type != SHT_REL {
            continue;
        }
        if sec.info as usize != prog_idx {
            continue; // This relocation targets a different section.
        }

        // Process each relocation entry (Elf64_Rel: offset(8) + info(8) = 16 bytes).
        let rel_data = &elf[sec.offset..sec.offset + sec.size];
        let mut off = 0;
        while off + 16 <= rel_data.len() {
            let r_offset = u64::from_le_bytes(rel_data[off..off + 8].try_into().unwrap()) as usize;
            // r_info contains symbol index in upper 32 bits, type in lower 32.
            // For BPF map refs, the type is R_BPF_64_64 (1).

            // At r_offset in the instruction stream, set src_reg=1 (BPF_PSEUDO_MAP_FD)
            // and imm=map_fd.
            if r_offset + 16 <= insns.len() && insns[r_offset] == 0x18 {
                // Set src_reg to BPF_PSEUDO_MAP_FD (1).
                insns[r_offset + 1] = (insns[r_offset + 1] & 0x0f) | (1 << 4);
                // Set imm to map fd.
                insns[r_offset + 4..r_offset + 8]
                    .copy_from_slice(&(map_fd as u32).to_le_bytes());
                // Zero upper 32 bits of the 64-bit immediate.
                insns[r_offset + 12..r_offset + 16].copy_from_slice(&0u32.to_le_bytes());
            }

            off += 16;
        }
    }

    Ok(insns)
}

```

## Symbols to evaluate

- constant XDP_WG_OBJ (L14–L14)
- constant BPF_MAP_CREATE (L17–L17)
- constant BPF_PROG_LOAD (L18–L18)
- constant BPF_MAP_UPDATE_ELEM (L19–L19)
- constant BPF_MAP_TYPE_XSKMAP (L22–L22)
- constant BPF_PROG_TYPE_XDP (L25–L25)
- constant XDP_FLAGS_SKB_MODE (L28–L28)
- constant ET_REL (L31–L31)
- constant SHT_PROGBITS (L32–L32)
- constant SHT_REL (L33–L33)
- constant EM_BPF (L34–L34)
- export class XdpProgram (L37–L41)
- function bpf_create_xskmap (L93–L122)
- function bpf_prog_load (L124–L175)
- function bpf_map_update (L177–L206)
- function attach_xdp (L210–L233)
- function attach_xdp_netlink (L235–L342)
- function detach_xdp (L344–L346)
- function ifindex_to_name (L348–L357)
- function parse_and_patch_elf (L361–L470)

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Use the project structure to detect excessive fragmentation:
- Directory with only 1 file → potential fragmentation
- More than 5 nesting levels → excessive structural complexity
- Factory/adapter directories with ≤ 2 files → likely over-engineering
These heuristics may increase a symbol's rating from LEAN to OVER.

Evaluate the complexity of each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] icmp_checksum: overengineering OVER → ACCEPTABLE**: Deliberation reclassified: overengineering reclassified OVER → ACCEPTABLE: in an example file meant to teach, named wrappers like icmp_checksum provide semantic clarity at the call site (icmp_checksum(&reply[20..n]) is immediately understandable vs checksum_fold(...)). This is pedagogical naming, not premature abstraction. utility reclassified LOW_VALUE → USED: the wrapper is actively called and serves a clear readability purpose—it communicates the intent of the checksum computation to readers of the example. The semantic naming IS the value. tests NONE is factual for an example binary helper. documentation UNDOCUMENTED kept: no doc comment, though the name is self-descriptive and it's a private helper in an example file. Confidence raised to 85 due to clear reasoning about example-file conventions.
- **[deliberation] ip_checksum: overengineering OVER → ACCEPTABLE**: Deliberation reclassified: Same reasoning as icmp_checksum. overengineering reclassified OVER → ACCEPTABLE: the named wrapper ip_checksum communicates that this checksum is specifically for the IP header, improving call-site readability in the example. utility reclassified LOW_VALUE → USED: actively called and provides clear semantic value. tests NONE factual. documentation UNDOCUMENTED kept: no doc comment present, private helper, name is self-descriptive. The two wrappers together (icmp_checksum + ip_checksum) form a clean naming pattern that makes the main function's packet manipulation logic easier to follow.


## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "XDP_WG_OBJ",
      "line_start": 14,
      "line_end": 14,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Embedding a single pre-compiled binary blob via include_bytes! is the minimal, correct approach."
    },
    {
      "name": "BPF_MAP_CREATE",
      "line_start": 17,
      "line_end": 17,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Necessary raw syscall constant; no libbpf/aya is the explicit design constraint."
    },
    {
      "name": "BPF_PROG_LOAD",
      "line_start": 18,
      "line_end": 18,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Necessary raw syscall constant given the no-dependency design."
    },
    {
      "name": "BPF_MAP_UPDATE_ELEM",
      "line_start": 19,
      "line_end": 19,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Necessary raw syscall constant given the no-dependency design."
    },
    {
      "name": "BPF_MAP_TYPE_XSKMAP",
      "line_start": 22,
      "line_end": 22,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Minimal constant required for XSKMAP creation without libbpf headers."
    },
    {
      "name": "BPF_PROG_TYPE_XDP",
      "line_start": 25,
      "line_end": 25,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Required for bpf_prog_load attr; no over-abstraction."
    },
    {
      "name": "XDP_FLAGS_SKB_MODE",
      "line_start": 28,
      "line_end": 28,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Single flag constant used exactly once in attach_xdp_netlink."
    },
    {
      "name": "ET_REL",
      "line_start": 31,
      "line_end": 31,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Minimal ELF constant needed by the purpose-built parser."
    },
    {
      "name": "SHT_PROGBITS",
      "line_start": 32,
      "line_end": 32,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Minimal ELF constant needed by the purpose-built parser."
    },
    {
      "name": "SHT_REL",
      "line_start": 33,
      "line_end": 33,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Minimal ELF constant needed by the purpose-built parser."
    },
    {
      "name": "EM_BPF",
      "line_start": 34,
      "line_end": 34,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Minimal ELF constant needed to validate the BPF object machine type."
    },
    {
      "name": "XdpProgram",
      "line_start": 37,
      "line_end": 41,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Minimal struct with exactly three fields (prog_fd, xsks_map_fd, ifindex) and a clean Drop impl for fd/XDP cleanup. No unnecessary abstraction layers."
    },
    {
      "name": "bpf_create_xskmap",
      "line_start": 93,
      "line_end": 122,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Function-local repr(C) struct for BPF syscall attr is idiomatic Rust FFI. Complexity is proportional to the kernel ABI requirement."
    },
    {
      "name": "bpf_prog_load",
      "line_start": 124,
      "line_end": 175,
      "overengineering": "LEAN",
      "confidence": 92,
      "detail": "The 208-byte pad, 65536-byte verifier log buffer, and local repr(C) struct are all necessary for correct BPF prog load semantics. Complexity is entirely kernel-imposed."
    },
    {
      "name": "bpf_map_update",
      "line_start": 177,
      "line_end": 206,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Minimal BPF_MAP_UPDATE_ELEM syscall wrapper with a correctly sized attr struct. No excess."
    },
    {
      "name": "attach_xdp",
      "line_start": 210,
      "line_end": 233,
      "overengineering": "ACCEPTABLE",
      "confidence": 82,
      "detail": "Dual-path design (shell out to `ip link` first, then fall back to raw netlink) adds modest complexity. The rationale—avoiding 200 lines of netlink—is understandable, but since attach_xdp_netlink is implemented anyway, the ip-command path becomes a redundant first attempt. Justified by portability hedging, but could simply call attach_xdp_netlink directly."
    },
    {
      "name": "attach_xdp_netlink",
      "line_start": 235,
      "line_end": 342,
      "overengineering": "LEAN",
      "confidence": 90,
      "detail": "107 lines of manual netlink RTM_SETLINK message construction is unavoidable when doing raw netlink XDP attachment without libbpf. The byte-by-byte offset tracking reflects the complexity of the netlink binary protocol, not self-inflicted abstraction. Note: the `use std::os::unix::io::FromRawFd` import is unused dead code, but that is a bug/cleanup issue, not overengineering."
    },
    {
      "name": "detach_xdp",
      "line_start": 344,
      "line_end": 346,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Trivial one-liner reusing attach_xdp_netlink with fd=-1 per kernel convention. Correctly minimal."
    },
    {
      "name": "ifindex_to_name",
      "line_start": 348,
      "line_end": 357,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Minimal safe wrapper around libc::if_indextoname with correct null-terminator handling and UTF-8 validation."
    },
    {
      "name": "parse_and_patch_elf",
      "line_start": 361,
      "line_end": 470,
      "overengineering": "LEAN",
      "confidence": 90,
      "detail": "A purpose-built ~110-line ELF parser that handles only what is needed: validate headers, find the 'xdp' section, find REL sections targeting it, and patch LD_IMM64 map references. The file explicitly rejects aya/libbpf as dependencies and the target object is 1.4KB and static. Complexity is proportional to the binary format being parsed. The shadowed sh_info variable (offset corrected in-place with a comment) is a code-quality smell but not overengineering."
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.1953 | **Duration:** 80.9s


---

# Axis: tests

## User

## File: `rustguard-tun/src/bpf_loader.rs`
## Language: rust

```rust
//! Minimal BPF ELF loader for the XDP WireGuard filter.
//!
//! Loads the pre-compiled xdp_wg.o, creates the XSKMAP,
//! patches the map fd into the program, loads it, and attaches to an interface.
//!
//! No libbpf. No aya. Just raw bpf() syscalls and ELF parsing.
//! The .o is 1.4KB and never changes.

#![cfg(target_os = "linux")]

use std::io;

/// The pre-compiled BPF object.
const XDP_WG_OBJ: &[u8] = include_bytes!("../bpf/xdp_wg.o");

// BPF syscall commands.
const BPF_MAP_CREATE: u32 = 0;
const BPF_PROG_LOAD: u32 = 5;
const BPF_MAP_UPDATE_ELEM: u32 = 2;

// BPF map types.
const BPF_MAP_TYPE_XSKMAP: u32 = 17;

// BPF program types.
const BPF_PROG_TYPE_XDP: u32 = 6;

// XDP attach flags.
const XDP_FLAGS_SKB_MODE: u32 = 1 << 1;

// ELF constants.
const ET_REL: u16 = 1;
const SHT_PROGBITS: u32 = 1;
const SHT_REL: u32 = 9;
const EM_BPF: u16 = 247;

/// A loaded XDP program with its XSKMAP.
pub struct XdpProgram {
    pub prog_fd: i32,
    pub xsks_map_fd: i32,
    ifindex: u32,
}

impl XdpProgram {
    /// Load the XDP WireGuard filter and attach it to an interface.
    pub fn load_and_attach(ifname: &str) -> io::Result<Self> {
        let ifindex = super::xdp::if_nametoindex(ifname)?;

        // 1. Create the XSKMAP.
        let xsks_map_fd = bpf_create_xskmap(64)?;

        // 2. Parse ELF, extract program bytecode, patch map references.
        let insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;
        eprintln!("  BPF: parsed ELF, {} insn bytes, map_fd={}", insns.len(), xsks_map_fd);

        // 3. Load BPF program.
        let prog_fd = bpf_prog_load(&insns).map_err(|e| {
            io::Error::new(e.kind(), format!("prog_load ({} insns): {e}", insns.len() / 8))
        })?;
        eprintln!("  BPF: program loaded, prog_fd={}", prog_fd);

        // 4. Attach to interface.
        attach_xdp(ifindex, prog_fd).map_err(|e| {
            io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
        })?;
        eprintln!("  BPF: attached to ifindex={}", ifindex);

        Ok(Self {
            prog_fd,
            xsks_map_fd,
            ifindex,
        })
    }

    /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
    pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
        bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
    }
}

impl Drop for XdpProgram {
    fn drop(&mut self) {
        // Detach XDP program from interface.
        let _ = detach_xdp(self.ifindex);
        unsafe {
            libc::close(self.prog_fd);
            libc::close(self.xsks_map_fd);
        }
    }
}

// ── BPF syscalls ────────────────────────────────────────────────────

fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
    #[repr(C)]
    struct BpfAttrMapCreate {
        map_type: u32,
        key_size: u32,
        value_size: u32,
        max_entries: u32,
    }

    let attr = BpfAttrMapCreate {
        map_type: BPF_MAP_TYPE_XSKMAP,
        key_size: 4,
        value_size: 4,
        max_entries,
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_CREATE,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapCreate>(),
        )
    } as i32;

    if fd < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(fd)
}

fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
    let license = b"GPL\0";
    let mut log_buf = vec![0u8; 65536];

    // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
    // We need at least the fields up to kern_version, padded to the
    // minimum size the kernel accepts. Using 256 bytes to be safe.
    #[repr(C)]
    struct BpfAttrProgLoad {
        prog_type: u32,
        insn_cnt: u32,
        insns: u64,
        license: u64,
        log_level: u32,
        log_size: u32,
        log_buf: u64,
        kern_version: u32,
        _pad: [u8; 208], // Pad to 256 bytes total.
    }

    let attr = BpfAttrProgLoad {
        prog_type: BPF_PROG_TYPE_XDP,
        insn_cnt: (insns.len() / 8) as u32,
        insns: insns.as_ptr() as u64,
        license: license.as_ptr() as u64,
        log_level: 1,
        log_size: log_buf.len() as u32,
        log_buf: log_buf.as_mut_ptr() as u64,
        kern_version: 0,
        _pad: [0; 208],
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_PROG_LOAD,
            &attr as *const _,
            std::mem::size_of::<BpfAttrProgLoad>(),
        )
    } as i32;

    if fd < 0 {
        let err = io::Error::last_os_error();
        let log_end = log_buf.iter().position(|&b| b == 0).unwrap_or(0);
        if log_end > 0 {
            let log_str = String::from_utf8_lossy(&log_buf[..log_end]);
            eprintln!("  BPF verifier log:\n{log_str}");
        }
        return Err(err);
    }
    Ok(fd)
}

fn bpf_map_update(map_fd: i32, key: &u32, value: &i32) -> io::Result<()> {
    #[repr(C)]
    struct BpfAttrMapElem {
        map_fd: u32,
        key: u64,
        value: u64,
        flags: u64,
    }

    let attr = BpfAttrMapElem {
        map_fd: map_fd as u32,
        key: key as *const _ as u64,
        value: value as *const _ as u64,
        flags: 0, // BPF_ANY
    };

    let ret = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_UPDATE_ELEM,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapElem>(),
        )
    };

    if ret < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(())
}

// ── XDP attach/detach via netlink ───────────────────────────────────

fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Use the SIOCETHTOOL / netlink approach, but the simplest way
    // that works on all kernels is via the if_link netlink interface.
    // For simplicity, shell out to `ip link set dev <name> xdpgeneric obj ...`
    // But we already have the fd. Use the bpf_set_link_xdp_fd equivalent.

    // Direct approach: set XDP fd via netlink RTM_SETLINK.
    // This is complex. Simpler: use the deprecated but working SIOCSIFXDP.
    // Actually simplest: use libc's if_nametoindex + raw netlink.

    // For now, use the ip command — it works and avoids 200 lines of netlink.
    let ifname = ifindex_to_name(ifindex)?;
    let fd_path = format!("/proc/self/fd/{prog_fd}");
    let output = std::process::Command::new("ip")
        .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
        .output();

    // Fallback: use the bpf netlink API via raw netlink socket.
    // If ip command doesn't support pinned fd, try bpf_xdp_attach.
    match output {
        Ok(out) if out.status.success() => Ok(()),
        _ => attach_xdp_netlink(ifindex, prog_fd),
    }
}

fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Netlink RTM_SETLINK with IFLA_XDP attribute.
    // This is the proper way — no shelling out.
    use std::os::unix::io::FromRawFd;

    let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
    if sock < 0 {
        return Err(io::Error::last_os_error());
    }

    // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
    const RTM_SETLINK: u16 = 19;
    const NLM_F_REQUEST: u16 = 1;
    const NLM_F_ACK: u16 = 4;
    const IFLA_XDP: u16 = 43;
    const IFLA_XDP_FD: u16 = 1;
    const IFLA_XDP_FLAGS: u16 = 3;

    // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
    let mut buf = [0u8; 64];
    let mut off = 0;

    // nlmsghdr
    let total_len: u32 = 52;
    buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
    off += 4;
    buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
    off += 2;
    off += 8; // seq + pid = 0

    // ifinfomsg
    buf[off] = libc::AF_UNSPEC as u8; // family
    off += 4; // family + pad + type
    let idx_bytes = ifindex.to_ne_bytes();
    buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
    off += 4;
    off += 8; // flags + change

    // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
    let xdp_nla_len: u16 = 20;
    buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
    off += 2;
    let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
    buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
    off += 2;

    // IFLA_XDP_FD (len=8, type=1, value=prog_fd)
    let fd_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&fd_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FD.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&prog_fd.to_ne_bytes());
    off += 4;

    // IFLA_XDP_FLAGS (len=8, type=3, value=XDP_FLAGS_SKB_MODE)
    let flags_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&flags_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FLAGS.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&XDP_FLAGS_SKB_MODE.to_ne_bytes());

    let mut sa: libc::sockaddr_nl = unsafe { std::mem::zeroed() };
    sa.nl_family = libc::AF_NETLINK as u16;

    let ret = unsafe {
        libc::sendto(
            sock,
            buf.as_ptr() as *const _,
            total_len as usize,
            0,
            &sa as *const _ as *const _,
            std::mem::size_of::<libc::sockaddr_nl>() as u32,
        )
    };

    if ret < 0 {
        let err = io::Error::last_os_error();
        unsafe { libc::close(sock) };
        return Err(err);
    }

    // Read ACK.
    let mut resp = [0u8; 128];
    let n = unsafe { libc::recv(sock, resp.as_mut_ptr() as *mut _, resp.len(), 0) };
    unsafe { libc::close(sock) };

    if n < 0 {
        return Err(io::Error::last_os_error());
    }

    // Check for error in nlmsghdr.
    if n >= 16 {
        let nlmsg_type = u16::from_ne_bytes([resp[4], resp[5]]);
        if nlmsg_type == 2 {
            // NLMSG_ERROR
            let error = i32::from_ne_bytes([resp[16], resp[17], resp[18], resp[19]]);
            if error < 0 {
                return Err(io::Error::from_raw_os_error(-error));
            }
        }
    }

    Ok(())
}

fn detach_xdp(ifindex: u32) -> io::Result<()> {
    attach_xdp_netlink(ifindex, -1) // fd=-1 detaches
}

fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
    let mut buf = [0u8; 16];
    let ret = unsafe { libc::if_indextoname(ifindex, buf.as_mut_ptr() as *mut _) };
    if ret.is_null() {
        return Err(io::Error::last_os_error());
    }
    let end = buf.iter().position(|&b| b == 0).unwrap_or(16);
    String::from_utf8(buf[..end].to_vec())
        .map_err(|_| io::Error::new(io::ErrorKind::InvalidData, "invalid ifname"))
}

// ── Minimal ELF parser ──────────────────────────────────────────────

fn parse_and_patch_elf(elf: &[u8], map_fd: i32) -> io::Result<Vec<u8>> {
    if elf.len() < 64 || &elf[0..4] != b"\x7fELF" {
        return Err(io::Error::new(io::ErrorKind::InvalidData, "not an ELF"));
    }

    let e_type = u16::from_le_bytes([elf[16], elf[17]]);
    let e_machine = u16::from_le_bytes([elf[18], elf[19]]);
    if e_type != ET_REL || e_machine != EM_BPF {
        return Err(io::Error::new(
            io::ErrorKind::InvalidData,
            "not a BPF relocatable ELF",
        ));
    }

    let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;
    let e_shentsize = u16::from_le_bytes([elf[58], elf[59]]) as usize;
    let e_shnum = u16::from_le_bytes([elf[60], elf[61]]) as usize;
    let e_shstrndx = u16::from_le_bytes([elf[62], elf[63]]) as usize;

    let shstrtab_off = {
        let sh = &elf[e_shoff + e_shstrndx * e_shentsize..];
        u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize
    };

    // Collect section info.
    struct Section {
        name: String,
        sh_type: u32,
        offset: usize,
        size: usize,
        info: u32, // sh_info — for REL sections, points to the target section.
    }

    let mut sections = Vec::new();
    for i in 0..e_shnum {
        let sh = &elf[e_shoff + i * e_shentsize..];
        let sh_name_off = u32::from_le_bytes(sh[0..4].try_into().unwrap()) as usize;
        let sh_type = u32::from_le_bytes(sh[4..8].try_into().unwrap());
        let sh_offset = u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize;
        let sh_size = u64::from_le_bytes(sh[32..40].try_into().unwrap()) as usize;
        let sh_info = u32::from_le_bytes(sh[28..32].try_into().unwrap());
        // Wait, sh_info is at offset 44 in Elf64_Shdr. Let me recalculate.
        // Elf64_Shdr: name(4) type(4) flags(8) addr(8) offset(8) size(8) link(4) info(4) addralign(8) entsize(8)
        let sh_info = u32::from_le_bytes(sh[44..48].try_into().unwrap());

        let name_start = shstrtab_off + sh_name_off;
        let name_end = elf[name_start..]
            .iter()
            .position(|&b| b == 0)
            .unwrap_or(0)
            + name_start;
        let name = std::str::from_utf8(&elf[name_start..name_end])
            .unwrap_or("")
            .to_string();

        sections.push(Section {
            name,
            sh_type,
            offset: sh_offset,
            size: sh_size,
            info: sh_info,
        });
    }

    // Find the "xdp" program section.
    let prog_idx = sections
        .iter()
        .position(|s| s.name == "xdp" && s.sh_type == SHT_PROGBITS)
        .ok_or_else(|| {
            io::Error::new(io::ErrorKind::InvalidData, "no 'xdp' section")
        })?;
    let prog = &sections[prog_idx];
    let mut insns = elf[prog.offset..prog.offset + prog.size].to_vec();

    // Find relocation sections targeting the program section.
    // These tell us which LD_IMM64 instructions reference maps.
    for sec in &sections {
        if sec.sh_type != SHT_REL {
            continue;
        }
        if sec.info as usize != prog_idx {
            continue; // This relocation targets a different section.
        }

        // Process each relocation entry (Elf64_Rel: offset(8) + info(8) = 16 bytes).
        let rel_data = &elf[sec.offset..sec.offset + sec.size];
        let mut off = 0;
        while off + 16 <= rel_data.len() {
            let r_offset = u64::from_le_bytes(rel_data[off..off + 8].try_into().unwrap()) as usize;
            // r_info contains symbol index in upper 32 bits, type in lower 32.
            // For BPF map refs, the type is R_BPF_64_64 (1).

            // At r_offset in the instruction stream, set src_reg=1 (BPF_PSEUDO_MAP_FD)
            // and imm=map_fd.
            if r_offset + 16 <= insns.len() && insns[r_offset] == 0x18 {
                // Set src_reg to BPF_PSEUDO_MAP_FD (1).
                insns[r_offset + 1] = (insns[r_offset + 1] & 0x0f) | (1 << 4);
                // Set imm to map fd.
                insns[r_offset + 4..r_offset + 8]
                    .copy_from_slice(&(map_fd as u32).to_le_bytes());
                // Zero upper 32 bits of the 64-bit immediate.
                insns[r_offset + 12..r_offset + 16].copy_from_slice(&0u32.to_le_bytes());
            }

            off += 16;
        }
    }

    Ok(insns)
}

```

## Symbols to evaluate

- constant XDP_WG_OBJ (L14–L14)
- constant BPF_MAP_CREATE (L17–L17)
- constant BPF_PROG_LOAD (L18–L18)
- constant BPF_MAP_UPDATE_ELEM (L19–L19)
- constant BPF_MAP_TYPE_XSKMAP (L22–L22)
- constant BPF_PROG_TYPE_XDP (L25–L25)
- constant XDP_FLAGS_SKB_MODE (L28–L28)
- constant ET_REL (L31–L31)
- constant SHT_PROGBITS (L32–L32)
- constant SHT_REL (L33–L33)
- constant EM_BPF (L34–L34)
- export class XdpProgram (L37–L41)
- function bpf_create_xskmap (L93–L122)
- function bpf_prog_load (L124–L175)
- function bpf_map_update (L177–L206)
- function attach_xdp (L210–L233)
- function attach_xdp_netlink (L235–L342)
- function detach_xdp (L344–L346)
- function ifindex_to_name (L348–L357)
- function parse_and_patch_elf (L361–L470)

## Test File

No test file found for this source file.

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Evaluate the test quality for each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] TransportSession: tests NONE → WEAK**: Deliberation reclassified: UTILITY DEAD→USED: This is a `pub struct` in a `-core` library crate (`rustguard-core`). Library crate public types are consumed by downstream crates in the workspace (e.g., `rustguard`). The utility evaluator likely only searched within the same crate and missed cross-crate imports, a classic false positive for workspace library crates. Reclassified to USED. TESTS NONE→WEAK: The merged field says NONE but the detail text itself says WEAK, and the source clearly contains 6 test functions in a `#[cfg(test)]` module covering encrypt/decrypt roundtrip, counter monotonicity, wrong-counter rejection, replay rejection, out-of-order delivery, and bidirectional use. The WEAK assessment is accurate: `encrypt_to` and `decrypt_in_place` (separate code paths using `seal_to`/`open_to`) have zero coverage, and nonce exhaustion / edge cases are untested. DOCUMENTATION UNDOCUMENTED→DOCUMENTED: The detail text itself concludes DOCUMENTED. The struct has a thorough `///` block (lines 7–11) explaining purpose, derivation, and key-role convention. Both public fields and all private fields carry `///` comments. Four of six methods have doc comments. Two public items (`new`, `send_counter`) lack doc comments (noted in best_practices rule 9), but the overall coverage is well above UNDOCUMENTED. The struct-level and field-level documentation is comprehensive.
- **[deliberation] COOKIE_LEN: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD reclassified → USED: this is a `pub const` in a library crate (`rustguard-core`). Downstream crates (e.g., message layout code) depend on it; pre-computed analysis likely doesn't capture cross-crate workspace imports. The entire cookie module is clearly used infrastructure for a WireGuard implementation. Tests NONE contradicts the detail text which says '[GOOD] Pure constant. Used throughout and indirectly validated in every roundtrip test.' For a pure constant with no runtime behavior, indirect validation is sufficient — reclassified to GOOD. Documentation UNDOCUMENTED is wrong — line 23 has `/// Cookie size.` which IS a doc comment, making this PARTIAL (brief but present).
- **[deliberation] now: tests WEAK → GOOD**: Deliberation reclassified: Tests WEAK reclassified → GOOD: a 2-line wrapper around `Instant::now()` has nothing meaningful to test directly. It is called in every test via `CookieChecker::new()`. Requiring dedicated tests for this trivial wrapper is unreasonable. Documentation UNDOCUMENTED kept — no doc comment exists, though the function is private and self-descriptive.
- **[deliberation] CookieChecker: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD reclassified → USED: CookieChecker is the primary server-side public API of this module in a library crate. Pre-computed cross-crate analysis likely misses workspace-level imports. The entire module exists to support WireGuard cookie DoS protection — this struct is clearly intended for use by consuming crates. Tests NONE contradicts the detail text which says 'happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests' — reclassified to WEAK (tested but with gaps in verify_mac1, rotation, new_with). Documentation UNDOCUMENTED contradicts detail: 'Struct has a /// summary and all four fields have field-level /// comments.' Line 49 has a doc comment. Reclassified to PARTIAL (docs exist but missing examples and constructor docs).
- **[deliberation] CookieState: tests NONE → WEAK**: Deliberation reclassified: Correction NEEDS_FIX kept: unlike elapsed_since, the consequence here is that process_reply successfully decrypts a cookie but never marks it as fresh, making compute_mac2 silently return zeros on no_std. While the overall no_std design is intentional, this specific behavior is more insidious — a successful decryption followed by silent failure to use the cookie is a subtle trap with no comment explaining it. Utility DEAD → USED: same reasoning as CookieChecker — primary client-side public API in a library crate. Tests NONE → WEAK: detail says 'Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie.' Documentation UNDOCUMENTED → PARTIAL: detail says 'Struct has a /// summary and both private fields have doc comments.'
- **[deliberation] ReplayWindow: tests NONE → WEAK**: Deliberation reclassified: CORRECTION ERROR confirmed at high confidence: shift_window's bit-shift block (L112-116) iterates in reverse with right-shift (>>), moving bits toward lower ages instead of higher ages. Verified with concrete example: top=0, bitmap[0]=1, then shift_window(1) right-shifts bitmap[0] to 0, losing the counter-0 mark. A subsequent check(0) would incorrectly accept the replay. The word-level copy_within is correct; only the sub-word bit-shift is inverted. Tests don't catch this because they never re-check a previously-seen counter after a shift has occurred. UTILITY reclassified DEAD→USED: this is a pub struct in a library crate (rustguard-core). Library public API types are not dead simply because no in-tree consumer is visible — the struct is the module's primary export and is extensively exercised in tests. TESTS reclassified NONE→WEAK: the detail text itself says '[WEAK]' and describes solid behavioral coverage via check_and_update (sequential, duplicate rejection, out-of-order, boundary, stress). However, check() and update() are never tested independently, and the critical shift_window bit-direction bug goes undetected due to test design gaps. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the struct's two fields have /// doc comments (L15, L17), and all three public methods have /// doc comments. Only the struct-level definition and new() constructor lack /// docs. This is clearly partial, not absent.
- **[deliberation] hash: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive: this is a library crate's public API. Exported functions in a crypto library ARE the product — absence of in-workspace importers does not make them dead. The crate is designed to be consumed externally (rustguard-crypto). Tests NONE contradicts the detail text which describes three existing tests (hash_deterministic, hash_different_inputs, hash_multi_chunk); reclassified to WEAK since tests exist but lack reference vectors. Documentation UNDOCUMENTED contradicts the detail and source: line 8 has `/// BLAKE2s-256 hash.` — it's PARTIAL (brief but present, missing params/examples).
- **[deliberation] mac: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive for the same library-crate reason as hash. Tests NONE contradicts the detail text which describes mac_differs_with_key and indirect coverage via hmac_differs_from_keyed_blake2s; reclassified to WEAK (tests exist but no reference vectors). Documentation UNDOCUMENTED contradicts the source: lines 17-20 have a multi-line /// block explaining keyed MAC vs HMAC distinction. Reclassified to PARTIAL per the detail's own assessment.
- **[deliberation] hkdf: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive: hkdf is a core public function of a crypto library crate, essential for the WireGuard KDF. Tests NONE contradicts the detail which describes two tests (hkdf_produces_different_outputs, hkdf_deterministic); reclassified to WEAK since no RFC 5869/WireGuard spec test vectors are present. Documentation UNDOCUMENTED contradicts source: lines 31-36 have a substantial /// block describing the algorithm and return semantics. Reclassified to PARTIAL.
- **[deliberation] AEAD_TAG_LEN: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD→USED: This is a `pub const` in a library crate (`rustguard-crypto`). It is the fundamental tag length constant meant for downstream consumers. The evaluator itself noted it matches Known False Positives. Zero in-crate importers is expected for a library public API constant. Tests NONE→GOOD: Pure compile-time constant with no runtime behavior; indirectly validated in `seal_then_open` assertion (`ciphertext.len() == plaintext.len() + AEAD_TAG_LEN`). No dedicated test needed. Documentation UNDOCUMENTED confirmed: line 7 has no `///` comment, so UNDOCUMENTED is correct.
- **[deliberation] MAX_PACKET_SIZE: tests NONE → GOOD**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the doc comment on L9 says 'MTU 1420 + tag' but the expression computes `1500 + AEAD_TAG_LEN = 1516`, a clear mismatch (~80 bytes difference). Either the doc or the value is wrong. Utility DEAD→USED: library public constant for downstream consumption, same false positive pattern as AEAD_TAG_LEN. Tests NONE→GOOD: compile-time constant with no runtime behavior, no dedicated test required. Documentation UNDOCUMENTED→PARTIAL: line 9 has a `///` doc comment present (`/// Maximum WireGuard transport payload (MTU 1420 + tag).`), so UNDOCUMENTED is incorrect. Reclassified to PARTIAL due to the inaccurate MTU value in the doc.
- **[deliberation] seal: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD→USED: primary public encryption API of the library crate, consumed by downstream crates. Tests NONE→WEAK: `seal` is directly called in all 5 test functions (seal_then_open, wrong_key_fails, wrong_counter_fails, tampered_ciphertext_fails, aad_mismatch_fails), so NONE is factually incorrect. Reclassified to WEAK since edge cases (empty plaintext, large payloads, non-zero counter as primary input) are missing. Documentation UNDOCUMENTED→PARTIAL: has a multi-line `///` doc block on L12-15 describing purpose, nonce construction, and AAD semantics. Missing param-level docs and examples, hence PARTIAL not DOCUMENTED.
- **[deliberation] open: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD→USED: primary public decryption API of the library crate. Tests NONE→GOOD: five tests thoroughly exercise `open` — happy path, wrong key, wrong counter, tampered ciphertext, and AAD mismatch. All four primary authentication-failure modes are covered. NONE is clearly wrong. Documentation UNDOCUMENTED→PARTIAL: has a `///` block on L31-33 describing purpose and three failure conditions for `None` return. Missing param-level docs and examples, hence PARTIAL.
- **[deliberation] Tai64n: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD → USED: This is a `pub struct` in `rustguard-crypto`, a library crate whose entire purpose is to export types for cross-crate consumption. The evaluator itself noted this false-positive pattern and reduced confidence. Cross-crate imports were outside analysis scope, and similar symbols (TransportSession, CookieChecker) were previously misclassified. Reclassified with high confidence. Tests NONE → WEAK: The symbol field says NONE but the detail section itself begins with '[WEAK] Three inline tests cover monotonicity...' — there are clearly three tests in the `mod tests` block (L50-L80). They exist but only check roundtrip equality, never assert against known-correct byte encodings. WEAK is the accurate classification. Documentation UNDOCUMENTED → PARTIAL: Lines 4-7 contain four `///` doc comments on the struct covering purpose, byte layout, WireGuard use-case, and replay-monotonicity invariant. The detail section even states 'Struct carries four lines of /// doc comments — solid struct-level narrative.' The struct is documented; only `as_bytes` and `from_bytes` methods lack docs. PARTIAL is correct.
- **[deliberation] JoinConfig: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD → USED: JoinConfig is a public API struct serving as the sole parameter to the crate's primary entry point `run()`. The DEAD classification is a systematic false positive from cross-crate analysis not capturing workspace-internal consumers. This matches the documented false-positive pattern for library crate public types. Tests NONE → GOOD: The evaluator's own detail text says 'Per rule 6, types/interfaces/structs with no runtime behavior are GOOD by default' — this is a plain data struct with two fields and no methods, so GOOD is correct. Documentation UNDOCUMENTED kept: valid finding, public config type lacks /// doc comments and field descriptions.
- **[deliberation] ParsedUdp: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD → USED: This is a pub struct in a library crate returned by the public parse_eth_udp function. The original detail itself acknowledges this matches the Known False Positives pattern for library crate exports consumed by downstream crates. Tests NONE → WEAK: The detail text itself states 'The struct's two fields (src_addr, payload) are exercised by the single inline test parse_ipv4_udp_frame, which asserts both field values.' This is partial coverage (IPv4 path only, no IPv6), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Struct has a /// doc comment on L8' — the struct-level doc exists on line 8. Only the per-field docs are missing. That is PARTIAL, not UNDOCUMENTED.
- **[deliberation] parse_eth_udp: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD → USED: Public function in a library crate serving as the main entry point for Ethernet frame parsing. The original detail itself notes it matches the Known False Positives pattern for library crate public APIs consumed by downstream crates not visible in same-crate import analysis. Tests NONE → WEAK: The detail states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path through parse_eth_udp.' This is exercised but with limited coverage (no IPv6 branch, no error paths), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Has a two-line /// block (L14–L15) describing purpose and the None return condition.' A doc comment exists; it merely lacks #Examples and parameter descriptions. That is PARTIAL.
- **[deliberation] PersistedPeer: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD is a false positive: PersistedPeer is a pub struct in a library crate (rustguard-enroll) intended to be consumed by other workspace crates. Zero in-file importers is expected for a public API type — the evaluator only searched within the crate boundary. Tests reclassified from NONE to GOOD: the detail text itself states 'Both inline tests directly construct and assert on PersistedPeer instances, exercising both public fields' and concludes GOOD for a plain data struct — the NONE field value contradicts the detail. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 11 clearly shows `/// A persisted peer — just enough to re-enroll on restart.` and best_practices rule 9 confirms 'All public items carry /// doc comments'. Field-level docs are absent but struct-level doc exists.
- **[deliberation] save: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate meant for external consumers. Tests reclassified from NONE to WEAK: the detail text itself says 'Covered only by the save_and_load_roundtrip happy-path test with two peers' — the function IS called in the test at line 108, so NONE is factually incorrect. WEAK is appropriate given only the happy path is covered and error paths are untested. Documentation reclassified from UNDOCUMENTED to PARTIAL: lines 24-25 show a two-line /// doc comment describing purpose and format. It exists but lacks #Errors and parameter docs.
- **[deliberation] load: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate for external consumption. Tests reclassified from NONE to WEAK: both test functions call load() — save_and_load_roundtrip (line 109) and load_nonexistent_returns_empty (line 116). The detail text itself describes WEAK coverage with specific gaps (error paths untested). NONE is factually wrong. Documentation reclassified from UNDOCUMENTED to PARTIAL: line 48 shows `/// Load peers from the state file.` — the doc exists but is minimal, missing #Errors section and the important NotFound → Ok(vec![]) behavior.


## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "XDP_WG_OBJ",
      "line_start": 14,
      "line_end": 14,
      "tests": "GOOD",
      "confidence": 90,
      "detail": "Compile-time constant embedding binary bytes via include_bytes!. No runtime behaviour to test; correctness is enforced at compile time. Rule 6 applies."
    },
    {
      "name": "BPF_MAP_CREATE",
      "line_start": 17,
      "line_end": 17,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric constant representing a kernel ABI value. No runtime behaviour; correctness validated by the kernel refusing invalid commands. Rule 6 applies."
    },
    {
      "name": "BPF_PROG_LOAD",
      "line_start": 18,
      "line_end": 18,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric constant for BPF syscall command. No runtime behaviour; kernel enforces correctness at call site. Rule 6 applies."
    },
    {
      "name": "BPF_MAP_UPDATE_ELEM",
      "line_start": 19,
      "line_end": 19,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric constant for BPF syscall command. No runtime behaviour; correctness is structural. Rule 6 applies."
    },
    {
      "name": "BPF_MAP_TYPE_XSKMAP",
      "line_start": 22,
      "line_end": 22,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric constant representing a kernel BPF map type. No runtime behaviour of its own. Rule 6 applies."
    },
    {
      "name": "BPF_PROG_TYPE_XDP",
      "line_start": 25,
      "line_end": 25,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric constant for BPF program type. No runtime behaviour; kernel validates at load time. Rule 6 applies."
    },
    {
      "name": "XDP_FLAGS_SKB_MODE",
      "line_start": 28,
      "line_end": 28,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric constant for XDP attach flag (bit field). No runtime behaviour of its own. Rule 6 applies."
    },
    {
      "name": "ET_REL",
      "line_start": 31,
      "line_end": 31,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric ELF constant. No runtime behaviour; parse_and_patch_elf validates against it. Rule 6 applies."
    },
    {
      "name": "SHT_PROGBITS",
      "line_start": 32,
      "line_end": 32,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric ELF section-type constant. No runtime behaviour on its own. Rule 6 applies."
    },
    {
      "name": "SHT_REL",
      "line_start": 33,
      "line_end": 33,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric ELF section-type constant for relocation sections. No runtime behaviour of its own. Rule 6 applies."
    },
    {
      "name": "EM_BPF",
      "line_start": 34,
      "line_end": 34,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure numeric ELF machine-type constant. No runtime behaviour. Rule 6 applies."
    },
    {
      "name": "XdpProgram",
      "line_start": 37,
      "line_end": 41,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists for bpf_loader.rs. XdpProgram, its constructors (load_and_attach, register_xsk), and its Drop impl are completely untested. Critical Linux-only functionality with zero automated coverage."
    },
    {
      "name": "bpf_create_xskmap",
      "line_start": 93,
      "line_end": 122,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists. This private function issues a raw bpf() syscall to create an XSKMAP. No unit or integration tests cover success path, invalid max_entries, or error handling."
    },
    {
      "name": "bpf_prog_load",
      "line_start": 124,
      "line_end": 175,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists. Private function performing BPF program loading via syscall, including verifier log capture on failure. Zero test coverage of any code path."
    },
    {
      "name": "bpf_map_update",
      "line_start": 177,
      "line_end": 206,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists. Private function issuing BPF_MAP_UPDATE_ELEM syscall. Neither the happy path nor the error path (ret < 0) is tested."
    },
    {
      "name": "attach_xdp",
      "line_start": 210,
      "line_end": 233,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists. Private function that shells out to 'ip link set' with a fallback to attach_xdp_netlink. Neither path is tested; the fallback logic is completely unverified."
    },
    {
      "name": "attach_xdp_netlink",
      "line_start": 235,
      "line_end": 342,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists. This is the most complex function in the file — hand-crafted netlink RTM_SETLINK message assembly with manual byte offsets. The byte layout, ACK parsing, and error handling are entirely untested. High risk of silent corruption."
    },
    {
      "name": "detach_xdp",
      "line_start": 344,
      "line_end": 346,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists. Trivial wrapper calling attach_xdp_netlink with fd=-1. No test verifies that detach is successfully invoked on Drop or that it propagates errors correctly."
    },
    {
      "name": "ifindex_to_name",
      "line_start": 348,
      "line_end": 357,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists. Private function wrapping libc::if_indextoname. Neither the success path, the null-return error path, nor the UTF-8 error path is tested."
    },
    {
      "name": "parse_and_patch_elf",
      "line_start": 361,
      "line_end": 470,
      "tests": "NONE",
      "confidence": 95,
      "detail": "No test file exists. This is a pure function (takes &[u8] and an i32, returns Vec<u8>) that is fully unit-testable without kernel access. It handles ELF header validation, section enumeration, relocation patching, and LD_IMM64 rewriting. None of these paths — including the non-ELF error, wrong machine type, missing 'xdp' section, or relocation patching — are tested at all. This is the most critical gap in the file."
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.1769 | **Duration:** 51.8s


---

# Axis: best_practices

## User

## File: `rustguard-tun/src/bpf_loader.rs`
## Language: rust
## Context: general

```rust
//! Minimal BPF ELF loader for the XDP WireGuard filter.
//!
//! Loads the pre-compiled xdp_wg.o, creates the XSKMAP,
//! patches the map fd into the program, loads it, and attaches to an interface.
//!
//! No libbpf. No aya. Just raw bpf() syscalls and ELF parsing.
//! The .o is 1.4KB and never changes.

#![cfg(target_os = "linux")]

use std::io;

/// The pre-compiled BPF object.
const XDP_WG_OBJ: &[u8] = include_bytes!("../bpf/xdp_wg.o");

// BPF syscall commands.
const BPF_MAP_CREATE: u32 = 0;
const BPF_PROG_LOAD: u32 = 5;
const BPF_MAP_UPDATE_ELEM: u32 = 2;

// BPF map types.
const BPF_MAP_TYPE_XSKMAP: u32 = 17;

// BPF program types.
const BPF_PROG_TYPE_XDP: u32 = 6;

// XDP attach flags.
const XDP_FLAGS_SKB_MODE: u32 = 1 << 1;

// ELF constants.
const ET_REL: u16 = 1;
const SHT_PROGBITS: u32 = 1;
const SHT_REL: u32 = 9;
const EM_BPF: u16 = 247;

/// A loaded XDP program with its XSKMAP.
pub struct XdpProgram {
    pub prog_fd: i32,
    pub xsks_map_fd: i32,
    ifindex: u32,
}

impl XdpProgram {
    /// Load the XDP WireGuard filter and attach it to an interface.
    pub fn load_and_attach(ifname: &str) -> io::Result<Self> {
        let ifindex = super::xdp::if_nametoindex(ifname)?;

        // 1. Create the XSKMAP.
        let xsks_map_fd = bpf_create_xskmap(64)?;

        // 2. Parse ELF, extract program bytecode, patch map references.
        let insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;
        eprintln!("  BPF: parsed ELF, {} insn bytes, map_fd={}", insns.len(), xsks_map_fd);

        // 3. Load BPF program.
        let prog_fd = bpf_prog_load(&insns).map_err(|e| {
            io::Error::new(e.kind(), format!("prog_load ({} insns): {e}", insns.len() / 8))
        })?;
        eprintln!("  BPF: program loaded, prog_fd={}", prog_fd);

        // 4. Attach to interface.
        attach_xdp(ifindex, prog_fd).map_err(|e| {
            io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
        })?;
        eprintln!("  BPF: attached to ifindex={}", ifindex);

        Ok(Self {
            prog_fd,
            xsks_map_fd,
            ifindex,
        })
    }

    /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
    pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
        bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
    }
}

impl Drop for XdpProgram {
    fn drop(&mut self) {
        // Detach XDP program from interface.
        let _ = detach_xdp(self.ifindex);
        unsafe {
            libc::close(self.prog_fd);
            libc::close(self.xsks_map_fd);
        }
    }
}

// ── BPF syscalls ────────────────────────────────────────────────────

fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
    #[repr(C)]
    struct BpfAttrMapCreate {
        map_type: u32,
        key_size: u32,
        value_size: u32,
        max_entries: u32,
    }

    let attr = BpfAttrMapCreate {
        map_type: BPF_MAP_TYPE_XSKMAP,
        key_size: 4,
        value_size: 4,
        max_entries,
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_CREATE,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapCreate>(),
        )
    } as i32;

    if fd < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(fd)
}

fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
    let license = b"GPL\0";
    let mut log_buf = vec![0u8; 65536];

    // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
    // We need at least the fields up to kern_version, padded to the
    // minimum size the kernel accepts. Using 256 bytes to be safe.
    #[repr(C)]
    struct BpfAttrProgLoad {
        prog_type: u32,
        insn_cnt: u32,
        insns: u64,
        license: u64,
        log_level: u32,
        log_size: u32,
        log_buf: u64,
        kern_version: u32,
        _pad: [u8; 208], // Pad to 256 bytes total.
    }

    let attr = BpfAttrProgLoad {
        prog_type: BPF_PROG_TYPE_XDP,
        insn_cnt: (insns.len() / 8) as u32,
        insns: insns.as_ptr() as u64,
        license: license.as_ptr() as u64,
        log_level: 1,
        log_size: log_buf.len() as u32,
        log_buf: log_buf.as_mut_ptr() as u64,
        kern_version: 0,
        _pad: [0; 208],
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_PROG_LOAD,
            &attr as *const _,
            std::mem::size_of::<BpfAttrProgLoad>(),
        )
    } as i32;

    if fd < 0 {
        let err = io::Error::last_os_error();
        let log_end = log_buf.iter().position(|&b| b == 0).unwrap_or(0);
        if log_end > 0 {
            let log_str = String::from_utf8_lossy(&log_buf[..log_end]);
            eprintln!("  BPF verifier log:\n{log_str}");
        }
        return Err(err);
    }
    Ok(fd)
}

fn bpf_map_update(map_fd: i32, key: &u32, value: &i32) -> io::Result<()> {
    #[repr(C)]
    struct BpfAttrMapElem {
        map_fd: u32,
        key: u64,
        value: u64,
        flags: u64,
    }

    let attr = BpfAttrMapElem {
        map_fd: map_fd as u32,
        key: key as *const _ as u64,
        value: value as *const _ as u64,
        flags: 0, // BPF_ANY
    };

    let ret = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_UPDATE_ELEM,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapElem>(),
        )
    };

    if ret < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(())
}

// ── XDP attach/detach via netlink ───────────────────────────────────

fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Use the SIOCETHTOOL / netlink approach, but the simplest way
    // that works on all kernels is via the if_link netlink interface.
    // For simplicity, shell out to `ip link set dev <name> xdpgeneric obj ...`
    // But we already have the fd. Use the bpf_set_link_xdp_fd equivalent.

    // Direct approach: set XDP fd via netlink RTM_SETLINK.
    // This is complex. Simpler: use the deprecated but working SIOCSIFXDP.
    // Actually simplest: use libc's if_nametoindex + raw netlink.

    // For now, use the ip command — it works and avoids 200 lines of netlink.
    let ifname = ifindex_to_name(ifindex)?;
    let fd_path = format!("/proc/self/fd/{prog_fd}");
    let output = std::process::Command::new("ip")
        .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
        .output();

    // Fallback: use the bpf netlink API via raw netlink socket.
    // If ip command doesn't support pinned fd, try bpf_xdp_attach.
    match output {
        Ok(out) if out.status.success() => Ok(()),
        _ => attach_xdp_netlink(ifindex, prog_fd),
    }
}

fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Netlink RTM_SETLINK with IFLA_XDP attribute.
    // This is the proper way — no shelling out.
    use std::os::unix::io::FromRawFd;

    let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
    if sock < 0 {
        return Err(io::Error::last_os_error());
    }

    // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
    const RTM_SETLINK: u16 = 19;
    const NLM_F_REQUEST: u16 = 1;
    const NLM_F_ACK: u16 = 4;
    const IFLA_XDP: u16 = 43;
    const IFLA_XDP_FD: u16 = 1;
    const IFLA_XDP_FLAGS: u16 = 3;

    // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
    let mut buf = [0u8; 64];
    let mut off = 0;

    // nlmsghdr
    let total_len: u32 = 52;
    buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
    off += 4;
    buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
    off += 2;
    off += 8; // seq + pid = 0

    // ifinfomsg
    buf[off] = libc::AF_UNSPEC as u8; // family
    off += 4; // family + pad + type
    let idx_bytes = ifindex.to_ne_bytes();
    buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
    off += 4;
    off += 8; // flags + change

    // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
    let xdp_nla_len: u16 = 20;
    buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
    off += 2;
    let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
    buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
    off += 2;

    // IFLA_XDP_FD (len=8, type=1, value=prog_fd)
    let fd_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&fd_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FD.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&prog_fd.to_ne_bytes());
    off += 4;

    // IFLA_XDP_FLAGS (len=8, type=3, value=XDP_FLAGS_SKB_MODE)
    let flags_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&flags_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FLAGS.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&XDP_FLAGS_SKB_MODE.to_ne_bytes());

    let mut sa: libc::sockaddr_nl = unsafe { std::mem::zeroed() };
    sa.nl_family = libc::AF_NETLINK as u16;

    let ret = unsafe {
        libc::sendto(
            sock,
            buf.as_ptr() as *const _,
            total_len as usize,
            0,
            &sa as *const _ as *const _,
            std::mem::size_of::<libc::sockaddr_nl>() as u32,
        )
    };

    if ret < 0 {
        let err = io::Error::last_os_error();
        unsafe { libc::close(sock) };
        return Err(err);
    }

    // Read ACK.
    let mut resp = [0u8; 128];
    let n = unsafe { libc::recv(sock, resp.as_mut_ptr() as *mut _, resp.len(), 0) };
    unsafe { libc::close(sock) };

    if n < 0 {
        return Err(io::Error::last_os_error());
    }

    // Check for error in nlmsghdr.
    if n >= 16 {
        let nlmsg_type = u16::from_ne_bytes([resp[4], resp[5]]);
        if nlmsg_type == 2 {
            // NLMSG_ERROR
            let error = i32::from_ne_bytes([resp[16], resp[17], resp[18], resp[19]]);
            if error < 0 {
                return Err(io::Error::from_raw_os_error(-error));
            }
        }
    }

    Ok(())
}

fn detach_xdp(ifindex: u32) -> io::Result<()> {
    attach_xdp_netlink(ifindex, -1) // fd=-1 detaches
}

fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
    let mut buf = [0u8; 16];
    let ret = unsafe { libc::if_indextoname(ifindex, buf.as_mut_ptr() as *mut _) };
    if ret.is_null() {
        return Err(io::Error::last_os_error());
    }
    let end = buf.iter().position(|&b| b == 0).unwrap_or(16);
    String::from_utf8(buf[..end].to_vec())
        .map_err(|_| io::Error::new(io::ErrorKind::InvalidData, "invalid ifname"))
}

// ── Minimal ELF parser ──────────────────────────────────────────────

fn parse_and_patch_elf(elf: &[u8], map_fd: i32) -> io::Result<Vec<u8>> {
    if elf.len() < 64 || &elf[0..4] != b"\x7fELF" {
        return Err(io::Error::new(io::ErrorKind::InvalidData, "not an ELF"));
    }

    let e_type = u16::from_le_bytes([elf[16], elf[17]]);
    let e_machine = u16::from_le_bytes([elf[18], elf[19]]);
    if e_type != ET_REL || e_machine != EM_BPF {
        return Err(io::Error::new(
            io::ErrorKind::InvalidData,
            "not a BPF relocatable ELF",
        ));
    }

    let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;
    let e_shentsize = u16::from_le_bytes([elf[58], elf[59]]) as usize;
    let e_shnum = u16::from_le_bytes([elf[60], elf[61]]) as usize;
    let e_shstrndx = u16::from_le_bytes([elf[62], elf[63]]) as usize;

    let shstrtab_off = {
        let sh = &elf[e_shoff + e_shstrndx * e_shentsize..];
        u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize
    };

    // Collect section info.
    struct Section {
        name: String,
        sh_type: u32,
        offset: usize,
        size: usize,
        info: u32, // sh_info — for REL sections, points to the target section.
    }

    let mut sections = Vec::new();
    for i in 0..e_shnum {
        let sh = &elf[e_shoff + i * e_shentsize..];
        let sh_name_off = u32::from_le_bytes(sh[0..4].try_into().unwrap()) as usize;
        let sh_type = u32::from_le_bytes(sh[4..8].try_into().unwrap());
        let sh_offset = u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize;
        let sh_size = u64::from_le_bytes(sh[32..40].try_into().unwrap()) as usize;
        let sh_info = u32::from_le_bytes(sh[28..32].try_into().unwrap());
        // Wait, sh_info is at offset 44 in Elf64_Shdr. Let me recalculate.
        // Elf64_Shdr: name(4) type(4) flags(8) addr(8) offset(8) size(8) link(4) info(4) addralign(8) entsize(8)
        let sh_info = u32::from_le_bytes(sh[44..48].try_into().unwrap());

        let name_start = shstrtab_off + sh_name_off;
        let name_end = elf[name_start..]
            .iter()
            .position(|&b| b == 0)
            .unwrap_or(0)
            + name_start;
        let name = std::str::from_utf8(&elf[name_start..name_end])
            .unwrap_or("")
            .to_string();

        sections.push(Section {
            name,
            sh_type,
            offset: sh_offset,
            size: sh_size,
            info: sh_info,
        });
    }

    // Find the "xdp" program section.
    let prog_idx = sections
        .iter()
        .position(|s| s.name == "xdp" && s.sh_type == SHT_PROGBITS)
        .ok_or_else(|| {
            io::Error::new(io::ErrorKind::InvalidData, "no 'xdp' section")
        })?;
    let prog = &sections[prog_idx];
    let mut insns = elf[prog.offset..prog.offset + prog.size].to_vec();

    // Find relocation sections targeting the program section.
    // These tell us which LD_IMM64 instructions reference maps.
    for sec in &sections {
        if sec.sh_type != SHT_REL {
            continue;
        }
        if sec.info as usize != prog_idx {
            continue; // This relocation targets a different section.
        }

        // Process each relocation entry (Elf64_Rel: offset(8) + info(8) = 16 bytes).
        let rel_data = &elf[sec.offset..sec.offset + sec.size];
        let mut off = 0;
        while off + 16 <= rel_data.len() {
            let r_offset = u64::from_le_bytes(rel_data[off..off + 8].try_into().unwrap()) as usize;
            // r_info contains symbol index in upper 32 bits, type in lower 32.
            // For BPF map refs, the type is R_BPF_64_64 (1).

            // At r_offset in the instruction stream, set src_reg=1 (BPF_PSEUDO_MAP_FD)
            // and imm=map_fd.
            if r_offset + 16 <= insns.len() && insns[r_offset] == 0x18 {
                // Set src_reg to BPF_PSEUDO_MAP_FD (1).
                insns[r_offset + 1] = (insns[r_offset + 1] & 0x0f) | (1 << 4);
                // Set imm to map fd.
                insns[r_offset + 4..r_offset + 8]
                    .copy_from_slice(&(map_fd as u32).to_le_bytes());
                // Zero upper 32 bits of the 64-bit immediate.
                insns[r_offset + 12..r_offset + 16].copy_from_slice(&0u32.to_le_bytes());
            }

            off += 16;
        }
    }

    Ok(insns)
}

```

## File stats
- Lines: 470
- Symbols: 20
- Exported symbols: 1

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Use the project structure above to evaluate rule 11 (import organization) and detect file placement inconsistencies. If the file is in an unexpected directory given the project conventions, flag it as WARN for the most relevant rule.

Evaluate all 17 rules and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "score": 2.0,
  "rules": [
    {
      "rule_id": 1,
      "rule_name": "No unwrap in production code",
      "status": "FAIL",
      "severity": "CRITICAL",
      "detail": "Multiple .try_into().unwrap() calls in parse_and_patch_elf operate on externally-supplied byte slices (even though the ELF is compiled in, the function signature accepts &[u8]). Violations: elf[40..48].try_into().unwrap(), sh[24..32].try_into().unwrap(), sh[0..4].try_into().unwrap(), sh[4..8].try_into().unwrap(), sh[32..40].try_into().unwrap(), sh[44..48].try_into().unwrap(), rel_data[off..off+8].try_into().unwrap(). Each panics on malformed input. The .unwrap_or() usages are acceptable.",
      "lines": "L355-L450"
    },
    {
      "rule_id": 2,
      "rule_name": "No unsafe blocks without clear justification comment",
      "status": "FAIL",
      "severity": "CRITICAL",
      "detail": "The file contains over 15 unsafe blocks (libc::syscall for BPF, libc::close, libc::socket, libc::sendto, libc::recv, libc::if_indextoname, std::mem::zeroed) and none carry a // SAFETY: comment explaining invariants upheld by the caller. The module-level doc mentions raw syscalls but does not substitute for per-block // SAFETY: annotations required by Rust best practice. Additionally, use std::os::unix::io::FromRawFd is imported in attach_xdp_netlink but never used, suggesting an incomplete RAII refactor was abandoned.",
      "lines": "L85-L340"
    },
    {
      "rule_id": 3,
      "rule_name": "Proper error handling with Result/Option (no silent ignores)",
      "status": "WARN",
      "severity": "HIGH",
      "detail": "In Drop::drop, let _ = detach_xdp(self.ifindex) silently discards the detach error. While it is impossible to propagate errors from Drop, at minimum an eprintln! or a log::warn! call should surface the failure, consistent with the diagnostic eprintln! calls used elsewhere in this file.",
      "lines": "L85-L93"
    },
    {
      "rule_id": 4,
      "rule_name": "Derive common traits on public types",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "pub struct XdpProgram carries no derive attributes. Debug would be immediately useful for diagnostics (printing prog_fd, xsks_map_fd, ifindex). Clone and PartialEq are intentionally unsuitable here (they would alias raw FDs), but the absence of Debug on a public type is a clear omission.",
      "lines": "L36-L42"
    },
    {
      "rule_id": 5,
      "rule_name": "Lifetime annotations explicit where needed",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "All functions that accept references use simple, unambiguous lifetime elision rules (single-input references, no multiple output lifetimes). No explicit lifetime annotations are needed here and none are missing."
    },
    {
      "rule_id": 6,
      "rule_name": "Use clippy idioms",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "Two clippy-detectable issues: (1) use std::os::unix::io::FromRawFd is imported inside attach_xdp_netlink but never used — dead import. (2) In parse_and_patch_elf, sh_info is bound twice in the same loop body: first from sh[28..32] (wrong offset) then immediately shadowed by sh[44..48] (correct offset). The first binding is dead code and will trigger a clippy::shadow_unrelated or unused_variable warning.",
      "lines": "L232-L240"
    },
    {
      "rule_id": 7,
      "rule_name": "No panic! in library/production code",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No explicit panic!(), todo!(), unimplemented!(), or unreachable!() macro invocations are present. Implicit panics from .unwrap() are covered under Rule 1."
    },
    {
      "rule_id": 8,
      "rule_name": "Module organization",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "The file is correctly placed in rustguard-tun/src/ alongside linux.rs, xdp.rs, and linux_mq.rs — all Linux-specific TUN/XDP helpers. Internal helpers are private (non-pub), the public surface is minimal (XdpProgram + two methods), and sections are clearly delineated with // ── comments. No re-export concerns for this leaf module."
    },
    {
      "rule_id": 9,
      "rule_name": "Documentation comments on public items",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "The module, XdpProgram, load_and_attach, and register_xsk all have /// doc comments. However, the two public fields pub prog_fd: i32 and pub xsks_map_fd: i32 are undocumented. Callers need to know these are kernel file descriptors managed by the struct's Drop impl and should not be closed externally.",
      "lines": "L37-L40"
    },
    {
      "rule_id": 10,
      "rule_name": "No hardcoded secrets or credentials",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No secrets, API keys, passwords, or credentials present. All constants are standard Linux kernel interface values (BPF command codes, ELF machine types, netlink attribute IDs)."
    },
    {
      "rule_id": 11,
      "rule_name": "Memory safety (no leaks via mem::forget, proper Drop impls)",
      "status": "FAIL",
      "severity": "HIGH",
      "detail": "Resource leak in load_and_attach error paths: xsks_map_fd is created via bpf_create_xskmap before prog_fd exists. If parse_and_patch_elf or bpf_prog_load fails, xsks_map_fd is leaked (no Drop impl is reached because XdpProgram was never constructed). If attach_xdp fails, both xsks_map_fd and prog_fd are leaked. The Drop impl on XdpProgram is correct, but it only runs if the struct is successfully constructed. A scopeguard/RAII wrapper or explicit cleanup in each error branch is required.",
      "lines": "L44-L75"
    },
    {
      "rule_id": 12,
      "rule_name": "Concurrency safety (no data races, proper Send/Sync bounds)",
      "status": "PASS",
      "severity": "HIGH",
      "detail": "XdpProgram contains only i32 and u32 fields, so the compiler auto-derives Send + Sync, which is correct for a file-descriptor holder without interior mutability. The netlink socket in attach_xdp_netlink is created, used, and closed within a single function scope with no sharing. No Mutex, Arc, or thread-crossing patterns are used incorrectly."
    }
  ],
  "suggestions": [
    {
      "description": "Replace .try_into().unwrap() with bounds-checked error propagation in parse_and_patch_elf",
      "before": "let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;",
      "after": "let e_shoff = u64::from_le_bytes(\n    elf.get(40..48)\n        .and_then(|s| s.try_into().ok())\n        .ok_or_else(|| io::Error::new(io::ErrorKind::InvalidData, \"ELF header truncated\"))?\n) as usize;"
    },
    {
      "description": "Add // SAFETY: comments to every unsafe block documenting the invariants upheld",
      "before": "let fd = unsafe {\n    libc::syscall(\n        libc::SYS_bpf,\n        BPF_MAP_CREATE,\n        &attr as *const _,\n        std::mem::size_of::<BpfAttrMapCreate>(),\n    )\n} as i32;",
      "after": "// SAFETY: `attr` is a valid, fully-initialized BpfAttrMapCreate whose size matches\n// the third argument. SYS_bpf is a stable Linux syscall. No aliasing occurs.\nlet fd = unsafe {\n    libc::syscall(\n        libc::SYS_bpf,\n        BPF_MAP_CREATE,\n        &attr as *const _,\n        std::mem::size_of::<BpfAttrMapCreate>(),\n    )\n} as i32;"
    },
    {
      "description": "Fix file-descriptor leak in load_and_attach by cleaning up on error paths",
      "before": "let xsks_map_fd = bpf_create_xskmap(64)?;\nlet insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;\nlet prog_fd = bpf_prog_load(&insns)...?;\nattach_xdp(ifindex, prog_fd)...?;\nOk(Self { prog_fd, xsks_map_fd, ifindex })",
      "after": "let xsks_map_fd = bpf_create_xskmap(64)?;\nlet insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd).map_err(|e| {\n    unsafe { libc::close(xsks_map_fd); }\n    e\n})?;\nlet prog_fd = bpf_prog_load(&insns).map_err(|e| {\n    unsafe { libc::close(xsks_map_fd); }\n    io::Error::new(e.kind(), format!(\"prog_load: {e}\"))\n})?;\nattach_xdp(ifindex, prog_fd).map_err(|e| {\n    unsafe { libc::close(prog_fd); libc::close(xsks_map_fd); }\n    io::Error::new(e.kind(), format!(\"xdp_attach ifindex={ifindex}: {e}\"))\n})?;\nOk(Self { prog_fd, xsks_map_fd, ifindex })"
    },
    {
      "description": "Derive Debug on XdpProgram and document public fields",
      "before": "pub struct XdpProgram {\n    pub prog_fd: i32,\n    pub xsks_map_fd: i32,\n    ifindex: u32,\n}",
      "after": "#[derive(Debug)]\npub struct XdpProgram {\n    /// Kernel file descriptor for the loaded XDP BPF program. Managed by Drop; do not close externally.\n    pub prog_fd: i32,\n    /// Kernel file descriptor for the XSKMAP used to register AF_XDP sockets. Managed by Drop.\n    pub xsks_map_fd: i32,\n    ifindex: u32,\n}"
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.3393 | **Duration:** 633.5s


---

# Axis: documentation

## User

## File: `rustguard-tun/src/bpf_loader.rs`
## Language: rust

```rust
//! Minimal BPF ELF loader for the XDP WireGuard filter.
//!
//! Loads the pre-compiled xdp_wg.o, creates the XSKMAP,
//! patches the map fd into the program, loads it, and attaches to an interface.
//!
//! No libbpf. No aya. Just raw bpf() syscalls and ELF parsing.
//! The .o is 1.4KB and never changes.

#![cfg(target_os = "linux")]

use std::io;

/// The pre-compiled BPF object.
const XDP_WG_OBJ: &[u8] = include_bytes!("../bpf/xdp_wg.o");

// BPF syscall commands.
const BPF_MAP_CREATE: u32 = 0;
const BPF_PROG_LOAD: u32 = 5;
const BPF_MAP_UPDATE_ELEM: u32 = 2;

// BPF map types.
const BPF_MAP_TYPE_XSKMAP: u32 = 17;

// BPF program types.
const BPF_PROG_TYPE_XDP: u32 = 6;

// XDP attach flags.
const XDP_FLAGS_SKB_MODE: u32 = 1 << 1;

// ELF constants.
const ET_REL: u16 = 1;
const SHT_PROGBITS: u32 = 1;
const SHT_REL: u32 = 9;
const EM_BPF: u16 = 247;

/// A loaded XDP program with its XSKMAP.
pub struct XdpProgram {
    pub prog_fd: i32,
    pub xsks_map_fd: i32,
    ifindex: u32,
}

impl XdpProgram {
    /// Load the XDP WireGuard filter and attach it to an interface.
    pub fn load_and_attach(ifname: &str) -> io::Result<Self> {
        let ifindex = super::xdp::if_nametoindex(ifname)?;

        // 1. Create the XSKMAP.
        let xsks_map_fd = bpf_create_xskmap(64)?;

        // 2. Parse ELF, extract program bytecode, patch map references.
        let insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;
        eprintln!("  BPF: parsed ELF, {} insn bytes, map_fd={}", insns.len(), xsks_map_fd);

        // 3. Load BPF program.
        let prog_fd = bpf_prog_load(&insns).map_err(|e| {
            io::Error::new(e.kind(), format!("prog_load ({} insns): {e}", insns.len() / 8))
        })?;
        eprintln!("  BPF: program loaded, prog_fd={}", prog_fd);

        // 4. Attach to interface.
        attach_xdp(ifindex, prog_fd).map_err(|e| {
            io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
        })?;
        eprintln!("  BPF: attached to ifindex={}", ifindex);

        Ok(Self {
            prog_fd,
            xsks_map_fd,
            ifindex,
        })
    }

    /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
    pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
        bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
    }
}

impl Drop for XdpProgram {
    fn drop(&mut self) {
        // Detach XDP program from interface.
        let _ = detach_xdp(self.ifindex);
        unsafe {
            libc::close(self.prog_fd);
            libc::close(self.xsks_map_fd);
        }
    }
}

// ── BPF syscalls ────────────────────────────────────────────────────

fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
    #[repr(C)]
    struct BpfAttrMapCreate {
        map_type: u32,
        key_size: u32,
        value_size: u32,
        max_entries: u32,
    }

    let attr = BpfAttrMapCreate {
        map_type: BPF_MAP_TYPE_XSKMAP,
        key_size: 4,
        value_size: 4,
        max_entries,
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_CREATE,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapCreate>(),
        )
    } as i32;

    if fd < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(fd)
}

fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
    let license = b"GPL\0";
    let mut log_buf = vec![0u8; 65536];

    // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
    // We need at least the fields up to kern_version, padded to the
    // minimum size the kernel accepts. Using 256 bytes to be safe.
    #[repr(C)]
    struct BpfAttrProgLoad {
        prog_type: u32,
        insn_cnt: u32,
        insns: u64,
        license: u64,
        log_level: u32,
        log_size: u32,
        log_buf: u64,
        kern_version: u32,
        _pad: [u8; 208], // Pad to 256 bytes total.
    }

    let attr = BpfAttrProgLoad {
        prog_type: BPF_PROG_TYPE_XDP,
        insn_cnt: (insns.len() / 8) as u32,
        insns: insns.as_ptr() as u64,
        license: license.as_ptr() as u64,
        log_level: 1,
        log_size: log_buf.len() as u32,
        log_buf: log_buf.as_mut_ptr() as u64,
        kern_version: 0,
        _pad: [0; 208],
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_PROG_LOAD,
            &attr as *const _,
            std::mem::size_of::<BpfAttrProgLoad>(),
        )
    } as i32;

    if fd < 0 {
        let err = io::Error::last_os_error();
        let log_end = log_buf.iter().position(|&b| b == 0).unwrap_or(0);
        if log_end > 0 {
            let log_str = String::from_utf8_lossy(&log_buf[..log_end]);
            eprintln!("  BPF verifier log:\n{log_str}");
        }
        return Err(err);
    }
    Ok(fd)
}

fn bpf_map_update(map_fd: i32, key: &u32, value: &i32) -> io::Result<()> {
    #[repr(C)]
    struct BpfAttrMapElem {
        map_fd: u32,
        key: u64,
        value: u64,
        flags: u64,
    }

    let attr = BpfAttrMapElem {
        map_fd: map_fd as u32,
        key: key as *const _ as u64,
        value: value as *const _ as u64,
        flags: 0, // BPF_ANY
    };

    let ret = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_UPDATE_ELEM,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapElem>(),
        )
    };

    if ret < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(())
}

// ── XDP attach/detach via netlink ───────────────────────────────────

fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Use the SIOCETHTOOL / netlink approach, but the simplest way
    // that works on all kernels is via the if_link netlink interface.
    // For simplicity, shell out to `ip link set dev <name> xdpgeneric obj ...`
    // But we already have the fd. Use the bpf_set_link_xdp_fd equivalent.

    // Direct approach: set XDP fd via netlink RTM_SETLINK.
    // This is complex. Simpler: use the deprecated but working SIOCSIFXDP.
    // Actually simplest: use libc's if_nametoindex + raw netlink.

    // For now, use the ip command — it works and avoids 200 lines of netlink.
    let ifname = ifindex_to_name(ifindex)?;
    let fd_path = format!("/proc/self/fd/{prog_fd}");
    let output = std::process::Command::new("ip")
        .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
        .output();

    // Fallback: use the bpf netlink API via raw netlink socket.
    // If ip command doesn't support pinned fd, try bpf_xdp_attach.
    match output {
        Ok(out) if out.status.success() => Ok(()),
        _ => attach_xdp_netlink(ifindex, prog_fd),
    }
}

fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Netlink RTM_SETLINK with IFLA_XDP attribute.
    // This is the proper way — no shelling out.
    use std::os::unix::io::FromRawFd;

    let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
    if sock < 0 {
        return Err(io::Error::last_os_error());
    }

    // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
    const RTM_SETLINK: u16 = 19;
    const NLM_F_REQUEST: u16 = 1;
    const NLM_F_ACK: u16 = 4;
    const IFLA_XDP: u16 = 43;
    const IFLA_XDP_FD: u16 = 1;
    const IFLA_XDP_FLAGS: u16 = 3;

    // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
    let mut buf = [0u8; 64];
    let mut off = 0;

    // nlmsghdr
    let total_len: u32 = 52;
    buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
    off += 4;
    buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
    off += 2;
    off += 8; // seq + pid = 0

    // ifinfomsg
    buf[off] = libc::AF_UNSPEC as u8; // family
    off += 4; // family + pad + type
    let idx_bytes = ifindex.to_ne_bytes();
    buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
    off += 4;
    off += 8; // flags + change

    // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
    let xdp_nla_len: u16 = 20;
    buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
    off += 2;
    let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
    buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
    off += 2;

    // IFLA_XDP_FD (len=8, type=1, value=prog_fd)
    let fd_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&fd_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FD.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&prog_fd.to_ne_bytes());
    off += 4;

    // IFLA_XDP_FLAGS (len=8, type=3, value=XDP_FLAGS_SKB_MODE)
    let flags_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&flags_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FLAGS.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&XDP_FLAGS_SKB_MODE.to_ne_bytes());

    let mut sa: libc::sockaddr_nl = unsafe { std::mem::zeroed() };
    sa.nl_family = libc::AF_NETLINK as u16;

    let ret = unsafe {
        libc::sendto(
            sock,
            buf.as_ptr() as *const _,
            total_len as usize,
            0,
            &sa as *const _ as *const _,
            std::mem::size_of::<libc::sockaddr_nl>() as u32,
        )
    };

    if ret < 0 {
        let err = io::Error::last_os_error();
        unsafe { libc::close(sock) };
        return Err(err);
    }

    // Read ACK.
    let mut resp = [0u8; 128];
    let n = unsafe { libc::recv(sock, resp.as_mut_ptr() as *mut _, resp.len(), 0) };
    unsafe { libc::close(sock) };

    if n < 0 {
        return Err(io::Error::last_os_error());
    }

    // Check for error in nlmsghdr.
    if n >= 16 {
        let nlmsg_type = u16::from_ne_bytes([resp[4], resp[5]]);
        if nlmsg_type == 2 {
            // NLMSG_ERROR
            let error = i32::from_ne_bytes([resp[16], resp[17], resp[18], resp[19]]);
            if error < 0 {
                return Err(io::Error::from_raw_os_error(-error));
            }
        }
    }

    Ok(())
}

fn detach_xdp(ifindex: u32) -> io::Result<()> {
    attach_xdp_netlink(ifindex, -1) // fd=-1 detaches
}

fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
    let mut buf = [0u8; 16];
    let ret = unsafe { libc::if_indextoname(ifindex, buf.as_mut_ptr() as *mut _) };
    if ret.is_null() {
        return Err(io::Error::last_os_error());
    }
    let end = buf.iter().position(|&b| b == 0).unwrap_or(16);
    String::from_utf8(buf[..end].to_vec())
        .map_err(|_| io::Error::new(io::ErrorKind::InvalidData, "invalid ifname"))
}

// ── Minimal ELF parser ──────────────────────────────────────────────

fn parse_and_patch_elf(elf: &[u8], map_fd: i32) -> io::Result<Vec<u8>> {
    if elf.len() < 64 || &elf[0..4] != b"\x7fELF" {
        return Err(io::Error::new(io::ErrorKind::InvalidData, "not an ELF"));
    }

    let e_type = u16::from_le_bytes([elf[16], elf[17]]);
    let e_machine = u16::from_le_bytes([elf[18], elf[19]]);
    if e_type != ET_REL || e_machine != EM_BPF {
        return Err(io::Error::new(
            io::ErrorKind::InvalidData,
            "not a BPF relocatable ELF",
        ));
    }

    let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;
    let e_shentsize = u16::from_le_bytes([elf[58], elf[59]]) as usize;
    let e_shnum = u16::from_le_bytes([elf[60], elf[61]]) as usize;
    let e_shstrndx = u16::from_le_bytes([elf[62], elf[63]]) as usize;

    let shstrtab_off = {
        let sh = &elf[e_shoff + e_shstrndx * e_shentsize..];
        u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize
    };

    // Collect section info.
    struct Section {
        name: String,
        sh_type: u32,
        offset: usize,
        size: usize,
        info: u32, // sh_info — for REL sections, points to the target section.
    }

    let mut sections = Vec::new();
    for i in 0..e_shnum {
        let sh = &elf[e_shoff + i * e_shentsize..];
        let sh_name_off = u32::from_le_bytes(sh[0..4].try_into().unwrap()) as usize;
        let sh_type = u32::from_le_bytes(sh[4..8].try_into().unwrap());
        let sh_offset = u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize;
        let sh_size = u64::from_le_bytes(sh[32..40].try_into().unwrap()) as usize;
        let sh_info = u32::from_le_bytes(sh[28..32].try_into().unwrap());
        // Wait, sh_info is at offset 44 in Elf64_Shdr. Let me recalculate.
        // Elf64_Shdr: name(4) type(4) flags(8) addr(8) offset(8) size(8) link(4) info(4) addralign(8) entsize(8)
        let sh_info = u32::from_le_bytes(sh[44..48].try_into().unwrap());

        let name_start = shstrtab_off + sh_name_off;
        let name_end = elf[name_start..]
            .iter()
            .position(|&b| b == 0)
            .unwrap_or(0)
            + name_start;
        let name = std::str::from_utf8(&elf[name_start..name_end])
            .unwrap_or("")
            .to_string();

        sections.push(Section {
            name,
            sh_type,
            offset: sh_offset,
            size: sh_size,
            info: sh_info,
        });
    }

    // Find the "xdp" program section.
    let prog_idx = sections
        .iter()
        .position(|s| s.name == "xdp" && s.sh_type == SHT_PROGBITS)
        .ok_or_else(|| {
            io::Error::new(io::ErrorKind::InvalidData, "no 'xdp' section")
        })?;
    let prog = &sections[prog_idx];
    let mut insns = elf[prog.offset..prog.offset + prog.size].to_vec();

    // Find relocation sections targeting the program section.
    // These tell us which LD_IMM64 instructions reference maps.
    for sec in &sections {
        if sec.sh_type != SHT_REL {
            continue;
        }
        if sec.info as usize != prog_idx {
            continue; // This relocation targets a different section.
        }

        // Process each relocation entry (Elf64_Rel: offset(8) + info(8) = 16 bytes).
        let rel_data = &elf[sec.offset..sec.offset + sec.size];
        let mut off = 0;
        while off + 16 <= rel_data.len() {
            let r_offset = u64::from_le_bytes(rel_data[off..off + 8].try_into().unwrap()) as usize;
            // r_info contains symbol index in upper 32 bits, type in lower 32.
            // For BPF map refs, the type is R_BPF_64_64 (1).

            // At r_offset in the instruction stream, set src_reg=1 (BPF_PSEUDO_MAP_FD)
            // and imm=map_fd.
            if r_offset + 16 <= insns.len() && insns[r_offset] == 0x18 {
                // Set src_reg to BPF_PSEUDO_MAP_FD (1).
                insns[r_offset + 1] = (insns[r_offset + 1] & 0x0f) | (1 << 4);
                // Set imm to map fd.
                insns[r_offset + 4..r_offset + 8]
                    .copy_from_slice(&(map_fd as u32).to_le_bytes());
                // Zero upper 32 bits of the 64-bit immediate.
                insns[r_offset + 12..r_offset + 16].copy_from_slice(&0u32.to_le_bytes());
            }

            off += 16;
        }
    }

    Ok(insns)
}

```

## Symbols to evaluate

- constant XDP_WG_OBJ (L14–L14)
- constant BPF_MAP_CREATE (L17–L17)
- constant BPF_PROG_LOAD (L18–L18)
- constant BPF_MAP_UPDATE_ELEM (L19–L19)
- constant BPF_MAP_TYPE_XSKMAP (L22–L22)
- constant BPF_PROG_TYPE_XDP (L25–L25)
- constant XDP_FLAGS_SKB_MODE (L28–L28)
- constant ET_REL (L31–L31)
- constant SHT_PROGBITS (L32–L32)
- constant SHT_REL (L33–L33)
- constant EM_BPF (L34–L34)
- export class XdpProgram (L37–L41)
- function bpf_create_xskmap (L93–L122)
- function bpf_prog_load (L124–L175)
- function bpf_map_update (L177–L206)
- function attach_xdp (L210–L233)
- function attach_xdp_netlink (L235–L342)
- function detach_xdp (L344–L346)
- function ifindex_to_name (L348–L357)
- function parse_and_patch_elf (L361–L470)

## Documentation Directory

No /docs/ directory found — evaluate JSDoc only, skip concept coverage.

Evaluate documentation for each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] TransportSession: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: UTILITY DEAD→USED: This is a `pub struct` in a `-core` library crate (`rustguard-core`). Library crate public types are consumed by downstream crates in the workspace (e.g., `rustguard`). The utility evaluator likely only searched within the same crate and missed cross-crate imports, a classic false positive for workspace library crates. Reclassified to USED. TESTS NONE→WEAK: The merged field says NONE but the detail text itself says WEAK, and the source clearly contains 6 test functions in a `#[cfg(test)]` module covering encrypt/decrypt roundtrip, counter monotonicity, wrong-counter rejection, replay rejection, out-of-order delivery, and bidirectional use. The WEAK assessment is accurate: `encrypt_to` and `decrypt_in_place` (separate code paths using `seal_to`/`open_to`) have zero coverage, and nonce exhaustion / edge cases are untested. DOCUMENTATION UNDOCUMENTED→DOCUMENTED: The detail text itself concludes DOCUMENTED. The struct has a thorough `///` block (lines 7–11) explaining purpose, derivation, and key-role convention. Both public fields and all private fields carry `///` comments. Four of six methods have doc comments. Two public items (`new`, `send_counter`) lack doc comments (noted in best_practices rule 9), but the overall coverage is well above UNDOCUMENTED. The struct-level and field-level documentation is comprehensive.
- **[deliberation] COOKIE_LEN: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD reclassified → USED: this is a `pub const` in a library crate (`rustguard-core`). Downstream crates (e.g., message layout code) depend on it; pre-computed analysis likely doesn't capture cross-crate workspace imports. The entire cookie module is clearly used infrastructure for a WireGuard implementation. Tests NONE contradicts the detail text which says '[GOOD] Pure constant. Used throughout and indirectly validated in every roundtrip test.' For a pure constant with no runtime behavior, indirect validation is sufficient — reclassified to GOOD. Documentation UNDOCUMENTED is wrong — line 23 has `/// Cookie size.` which IS a doc comment, making this PARTIAL (brief but present).
- **[deliberation] CookieChecker: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD reclassified → USED: CookieChecker is the primary server-side public API of this module in a library crate. Pre-computed cross-crate analysis likely misses workspace-level imports. The entire module exists to support WireGuard cookie DoS protection — this struct is clearly intended for use by consuming crates. Tests NONE contradicts the detail text which says 'happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests' — reclassified to WEAK (tested but with gaps in verify_mac1, rotation, new_with). Documentation UNDOCUMENTED contradicts detail: 'Struct has a /// summary and all four fields have field-level /// comments.' Line 49 has a doc comment. Reclassified to PARTIAL (docs exist but missing examples and constructor docs).
- **[deliberation] CookieState: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX kept: unlike elapsed_since, the consequence here is that process_reply successfully decrypts a cookie but never marks it as fresh, making compute_mac2 silently return zeros on no_std. While the overall no_std design is intentional, this specific behavior is more insidious — a successful decryption followed by silent failure to use the cookie is a subtle trap with no comment explaining it. Utility DEAD → USED: same reasoning as CookieChecker — primary client-side public API in a library crate. Tests NONE → WEAK: detail says 'Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie.' Documentation UNDOCUMENTED → PARTIAL: detail says 'Struct has a /// summary and both private fields have doc comments.'
- **[deliberation] ReplayWindow: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: CORRECTION ERROR confirmed at high confidence: shift_window's bit-shift block (L112-116) iterates in reverse with right-shift (>>), moving bits toward lower ages instead of higher ages. Verified with concrete example: top=0, bitmap[0]=1, then shift_window(1) right-shifts bitmap[0] to 0, losing the counter-0 mark. A subsequent check(0) would incorrectly accept the replay. The word-level copy_within is correct; only the sub-word bit-shift is inverted. Tests don't catch this because they never re-check a previously-seen counter after a shift has occurred. UTILITY reclassified DEAD→USED: this is a pub struct in a library crate (rustguard-core). Library public API types are not dead simply because no in-tree consumer is visible — the struct is the module's primary export and is extensively exercised in tests. TESTS reclassified NONE→WEAK: the detail text itself says '[WEAK]' and describes solid behavioral coverage via check_and_update (sequential, duplicate rejection, out-of-order, boundary, stress). However, check() and update() are never tested independently, and the critical shift_window bit-direction bug goes undetected due to test design gaps. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the struct's two fields have /// doc comments (L15, L17), and all three public methods have /// doc comments. Only the struct-level definition and new() constructor lack /// docs. This is clearly partial, not absent.
- **[deliberation] do_handshake: documentation PARTIAL → DOCUMENTED**: Deliberation reclassified: Documentation PARTIAL is too strict for a private test helper. The doc comment '/// Helper: run a full handshake between two peers, return their sessions.' clearly states purpose and return semantics. The return type signature `(TransportSession, TransportSession)` is self-documenting — the variable names at every call site (`(init, resp)` or `(init_session, resp_session)`) make tuple ordering unambiguous. All other test symbols received DOCUMENTED under test-module leniency; a helper used exclusively by those tests deserves the same leniency. Requiring `# Examples` or field-by-field tuple documentation for a 20-line private test helper is unreasonable. Reclassified to DOCUMENTED.
- **[deliberation] BLAKE2S_BLOCK_SIZE: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Documentation UNDOCUMENTED reclassified to DOCUMENTED. This is a private constant named BLAKE2S_BLOCK_SIZE with value 64 — the name is self-documenting for anyone with basic cryptographic knowledge. The original evaluator even acknowledged it is 'tolerable.' For a private constant whose name IS the specification reference, requiring a doc comment is excessive.
- **[deliberation] hash: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: this is a library crate's public API. Exported functions in a crypto library ARE the product — absence of in-workspace importers does not make them dead. The crate is designed to be consumed externally (rustguard-crypto). Tests NONE contradicts the detail text which describes three existing tests (hash_deterministic, hash_different_inputs, hash_multi_chunk); reclassified to WEAK since tests exist but lack reference vectors. Documentation UNDOCUMENTED contradicts the detail and source: line 8 has `/// BLAKE2s-256 hash.` — it's PARTIAL (brief but present, missing params/examples).
- **[deliberation] mac: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive for the same library-crate reason as hash. Tests NONE contradicts the detail text which describes mac_differs_with_key and indirect coverage via hmac_differs_from_keyed_blake2s; reclassified to WEAK (tests exist but no reference vectors). Documentation UNDOCUMENTED contradicts the source: lines 17-20 have a multi-line /// block explaining keyed MAC vs HMAC distinction. Reclassified to PARTIAL per the detail's own assessment.
- **[deliberation] hkdf: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: hkdf is a core public function of a crypto library crate, essential for the WireGuard KDF. Tests NONE contradicts the detail which describes two tests (hkdf_produces_different_outputs, hkdf_deterministic); reclassified to WEAK since no RFC 5869/WireGuard spec test vectors are present. Documentation UNDOCUMENTED contradicts source: lines 31-36 have a substantial /// block describing the algorithm and return semantics. Reclassified to PARTIAL.
- **[deliberation] MAX_PACKET_SIZE: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the doc comment on L9 says 'MTU 1420 + tag' but the expression computes `1500 + AEAD_TAG_LEN = 1516`, a clear mismatch (~80 bytes difference). Either the doc or the value is wrong. Utility DEAD→USED: library public constant for downstream consumption, same false positive pattern as AEAD_TAG_LEN. Tests NONE→GOOD: compile-time constant with no runtime behavior, no dedicated test required. Documentation UNDOCUMENTED→PARTIAL: line 9 has a `///` doc comment present (`/// Maximum WireGuard transport payload (MTU 1420 + tag).`), so UNDOCUMENTED is incorrect. Reclassified to PARTIAL due to the inaccurate MTU value in the doc.
- **[deliberation] seal: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: primary public encryption API of the library crate, consumed by downstream crates. Tests NONE→WEAK: `seal` is directly called in all 5 test functions (seal_then_open, wrong_key_fails, wrong_counter_fails, tampered_ciphertext_fails, aad_mismatch_fails), so NONE is factually incorrect. Reclassified to WEAK since edge cases (empty plaintext, large payloads, non-zero counter as primary input) are missing. Documentation UNDOCUMENTED→PARTIAL: has a multi-line `///` doc block on L12-15 describing purpose, nonce construction, and AAD semantics. Missing param-level docs and examples, hence PARTIAL not DOCUMENTED.
- **[deliberation] open: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: primary public decryption API of the library crate. Tests NONE→GOOD: five tests thoroughly exercise `open` — happy path, wrong key, wrong counter, tampered ciphertext, and AAD mismatch. All four primary authentication-failure modes are covered. NONE is clearly wrong. Documentation UNDOCUMENTED→PARTIAL: has a `///` block on L31-33 describing purpose and three failure conditions for `None` return. Missing param-level docs and examples, hence PARTIAL.
- **[deliberation] xseal: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: public API variant for WireGuard cookie encryption, consumed by downstream crates. Tests NONE confirmed: no test covers xseal — this is a genuine gap on a security-critical path. Documentation UNDOCUMENTED→PARTIAL: has a single-line `///` on L48 noting algorithm, nonce size, and use case. Minimal but present, so UNDOCUMENTED is incorrect.
- **[deliberation] xopen: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: public API variant for WireGuard cookie decryption, consumed by downstream crates. Tests NONE confirmed: no test covers xopen — roundtrip, wrong key, tampered data, and nonce mismatch all untested. Documentation UNDOCUMENTED→PARTIAL: has a single-line `///` on L57 noting algorithm, nonce size, and use case. Present but lacks param docs and return semantics.
- **[deliberation] seal_to: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: zero-allocation public API for hot-path transport encryption, consumed by downstream crates. Tests NONE confirmed: no test covers seal_to — output length, correct tag placement, and buffer semantics all unverified. Documentation UNDOCUMENTED→PARTIAL: has a three-line `///` block on L66-68 describing purpose, buffer precondition, and zero-allocation property. Missing `# Panics` section for `.expect()` and param-level docs.
- **[deliberation] open_to: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the function guards `ct_len < AEAD_TAG_LEN` (defensive intent) but never checks `ct_len > buf.len()`. Since `buf[..ct_len]` on L91 will panic if ct_len exceeds buf length, and this function processes potentially adversarial input while advertising Option return, the missing upper-bound guard is a genuine inconsistency. Utility DEAD→USED: zero-allocation public API for hot-path transport decryption. Tests NONE confirmed: completely untested. Documentation UNDOCUMENTED→PARTIAL: has a three-line `///` block on L81-83 describing purpose, buffer contract, and zero-allocation guarantee.
- **[deliberation] Tai64n: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD → USED: This is a `pub struct` in `rustguard-crypto`, a library crate whose entire purpose is to export types for cross-crate consumption. The evaluator itself noted this false-positive pattern and reduced confidence. Cross-crate imports were outside analysis scope, and similar symbols (TransportSession, CookieChecker) were previously misclassified. Reclassified with high confidence. Tests NONE → WEAK: The symbol field says NONE but the detail section itself begins with '[WEAK] Three inline tests cover monotonicity...' — there are clearly three tests in the `mod tests` block (L50-L80). They exist but only check roundtrip equality, never assert against known-correct byte encodings. WEAK is the accurate classification. Documentation UNDOCUMENTED → PARTIAL: Lines 4-7 contain four `///` doc comments on the struct covering purpose, byte layout, WireGuard use-case, and replay-monotonicity invariant. The detail section even states 'Struct carries four lines of /// doc comments — solid struct-level narrative.' The struct is documented; only `as_bytes` and `from_bytes` methods lack docs. PARTIAL is correct.
- **[deliberation] TAI64_EPOCH_OFFSET: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Tests WEAK: Kept as-is. The constant is only exercised through roundtrip tests that would pass even with an incorrect value. A test asserting `from_unix(0,0).as_bytes()[..8]` against the known hex value would be the minimal correctness check but is absent. However, as a private constant this is lower severity. Documentation UNDOCUMENTED → DOCUMENTED: Line 11 contains `/// TAI is ahead of UTC by 10 seconds (1970 epoch) + 2^62 offset.` — a `///` doc comment directly above the constant that clearly explains the composition of the hex literal (10-second TAI-UTC offset + 2^62 base). The evaluator's own detail acknowledges the name is 'moderately self-descriptive' and the struct-level docs provide indirect context. With an explicit doc comment present, UNDOCUMENTED is incorrect. Confidence raised from 60 to 88 since the doc comment is plainly visible in the source.
- **[deliberation] KEEPALIVE_TIMEOUT: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Utility DEAD → LOW_VALUE: this is an exported WireGuard protocol constant (§6.3). The needs_keepalive method deliberately takes keepalive_interval as an Option<Duration> parameter, making KEEPALIVE_TIMEOUT the canonical default value for external callers to pass in. For a library crate, exported protocol constants serve as public API even without internal usage — downstream consumers may use it. Removing it would break the API contract. Tests NONE: stands — no test references it. Documentation: the source clearly has '/// Keepalive interval when configured.' on L51, and the detail text itself says '[DOCUMENTED]'. The UNDOCUMENTED classification contradicts the evaluator's own detail — reclassify to DOCUMENTED.
- **[deliberation] Peer: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD → USED: The detail itself acknowledges this matches a known false-positive pattern for exported structs in workspace crates. `Peer` is the fundamental domain struct for a WireGuard daemon — it is inconceivable that a daemon named `rustguard-daemon` does not use its own Peer representation. The pre-analysis hedged with 'LIKELY DEAD', and the analysis simply lacked visibility into sibling modules (device.rs, handshake.rs, etc.) that inevitably consume this type. Reclassified with high confidence. Tests NONE: Agreed — no tests exist for this module or its methods (from_config, allows_ip, has_active_session). Kept as-is. Documentation UNDOCUMENTED → PARTIAL: The source clearly shows a struct-level `///` doc comment at L15, plus field-level `///` docs on `session` (L22) and `timers` (L24). Five fields remain undocumented and `from_config` lacks a doc comment, so coverage is incomplete but not absent. PARTIAL is the correct classification per the detail's own description.
- **[deliberation] run: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX confirmed: (1) MSG_RESPONSE peer-matching at L296-301 uses a heuristic that breaks with multiple peers — the first peer without an active session is selected regardless of which peer the handshake was for. (2) Timer keepalive nonce exhaustion at ~L340 continues without clearing the session, preventing rekeying. Both are real bugs visible in the source. Utility reclassified DEAD→USED: this is `pub fn run()`, the sole public entry point of the daemon tunnel module. It is almost certainly called from main.rs or a binary crate entry point. The pre-computed analysis likely did not trace binary crate call graphs. A pub fn named 'run' in a daemon module being dead code would be nonsensical. Overengineering ACCEPTABLE kept — large but justified orchestration function. Tests NONE accurate. Documentation reclassified UNDOCUMENTED→PARTIAL: the function does have a /// doc comment ('Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.') as visible at L48, so it is not fully undocumented; it is partial (missing parameter/error docs).
- **[deliberation] ParsedUdp: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD → USED: This is a pub struct in a library crate returned by the public parse_eth_udp function. The original detail itself acknowledges this matches the Known False Positives pattern for library crate exports consumed by downstream crates. Tests NONE → WEAK: The detail text itself states 'The struct's two fields (src_addr, payload) are exercised by the single inline test parse_ipv4_udp_frame, which asserts both field values.' This is partial coverage (IPv4 path only, no IPv6), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Struct has a /// doc comment on L8' — the struct-level doc exists on line 8. Only the per-field docs are missing. That is PARTIAL, not UNDOCUMENTED.
- **[deliberation] parse_eth_udp: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD → USED: Public function in a library crate serving as the main entry point for Ethernet frame parsing. The original detail itself notes it matches the Known False Positives pattern for library crate public APIs consumed by downstream crates not visible in same-crate import analysis. Tests NONE → WEAK: The detail states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path through parse_eth_udp.' This is exercised but with limited coverage (no IPv6 branch, no error paths), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Has a two-line /// block (L14–L15) describing purpose and the None return condition.' A doc comment exists; it merely lacks #Examples and parameter descriptions. That is PARTIAL.
- **[deliberation] ENROLL_REQUEST_MAGIC: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant with a self-descriptive name and an inline comment showing the ASCII interpretation ('RGE\x01'). The evaluator itself noted 'tolerated under leniency rules' for private items. Reclassifying to DOCUMENTED — private items with clear naming and inline comments meet the bar.
- **[deliberation] ENROLL_RESPONSE_MAGIC: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Same rationale as ENROLL_REQUEST_MAGIC: private constant with self-descriptive name and inline ASCII comment. Evaluator acknowledged leniency applies. Reclassifying to DOCUMENTED.
- **[deliberation] random_nonce: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Tests WEAK: only tested indirectly through build functions, acceptable for a private thin wrapper around getrandom but still technically weak. Documentation reclassified to DOCUMENTED: private 4-line function with a self-descriptive name and trivial body (getrandom fill + expect). The evaluator explicitly noted 'tolerated under leniency rules' for private items. No `///` needed here.
- **[deliberation] BATCH_SIZE: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: BATCH_SIZE is actively used within this file in RecvBatch struct field types (L20-22), for-loop bounds, and array initializations in recv_batch. It is part of the module's coherent public API. Static analysis missed intra-module usage. Documentation UNDOCUMENTED→PARTIAL: Source clearly shows `/// Maximum batch size for recvmmsg/sendmmsg.` at L13; the detail text itself states PARTIAL. The top-level UNDOCUMENTED classification contradicts the detail. Tests NONE kept: factual, though low concern for a compile-time constant.
- **[deliberation] PKT_BUF_SIZE: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: PKT_BUF_SIZE is used in RecvBatch struct definition (L20) and recv_batch buffer allocation (L50, L113). Actively consumed within the file. Documentation UNDOCUMENTED→PARTIAL: Source shows `/// Per-packet buffer size.` at L15; detail text confirms PARTIAL. Tests NONE kept: factual, low concern for a constant.
- **[deliberation] RecvBatch: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: RecvBatch is used as parameter type in both recv_batch function signatures (L40, L113). It is the core data structure of this module's API. Documentation UNDOCUMENTED→PARTIAL: Source has `/// A batch of received packets.` at L18; detail text confirms PARTIAL (struct doc exists but fields lack individual docs). Tests NONE kept: struct and constructor have zero coverage, which is a legitimate gap for a public type with unsafe-adjacent usage patterns.
- **[deliberation] recv_batch: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: This is the macOS recv_batch at L112-L126. Utility DEAD→USED: macOS fallback is the platform-specific implementation consumers will call; cfg-gating makes static import analysis unreliable. Documentation UNDOCUMENTED→PARTIAL: Source shows `/// macOS fallback: receive one packet at a time.` at L110; detail text confirms PARTIAL. Tests NONE kept: WouldBlock and single-packet paths need coverage.
- **[deliberation] PersistedPeer: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: PersistedPeer is a pub struct in a library crate (rustguard-enroll) intended to be consumed by other workspace crates. Zero in-file importers is expected for a public API type — the evaluator only searched within the crate boundary. Tests reclassified from NONE to GOOD: the detail text itself states 'Both inline tests directly construct and assert on PersistedPeer instances, exercising both public fields' and concludes GOOD for a plain data struct — the NONE field value contradicts the detail. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 11 clearly shows `/// A persisted peer — just enough to re-enroll on restart.` and best_practices rule 9 confirms 'All public items carry /// doc comments'. Field-level docs are absent but struct-level doc exists.
- **[deliberation] default_state_path: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate, designed for external consumers to call for the default path. No in-crate callers is expected — tests use temp dirs instead, and the binary crate would call this. Tests NONE is accurate: no test directly invokes this function, though for a trivial one-liner the risk is minimal. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 19 shows `/// Default state file path.` — the doc exists but is minimal (no examples, no platform notes).
- **[deliberation] save: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate meant for external consumers. Tests reclassified from NONE to WEAK: the detail text itself says 'Covered only by the save_and_load_roundtrip happy-path test with two peers' — the function IS called in the test at line 108, so NONE is factually incorrect. WEAK is appropriate given only the happy path is covered and error paths are untested. Documentation reclassified from UNDOCUMENTED to PARTIAL: lines 24-25 show a two-line /// doc comment describing purpose and format. It exists but lacks #Errors and parameter docs.
- **[deliberation] load: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate for external consumption. Tests reclassified from NONE to WEAK: both test functions call load() — save_and_load_roundtrip (line 109) and load_nonexistent_returns_empty (line 116). The detail text itself describes WEAK coverage with specific gaps (error paths untested). NONE is factually wrong. Documentation reclassified from UNDOCUMENTED to PARTIAL: line 48 shows `/// Load peers from the state file.` — the doc exists but is minimal, missing #Errors section and the important NotFound → Ok(vec![]) behavior.
- **[deliberation] WINDOW_SIZE: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Documentation reclassified: private constant with a perfectly self-descriptive name (`WINDOW_SIZE`) whose value (2048) is directly referenced in the module-level `//!` doc comment ('2048-bit bitmap'). Under private-item leniency, this qualifies as DOCUMENTED. The original evaluator itself noted it is 'tolerated under private-item leniency rules,' contradicting its own UNDOCUMENTED classification.
- **[deliberation] BITMAP_LEN: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Documentation reclassified: private constant with a self-descriptive name and an inline comment (`// 32 u64s`) explaining the derived value. The derivation expression `(WINDOW_SIZE / 64) as usize` is itself self-documenting. Under private-item leniency for non-exported constants, this is adequately documented.


## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "XDP_WG_OBJ",
      "line_start": 14,
      "line_end": 14,
      "documentation": "DOCUMENTED",
      "confidence": 75,
      "detail": "Has `/// The pre-compiled BPF object.` at L13. Private constant; one-line doc is sufficient under private-item leniency. Name and doc together are self-explanatory."
    },
    {
      "name": "BPF_MAP_CREATE",
      "line_start": 17,
      "line_end": 17,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; only a section-header `// BPF syscall commands.` applies. Private constant mirroring a Linux kernel enum value. Tolerated under private-item leniency due to self-descriptive name."
    },
    {
      "name": "BPF_PROG_LOAD",
      "line_start": 18,
      "line_end": 18,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; falls under the same `// BPF syscall commands.` section header as BPF_MAP_CREATE. Private constant tolerated under leniency rules."
    },
    {
      "name": "BPF_MAP_UPDATE_ELEM",
      "line_start": 19,
      "line_end": 19,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; shares `// BPF syscall commands.` section comment. Private constant tolerated under private-item leniency."
    },
    {
      "name": "BPF_MAP_TYPE_XSKMAP",
      "line_start": 22,
      "line_end": 22,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; only has `// BPF map types.` section header at L21. Private constant tolerated under leniency; name mirrors kernel enum directly."
    },
    {
      "name": "BPF_PROG_TYPE_XDP",
      "line_start": 25,
      "line_end": 25,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; covered only by `// BPF program types.` section header at L24. Private constant tolerated under private-item leniency."
    },
    {
      "name": "XDP_FLAGS_SKB_MODE",
      "line_start": 28,
      "line_end": 28,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; only `// XDP attach flags.` section header at L27. Private constant; the bit-shift expression `1 << 1` is not explained. Tolerated under leniency."
    },
    {
      "name": "ET_REL",
      "line_start": 31,
      "line_end": 31,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; grouped under `// ELF constants.` at L30. Private constant mirroring ELF spec value. Tolerated under private-item leniency."
    },
    {
      "name": "SHT_PROGBITS",
      "line_start": 32,
      "line_end": 32,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; grouped under `// ELF constants.` section header. Private constant tolerated under leniency."
    },
    {
      "name": "SHT_REL",
      "line_start": 33,
      "line_end": 33,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; grouped under `// ELF constants.` section header. Private constant tolerated under leniency."
    },
    {
      "name": "EM_BPF",
      "line_start": 34,
      "line_end": 34,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment; grouped under `// ELF constants.` section header. Private constant tolerated under leniency; numeric value 247 matches ELF EM_BPF but is unexplained."
    },
    {
      "name": "XdpProgram",
      "line_start": 37,
      "line_end": 41,
      "documentation": "PARTIAL",
      "confidence": 88,
      "detail": "Has struct-level `/// A loaded XDP program with its XSKMAP.` at L36. However, both public fields `prog_fd` and `xsks_map_fd` lack `///` field-level doc comments explaining their semantics (e.g., ownership, validity guarantees). Methods `load_and_attach` and `register_xsk` also carry only brief single-line docs with no `# Examples` sections on this public API."
    },
    {
      "name": "bpf_create_xskmap",
      "line_start": 93,
      "line_end": 122,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment. Private function; tolerated under private-item leniency. The `max_entries` parameter and returned fd semantics are undocumented, but the name and context within the file are clear."
    },
    {
      "name": "bpf_prog_load",
      "line_start": 124,
      "line_end": 175,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment. Private function with substantial complexity (verifier log handling, padded struct layout). Inline `//` comments partially compensate, but no `///` doc exists. Tolerated under private-item leniency."
    },
    {
      "name": "bpf_map_update",
      "line_start": 177,
      "line_end": 206,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment. Private function; name and signature are fairly self-descriptive. The `flags: 0 // BPF_ANY` inline comment aids clarity. Tolerated under private-item leniency."
    },
    {
      "name": "attach_xdp",
      "line_start": 210,
      "line_end": 233,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment. Private function with a non-trivial fallback strategy (ip command → netlink). The strategy is described via `//` inline comments, but not via `///` docs. Tolerated under private-item leniency."
    },
    {
      "name": "attach_xdp_netlink",
      "line_start": 235,
      "line_end": 342,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment. Private function implementing raw netlink RTM_SETLINK; the complex byte-layout logic has inline `//` comments. No doc comment describing the contract (fd=-1 detach semantics or error conditions). Tolerated under private-item leniency."
    },
    {
      "name": "detach_xdp",
      "line_start": 344,
      "line_end": 346,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment. One-liner with an inline `// fd=-1 detaches` comment explaining the mechanism. Private function tolerated under leniency; the semantics are communicated via the inline comment."
    },
    {
      "name": "ifindex_to_name",
      "line_start": 348,
      "line_end": 357,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment. Private function with a self-descriptive name. Error path (null return from if_indextoname, UTF-8 failure) is not documented. Tolerated under private-item leniency."
    },
    {
      "name": "parse_and_patch_elf",
      "line_start": 361,
      "line_end": 470,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "No `///` doc comment. Private function with significant complexity (ELF parsing, relocation patching). The relocation patching strategy and BPF_PSEUDO_MAP_FD encoding are described inline via `//` comments but not via `///` docs. Tolerated under private-item leniency."
    }
  ],
  "docs_coverage": {
    "concepts": [],
    "score_pct": 18
  }
}

## rate_limit_event

## Result (success)

**Cost:** $0.3171 | **Duration:** 115.8s


---

# Deliberation Pass

## User

## Merged ReviewFile (from 7 axis evaluators)

```json
{
  "version": 2,
  "file": "rustguard-tun/src/bpf_loader.rs",
  "is_generated": false,
  "language": "rust",
  "parse_method": "ast",
  "verdict": "NEEDS_REFACTOR",
  "symbols": [
    {
      "name": "XDP_WG_OBJ",
      "kind": "constant",
      "exported": false,
      "line_start": 14,
      "line_end": 14,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 75,
      "detail": "[USED] Passed to parse_and_patch_elf at L68; included as pre-compiled BPF bytecode | [UNIQUE] Constant holding pre-compiled BPF object bytecode; no similar symbols found in RAG results | [OK] Pre-compiled BPF object embedded at compile time via include_bytes!. No correctness issues. | [LEAN] Embedding a single pre-compiled binary blob via include_bytes! is the minimal, correct approach. | [GOOD] Compile-time constant embedding binary bytes via include_bytes!. No runtime behaviour to test; correctness is enforced at compile time. Rule 6 applies. | [DOCUMENTED] Has `/// The pre-compiled BPF object.` at L13. Private constant; one-line doc is sufficient under private-item leniency. Name and doc together are self-explanatory."
    },
    {
      "name": "BPF_MAP_CREATE",
      "kind": "constant",
      "exported": false,
      "line_start": 17,
      "line_end": 17,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in bpf_create_xskmap syscall at L109 as command argument | [UNIQUE] Constant defining BPF syscall command; no duplicates found | [OK] BPF_MAP_CREATE syscall command is 0 per Linux kernel headers. Correct. | [LEAN] Necessary raw syscall constant; no libbpf/aya is the explicit design constraint. | [GOOD] Pure numeric constant representing a kernel ABI value. No runtime behaviour; correctness validated by the kernel refusing invalid commands. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; only a section-header `// BPF syscall commands.` applies. Private constant mirroring a Linux kernel enum value. Tolerated under private-item leniency due to self-descriptive name."
    },
    {
      "name": "BPF_PROG_LOAD",
      "kind": "constant",
      "exported": false,
      "line_start": 18,
      "line_end": 18,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in bpf_prog_load syscall at L157 as command argument | [UNIQUE] Constant defining BPF syscall command; no duplicates found | [OK] BPF_PROG_LOAD syscall command is 5 per Linux kernel headers. Correct. | [LEAN] Necessary raw syscall constant given the no-dependency design. | [GOOD] Pure numeric constant for BPF syscall command. No runtime behaviour; kernel enforces correctness at call site. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; falls under the same `// BPF syscall commands.` section header as BPF_MAP_CREATE. Private constant tolerated under leniency rules."
    },
    {
      "name": "BPF_MAP_UPDATE_ELEM",
      "kind": "constant",
      "exported": false,
      "line_start": 19,
      "line_end": 19,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in bpf_map_update syscall at L197 as command argument | [UNIQUE] Constant defining BPF syscall command; no duplicates found | [OK] BPF_MAP_UPDATE_ELEM syscall command is 2 per Linux kernel headers. Correct. | [LEAN] Necessary raw syscall constant given the no-dependency design. | [GOOD] Pure numeric constant for BPF syscall command. No runtime behaviour; correctness is structural. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; shares `// BPF syscall commands.` section comment. Private constant tolerated under private-item leniency."
    },
    {
      "name": "BPF_MAP_TYPE_XSKMAP",
      "kind": "constant",
      "exported": false,
      "line_start": 22,
      "line_end": 22,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in BpfAttrMapCreate at L105 to specify map type | [UNIQUE] Constant defining BPF map type; no duplicates found | [OK] BPF_MAP_TYPE_XSKMAP = 17 matches the Linux kernel enum value. Correct. | [LEAN] Minimal constant required for XSKMAP creation without libbpf headers. | [GOOD] Pure numeric constant representing a kernel BPF map type. No runtime behaviour of its own. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; only has `// BPF map types.` section header at L21. Private constant tolerated under leniency; name mirrors kernel enum directly."
    },
    {
      "name": "BPF_PROG_TYPE_XDP",
      "kind": "constant",
      "exported": false,
      "line_start": 25,
      "line_end": 25,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in BpfAttrProgLoad at L149 to specify program type | [UNIQUE] Constant defining BPF program type; no duplicates found | [OK] BPF_PROG_TYPE_XDP = 6 matches the Linux kernel enum value. Correct. | [LEAN] Required for bpf_prog_load attr; no over-abstraction. | [GOOD] Pure numeric constant for BPF program type. No runtime behaviour; kernel validates at load time. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; covered only by `// BPF program types.` section header at L24. Private constant tolerated under private-item leniency."
    },
    {
      "name": "XDP_FLAGS_SKB_MODE",
      "kind": "constant",
      "exported": false,
      "line_start": 28,
      "line_end": 28,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in attach_xdp_netlink at L333 for XDP flags netlink attribute | [UNIQUE] Constant defining XDP attach flag; no duplicates found | [OK] XDP_FLAGS_SKB_MODE = 1<<1 = 2 matches the Linux kernel definition. Correct. | [LEAN] Single flag constant used exactly once in attach_xdp_netlink. | [GOOD] Pure numeric constant for XDP attach flag (bit field). No runtime behaviour of its own. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; only `// XDP attach flags.` section header at L27. Private constant; the bit-shift expression `1 << 1` is not explained. Tolerated under leniency."
    },
    {
      "name": "ET_REL",
      "kind": "constant",
      "exported": false,
      "line_start": 31,
      "line_end": 31,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in parse_and_patch_elf at L390 to validate ELF type | [UNIQUE] ELF constant for relocatable file type; no duplicates found | [OK] ET_REL = 1 is the correct ELF relocatable object e_type value. | [LEAN] Minimal ELF constant needed by the purpose-built parser. | [GOOD] Pure numeric ELF constant. No runtime behaviour; parse_and_patch_elf validates against it. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; grouped under `// ELF constants.` at L30. Private constant mirroring ELF spec value. Tolerated under private-item leniency."
    },
    {
      "name": "SHT_PROGBITS",
      "kind": "constant",
      "exported": false,
      "line_start": 32,
      "line_end": 32,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in parse_and_patch_elf at L424 to identify program section | [UNIQUE] ELF constant for section type; no duplicates found | [OK] SHT_PROGBITS = 1 is the correct ELF section header type value. | [LEAN] Minimal ELF constant needed by the purpose-built parser. | [GOOD] Pure numeric ELF section-type constant. No runtime behaviour on its own. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; grouped under `// ELF constants.` section header. Private constant tolerated under leniency."
    },
    {
      "name": "SHT_REL",
      "kind": "constant",
      "exported": false,
      "line_start": 33,
      "line_end": 33,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in parse_and_patch_elf at L448 to identify relocation sections | [UNIQUE] ELF constant for relocation section type; no duplicates found | [OK] SHT_REL = 9 is the correct ELF relocation-without-addends section type. BPF ELFs use SHT_REL not SHT_RELA, so this is the right constant. | [LEAN] Minimal ELF constant needed by the purpose-built parser. | [GOOD] Pure numeric ELF section-type constant for relocation sections. No runtime behaviour of its own. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; grouped under `// ELF constants.` section header. Private constant tolerated under leniency."
    },
    {
      "name": "EM_BPF",
      "kind": "constant",
      "exported": false,
      "line_start": 34,
      "line_end": 34,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Used in parse_and_patch_elf at L391 to validate ELF machine type | [UNIQUE] ELF constant for BPF machine type; no duplicates found | [OK] EM_BPF = 247 is the correct ELF machine type for eBPF. | [LEAN] Minimal ELF constant needed to validate the BPF object machine type. | [GOOD] Pure numeric ELF machine-type constant. No runtime behaviour. Rule 6 applies. | [UNDOCUMENTED] No `///` doc comment; grouped under `// ELF constants.` section header. Private constant tolerated under leniency; numeric value 247 matches ELF EM_BPF but is unexplained."
    },
    {
      "name": "XdpProgram",
      "kind": "class",
      "exported": true,
      "line_start": 37,
      "line_end": 41,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "DEAD",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[DEAD] Exported pub struct with 0 runtime importers per pre-computed analysis. Matches known false-positive pattern for library crate exports (cross-crate imports not visible in analysis scope), but no contradiction in provided context. Confidence lowered due to systematic pattern of missing workspace-level imports in earlier evaluations. | [UNIQUE] Struct wrapping loaded XDP program with fd pair; no similar types found | [NEEDS_FIX] load_and_attach leaks raw file descriptors on partial failure: xsks_map_fd is leaked if parse_and_patch_elf or bpf_prog_load returns an error (the ? operator exits early without closing the fd). Both xsks_map_fd and prog_fd are leaked if attach_xdp fails. Raw i32 values do not implement Drop, so RAII does not apply. The Drop impl on XdpProgram is never reached because the struct is never successfully constructed. | [LEAN] Minimal struct with exactly three fields (prog_fd, xsks_map_fd, ifindex) and a clean Drop impl for fd/XDP cleanup. No unnecessary abstraction layers. | [NONE] No test file exists for bpf_loader.rs. XdpProgram, its constructors (load_and_attach, register_xsk), and its Drop impl are completely untested. Critical Linux-only functionality with zero automated coverage. | [PARTIAL] Has struct-level `/// A loaded XDP program with its XSKMAP.` at L36. However, both public fields `prog_fd` and `xsks_map_fd` lack `///` field-level doc comments explaining their semantics (e.g., ownership, validity guarantees). Methods `load_and_attach` and `register_xsk` also carry only brief single-line docs with no `# Examples` sections on this public API."
    },
    {
      "name": "bpf_create_xskmap",
      "kind": "function",
      "exported": false,
      "line_start": 93,
      "line_end": 122,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Called in load_and_attach at L63 to create XSKMAP before loading program | [UNIQUE] Creates BPF XSKMAP via syscall; no similar functions found in RAG results | [OK] BpfAttrMapCreate is 16 bytes with no interior padding (four u32 fields). All fields are explicitly initialized. Syscall command, map type, key_size=4 and value_size=4 are all correct for XSKMAP. | [LEAN] Function-local repr(C) struct for BPF syscall attr is idiomatic Rust FFI. Complexity is proportional to the kernel ABI requirement. | [NONE] No test file exists. This private function issues a raw bpf() syscall to create an XSKMAP. No unit or integration tests cover success path, invalid max_entries, or error handling. | [UNDOCUMENTED] No `///` doc comment. Private function; tolerated under private-item leniency. The `max_entries` parameter and returned fd semantics are undocumented, but the name and context within the file are clear."
    },
    {
      "name": "bpf_prog_load",
      "kind": "function",
      "exported": false,
      "line_start": 124,
      "line_end": 175,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Called in load_and_attach at L70 to load BPF bytecode into kernel | [UNIQUE] Loads BPF program bytecode via syscall; no similar functions found | [NEEDS_FIX] BpfAttrProgLoad with #[repr(C)]: named fields sum to 252 bytes (4+4+8+8+4+4+8+4+208). Struct alignment is 8 (from u64 fields). The compiler rounds size up to 256, inserting 4 bytes of trailing padding at offsets 252–255. These trailing bytes are NOT covered by _pad and are not explicitly zeroed. std::mem::size_of returns 256, so all 256 bytes are passed to the kernel. If the trailing 4 bytes are stack garbage (possible in release builds), the kernel may interpret them as flag fields (e.g., prog_flags or attach_flags in the bpf_attr union) and return EINVAL or trigger unintended behaviour. | [LEAN] The 208-byte pad, 65536-byte verifier log buffer, and local repr(C) struct are all necessary for correct BPF prog load semantics. Complexity is entirely kernel-imposed. | [NONE] No test file exists. Private function performing BPF program loading via syscall, including verifier log capture on failure. Zero test coverage of any code path. | [UNDOCUMENTED] No `///` doc comment. Private function with substantial complexity (verifier log handling, padded struct layout). Inline `//` comments partially compensate, but no `///` doc exists. Tolerated under private-item leniency."
    },
    {
      "name": "bpf_map_update",
      "kind": "function",
      "exported": false,
      "line_start": 177,
      "line_end": 206,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Called in register_xsk at L90 to add AF_XDP socket fd to XSKMAP | [UNIQUE] Updates BPF map element via syscall; no similar functions found | [OK] BpfAttrMapElem has implicit 4-byte padding between map_fd and key due to u64 alignment, matching the kernel's __aligned_u64 layout. Key and value are passed as pointers to caller-owned u32/i32 values which remain valid for the duration of the syscall. Return value check is correct. | [LEAN] Minimal BPF_MAP_UPDATE_ELEM syscall wrapper with a correctly sized attr struct. No excess. | [NONE] No test file exists. Private function issuing BPF_MAP_UPDATE_ELEM syscall. Neither the happy path nor the error path (ret < 0) is tested. | [UNDOCUMENTED] No `///` doc comment. Private function; name and signature are fairly self-descriptive. The `flags: 0 // BPF_ANY` inline comment aids clarity. Tolerated under private-item leniency."
    },
    {
      "name": "attach_xdp",
      "kind": "function",
      "exported": false,
      "line_start": 210,
      "line_end": 233,
      "correction": "OK",
      "overengineering": "ACCEPTABLE",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Called in load_and_attach at L74 to attach program to interface | [UNIQUE] Wrapper that tries ip command first, then falls back to netlink. RAG score 0.841 with attach_xdp_netlink, but different semantic contracts: wrapper with fallback vs low-level implementation. Not interchangeable despite high similarity score. | [OK] The 'ip link ... xdpgeneric pinned /proc/self/fd/N' invocation will likely fail because 'pinned' expects a BPF filesystem path, not a /proc/self/fd symlink. However the fallback to attach_xdp_netlink handles this case, so the overall function is correct. | [ACCEPTABLE] Dual-path design (shell out to `ip link` first, then fall back to raw netlink) adds modest complexity. The rationale—avoiding 200 lines of netlink—is understandable, but since attach_xdp_netlink is implemented anyway, the ip-command path becomes a redundant first attempt. Justified by portability hedging, but could simply call attach_xdp_netlink directly. | [NONE] No test file exists. Private function that shells out to 'ip link set' with a fallback to attach_xdp_netlink. Neither path is tested; the fallback logic is completely unverified. | [UNDOCUMENTED] No `///` doc comment. Private function with a non-trivial fallback strategy (ip command → netlink). The strategy is described via `//` inline comments, but not via `///` docs. Tolerated under private-item leniency."
    },
    {
      "name": "attach_xdp_netlink",
      "kind": "function",
      "exported": false,
      "line_start": 235,
      "line_end": 342,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Called in attach_xdp at L231 and in detach_xdp at L345; core netlink-based XDP attach/detach implementation | [UNIQUE] Low-level netlink socket implementation for XDP attachment. RAG score 0.841 with attach_xdp, but attach_xdp calls this as fallback; different logical contracts and purposes. Not interchangeable. | [NEEDS_FIX] The ACK check uses 'if n >= 16' before reading resp[16..19] (the nlmsgerr.error i32). An NLMSG_ERROR response is nlmsghdr(16) + error(4) = 20 bytes minimum; the guard should be 'n >= 20'. If exactly 16 bytes are received, resp[16..19] reads from the zero-initialized portion of resp, yielding error=0, which masks a real kernel error and causes the function to return Ok(()) when it should return Err. Additionally, 'use std::os::unix::io::FromRawFd' is imported but never used. | [LEAN] 107 lines of manual netlink RTM_SETLINK message construction is unavoidable when doing raw netlink XDP attachment without libbpf. The byte-by-byte offset tracking reflects the complexity of the netlink binary protocol, not self-inflicted abstraction. Note: the `use std::os::unix::io::FromRawFd` import is unused dead code, but that is a bug/cleanup issue, not overengineering. | [NONE] No test file exists. This is the most complex function in the file — hand-crafted netlink RTM_SETLINK message assembly with manual byte offsets. The byte layout, ACK parsing, and error handling are entirely untested. High risk of silent corruption. | [UNDOCUMENTED] No `///` doc comment. Private function implementing raw netlink RTM_SETLINK; the complex byte-layout logic has inline `//` comments. No doc comment describing the contract (fd=-1 detach semantics or error conditions). Tolerated under private-item leniency."
    },
    {
      "name": "detach_xdp",
      "kind": "function",
      "exported": false,
      "line_start": 344,
      "line_end": 346,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Called in Drop impl for XdpProgram at L80 to clean up XDP attachment | [UNIQUE] Trivial 1-line wrapper calling attach_xdp_netlink with -1 to detach; thin wrapper with distinct semantics | [OK] Passing prog_fd=-1 to attach_xdp_netlink is the correct kernel semantic for XDP detach via RTM_SETLINK/IFLA_XDP_FD. | [LEAN] Trivial one-liner reusing attach_xdp_netlink with fd=-1 per kernel convention. Correctly minimal. | [NONE] No test file exists. Trivial wrapper calling attach_xdp_netlink with fd=-1. No test verifies that detach is successfully invoked on Drop or that it propagates errors correctly. | [UNDOCUMENTED] No `///` doc comment. One-liner with an inline `// fd=-1 detaches` comment explaining the mechanism. Private function tolerated under leniency; the semantics are communicated via the inline comment."
    },
    {
      "name": "ifindex_to_name",
      "kind": "function",
      "exported": false,
      "line_start": 348,
      "line_end": 357,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Called in attach_xdp at L224 to resolve interface index to name for ip command | [UNIQUE] Converts interface index to name via libc. RAG score 0.779 with if_nametoindex from xdp.rs, but that function performs reverse operation (name→index). Opposite logic despite domain similarity. | [OK] Correctly uses a 16-byte buffer (IFNAMSIZ=16), checks for null return from if_indextoname, finds the null terminator safely with unwrap_or(16), and converts to UTF-8 with appropriate error mapping. | [LEAN] Minimal safe wrapper around libc::if_indextoname with correct null-terminator handling and UTF-8 validation. | [NONE] No test file exists. Private function wrapping libc::if_indextoname. Neither the success path, the null-return error path, nor the UTF-8 error path is tested. | [UNDOCUMENTED] No `///` doc comment. Private function with a self-descriptive name. Error path (null return from if_indextoname, UTF-8 failure) is not documented. Tolerated under private-item leniency."
    },
    {
      "name": "parse_and_patch_elf",
      "kind": "function",
      "exported": false,
      "line_start": 361,
      "line_end": 470,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Called in load_and_attach at L68 to parse ELF, extract bytecode, and patch map references | [UNIQUE] Parses ELF binary, extracts BPF program section, patches map fd references; no similar functions found | [OK] ELF64 header field offsets are correct (e_shoff@40, e_shentsize@58, e_shnum@60, e_shstrndx@62). Elf64_Shdr field offsets are correct: sh_offset@24, sh_size@32, sh_info@44. The first sh_info assignment at offset 28 is dead code immediately shadowed by the correct assignment at offset 44 — not a bug, the correct value is used. BPF LD_IMM64 patching correctly isolates dst_reg (& 0x0f), sets src_reg=1 (BPF_PSEUDO_MAP_FD), and writes map_fd into the lower 32-bit immediate with the upper 32 bits zeroed. | [LEAN] A purpose-built ~110-line ELF parser that handles only what is needed: validate headers, find the 'xdp' section, find REL sections targeting it, and patch LD_IMM64 map references. The file explicitly rejects aya/libbpf as dependencies and the target object is 1.4KB and static. Complexity is proportional to the binary format being parsed. The shadowed sh_info variable (offset corrected in-place with a comment) is a code-quality smell but not overengineering. | [NONE] No test file exists. This is a pure function (takes &[u8] and an i32, returns Vec<u8>) that is fully unit-testable without kernel access. It handles ELF header validation, section enumeration, relocation patching, and LD_IMM64 rewriting. None of these paths — including the non-ELF error, wrong machine type, missing 'xdp' section, or relocation patching — are tested at all. This is the most critical gap in the file. | [UNDOCUMENTED] No `///` doc comment. Private function with significant complexity (ELF parsing, relocation patching). The relocation patching strategy and BPF_PSEUDO_MAP_FD encoding are described inline via `//` comments but not via `///` docs. Tolerated under private-item leniency."
    }
  ],
  "actions": [
    {
      "id": 1,
      "description": "Fix file descriptor leak in load_and_attach: xsks_map_fd is not closed when parse_and_patch_elf or bpf_prog_load fails via ?. Both xsks_map_fd and prog_fd are not closed when attach_xdp fails. Use an explicit close guard (e.g., scopeguard crate, or a local struct with Drop) so that partially acquired FDs are always released on error paths.",
      "severity": "medium",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L48",
      "source": "correction"
    },
    {
      "id": 2,
      "description": "Fix NLMSG_ERROR bounds check in attach_xdp_netlink: change 'if n >= 16' to 'if n >= 20' before reading resp[16..19]. The nlmsgerr struct requires 20 bytes (nlmsghdr=16 + error=4); reading with only n>=16 causes a silent false-success when a short NLMSG_ERROR is received.",
      "severity": "low",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L322",
      "source": "correction"
    },
    {
      "id": 3,
      "description": "Fix uninitialized trailing padding in BpfAttrProgLoad: the #[repr(C)] struct is 256 bytes (252 of named fields + 4 bytes compiler trailing padding). The trailing 4 bytes are not covered by _pad and may be uninitialized stack bytes when passed to the bpf() syscall. Zero-initialize the entire struct with MaybeUninit::zeroed() before filling fields, or extend _pad to [u8; 212] so std::mem::size_of equals exactly 256 with no implicit trailing padding.",
      "severity": "low",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L138",
      "source": "correction"
    },
    {
      "id": 4,
      "description": "Remove dead code: `XdpProgram` is exported but unused",
      "severity": "medium",
      "effort": "trivial",
      "category": "refactor",
      "source": "utility",
      "target_symbol": "XdpProgram",
      "target_lines": "L37-L41"
    },
    {
      "id": 5,
      "description": "Add JSDoc documentation for exported symbol: `XdpProgram`",
      "severity": "medium",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "XdpProgram",
      "target_lines": "L37-L41"
    }
  ],
  "file_level": {
    "unused_imports": [],
    "circular_dependencies": [],
    "general_notes": "Best practices score: 2/10 (3 FAIL, 4 WARN, 5 PASS)"
  },
  "best_practices": {
    "score": 2,
    "rules": [
      {
        "rule_id": 1,
        "rule_name": "No unwrap in production code",
        "status": "FAIL",
        "severity": "CRITICAL",
        "detail": "Multiple .try_into().unwrap() calls in parse_and_patch_elf operate on externally-supplied byte slices (even though the ELF is compiled in, the function signature accepts &[u8]). Violations: elf[40..48].try_into().unwrap(), sh[24..32].try_into().unwrap(), sh[0..4].try_into().unwrap(), sh[4..8].try_into().unwrap(), sh[32..40].try_into().unwrap(), sh[44..48].try_into().unwrap(), rel_data[off..off+8].try_into().unwrap(). Each panics on malformed input. The .unwrap_or() usages are acceptable.",
        "lines": "L355-L450"
      },
      {
        "rule_id": 2,
        "rule_name": "No unsafe blocks without clear justification comment",
        "status": "FAIL",
        "severity": "CRITICAL",
        "detail": "The file contains over 15 unsafe blocks (libc::syscall for BPF, libc::close, libc::socket, libc::sendto, libc::recv, libc::if_indextoname, std::mem::zeroed) and none carry a // SAFETY: comment explaining invariants upheld by the caller. The module-level doc mentions raw syscalls but does not substitute for per-block // SAFETY: annotations required by Rust best practice. Additionally, use std::os::unix::io::FromRawFd is imported in attach_xdp_netlink but never used, suggesting an incomplete RAII refactor was abandoned.",
        "lines": "L85-L340"
      },
      {
        "rule_id": 3,
        "rule_name": "Proper error handling with Result/Option (no silent ignores)",
        "status": "WARN",
        "severity": "HIGH",
        "detail": "In Drop::drop, let _ = detach_xdp(self.ifindex) silently discards the detach error. While it is impossible to propagate errors from Drop, at minimum an eprintln! or a log::warn! call should surface the failure, consistent with the diagnostic eprintln! calls used elsewhere in this file.",
        "lines": "L85-L93"
      },
      {
        "rule_id": 4,
        "rule_name": "Derive common traits on public types",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "pub struct XdpProgram carries no derive attributes. Debug would be immediately useful for diagnostics (printing prog_fd, xsks_map_fd, ifindex). Clone and PartialEq are intentionally unsuitable here (they would alias raw FDs), but the absence of Debug on a public type is a clear omission.",
        "lines": "L36-L42"
      },
      {
        "rule_id": 5,
        "rule_name": "Lifetime annotations explicit where needed",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "All functions that accept references use simple, unambiguous lifetime elision rules (single-input references, no multiple output lifetimes). No explicit lifetime annotations are needed here and none are missing."
      },
      {
        "rule_id": 6,
        "rule_name": "Use clippy idioms",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "Two clippy-detectable issues: (1) use std::os::unix::io::FromRawFd is imported inside attach_xdp_netlink but never used — dead import. (2) In parse_and_patch_elf, sh_info is bound twice in the same loop body: first from sh[28..32] (wrong offset) then immediately shadowed by sh[44..48] (correct offset). The first binding is dead code and will trigger a clippy::shadow_unrelated or unused_variable warning.",
        "lines": "L232-L240"
      },
      {
        "rule_id": 7,
        "rule_name": "No panic! in library/production code",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No explicit panic!(), todo!(), unimplemented!(), or unreachable!() macro invocations are present. Implicit panics from .unwrap() are covered under Rule 1."
      },
      {
        "rule_id": 8,
        "rule_name": "Module organization",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "The file is correctly placed in rustguard-tun/src/ alongside linux.rs, xdp.rs, and linux_mq.rs — all Linux-specific TUN/XDP helpers. Internal helpers are private (non-pub), the public surface is minimal (XdpProgram + two methods), and sections are clearly delineated with // ── comments. No re-export concerns for this leaf module."
      },
      {
        "rule_id": 9,
        "rule_name": "Documentation comments on public items",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "The module, XdpProgram, load_and_attach, and register_xsk all have /// doc comments. However, the two public fields pub prog_fd: i32 and pub xsks_map_fd: i32 are undocumented. Callers need to know these are kernel file descriptors managed by the struct's Drop impl and should not be closed externally.",
        "lines": "L37-L40"
      },
      {
        "rule_id": 10,
        "rule_name": "No hardcoded secrets or credentials",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No secrets, API keys, passwords, or credentials present. All constants are standard Linux kernel interface values (BPF command codes, ELF machine types, netlink attribute IDs)."
      },
      {
        "rule_id": 11,
        "rule_name": "Memory safety (no leaks via mem::forget, proper Drop impls)",
        "status": "FAIL",
        "severity": "HIGH",
        "detail": "Resource leak in load_and_attach error paths: xsks_map_fd is created via bpf_create_xskmap before prog_fd exists. If parse_and_patch_elf or bpf_prog_load fails, xsks_map_fd is leaked (no Drop impl is reached because XdpProgram was never constructed). If attach_xdp fails, both xsks_map_fd and prog_fd are leaked. The Drop impl on XdpProgram is correct, but it only runs if the struct is successfully constructed. A scopeguard/RAII wrapper or explicit cleanup in each error branch is required.",
        "lines": "L44-L75"
      },
      {
        "rule_id": 12,
        "rule_name": "Concurrency safety (no data races, proper Send/Sync bounds)",
        "status": "PASS",
        "severity": "HIGH",
        "detail": "XdpProgram contains only i32 and u32 fields, so the compiler auto-derives Send + Sync, which is correct for a file-descriptor holder without interior mutability. The netlink socket in attach_xdp_netlink is created, used, and closed within a single function scope with no sharing. No Mutex, Arc, or thread-crossing patterns are used incorrectly."
      }
    ],
    "suggestions": [
      {
        "description": "Replace .try_into().unwrap() with bounds-checked error propagation in parse_and_patch_elf",
        "before": "let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;",
        "after": "let e_shoff = u64::from_le_bytes(\n    elf.get(40..48)\n        .and_then(|s| s.try_into().ok())\n        .ok_or_else(|| io::Error::new(io::ErrorKind::InvalidData, \"ELF header truncated\"))?\n) as usize;"
      },
      {
        "description": "Add // SAFETY: comments to every unsafe block documenting the invariants upheld",
        "before": "let fd = unsafe {\n    libc::syscall(\n        libc::SYS_bpf,\n        BPF_MAP_CREATE,\n        &attr as *const _,\n        std::mem::size_of::<BpfAttrMapCreate>(),\n    )\n} as i32;",
        "after": "// SAFETY: `attr` is a valid, fully-initialized BpfAttrMapCreate whose size matches\n// the third argument. SYS_bpf is a stable Linux syscall. No aliasing occurs.\nlet fd = unsafe {\n    libc::syscall(\n        libc::SYS_bpf,\n        BPF_MAP_CREATE,\n        &attr as *const _,\n        std::mem::size_of::<BpfAttrMapCreate>(),\n    )\n} as i32;"
      },
      {
        "description": "Fix file-descriptor leak in load_and_attach by cleaning up on error paths",
        "before": "let xsks_map_fd = bpf_create_xskmap(64)?;\nlet insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;\nlet prog_fd = bpf_prog_load(&insns)...?;\nattach_xdp(ifindex, prog_fd)...?;\nOk(Self { prog_fd, xsks_map_fd, ifindex })",
        "after": "let xsks_map_fd = bpf_create_xskmap(64)?;\nlet insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd).map_err(|e| {\n    unsafe { libc::close(xsks_map_fd); }\n    e\n})?;\nlet prog_fd = bpf_prog_load(&insns).map_err(|e| {\n    unsafe { libc::close(xsks_map_fd); }\n    io::Error::new(e.kind(), format!(\"prog_load: {e}\"))\n})?;\nattach_xdp(ifindex, prog_fd).map_err(|e| {\n    unsafe { libc::close(prog_fd); libc::close(xsks_map_fd); }\n    io::Error::new(e.kind(), format!(\"xdp_attach ifindex={ifindex}: {e}\"))\n})?;\nOk(Self { prog_fd, xsks_map_fd, ifindex })"
      },
      {
        "description": "Derive Debug on XdpProgram and document public fields",
        "before": "pub struct XdpProgram {\n    pub prog_fd: i32,\n    pub xsks_map_fd: i32,\n    ifindex: u32,\n}",
        "after": "#[derive(Debug)]\npub struct XdpProgram {\n    /// Kernel file descriptor for the loaded XDP BPF program. Managed by Drop; do not close externally.\n    pub prog_fd: i32,\n    /// Kernel file descriptor for the XSKMAP used to register AF_XDP sockets. Managed by Drop.\n    pub xsks_map_fd: i32,\n    ifindex: u32,\n}"
      }
    ]
  },
  "docs_coverage": {
    "concepts": [],
    "score_pct": 18
  },
  "axis_meta": {
    "utility": {
      "model": "claude-haiku-4-5-20251001",
      "cost_usd": 0.0745948,
      "duration_ms": 80800
    },
    "duplication": {
      "model": "claude-haiku-4-5-20251001",
      "cost_usd": 0.07188585,
      "duration_ms": 70508
    },
    "correction": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.7110037499999999,
      "duration_ms": 365501
    },
    "overengineering": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.19530175000000002,
      "duration_ms": 80898
    },
    "tests": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.176946,
      "duration_ms": 51846
    },
    "best_practices": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.339315,
      "duration_ms": 633485
    },
    "documentation": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.31708875000000003,
      "duration_ms": 115811
    }
  }
}
```

## Source code of `rustguard-tun/src/bpf_loader.rs`

```typescript
//! Minimal BPF ELF loader for the XDP WireGuard filter.
//!
//! Loads the pre-compiled xdp_wg.o, creates the XSKMAP,
//! patches the map fd into the program, loads it, and attaches to an interface.
//!
//! No libbpf. No aya. Just raw bpf() syscalls and ELF parsing.
//! The .o is 1.4KB and never changes.

#![cfg(target_os = "linux")]

use std::io;

/// The pre-compiled BPF object.
const XDP_WG_OBJ: &[u8] = include_bytes!("../bpf/xdp_wg.o");

// BPF syscall commands.
const BPF_MAP_CREATE: u32 = 0;
const BPF_PROG_LOAD: u32 = 5;
const BPF_MAP_UPDATE_ELEM: u32 = 2;

// BPF map types.
const BPF_MAP_TYPE_XSKMAP: u32 = 17;

// BPF program types.
const BPF_PROG_TYPE_XDP: u32 = 6;

// XDP attach flags.
const XDP_FLAGS_SKB_MODE: u32 = 1 << 1;

// ELF constants.
const ET_REL: u16 = 1;
const SHT_PROGBITS: u32 = 1;
const SHT_REL: u32 = 9;
const EM_BPF: u16 = 247;

/// A loaded XDP program with its XSKMAP.
pub struct XdpProgram {
    pub prog_fd: i32,
    pub xsks_map_fd: i32,
    ifindex: u32,
}

impl XdpProgram {
    /// Load the XDP WireGuard filter and attach it to an interface.
    pub fn load_and_attach(ifname: &str) -> io::Result<Self> {
        let ifindex = super::xdp::if_nametoindex(ifname)?;

        // 1. Create the XSKMAP.
        let xsks_map_fd = bpf_create_xskmap(64)?;

        // 2. Parse ELF, extract program bytecode, patch map references.
        let insns = parse_and_patch_elf(XDP_WG_OBJ, xsks_map_fd)?;
        eprintln!("  BPF: parsed ELF, {} insn bytes, map_fd={}", insns.len(), xsks_map_fd);

        // 3. Load BPF program.
        let prog_fd = bpf_prog_load(&insns).map_err(|e| {
            io::Error::new(e.kind(), format!("prog_load ({} insns): {e}", insns.len() / 8))
        })?;
        eprintln!("  BPF: program loaded, prog_fd={}", prog_fd);

        // 4. Attach to interface.
        attach_xdp(ifindex, prog_fd).map_err(|e| {
            io::Error::new(e.kind(), format!("xdp_attach ifindex={ifindex}: {e}"))
        })?;
        eprintln!("  BPF: attached to ifindex={}", ifindex);

        Ok(Self {
            prog_fd,
            xsks_map_fd,
            ifindex,
        })
    }

    /// Register an AF_XDP socket fd in the XSKMAP for a given queue.
    pub fn register_xsk(&self, queue_id: u32, xsk_fd: i32) -> io::Result<()> {
        bpf_map_update(self.xsks_map_fd, &queue_id, &xsk_fd)
    }
}

impl Drop for XdpProgram {
    fn drop(&mut self) {
        // Detach XDP program from interface.
        let _ = detach_xdp(self.ifindex);
        unsafe {
            libc::close(self.prog_fd);
            libc::close(self.xsks_map_fd);
        }
    }
}

// ── BPF syscalls ────────────────────────────────────────────────────

fn bpf_create_xskmap(max_entries: u32) -> io::Result<i32> {
    #[repr(C)]
    struct BpfAttrMapCreate {
        map_type: u32,
        key_size: u32,
        value_size: u32,
        max_entries: u32,
    }

    let attr = BpfAttrMapCreate {
        map_type: BPF_MAP_TYPE_XSKMAP,
        key_size: 4,
        value_size: 4,
        max_entries,
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_CREATE,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapCreate>(),
        )
    } as i32;

    if fd < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(fd)
}

fn bpf_prog_load(insns: &[u8]) -> io::Result<i32> {
    let license = b"GPL\0";
    let mut log_buf = vec![0u8; 65536];

    // The kernel's bpf_attr union for BPF_PROG_LOAD is large.
    // We need at least the fields up to kern_version, padded to the
    // minimum size the kernel accepts. Using 256 bytes to be safe.
    #[repr(C)]
    struct BpfAttrProgLoad {
        prog_type: u32,
        insn_cnt: u32,
        insns: u64,
        license: u64,
        log_level: u32,
        log_size: u32,
        log_buf: u64,
        kern_version: u32,
        _pad: [u8; 208], // Pad to 256 bytes total.
    }

    let attr = BpfAttrProgLoad {
        prog_type: BPF_PROG_TYPE_XDP,
        insn_cnt: (insns.len() / 8) as u32,
        insns: insns.as_ptr() as u64,
        license: license.as_ptr() as u64,
        log_level: 1,
        log_size: log_buf.len() as u32,
        log_buf: log_buf.as_mut_ptr() as u64,
        kern_version: 0,
        _pad: [0; 208],
    };

    let fd = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_PROG_LOAD,
            &attr as *const _,
            std::mem::size_of::<BpfAttrProgLoad>(),
        )
    } as i32;

    if fd < 0 {
        let err = io::Error::last_os_error();
        let log_end = log_buf.iter().position(|&b| b == 0).unwrap_or(0);
        if log_end > 0 {
            let log_str = String::from_utf8_lossy(&log_buf[..log_end]);
            eprintln!("  BPF verifier log:\n{log_str}");
        }
        return Err(err);
    }
    Ok(fd)
}

fn bpf_map_update(map_fd: i32, key: &u32, value: &i32) -> io::Result<()> {
    #[repr(C)]
    struct BpfAttrMapElem {
        map_fd: u32,
        key: u64,
        value: u64,
        flags: u64,
    }

    let attr = BpfAttrMapElem {
        map_fd: map_fd as u32,
        key: key as *const _ as u64,
        value: value as *const _ as u64,
        flags: 0, // BPF_ANY
    };

    let ret = unsafe {
        libc::syscall(
            libc::SYS_bpf,
            BPF_MAP_UPDATE_ELEM,
            &attr as *const _,
            std::mem::size_of::<BpfAttrMapElem>(),
        )
    };

    if ret < 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(())
}

// ── XDP attach/detach via netlink ───────────────────────────────────

fn attach_xdp(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Use the SIOCETHTOOL / netlink approach, but the simplest way
    // that works on all kernels is via the if_link netlink interface.
    // For simplicity, shell out to `ip link set dev <name> xdpgeneric obj ...`
    // But we already have the fd. Use the bpf_set_link_xdp_fd equivalent.

    // Direct approach: set XDP fd via netlink RTM_SETLINK.
    // This is complex. Simpler: use the deprecated but working SIOCSIFXDP.
    // Actually simplest: use libc's if_nametoindex + raw netlink.

    // For now, use the ip command — it works and avoids 200 lines of netlink.
    let ifname = ifindex_to_name(ifindex)?;
    let fd_path = format!("/proc/self/fd/{prog_fd}");
    let output = std::process::Command::new("ip")
        .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
        .output();

    // Fallback: use the bpf netlink API via raw netlink socket.
    // If ip command doesn't support pinned fd, try bpf_xdp_attach.
    match output {
        Ok(out) if out.status.success() => Ok(()),
        _ => attach_xdp_netlink(ifindex, prog_fd),
    }
}

fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
    // Netlink RTM_SETLINK with IFLA_XDP attribute.
    // This is the proper way — no shelling out.
    use std::os::unix::io::FromRawFd;

    let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
    if sock < 0 {
        return Err(io::Error::last_os_error());
    }

    // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
    const RTM_SETLINK: u16 = 19;
    const NLM_F_REQUEST: u16 = 1;
    const NLM_F_ACK: u16 = 4;
    const IFLA_XDP: u16 = 43;
    const IFLA_XDP_FD: u16 = 1;
    const IFLA_XDP_FLAGS: u16 = 3;

    // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
    let mut buf = [0u8; 64];
    let mut off = 0;

    // nlmsghdr
    let total_len: u32 = 52;
    buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
    off += 4;
    buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
    off += 2;
    off += 8; // seq + pid = 0

    // ifinfomsg
    buf[off] = libc::AF_UNSPEC as u8; // family
    off += 4; // family + pad + type
    let idx_bytes = ifindex.to_ne_bytes();
    buf[off..off + 4].copy_from_slice(&idx_bytes); // ifindex
    off += 4;
    off += 8; // flags + change

    // IFLA_XDP (nested, len = 4 + 8 + 8 = 20)
    let xdp_nla_len: u16 = 20;
    buf[off..off + 2].copy_from_slice(&xdp_nla_len.to_ne_bytes());
    off += 2;
    let xdp_type = IFLA_XDP | (1 << 15); // NLA_F_NESTED
    buf[off..off + 2].copy_from_slice(&xdp_type.to_ne_bytes());
    off += 2;

    // IFLA_XDP_FD (len=8, type=1, value=prog_fd)
    let fd_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&fd_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FD.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&prog_fd.to_ne_bytes());
    off += 4;

    // IFLA_XDP_FLAGS (len=8, type=3, value=XDP_FLAGS_SKB_MODE)
    let flags_nla_len: u16 = 8;
    buf[off..off + 2].copy_from_slice(&flags_nla_len.to_ne_bytes());
    off += 2;
    buf[off..off + 2].copy_from_slice(&IFLA_XDP_FLAGS.to_ne_bytes());
    off += 2;
    buf[off..off + 4].copy_from_slice(&XDP_FLAGS_SKB_MODE.to_ne_bytes());

    let mut sa: libc::sockaddr_nl = unsafe { std::mem::zeroed() };
    sa.nl_family = libc::AF_NETLINK as u16;

    let ret = unsafe {
        libc::sendto(
            sock,
            buf.as_ptr() as *const _,
            total_len as usize,
            0,
            &sa as *const _ as *const _,
            std::mem::size_of::<libc::sockaddr_nl>() as u32,
        )
    };

    if ret < 0 {
        let err = io::Error::last_os_error();
        unsafe { libc::close(sock) };
        return Err(err);
    }

    // Read ACK.
    let mut resp = [0u8; 128];
    let n = unsafe { libc::recv(sock, resp.as_mut_ptr() as *mut _, resp.len(), 0) };
    unsafe { libc::close(sock) };

    if n < 0 {
        return Err(io::Error::last_os_error());
    }

    // Check for error in nlmsghdr.
    if n >= 16 {
        let nlmsg_type = u16::from_ne_bytes([resp[4], resp[5]]);
        if nlmsg_type == 2 {
            // NLMSG_ERROR
            let error = i32::from_ne_bytes([resp[16], resp[17], resp[18], resp[19]]);
            if error < 0 {
                return Err(io::Error::from_raw_os_error(-error));
            }
        }
    }

    Ok(())
}

fn detach_xdp(ifindex: u32) -> io::Result<()> {
    attach_xdp_netlink(ifindex, -1) // fd=-1 detaches
}

fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
    let mut buf = [0u8; 16];
    let ret = unsafe { libc::if_indextoname(ifindex, buf.as_mut_ptr() as *mut _) };
    if ret.is_null() {
        return Err(io::Error::last_os_error());
    }
    let end = buf.iter().position(|&b| b == 0).unwrap_or(16);
    String::from_utf8(buf[..end].to_vec())
        .map_err(|_| io::Error::new(io::ErrorKind::InvalidData, "invalid ifname"))
}

// ── Minimal ELF parser ──────────────────────────────────────────────

fn parse_and_patch_elf(elf: &[u8], map_fd: i32) -> io::Result<Vec<u8>> {
    if elf.len() < 64 || &elf[0..4] != b"\x7fELF" {
        return Err(io::Error::new(io::ErrorKind::InvalidData, "not an ELF"));
    }

    let e_type = u16::from_le_bytes([elf[16], elf[17]]);
    let e_machine = u16::from_le_bytes([elf[18], elf[19]]);
    if e_type != ET_REL || e_machine != EM_BPF {
        return Err(io::Error::new(
            io::ErrorKind::InvalidData,
            "not a BPF relocatable ELF",
        ));
    }

    let e_shoff = u64::from_le_bytes(elf[40..48].try_into().unwrap()) as usize;
    let e_shentsize = u16::from_le_bytes([elf[58], elf[59]]) as usize;
    let e_shnum = u16::from_le_bytes([elf[60], elf[61]]) as usize;
    let e_shstrndx = u16::from_le_bytes([elf[62], elf[63]]) as usize;

    let shstrtab_off = {
        let sh = &elf[e_shoff + e_shstrndx * e_shentsize..];
        u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize
    };

    // Collect section info.
    struct Section {
        name: String,
        sh_type: u32,
        offset: usize,
        size: usize,
        info: u32, // sh_info — for REL sections, points to the target section.
    }

    let mut sections = Vec::new();
    for i in 0..e_shnum {
        let sh = &elf[e_shoff + i * e_shentsize..];
        let sh_name_off = u32::from_le_bytes(sh[0..4].try_into().unwrap()) as usize;
        let sh_type = u32::from_le_bytes(sh[4..8].try_into().unwrap());
        let sh_offset = u64::from_le_bytes(sh[24..32].try_into().unwrap()) as usize;
        let sh_size = u64::from_le_bytes(sh[32..40].try_into().unwrap()) as usize;
        let sh_info = u32::from_le_bytes(sh[28..32].try_into().unwrap());
        // Wait, sh_info is at offset 44 in Elf64_Shdr. Let me recalculate.
        // Elf64_Shdr: name(4) type(4) flags(8) addr(8) offset(8) size(8) link(4) info(4) addralign(8) entsize(8)
        let sh_info = u32::from_le_bytes(sh[44..48].try_into().unwrap());

        let name_start = shstrtab_off + sh_name_off;
        let name_end = elf[name_start..]
            .iter()
            .position(|&b| b == 0)
            .unwrap_or(0)
            + name_start;
        let name = std::str::from_utf8(&elf[name_start..name_end])
            .unwrap_or("")
            .to_string();

        sections.push(Section {
            name,
            sh_type,
            offset: sh_offset,
            size: sh_size,
            info: sh_info,
        });
    }

    // Find the "xdp" program section.
    let prog_idx = sections
        .iter()
        .position(|s| s.name == "xdp" && s.sh_type == SHT_PROGBITS)
        .ok_or_else(|| {
            io::Error::new(io::ErrorKind::InvalidData, "no 'xdp' section")
        })?;
    let prog = &sections[prog_idx];
    let mut insns = elf[prog.offset..prog.offset + prog.size].to_vec();

    // Find relocation sections targeting the program section.
    // These tell us which LD_IMM64 instructions reference maps.
    for sec in &sections {
        if sec.sh_type != SHT_REL {
            continue;
        }
        if sec.info as usize != prog_idx {
            continue; // This relocation targets a different section.
        }

        // Process each relocation entry (Elf64_Rel: offset(8) + info(8) = 16 bytes).
        let rel_data = &elf[sec.offset..sec.offset + sec.size];
        let mut off = 0;
        while off + 16 <= rel_data.len() {
            let r_offset = u64::from_le_bytes(rel_data[off..off + 8].try_into().unwrap()) as usize;
            // r_info contains symbol index in upper 32 bits, type in lower 32.
            // For BPF map refs, the type is R_BPF_64_64 (1).

            // At r_offset in the instruction stream, set src_reg=1 (BPF_PSEUDO_MAP_FD)
            // and imm=map_fd.
            if r_offset + 16 <= insns.len() && insns[r_offset] == 0x18 {
                // Set src_reg to BPF_PSEUDO_MAP_FD (1).
                insns[r_offset + 1] = (insns[r_offset + 1] & 0x0f) | (1 << 4);
                // Set imm to map fd.
                insns[r_offset + 4..r_offset + 8]
                    .copy_from_slice(&(map_fd as u32).to_le_bytes());
                // Zero upper 32 bits of the 64-bit immediate.
                insns[r_offset + 12..r_offset + 16].copy_from_slice(&0u32.to_le_bytes());
            }

            off += 16;
        }
    }

    Ok(insns)
}

```

## Instructions

Deliberate ONLY symbols with findings — skip clean symbols. For each symbol, address ALL its axis findings together in one entry (e.g., if a symbol is DEAD + NEEDS_FIX, deliberate both in the same entry). Recompute the verdict.

Rules:
- Only include axes with findings in original/deliberated (omit clean axes)
- Do NOT add new findings, symbols, or actions
- Protect ERROR (require ≥ 95 confidence to downgrade)
- Reclassification requires confidence ≥ 85
- Copy original values when you agree with the finding

## System (init)

**Model:** claude-opus-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{"verdict":"NEEDS_REFACTOR","symbols":[{"name":"BPF_MAP_CREATE","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"UNDOCUMENTED is technically correct (no `///` doc comment). Private constant mirroring Linux kernel enum value with a section-header comment. Tolerated under private-item leniency; the name is self-descriptive to any BPF developer."},{"name":"BPF_PROG_LOAD","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Same pattern as BPF_MAP_CREATE: private constant matching kernel ABI, grouped under section comment. UNDOCUMENTED is factually correct but tolerated under private-item leniency."},{"name":"BPF_MAP_UPDATE_ELEM","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Same pattern as other BPF syscall command constants. UNDOCUMENTED factually correct; private constant with self-descriptive name tolerated under leniency."},{"name":"BPF_MAP_TYPE_XSKMAP","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Private constant mirroring kernel BPF_MAP_TYPE_XSKMAP (value 17). No `///` doc but name is the documentation for domain experts. Tolerated under private-item leniency."},{"name":"BPF_PROG_TYPE_XDP","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Private constant mirroring kernel BPF_PROG_TYPE_XDP (value 6). UNDOCUMENTED factually correct; tolerated under private-item leniency."},{"name":"XDP_FLAGS_SKB_MODE","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Private constant for XDP attach flag. Bit-shift expression `1 << 1` is standard kernel convention. UNDOCUMENTED correct; tolerated under leniency."},{"name":"ET_REL","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Private ELF constant (ET_REL=1). Standard ELF spec value with universally recognized name. UNDOCUMENTED correct; tolerated under leniency."},{"name":"SHT_PROGBITS","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Private ELF section type constant. Standard name from ELF spec. UNDOCUMENTED correct; tolerated under leniency."},{"name":"SHT_REL","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Private ELF relocation section type constant. Standard ELF name. UNDOCUMENTED correct; tolerated under leniency."},{"name":"EM_BPF","original":{"documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"documentation":"UNDOCUMENTED","confidence":60},"reasoning":"Private ELF machine type constant (247). Standard value from ELF spec. UNDOCUMENTED correct; tolerated under leniency."},{"name":"XdpProgram","original":{"correction":"NEEDS_FIX","utility":"DEAD","tests":"NONE","documentation":"PARTIAL","confidence":60},"deliberated":{"correction":"NEEDS_FIX","utility":"USED","tests":"NONE","documentation":"PARTIAL","confidence":88},"reasoning":"UTILITY reclassified DEAD → USED: This is a pub struct in a library crate with `#![cfg(target_os = \"linux\")]`. It is the primary public API of bpf_loader.rs — the evaluator itself flagged this as matching the 'known false-positive pattern for library crate exports (cross-crate imports not visible).' It would be nonsensical for the sole pub type in this module to be unused. CORRECTION NEEDS_FIX confirmed: The fd leak in load_and_attach is genuine — xsks_map_fd leaks if parse_and_patch_elf/bpf_prog_load fails, and both fds leak if attach_xdp fails. Raw i32 fds have no Drop. TESTS NONE confirmed: zero automated test coverage for constructors and Drop. DOCUMENTATION PARTIAL confirmed: struct-level doc exists but public fields prog_fd and xsks_map_fd lack field-level docs describing ownership semantics."},{"name":"bpf_create_xskmap","original":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"reasoning":"TESTS NONE correct: no test coverage for raw BPF syscall wrapper. Testing requires root/BPF capability so absence is understandable but still a gap. UNDOCUMENTED correct: private function tolerated under leniency."},{"name":"bpf_prog_load","original":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":75},"reasoning":"CORRECTION NEEDS_FIX confirmed: The #[repr(C)] struct BpfAttrProgLoad has named fields summing to 252 bytes with alignment 8, so size_of is 256. The _pad: [u8; 208] comment says 'Pad to 256 bytes total' but only reaches 252. The 4 trailing padding bytes are not guaranteed zero in release builds. The kernel's check_uarg_tail_zero may reject non-zero excess bytes with -E2BIG. Fix: change _pad to [u8; 212]. Confidence raised slightly as the math is verifiable. TESTS NONE and UNDOCUMENTED both confirmed."},{"name":"bpf_map_update","original":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"reasoning":"TESTS NONE correct: no coverage for BPF_MAP_UPDATE_ELEM syscall wrapper. UNDOCUMENTED correct: private function tolerated under leniency; inline `// BPF_ANY` comment partially compensates."},{"name":"attach_xdp","original":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"reasoning":"TESTS NONE confirmed: dual-path fallback logic (ip command → netlink) is completely unverified. UNDOCUMENTED correct: private function with non-trivial fallback strategy; inline comments explain approach but no `///` doc. Tolerated under leniency."},{"name":"attach_xdp_netlink","original":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":80},"reasoning":"CORRECTION NEEDS_FIX confirmed: The ACK bounds check `if n >= 16` is insufficient — NLMSG_ERROR requires 20 bytes (nlmsghdr=16 + error=4). With exactly 16-19 bytes received, resp[16..19] reads from zero-initialized buffer, yielding error=0 and masking a real kernel error. Fix: change to `n >= 20`. The unused `use std::os::unix::io::FromRawFd` import is also valid dead code. TESTS NONE confirmed: most complex function in file with hand-crafted netlink message, zero test coverage. UNDOCUMENTED confirmed: private function tolerated under leniency."},{"name":"detach_xdp","original":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"reasoning":"TESTS NONE correct: trivial wrapper but detach-on-Drop path is unverified. UNDOCUMENTED correct: one-liner with inline `// fd=-1 detaches` comment. Private function tolerated under leniency."},{"name":"ifindex_to_name","original":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"reasoning":"TESTS NONE correct: libc wrapper with error paths (null return, UTF-8 failure) untested. UNDOCUMENTED correct: private function with self-descriptive name tolerated under leniency."},{"name":"parse_and_patch_elf","original":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"reasoning":"TESTS NONE confirmed and this is the most critical testing gap: parse_and_patch_elf is a pure function (takes &[u8] + i32, returns Vec<u8>) fully unit-testable without kernel access. ELF header validation, section enumeration, relocation patching, and LD_IMM64 rewriting are all untested. The shadowed sh_info variable is a code smell noted in best_practices but not a correctness issue (correct value is used). UNDOCUMENTED confirmed: private function with substantial complexity; inline comments partially compensate."}],"removed_actions":[4],"reasoning":"Overall: NEEDS_REFACTOR is the correct verdict. Three confirmed NEEDS_FIX issues exist: (1) fd leak in XdpProgram::load_and_attach on error paths, (2) insufficient NLMSG_ERROR bounds check in attach_xdp_netlink (n>=16 should be n>=20), and (3) 4 bytes of uninitialized trailing padding in BpfAttrProgLoad. The XdpProgram DEAD finding is reclassified to USED — it is the sole public API of this module in a library crate, matching the known false-positive pattern for cross-crate exports invisible to single-crate analysis. Action 4 (remove dead code XdpProgram) is therefore removed. Zero test coverage across all functions is the biggest systemic issue; parse_and_patch_elf is especially notable as a pure function that could be easily unit-tested. The 10 private constants mirroring Linux kernel ABI values are UNDOCUMENTED but tolerated under private-item leniency — their names are the documentation. Best practices findings about unwrap() in parse_and_patch_elf and missing SAFETY comments on unsafe blocks are valid quality concerns captured in the best_practices section."}

## rate_limit_event

## Result (success)

**Cost:** $0.4151 | **Duration:** 208.1s


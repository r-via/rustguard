# Axis: utility

## User

## File: `rustguard-core/src/cookie.rs`
## Language: rust

```rust
//! WireGuard cookie mechanism for DoS protection.
//!
//! Under load, the responder can require proof that the initiator
//! can receive packets at its claimed IP. The flow:
//!
//! 1. Initiator sends handshake with MAC2 = zeros (no cookie yet)
//! 2. Responder is under load, sends Cookie Reply instead of processing
//! 3. Initiator decrypts cookie, stores it
//! 4. Initiator resends handshake with MAC2 = MAC(cookie, msg || mac1)
//! 5. Responder verifies MAC2 and processes the handshake
//!
//! The cookie is MAC(secret, sender_ip_port), where secret rotates every 2 min.

use core::time::Duration;

use rustguard_crypto::{self as crypto, PublicKey, LABEL_COOKIE, LABEL_MAC1};

use crate::messages::CookieReply;

/// How often the cookie secret rotates.
const COOKIE_SECRET_LIFETIME: Duration = Duration::from_secs(120);

/// Cookie size.
pub const COOKIE_LEN: usize = 16;

/// Monotonic timestamp type — same abstraction as timers.rs.
#[cfg(feature = "std")]
type Timestamp = std::time::Instant;
#[cfg(not(feature = "std"))]
type Timestamp = u64;

#[cfg(feature = "std")]
fn now() -> Timestamp {
    std::time::Instant::now()
}

#[cfg(feature = "std")]
fn elapsed_since(ts: Timestamp) -> Duration {
    ts.elapsed()
}

#[cfg(not(feature = "std"))]
fn elapsed_since(_ts: Timestamp) -> Duration {
    Duration::ZERO
}

/// Server-side cookie state: generates cookies and validates MAC2.
pub struct CookieChecker {
    /// Our static public key (used to derive encryption key).
    our_public: PublicKey,
    /// Rotating secret for cookie generation.
    secret: [u8; 32],
    /// When the secret was last generated.
    secret_generated: Timestamp,
    /// Whether we're "under load" and should require cookies.
    pub under_load: bool,
}

/// Client-side cookie state: stores a received cookie for MAC2.
pub struct CookieState {
    /// The decrypted cookie, if we have one.
    cookie: Option<[u8; COOKIE_LEN]>,
    /// When we received the cookie.
    received: Option<Timestamp>,
}

impl CookieChecker {
    #[cfg(feature = "std")]
    pub fn new(our_public: PublicKey) -> Self {
        Self {
            our_public,
            secret: random_bytes(),
            secret_generated: now(),
            under_load: false,
        }
    }

    /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
    pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
        Self {
            our_public,
            secret,
            secret_generated: now_ts,
            under_load: false,
        }
    }

    /// Rotate the secret if it's stale.
    fn maybe_rotate_secret(&mut self) {
        if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
            self.secret = random_bytes();
            #[cfg(feature = "std")]
            { self.secret_generated = now(); }
        }
    }

    /// Generate a cookie for pre-encoded source address bytes.
    /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
    ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
    fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
        self.maybe_rotate_secret();
        let full = crypto::mac(&self.secret, &[addr_bytes]);
        let mut cookie = [0u8; COOKIE_LEN];
        cookie.copy_from_slice(&full[..COOKIE_LEN]);
        cookie
    }

    /// Build a Cookie Reply message from pre-encoded address bytes.
    pub fn create_reply_from_bytes(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        addr_bytes: &[u8],
    ) -> CookieReply {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
        let nonce = random_nonce();
        let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);

        CookieReply {
            receiver_index,
            nonce,
            encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
        }
    }

    /// Build a Cookie Reply message for a SocketAddr (std convenience).
    #[cfg(feature = "std")]
    pub fn create_reply(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> CookieReply {
        let addr_bytes = encode_addr(src);
        self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
    }

    /// Verify MAC1 on an incoming message.
    pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
        let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
        let expected = crypto::mac(&key, &[msg_bytes]);
        constant_time_eq_16(mac1, &expected[..16])
    }

    /// Verify MAC2 from pre-encoded address bytes.
    pub fn verify_mac2_from_bytes(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        addr_bytes: &[u8],
    ) -> bool {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let expected = crypto::mac(&cookie, &[msg_with_mac1]);
        constant_time_eq_16(mac2, &expected[..16])
    }

    /// Verify MAC2 on an incoming message (std convenience).
    #[cfg(feature = "std")]
    pub fn verify_mac2(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> bool {
        let addr_bytes = encode_addr(src);
        self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
    }
}

impl CookieState {
    pub fn new() -> Self {
        Self {
            cookie: None,
            received: None,
        }
    }

    /// Process a Cookie Reply and store the decrypted cookie.
    ///
    /// `their_public`: the responder's public key.
    /// `mac1`: our MAC1 from the original message.
    pub fn process_reply(
        &mut self,
        reply: &CookieReply,
        their_public: &PublicKey,
        mac1: &[u8; 16],
    ) -> bool {
        let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
        match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
            Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                let mut cookie = [0u8; COOKIE_LEN];
                cookie.copy_from_slice(&plaintext);
                self.cookie = Some(cookie);
                #[cfg(feature = "std")]
                { self.received = Some(now()); }
                true
            }
            _ => false,
        }
    }

    /// Compute MAC2 using our stored cookie. Returns zeros if no cookie.
    pub fn compute_mac2(&self, msg_with_mac1: &[u8]) -> [u8; 16] {
        match &self.cookie {
            Some(cookie) if self.is_fresh() => {
                let full = crypto::mac(cookie, &[msg_with_mac1]);
                let mut mac2 = [0u8; 16];
                mac2.copy_from_slice(&full[..16]);
                mac2
            }
            _ => [0u8; 16],
        }
    }

    /// Cookie is fresh if received within the last 120 seconds.
    fn is_fresh(&self) -> bool {
        match self.received {
            Some(ts) => elapsed_since(ts) < COOKIE_SECRET_LIFETIME,
            None => false,
        }
    }
}

/// Encode a socket address to bytes for cookie MAC.
#[cfg(feature = "std")]
fn encode_addr(addr: &std::net::SocketAddr) -> alloc::vec::Vec<u8> {
    use alloc::vec::Vec;
    match addr {
        std::net::SocketAddr::V4(a) => {
            let mut buf = Vec::with_capacity(6);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
        std::net::SocketAddr::V6(a) => {
            let mut buf = Vec::with_capacity(18);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
    }
}

fn random_bytes() -> [u8; 32] {
    let mut buf = [0u8; 32];
    fill_random(&mut buf);
    buf
}

fn random_nonce() -> [u8; 24] {
    let mut buf = [0u8; 24];
    fill_random(&mut buf);
    buf
}

/// Fill a buffer with cryptographically secure random bytes.
/// On std, uses getrandom. On no_std, this is a stub — kernel module
/// must provide its own RNG via get_random_bytes().
#[cfg(feature = "std")]
fn fill_random(buf: &mut [u8]) {
    getrandom::getrandom(buf).expect("failed to get random bytes");
}

#[cfg(not(feature = "std"))]
fn fill_random(buf: &mut [u8]) {
    // Kernel module overrides this — stub zeros for compilation.
    // In practice, the kernel module uses its own cookie checker
    // that calls get_random_bytes() directly.
    let _ = buf;
}

fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
    if b.len() < 16 {
        return false;
    }
    use subtle::ConstantTimeEq;
    a.ct_eq(&b[..16]).into()
}

#[cfg(test)]
mod tests {
    use super::*;
    use rustguard_crypto::StaticSecret;

    fn setup() -> (CookieChecker, CookieState, PublicKey) {
        let secret = StaticSecret::random();
        let public = secret.public_key();
        let checker = CookieChecker::new(public.clone());
        let state = CookieState::new();
        (checker, state, public)
    }

    #[test]
    fn cookie_roundtrip() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(state.process_reply(&reply, &public, &mac1));
        assert!(state.cookie.is_some());
    }

    #[test]
    fn wrong_mac1_fails_decrypt() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];
        let wrong_mac1 = [0x43u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(!state.process_reply(&reply, &public, &wrong_mac1));
    }

    #[test]
    fn mac2_verified_after_cookie() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        // Get a cookie.
        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        // Compute MAC2 with the cookie.
        let msg = b"some message with mac1 appended";
        let mac2 = state.compute_mac2(msg);
        assert_ne!(mac2, [0u8; 16]); // Should not be zeros.

        // Verify MAC2.
        assert!(checker.verify_mac2(msg, &mac2, &src));
    }

    #[test]
    fn mac2_wrong_source_fails() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let wrong_src: std::net::SocketAddr = "10.0.0.2:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        let msg = b"message";
        let mac2 = state.compute_mac2(msg);

        // Different source should fail.
        assert!(!checker.verify_mac2(msg, &mac2, &wrong_src));
    }

    #[test]
    fn no_cookie_gives_zero_mac2() {
        let state = CookieState::new();
        let mac2 = state.compute_mac2(b"anything");
        assert_eq!(mac2, [0u8; 16]);
    }
}

```

## Symbols to evaluate

- constant COOKIE_SECRET_LIFETIME (L21–L21)
- export constant COOKIE_LEN (L24–L24)
- function now (L33–L35)
- function elapsed_since (L38–L40)
- function elapsed_since (L43–L45)
- export class CookieChecker (L48–L57)
- export class CookieState (L60–L65)
- function encode_addr (L227–L243)
- function random_bytes (L245–L249)
- function random_nonce (L251–L255)
- function fill_random (L261–L263)
- function fill_random (L266–L271)
- function constant_time_eq_16 (L273–L279)

## Pre-computed Import Analysis

- COOKIE_SECRET_LIFETIME (not exported): internal only — check local usage in file
- COOKIE_LEN (exported): imported by 0 files — LIKELY DEAD
- now (not exported): internal only — check local usage in file
- elapsed_since (not exported): internal only — check local usage in file
- elapsed_since (not exported): internal only — check local usage in file
- CookieChecker (exported): imported by 0 files — LIKELY DEAD
- CookieState (exported): imported by 0 files — LIKELY DEAD
- encode_addr (not exported): internal only — check local usage in file
- random_bytes (not exported): internal only — check local usage in file
- random_nonce (not exported): internal only — check local usage in file
- fill_random (not exported): internal only — check local usage in file
- fill_random (not exported): internal only — check local usage in file
- constant_time_eq_16 (not exported): internal only — check local usage in file

Evaluate the utility of each symbol and output the JSON.

## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "COOKIE_SECRET_LIFETIME",
      "line_start": 21,
      "line_end": 21,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used locally in maybe_rotate_secret (line 90) and is_fresh (line 212)"
    },
    {
      "name": "COOKIE_LEN",
      "line_start": 24,
      "line_end": 24,
      "utility": "DEAD",
      "confidence": 95,
      "detail": "Exported const with 0 external importers per pre-computed analysis. Rule 2: exported symbol with no importers = DEAD"
    },
    {
      "name": "now",
      "line_start": 33,
      "line_end": 35,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported std function used locally on lines 76 (new), 97 (maybe_rotate_secret), and 178 (process_reply)"
    },
    {
      "name": "elapsed_since",
      "line_start": 38,
      "line_end": 40,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported std version used on lines 89 (maybe_rotate_secret) and 212 (is_fresh)"
    },
    {
      "name": "elapsed_since",
      "line_start": 43,
      "line_end": 45,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported no_std stub version; called on lines 89 and 212 when std feature disabled"
    },
    {
      "name": "CookieChecker",
      "line_start": 48,
      "line_end": 57,
      "utility": "DEAD",
      "confidence": 95,
      "detail": "Exported struct with 0 external importers per pre-computed analysis. Rule 2: exported symbol with no importers = DEAD"
    },
    {
      "name": "CookieState",
      "line_start": 60,
      "line_end": 65,
      "utility": "DEAD",
      "confidence": 95,
      "detail": "Exported struct with 0 external importers per pre-computed analysis. Rule 2: exported symbol with no importers = DEAD"
    },
    {
      "name": "encode_addr",
      "line_start": 227,
      "line_end": 243,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported std-only function used locally on lines 130 (create_reply) and 162 (verify_mac2)"
    },
    {
      "name": "random_bytes",
      "line_start": 245,
      "line_end": 249,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported function used on lines 75 (new) and 93 (maybe_rotate_secret) to generate secrets"
    },
    {
      "name": "random_nonce",
      "line_start": 251,
      "line_end": 255,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported function used on line 117 in create_reply_from_bytes to generate cipher nonce"
    },
    {
      "name": "fill_random",
      "line_start": 261,
      "line_end": 263,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported std version called by random_bytes and random_nonce on lines 247 and 253"
    },
    {
      "name": "fill_random",
      "line_start": 266,
      "line_end": 271,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported no_std stub version; alternative implementation called when std feature disabled"
    },
    {
      "name": "constant_time_eq_16",
      "line_start": 273,
      "line_end": 279,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported function used in verify_mac1 (line 140) and verify_mac2_from_bytes (line 151) for constant-time comparison"
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.0720 | **Duration:** 89.3s


---

# Axis: duplication

## User

## File: `rustguard-core/src/cookie.rs`
## Language: rust

```rust
//! WireGuard cookie mechanism for DoS protection.
//!
//! Under load, the responder can require proof that the initiator
//! can receive packets at its claimed IP. The flow:
//!
//! 1. Initiator sends handshake with MAC2 = zeros (no cookie yet)
//! 2. Responder is under load, sends Cookie Reply instead of processing
//! 3. Initiator decrypts cookie, stores it
//! 4. Initiator resends handshake with MAC2 = MAC(cookie, msg || mac1)
//! 5. Responder verifies MAC2 and processes the handshake
//!
//! The cookie is MAC(secret, sender_ip_port), where secret rotates every 2 min.

use core::time::Duration;

use rustguard_crypto::{self as crypto, PublicKey, LABEL_COOKIE, LABEL_MAC1};

use crate::messages::CookieReply;

/// How often the cookie secret rotates.
const COOKIE_SECRET_LIFETIME: Duration = Duration::from_secs(120);

/// Cookie size.
pub const COOKIE_LEN: usize = 16;

/// Monotonic timestamp type — same abstraction as timers.rs.
#[cfg(feature = "std")]
type Timestamp = std::time::Instant;
#[cfg(not(feature = "std"))]
type Timestamp = u64;

#[cfg(feature = "std")]
fn now() -> Timestamp {
    std::time::Instant::now()
}

#[cfg(feature = "std")]
fn elapsed_since(ts: Timestamp) -> Duration {
    ts.elapsed()
}

#[cfg(not(feature = "std"))]
fn elapsed_since(_ts: Timestamp) -> Duration {
    Duration::ZERO
}

/// Server-side cookie state: generates cookies and validates MAC2.
pub struct CookieChecker {
    /// Our static public key (used to derive encryption key).
    our_public: PublicKey,
    /// Rotating secret for cookie generation.
    secret: [u8; 32],
    /// When the secret was last generated.
    secret_generated: Timestamp,
    /// Whether we're "under load" and should require cookies.
    pub under_load: bool,
}

/// Client-side cookie state: stores a received cookie for MAC2.
pub struct CookieState {
    /// The decrypted cookie, if we have one.
    cookie: Option<[u8; COOKIE_LEN]>,
    /// When we received the cookie.
    received: Option<Timestamp>,
}

impl CookieChecker {
    #[cfg(feature = "std")]
    pub fn new(our_public: PublicKey) -> Self {
        Self {
            our_public,
            secret: random_bytes(),
            secret_generated: now(),
            under_load: false,
        }
    }

    /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
    pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
        Self {
            our_public,
            secret,
            secret_generated: now_ts,
            under_load: false,
        }
    }

    /// Rotate the secret if it's stale.
    fn maybe_rotate_secret(&mut self) {
        if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
            self.secret = random_bytes();
            #[cfg(feature = "std")]
            { self.secret_generated = now(); }
        }
    }

    /// Generate a cookie for pre-encoded source address bytes.
    /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
    ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
    fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
        self.maybe_rotate_secret();
        let full = crypto::mac(&self.secret, &[addr_bytes]);
        let mut cookie = [0u8; COOKIE_LEN];
        cookie.copy_from_slice(&full[..COOKIE_LEN]);
        cookie
    }

    /// Build a Cookie Reply message from pre-encoded address bytes.
    pub fn create_reply_from_bytes(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        addr_bytes: &[u8],
    ) -> CookieReply {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
        let nonce = random_nonce();
        let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);

        CookieReply {
            receiver_index,
            nonce,
            encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
        }
    }

    /// Build a Cookie Reply message for a SocketAddr (std convenience).
    #[cfg(feature = "std")]
    pub fn create_reply(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> CookieReply {
        let addr_bytes = encode_addr(src);
        self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
    }

    /// Verify MAC1 on an incoming message.
    pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
        let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
        let expected = crypto::mac(&key, &[msg_bytes]);
        constant_time_eq_16(mac1, &expected[..16])
    }

    /// Verify MAC2 from pre-encoded address bytes.
    pub fn verify_mac2_from_bytes(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        addr_bytes: &[u8],
    ) -> bool {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let expected = crypto::mac(&cookie, &[msg_with_mac1]);
        constant_time_eq_16(mac2, &expected[..16])
    }

    /// Verify MAC2 on an incoming message (std convenience).
    #[cfg(feature = "std")]
    pub fn verify_mac2(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> bool {
        let addr_bytes = encode_addr(src);
        self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
    }
}

impl CookieState {
    pub fn new() -> Self {
        Self {
            cookie: None,
            received: None,
        }
    }

    /// Process a Cookie Reply and store the decrypted cookie.
    ///
    /// `their_public`: the responder's public key.
    /// `mac1`: our MAC1 from the original message.
    pub fn process_reply(
        &mut self,
        reply: &CookieReply,
        their_public: &PublicKey,
        mac1: &[u8; 16],
    ) -> bool {
        let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
        match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
            Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                let mut cookie = [0u8; COOKIE_LEN];
                cookie.copy_from_slice(&plaintext);
                self.cookie = Some(cookie);
                #[cfg(feature = "std")]
                { self.received = Some(now()); }
                true
            }
            _ => false,
        }
    }

    /// Compute MAC2 using our stored cookie. Returns zeros if no cookie.
    pub fn compute_mac2(&self, msg_with_mac1: &[u8]) -> [u8; 16] {
        match &self.cookie {
            Some(cookie) if self.is_fresh() => {
                let full = crypto::mac(cookie, &[msg_with_mac1]);
                let mut mac2 = [0u8; 16];
                mac2.copy_from_slice(&full[..16]);
                mac2
            }
            _ => [0u8; 16],
        }
    }

    /// Cookie is fresh if received within the last 120 seconds.
    fn is_fresh(&self) -> bool {
        match self.received {
            Some(ts) => elapsed_since(ts) < COOKIE_SECRET_LIFETIME,
            None => false,
        }
    }
}

/// Encode a socket address to bytes for cookie MAC.
#[cfg(feature = "std")]
fn encode_addr(addr: &std::net::SocketAddr) -> alloc::vec::Vec<u8> {
    use alloc::vec::Vec;
    match addr {
        std::net::SocketAddr::V4(a) => {
            let mut buf = Vec::with_capacity(6);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
        std::net::SocketAddr::V6(a) => {
            let mut buf = Vec::with_capacity(18);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
    }
}

fn random_bytes() -> [u8; 32] {
    let mut buf = [0u8; 32];
    fill_random(&mut buf);
    buf
}

fn random_nonce() -> [u8; 24] {
    let mut buf = [0u8; 24];
    fill_random(&mut buf);
    buf
}

/// Fill a buffer with cryptographically secure random bytes.
/// On std, uses getrandom. On no_std, this is a stub — kernel module
/// must provide its own RNG via get_random_bytes().
#[cfg(feature = "std")]
fn fill_random(buf: &mut [u8]) {
    getrandom::getrandom(buf).expect("failed to get random bytes");
}

#[cfg(not(feature = "std"))]
fn fill_random(buf: &mut [u8]) {
    // Kernel module overrides this — stub zeros for compilation.
    // In practice, the kernel module uses its own cookie checker
    // that calls get_random_bytes() directly.
    let _ = buf;
}

fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
    if b.len() < 16 {
        return false;
    }
    use subtle::ConstantTimeEq;
    a.ct_eq(&b[..16]).into()
}

#[cfg(test)]
mod tests {
    use super::*;
    use rustguard_crypto::StaticSecret;

    fn setup() -> (CookieChecker, CookieState, PublicKey) {
        let secret = StaticSecret::random();
        let public = secret.public_key();
        let checker = CookieChecker::new(public.clone());
        let state = CookieState::new();
        (checker, state, public)
    }

    #[test]
    fn cookie_roundtrip() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(state.process_reply(&reply, &public, &mac1));
        assert!(state.cookie.is_some());
    }

    #[test]
    fn wrong_mac1_fails_decrypt() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];
        let wrong_mac1 = [0x43u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(!state.process_reply(&reply, &public, &wrong_mac1));
    }

    #[test]
    fn mac2_verified_after_cookie() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        // Get a cookie.
        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        // Compute MAC2 with the cookie.
        let msg = b"some message with mac1 appended";
        let mac2 = state.compute_mac2(msg);
        assert_ne!(mac2, [0u8; 16]); // Should not be zeros.

        // Verify MAC2.
        assert!(checker.verify_mac2(msg, &mac2, &src));
    }

    #[test]
    fn mac2_wrong_source_fails() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let wrong_src: std::net::SocketAddr = "10.0.0.2:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        let msg = b"message";
        let mac2 = state.compute_mac2(msg);

        // Different source should fail.
        assert!(!checker.verify_mac2(msg, &mac2, &wrong_src));
    }

    #[test]
    fn no_cookie_gives_zero_mac2() {
        let state = CookieState::new();
        let mac2 = state.compute_mac2(b"anything");
        assert_eq!(mac2, [0u8; 16]);
    }
}

```

## Symbols to evaluate

- constant COOKIE_SECRET_LIFETIME (L21–L21)
- export constant COOKIE_LEN (L24–L24)
- function now (L33–L35)
- function elapsed_since (L38–L40)
- function elapsed_since (L43–L45)
- export class CookieChecker (L48–L57)
- export class CookieState (L60–L65)
- function encode_addr (L227–L243)
- function random_bytes (L245–L249)
- function random_nonce (L251–L255)
- function fill_random (L261–L263)
- function fill_random (L266–L271)
- function constant_time_eq_16 (L273–L279)

## RAG — Semantic Duplication

### now (L33–L35)
Trivial function (≤ 2 lines). Mark UNIQUE.

### elapsed_since (L38–L40)
Trivial function (≤ 2 lines). Mark UNIQUE.

### elapsed_since (L43–L45)
Trivial function (≤ 2 lines). Mark UNIQUE.

### encode_addr (L227–L243)
No similar functions found. Mark UNIQUE.

### random_bytes (L245–L249)
Similar functions found:
- **random_bytes** in `rustguard-kmod/src/noise.rs` (score: 0.841)
  Signature: fn random_bytes<const N: usize>() -> [u8; N] {
  Complexity: 1/5
  Source:
  ```rust
      fn wg_get_random_bytes(buf: *mut u8, len: u32);
      fn wg_memzero(ptr: *mut u8, len: usize);
      fn wg_crypto_memneq(a: *const u8, b: *const u8, len: usize) -> i32;
  }
  
  /// Zeroize a mutable byte slice using memzero_explicit (cannot be optimized away).
  pub(crate) fn zeroize(buf: &mut [u8]) {
      if !buf.is_empty() {
          unsafe { wg_memzero(buf.as_mut_ptr(), buf.len()) };
      }
  }
  
  /// Constant-time equality comparison using kernel crypto_memneq.
  fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
      if a.len() != b.len() {
          return false;
      }
      unsafe { wg_crypto_memneq(a.as_ptr(), b.as_ptr(), a.len()) == 0 }
  }
  
  // ── WireGuard protocol constants ──────────────────────────────────────
  
  const CONSTRUCTION: &[u8] = b"Noise_IKpsk2_25519_ChaChaPoly_BLAKE2s";
  const IDENTIFIER: &[u8] = b"WireGuard v1 zx2c4 Jason@zx2c4.com";
  const LABEL_MAC1: &[u8] = b"mac1----";
  
  /// TAI64N epoch offset (2^62 + 10 leap seconds).
  const TAI64_EPOCH_OFFSET: u64 = 0x4000_0000_0000_000a;
  
  // ── Crypto helpers (calling C shim) ───────────────────────────────────
  
  /// BLAKE2s-256 hash of concatenated chunks.
  fn hash(chunks: &[&[u8]]) -> [u8; 32] {
      let ptrs: [*const u8; 8] = {
          let mut p = [core::ptr::null(); 8];
          for (i, c) in chunks.iter().enumerate().take(8) {
              p[i] = c.as_ptr();
          }
          p
      };
      let lens: [u32; 8] = {
          let mut l = [0u32; 8];
          for (i, c) in chunks.iter().enumerate().take(8) {
              l[i] = c.len() as u32;
          }
          l
      };
      let mut out = [0u8; 32];
      unsafe {
          wg_blake2s_hash(ptrs.as_ptr(), lens.as_ptr(), chunks.len() as u32, out.as_mut_ptr());
  ```
- **random_bytes** in `rustguard-kmod/src/cookie.rs` (score: 0.828)
  Signature: fn random_bytes<const N: usize>() -> [u8; N] {
  Complexity: 1/5
  Source:
  ```rust
      fn wg_get_random_bytes(buf: *mut u8, len: u32);
      fn wg_ktime_get_ns() -> u64;
      // M7: Kernel crypto_memneq — guaranteed constant-time, not subject to compiler optimization.
      fn wg_crypto_memneq(a: *const u8, b: *const u8, len: usize) -> i32;
  }
  
  fn hash(chunks: &[&[u8]]) -> [u8; 32] {
      let mut ptrs = [core::ptr::null(); 4];
      let mut lens = [0u32; 4];
      for (i, c) in chunks.iter().enumerate().take(4) {
          ptrs[i] = c.as_ptr();
          lens[i] = c.len() as u32;
      }
      let mut out = [0u8; 32];
      unsafe { wg_blake2s_hash(ptrs.as_ptr(), lens.as_ptr(), chunks.len() as u32, out.as_mut_ptr()) };
      out
  }
  
  fn mac(key: &[u8], data: &[u8]) -> [u8; 32] {
      let mut out = [0u8; 32];
      unsafe { wg_blake2s_256_mac(key.as_ptr(), key.len() as u32, data.as_ptr(), data.len() as u32, out.as_mut_ptr()) };
      out
  }
  
  fn random_bytes<const N: usize>() -> [u8; N] {
      let mut buf = [0u8; N];
      unsafe { wg_get_random_bytes(buf.as_mut_ptr(), N as u32) };
      buf
  }
  
  /// Server-side: generates cookies and validates MAC2.
  pub(crate) struct CookieChecker {
      our_public: [u8; 32],
      secret: [u8; 32],
      secret_generated: u64,
      pub(crate) under_load: bool,
  }
  
  /// Client-side: stores a received cookie for MAC2.
  pub(crate) struct CookieState {
      cookie: Option<[u8; COOKIE_LEN]>,
      received: u64,
  }
  
  impl CookieChecker {
      pub(crate) fn new(our_public: [u8; 32]) -> Self {
          Self {
              our_public,
              secret: random_bytes(),
              secret_generated: unsafe { wg_ktime_get_ns() },
  ```
- **fill_random** in `rustguard-core/src/cookie.rs` (score: 0.762)
  Signature: fn fill_random(buf: &mut [u8]) {
  Complexity: 1/5
  Source:
  ```rust
      fill_random(&mut buf);
      buf
  }
  
  fn random_nonce() -> [u8; 24] {
      let mut buf = [0u8; 24];
      fill_random(&mut buf);
      buf
  }
  
  /// Fill a buffer with cryptographically secure random bytes.
  /// On std, uses getrandom. On no_std, this is a stub — kernel module
  /// must provide its own RNG via get_random_bytes().
  #[cfg(feature = "std")]
  fn fill_random(buf: &mut [u8]) {
      getrandom::getrandom(buf).expect("failed to get random bytes");
  }
  
  #[cfg(not(feature = "std"))]
  fn fill_random(buf: &mut [u8]) {
      // Kernel module overrides this — stub zeros for compilation.
      // In practice, the kernel module uses its own cookie checker
      // that calls get_random_bytes() directly.
      let _ = buf;
  }
  
  fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
      if b.len() < 16 {
          return false;
      }
      use subtle::ConstantTimeEq;
      a.ct_eq(&b[..16]).into()
  }
  
  #[cfg(test)]
  mod tests {
      use super::*;
      use rustguard_crypto::StaticSecret;
  
      fn setup() -> (CookieChecker, CookieState, PublicKey) {
          let secret = StaticSecret::random();
          let public = secret.public_key();
          let checker = CookieChecker::new(public.clone());
          let state = CookieState::new();
          (checker, state, public)
      }
  
      #[test]
      fn cookie_roundtrip() {
          let (mut checker, mut state, public) = setup();
  ```
- **random_nonce** in `rustguard-core/src/cookie.rs` (score: 0.762)
  Signature: fn random_nonce() -> [u8; 24] {
  Complexity: 1/5
  Calls: fill_random
  Source:
  ```rust
          let nonce = random_nonce();
          let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);
  
          CookieReply {
              receiver_index,
              nonce,
              encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
          }
      }
  
      /// Build a Cookie Reply message for a SocketAddr (std convenience).
      #[cfg(feature = "std")]
      pub fn create_reply(
          &mut self,
          receiver_index: u32,
          mac1: &[u8; 16],
          src: &std::net::SocketAddr,
      ) -> CookieReply {
          let addr_bytes = encode_addr(src);
          self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
      }
  
      /// Verify MAC1 on an incoming message.
      pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
          let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
          let expected = crypto::mac(&key, &[msg_bytes]);
          constant_time_eq_16(mac1, &expected[..16])
      }
  
      /// Verify MAC2 from pre-encoded address bytes.
      pub fn verify_mac2_from_bytes(
          &mut self,
          msg_with_mac1: &[u8],
          mac2: &[u8; 16],
          addr_bytes: &[u8],
      ) -> bool {
          let cookie = self.make_cookie_from_bytes(addr_bytes);
          let expected = crypto::mac(&cookie, &[msg_with_mac1]);
          constant_time_eq_16(mac2, &expected[..16])
      }
  
      /// Verify MAC2 on an incoming message (std convenience).
      #[cfg(feature = "std")]
      pub fn verify_mac2(
          &mut self,
          msg_with_mac1: &[u8],
          mac2: &[u8; 16],
          src: &std::net::SocketAddr,
      ) -> bool {
          let addr_bytes = encode_addr(src);
  ```
- **random_nonce** in `rustguard-enroll/src/protocol.rs` (score: 0.747)
  Signature: fn random_nonce() -> [u8; 24] {
  Complexity: 1/5
  Source:
  ```rust
      let nonce = random_nonce();
      let encrypted = crypto::xseal(token_key, &nonce, &ENROLL_REQUEST_MAGIC, our_pubkey);
  
      let mut buf = [0u8; ENROLL_REQUEST_SIZE];
      buf[0..4].copy_from_slice(&ENROLL_REQUEST_MAGIC);
      buf[4..28].copy_from_slice(&nonce);
      buf[28..76].copy_from_slice(&encrypted); // 32 + 16 tag = 48
      buf
  }
  
  /// Parse and decrypt an enrollment request.
  /// Returns the client's public key.
  pub fn parse_request(token_key: &[u8; 32], buf: &[u8]) -> Option<[u8; 32]> {
      if buf.len() < ENROLL_REQUEST_SIZE {
          return None;
      }
      if buf[0..4] != ENROLL_REQUEST_MAGIC {
          return None;
      }
      let nonce: [u8; 24] = buf[4..28].try_into().ok()?;
      let ciphertext = &buf[28..76];
      let plaintext = crypto::xopen(token_key, &nonce, &ENROLL_REQUEST_MAGIC, ciphertext)?;
      plaintext.try_into().ok()
  }
  
  /// Enrollment response payload: server pubkey + assigned IPv4 + prefix length.
  pub struct EnrollmentOffer {
      pub server_pubkey: [u8; 32],
      pub assigned_ip: std::net::Ipv4Addr,
      pub prefix_len: u8,
  }
  
  /// Build an enrollment response.
  pub fn build_response(token_key: &[u8; 32], offer: &EnrollmentOffer) -> [u8; ENROLL_RESPONSE_SIZE] {
      let mut plaintext = [0u8; 37]; // 32 + 4 + 1
      plaintext[0..32].copy_from_slice(&offer.server_pubkey);
      plaintext[32..36].copy_from_slice(&offer.assigned_ip.octets());
      plaintext[36] = offer.prefix_len;
  
      let nonce = random_nonce();
      let encrypted = crypto::xseal(token_key, &nonce, &ENROLL_RESPONSE_MAGIC, &plaintext);
  
      let mut buf = [0u8; ENROLL_RESPONSE_SIZE];
      buf[0..4].copy_from_slice(&ENROLL_RESPONSE_MAGIC);
      buf[4..28].copy_from_slice(&nonce);
      buf[28..81].copy_from_slice(&encrypted); // 37 + 16 tag = 53
      buf
  }
  
  /// Parse and decrypt an enrollment response.
  ```
- **fill_random** in `rustguard-daemon/src/tunnel.rs` (score: 0.732)
  Signature: fn fill_random(buf: &mut [u8]) {
  Complexity: 1/5
  Source:
  ```rust
      fill_random(&mut buf);
      u32::from_le_bytes(buf)
  }
  
  /// Read random bytes from the OS via the getrandom crate (cross-platform).
  fn fill_random(buf: &mut [u8]) {
      getrandom::getrandom(buf).expect("failed to get random bytes");
  }
  
  /// Install a Ctrl-C / SIGTERM handler.
  fn ctrlc_handler(f: impl FnOnce() + Send + 'static) {
      // Simple approach: spawn a thread that blocks on a signal.
      let f = std::sync::Mutex::new(Some(f));
      unsafe {
          libc::signal(libc::SIGINT, signal_noop as *const () as libc::sighandler_t);
          libc::signal(libc::SIGTERM, signal_noop as *const () as libc::sighandler_t);
      }
      thread::spawn(move || {
          let mut sigset: libc::sigset_t = unsafe { std::mem::zeroed() };
          unsafe {
              libc::sigemptyset(&mut sigset);
              libc::sigaddset(&mut sigset, libc::SIGINT);
              libc::sigaddset(&mut sigset, libc::SIGTERM);
              libc::sigprocmask(libc::SIG_BLOCK, &sigset, std::ptr::null_mut());
              let mut sig: libc::c_int = 0;
              libc::sigwait(&sigset, &mut sig);
          }
          if let Some(f) = f.lock().unwrap().take() {
              f();
          }
      });
  }
  
  extern "C" fn signal_noop(_: libc::c_int) {}
  
  fn base64_key(key: &[u8; 32]) -> String {
      use base64::prelude::*;
      BASE64_STANDARD.encode(key)
  }
  
  /// Assign an IPv6 address to a TUN interface.
  #[cfg(target_os = "macos")]
  fn assign_v6_address(ifname: &str, addr: &str) -> io::Result<std::process::Output> {
      Command::new("ifconfig")
          .args([ifname, "inet6", addr])
          .output()
  }
  
  #[cfg(target_os = "linux")]
  fn assign_v6_address(ifname: &str, addr: &str) -> io::Result<std::process::Output> {
  ```

### random_nonce (L251–L255)
Similar functions found:
- **random_nonce** in `rustguard-enroll/src/protocol.rs` (score: 0.968)
  Signature: fn random_nonce() -> [u8; 24] {
  Complexity: 1/5
  Source:
  ```rust
      let nonce = random_nonce();
      let encrypted = crypto::xseal(token_key, &nonce, &ENROLL_REQUEST_MAGIC, our_pubkey);
  
      let mut buf = [0u8; ENROLL_REQUEST_SIZE];
      buf[0..4].copy_from_slice(&ENROLL_REQUEST_MAGIC);
      buf[4..28].copy_from_slice(&nonce);
      buf[28..76].copy_from_slice(&encrypted); // 32 + 16 tag = 48
      buf
  }
  
  /// Parse and decrypt an enrollment request.
  /// Returns the client's public key.
  pub fn parse_request(token_key: &[u8; 32], buf: &[u8]) -> Option<[u8; 32]> {
      if buf.len() < ENROLL_REQUEST_SIZE {
          return None;
      }
      if buf[0..4] != ENROLL_REQUEST_MAGIC {
          return None;
      }
      let nonce: [u8; 24] = buf[4..28].try_into().ok()?;
      let ciphertext = &buf[28..76];
      let plaintext = crypto::xopen(token_key, &nonce, &ENROLL_REQUEST_MAGIC, ciphertext)?;
      plaintext.try_into().ok()
  }
  
  /// Enrollment response payload: server pubkey + assigned IPv4 + prefix length.
  pub struct EnrollmentOffer {
      pub server_pubkey: [u8; 32],
      pub assigned_ip: std::net::Ipv4Addr,
      pub prefix_len: u8,
  }
  
  /// Build an enrollment response.
  pub fn build_response(token_key: &[u8; 32], offer: &EnrollmentOffer) -> [u8; ENROLL_RESPONSE_SIZE] {
      let mut plaintext = [0u8; 37]; // 32 + 4 + 1
      plaintext[0..32].copy_from_slice(&offer.server_pubkey);
      plaintext[32..36].copy_from_slice(&offer.assigned_ip.octets());
      plaintext[36] = offer.prefix_len;
  
      let nonce = random_nonce();
      let encrypted = crypto::xseal(token_key, &nonce, &ENROLL_RESPONSE_MAGIC, &plaintext);
  
      let mut buf = [0u8; ENROLL_RESPONSE_SIZE];
      buf[0..4].copy_from_slice(&ENROLL_RESPONSE_MAGIC);
      buf[4..28].copy_from_slice(&nonce);
      buf[28..81].copy_from_slice(&encrypted); // 37 + 16 tag = 53
      buf
  }
  
  /// Parse and decrypt an enrollment response.
  ```
- **random_bytes** in `rustguard-core/src/cookie.rs` (score: 0.762)
  Signature: fn random_bytes() -> [u8; 32] {
  Complexity: 1/5
  Calls: fill_random
  Source:
  ```rust
              secret: random_bytes(),
              secret_generated: now(),
              under_load: false,
          }
      }
  
      /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
      pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
          Self {
              our_public,
              secret,
              secret_generated: now_ts,
              under_load: false,
          }
      }
  
      /// Rotate the secret if it's stale.
      fn maybe_rotate_secret(&mut self) {
          if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
              self.secret = random_bytes();
              #[cfg(feature = "std")]
              { self.secret_generated = now(); }
          }
      }
  
      /// Generate a cookie for pre-encoded source address bytes.
      /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
      ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
      fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
          self.maybe_rotate_secret();
          let full = crypto::mac(&self.secret, &[addr_bytes]);
          let mut cookie = [0u8; COOKIE_LEN];
          cookie.copy_from_slice(&full[..COOKIE_LEN]);
          cookie
      }
  
      /// Build a Cookie Reply message from pre-encoded address bytes.
      pub fn create_reply_from_bytes(
          &mut self,
          receiver_index: u32,
          mac1: &[u8; 16],
          addr_bytes: &[u8],
      ) -> CookieReply {
          let cookie = self.make_cookie_from_bytes(addr_bytes);
          let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
          let nonce = random_nonce();
          let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);
  
          CookieReply {
              receiver_index,
  ```
- **random_bytes** in `rustguard-kmod/src/noise.rs` (score: 0.721)
  Signature: fn random_bytes<const N: usize>() -> [u8; N] {
  Complexity: 1/5
  Source:
  ```rust
      fn wg_get_random_bytes(buf: *mut u8, len: u32);
      fn wg_memzero(ptr: *mut u8, len: usize);
      fn wg_crypto_memneq(a: *const u8, b: *const u8, len: usize) -> i32;
  }
  
  /// Zeroize a mutable byte slice using memzero_explicit (cannot be optimized away).
  pub(crate) fn zeroize(buf: &mut [u8]) {
      if !buf.is_empty() {
          unsafe { wg_memzero(buf.as_mut_ptr(), buf.len()) };
      }
  }
  
  /// Constant-time equality comparison using kernel crypto_memneq.
  fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
      if a.len() != b.len() {
          return false;
      }
      unsafe { wg_crypto_memneq(a.as_ptr(), b.as_ptr(), a.len()) == 0 }
  }
  
  // ── WireGuard protocol constants ──────────────────────────────────────
  
  const CONSTRUCTION: &[u8] = b"Noise_IKpsk2_25519_ChaChaPoly_BLAKE2s";
  const IDENTIFIER: &[u8] = b"WireGuard v1 zx2c4 Jason@zx2c4.com";
  const LABEL_MAC1: &[u8] = b"mac1----";
  
  /// TAI64N epoch offset (2^62 + 10 leap seconds).
  const TAI64_EPOCH_OFFSET: u64 = 0x4000_0000_0000_000a;
  
  // ── Crypto helpers (calling C shim) ───────────────────────────────────
  
  /// BLAKE2s-256 hash of concatenated chunks.
  fn hash(chunks: &[&[u8]]) -> [u8; 32] {
      let ptrs: [*const u8; 8] = {
          let mut p = [core::ptr::null(); 8];
          for (i, c) in chunks.iter().enumerate().take(8) {
              p[i] = c.as_ptr();
          }
          p
      };
      let lens: [u32; 8] = {
          let mut l = [0u32; 8];
          for (i, c) in chunks.iter().enumerate().take(8) {
              l[i] = c.len() as u32;
          }
          l
      };
      let mut out = [0u8; 32];
      unsafe {
          wg_blake2s_hash(ptrs.as_ptr(), lens.as_ptr(), chunks.len() as u32, out.as_mut_ptr());
  ```
- **random_bytes** in `rustguard-kmod/src/cookie.rs` (score: 0.720)
  Signature: fn random_bytes<const N: usize>() -> [u8; N] {
  Complexity: 1/5
  Source:
  ```rust
      fn wg_get_random_bytes(buf: *mut u8, len: u32);
      fn wg_ktime_get_ns() -> u64;
      // M7: Kernel crypto_memneq — guaranteed constant-time, not subject to compiler optimization.
      fn wg_crypto_memneq(a: *const u8, b: *const u8, len: usize) -> i32;
  }
  
  fn hash(chunks: &[&[u8]]) -> [u8; 32] {
      let mut ptrs = [core::ptr::null(); 4];
      let mut lens = [0u32; 4];
      for (i, c) in chunks.iter().enumerate().take(4) {
          ptrs[i] = c.as_ptr();
          lens[i] = c.len() as u32;
      }
      let mut out = [0u8; 32];
      unsafe { wg_blake2s_hash(ptrs.as_ptr(), lens.as_ptr(), chunks.len() as u32, out.as_mut_ptr()) };
      out
  }
  
  fn mac(key: &[u8], data: &[u8]) -> [u8; 32] {
      let mut out = [0u8; 32];
      unsafe { wg_blake2s_256_mac(key.as_ptr(), key.len() as u32, data.as_ptr(), data.len() as u32, out.as_mut_ptr()) };
      out
  }
  
  fn random_bytes<const N: usize>() -> [u8; N] {
      let mut buf = [0u8; N];
      unsafe { wg_get_random_bytes(buf.as_mut_ptr(), N as u32) };
      buf
  }
  
  /// Server-side: generates cookies and validates MAC2.
  pub(crate) struct CookieChecker {
      our_public: [u8; 32],
      secret: [u8; 32],
      secret_generated: u64,
      pub(crate) under_load: bool,
  }
  
  /// Client-side: stores a received cookie for MAC2.
  pub(crate) struct CookieState {
      cookie: Option<[u8; COOKIE_LEN]>,
      received: u64,
  }
  
  impl CookieChecker {
      pub(crate) fn new(our_public: [u8; 32]) -> Self {
          Self {
              our_public,
              secret: random_bytes(),
              secret_generated: unsafe { wg_ktime_get_ns() },
  ```
- **fill_random** in `rustguard-core/src/cookie.rs` (score: 0.700)
  Signature: fn fill_random(buf: &mut [u8]) {
  Complexity: 1/5
  Source:
  ```rust
      fill_random(&mut buf);
      buf
  }
  
  fn random_nonce() -> [u8; 24] {
      let mut buf = [0u8; 24];
      fill_random(&mut buf);
      buf
  }
  
  /// Fill a buffer with cryptographically secure random bytes.
  /// On std, uses getrandom. On no_std, this is a stub — kernel module
  /// must provide its own RNG via get_random_bytes().
  #[cfg(feature = "std")]
  fn fill_random(buf: &mut [u8]) {
      getrandom::getrandom(buf).expect("failed to get random bytes");
  }
  
  #[cfg(not(feature = "std"))]
  fn fill_random(buf: &mut [u8]) {
      // Kernel module overrides this — stub zeros for compilation.
      // In practice, the kernel module uses its own cookie checker
      // that calls get_random_bytes() directly.
      let _ = buf;
  }
  
  fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
      if b.len() < 16 {
          return false;
      }
      use subtle::ConstantTimeEq;
      a.ct_eq(&b[..16]).into()
  }
  
  #[cfg(test)]
  mod tests {
      use super::*;
      use rustguard_crypto::StaticSecret;
  
      fn setup() -> (CookieChecker, CookieState, PublicKey) {
          let secret = StaticSecret::random();
          let public = secret.public_key();
          let checker = CookieChecker::new(public.clone());
          let state = CookieState::new();
          (checker, state, public)
      }
  
      #[test]
      fn cookie_roundtrip() {
          let (mut checker, mut state, public) = setup();
  ```

### fill_random (L261–L263)
Trivial function (≤ 2 lines). Mark UNIQUE.

### fill_random (L266–L271)
Similar functions found:
- **fill_random** in `rustguard-daemon/src/tunnel.rs` (score: 1.000)
  Signature: fn fill_random(buf: &mut [u8]) {
  Complexity: 1/5
  Source:
  ```rust
      fill_random(&mut buf);
      u32::from_le_bytes(buf)
  }
  
  /// Read random bytes from the OS via the getrandom crate (cross-platform).
  fn fill_random(buf: &mut [u8]) {
      getrandom::getrandom(buf).expect("failed to get random bytes");
  }
  
  /// Install a Ctrl-C / SIGTERM handler.
  fn ctrlc_handler(f: impl FnOnce() + Send + 'static) {
      // Simple approach: spawn a thread that blocks on a signal.
      let f = std::sync::Mutex::new(Some(f));
      unsafe {
          libc::signal(libc::SIGINT, signal_noop as *const () as libc::sighandler_t);
          libc::signal(libc::SIGTERM, signal_noop as *const () as libc::sighandler_t);
      }
      thread::spawn(move || {
          let mut sigset: libc::sigset_t = unsafe { std::mem::zeroed() };
          unsafe {
              libc::sigemptyset(&mut sigset);
              libc::sigaddset(&mut sigset, libc::SIGINT);
              libc::sigaddset(&mut sigset, libc::SIGTERM);
              libc::sigprocmask(libc::SIG_BLOCK, &sigset, std::ptr::null_mut());
              let mut sig: libc::c_int = 0;
              libc::sigwait(&sigset, &mut sig);
          }
          if let Some(f) = f.lock().unwrap().take() {
              f();
          }
      });
  }
  
  extern "C" fn signal_noop(_: libc::c_int) {}
  
  fn base64_key(key: &[u8; 32]) -> String {
      use base64::prelude::*;
      BASE64_STANDARD.encode(key)
  }
  
  /// Assign an IPv6 address to a TUN interface.
  #[cfg(target_os = "macos")]
  fn assign_v6_address(ifname: &str, addr: &str) -> io::Result<std::process::Output> {
      Command::new("ifconfig")
          .args([ifname, "inet6", addr])
          .output()
  }
  
  #[cfg(target_os = "linux")]
  fn assign_v6_address(ifname: &str, addr: &str) -> io::Result<std::process::Output> {
  ```
- **random_bytes** in `rustguard-core/src/cookie.rs` (score: 0.740)
  Signature: fn random_bytes() -> [u8; 32] {
  Complexity: 1/5
  Calls: fill_random
  Source:
  ```rust
              secret: random_bytes(),
              secret_generated: now(),
              under_load: false,
          }
      }
  
      /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
      pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
          Self {
              our_public,
              secret,
              secret_generated: now_ts,
              under_load: false,
          }
      }
  
      /// Rotate the secret if it's stale.
      fn maybe_rotate_secret(&mut self) {
          if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
              self.secret = random_bytes();
              #[cfg(feature = "std")]
              { self.secret_generated = now(); }
          }
      }
  
      /// Generate a cookie for pre-encoded source address bytes.
      /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
      ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
      fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
          self.maybe_rotate_secret();
          let full = crypto::mac(&self.secret, &[addr_bytes]);
          let mut cookie = [0u8; COOKIE_LEN];
          cookie.copy_from_slice(&full[..COOKIE_LEN]);
          cookie
      }
  
      /// Build a Cookie Reply message from pre-encoded address bytes.
      pub fn create_reply_from_bytes(
          &mut self,
          receiver_index: u32,
          mac1: &[u8; 16],
          addr_bytes: &[u8],
      ) -> CookieReply {
          let cookie = self.make_cookie_from_bytes(addr_bytes);
          let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
          let nonce = random_nonce();
          let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);
  
          CookieReply {
              receiver_index,
  ```

### constant_time_eq_16 (L273–L279)
Similar functions found:
- **constant_time_eq** in `rustguard-core/src/handshake.rs` (score: 0.854)
  Signature: fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
  Complexity: 1/5
  Source:
  ```rust
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Decrypt initiator's static public key.
      let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
      h = new_h;
  
      let initiator_public = PublicKey::from_bytes(
          static_bytes
              .as_slice()
              .try_into()
              .ok()?,
      );
  
      // DH(our_static, initiator_static)
      let dh2 = our_static.diffie_hellman(&initiator_public);
      let key2;
      (ck, key2) = mix_key(&ck, &dh2);
  
      // Decrypt timestamp.
      let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
      h = new_h;
  
      // Validate timestamp is newer than last seen — prevents replay.
      let timestamp = Tai64n::from_bytes(
          timestamp_bytes.as_slice().try_into().ok()?,
      );
      if let Some(last) = last_timestamp {
          if !timestamp.is_after(last) {
              return None; // Replayed or stale initiation.
          }
      }
  ```
- **constant_time_eq** in `rustguard-kmod/src/cookie.rs` (score: 0.813)
  Signature: fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
  Complexity: 1/5
  Source:
  ```rust
          constant_time_eq(&mac1[..16], &expected[..16])
      }
  
      /// Verify MAC2 from pre-encoded address bytes.
      pub(crate) fn verify_mac2(&mut self, msg_with_mac1: &[u8], mac2: &[u8], addr_bytes: &[u8]) -> bool {
          if mac2.len() < 16 { return false; }
          let cookie = self.make_cookie(addr_bytes);
          let expected = mac(&cookie, msg_with_mac1);
          constant_time_eq(&mac2[..16], &expected[..16])
      }
  
      /// Create a Cookie Reply message (type 3, 64 bytes).
      pub(crate) fn create_reply(
          &mut self, receiver_index: u32, mac1: &[u8; 16], addr_bytes: &[u8],
      ) -> [u8; 64] {
          let cookie = self.make_cookie(addr_bytes);
          let key = hash(&[LABEL_COOKIE, &self.our_public]);
          let nonce: [u8; 24] = random_bytes();
  
          let mut encrypted_cookie = [0u8; 32]; // 16 cookie + 16 tag
          unsafe {
              wg_xchacha20poly1305_encrypt(
                  key.as_ptr(), nonce.as_ptr(),
                  cookie.as_ptr(), COOKIE_LEN as u32,
                  mac1.as_ptr(), 16,
                  encrypted_cookie.as_mut_ptr(),
              );
          }
  
          let mut reply = [0u8; 64];
          reply[0..4].copy_from_slice(&3u32.to_le_bytes()); // MSG_COOKIE_REPLY
          reply[4..8].copy_from_slice(&receiver_index.to_le_bytes());
          reply[8..32].copy_from_slice(&nonce);
          reply[32..64].copy_from_slice(&encrypted_cookie);
          reply
      }
  }
  
  impl CookieState {
      pub(crate) fn new() -> Self {
          Self { cookie: None, received: 0 }
      }
  
      /// Process a Cookie Reply and store the decrypted cookie.
      pub(crate) fn process_reply(
          &mut self, reply: &[u8; 64], their_public: &[u8; 32], our_mac1: &[u8; 16],
      ) -> bool {
          let nonce: [u8; 24] = reply[8..32].try_into().unwrap_or([0; 24]);
          let encrypted = &reply[32..64];
  
  ```
- **constant_time_eq** in `rustguard-kmod/src/noise.rs` (score: 0.813)
  Signature: fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
  Complexity: 1/5
  Source:
  ```rust
  fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
      if a.len() != b.len() {
          return false;
      }
      unsafe { wg_crypto_memneq(a.as_ptr(), b.as_ptr(), a.len()) == 0 }
  }
  
  // ── WireGuard protocol constants ──────────────────────────────────────
  
  const CONSTRUCTION: &[u8] = b"Noise_IKpsk2_25519_ChaChaPoly_BLAKE2s";
  const IDENTIFIER: &[u8] = b"WireGuard v1 zx2c4 Jason@zx2c4.com";
  const LABEL_MAC1: &[u8] = b"mac1----";
  
  /// TAI64N epoch offset (2^62 + 10 leap seconds).
  const TAI64_EPOCH_OFFSET: u64 = 0x4000_0000_0000_000a;
  
  // ── Crypto helpers (calling C shim) ───────────────────────────────────
  
  /// BLAKE2s-256 hash of concatenated chunks.
  fn hash(chunks: &[&[u8]]) -> [u8; 32] {
      let ptrs: [*const u8; 8] = {
          let mut p = [core::ptr::null(); 8];
          for (i, c) in chunks.iter().enumerate().take(8) {
              p[i] = c.as_ptr();
          }
          p
      };
      let lens: [u32; 8] = {
          let mut l = [0u32; 8];
          for (i, c) in chunks.iter().enumerate().take(8) {
              l[i] = c.len() as u32;
          }
          l
      };
      let mut out = [0u8; 32];
      unsafe {
          wg_blake2s_hash(ptrs.as_ptr(), lens.as_ptr(), chunks.len() as u32, out.as_mut_ptr());
      }
      out
  }
  
  /// Keyed BLAKE2s MAC (for MAC1/MAC2).
  fn mac(key: &[u8], data: &[u8]) -> [u8; 32] {
      let mut out = [0u8; 32];
      unsafe {
          wg_blake2s_256_mac(
              key.as_ptr(), key.len() as u32,
              data.as_ptr(), data.len() as u32,
              out.as_mut_ptr(),
          );
  ```

Evaluate the duplication of each symbol and output the JSON.

## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "COOKIE_SECRET_LIFETIME",
      "line_start": 21,
      "line_end": 21,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Simple constant declaration, no duplicates found in codebase",
      "duplicate_target": null
    },
    {
      "name": "COOKIE_LEN",
      "line_start": 24,
      "line_end": 24,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Exported constant for cookie byte length, no similar symbols found",
      "duplicate_target": null
    },
    {
      "name": "now",
      "line_start": 33,
      "line_end": 35,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Trivial 2-line function returning current time, conditional std feature",
      "duplicate_target": null
    },
    {
      "name": "elapsed_since",
      "line_start": 38,
      "line_end": 40,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Trivial 2-line std version computing elapsed duration since timestamp",
      "duplicate_target": null
    },
    {
      "name": "elapsed_since",
      "line_start": 43,
      "line_end": 45,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Trivial no_std stub returning zero duration; different semantic contract than std version",
      "duplicate_target": null
    },
    {
      "name": "CookieChecker",
      "line_start": 48,
      "line_end": 57,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Struct definition for server-side cookie state management, no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "CookieState",
      "line_start": 60,
      "line_end": 65,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Struct definition for client-side cookie state, no duplicates found",
      "duplicate_target": null
    },
    {
      "name": "encode_addr",
      "line_start": 227,
      "line_end": 243,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Encodes socket address to bytes for IPv4/IPv6; no similar functions found",
      "duplicate_target": null
    },
    {
      "name": "random_bytes",
      "line_start": 245,
      "line_end": 249,
      "duplication": "UNIQUE",
      "confidence": 85,
      "detail": "Generates 32-byte random array via fill_random; kmod versions have generic signature and different FFI impl",
      "duplicate_target": null
    },
    {
      "name": "random_nonce",
      "line_start": 251,
      "line_end": 255,
      "duplication": "DUPLICATE",
      "confidence": 90,
      "detail": "Generates 24-byte random nonce by filling buffer and returning it; extremely high semantic similarity",
      "duplicate_target": {
        "file": "rustguard-enroll/src/protocol.rs",
        "symbol": "random_nonce",
        "similarity": "0.968 — identical function pattern: fill buffer with random bytes, return nonce"
      }
    },
    {
      "name": "fill_random",
      "line_start": 261,
      "line_end": 263,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Trivial 2-line std feature version using getrandom crate",
      "duplicate_target": null
    },
    {
      "name": "fill_random",
      "line_start": 266,
      "line_end": 271,
      "duplication": "UNIQUE",
      "confidence": 85,
      "detail": "No_std stub implementation that is a no-op; different semantic contract than std version despite RAG similarity signal",
      "duplicate_target": null
    },
    {
      "name": "constant_time_eq_16",
      "line_start": 273,
      "line_end": 279,
      "duplication": "UNIQUE",
      "confidence": 80,
      "detail": "16-byte constant-time comparison with variable-length buffer handling; different signature and semantic contract than generic constant_time_eq in handshake.rs",
      "duplicate_target": null
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.0765 | **Duration:** 85.5s


---

# Axis: correction

## User

## File: `rustguard-core/src/cookie.rs`
## Language: rust

```rust
//! WireGuard cookie mechanism for DoS protection.
//!
//! Under load, the responder can require proof that the initiator
//! can receive packets at its claimed IP. The flow:
//!
//! 1. Initiator sends handshake with MAC2 = zeros (no cookie yet)
//! 2. Responder is under load, sends Cookie Reply instead of processing
//! 3. Initiator decrypts cookie, stores it
//! 4. Initiator resends handshake with MAC2 = MAC(cookie, msg || mac1)
//! 5. Responder verifies MAC2 and processes the handshake
//!
//! The cookie is MAC(secret, sender_ip_port), where secret rotates every 2 min.

use core::time::Duration;

use rustguard_crypto::{self as crypto, PublicKey, LABEL_COOKIE, LABEL_MAC1};

use crate::messages::CookieReply;

/// How often the cookie secret rotates.
const COOKIE_SECRET_LIFETIME: Duration = Duration::from_secs(120);

/// Cookie size.
pub const COOKIE_LEN: usize = 16;

/// Monotonic timestamp type — same abstraction as timers.rs.
#[cfg(feature = "std")]
type Timestamp = std::time::Instant;
#[cfg(not(feature = "std"))]
type Timestamp = u64;

#[cfg(feature = "std")]
fn now() -> Timestamp {
    std::time::Instant::now()
}

#[cfg(feature = "std")]
fn elapsed_since(ts: Timestamp) -> Duration {
    ts.elapsed()
}

#[cfg(not(feature = "std"))]
fn elapsed_since(_ts: Timestamp) -> Duration {
    Duration::ZERO
}

/// Server-side cookie state: generates cookies and validates MAC2.
pub struct CookieChecker {
    /// Our static public key (used to derive encryption key).
    our_public: PublicKey,
    /// Rotating secret for cookie generation.
    secret: [u8; 32],
    /// When the secret was last generated.
    secret_generated: Timestamp,
    /// Whether we're "under load" and should require cookies.
    pub under_load: bool,
}

/// Client-side cookie state: stores a received cookie for MAC2.
pub struct CookieState {
    /// The decrypted cookie, if we have one.
    cookie: Option<[u8; COOKIE_LEN]>,
    /// When we received the cookie.
    received: Option<Timestamp>,
}

impl CookieChecker {
    #[cfg(feature = "std")]
    pub fn new(our_public: PublicKey) -> Self {
        Self {
            our_public,
            secret: random_bytes(),
            secret_generated: now(),
            under_load: false,
        }
    }

    /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
    pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
        Self {
            our_public,
            secret,
            secret_generated: now_ts,
            under_load: false,
        }
    }

    /// Rotate the secret if it's stale.
    fn maybe_rotate_secret(&mut self) {
        if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
            self.secret = random_bytes();
            #[cfg(feature = "std")]
            { self.secret_generated = now(); }
        }
    }

    /// Generate a cookie for pre-encoded source address bytes.
    /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
    ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
    fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
        self.maybe_rotate_secret();
        let full = crypto::mac(&self.secret, &[addr_bytes]);
        let mut cookie = [0u8; COOKIE_LEN];
        cookie.copy_from_slice(&full[..COOKIE_LEN]);
        cookie
    }

    /// Build a Cookie Reply message from pre-encoded address bytes.
    pub fn create_reply_from_bytes(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        addr_bytes: &[u8],
    ) -> CookieReply {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
        let nonce = random_nonce();
        let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);

        CookieReply {
            receiver_index,
            nonce,
            encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
        }
    }

    /// Build a Cookie Reply message for a SocketAddr (std convenience).
    #[cfg(feature = "std")]
    pub fn create_reply(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> CookieReply {
        let addr_bytes = encode_addr(src);
        self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
    }

    /// Verify MAC1 on an incoming message.
    pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
        let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
        let expected = crypto::mac(&key, &[msg_bytes]);
        constant_time_eq_16(mac1, &expected[..16])
    }

    /// Verify MAC2 from pre-encoded address bytes.
    pub fn verify_mac2_from_bytes(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        addr_bytes: &[u8],
    ) -> bool {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let expected = crypto::mac(&cookie, &[msg_with_mac1]);
        constant_time_eq_16(mac2, &expected[..16])
    }

    /// Verify MAC2 on an incoming message (std convenience).
    #[cfg(feature = "std")]
    pub fn verify_mac2(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> bool {
        let addr_bytes = encode_addr(src);
        self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
    }
}

impl CookieState {
    pub fn new() -> Self {
        Self {
            cookie: None,
            received: None,
        }
    }

    /// Process a Cookie Reply and store the decrypted cookie.
    ///
    /// `their_public`: the responder's public key.
    /// `mac1`: our MAC1 from the original message.
    pub fn process_reply(
        &mut self,
        reply: &CookieReply,
        their_public: &PublicKey,
        mac1: &[u8; 16],
    ) -> bool {
        let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
        match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
            Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                let mut cookie = [0u8; COOKIE_LEN];
                cookie.copy_from_slice(&plaintext);
                self.cookie = Some(cookie);
                #[cfg(feature = "std")]
                { self.received = Some(now()); }
                true
            }
            _ => false,
        }
    }

    /// Compute MAC2 using our stored cookie. Returns zeros if no cookie.
    pub fn compute_mac2(&self, msg_with_mac1: &[u8]) -> [u8; 16] {
        match &self.cookie {
            Some(cookie) if self.is_fresh() => {
                let full = crypto::mac(cookie, &[msg_with_mac1]);
                let mut mac2 = [0u8; 16];
                mac2.copy_from_slice(&full[..16]);
                mac2
            }
            _ => [0u8; 16],
        }
    }

    /// Cookie is fresh if received within the last 120 seconds.
    fn is_fresh(&self) -> bool {
        match self.received {
            Some(ts) => elapsed_since(ts) < COOKIE_SECRET_LIFETIME,
            None => false,
        }
    }
}

/// Encode a socket address to bytes for cookie MAC.
#[cfg(feature = "std")]
fn encode_addr(addr: &std::net::SocketAddr) -> alloc::vec::Vec<u8> {
    use alloc::vec::Vec;
    match addr {
        std::net::SocketAddr::V4(a) => {
            let mut buf = Vec::with_capacity(6);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
        std::net::SocketAddr::V6(a) => {
            let mut buf = Vec::with_capacity(18);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
    }
}

fn random_bytes() -> [u8; 32] {
    let mut buf = [0u8; 32];
    fill_random(&mut buf);
    buf
}

fn random_nonce() -> [u8; 24] {
    let mut buf = [0u8; 24];
    fill_random(&mut buf);
    buf
}

/// Fill a buffer with cryptographically secure random bytes.
/// On std, uses getrandom. On no_std, this is a stub — kernel module
/// must provide its own RNG via get_random_bytes().
#[cfg(feature = "std")]
fn fill_random(buf: &mut [u8]) {
    getrandom::getrandom(buf).expect("failed to get random bytes");
}

#[cfg(not(feature = "std"))]
fn fill_random(buf: &mut [u8]) {
    // Kernel module overrides this — stub zeros for compilation.
    // In practice, the kernel module uses its own cookie checker
    // that calls get_random_bytes() directly.
    let _ = buf;
}

fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
    if b.len() < 16 {
        return false;
    }
    use subtle::ConstantTimeEq;
    a.ct_eq(&b[..16]).into()
}

#[cfg(test)]
mod tests {
    use super::*;
    use rustguard_crypto::StaticSecret;

    fn setup() -> (CookieChecker, CookieState, PublicKey) {
        let secret = StaticSecret::random();
        let public = secret.public_key();
        let checker = CookieChecker::new(public.clone());
        let state = CookieState::new();
        (checker, state, public)
    }

    #[test]
    fn cookie_roundtrip() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(state.process_reply(&reply, &public, &mac1));
        assert!(state.cookie.is_some());
    }

    #[test]
    fn wrong_mac1_fails_decrypt() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];
        let wrong_mac1 = [0x43u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(!state.process_reply(&reply, &public, &wrong_mac1));
    }

    #[test]
    fn mac2_verified_after_cookie() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        // Get a cookie.
        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        // Compute MAC2 with the cookie.
        let msg = b"some message with mac1 appended";
        let mac2 = state.compute_mac2(msg);
        assert_ne!(mac2, [0u8; 16]); // Should not be zeros.

        // Verify MAC2.
        assert!(checker.verify_mac2(msg, &mac2, &src));
    }

    #[test]
    fn mac2_wrong_source_fails() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let wrong_src: std::net::SocketAddr = "10.0.0.2:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        let msg = b"message";
        let mac2 = state.compute_mac2(msg);

        // Different source should fail.
        assert!(!checker.verify_mac2(msg, &mac2, &wrong_src));
    }

    #[test]
    fn no_cookie_gives_zero_mac2() {
        let state = CookieState::new();
        let mac2 = state.compute_mac2(b"anything");
        assert_eq!(mac2, [0u8; 16]);
    }
}

```

## Symbols to evaluate

- constant COOKIE_SECRET_LIFETIME (L21–L21)
- export constant COOKIE_LEN (L24–L24)
- function now (L33–L35)
- function elapsed_since (L38–L40)
- function elapsed_since (L43–L45)
- export class CookieChecker (L48–L57)
- export class CookieState (L60–L65)
- function encode_addr (L227–L243)
- function random_bytes (L245–L249)
- function random_nonce (L251–L255)
- function fill_random (L261–L263)
- function fill_random (L266–L271)
- function constant_time_eq_16 (L273–L279)

Identify any bugs, logic errors, or correctness issues and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "COOKIE_SECRET_LIFETIME",
      "line_start": 21,
      "line_end": 21,
      "correction": "OK",
      "confidence": 98,
      "detail": "Duration::from_secs(120) correctly represents the 2-minute secret rotation window stated in the module doc."
    },
    {
      "name": "COOKIE_LEN",
      "line_start": 24,
      "line_end": 24,
      "correction": "OK",
      "confidence": 98,
      "detail": "16-byte cookie size matches the WireGuard specification."
    },
    {
      "name": "now",
      "line_start": 33,
      "line_end": 35,
      "correction": "OK",
      "confidence": 98,
      "detail": "Correctly returns std::time::Instant::now() for the std platform; no correctness issues."
    },
    {
      "name": "elapsed_since",
      "line_start": 38,
      "line_end": 40,
      "correction": "OK",
      "confidence": 98,
      "detail": "Correctly delegates to Instant::elapsed() which handles monotonic clock arithmetic."
    },
    {
      "name": "elapsed_since",
      "line_start": 43,
      "line_end": 45,
      "correction": "NEEDS_FIX",
      "confidence": 72,
      "detail": "Stub always returns Duration::ZERO. Consequently, the condition in maybe_rotate_secret (ZERO >= 120 s) is permanently false, so the cookie secret provided to new_with never rotates regardless of real elapsed time. While documented as a no_std stub, this silently disables a security-critical rotation for any no_std caller expecting periodic rotation."
    },
    {
      "name": "CookieChecker",
      "line_start": 48,
      "line_end": 57,
      "correction": "OK",
      "confidence": 88,
      "detail": "Struct and impl are correct on std. On no_std the secret never rotates because elapsed_since always returns ZERO, but the code comments acknowledge the kernel module uses an independent mechanism, making this an intentional design limitation rather than an unintended bug."
    },
    {
      "name": "CookieState",
      "line_start": 60,
      "line_end": 65,
      "correction": "NEEDS_FIX",
      "confidence": 93,
      "detail": "In process_reply, self.received is assigned only inside #[cfg(feature = \"std\")]. On no_std, after a successful cookie decryption, received stays None. is_fresh() returns false when received is None, so compute_mac2 always returns [0u8; 16] on no_std. The cookie is correctly decrypted and stored in self.cookie but is permanently treated as stale — the entire client-side cookie mechanism is non-functional on no_std."
    },
    {
      "name": "encode_addr",
      "line_start": 227,
      "line_end": 243,
      "correction": "NEEDS_FIX",
      "confidence": 65,
      "detail": "The function (gated with #[cfg(feature = \"std\")]) uses the path alloc::vec::Vec. In a crate that declares extern crate alloc only for no_std (a common pattern), the alloc:: namespace is not in scope when compiling with the std feature, causing a compile error. The return type and the Vec::with_capacity calls should use std::vec::Vec or an unqualified Vec via a use statement."
    },
    {
      "name": "random_bytes",
      "line_start": 245,
      "line_end": 249,
      "correction": "OK",
      "confidence": 90,
      "detail": "Correctly allocates a zeroed 32-byte buffer and delegates to fill_random; any defect is in fill_random itself."
    },
    {
      "name": "random_nonce",
      "line_start": 251,
      "line_end": 255,
      "correction": "OK",
      "confidence": 90,
      "detail": "24-byte nonce buffer is correct for XSalsa20 (used by xseal/xopen); delegation to fill_random is correct."
    },
    {
      "name": "fill_random",
      "line_start": 261,
      "line_end": 263,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly calls getrandom::getrandom and panics on failure; panicking is appropriate for a cryptographic RNG that must not silently degrade."
    },
    {
      "name": "fill_random",
      "line_start": 266,
      "line_end": 271,
      "correction": "NEEDS_FIX",
      "confidence": 87,
      "detail": "let _ = buf discards the mutable reference without writing any bytes; the buffer remains all zeros. Every call to random_bytes() or random_nonce() on no_std therefore returns an all-zero secret or nonce. A zero cookie secret means all peers share the same cookie, destroying DoS protection; a zero nonce violates the uniqueness requirement of the AEAD construction. Even as a compilation stub this is dangerous — it should at minimum emit a compile_error! or panic so misuse cannot occur silently."
    },
    {
      "name": "constant_time_eq_16",
      "line_start": 273,
      "line_end": 279,
      "correction": "NEEDS_FIX",
      "confidence": 76,
      "detail": "The early return 'if b.len() < 16 { return false; }' is a data-dependent branch that leaks whether b is shorter than 16 bytes via a timing side channel, violating the constant-time contract implied by the function's use of subtle::ConstantTimeEq. All current callers pass &expected[..16] (always exactly 16 bytes) so the branch is never taken in practice, but a future caller with variable-length input could trigger a timing oracle. The check should be replaced with a constant-time length comparison or the function should be restricted to [u8; 16] inputs."
    }
  ],
  "actions": [
    {
      "description": "CookieState::process_reply: on no_std, self.received is never set after a successful decryption. Accept a monotonic timestamp parameter (or a no_std-compatible clock abstraction) and store it unconditionally so that is_fresh() can function correctly and compute_mac2 returns a real MAC instead of zeros.",
      "severity": "MAJOR",
      "line": 213
    },
    {
      "description": "no_std fill_random stub silently produces all-zero output (let _ = buf is a no-op). Replace with a compile_error! macro or a link-time weak symbol that the kernel module must override, preventing accidental silent use of zero secrets and nonces.",
      "severity": "CRITICAL",
      "line": 269
    },
    {
      "description": "encode_addr uses alloc::vec::Vec inside a #[cfg(feature = \"std\")] function; this requires extern crate alloc in scope in std mode, which is typically not declared. Replace alloc::vec::Vec with std::vec::Vec (or introduce use std::vec::Vec) to ensure the code compiles with the std feature.",
      "severity": "MAJOR",
      "line": 230
    },
    {
      "description": "constant_time_eq_16: the early-exit branch on b.len() < 16 is not constant-time. Change the signature to accept &[u8; 16] for b (eliminating the length check entirely) or use a constant-time Choice-based comparison that does not branch on length.",
      "severity": "MINOR",
      "line": 275
    },
    {
      "description": "no_std elapsed_since always returns Duration::ZERO, permanently disabling cookie-secret rotation in maybe_rotate_secret. If no_std callers require rotation, provide a monotonic tick counter through the existing new_with constructor or a dedicated rotate_if_stale(now_ts) method.",
      "severity": "MINOR",
      "line": 44
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.4290 | **Duration:** 244.2s


---

# Axis: overengineering

## User

## File: `rustguard-core/src/cookie.rs`
## Language: rust

```rust
//! WireGuard cookie mechanism for DoS protection.
//!
//! Under load, the responder can require proof that the initiator
//! can receive packets at its claimed IP. The flow:
//!
//! 1. Initiator sends handshake with MAC2 = zeros (no cookie yet)
//! 2. Responder is under load, sends Cookie Reply instead of processing
//! 3. Initiator decrypts cookie, stores it
//! 4. Initiator resends handshake with MAC2 = MAC(cookie, msg || mac1)
//! 5. Responder verifies MAC2 and processes the handshake
//!
//! The cookie is MAC(secret, sender_ip_port), where secret rotates every 2 min.

use core::time::Duration;

use rustguard_crypto::{self as crypto, PublicKey, LABEL_COOKIE, LABEL_MAC1};

use crate::messages::CookieReply;

/// How often the cookie secret rotates.
const COOKIE_SECRET_LIFETIME: Duration = Duration::from_secs(120);

/// Cookie size.
pub const COOKIE_LEN: usize = 16;

/// Monotonic timestamp type — same abstraction as timers.rs.
#[cfg(feature = "std")]
type Timestamp = std::time::Instant;
#[cfg(not(feature = "std"))]
type Timestamp = u64;

#[cfg(feature = "std")]
fn now() -> Timestamp {
    std::time::Instant::now()
}

#[cfg(feature = "std")]
fn elapsed_since(ts: Timestamp) -> Duration {
    ts.elapsed()
}

#[cfg(not(feature = "std"))]
fn elapsed_since(_ts: Timestamp) -> Duration {
    Duration::ZERO
}

/// Server-side cookie state: generates cookies and validates MAC2.
pub struct CookieChecker {
    /// Our static public key (used to derive encryption key).
    our_public: PublicKey,
    /// Rotating secret for cookie generation.
    secret: [u8; 32],
    /// When the secret was last generated.
    secret_generated: Timestamp,
    /// Whether we're "under load" and should require cookies.
    pub under_load: bool,
}

/// Client-side cookie state: stores a received cookie for MAC2.
pub struct CookieState {
    /// The decrypted cookie, if we have one.
    cookie: Option<[u8; COOKIE_LEN]>,
    /// When we received the cookie.
    received: Option<Timestamp>,
}

impl CookieChecker {
    #[cfg(feature = "std")]
    pub fn new(our_public: PublicKey) -> Self {
        Self {
            our_public,
            secret: random_bytes(),
            secret_generated: now(),
            under_load: false,
        }
    }

    /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
    pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
        Self {
            our_public,
            secret,
            secret_generated: now_ts,
            under_load: false,
        }
    }

    /// Rotate the secret if it's stale.
    fn maybe_rotate_secret(&mut self) {
        if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
            self.secret = random_bytes();
            #[cfg(feature = "std")]
            { self.secret_generated = now(); }
        }
    }

    /// Generate a cookie for pre-encoded source address bytes.
    /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
    ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
    fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
        self.maybe_rotate_secret();
        let full = crypto::mac(&self.secret, &[addr_bytes]);
        let mut cookie = [0u8; COOKIE_LEN];
        cookie.copy_from_slice(&full[..COOKIE_LEN]);
        cookie
    }

    /// Build a Cookie Reply message from pre-encoded address bytes.
    pub fn create_reply_from_bytes(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        addr_bytes: &[u8],
    ) -> CookieReply {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
        let nonce = random_nonce();
        let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);

        CookieReply {
            receiver_index,
            nonce,
            encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
        }
    }

    /// Build a Cookie Reply message for a SocketAddr (std convenience).
    #[cfg(feature = "std")]
    pub fn create_reply(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> CookieReply {
        let addr_bytes = encode_addr(src);
        self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
    }

    /// Verify MAC1 on an incoming message.
    pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
        let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
        let expected = crypto::mac(&key, &[msg_bytes]);
        constant_time_eq_16(mac1, &expected[..16])
    }

    /// Verify MAC2 from pre-encoded address bytes.
    pub fn verify_mac2_from_bytes(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        addr_bytes: &[u8],
    ) -> bool {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let expected = crypto::mac(&cookie, &[msg_with_mac1]);
        constant_time_eq_16(mac2, &expected[..16])
    }

    /// Verify MAC2 on an incoming message (std convenience).
    #[cfg(feature = "std")]
    pub fn verify_mac2(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> bool {
        let addr_bytes = encode_addr(src);
        self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
    }
}

impl CookieState {
    pub fn new() -> Self {
        Self {
            cookie: None,
            received: None,
        }
    }

    /// Process a Cookie Reply and store the decrypted cookie.
    ///
    /// `their_public`: the responder's public key.
    /// `mac1`: our MAC1 from the original message.
    pub fn process_reply(
        &mut self,
        reply: &CookieReply,
        their_public: &PublicKey,
        mac1: &[u8; 16],
    ) -> bool {
        let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
        match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
            Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                let mut cookie = [0u8; COOKIE_LEN];
                cookie.copy_from_slice(&plaintext);
                self.cookie = Some(cookie);
                #[cfg(feature = "std")]
                { self.received = Some(now()); }
                true
            }
            _ => false,
        }
    }

    /// Compute MAC2 using our stored cookie. Returns zeros if no cookie.
    pub fn compute_mac2(&self, msg_with_mac1: &[u8]) -> [u8; 16] {
        match &self.cookie {
            Some(cookie) if self.is_fresh() => {
                let full = crypto::mac(cookie, &[msg_with_mac1]);
                let mut mac2 = [0u8; 16];
                mac2.copy_from_slice(&full[..16]);
                mac2
            }
            _ => [0u8; 16],
        }
    }

    /// Cookie is fresh if received within the last 120 seconds.
    fn is_fresh(&self) -> bool {
        match self.received {
            Some(ts) => elapsed_since(ts) < COOKIE_SECRET_LIFETIME,
            None => false,
        }
    }
}

/// Encode a socket address to bytes for cookie MAC.
#[cfg(feature = "std")]
fn encode_addr(addr: &std::net::SocketAddr) -> alloc::vec::Vec<u8> {
    use alloc::vec::Vec;
    match addr {
        std::net::SocketAddr::V4(a) => {
            let mut buf = Vec::with_capacity(6);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
        std::net::SocketAddr::V6(a) => {
            let mut buf = Vec::with_capacity(18);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
    }
}

fn random_bytes() -> [u8; 32] {
    let mut buf = [0u8; 32];
    fill_random(&mut buf);
    buf
}

fn random_nonce() -> [u8; 24] {
    let mut buf = [0u8; 24];
    fill_random(&mut buf);
    buf
}

/// Fill a buffer with cryptographically secure random bytes.
/// On std, uses getrandom. On no_std, this is a stub — kernel module
/// must provide its own RNG via get_random_bytes().
#[cfg(feature = "std")]
fn fill_random(buf: &mut [u8]) {
    getrandom::getrandom(buf).expect("failed to get random bytes");
}

#[cfg(not(feature = "std"))]
fn fill_random(buf: &mut [u8]) {
    // Kernel module overrides this — stub zeros for compilation.
    // In practice, the kernel module uses its own cookie checker
    // that calls get_random_bytes() directly.
    let _ = buf;
}

fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
    if b.len() < 16 {
        return false;
    }
    use subtle::ConstantTimeEq;
    a.ct_eq(&b[..16]).into()
}

#[cfg(test)]
mod tests {
    use super::*;
    use rustguard_crypto::StaticSecret;

    fn setup() -> (CookieChecker, CookieState, PublicKey) {
        let secret = StaticSecret::random();
        let public = secret.public_key();
        let checker = CookieChecker::new(public.clone());
        let state = CookieState::new();
        (checker, state, public)
    }

    #[test]
    fn cookie_roundtrip() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(state.process_reply(&reply, &public, &mac1));
        assert!(state.cookie.is_some());
    }

    #[test]
    fn wrong_mac1_fails_decrypt() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];
        let wrong_mac1 = [0x43u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(!state.process_reply(&reply, &public, &wrong_mac1));
    }

    #[test]
    fn mac2_verified_after_cookie() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        // Get a cookie.
        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        // Compute MAC2 with the cookie.
        let msg = b"some message with mac1 appended";
        let mac2 = state.compute_mac2(msg);
        assert_ne!(mac2, [0u8; 16]); // Should not be zeros.

        // Verify MAC2.
        assert!(checker.verify_mac2(msg, &mac2, &src));
    }

    #[test]
    fn mac2_wrong_source_fails() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let wrong_src: std::net::SocketAddr = "10.0.0.2:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        let msg = b"message";
        let mac2 = state.compute_mac2(msg);

        // Different source should fail.
        assert!(!checker.verify_mac2(msg, &mac2, &wrong_src));
    }

    #[test]
    fn no_cookie_gives_zero_mac2() {
        let state = CookieState::new();
        let mac2 = state.compute_mac2(b"anything");
        assert_eq!(mac2, [0u8; 16]);
    }
}

```

## Symbols to evaluate

- constant COOKIE_SECRET_LIFETIME (L21–L21)
- export constant COOKIE_LEN (L24–L24)
- function now (L33–L35)
- function elapsed_since (L38–L40)
- function elapsed_since (L43–L45)
- export class CookieChecker (L48–L57)
- export class CookieState (L60–L65)
- function encode_addr (L227–L243)
- function random_bytes (L245–L249)
- function random_nonce (L251–L255)
- function fill_random (L261–L263)
- function fill_random (L266–L271)
- function constant_time_eq_16 (L273–L279)

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Use the project structure to detect excessive fragmentation:
- Directory with only 1 file → potential fragmentation
- More than 5 nesting levels → excessive structural complexity
- Factory/adapter directories with ≤ 2 files → likely over-engineering
These heuristics may increase a symbol's rating from LEAN to OVER.

Evaluate the complexity of each symbol and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "COOKIE_SECRET_LIFETIME",
      "line_start": 21,
      "line_end": 21,
      "overengineering": "LEAN",
      "confidence": 98,
      "detail": "Simple named constant encoding the WireGuard spec's 2-minute secret rotation period. No abstraction beyond what the spec requires."
    },
    {
      "name": "COOKIE_LEN",
      "line_start": 24,
      "line_end": 24,
      "overengineering": "LEAN",
      "confidence": 98,
      "detail": "Minimal constant for the 16-byte cookie size. Appropriately exported for use by message layout code."
    },
    {
      "name": "now",
      "line_start": 33,
      "line_end": 35,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Thin cfg-gated wrapper that centralizes the std/no_std timestamp abstraction in one place rather than scattering #[cfg] attributes throughout impl blocks. Minimal and justified."
    },
    {
      "name": "elapsed_since",
      "line_start": 38,
      "line_end": 40,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Std variant is a one-liner delegating to Instant::elapsed. Needed to pair with the no_std stub and keep cfg logic out of business logic."
    },
    {
      "name": "elapsed_since",
      "line_start": 43,
      "line_end": 45,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "No_std stub returning Duration::ZERO is the minimal correct behaviour; the comment correctly states kernel builds use their own cookie checker. No excess here."
    },
    {
      "name": "CookieChecker",
      "line_start": 48,
      "line_end": 57,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Struct fields map 1-to-1 onto what the WireGuard spec requires for server-side cookie state: public key for key derivation, rotating secret, its timestamp, and a load flag. Nothing extraneous."
    },
    {
      "name": "CookieState",
      "line_start": 60,
      "line_end": 65,
      "overengineering": "LEAN",
      "confidence": 97,
      "detail": "Two fields (cookie bytes + received timestamp) are the minimal client-side state needed to produce MAC2 and check freshness per spec."
    },
    {
      "name": "encode_addr",
      "line_start": 227,
      "line_end": 243,
      "overengineering": "LEAN",
      "confidence": 92,
      "detail": "Straightforward IPv4/IPv6 serialisation into raw bytes for cookie MAC input. The match on V4/V6 is unavoidable given Rust's SocketAddr enum. Vec allocation is fine on the std path."
    },
    {
      "name": "random_bytes",
      "line_start": 245,
      "line_end": 249,
      "overengineering": "LEAN",
      "confidence": 93,
      "detail": "Thin wrapper that allocates a 32-byte buffer and delegates to fill_random. Keeps call sites clean. A const-generic unification with random_nonce is possible but would add complexity, not reduce it."
    },
    {
      "name": "random_nonce",
      "line_start": 251,
      "line_end": 255,
      "overengineering": "LEAN",
      "confidence": 93,
      "detail": "Mirrors random_bytes for a 24-byte nonce. The duplication is trivial (4 lines) and unifying into a const-generic helper would not be simpler overall."
    },
    {
      "name": "fill_random",
      "line_start": 261,
      "line_end": 263,
      "overengineering": "LEAN",
      "confidence": 96,
      "detail": "Single-line std impl delegating directly to getrandom. Appropriate for a crypto-level primitive."
    },
    {
      "name": "fill_random",
      "line_start": 266,
      "line_end": 271,
      "overengineering": "LEAN",
      "confidence": 96,
      "detail": "No_std stub is the minimal compilable placeholder. The comment is honest about the kernel module responsibility. No overengineering."
    },
    {
      "name": "constant_time_eq_16",
      "line_start": 273,
      "line_end": 279,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Security-critical constant-time comparison using the subtle crate. The mixed-type signature (fixed array vs slice) is intentional to match call sites where one operand comes from a slice. Minimal and correct."
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.1067 | **Duration:** 38.3s


---

# Axis: tests

## User

## File: `rustguard-core/src/cookie.rs`
## Language: rust

```rust
//! WireGuard cookie mechanism for DoS protection.
//!
//! Under load, the responder can require proof that the initiator
//! can receive packets at its claimed IP. The flow:
//!
//! 1. Initiator sends handshake with MAC2 = zeros (no cookie yet)
//! 2. Responder is under load, sends Cookie Reply instead of processing
//! 3. Initiator decrypts cookie, stores it
//! 4. Initiator resends handshake with MAC2 = MAC(cookie, msg || mac1)
//! 5. Responder verifies MAC2 and processes the handshake
//!
//! The cookie is MAC(secret, sender_ip_port), where secret rotates every 2 min.

use core::time::Duration;

use rustguard_crypto::{self as crypto, PublicKey, LABEL_COOKIE, LABEL_MAC1};

use crate::messages::CookieReply;

/// How often the cookie secret rotates.
const COOKIE_SECRET_LIFETIME: Duration = Duration::from_secs(120);

/// Cookie size.
pub const COOKIE_LEN: usize = 16;

/// Monotonic timestamp type — same abstraction as timers.rs.
#[cfg(feature = "std")]
type Timestamp = std::time::Instant;
#[cfg(not(feature = "std"))]
type Timestamp = u64;

#[cfg(feature = "std")]
fn now() -> Timestamp {
    std::time::Instant::now()
}

#[cfg(feature = "std")]
fn elapsed_since(ts: Timestamp) -> Duration {
    ts.elapsed()
}

#[cfg(not(feature = "std"))]
fn elapsed_since(_ts: Timestamp) -> Duration {
    Duration::ZERO
}

/// Server-side cookie state: generates cookies and validates MAC2.
pub struct CookieChecker {
    /// Our static public key (used to derive encryption key).
    our_public: PublicKey,
    /// Rotating secret for cookie generation.
    secret: [u8; 32],
    /// When the secret was last generated.
    secret_generated: Timestamp,
    /// Whether we're "under load" and should require cookies.
    pub under_load: bool,
}

/// Client-side cookie state: stores a received cookie for MAC2.
pub struct CookieState {
    /// The decrypted cookie, if we have one.
    cookie: Option<[u8; COOKIE_LEN]>,
    /// When we received the cookie.
    received: Option<Timestamp>,
}

impl CookieChecker {
    #[cfg(feature = "std")]
    pub fn new(our_public: PublicKey) -> Self {
        Self {
            our_public,
            secret: random_bytes(),
            secret_generated: now(),
            under_load: false,
        }
    }

    /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
    pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
        Self {
            our_public,
            secret,
            secret_generated: now_ts,
            under_load: false,
        }
    }

    /// Rotate the secret if it's stale.
    fn maybe_rotate_secret(&mut self) {
        if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
            self.secret = random_bytes();
            #[cfg(feature = "std")]
            { self.secret_generated = now(); }
        }
    }

    /// Generate a cookie for pre-encoded source address bytes.
    /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
    ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
    fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
        self.maybe_rotate_secret();
        let full = crypto::mac(&self.secret, &[addr_bytes]);
        let mut cookie = [0u8; COOKIE_LEN];
        cookie.copy_from_slice(&full[..COOKIE_LEN]);
        cookie
    }

    /// Build a Cookie Reply message from pre-encoded address bytes.
    pub fn create_reply_from_bytes(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        addr_bytes: &[u8],
    ) -> CookieReply {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
        let nonce = random_nonce();
        let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);

        CookieReply {
            receiver_index,
            nonce,
            encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
        }
    }

    /// Build a Cookie Reply message for a SocketAddr (std convenience).
    #[cfg(feature = "std")]
    pub fn create_reply(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> CookieReply {
        let addr_bytes = encode_addr(src);
        self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
    }

    /// Verify MAC1 on an incoming message.
    pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
        let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
        let expected = crypto::mac(&key, &[msg_bytes]);
        constant_time_eq_16(mac1, &expected[..16])
    }

    /// Verify MAC2 from pre-encoded address bytes.
    pub fn verify_mac2_from_bytes(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        addr_bytes: &[u8],
    ) -> bool {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let expected = crypto::mac(&cookie, &[msg_with_mac1]);
        constant_time_eq_16(mac2, &expected[..16])
    }

    /// Verify MAC2 on an incoming message (std convenience).
    #[cfg(feature = "std")]
    pub fn verify_mac2(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> bool {
        let addr_bytes = encode_addr(src);
        self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
    }
}

impl CookieState {
    pub fn new() -> Self {
        Self {
            cookie: None,
            received: None,
        }
    }

    /// Process a Cookie Reply and store the decrypted cookie.
    ///
    /// `their_public`: the responder's public key.
    /// `mac1`: our MAC1 from the original message.
    pub fn process_reply(
        &mut self,
        reply: &CookieReply,
        their_public: &PublicKey,
        mac1: &[u8; 16],
    ) -> bool {
        let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
        match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
            Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                let mut cookie = [0u8; COOKIE_LEN];
                cookie.copy_from_slice(&plaintext);
                self.cookie = Some(cookie);
                #[cfg(feature = "std")]
                { self.received = Some(now()); }
                true
            }
            _ => false,
        }
    }

    /// Compute MAC2 using our stored cookie. Returns zeros if no cookie.
    pub fn compute_mac2(&self, msg_with_mac1: &[u8]) -> [u8; 16] {
        match &self.cookie {
            Some(cookie) if self.is_fresh() => {
                let full = crypto::mac(cookie, &[msg_with_mac1]);
                let mut mac2 = [0u8; 16];
                mac2.copy_from_slice(&full[..16]);
                mac2
            }
            _ => [0u8; 16],
        }
    }

    /// Cookie is fresh if received within the last 120 seconds.
    fn is_fresh(&self) -> bool {
        match self.received {
            Some(ts) => elapsed_since(ts) < COOKIE_SECRET_LIFETIME,
            None => false,
        }
    }
}

/// Encode a socket address to bytes for cookie MAC.
#[cfg(feature = "std")]
fn encode_addr(addr: &std::net::SocketAddr) -> alloc::vec::Vec<u8> {
    use alloc::vec::Vec;
    match addr {
        std::net::SocketAddr::V4(a) => {
            let mut buf = Vec::with_capacity(6);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
        std::net::SocketAddr::V6(a) => {
            let mut buf = Vec::with_capacity(18);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
    }
}

fn random_bytes() -> [u8; 32] {
    let mut buf = [0u8; 32];
    fill_random(&mut buf);
    buf
}

fn random_nonce() -> [u8; 24] {
    let mut buf = [0u8; 24];
    fill_random(&mut buf);
    buf
}

/// Fill a buffer with cryptographically secure random bytes.
/// On std, uses getrandom. On no_std, this is a stub — kernel module
/// must provide its own RNG via get_random_bytes().
#[cfg(feature = "std")]
fn fill_random(buf: &mut [u8]) {
    getrandom::getrandom(buf).expect("failed to get random bytes");
}

#[cfg(not(feature = "std"))]
fn fill_random(buf: &mut [u8]) {
    // Kernel module overrides this — stub zeros for compilation.
    // In practice, the kernel module uses its own cookie checker
    // that calls get_random_bytes() directly.
    let _ = buf;
}

fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
    if b.len() < 16 {
        return false;
    }
    use subtle::ConstantTimeEq;
    a.ct_eq(&b[..16]).into()
}

#[cfg(test)]
mod tests {
    use super::*;
    use rustguard_crypto::StaticSecret;

    fn setup() -> (CookieChecker, CookieState, PublicKey) {
        let secret = StaticSecret::random();
        let public = secret.public_key();
        let checker = CookieChecker::new(public.clone());
        let state = CookieState::new();
        (checker, state, public)
    }

    #[test]
    fn cookie_roundtrip() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(state.process_reply(&reply, &public, &mac1));
        assert!(state.cookie.is_some());
    }

    #[test]
    fn wrong_mac1_fails_decrypt() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];
        let wrong_mac1 = [0x43u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(!state.process_reply(&reply, &public, &wrong_mac1));
    }

    #[test]
    fn mac2_verified_after_cookie() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        // Get a cookie.
        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        // Compute MAC2 with the cookie.
        let msg = b"some message with mac1 appended";
        let mac2 = state.compute_mac2(msg);
        assert_ne!(mac2, [0u8; 16]); // Should not be zeros.

        // Verify MAC2.
        assert!(checker.verify_mac2(msg, &mac2, &src));
    }

    #[test]
    fn mac2_wrong_source_fails() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let wrong_src: std::net::SocketAddr = "10.0.0.2:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        let msg = b"message";
        let mac2 = state.compute_mac2(msg);

        // Different source should fail.
        assert!(!checker.verify_mac2(msg, &mac2, &wrong_src));
    }

    #[test]
    fn no_cookie_gives_zero_mac2() {
        let state = CookieState::new();
        let mac2 = state.compute_mac2(b"anything");
        assert_eq!(mac2, [0u8; 16]);
    }
}

```

## Symbols to evaluate

- constant COOKIE_SECRET_LIFETIME (L21–L21)
- export constant COOKIE_LEN (L24–L24)
- function now (L33–L35)
- function elapsed_since (L38–L40)
- function elapsed_since (L43–L45)
- export class CookieChecker (L48–L57)
- export class CookieState (L60–L65)
- function encode_addr (L227–L243)
- function random_bytes (L245–L249)
- function random_nonce (L251–L255)
- function fill_random (L261–L263)
- function fill_random (L266–L271)
- function constant_time_eq_16 (L273–L279)

## Test File

No test file found for this source file.

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Evaluate the test quality for each symbol and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "COOKIE_SECRET_LIFETIME",
      "line_start": 21,
      "line_end": 21,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure constant with no runtime behavior. The 120-second value is indirectly exercised by the freshness and rotation logic in the inline test suite. Constants require no direct tests."
    },
    {
      "name": "COOKIE_LEN",
      "line_start": 24,
      "line_end": 24,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Pure constant. Used throughout and indirectly validated in every roundtrip test that checks cookie length correctness (e.g., cookie_roundtrip, no_cookie_gives_zero_mac2)."
    },
    {
      "name": "now",
      "line_start": 33,
      "line_end": 35,
      "tests": "WEAK",
      "confidence": 75,
      "detail": "Trivial std wrapper around Instant::now(). Called implicitly via CookieChecker::new() in setup(). Never tested directly, and no test verifies time-related monotonicity or behavior at boundaries."
    },
    {
      "name": "elapsed_since",
      "line_start": 38,
      "line_end": 40,
      "tests": "WEAK",
      "confidence": 75,
      "detail": "The std variant is indirectly exercised via is_fresh() and maybe_rotate_secret() in the inline tests, but the critical expiry path (elapsed >= 120s) is never tested — no test manipulates time to verify that stale cookies/secrets are correctly detected."
    },
    {
      "name": "elapsed_since",
      "line_start": 43,
      "line_end": 45,
      "tests": "NONE",
      "confidence": 93,
      "detail": "The no_std stub always returns Duration::ZERO. There are no tests compiled under the no_std configuration and no test exercises this variant or verifies its always-zero behavior has expected downstream effects (e.g., that rotation never triggers)."
    },
    {
      "name": "CookieChecker",
      "line_start": 48,
      "line_end": 57,
      "tests": "WEAK",
      "confidence": 88,
      "detail": "The happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests. However, verify_mac1 is never called in any test despite being a public API used in handshake validation. new_with() (the no_std/explicit-timestamp constructor) is never exercised. Secret rotation via maybe_rotate_secret is never triggered (no time manipulation). These gaps cover critical security paths."
    },
    {
      "name": "CookieState",
      "line_start": 60,
      "line_end": 65,
      "tests": "WEAK",
      "confidence": 82,
      "detail": "Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie. However, is_fresh() stale branch (cookie older than 120s returning false) is never tested, meaning expired cookies silently yielding zero MAC2 is not verified. The received=None path in is_fresh is also only implicitly covered."
    },
    {
      "name": "encode_addr",
      "line_start": 227,
      "line_end": 243,
      "tests": "WEAK",
      "confidence": 87,
      "detail": "IPv4 encoding is exercised indirectly through every std test using '10.0.0.1:12345'. The IPv6 branch (16-byte IP + 2-byte port) is never exercised. Given that cookie correctness depends on address encoding, the missing IPv6 coverage is a meaningful gap."
    },
    {
      "name": "random_bytes",
      "line_start": 245,
      "line_end": 249,
      "tests": "WEAK",
      "confidence": 70,
      "detail": "Only indirectly invoked when CookieChecker::new() is called in setup(). No test checks output length, non-zero distribution, or uniqueness across calls. Given its role in generating the rotating secret, the lack of direct testing is notable."
    },
    {
      "name": "random_nonce",
      "line_start": 251,
      "line_end": 255,
      "tests": "WEAK",
      "confidence": 70,
      "detail": "Invoked indirectly via create_reply_from_bytes inside cookie_roundtrip and related tests. No test validates nonce uniqueness, length, or randomness quality — only that the downstream decrypt succeeds with the generated nonce."
    },
    {
      "name": "fill_random",
      "line_start": 261,
      "line_end": 263,
      "tests": "WEAK",
      "confidence": 68,
      "detail": "The std variant is exercised only transitively through random_bytes/random_nonce in every test. No test directly validates that getrandom produces non-trivial output or handles failure paths."
    },
    {
      "name": "fill_random",
      "line_start": 266,
      "line_end": 271,
      "tests": "NONE",
      "confidence": 92,
      "detail": "The no_std stub is a silent no-op (leaves buffer as zeros). There are no tests for the no_std configuration, and no test verifies the stub's documented contract or that downstream callers handle zeroed random data safely."
    },
    {
      "name": "constant_time_eq_16",
      "line_start": 273,
      "line_end": 279,
      "tests": "WEAK",
      "confidence": 82,
      "detail": "Indirectly tested through verify_mac2 in mac2_verified_after_cookie and mac2_wrong_source_fails. The short-slice guard (b.len() < 16 returns false) is never exercised. The constant-time property itself is not validated. verify_mac1 (the other caller) is not tested at all."
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.1455 | **Duration:** 60.4s


---

# Axis: best_practices

## User

## File: `rustguard-core/src/cookie.rs`
## Language: rust
## Context: general

```rust
//! WireGuard cookie mechanism for DoS protection.
//!
//! Under load, the responder can require proof that the initiator
//! can receive packets at its claimed IP. The flow:
//!
//! 1. Initiator sends handshake with MAC2 = zeros (no cookie yet)
//! 2. Responder is under load, sends Cookie Reply instead of processing
//! 3. Initiator decrypts cookie, stores it
//! 4. Initiator resends handshake with MAC2 = MAC(cookie, msg || mac1)
//! 5. Responder verifies MAC2 and processes the handshake
//!
//! The cookie is MAC(secret, sender_ip_port), where secret rotates every 2 min.

use core::time::Duration;

use rustguard_crypto::{self as crypto, PublicKey, LABEL_COOKIE, LABEL_MAC1};

use crate::messages::CookieReply;

/// How often the cookie secret rotates.
const COOKIE_SECRET_LIFETIME: Duration = Duration::from_secs(120);

/// Cookie size.
pub const COOKIE_LEN: usize = 16;

/// Monotonic timestamp type — same abstraction as timers.rs.
#[cfg(feature = "std")]
type Timestamp = std::time::Instant;
#[cfg(not(feature = "std"))]
type Timestamp = u64;

#[cfg(feature = "std")]
fn now() -> Timestamp {
    std::time::Instant::now()
}

#[cfg(feature = "std")]
fn elapsed_since(ts: Timestamp) -> Duration {
    ts.elapsed()
}

#[cfg(not(feature = "std"))]
fn elapsed_since(_ts: Timestamp) -> Duration {
    Duration::ZERO
}

/// Server-side cookie state: generates cookies and validates MAC2.
pub struct CookieChecker {
    /// Our static public key (used to derive encryption key).
    our_public: PublicKey,
    /// Rotating secret for cookie generation.
    secret: [u8; 32],
    /// When the secret was last generated.
    secret_generated: Timestamp,
    /// Whether we're "under load" and should require cookies.
    pub under_load: bool,
}

/// Client-side cookie state: stores a received cookie for MAC2.
pub struct CookieState {
    /// The decrypted cookie, if we have one.
    cookie: Option<[u8; COOKIE_LEN]>,
    /// When we received the cookie.
    received: Option<Timestamp>,
}

impl CookieChecker {
    #[cfg(feature = "std")]
    pub fn new(our_public: PublicKey) -> Self {
        Self {
            our_public,
            secret: random_bytes(),
            secret_generated: now(),
            under_load: false,
        }
    }

    /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
    pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
        Self {
            our_public,
            secret,
            secret_generated: now_ts,
            under_load: false,
        }
    }

    /// Rotate the secret if it's stale.
    fn maybe_rotate_secret(&mut self) {
        if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
            self.secret = random_bytes();
            #[cfg(feature = "std")]
            { self.secret_generated = now(); }
        }
    }

    /// Generate a cookie for pre-encoded source address bytes.
    /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
    ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
    fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
        self.maybe_rotate_secret();
        let full = crypto::mac(&self.secret, &[addr_bytes]);
        let mut cookie = [0u8; COOKIE_LEN];
        cookie.copy_from_slice(&full[..COOKIE_LEN]);
        cookie
    }

    /// Build a Cookie Reply message from pre-encoded address bytes.
    pub fn create_reply_from_bytes(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        addr_bytes: &[u8],
    ) -> CookieReply {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
        let nonce = random_nonce();
        let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);

        CookieReply {
            receiver_index,
            nonce,
            encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
        }
    }

    /// Build a Cookie Reply message for a SocketAddr (std convenience).
    #[cfg(feature = "std")]
    pub fn create_reply(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> CookieReply {
        let addr_bytes = encode_addr(src);
        self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
    }

    /// Verify MAC1 on an incoming message.
    pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
        let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
        let expected = crypto::mac(&key, &[msg_bytes]);
        constant_time_eq_16(mac1, &expected[..16])
    }

    /// Verify MAC2 from pre-encoded address bytes.
    pub fn verify_mac2_from_bytes(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        addr_bytes: &[u8],
    ) -> bool {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let expected = crypto::mac(&cookie, &[msg_with_mac1]);
        constant_time_eq_16(mac2, &expected[..16])
    }

    /// Verify MAC2 on an incoming message (std convenience).
    #[cfg(feature = "std")]
    pub fn verify_mac2(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> bool {
        let addr_bytes = encode_addr(src);
        self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
    }
}

impl CookieState {
    pub fn new() -> Self {
        Self {
            cookie: None,
            received: None,
        }
    }

    /// Process a Cookie Reply and store the decrypted cookie.
    ///
    /// `their_public`: the responder's public key.
    /// `mac1`: our MAC1 from the original message.
    pub fn process_reply(
        &mut self,
        reply: &CookieReply,
        their_public: &PublicKey,
        mac1: &[u8; 16],
    ) -> bool {
        let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
        match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
            Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                let mut cookie = [0u8; COOKIE_LEN];
                cookie.copy_from_slice(&plaintext);
                self.cookie = Some(cookie);
                #[cfg(feature = "std")]
                { self.received = Some(now()); }
                true
            }
            _ => false,
        }
    }

    /// Compute MAC2 using our stored cookie. Returns zeros if no cookie.
    pub fn compute_mac2(&self, msg_with_mac1: &[u8]) -> [u8; 16] {
        match &self.cookie {
            Some(cookie) if self.is_fresh() => {
                let full = crypto::mac(cookie, &[msg_with_mac1]);
                let mut mac2 = [0u8; 16];
                mac2.copy_from_slice(&full[..16]);
                mac2
            }
            _ => [0u8; 16],
        }
    }

    /// Cookie is fresh if received within the last 120 seconds.
    fn is_fresh(&self) -> bool {
        match self.received {
            Some(ts) => elapsed_since(ts) < COOKIE_SECRET_LIFETIME,
            None => false,
        }
    }
}

/// Encode a socket address to bytes for cookie MAC.
#[cfg(feature = "std")]
fn encode_addr(addr: &std::net::SocketAddr) -> alloc::vec::Vec<u8> {
    use alloc::vec::Vec;
    match addr {
        std::net::SocketAddr::V4(a) => {
            let mut buf = Vec::with_capacity(6);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
        std::net::SocketAddr::V6(a) => {
            let mut buf = Vec::with_capacity(18);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
    }
}

fn random_bytes() -> [u8; 32] {
    let mut buf = [0u8; 32];
    fill_random(&mut buf);
    buf
}

fn random_nonce() -> [u8; 24] {
    let mut buf = [0u8; 24];
    fill_random(&mut buf);
    buf
}

/// Fill a buffer with cryptographically secure random bytes.
/// On std, uses getrandom. On no_std, this is a stub — kernel module
/// must provide its own RNG via get_random_bytes().
#[cfg(feature = "std")]
fn fill_random(buf: &mut [u8]) {
    getrandom::getrandom(buf).expect("failed to get random bytes");
}

#[cfg(not(feature = "std"))]
fn fill_random(buf: &mut [u8]) {
    // Kernel module overrides this — stub zeros for compilation.
    // In practice, the kernel module uses its own cookie checker
    // that calls get_random_bytes() directly.
    let _ = buf;
}

fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
    if b.len() < 16 {
        return false;
    }
    use subtle::ConstantTimeEq;
    a.ct_eq(&b[..16]).into()
}

#[cfg(test)]
mod tests {
    use super::*;
    use rustguard_crypto::StaticSecret;

    fn setup() -> (CookieChecker, CookieState, PublicKey) {
        let secret = StaticSecret::random();
        let public = secret.public_key();
        let checker = CookieChecker::new(public.clone());
        let state = CookieState::new();
        (checker, state, public)
    }

    #[test]
    fn cookie_roundtrip() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(state.process_reply(&reply, &public, &mac1));
        assert!(state.cookie.is_some());
    }

    #[test]
    fn wrong_mac1_fails_decrypt() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];
        let wrong_mac1 = [0x43u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(!state.process_reply(&reply, &public, &wrong_mac1));
    }

    #[test]
    fn mac2_verified_after_cookie() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        // Get a cookie.
        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        // Compute MAC2 with the cookie.
        let msg = b"some message with mac1 appended";
        let mac2 = state.compute_mac2(msg);
        assert_ne!(mac2, [0u8; 16]); // Should not be zeros.

        // Verify MAC2.
        assert!(checker.verify_mac2(msg, &mac2, &src));
    }

    #[test]
    fn mac2_wrong_source_fails() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let wrong_src: std::net::SocketAddr = "10.0.0.2:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        let msg = b"message";
        let mac2 = state.compute_mac2(msg);

        // Different source should fail.
        assert!(!checker.verify_mac2(msg, &mac2, &wrong_src));
    }

    #[test]
    fn no_cookie_gives_zero_mac2() {
        let state = CookieState::new();
        let mac2 = state.compute_mac2(b"anything");
        assert_eq!(mac2, [0u8; 16]);
    }
}

```

## File stats
- Lines: 358
- Symbols: 13
- Exported symbols: 3

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Use the project structure above to evaluate rule 11 (import organization) and detect file placement inconsistencies. If the file is in an unexpected directory given the project conventions, flag it as WARN for the most relevant rule.

Evaluate all 17 rules and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "score": 8.5,
  "rules": [
    {
      "rule_id": 1,
      "rule_name": "No unwrap in production code",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No bare `.unwrap()` calls in production paths. Two `.expect()` calls exist: one in `fill_random` (getrandom failure, std-only) and one in `create_reply_from_bytes` (try_into on a size-known crypto output). Both are acceptable per the rule. All `.unwrap()` calls are confined to the `#[cfg(test)]` module."
    },
    {
      "rule_id": 2,
      "rule_name": "No unsafe blocks without clear justification comment",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No `unsafe` blocks anywhere in the file."
    },
    {
      "rule_id": 3,
      "rule_name": "Proper error handling with Result/Option",
      "status": "PASS",
      "severity": "HIGH",
      "detail": "`process_reply` uses exhaustive pattern matching on `Option<Vec<u8>>`. `compute_mac2` safely falls back to zero MAC on absent/stale cookie. The no_std `fill_random` stub is a documented compile-time placeholder, not a silently swallowed error. No silent error ignores detected."
    },
    {
      "rule_id": 4,
      "rule_name": "Derive common traits on public types",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "`CookieChecker` and `CookieState` are both `pub` structs but derive neither `Debug`, `Clone`, nor `PartialEq`. At minimum `Debug` is expected on public library types to aid diagnostics and test assertions. `CookieChecker` contains a mutable secret so `Clone` may be intentionally withheld, but `Debug` (with the secret redacted via a custom impl or `#[debug = \"...\"]`) and `PartialEq` on `CookieState` would be beneficial.",
      "lines": "L52-L69"
    },
    {
      "rule_id": 5,
      "rule_name": "Lifetime annotations explicit where needed",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "All functions with reference parameters either return no reference (making elision unambiguous) or take a single `&self`/`&mut self` receiver. No case requires explicit lifetime annotations beyond what elision handles correctly."
    },
    {
      "rule_id": 6,
      "rule_name": "Use clippy idioms",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "`CookieState::new()` returns `Self` but the type does not implement the `Default` trait. Clippy (`clippy::new_without_default`) would flag this. Implementing `Default` is idiomatic when `new()` takes no arguments and returns a fixed default state.",
      "lines": "L194-L199"
    },
    {
      "rule_id": 7,
      "rule_name": "No panic! in library/production code",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No `panic!` macro calls anywhere in the production code paths."
    },
    {
      "rule_id": 8,
      "rule_name": "Module organization",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "The file is correctly placed in `rustguard-core/src/cookie.rs` and mirrors the parallel `rustguard-kmod/src/cookie.rs` for kernel use — a clean split consistent with the project's std/no_std dual-target convention. Public surface (`CookieChecker`, `CookieState`, `COOKIE_LEN`) is minimal and well-scoped. No re-export issues."
    },
    {
      "rule_id": 9,
      "rule_name": "Documentation comments on public items",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "`CookieChecker::new` (the `#[cfg(feature = \"std\")]` constructor, L73-L82) and `CookieState::new` (L194-L199) are public methods that lack `///` doc comments. All other public items and methods are well-documented.",
      "lines": "L73-L82, L194-L199"
    },
    {
      "rule_id": 10,
      "rule_name": "No hardcoded secrets or credentials",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No hardcoded secrets. The cookie secret is generated via `random_bytes()` → `getrandom` (std) or a kernel RNG stub (no_std). Test fixtures use fixed bytes for MAC1 (`[0x42u8; 16]`) which are test vectors, not credentials."
    },
    {
      "rule_id": 11,
      "rule_name": "Memory safety",
      "status": "PASS",
      "severity": "HIGH",
      "detail": "No `mem::forget`, `Box::leak`, or manual pointer manipulation. All heap allocation (`Vec` in `encode_addr`) is scoped to function bodies and dropped normally. `CookieChecker` and `CookieState` hold only stack-sized arrays and `Option` wrappers — no custom `Drop` needed."
    },
    {
      "rule_id": 12,
      "rule_name": "Concurrency safety",
      "status": "PASS",
      "severity": "HIGH",
      "detail": "Both `CookieChecker` and `CookieState` hold only `Send + Sync` primitive types (`[u8; N]`, `bool`, `Option<...>`). Mutable operations take `&mut self`, enforcing exclusive access at the call site via Rust's borrow checker. No interior mutability, no raw pointers, and no unsafe concurrency primitives are used."
    }
  ],
  "suggestions": [
    {
      "description": "Implement `Debug` (at minimum) on public structs. Since `CookieChecker` contains a rotating secret, use a manual `Debug` impl that redacts it instead of a blanket derive.",
      "before": "pub struct CookieChecker {\n    our_public: PublicKey,\n    secret: [u8; 32],\n    secret_generated: Timestamp,\n    pub under_load: bool,\n}",
      "after": "pub struct CookieChecker {\n    our_public: PublicKey,\n    secret: [u8; 32],\n    secret_generated: Timestamp,\n    pub under_load: bool,\n}\n\nimpl core::fmt::Debug for CookieChecker {\n    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {\n        f.debug_struct(\"CookieChecker\")\n            .field(\"our_public\", &self.our_public)\n            .field(\"secret\", &\"[redacted]\")\n            .field(\"under_load\", &self.under_load)\n            .finish()\n    }\n}"
    },
    {
      "description": "Implement `Default` for `CookieState` to satisfy `clippy::new_without_default` and follow idiomatic Rust.",
      "before": "impl CookieState {\n    pub fn new() -> Self {\n        Self {\n            cookie: None,\n            received: None,\n        }\n    }",
      "after": "#[derive(Default)]\npub struct CookieState {\n    cookie: Option<[u8; COOKIE_LEN]>,\n    received: Option<Timestamp>,\n}\n\nimpl CookieState {\n    pub fn new() -> Self {\n        Self::default()\n    }"
    },
    {
      "description": "Add `///` doc comments to the two undocumented public constructors.",
      "before": "#[cfg(feature = \"std\")]\npub fn new(our_public: PublicKey) -> Self {",
      "after": "/// Create a new `CookieChecker` with a freshly generated random secret (std only).\n#[cfg(feature = \"std\")]\npub fn new(our_public: PublicKey) -> Self {"
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.1648 | **Duration:** 74.7s


---

# Axis: documentation

## User

## File: `rustguard-core/src/cookie.rs`
## Language: rust

```rust
//! WireGuard cookie mechanism for DoS protection.
//!
//! Under load, the responder can require proof that the initiator
//! can receive packets at its claimed IP. The flow:
//!
//! 1. Initiator sends handshake with MAC2 = zeros (no cookie yet)
//! 2. Responder is under load, sends Cookie Reply instead of processing
//! 3. Initiator decrypts cookie, stores it
//! 4. Initiator resends handshake with MAC2 = MAC(cookie, msg || mac1)
//! 5. Responder verifies MAC2 and processes the handshake
//!
//! The cookie is MAC(secret, sender_ip_port), where secret rotates every 2 min.

use core::time::Duration;

use rustguard_crypto::{self as crypto, PublicKey, LABEL_COOKIE, LABEL_MAC1};

use crate::messages::CookieReply;

/// How often the cookie secret rotates.
const COOKIE_SECRET_LIFETIME: Duration = Duration::from_secs(120);

/// Cookie size.
pub const COOKIE_LEN: usize = 16;

/// Monotonic timestamp type — same abstraction as timers.rs.
#[cfg(feature = "std")]
type Timestamp = std::time::Instant;
#[cfg(not(feature = "std"))]
type Timestamp = u64;

#[cfg(feature = "std")]
fn now() -> Timestamp {
    std::time::Instant::now()
}

#[cfg(feature = "std")]
fn elapsed_since(ts: Timestamp) -> Duration {
    ts.elapsed()
}

#[cfg(not(feature = "std"))]
fn elapsed_since(_ts: Timestamp) -> Duration {
    Duration::ZERO
}

/// Server-side cookie state: generates cookies and validates MAC2.
pub struct CookieChecker {
    /// Our static public key (used to derive encryption key).
    our_public: PublicKey,
    /// Rotating secret for cookie generation.
    secret: [u8; 32],
    /// When the secret was last generated.
    secret_generated: Timestamp,
    /// Whether we're "under load" and should require cookies.
    pub under_load: bool,
}

/// Client-side cookie state: stores a received cookie for MAC2.
pub struct CookieState {
    /// The decrypted cookie, if we have one.
    cookie: Option<[u8; COOKIE_LEN]>,
    /// When we received the cookie.
    received: Option<Timestamp>,
}

impl CookieChecker {
    #[cfg(feature = "std")]
    pub fn new(our_public: PublicKey) -> Self {
        Self {
            our_public,
            secret: random_bytes(),
            secret_generated: now(),
            under_load: false,
        }
    }

    /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
    pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
        Self {
            our_public,
            secret,
            secret_generated: now_ts,
            under_load: false,
        }
    }

    /// Rotate the secret if it's stale.
    fn maybe_rotate_secret(&mut self) {
        if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
            self.secret = random_bytes();
            #[cfg(feature = "std")]
            { self.secret_generated = now(); }
        }
    }

    /// Generate a cookie for pre-encoded source address bytes.
    /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
    ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
    fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
        self.maybe_rotate_secret();
        let full = crypto::mac(&self.secret, &[addr_bytes]);
        let mut cookie = [0u8; COOKIE_LEN];
        cookie.copy_from_slice(&full[..COOKIE_LEN]);
        cookie
    }

    /// Build a Cookie Reply message from pre-encoded address bytes.
    pub fn create_reply_from_bytes(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        addr_bytes: &[u8],
    ) -> CookieReply {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
        let nonce = random_nonce();
        let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);

        CookieReply {
            receiver_index,
            nonce,
            encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
        }
    }

    /// Build a Cookie Reply message for a SocketAddr (std convenience).
    #[cfg(feature = "std")]
    pub fn create_reply(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> CookieReply {
        let addr_bytes = encode_addr(src);
        self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
    }

    /// Verify MAC1 on an incoming message.
    pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
        let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
        let expected = crypto::mac(&key, &[msg_bytes]);
        constant_time_eq_16(mac1, &expected[..16])
    }

    /// Verify MAC2 from pre-encoded address bytes.
    pub fn verify_mac2_from_bytes(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        addr_bytes: &[u8],
    ) -> bool {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let expected = crypto::mac(&cookie, &[msg_with_mac1]);
        constant_time_eq_16(mac2, &expected[..16])
    }

    /// Verify MAC2 on an incoming message (std convenience).
    #[cfg(feature = "std")]
    pub fn verify_mac2(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> bool {
        let addr_bytes = encode_addr(src);
        self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
    }
}

impl CookieState {
    pub fn new() -> Self {
        Self {
            cookie: None,
            received: None,
        }
    }

    /// Process a Cookie Reply and store the decrypted cookie.
    ///
    /// `their_public`: the responder's public key.
    /// `mac1`: our MAC1 from the original message.
    pub fn process_reply(
        &mut self,
        reply: &CookieReply,
        their_public: &PublicKey,
        mac1: &[u8; 16],
    ) -> bool {
        let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
        match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
            Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                let mut cookie = [0u8; COOKIE_LEN];
                cookie.copy_from_slice(&plaintext);
                self.cookie = Some(cookie);
                #[cfg(feature = "std")]
                { self.received = Some(now()); }
                true
            }
            _ => false,
        }
    }

    /// Compute MAC2 using our stored cookie. Returns zeros if no cookie.
    pub fn compute_mac2(&self, msg_with_mac1: &[u8]) -> [u8; 16] {
        match &self.cookie {
            Some(cookie) if self.is_fresh() => {
                let full = crypto::mac(cookie, &[msg_with_mac1]);
                let mut mac2 = [0u8; 16];
                mac2.copy_from_slice(&full[..16]);
                mac2
            }
            _ => [0u8; 16],
        }
    }

    /// Cookie is fresh if received within the last 120 seconds.
    fn is_fresh(&self) -> bool {
        match self.received {
            Some(ts) => elapsed_since(ts) < COOKIE_SECRET_LIFETIME,
            None => false,
        }
    }
}

/// Encode a socket address to bytes for cookie MAC.
#[cfg(feature = "std")]
fn encode_addr(addr: &std::net::SocketAddr) -> alloc::vec::Vec<u8> {
    use alloc::vec::Vec;
    match addr {
        std::net::SocketAddr::V4(a) => {
            let mut buf = Vec::with_capacity(6);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
        std::net::SocketAddr::V6(a) => {
            let mut buf = Vec::with_capacity(18);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
    }
}

fn random_bytes() -> [u8; 32] {
    let mut buf = [0u8; 32];
    fill_random(&mut buf);
    buf
}

fn random_nonce() -> [u8; 24] {
    let mut buf = [0u8; 24];
    fill_random(&mut buf);
    buf
}

/// Fill a buffer with cryptographically secure random bytes.
/// On std, uses getrandom. On no_std, this is a stub — kernel module
/// must provide its own RNG via get_random_bytes().
#[cfg(feature = "std")]
fn fill_random(buf: &mut [u8]) {
    getrandom::getrandom(buf).expect("failed to get random bytes");
}

#[cfg(not(feature = "std"))]
fn fill_random(buf: &mut [u8]) {
    // Kernel module overrides this — stub zeros for compilation.
    // In practice, the kernel module uses its own cookie checker
    // that calls get_random_bytes() directly.
    let _ = buf;
}

fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
    if b.len() < 16 {
        return false;
    }
    use subtle::ConstantTimeEq;
    a.ct_eq(&b[..16]).into()
}

#[cfg(test)]
mod tests {
    use super::*;
    use rustguard_crypto::StaticSecret;

    fn setup() -> (CookieChecker, CookieState, PublicKey) {
        let secret = StaticSecret::random();
        let public = secret.public_key();
        let checker = CookieChecker::new(public.clone());
        let state = CookieState::new();
        (checker, state, public)
    }

    #[test]
    fn cookie_roundtrip() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(state.process_reply(&reply, &public, &mac1));
        assert!(state.cookie.is_some());
    }

    #[test]
    fn wrong_mac1_fails_decrypt() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];
        let wrong_mac1 = [0x43u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(!state.process_reply(&reply, &public, &wrong_mac1));
    }

    #[test]
    fn mac2_verified_after_cookie() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        // Get a cookie.
        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        // Compute MAC2 with the cookie.
        let msg = b"some message with mac1 appended";
        let mac2 = state.compute_mac2(msg);
        assert_ne!(mac2, [0u8; 16]); // Should not be zeros.

        // Verify MAC2.
        assert!(checker.verify_mac2(msg, &mac2, &src));
    }

    #[test]
    fn mac2_wrong_source_fails() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let wrong_src: std::net::SocketAddr = "10.0.0.2:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        let msg = b"message";
        let mac2 = state.compute_mac2(msg);

        // Different source should fail.
        assert!(!checker.verify_mac2(msg, &mac2, &wrong_src));
    }

    #[test]
    fn no_cookie_gives_zero_mac2() {
        let state = CookieState::new();
        let mac2 = state.compute_mac2(b"anything");
        assert_eq!(mac2, [0u8; 16]);
    }
}

```

## Symbols to evaluate

- constant COOKIE_SECRET_LIFETIME (L21–L21)
- export constant COOKIE_LEN (L24–L24)
- function now (L33–L35)
- function elapsed_since (L38–L40)
- function elapsed_since (L43–L45)
- export class CookieChecker (L48–L57)
- export class CookieState (L60–L65)
- function encode_addr (L227–L243)
- function random_bytes (L245–L249)
- function random_nonce (L251–L255)
- function fill_random (L261–L263)
- function fill_random (L266–L271)
- function constant_time_eq_16 (L273–L279)

## Documentation Directory

No /docs/ directory found — evaluate JSDoc only, skip concept coverage.

Evaluate documentation for each symbol and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "COOKIE_SECRET_LIFETIME",
      "line_start": 21,
      "line_end": 21,
      "documentation": "PARTIAL",
      "confidence": 82,
      "detail": "Has a brief `///` comment ('How often the cookie secret rotates.') but omits the significance of the 120-second value, its relationship to the WireGuard spec, or what happens on rotation."
    },
    {
      "name": "COOKIE_LEN",
      "line_start": 24,
      "line_end": 24,
      "documentation": "PARTIAL",
      "confidence": 88,
      "detail": "Comment 'Cookie size.' is trivially unhelpful — it restates the name without explaining what 16 bytes represents in the WireGuard cookie protocol or how it is used."
    },
    {
      "name": "now",
      "line_start": 33,
      "line_end": 35,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "Private function with no `///` doc comment. Name is clear, so leniency applies, but no documentation exists."
    },
    {
      "name": "elapsed_since",
      "line_start": 38,
      "line_end": 40,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "Private std-feature function with no `///` doc comment. Name is self-explanatory but the function is entirely undocumented."
    },
    {
      "name": "elapsed_since",
      "line_start": 43,
      "line_end": 45,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "Private no_std stub with no `///` doc comment. The always-ZERO return behavior under no_std is not documented at all, which is a notable omission."
    },
    {
      "name": "CookieChecker",
      "line_start": 48,
      "line_end": 57,
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "Struct has a `///` summary and all four fields have field-level `///` comments. However, the public API lacks `# Examples` sections, and the `new` (std) constructor is entirely undocumented. No description of the overall usage lifecycle."
    },
    {
      "name": "CookieState",
      "line_start": 60,
      "line_end": 65,
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "Struct has a `///` summary and both private fields have doc comments. No `# Examples` section and the `new` constructor carries no `///` documentation. Sufficient for orientation but incomplete for a public API."
    },
    {
      "name": "encode_addr",
      "line_start": 227,
      "line_end": 243,
      "documentation": "PARTIAL",
      "confidence": 72,
      "detail": "Private function has a single-line `///` comment. It does not document the output byte layout (IPv4: 6 bytes, IPv6: 18 bytes), which is non-obvious and actually documented in a different private method's comment instead."
    },
    {
      "name": "random_bytes",
      "line_start": 245,
      "line_end": 249,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "Private function with no `///` doc comment. Name is descriptive, so leniency applies, but no documentation present."
    },
    {
      "name": "random_nonce",
      "line_start": 251,
      "line_end": 255,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "Private function with no `///` doc comment. Does not document the 24-byte nonce size or its use with XChaCha20-Poly1305."
    },
    {
      "name": "fill_random",
      "line_start": 261,
      "line_end": 263,
      "documentation": "DOCUMENTED",
      "confidence": 85,
      "detail": "The `///` block preceding this std variant explains purpose, the underlying crate used (getrandom), and cross-platform behavior for no_std. Adequate for a private utility function; no examples expected."
    },
    {
      "name": "fill_random",
      "line_start": 266,
      "line_end": 271,
      "documentation": "UNDOCUMENTED",
      "confidence": 65,
      "detail": "The no_std variant has only inline `//` comments, not `///` doc comments. The `///` block above attaches to the std variant only. The important stub-zeros behavior and kernel override expectation are not expressed as doc comments."
    },
    {
      "name": "constant_time_eq_16",
      "line_start": 273,
      "line_end": 279,
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "Private function with no `///` doc comment. Its constant-time guarantee — a security-critical property — is implied by the name but never documented."
    }
  ],
  "docs_coverage": {
    "concepts": [],
    "score_pct": 38
  }
}

## rate_limit_event

## Result (success)

**Cost:** $0.1630 | **Duration:** 72.2s


---

# Deliberation Pass

## User

## Merged ReviewFile (from 7 axis evaluators)

```json
{
  "version": 2,
  "file": "rustguard-core/src/cookie.rs",
  "is_generated": false,
  "language": "rust",
  "parse_method": "ast",
  "verdict": "NEEDS_REFACTOR",
  "symbols": [
    {
      "name": "COOKIE_SECRET_LIFETIME",
      "kind": "constant",
      "exported": false,
      "line_start": 21,
      "line_end": 21,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "PARTIAL",
      "confidence": 82,
      "detail": "[USED] Non-exported constant used locally in maybe_rotate_secret (line 90) and is_fresh (line 212) | [UNIQUE] Simple constant declaration, no duplicates found in codebase | [OK] Duration::from_secs(120) correctly represents the 2-minute secret rotation window stated in the module doc. | [LEAN] Simple named constant encoding the WireGuard spec's 2-minute secret rotation period. No abstraction beyond what the spec requires. | [GOOD] Pure constant with no runtime behavior. The 120-second value is indirectly exercised by the freshness and rotation logic in the inline test suite. Constants require no direct tests. | [PARTIAL] Has a brief `///` comment ('How often the cookie secret rotates.') but omits the significance of the 120-second value, its relationship to the WireGuard spec, or what happens on rotation."
    },
    {
      "name": "COOKIE_LEN",
      "kind": "constant",
      "exported": true,
      "line_start": 24,
      "line_end": 24,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "DEAD",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 88,
      "detail": "[DEAD] Exported const with 0 external importers per pre-computed analysis. Rule 2: exported symbol with no importers = DEAD | [UNIQUE] Exported constant for cookie byte length, no similar symbols found | [OK] 16-byte cookie size matches the WireGuard specification. | [LEAN] Minimal constant for the 16-byte cookie size. Appropriately exported for use by message layout code. | [GOOD] Pure constant. Used throughout and indirectly validated in every roundtrip test that checks cookie length correctness (e.g., cookie_roundtrip, no_cookie_gives_zero_mac2). | [PARTIAL] Comment 'Cookie size.' is trivially unhelpful — it restates the name without explaining what 16 bytes represents in the WireGuard cookie protocol or how it is used."
    },
    {
      "name": "now",
      "kind": "function",
      "exported": false,
      "line_start": 33,
      "line_end": 35,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Non-exported std function used locally on lines 76 (new), 97 (maybe_rotate_secret), and 178 (process_reply) | [UNIQUE] Trivial 2-line function returning current time, conditional std feature | [OK] Correctly returns std::time::Instant::now() for the std platform; no correctness issues. | [LEAN] Thin cfg-gated wrapper that centralizes the std/no_std timestamp abstraction in one place rather than scattering #[cfg] attributes throughout impl blocks. Minimal and justified. | [WEAK] Trivial std wrapper around Instant::now(). Called implicitly via CookieChecker::new() in setup(). Never tested directly, and no test verifies time-related monotonicity or behavior at boundaries. | [UNDOCUMENTED] Private function with no `///` doc comment. Name is clear, so leniency applies, but no documentation exists."
    },
    {
      "name": "elapsed_since",
      "kind": "function",
      "exported": false,
      "line_start": 38,
      "line_end": 40,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Non-exported no_std stub version; called on lines 89 and 212 when std feature disabled | [UNIQUE] Trivial no_std stub returning zero duration; different semantic contract than std version | [NEEDS_FIX] Stub always returns Duration::ZERO. Consequently, the condition in maybe_rotate_secret (ZERO >= 120 s) is permanently false, so the cookie secret provided to new_with never rotates regardless of real elapsed time. While documented as a no_std stub, this silently disables a security-critical rotation for any no_std caller expecting periodic rotation. | [LEAN] No_std stub returning Duration::ZERO is the minimal correct behaviour; the comment correctly states kernel builds use their own cookie checker. No excess here. | [NONE] The no_std stub always returns Duration::ZERO. There are no tests compiled under the no_std configuration and no test exercises this variant or verifies its always-zero behavior has expected downstream effects (e.g., that rotation never triggers). | [UNDOCUMENTED] Private no_std stub with no `///` doc comment. The always-ZERO return behavior under no_std is not documented at all, which is a notable omission."
    },
    {
      "name": "elapsed_since",
      "kind": "function",
      "exported": false,
      "line_start": 43,
      "line_end": 45,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Non-exported no_std stub version; called on lines 89 and 212 when std feature disabled | [UNIQUE] Trivial no_std stub returning zero duration; different semantic contract than std version | [NEEDS_FIX] Stub always returns Duration::ZERO. Consequently, the condition in maybe_rotate_secret (ZERO >= 120 s) is permanently false, so the cookie secret provided to new_with never rotates regardless of real elapsed time. While documented as a no_std stub, this silently disables a security-critical rotation for any no_std caller expecting periodic rotation. | [LEAN] No_std stub returning Duration::ZERO is the minimal correct behaviour; the comment correctly states kernel builds use their own cookie checker. No excess here. | [NONE] The no_std stub always returns Duration::ZERO. There are no tests compiled under the no_std configuration and no test exercises this variant or verifies its always-zero behavior has expected downstream effects (e.g., that rotation never triggers). | [UNDOCUMENTED] Private no_std stub with no `///` doc comment. The always-ZERO return behavior under no_std is not documented at all, which is a notable omission."
    },
    {
      "name": "CookieChecker",
      "kind": "class",
      "exported": true,
      "line_start": 48,
      "line_end": 57,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "DEAD",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 85,
      "detail": "[DEAD] Exported struct with 0 external importers per pre-computed analysis. Rule 2: exported symbol with no importers = DEAD | [UNIQUE] Struct definition for server-side cookie state management, no duplicates found | [OK] Struct and impl are correct on std. On no_std the secret never rotates because elapsed_since always returns ZERO, but the code comments acknowledge the kernel module uses an independent mechanism, making this an intentional design limitation rather than an unintended bug. | [LEAN] Struct fields map 1-to-1 onto what the WireGuard spec requires for server-side cookie state: public key for key derivation, rotating secret, its timestamp, and a load flag. Nothing extraneous. | [WEAK] The happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests. However, verify_mac1 is never called in any test despite being a public API used in handshake validation. new_with() (the no_std/explicit-timestamp constructor) is never exercised. Secret rotation via maybe_rotate_secret is never triggered (no time manipulation). These gaps cover critical security paths. | [PARTIAL] Struct has a `///` summary and all four fields have field-level `///` comments. However, the public API lacks `# Examples` sections, and the `new` (std) constructor is entirely undocumented. No description of the overall usage lifecycle."
    },
    {
      "name": "CookieState",
      "kind": "class",
      "exported": true,
      "line_start": 60,
      "line_end": 65,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "DEAD",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 82,
      "detail": "[DEAD] Exported struct with 0 external importers per pre-computed analysis. Rule 2: exported symbol with no importers = DEAD | [UNIQUE] Struct definition for client-side cookie state, no duplicates found | [NEEDS_FIX] In process_reply, self.received is assigned only inside #[cfg(feature = \"std\")]. On no_std, after a successful cookie decryption, received stays None. is_fresh() returns false when received is None, so compute_mac2 always returns [0u8; 16] on no_std. The cookie is correctly decrypted and stored in self.cookie but is permanently treated as stale — the entire client-side cookie mechanism is non-functional on no_std. | [LEAN] Two fields (cookie bytes + received timestamp) are the minimal client-side state needed to produce MAC2 and check freshness per spec. | [WEAK] Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie. However, is_fresh() stale branch (cookie older than 120s returning false) is never tested, meaning expired cookies silently yielding zero MAC2 is not verified. The received=None path in is_fresh is also only implicitly covered. | [PARTIAL] Struct has a `///` summary and both private fields have doc comments. No `# Examples` section and the `new` constructor carries no `///` documentation. Sufficient for orientation but incomplete for a public API."
    },
    {
      "name": "encode_addr",
      "kind": "function",
      "exported": false,
      "line_start": 227,
      "line_end": 243,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 65,
      "detail": "[USED] Non-exported std-only function used locally on lines 130 (create_reply) and 162 (verify_mac2) | [UNIQUE] Encodes socket address to bytes for IPv4/IPv6; no similar functions found | [NEEDS_FIX] The function (gated with #[cfg(feature = \"std\")]) uses the path alloc::vec::Vec. In a crate that declares extern crate alloc only for no_std (a common pattern), the alloc:: namespace is not in scope when compiling with the std feature, causing a compile error. The return type and the Vec::with_capacity calls should use std::vec::Vec or an unqualified Vec via a use statement. | [LEAN] Straightforward IPv4/IPv6 serialisation into raw bytes for cookie MAC input. The match on V4/V6 is unavoidable given Rust's SocketAddr enum. Vec allocation is fine on the std path. | [WEAK] IPv4 encoding is exercised indirectly through every std test using '10.0.0.1:12345'. The IPv6 branch (16-byte IP + 2-byte port) is never exercised. Given that cookie correctness depends on address encoding, the missing IPv6 coverage is a meaningful gap. | [PARTIAL] Private function has a single-line `///` comment. It does not document the output byte layout (IPv4: 6 bytes, IPv6: 18 bytes), which is non-obvious and actually documented in a different private method's comment instead."
    },
    {
      "name": "random_bytes",
      "kind": "function",
      "exported": false,
      "line_start": 245,
      "line_end": 249,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Non-exported function used on lines 75 (new) and 93 (maybe_rotate_secret) to generate secrets | [UNIQUE] Generates 32-byte random array via fill_random; kmod versions have generic signature and different FFI impl | [OK] Correctly allocates a zeroed 32-byte buffer and delegates to fill_random; any defect is in fill_random itself. | [LEAN] Thin wrapper that allocates a 32-byte buffer and delegates to fill_random. Keeps call sites clean. A const-generic unification with random_nonce is possible but would add complexity, not reduce it. | [WEAK] Only indirectly invoked when CookieChecker::new() is called in setup(). No test checks output length, non-zero distribution, or uniqueness across calls. Given its role in generating the rotating secret, the lack of direct testing is notable. | [UNDOCUMENTED] Private function with no `///` doc comment. Name is descriptive, so leniency applies, but no documentation present."
    },
    {
      "name": "random_nonce",
      "kind": "function",
      "exported": false,
      "line_start": 251,
      "line_end": 255,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "DUPLICATE",
      "tests": "WEAK",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Non-exported function used on line 117 in create_reply_from_bytes to generate cipher nonce | [DUPLICATE] Generates 24-byte random nonce by filling buffer and returning it; extremely high semantic similarity | [OK] 24-byte nonce buffer is correct for XSalsa20 (used by xseal/xopen); delegation to fill_random is correct. | [LEAN] Mirrors random_bytes for a 24-byte nonce. The duplication is trivial (4 lines) and unifying into a const-generic helper would not be simpler overall. | [WEAK] Invoked indirectly via create_reply_from_bytes inside cookie_roundtrip and related tests. No test validates nonce uniqueness, length, or randomness quality — only that the downstream decrypt succeeds with the generated nonce. | [UNDOCUMENTED] Private function with no `///` doc comment. Does not document the 24-byte nonce size or its use with XChaCha20-Poly1305.",
      "duplicate_target": {
        "file": "rustguard-enroll/src/protocol.rs",
        "symbol": "random_nonce",
        "similarity": "0.968 — identical function pattern: fill buffer with random bytes, return nonce"
      }
    },
    {
      "name": "fill_random",
      "kind": "function",
      "exported": false,
      "line_start": 261,
      "line_end": 263,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 65,
      "detail": "[USED] Non-exported no_std stub version; alternative implementation called when std feature disabled | [UNIQUE] No_std stub implementation that is a no-op; different semantic contract than std version despite RAG similarity signal | [NEEDS_FIX] let _ = buf discards the mutable reference without writing any bytes; the buffer remains all zeros. Every call to random_bytes() or random_nonce() on no_std therefore returns an all-zero secret or nonce. A zero cookie secret means all peers share the same cookie, destroying DoS protection; a zero nonce violates the uniqueness requirement of the AEAD construction. Even as a compilation stub this is dangerous — it should at minimum emit a compile_error! or panic so misuse cannot occur silently. | [LEAN] No_std stub is the minimal compilable placeholder. The comment is honest about the kernel module responsibility. No overengineering. | [NONE] The no_std stub is a silent no-op (leaves buffer as zeros). There are no tests for the no_std configuration, and no test verifies the stub's documented contract or that downstream callers handle zeroed random data safely. | [UNDOCUMENTED] The no_std variant has only inline `//` comments, not `///` doc comments. The `///` block above attaches to the std variant only. The important stub-zeros behavior and kernel override expectation are not expressed as doc comments."
    },
    {
      "name": "fill_random",
      "kind": "function",
      "exported": false,
      "line_start": 266,
      "line_end": 271,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 65,
      "detail": "[USED] Non-exported no_std stub version; alternative implementation called when std feature disabled | [UNIQUE] No_std stub implementation that is a no-op; different semantic contract than std version despite RAG similarity signal | [NEEDS_FIX] let _ = buf discards the mutable reference without writing any bytes; the buffer remains all zeros. Every call to random_bytes() or random_nonce() on no_std therefore returns an all-zero secret or nonce. A zero cookie secret means all peers share the same cookie, destroying DoS protection; a zero nonce violates the uniqueness requirement of the AEAD construction. Even as a compilation stub this is dangerous — it should at minimum emit a compile_error! or panic so misuse cannot occur silently. | [LEAN] No_std stub is the minimal compilable placeholder. The comment is honest about the kernel module responsibility. No overengineering. | [NONE] The no_std stub is a silent no-op (leaves buffer as zeros). There are no tests for the no_std configuration, and no test verifies the stub's documented contract or that downstream callers handle zeroed random data safely. | [UNDOCUMENTED] The no_std variant has only inline `//` comments, not `///` doc comments. The `///` block above attaches to the std variant only. The important stub-zeros behavior and kernel override expectation are not expressed as doc comments."
    },
    {
      "name": "constant_time_eq_16",
      "kind": "function",
      "exported": false,
      "line_start": 273,
      "line_end": 279,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "UNDOCUMENTED",
      "confidence": 60,
      "detail": "[USED] Non-exported function used in verify_mac1 (line 140) and verify_mac2_from_bytes (line 151) for constant-time comparison | [UNIQUE] 16-byte constant-time comparison with variable-length buffer handling; different signature and semantic contract than generic constant_time_eq in handshake.rs | [NEEDS_FIX] The early return 'if b.len() < 16 { return false; }' is a data-dependent branch that leaks whether b is shorter than 16 bytes via a timing side channel, violating the constant-time contract implied by the function's use of subtle::ConstantTimeEq. All current callers pass &expected[..16] (always exactly 16 bytes) so the branch is never taken in practice, but a future caller with variable-length input could trigger a timing oracle. The check should be replaced with a constant-time length comparison or the function should be restricted to [u8; 16] inputs. | [LEAN] Security-critical constant-time comparison using the subtle crate. The mixed-type signature (fixed array vs slice) is intentional to match call sites where one operand comes from a slice. Minimal and correct. | [WEAK] Indirectly tested through verify_mac2 in mac2_verified_after_cookie and mac2_wrong_source_fails. The short-slice guard (b.len() < 16 returns false) is never exercised. The constant-time property itself is not validated. verify_mac1 (the other caller) is not tested at all. | [UNDOCUMENTED] Private function with no `///` doc comment. Its constant-time guarantee — a security-critical property — is implied by the name but never documented."
    }
  ],
  "actions": [
    {
      "id": 1,
      "description": "CookieState::process_reply: on no_std, self.received is never set after a successful decryption. Accept a monotonic timestamp parameter (or a no_std-compatible clock abstraction) and store it unconditionally so that is_fresh() can function correctly and compute_mac2 returns a real MAC instead of zeros.",
      "severity": "medium",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L213",
      "source": "correction"
    },
    {
      "id": 2,
      "description": "no_std fill_random stub silently produces all-zero output (let _ = buf is a no-op). Replace with a compile_error! macro or a link-time weak symbol that the kernel module must override, preventing accidental silent use of zero secrets and nonces.",
      "severity": "high",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L269",
      "source": "correction"
    },
    {
      "id": 3,
      "description": "encode_addr uses alloc::vec::Vec inside a #[cfg(feature = \"std\")] function; this requires extern crate alloc in scope in std mode, which is typically not declared. Replace alloc::vec::Vec with std::vec::Vec (or introduce use std::vec::Vec) to ensure the code compiles with the std feature.",
      "severity": "medium",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L230",
      "source": "correction"
    },
    {
      "id": 4,
      "description": "constant_time_eq_16: the early-exit branch on b.len() < 16 is not constant-time. Change the signature to accept &[u8; 16] for b (eliminating the length check entirely) or use a constant-time Choice-based comparison that does not branch on length.",
      "severity": "low",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L275",
      "source": "correction"
    },
    {
      "id": 5,
      "description": "no_std elapsed_since always returns Duration::ZERO, permanently disabling cookie-secret rotation in maybe_rotate_secret. If no_std callers require rotation, provide a monotonic tick counter through the existing new_with constructor or a dedicated rotate_if_stale(now_ts) method.",
      "severity": "low",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L44",
      "source": "correction"
    },
    {
      "id": 6,
      "description": "Complete JSDoc documentation for: `COOKIE_SECRET_LIFETIME`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "COOKIE_SECRET_LIFETIME",
      "target_lines": "L21-L21"
    },
    {
      "id": 7,
      "description": "Remove dead code: `COOKIE_LEN` is exported but unused",
      "severity": "high",
      "effort": "trivial",
      "category": "quickwin",
      "source": "utility",
      "target_symbol": "COOKIE_LEN",
      "target_lines": "L24-L24"
    },
    {
      "id": 8,
      "description": "Add JSDoc documentation for exported symbol: `COOKIE_LEN`",
      "severity": "medium",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "COOKIE_LEN",
      "target_lines": "L24-L24"
    },
    {
      "id": 9,
      "description": "Remove dead code: `CookieChecker` is exported but unused",
      "severity": "high",
      "effort": "trivial",
      "category": "quickwin",
      "source": "utility",
      "target_symbol": "CookieChecker",
      "target_lines": "L48-L57"
    },
    {
      "id": 10,
      "description": "Add JSDoc documentation for exported symbol: `CookieChecker`",
      "severity": "medium",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "CookieChecker",
      "target_lines": "L48-L57"
    },
    {
      "id": 11,
      "description": "Remove dead code: `CookieState` is exported but unused",
      "severity": "high",
      "effort": "trivial",
      "category": "quickwin",
      "source": "utility",
      "target_symbol": "CookieState",
      "target_lines": "L60-L65"
    },
    {
      "id": 12,
      "description": "Add JSDoc documentation for exported symbol: `CookieState`",
      "severity": "medium",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "CookieState",
      "target_lines": "L60-L65"
    },
    {
      "id": 13,
      "description": "Complete JSDoc documentation for: `encode_addr`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "encode_addr",
      "target_lines": "L227-L243"
    },
    {
      "id": 14,
      "description": "Deduplicate: `random_nonce` duplicates `random_nonce` in `rustguard-enroll/src/protocol.rs`",
      "severity": "medium",
      "effort": "small",
      "category": "refactor",
      "source": "duplication",
      "target_symbol": "random_nonce",
      "target_lines": "L251-L255"
    }
  ],
  "file_level": {
    "unused_imports": [],
    "circular_dependencies": [],
    "general_notes": "Best practices score: 8.5/10 (0 FAIL, 3 WARN, 9 PASS)"
  },
  "best_practices": {
    "score": 8.5,
    "rules": [
      {
        "rule_id": 1,
        "rule_name": "No unwrap in production code",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No bare `.unwrap()` calls in production paths. Two `.expect()` calls exist: one in `fill_random` (getrandom failure, std-only) and one in `create_reply_from_bytes` (try_into on a size-known crypto output). Both are acceptable per the rule. All `.unwrap()` calls are confined to the `#[cfg(test)]` module."
      },
      {
        "rule_id": 2,
        "rule_name": "No unsafe blocks without clear justification comment",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No `unsafe` blocks anywhere in the file."
      },
      {
        "rule_id": 3,
        "rule_name": "Proper error handling with Result/Option",
        "status": "PASS",
        "severity": "HIGH",
        "detail": "`process_reply` uses exhaustive pattern matching on `Option<Vec<u8>>`. `compute_mac2` safely falls back to zero MAC on absent/stale cookie. The no_std `fill_random` stub is a documented compile-time placeholder, not a silently swallowed error. No silent error ignores detected."
      },
      {
        "rule_id": 4,
        "rule_name": "Derive common traits on public types",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "`CookieChecker` and `CookieState` are both `pub` structs but derive neither `Debug`, `Clone`, nor `PartialEq`. At minimum `Debug` is expected on public library types to aid diagnostics and test assertions. `CookieChecker` contains a mutable secret so `Clone` may be intentionally withheld, but `Debug` (with the secret redacted via a custom impl or `#[debug = \"...\"]`) and `PartialEq` on `CookieState` would be beneficial.",
        "lines": "L52-L69"
      },
      {
        "rule_id": 5,
        "rule_name": "Lifetime annotations explicit where needed",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "All functions with reference parameters either return no reference (making elision unambiguous) or take a single `&self`/`&mut self` receiver. No case requires explicit lifetime annotations beyond what elision handles correctly."
      },
      {
        "rule_id": 6,
        "rule_name": "Use clippy idioms",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "`CookieState::new()` returns `Self` but the type does not implement the `Default` trait. Clippy (`clippy::new_without_default`) would flag this. Implementing `Default` is idiomatic when `new()` takes no arguments and returns a fixed default state.",
        "lines": "L194-L199"
      },
      {
        "rule_id": 7,
        "rule_name": "No panic! in library/production code",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No `panic!` macro calls anywhere in the production code paths."
      },
      {
        "rule_id": 8,
        "rule_name": "Module organization",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "The file is correctly placed in `rustguard-core/src/cookie.rs` and mirrors the parallel `rustguard-kmod/src/cookie.rs` for kernel use — a clean split consistent with the project's std/no_std dual-target convention. Public surface (`CookieChecker`, `CookieState`, `COOKIE_LEN`) is minimal and well-scoped. No re-export issues."
      },
      {
        "rule_id": 9,
        "rule_name": "Documentation comments on public items",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "`CookieChecker::new` (the `#[cfg(feature = \"std\")]` constructor, L73-L82) and `CookieState::new` (L194-L199) are public methods that lack `///` doc comments. All other public items and methods are well-documented.",
        "lines": "L73-L82, L194-L199"
      },
      {
        "rule_id": 10,
        "rule_name": "No hardcoded secrets or credentials",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No hardcoded secrets. The cookie secret is generated via `random_bytes()` → `getrandom` (std) or a kernel RNG stub (no_std). Test fixtures use fixed bytes for MAC1 (`[0x42u8; 16]`) which are test vectors, not credentials."
      },
      {
        "rule_id": 11,
        "rule_name": "Memory safety",
        "status": "PASS",
        "severity": "HIGH",
        "detail": "No `mem::forget`, `Box::leak`, or manual pointer manipulation. All heap allocation (`Vec` in `encode_addr`) is scoped to function bodies and dropped normally. `CookieChecker` and `CookieState` hold only stack-sized arrays and `Option` wrappers — no custom `Drop` needed."
      },
      {
        "rule_id": 12,
        "rule_name": "Concurrency safety",
        "status": "PASS",
        "severity": "HIGH",
        "detail": "Both `CookieChecker` and `CookieState` hold only `Send + Sync` primitive types (`[u8; N]`, `bool`, `Option<...>`). Mutable operations take `&mut self`, enforcing exclusive access at the call site via Rust's borrow checker. No interior mutability, no raw pointers, and no unsafe concurrency primitives are used."
      }
    ],
    "suggestions": [
      {
        "description": "Implement `Debug` (at minimum) on public structs. Since `CookieChecker` contains a rotating secret, use a manual `Debug` impl that redacts it instead of a blanket derive.",
        "before": "pub struct CookieChecker {\n    our_public: PublicKey,\n    secret: [u8; 32],\n    secret_generated: Timestamp,\n    pub under_load: bool,\n}",
        "after": "pub struct CookieChecker {\n    our_public: PublicKey,\n    secret: [u8; 32],\n    secret_generated: Timestamp,\n    pub under_load: bool,\n}\n\nimpl core::fmt::Debug for CookieChecker {\n    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {\n        f.debug_struct(\"CookieChecker\")\n            .field(\"our_public\", &self.our_public)\n            .field(\"secret\", &\"[redacted]\")\n            .field(\"under_load\", &self.under_load)\n            .finish()\n    }\n}"
      },
      {
        "description": "Implement `Default` for `CookieState` to satisfy `clippy::new_without_default` and follow idiomatic Rust.",
        "before": "impl CookieState {\n    pub fn new() -> Self {\n        Self {\n            cookie: None,\n            received: None,\n        }\n    }",
        "after": "#[derive(Default)]\npub struct CookieState {\n    cookie: Option<[u8; COOKIE_LEN]>,\n    received: Option<Timestamp>,\n}\n\nimpl CookieState {\n    pub fn new() -> Self {\n        Self::default()\n    }"
      },
      {
        "description": "Add `///` doc comments to the two undocumented public constructors.",
        "before": "#[cfg(feature = \"std\")]\npub fn new(our_public: PublicKey) -> Self {",
        "after": "/// Create a new `CookieChecker` with a freshly generated random secret (std only).\n#[cfg(feature = \"std\")]\npub fn new(our_public: PublicKey) -> Self {"
      }
    ]
  },
  "docs_coverage": {
    "concepts": [],
    "score_pct": 38
  },
  "axis_meta": {
    "utility": {
      "model": "claude-haiku-4-5-20251001",
      "cost_usd": 0.07195919999999999,
      "duration_ms": 89338
    },
    "duplication": {
      "model": "claude-haiku-4-5-20251001",
      "cost_usd": 0.07647295000000001,
      "duration_ms": 85546
    },
    "correction": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.42902025,
      "duration_ms": 244218
    },
    "overengineering": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.10671399999999999,
      "duration_ms": 38257
    },
    "tests": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.14546399999999998,
      "duration_ms": 60355
    },
    "best_practices": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.16483275,
      "duration_ms": 74687
    },
    "documentation": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.16300775,
      "duration_ms": 72175
    }
  }
}
```

## Source code of `rustguard-core/src/cookie.rs`

```typescript
//! WireGuard cookie mechanism for DoS protection.
//!
//! Under load, the responder can require proof that the initiator
//! can receive packets at its claimed IP. The flow:
//!
//! 1. Initiator sends handshake with MAC2 = zeros (no cookie yet)
//! 2. Responder is under load, sends Cookie Reply instead of processing
//! 3. Initiator decrypts cookie, stores it
//! 4. Initiator resends handshake with MAC2 = MAC(cookie, msg || mac1)
//! 5. Responder verifies MAC2 and processes the handshake
//!
//! The cookie is MAC(secret, sender_ip_port), where secret rotates every 2 min.

use core::time::Duration;

use rustguard_crypto::{self as crypto, PublicKey, LABEL_COOKIE, LABEL_MAC1};

use crate::messages::CookieReply;

/// How often the cookie secret rotates.
const COOKIE_SECRET_LIFETIME: Duration = Duration::from_secs(120);

/// Cookie size.
pub const COOKIE_LEN: usize = 16;

/// Monotonic timestamp type — same abstraction as timers.rs.
#[cfg(feature = "std")]
type Timestamp = std::time::Instant;
#[cfg(not(feature = "std"))]
type Timestamp = u64;

#[cfg(feature = "std")]
fn now() -> Timestamp {
    std::time::Instant::now()
}

#[cfg(feature = "std")]
fn elapsed_since(ts: Timestamp) -> Duration {
    ts.elapsed()
}

#[cfg(not(feature = "std"))]
fn elapsed_since(_ts: Timestamp) -> Duration {
    Duration::ZERO
}

/// Server-side cookie state: generates cookies and validates MAC2.
pub struct CookieChecker {
    /// Our static public key (used to derive encryption key).
    our_public: PublicKey,
    /// Rotating secret for cookie generation.
    secret: [u8; 32],
    /// When the secret was last generated.
    secret_generated: Timestamp,
    /// Whether we're "under load" and should require cookies.
    pub under_load: bool,
}

/// Client-side cookie state: stores a received cookie for MAC2.
pub struct CookieState {
    /// The decrypted cookie, if we have one.
    cookie: Option<[u8; COOKIE_LEN]>,
    /// When we received the cookie.
    received: Option<Timestamp>,
}

impl CookieChecker {
    #[cfg(feature = "std")]
    pub fn new(our_public: PublicKey) -> Self {
        Self {
            our_public,
            secret: random_bytes(),
            secret_generated: now(),
            under_load: false,
        }
    }

    /// Create a checker with an explicit initial secret and timestamp (no_std / kernel).
    pub fn new_with(our_public: PublicKey, secret: [u8; 32], now_ts: Timestamp) -> Self {
        Self {
            our_public,
            secret,
            secret_generated: now_ts,
            under_load: false,
        }
    }

    /// Rotate the secret if it's stale.
    fn maybe_rotate_secret(&mut self) {
        if elapsed_since(self.secret_generated) >= COOKIE_SECRET_LIFETIME {
            self.secret = random_bytes();
            #[cfg(feature = "std")]
            { self.secret_generated = now(); }
        }
    }

    /// Generate a cookie for pre-encoded source address bytes.
    /// Address encoding: IPv4 = 4 bytes IP + 2 bytes port (6 total).
    ///                   IPv6 = 16 bytes IP + 2 bytes port (18 total).
    fn make_cookie_from_bytes(&mut self, addr_bytes: &[u8]) -> [u8; COOKIE_LEN] {
        self.maybe_rotate_secret();
        let full = crypto::mac(&self.secret, &[addr_bytes]);
        let mut cookie = [0u8; COOKIE_LEN];
        cookie.copy_from_slice(&full[..COOKIE_LEN]);
        cookie
    }

    /// Build a Cookie Reply message from pre-encoded address bytes.
    pub fn create_reply_from_bytes(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        addr_bytes: &[u8],
    ) -> CookieReply {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let key = crypto::hash(&[LABEL_COOKIE, self.our_public.as_ref()]);
        let nonce = random_nonce();
        let encrypted = crypto::xseal(&key, &nonce, mac1, &cookie);

        CookieReply {
            receiver_index,
            nonce,
            encrypted_cookie: encrypted.try_into().expect("cookie + tag = 32 bytes"),
        }
    }

    /// Build a Cookie Reply message for a SocketAddr (std convenience).
    #[cfg(feature = "std")]
    pub fn create_reply(
        &mut self,
        receiver_index: u32,
        mac1: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> CookieReply {
        let addr_bytes = encode_addr(src);
        self.create_reply_from_bytes(receiver_index, mac1, &addr_bytes)
    }

    /// Verify MAC1 on an incoming message.
    pub fn verify_mac1(&self, msg_bytes: &[u8], mac1: &[u8; 16]) -> bool {
        let key = crypto::hash(&[LABEL_MAC1, self.our_public.as_ref()]);
        let expected = crypto::mac(&key, &[msg_bytes]);
        constant_time_eq_16(mac1, &expected[..16])
    }

    /// Verify MAC2 from pre-encoded address bytes.
    pub fn verify_mac2_from_bytes(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        addr_bytes: &[u8],
    ) -> bool {
        let cookie = self.make_cookie_from_bytes(addr_bytes);
        let expected = crypto::mac(&cookie, &[msg_with_mac1]);
        constant_time_eq_16(mac2, &expected[..16])
    }

    /// Verify MAC2 on an incoming message (std convenience).
    #[cfg(feature = "std")]
    pub fn verify_mac2(
        &mut self,
        msg_with_mac1: &[u8],
        mac2: &[u8; 16],
        src: &std::net::SocketAddr,
    ) -> bool {
        let addr_bytes = encode_addr(src);
        self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
    }
}

impl CookieState {
    pub fn new() -> Self {
        Self {
            cookie: None,
            received: None,
        }
    }

    /// Process a Cookie Reply and store the decrypted cookie.
    ///
    /// `their_public`: the responder's public key.
    /// `mac1`: our MAC1 from the original message.
    pub fn process_reply(
        &mut self,
        reply: &CookieReply,
        their_public: &PublicKey,
        mac1: &[u8; 16],
    ) -> bool {
        let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
        match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
            Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                let mut cookie = [0u8; COOKIE_LEN];
                cookie.copy_from_slice(&plaintext);
                self.cookie = Some(cookie);
                #[cfg(feature = "std")]
                { self.received = Some(now()); }
                true
            }
            _ => false,
        }
    }

    /// Compute MAC2 using our stored cookie. Returns zeros if no cookie.
    pub fn compute_mac2(&self, msg_with_mac1: &[u8]) -> [u8; 16] {
        match &self.cookie {
            Some(cookie) if self.is_fresh() => {
                let full = crypto::mac(cookie, &[msg_with_mac1]);
                let mut mac2 = [0u8; 16];
                mac2.copy_from_slice(&full[..16]);
                mac2
            }
            _ => [0u8; 16],
        }
    }

    /// Cookie is fresh if received within the last 120 seconds.
    fn is_fresh(&self) -> bool {
        match self.received {
            Some(ts) => elapsed_since(ts) < COOKIE_SECRET_LIFETIME,
            None => false,
        }
    }
}

/// Encode a socket address to bytes for cookie MAC.
#[cfg(feature = "std")]
fn encode_addr(addr: &std::net::SocketAddr) -> alloc::vec::Vec<u8> {
    use alloc::vec::Vec;
    match addr {
        std::net::SocketAddr::V4(a) => {
            let mut buf = Vec::with_capacity(6);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
        std::net::SocketAddr::V6(a) => {
            let mut buf = Vec::with_capacity(18);
            buf.extend_from_slice(&a.ip().octets());
            buf.extend_from_slice(&a.port().to_be_bytes());
            buf
        }
    }
}

fn random_bytes() -> [u8; 32] {
    let mut buf = [0u8; 32];
    fill_random(&mut buf);
    buf
}

fn random_nonce() -> [u8; 24] {
    let mut buf = [0u8; 24];
    fill_random(&mut buf);
    buf
}

/// Fill a buffer with cryptographically secure random bytes.
/// On std, uses getrandom. On no_std, this is a stub — kernel module
/// must provide its own RNG via get_random_bytes().
#[cfg(feature = "std")]
fn fill_random(buf: &mut [u8]) {
    getrandom::getrandom(buf).expect("failed to get random bytes");
}

#[cfg(not(feature = "std"))]
fn fill_random(buf: &mut [u8]) {
    // Kernel module overrides this — stub zeros for compilation.
    // In practice, the kernel module uses its own cookie checker
    // that calls get_random_bytes() directly.
    let _ = buf;
}

fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
    if b.len() < 16 {
        return false;
    }
    use subtle::ConstantTimeEq;
    a.ct_eq(&b[..16]).into()
}

#[cfg(test)]
mod tests {
    use super::*;
    use rustguard_crypto::StaticSecret;

    fn setup() -> (CookieChecker, CookieState, PublicKey) {
        let secret = StaticSecret::random();
        let public = secret.public_key();
        let checker = CookieChecker::new(public.clone());
        let state = CookieState::new();
        (checker, state, public)
    }

    #[test]
    fn cookie_roundtrip() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(state.process_reply(&reply, &public, &mac1));
        assert!(state.cookie.is_some());
    }

    #[test]
    fn wrong_mac1_fails_decrypt() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];
        let wrong_mac1 = [0x43u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        assert!(!state.process_reply(&reply, &public, &wrong_mac1));
    }

    #[test]
    fn mac2_verified_after_cookie() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        // Get a cookie.
        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        // Compute MAC2 with the cookie.
        let msg = b"some message with mac1 appended";
        let mac2 = state.compute_mac2(msg);
        assert_ne!(mac2, [0u8; 16]); // Should not be zeros.

        // Verify MAC2.
        assert!(checker.verify_mac2(msg, &mac2, &src));
    }

    #[test]
    fn mac2_wrong_source_fails() {
        let (mut checker, mut state, public) = setup();
        let src: std::net::SocketAddr = "10.0.0.1:12345".parse().unwrap();
        let wrong_src: std::net::SocketAddr = "10.0.0.2:12345".parse().unwrap();
        let mac1 = [0x42u8; 16];

        let reply = checker.create_reply(1, &mac1, &src);
        state.process_reply(&reply, &public, &mac1);

        let msg = b"message";
        let mac2 = state.compute_mac2(msg);

        // Different source should fail.
        assert!(!checker.verify_mac2(msg, &mac2, &wrong_src));
    }

    #[test]
    fn no_cookie_gives_zero_mac2() {
        let state = CookieState::new();
        let mac2 = state.compute_mac2(b"anything");
        assert_eq!(mac2, [0u8; 16]);
    }
}

```

## Instructions

Deliberate ONLY symbols with findings — skip clean symbols. For each symbol, address ALL its axis findings together in one entry (e.g., if a symbol is DEAD + NEEDS_FIX, deliberate both in the same entry). Recompute the verdict.

Rules:
- Only include axes with findings in original/deliberated (omit clean axes)
- Do NOT add new findings, symbols, or actions
- Protect ERROR (require ≥ 95 confidence to downgrade)
- Reclassification requires confidence ≥ 85
- Copy original values when you agree with the finding

## System (init)

**Model:** claude-opus-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{"verdict":"NEEDS_REFACTOR","symbols":[{"name":"COOKIE_SECRET_LIFETIME","original":{"documentation":"PARTIAL","confidence":82},"deliberated":{"documentation":"PARTIAL","confidence":82},"reasoning":"Documentation PARTIAL is fair — the `///` comment exists but doesn't reference the WireGuard spec's 2-minute rotation period. Minor doc improvement only."},{"name":"COOKIE_LEN","original":{"utility":"DEAD","tests":"NONE","documentation":"UNDOCUMENTED","confidence":88},"deliberated":{"utility":"USED","tests":"GOOD","documentation":"PARTIAL","confidence":90},"reasoning":"Utility DEAD reclassified → USED: this is a `pub const` in a library crate (`rustguard-core`). Downstream crates (e.g., message layout code) depend on it; pre-computed analysis likely doesn't capture cross-crate workspace imports. The entire cookie module is clearly used infrastructure for a WireGuard implementation. Tests NONE contradicts the detail text which says '[GOOD] Pure constant. Used throughout and indirectly validated in every roundtrip test.' For a pure constant with no runtime behavior, indirect validation is sufficient — reclassified to GOOD. Documentation UNDOCUMENTED is wrong — line 23 has `/// Cookie size.` which IS a doc comment, making this PARTIAL (brief but present)."},{"name":"now","original":{"tests":"WEAK","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"tests":"GOOD","documentation":"UNDOCUMENTED","confidence":85},"reasoning":"Tests WEAK reclassified → GOOD: a 2-line wrapper around `Instant::now()` has nothing meaningful to test directly. It is called in every test via `CookieChecker::new()`. Requiring dedicated tests for this trivial wrapper is unreasonable. Documentation UNDOCUMENTED kept — no doc comment exists, though the function is private and self-descriptive."},{"name":"elapsed_since","original":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"correction":"OK","tests":"WEAK","documentation":"UNDOCUMENTED","confidence":92},"reasoning":"Line 38-40 is the #[cfg(feature = \"std\")] version which calls `ts.elapsed()` — this is correct. The NEEDS_FIX detail about 'always returns Duration::ZERO' describes the NO_STD variant, not this one. This is a misapplied finding — reclassified to OK. Tests NONE → WEAK: the std version is exercised indirectly through every test that involves cookie creation/verification. Documentation UNDOCUMENTED kept — no doc comment on this private function."},{"name":"elapsed_since","original":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"correction":"OK","tests":"NONE","documentation":"UNDOCUMENTED","confidence":88},"reasoning":"Line 43-45 is the #[cfg(not(feature = \"std\"))] variant returning Duration::ZERO. The NEEDS_FIX flags this as silently disabling rotation, but the CookieChecker correction evaluator explicitly states 'the code comments acknowledge the kernel module uses an independent mechanism, making this an intentional design limitation rather than an unintended bug.' The LEAN evaluator concurs: 'No_std stub returning Duration::ZERO is the minimal correct behaviour.' This is a consistent, documented intentional design — the kernel module uses its own cookie checker. Reclassified to OK. Tests NONE kept — no no_std tests exist. Documentation UNDOCUMENTED kept."},{"name":"CookieChecker","original":{"utility":"DEAD","tests":"NONE","documentation":"UNDOCUMENTED","confidence":85},"deliberated":{"utility":"USED","tests":"WEAK","documentation":"PARTIAL","confidence":90},"reasoning":"Utility DEAD reclassified → USED: CookieChecker is the primary server-side public API of this module in a library crate. Pre-computed cross-crate analysis likely misses workspace-level imports. The entire module exists to support WireGuard cookie DoS protection — this struct is clearly intended for use by consuming crates. Tests NONE contradicts the detail text which says 'happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests' — reclassified to WEAK (tested but with gaps in verify_mac1, rotation, new_with). Documentation UNDOCUMENTED contradicts detail: 'Struct has a /// summary and all four fields have field-level /// comments.' Line 49 has a doc comment. Reclassified to PARTIAL (docs exist but missing examples and constructor docs)."},{"name":"CookieState","original":{"correction":"NEEDS_FIX","utility":"DEAD","tests":"NONE","documentation":"UNDOCUMENTED","confidence":82},"deliberated":{"correction":"NEEDS_FIX","utility":"USED","tests":"WEAK","documentation":"PARTIAL","confidence":85},"reasoning":"Correction NEEDS_FIX kept: unlike elapsed_since, the consequence here is that process_reply successfully decrypts a cookie but never marks it as fresh, making compute_mac2 silently return zeros on no_std. While the overall no_std design is intentional, this specific behavior is more insidious — a successful decryption followed by silent failure to use the cookie is a subtle trap with no comment explaining it. Utility DEAD → USED: same reasoning as CookieChecker — primary client-side public API in a library crate. Tests NONE → WEAK: detail says 'Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie.' Documentation UNDOCUMENTED → PARTIAL: detail says 'Struct has a /// summary and both private fields have doc comments.'"},{"name":"encode_addr","original":{"correction":"NEEDS_FIX","tests":"WEAK","documentation":"PARTIAL","confidence":65},"deliberated":{"correction":"OK","tests":"WEAK","documentation":"PARTIAL","confidence":86},"reasoning":"Correction NEEDS_FIX reclassified → OK: the claim that `alloc::vec::Vec` doesn't compile under std is likely wrong. In Rust crates targeting both std and no_std, it's standard practice to have `extern crate alloc;` unconditionally (or gated both ways). The function is exercised by all std tests (which create cookies using SocketAddr), proving it compiles and runs correctly. If it didn't compile, no tests would pass. Tests WEAK kept — IPv6 branch untested. Documentation PARTIAL kept."},{"name":"random_bytes","original":{"tests":"WEAK","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"tests":"WEAK","documentation":"UNDOCUMENTED","confidence":75},"reasoning":"Tests WEAK kept — indirectly exercised but no direct validation of output properties. Documentation UNDOCUMENTED kept — no doc comment on this private helper. Both findings are minor for a 4-line private function. Raised confidence slightly since assessments align with code."},{"name":"random_nonce","original":{"duplication":"DUPLICATE","tests":"WEAK","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"duplication":"UNIQUE","tests":"WEAK","documentation":"UNDOCUMENTED","confidence":88},"reasoning":"Duplication DUPLICATE reclassified → UNIQUE: the duplicate target is in `rustguard-enroll/src/protocol.rs`, a completely separate crate. These are trivial 4-line functions that fill a buffer with random bytes. Deduplicating them would require creating a shared utility crate or adding a cross-crate dependency for 4 lines of code — this would increase coupling disproportionately. Two crates independently needing to generate random nonces is normal, not a code smell. Tests WEAK and documentation UNDOCUMENTED kept."},{"name":"fill_random","original":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":65},"deliberated":{"correction":"OK","tests":"WEAK","documentation":"PARTIAL","confidence":90},"reasoning":"Line 261-263 is the #[cfg(feature = \"std\")] version using `getrandom::getrandom(buf).expect(...)` — this is correct and functional. The NEEDS_FIX detail about 'let _ = buf discards the mutable reference' describes the NO_STD variant, not this one. Misapplied finding — reclassified to OK. Tests NONE → WEAK: indirectly exercised through every test that calls random_bytes() or random_nonce(). Documentation UNDOCUMENTED → PARTIAL: the `///` doc comment block at lines 257-259 is directly above this cfg variant and attaches to it."},{"name":"fill_random","original":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":65},"deliberated":{"correction":"NEEDS_FIX","tests":"NONE","documentation":"UNDOCUMENTED","confidence":80},"reasoning":"Line 266-271 is the #[cfg(not(feature = \"std\"))] stub. NEEDS_FIX kept: `let _ = buf` silently produces all-zero 'random' output. While the comment explains this is a compilation stub and the kernel module provides its own RNG, the silent zero-fill is genuinely dangerous — if any no_std code path ever calls this, it gets zero secrets and zero nonces with no compile-time or runtime warning. A `compile_error!` or `unimplemented!` would be safer. This is the strongest no_std finding in the file. Tests NONE kept — no no_std tests exist. Documentation UNDOCUMENTED kept — only inline `//` comments, no `///` doc comment on this variant."},{"name":"constant_time_eq_16","original":{"correction":"NEEDS_FIX","tests":"WEAK","documentation":"UNDOCUMENTED","confidence":60},"deliberated":{"correction":"NEEDS_FIX","tests":"WEAK","documentation":"UNDOCUMENTED","confidence":70},"reasoning":"Correction NEEDS_FIX kept but confidence stays moderate: the early return on `b.len() < 16` is a timing side channel in a function named 'constant_time'. All current callers pass exactly 16 bytes so the branch is dead in practice, but the type signature allows variable-length input, creating a latent vulnerability for future callers. The fix (accepting `&[u8; 16]` instead of `&[u8]`) is trivial and would eliminate the issue. Tests WEAK kept — indirectly tested but the short-slice guard is never exercised. Documentation UNDOCUMENTED kept — no doc comment on this security-critical private function."}],"removed_actions":[3,5,7,9,11,14],"reasoning":"Major reclassifications: (1) Three exported symbols (COOKIE_LEN, CookieChecker, CookieState) reclassified DEAD → USED — these are the primary public API of a library crate and pre-computed analysis likely missed cross-crate workspace usage. (2) Two std-gated functions (elapsed_since L38-40, fill_random L261-263) had NEEDS_FIX findings that describe no_std behavior incorrectly applied to the std variants — reclassified to OK. (3) no_std elapsed_since reclassified NEEDS_FIX → OK as intentional documented design (kernel module uses its own checker). (4) encode_addr reclassified NEEDS_FIX → OK since `alloc::vec::Vec` compiles (proven by passing tests). (5) random_nonce DUPLICATE → UNIQUE since deduplicating a 4-line function across separate crates would add unnecessary coupling. Remaining NEEDS_FIX: CookieState's no_std process_reply (silently stores cookie but never marks fresh), fill_random no_std stub (zero-fills with no warning), and constant_time_eq_16's data-dependent branch. These are legitimate concerns warranting NEEDS_REFACTOR verdict, with the fill_random stub being the most serious."}

## rate_limit_event

## Result (success)

**Cost:** $0.4354 | **Duration:** 238.0s


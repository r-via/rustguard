# Axis: utility

## User

## File: `rustguard-core/src/handshake.rs`
## Language: rust

```rust
//! WireGuard Noise_IKpsk2 handshake.
//!
//! The handshake is a 1-RTT key exchange:
//!   Initiator -> Responder: ephemeral, encrypted static, encrypted timestamp
//!   Responder -> Initiator: ephemeral, encrypted empty
//!
//! After this exchange both sides derive symmetric transport keys.
//! The chaining key (ck) and handshake hash (h) are threaded through
//! every operation — this is the Noise pattern in action.

use alloc::vec::Vec;
use rustguard_crypto::{
    self as crypto, EphemeralSecret, PublicKey, SharedSecret, StaticSecret, Tai64n,
    CONSTRUCTION, IDENTIFIER, LABEL_MAC1,
};
use zeroize::{Zeroize, ZeroizeOnDrop};

use crate::messages::{Initiation, Response};
use crate::session::TransportSession;

/// Precomputed initial handshake state from the protocol constants.
/// These are the same for every handshake — compute once.
fn initial_chain_key() -> [u8; 32] {
    crypto::hash(&[CONSTRUCTION])
}

fn initial_hash(ck: &[u8; 32]) -> [u8; 32] {
    crypto::hash(&[ck, IDENTIFIER])
}

/// Mix a public key into the hash. Used to bind the responder's identity.
fn mix_hash(h: &[u8; 32], data: &[u8]) -> [u8; 32] {
    crypto::hash(&[h, data])
}

/// Mix the result of a DH into the chaining key.
/// Returns (new_ck, key) where key is for AEAD encryption.
fn mix_key(ck: &[u8; 32], dh_result: &SharedSecret) -> ([u8; 32], [u8; 32]) {
    let (new_ck, key, _) = crypto::hkdf(ck, dh_result.as_bytes());
    (new_ck, key)
}

/// Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.
fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
    let ciphertext = crypto::seal(key, 0, h, plaintext);
    let new_h = mix_hash(h, &ciphertext);
    (new_h, ciphertext)
}

/// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
    let plaintext = crypto::open(key, 0, h, ciphertext)?;
    let new_h = mix_hash(h, ciphertext);
    Some((new_h, plaintext))
}

/// Compute MAC1 over a message using the responder's public key.
/// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
    let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
    let full_mac = crypto::mac(&key, &[msg_bytes]);
    let mut mac1 = [0u8; 16];
    mac1.copy_from_slice(&full_mac[..16]);
    mac1
}

// ── Initiator side ──────────────────────────────────────────────────

/// State held by the initiator between sending msg1 and receiving msg2.
/// Zeroized on drop to prevent key material from lingering in memory.
#[derive(Zeroize, ZeroizeOnDrop)]
pub struct InitiatorHandshake {
    ck: [u8; 32],
    h: [u8; 32],
    ephemeral: EphemeralSecret,
    #[zeroize(skip)]
    sender_index: u32,
    #[zeroize(skip)]
    their_public: PublicKey,
    psk: [u8; 32],
}

/// Create a handshake initiation message (std convenience — uses OsRng and system clock).
#[cfg(feature = "std")]
pub fn create_initiation(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
) -> (Initiation, InitiatorHandshake) {
    create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
}

/// Create a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn create_initiation_psk(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
) -> (Initiation, InitiatorHandshake) {
    let ephemeral = EphemeralSecret::random();
    let timestamp = Tai64n::now();
    create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
}

/// Create a handshake initiation with explicit ephemeral key and timestamp.
/// This is the core function — usable from both std and no_std.
pub fn create_initiation_with(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
    ephemeral: EphemeralSecret,
    timestamp: Tai64n,
) -> (Initiation, InitiatorHandshake) {
    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix responder's public key into hash — both sides know this.
    h = mix_hash(&h, their_public.as_ref());

    let eph_public = ephemeral.public_key();

    // Mix ephemeral public into hash.
    h = mix_hash(&h, eph_public.as_ref());

    // DH(ephemeral, responder_static) -> mix into ck.
    let dh1 = ephemeral.diffie_hellman(their_public);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Encrypt our static public key.
    let our_public = our_static.public_key();
    let encrypted_static;
    (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());

    // DH(our_static, responder_static) -> mix into ck.
    let dh2 = our_static.diffie_hellman(their_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Encrypt timestamp.
    let encrypted_timestamp;
    (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());

    // Build message (mac1/mac2 computed over wire bytes).
    let mut msg = Initiation {
        sender_index,
        ephemeral: *eph_public.as_bytes(),
        encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
        encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16], // No cookie mechanism yet.
    };

    // MAC1 is computed over everything before mac1 field.
    let wire = msg.to_bytes();
    msg.mac1 = compute_mac1(their_public, &wire[..116]);

    let state = InitiatorHandshake {
        ck,
        h,
        ephemeral,
        sender_index,
        their_public: their_public.clone(),
        psk: *psk,
    };

    (msg, state)
}

/// Process a handshake response and derive transport keys.
pub fn process_response(
    state: InitiatorHandshake,
    our_static: &StaticSecret,
    msg: &Response,
) -> Option<TransportSession> {
    // Access fields by reference — struct will zeroize on drop.
    let mut ck = state.ck;
    let mut h = state.h;
    let sender_index = state.sender_index;
    let psk = state.psk;

    // Verify receiver_index matches our sender_index.
    if msg.receiver_index != sender_index {
        return None;
    }

    let resp_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph.as_ref());

    // DH(our_ephemeral, responder_ephemeral)
    let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh1);

    // DH(our_static, responder_ephemeral)
    let dh2 = our_static.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh2);

    // PSK phase (Noise_IKpsk2): mix PSK into chain.
    let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Decrypt empty payload.
    let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
    let (h, _empty) = result?;
    let _ = h;

    // Derive transport keys: initiator sends with first, receives with second.
    let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);

    Some(TransportSession::new(
        sender_index,
        msg.sender_index,
        send_key,
        recv_key,
    ))
}

// ── Responder side ──────────────────────────────────────────────────

/// Process a handshake initiation on the responder side (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
}

/// Process a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation_psk(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let resp_eph = EphemeralSecret::random();
    process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
}

/// Process a handshake initiation with explicit ephemeral (no_std compatible).
pub fn process_initiation_with(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
    resp_eph: EphemeralSecret,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let our_public = our_static.public_key();

    // ── Verify MAC1 FIRST — cheap check before any DH. ──
    let wire = msg.to_bytes();
    let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
    if !constant_time_eq(&msg.mac1, &expected_mac1) {
        return None;
    }
    // TODO: MAC2 / cookie validation under load.

    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix our public key into hash (responder's identity).
    h = mix_hash(&h, our_public.as_ref());

    let init_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix initiator ephemeral into hash.
    h = mix_hash(&h, init_eph.as_ref());

    // DH(our_static, initiator_ephemeral)
    let dh1 = our_static.diffie_hellman(&init_eph);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Decrypt initiator's static public key.
    let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
    h = new_h;

    let initiator_public = PublicKey::from_bytes(
        static_bytes
            .as_slice()
            .try_into()
            .ok()?,
    );

    // DH(our_static, initiator_static)
    let dh2 = our_static.diffie_hellman(&initiator_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Decrypt timestamp.
    let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
    h = new_h;

    // Validate timestamp is newer than last seen — prevents replay.
    let timestamp = Tai64n::from_bytes(
        timestamp_bytes.as_slice().try_into().ok()?,
    );
    if let Some(last) = last_timestamp {
        if !timestamp.is_after(last) {
            return None; // Replayed or stale initiation.
        }
    }

    // ── Build response ──

    let resp_eph_public = resp_eph.public_key();

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph_public.as_ref());

    // DH(resp_ephemeral, initiator_ephemeral)
    let dh3 = resp_eph.diffie_hellman(&init_eph);
    (ck, _) = mix_key(&ck, &dh3);

    // DH(resp_ephemeral, initiator_static)
    let dh4 = resp_eph.diffie_hellman(&initiator_public);
    (ck, _) = mix_key(&ck, &dh4);

    // PSK phase.
    let (new_ck, t, key3) = crypto::hkdf(&ck, psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Encrypt empty.
    let encrypted_empty;
    (h, encrypted_empty) = encrypt_and_hash(&key3, &h, &[]);
    let _ = h; // final hash

    // Derive transport keys: responder sends with second, receives with first.
    let (recv_key, send_key, _) = crypto::hkdf(&ck, &[]);

    let mut resp = Response {
        sender_index: responder_index,
        receiver_index: msg.sender_index,
        ephemeral: *resp_eph_public.as_bytes(),
        encrypted_empty: encrypted_empty.try_into().expect("empty encrypted is 16 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16],
    };

    // MAC1 on response.
    let resp_wire = resp.to_bytes();
    resp.mac1 = compute_mac1(&initiator_public, &resp_wire[..60]);

    let session = TransportSession::new(responder_index, msg.sender_index, send_key, recv_key);

    Some((initiator_public, timestamp, resp, session))
}

/// Constant-time comparison for MACs using the `subtle` crate.
fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
    use subtle::ConstantTimeEq;
    a.ct_eq(b).into()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn full_handshake_and_transport() {
        // Setup: both sides have static keys, initiator knows responder's public key.
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        // Step 1: Initiator creates handshake initiation.
        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        // Step 2: Responder processes initiation, creates response.
        let (peer_pubkey, _ts, resp_msg, mut resp_session) =
            process_initiation(&responder_static, &init_msg, 2)
                .expect("responder should accept initiation");

        // Responder correctly identifies the initiator.
        assert_eq!(peer_pubkey, initiator_static.public_key());

        // Step 3: Initiator processes response, gets transport session.
        let mut init_session =
            process_response(init_state, &initiator_static, &resp_msg)
                .expect("initiator should accept response");

        // Step 4: Both sides can now exchange encrypted data.
        let plaintext = b"hello from the trenches";
        let (counter, ciphertext) = init_session.encrypt(plaintext).unwrap();
        let decrypted = resp_session
            .decrypt(counter, &ciphertext)
            .expect("responder should decrypt");
        assert_eq!(&decrypted, plaintext);

        // And the other direction.
        let reply = b"copy that, over";
        let (counter, ciphertext) = resp_session.encrypt(reply).unwrap();
        let decrypted = init_session
            .decrypt(counter, &ciphertext)
            .expect("initiator should decrypt");
        assert_eq!(&decrypted, reply);
    }

    #[test]
    fn wrong_responder_key_rejects() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let wrong_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // A different responder (wrong key) tries to process the initiation.
        let result = process_initiation(&wrong_static, &init_msg, 2);
        assert!(result.is_none(), "wrong responder key should fail");
    }

    #[test]
    fn tampered_initiation_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (mut init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // Tamper with the encrypted static field.
        init_msg.encrypted_static[0] ^= 0xff;

        let result = process_initiation(&responder_static, &init_msg, 2);
        assert!(result.is_none(), "tampered initiation should fail");
    }

    #[test]
    fn tampered_response_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Tamper with the encrypted empty field.
        resp_msg.encrypted_empty[0] ^= 0xff;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "tampered response should fail");
    }

    #[test]
    fn mismatched_receiver_index_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Change receiver_index to wrong value.
        resp_msg.receiver_index = 999;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "wrong receiver index should fail");
    }
}

```

## Symbols to evaluate

- function initial_chain_key (L23–L25)
- function initial_hash (L27–L29)
- function mix_hash (L32–L34)
- function mix_key (L38–L41)
- function encrypt_and_hash (L44–L48)
- function decrypt_and_hash (L51–L55)
- export function compute_mac1 (L59–L65)
- export class InitiatorHandshake (L72–L81)
- export function create_initiation (L85–L91)
- export function create_initiation_psk (L95–L104)
- export function create_initiation_with (L108–L170)
- export function process_response (L173–L221)
- export function process_initiation (L227–L233)
- export function process_initiation_psk (L237–L246)
- export function process_initiation_with (L249–L357)
- function constant_time_eq (L360–L363)

## Pre-computed Import Analysis

- initial_chain_key (not exported): internal only — check local usage in file
- initial_hash (not exported): internal only — check local usage in file
- mix_hash (not exported): internal only — check local usage in file
- mix_key (not exported): internal only — check local usage in file
- encrypt_and_hash (not exported): internal only — check local usage in file
- decrypt_and_hash (not exported): internal only — check local usage in file
- compute_mac1 (exported): imported by 0 files — LIKELY DEAD
- InitiatorHandshake (exported): imported by 0 files — LIKELY DEAD
- create_initiation (exported): imported by 0 files — LIKELY DEAD
- create_initiation_psk (exported): imported by 0 files — LIKELY DEAD
- create_initiation_with (exported): imported by 0 files — LIKELY DEAD
- process_response (exported): imported by 0 files — LIKELY DEAD
- process_initiation (exported): imported by 0 files — LIKELY DEAD
- process_initiation_psk (exported): imported by 0 files — LIKELY DEAD
- process_initiation_with (exported): imported by 0 files — LIKELY DEAD
- constant_time_eq (not exported): internal only — check local usage in file

Evaluate the utility of each symbol and output the JSON.

## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "initial_chain_key",
      "line_start": 23,
      "line_end": 25,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in create_initiation_with (line 119) and process_initiation_with (line 275)"
    },
    {
      "name": "initial_hash",
      "line_start": 27,
      "line_end": 29,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in create_initiation_with (line 120) and process_initiation_with (line 276)"
    },
    {
      "name": "mix_hash",
      "line_start": 32,
      "line_end": 34,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in encrypt_and_hash (line 46), decrypt_and_hash (line 54), and multiple times in both initiator and responder paths"
    },
    {
      "name": "mix_key",
      "line_start": 38,
      "line_end": 41,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called multiple times in create_initiation_with (lines 132, 145) and process_initiation_with (lines 296, 318, 337, 341)"
    },
    {
      "name": "encrypt_and_hash",
      "line_start": 44,
      "line_end": 48,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in create_initiation_with (lines 139, 148) and process_initiation_with (line 351)"
    },
    {
      "name": "decrypt_and_hash",
      "line_start": 51,
      "line_end": 55,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in process_response (line 222) and process_initiation_with (lines 308, 319)"
    },
    {
      "name": "compute_mac1",
      "line_start": 59,
      "line_end": 65,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in create_initiation_with (line 162), process_initiation_with (lines 278, 356)"
    },
    {
      "name": "InitiatorHandshake",
      "line_start": 72,
      "line_end": 81,
      "utility": "USED",
      "confidence": 95,
      "detail": "Returned by create_initiation_with (line 163) and accepted by process_response (line 174); used extensively in tests"
    },
    {
      "name": "create_initiation",
      "line_start": 85,
      "line_end": 91,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in full_handshake_and_transport test (line 389) and other tests (lines 406, 427, 441)"
    },
    {
      "name": "create_initiation_psk",
      "line_start": 95,
      "line_end": 104,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called by create_initiation (line 91) and used as entry point for PSK-based handshakes"
    },
    {
      "name": "create_initiation_with",
      "line_start": 108,
      "line_end": 170,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called by create_initiation_psk (line 103); core function implementing initiator handshake message creation"
    },
    {
      "name": "process_response",
      "line_start": 173,
      "line_end": 221,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in full_handshake_and_transport test (line 392) and failure scenario tests (lines 450, 465)"
    },
    {
      "name": "process_initiation",
      "line_start": 227,
      "line_end": 233,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in tests (lines 390, 408, 428, 442); convenience wrapper for responder initiation processing"
    },
    {
      "name": "process_initiation_psk",
      "line_start": 237,
      "line_end": 246,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called by process_initiation (line 232); PSK-based responder handshake processing"
    },
    {
      "name": "process_initiation_with",
      "line_start": 249,
      "line_end": 357,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called by process_initiation_psk (line 245); core function implementing responder initiation processing"
    },
    {
      "name": "constant_time_eq",
      "line_start": 360,
      "line_end": 363,
      "utility": "USED",
      "confidence": 95,
      "detail": "Called in process_initiation_with (line 279) for constant-time MAC comparison; encapsulates subtle::ConstantTimeEq conversion"
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.0616 | **Duration:** 66.6s


---

# Axis: duplication

## User

## File: `rustguard-core/src/handshake.rs`
## Language: rust

```rust
//! WireGuard Noise_IKpsk2 handshake.
//!
//! The handshake is a 1-RTT key exchange:
//!   Initiator -> Responder: ephemeral, encrypted static, encrypted timestamp
//!   Responder -> Initiator: ephemeral, encrypted empty
//!
//! After this exchange both sides derive symmetric transport keys.
//! The chaining key (ck) and handshake hash (h) are threaded through
//! every operation — this is the Noise pattern in action.

use alloc::vec::Vec;
use rustguard_crypto::{
    self as crypto, EphemeralSecret, PublicKey, SharedSecret, StaticSecret, Tai64n,
    CONSTRUCTION, IDENTIFIER, LABEL_MAC1,
};
use zeroize::{Zeroize, ZeroizeOnDrop};

use crate::messages::{Initiation, Response};
use crate::session::TransportSession;

/// Precomputed initial handshake state from the protocol constants.
/// These are the same for every handshake — compute once.
fn initial_chain_key() -> [u8; 32] {
    crypto::hash(&[CONSTRUCTION])
}

fn initial_hash(ck: &[u8; 32]) -> [u8; 32] {
    crypto::hash(&[ck, IDENTIFIER])
}

/// Mix a public key into the hash. Used to bind the responder's identity.
fn mix_hash(h: &[u8; 32], data: &[u8]) -> [u8; 32] {
    crypto::hash(&[h, data])
}

/// Mix the result of a DH into the chaining key.
/// Returns (new_ck, key) where key is for AEAD encryption.
fn mix_key(ck: &[u8; 32], dh_result: &SharedSecret) -> ([u8; 32], [u8; 32]) {
    let (new_ck, key, _) = crypto::hkdf(ck, dh_result.as_bytes());
    (new_ck, key)
}

/// Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.
fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
    let ciphertext = crypto::seal(key, 0, h, plaintext);
    let new_h = mix_hash(h, &ciphertext);
    (new_h, ciphertext)
}

/// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
    let plaintext = crypto::open(key, 0, h, ciphertext)?;
    let new_h = mix_hash(h, ciphertext);
    Some((new_h, plaintext))
}

/// Compute MAC1 over a message using the responder's public key.
/// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
    let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
    let full_mac = crypto::mac(&key, &[msg_bytes]);
    let mut mac1 = [0u8; 16];
    mac1.copy_from_slice(&full_mac[..16]);
    mac1
}

// ── Initiator side ──────────────────────────────────────────────────

/// State held by the initiator between sending msg1 and receiving msg2.
/// Zeroized on drop to prevent key material from lingering in memory.
#[derive(Zeroize, ZeroizeOnDrop)]
pub struct InitiatorHandshake {
    ck: [u8; 32],
    h: [u8; 32],
    ephemeral: EphemeralSecret,
    #[zeroize(skip)]
    sender_index: u32,
    #[zeroize(skip)]
    their_public: PublicKey,
    psk: [u8; 32],
}

/// Create a handshake initiation message (std convenience — uses OsRng and system clock).
#[cfg(feature = "std")]
pub fn create_initiation(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
) -> (Initiation, InitiatorHandshake) {
    create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
}

/// Create a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn create_initiation_psk(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
) -> (Initiation, InitiatorHandshake) {
    let ephemeral = EphemeralSecret::random();
    let timestamp = Tai64n::now();
    create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
}

/// Create a handshake initiation with explicit ephemeral key and timestamp.
/// This is the core function — usable from both std and no_std.
pub fn create_initiation_with(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
    ephemeral: EphemeralSecret,
    timestamp: Tai64n,
) -> (Initiation, InitiatorHandshake) {
    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix responder's public key into hash — both sides know this.
    h = mix_hash(&h, their_public.as_ref());

    let eph_public = ephemeral.public_key();

    // Mix ephemeral public into hash.
    h = mix_hash(&h, eph_public.as_ref());

    // DH(ephemeral, responder_static) -> mix into ck.
    let dh1 = ephemeral.diffie_hellman(their_public);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Encrypt our static public key.
    let our_public = our_static.public_key();
    let encrypted_static;
    (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());

    // DH(our_static, responder_static) -> mix into ck.
    let dh2 = our_static.diffie_hellman(their_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Encrypt timestamp.
    let encrypted_timestamp;
    (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());

    // Build message (mac1/mac2 computed over wire bytes).
    let mut msg = Initiation {
        sender_index,
        ephemeral: *eph_public.as_bytes(),
        encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
        encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16], // No cookie mechanism yet.
    };

    // MAC1 is computed over everything before mac1 field.
    let wire = msg.to_bytes();
    msg.mac1 = compute_mac1(their_public, &wire[..116]);

    let state = InitiatorHandshake {
        ck,
        h,
        ephemeral,
        sender_index,
        their_public: their_public.clone(),
        psk: *psk,
    };

    (msg, state)
}

/// Process a handshake response and derive transport keys.
pub fn process_response(
    state: InitiatorHandshake,
    our_static: &StaticSecret,
    msg: &Response,
) -> Option<TransportSession> {
    // Access fields by reference — struct will zeroize on drop.
    let mut ck = state.ck;
    let mut h = state.h;
    let sender_index = state.sender_index;
    let psk = state.psk;

    // Verify receiver_index matches our sender_index.
    if msg.receiver_index != sender_index {
        return None;
    }

    let resp_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph.as_ref());

    // DH(our_ephemeral, responder_ephemeral)
    let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh1);

    // DH(our_static, responder_ephemeral)
    let dh2 = our_static.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh2);

    // PSK phase (Noise_IKpsk2): mix PSK into chain.
    let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Decrypt empty payload.
    let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
    let (h, _empty) = result?;
    let _ = h;

    // Derive transport keys: initiator sends with first, receives with second.
    let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);

    Some(TransportSession::new(
        sender_index,
        msg.sender_index,
        send_key,
        recv_key,
    ))
}

// ── Responder side ──────────────────────────────────────────────────

/// Process a handshake initiation on the responder side (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
}

/// Process a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation_psk(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let resp_eph = EphemeralSecret::random();
    process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
}

/// Process a handshake initiation with explicit ephemeral (no_std compatible).
pub fn process_initiation_with(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
    resp_eph: EphemeralSecret,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let our_public = our_static.public_key();

    // ── Verify MAC1 FIRST — cheap check before any DH. ──
    let wire = msg.to_bytes();
    let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
    if !constant_time_eq(&msg.mac1, &expected_mac1) {
        return None;
    }
    // TODO: MAC2 / cookie validation under load.

    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix our public key into hash (responder's identity).
    h = mix_hash(&h, our_public.as_ref());

    let init_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix initiator ephemeral into hash.
    h = mix_hash(&h, init_eph.as_ref());

    // DH(our_static, initiator_ephemeral)
    let dh1 = our_static.diffie_hellman(&init_eph);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Decrypt initiator's static public key.
    let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
    h = new_h;

    let initiator_public = PublicKey::from_bytes(
        static_bytes
            .as_slice()
            .try_into()
            .ok()?,
    );

    // DH(our_static, initiator_static)
    let dh2 = our_static.diffie_hellman(&initiator_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Decrypt timestamp.
    let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
    h = new_h;

    // Validate timestamp is newer than last seen — prevents replay.
    let timestamp = Tai64n::from_bytes(
        timestamp_bytes.as_slice().try_into().ok()?,
    );
    if let Some(last) = last_timestamp {
        if !timestamp.is_after(last) {
            return None; // Replayed or stale initiation.
        }
    }

    // ── Build response ──

    let resp_eph_public = resp_eph.public_key();

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph_public.as_ref());

    // DH(resp_ephemeral, initiator_ephemeral)
    let dh3 = resp_eph.diffie_hellman(&init_eph);
    (ck, _) = mix_key(&ck, &dh3);

    // DH(resp_ephemeral, initiator_static)
    let dh4 = resp_eph.diffie_hellman(&initiator_public);
    (ck, _) = mix_key(&ck, &dh4);

    // PSK phase.
    let (new_ck, t, key3) = crypto::hkdf(&ck, psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Encrypt empty.
    let encrypted_empty;
    (h, encrypted_empty) = encrypt_and_hash(&key3, &h, &[]);
    let _ = h; // final hash

    // Derive transport keys: responder sends with second, receives with first.
    let (recv_key, send_key, _) = crypto::hkdf(&ck, &[]);

    let mut resp = Response {
        sender_index: responder_index,
        receiver_index: msg.sender_index,
        ephemeral: *resp_eph_public.as_bytes(),
        encrypted_empty: encrypted_empty.try_into().expect("empty encrypted is 16 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16],
    };

    // MAC1 on response.
    let resp_wire = resp.to_bytes();
    resp.mac1 = compute_mac1(&initiator_public, &resp_wire[..60]);

    let session = TransportSession::new(responder_index, msg.sender_index, send_key, recv_key);

    Some((initiator_public, timestamp, resp, session))
}

/// Constant-time comparison for MACs using the `subtle` crate.
fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
    use subtle::ConstantTimeEq;
    a.ct_eq(b).into()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn full_handshake_and_transport() {
        // Setup: both sides have static keys, initiator knows responder's public key.
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        // Step 1: Initiator creates handshake initiation.
        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        // Step 2: Responder processes initiation, creates response.
        let (peer_pubkey, _ts, resp_msg, mut resp_session) =
            process_initiation(&responder_static, &init_msg, 2)
                .expect("responder should accept initiation");

        // Responder correctly identifies the initiator.
        assert_eq!(peer_pubkey, initiator_static.public_key());

        // Step 3: Initiator processes response, gets transport session.
        let mut init_session =
            process_response(init_state, &initiator_static, &resp_msg)
                .expect("initiator should accept response");

        // Step 4: Both sides can now exchange encrypted data.
        let plaintext = b"hello from the trenches";
        let (counter, ciphertext) = init_session.encrypt(plaintext).unwrap();
        let decrypted = resp_session
            .decrypt(counter, &ciphertext)
            .expect("responder should decrypt");
        assert_eq!(&decrypted, plaintext);

        // And the other direction.
        let reply = b"copy that, over";
        let (counter, ciphertext) = resp_session.encrypt(reply).unwrap();
        let decrypted = init_session
            .decrypt(counter, &ciphertext)
            .expect("initiator should decrypt");
        assert_eq!(&decrypted, reply);
    }

    #[test]
    fn wrong_responder_key_rejects() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let wrong_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // A different responder (wrong key) tries to process the initiation.
        let result = process_initiation(&wrong_static, &init_msg, 2);
        assert!(result.is_none(), "wrong responder key should fail");
    }

    #[test]
    fn tampered_initiation_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (mut init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // Tamper with the encrypted static field.
        init_msg.encrypted_static[0] ^= 0xff;

        let result = process_initiation(&responder_static, &init_msg, 2);
        assert!(result.is_none(), "tampered initiation should fail");
    }

    #[test]
    fn tampered_response_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Tamper with the encrypted empty field.
        resp_msg.encrypted_empty[0] ^= 0xff;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "tampered response should fail");
    }

    #[test]
    fn mismatched_receiver_index_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Change receiver_index to wrong value.
        resp_msg.receiver_index = 999;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "wrong receiver index should fail");
    }
}

```

## Symbols to evaluate

- function initial_chain_key (L23–L25)
- function initial_hash (L27–L29)
- function mix_hash (L32–L34)
- function mix_key (L38–L41)
- function encrypt_and_hash (L44–L48)
- function decrypt_and_hash (L51–L55)
- export function compute_mac1 (L59–L65)
- export class InitiatorHandshake (L72–L81)
- export function create_initiation (L85–L91)
- export function create_initiation_psk (L95–L104)
- export function create_initiation_with (L108–L170)
- export function process_response (L173–L221)
- export function process_initiation (L227–L233)
- export function process_initiation_psk (L237–L246)
- export function process_initiation_with (L249–L357)
- function constant_time_eq (L360–L363)

## RAG — Semantic Duplication

### initial_chain_key (L23–L25)
Trivial function (≤ 2 lines). Mark UNIQUE.

### initial_hash (L27–L29)
Trivial function (≤ 2 lines). Mark UNIQUE.

### mix_hash (L32–L34)
Trivial function (≤ 2 lines). Mark UNIQUE.

### mix_key (L38–L41)
Similar functions found:
- **mix_key** in `rustguard-kmod/src/noise.rs` (score: 0.961)
  Signature: fn mix_key(ck: &[u8; 32], dh_result: &[u8; 32]) -> ([u8; 32], [u8; 32]) {
  Complexity: 1/5
  Calls: hkdf
  Source:
  ```rust
  fn mix_key(ck: &[u8; 32], dh_result: &[u8; 32]) -> ([u8; 32], [u8; 32]) {
      let (new_ck, key, _) = hkdf(ck, dh_result);
      (new_ck, key)
  }
  
  /// Encrypt-and-hash: AEAD encrypt, mix ciphertext into hash.
  fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8], ct_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
      let ct_len = seal(key, 0, h, plaintext, ct_buf)?;
      let new_h = mix_hash(h, &ct_buf[..ct_len]);
      Some((new_h, ct_len))
  }
  
  /// Decrypt-and-hash: AEAD decrypt, mix ciphertext into hash.
  fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8], pt_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
      let pt_len = open(key, 0, h, ciphertext, pt_buf)?;
      let new_h = mix_hash(h, ciphertext);
      Some((new_h, pt_len))
  }
  
  /// Compute MAC1 over a message.
  fn compute_mac1(responder_public: &[u8; 32], msg_bytes: &[u8]) -> [u8; 16] {
      let key = hash(&[LABEL_MAC1, responder_public]);
      let full = mac(&key, msg_bytes);
      let mut mac1 = [0u8; 16];
      mac1.copy_from_slice(&full[..16]);
      mac1
  }
  
  // ── TAI64N timestamp ──────────────────────────────────────────────────
  
  /// Generate a TAI64N timestamp from kernel wall clock.
  fn tai64n_now() -> [u8; 12] {
      extern "C" {
          fn wg_ktime_get_real(secs: *mut i64, nsecs: *mut i64);
      }
      let mut secs: i64 = 0;
      let mut nsecs: i64 = 0;
      unsafe { wg_ktime_get_real(&mut secs, &mut nsecs) };
  
      let tai_secs = (secs as u64) + TAI64_EPOCH_OFFSET;
      let nanos = nsecs as u32;
      let mut buf = [0u8; 12];
      buf[..8].copy_from_slice(&tai_secs.to_be_bytes());
      buf[8..].copy_from_slice(&nanos.to_be_bytes());
      buf
  }
  
  // ── Wire format constants ─────────────────────────────────────────────
  
  /// Message type 1: Handshake Initiation (148 bytes).
  ```

### encrypt_and_hash (L44–L48)
Similar functions found:
- **encrypt_and_hash** in `rustguard-kmod/src/noise.rs` (score: 0.922)
  Signature: fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8], ct_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
  Complexity: 1/5
  Calls: seal, mix_hash
  Source:
  ```rust
  fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8], ct_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
      let ct_len = seal(key, 0, h, plaintext, ct_buf)?;
      let new_h = mix_hash(h, &ct_buf[..ct_len]);
      Some((new_h, ct_len))
  }
  
  /// Decrypt-and-hash: AEAD decrypt, mix ciphertext into hash.
  fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8], pt_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
      let pt_len = open(key, 0, h, ciphertext, pt_buf)?;
      let new_h = mix_hash(h, ciphertext);
      Some((new_h, pt_len))
  }
  
  /// Compute MAC1 over a message.
  fn compute_mac1(responder_public: &[u8; 32], msg_bytes: &[u8]) -> [u8; 16] {
      let key = hash(&[LABEL_MAC1, responder_public]);
      let full = mac(&key, msg_bytes);
      let mut mac1 = [0u8; 16];
      mac1.copy_from_slice(&full[..16]);
      mac1
  }
  
  // ── TAI64N timestamp ──────────────────────────────────────────────────
  
  /// Generate a TAI64N timestamp from kernel wall clock.
  fn tai64n_now() -> [u8; 12] {
      extern "C" {
          fn wg_ktime_get_real(secs: *mut i64, nsecs: *mut i64);
      }
      let mut secs: i64 = 0;
      let mut nsecs: i64 = 0;
      unsafe { wg_ktime_get_real(&mut secs, &mut nsecs) };
  
      let tai_secs = (secs as u64) + TAI64_EPOCH_OFFSET;
      let nanos = nsecs as u32;
      let mut buf = [0u8; 12];
      buf[..8].copy_from_slice(&tai_secs.to_be_bytes());
      buf[8..].copy_from_slice(&nanos.to_be_bytes());
      buf
  }
  
  // ── Wire format constants ─────────────────────────────────────────────
  
  /// Message type 1: Handshake Initiation (148 bytes).
  pub(crate) const MSG_INITIATION: u32 = 1;
  /// Message type 2: Handshake Response (92 bytes).
  pub(crate) const MSG_RESPONSE: u32 = 2;
  /// Message type 4: Transport Data.
  pub(crate) const MSG_TRANSPORT: u32 = 4;
  
  ```
- **decrypt_and_hash** in `rustguard-core/src/handshake.rs` (score: 0.782)
  Signature: fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
  Complexity: 1/5
  Calls: mix_hash
  Source:
  ```rust
  fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
      let plaintext = crypto::open(key, 0, h, ciphertext)?;
      let new_h = mix_hash(h, ciphertext);
      Some((new_h, plaintext))
  }
  
  /// Compute MAC1 over a message using the responder's public key.
  /// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
  pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
      let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
      let full_mac = crypto::mac(&key, &[msg_bytes]);
      let mut mac1 = [0u8; 16];
      mac1.copy_from_slice(&full_mac[..16]);
      mac1
  }
  
  // ── Initiator side ──────────────────────────────────────────────────
  
  /// State held by the initiator between sending msg1 and receiving msg2.
  /// Zeroized on drop to prevent key material from lingering in memory.
  #[derive(Zeroize, ZeroizeOnDrop)]
  pub struct InitiatorHandshake {
      ck: [u8; 32],
      h: [u8; 32],
      ephemeral: EphemeralSecret,
      #[zeroize(skip)]
      sender_index: u32,
      #[zeroize(skip)]
      their_public: PublicKey,
      psk: [u8; 32],
  }
  
  /// Create a handshake initiation message (std convenience — uses OsRng and system clock).
  #[cfg(feature = "std")]
  pub fn create_initiation(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
  ) -> (Initiation, InitiatorHandshake) {
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn create_initiation_psk(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
  ) -> (Initiation, InitiatorHandshake) {
  ```
- **decrypt_and_hash** in `rustguard-kmod/src/noise.rs` (score: 0.738)
  Signature: fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8], pt_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
  Complexity: 1/5
  Calls: open, mix_hash
  Source:
  ```rust
  fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8], pt_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
      let pt_len = open(key, 0, h, ciphertext, pt_buf)?;
      let new_h = mix_hash(h, ciphertext);
      Some((new_h, pt_len))
  }
  
  /// Compute MAC1 over a message.
  fn compute_mac1(responder_public: &[u8; 32], msg_bytes: &[u8]) -> [u8; 16] {
      let key = hash(&[LABEL_MAC1, responder_public]);
      let full = mac(&key, msg_bytes);
      let mut mac1 = [0u8; 16];
      mac1.copy_from_slice(&full[..16]);
      mac1
  }
  
  // ── TAI64N timestamp ──────────────────────────────────────────────────
  
  /// Generate a TAI64N timestamp from kernel wall clock.
  fn tai64n_now() -> [u8; 12] {
      extern "C" {
          fn wg_ktime_get_real(secs: *mut i64, nsecs: *mut i64);
      }
      let mut secs: i64 = 0;
      let mut nsecs: i64 = 0;
      unsafe { wg_ktime_get_real(&mut secs, &mut nsecs) };
  
      let tai_secs = (secs as u64) + TAI64_EPOCH_OFFSET;
      let nanos = nsecs as u32;
      let mut buf = [0u8; 12];
      buf[..8].copy_from_slice(&tai_secs.to_be_bytes());
      buf[8..].copy_from_slice(&nanos.to_be_bytes());
      buf
  }
  
  // ── Wire format constants ─────────────────────────────────────────────
  
  /// Message type 1: Handshake Initiation (148 bytes).
  pub(crate) const MSG_INITIATION: u32 = 1;
  /// Message type 2: Handshake Response (92 bytes).
  pub(crate) const MSG_RESPONSE: u32 = 2;
  /// Message type 4: Transport Data.
  pub(crate) const MSG_TRANSPORT: u32 = 4;
  
  pub(crate) const INITIATION_SIZE: usize = 148;
  pub(crate) const RESPONSE_SIZE: usize = 92;
  
  // ── Transport session ─────────────────────────────────────────────────
  
  /// Derived transport keys from a completed handshake.
  pub(crate) struct TransportKeys {
  ```

### decrypt_and_hash (L51–L55)
Similar functions found:
- **decrypt_and_hash** in `rustguard-kmod/src/noise.rs` (score: 0.918)
  Signature: fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8], pt_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
  Complexity: 1/5
  Calls: open, mix_hash
  Source:
  ```rust
  fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8], pt_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
      let pt_len = open(key, 0, h, ciphertext, pt_buf)?;
      let new_h = mix_hash(h, ciphertext);
      Some((new_h, pt_len))
  }
  
  /// Compute MAC1 over a message.
  fn compute_mac1(responder_public: &[u8; 32], msg_bytes: &[u8]) -> [u8; 16] {
      let key = hash(&[LABEL_MAC1, responder_public]);
      let full = mac(&key, msg_bytes);
      let mut mac1 = [0u8; 16];
      mac1.copy_from_slice(&full[..16]);
      mac1
  }
  
  // ── TAI64N timestamp ──────────────────────────────────────────────────
  
  /// Generate a TAI64N timestamp from kernel wall clock.
  fn tai64n_now() -> [u8; 12] {
      extern "C" {
          fn wg_ktime_get_real(secs: *mut i64, nsecs: *mut i64);
      }
      let mut secs: i64 = 0;
      let mut nsecs: i64 = 0;
      unsafe { wg_ktime_get_real(&mut secs, &mut nsecs) };
  
      let tai_secs = (secs as u64) + TAI64_EPOCH_OFFSET;
      let nanos = nsecs as u32;
      let mut buf = [0u8; 12];
      buf[..8].copy_from_slice(&tai_secs.to_be_bytes());
      buf[8..].copy_from_slice(&nanos.to_be_bytes());
      buf
  }
  
  // ── Wire format constants ─────────────────────────────────────────────
  
  /// Message type 1: Handshake Initiation (148 bytes).
  pub(crate) const MSG_INITIATION: u32 = 1;
  /// Message type 2: Handshake Response (92 bytes).
  pub(crate) const MSG_RESPONSE: u32 = 2;
  /// Message type 4: Transport Data.
  pub(crate) const MSG_TRANSPORT: u32 = 4;
  
  pub(crate) const INITIATION_SIZE: usize = 148;
  pub(crate) const RESPONSE_SIZE: usize = 92;
  
  // ── Transport session ─────────────────────────────────────────────────
  
  /// Derived transport keys from a completed handshake.
  pub(crate) struct TransportKeys {
  ```
- **encrypt_and_hash** in `rustguard-core/src/handshake.rs` (score: 0.782)
  Signature: fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
  Complexity: 1/5
  Calls: mix_hash
  Source:
  ```rust
  fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
      let ciphertext = crypto::seal(key, 0, h, plaintext);
      let new_h = mix_hash(h, &ciphertext);
      (new_h, ciphertext)
  }
  
  /// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
  fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
      let plaintext = crypto::open(key, 0, h, ciphertext)?;
      let new_h = mix_hash(h, ciphertext);
      Some((new_h, plaintext))
  }
  
  /// Compute MAC1 over a message using the responder's public key.
  /// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
  pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
      let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
      let full_mac = crypto::mac(&key, &[msg_bytes]);
      let mut mac1 = [0u8; 16];
      mac1.copy_from_slice(&full_mac[..16]);
      mac1
  }
  
  // ── Initiator side ──────────────────────────────────────────────────
  
  /// State held by the initiator between sending msg1 and receiving msg2.
  /// Zeroized on drop to prevent key material from lingering in memory.
  #[derive(Zeroize, ZeroizeOnDrop)]
  pub struct InitiatorHandshake {
      ck: [u8; 32],
      h: [u8; 32],
      ephemeral: EphemeralSecret,
      #[zeroize(skip)]
      sender_index: u32,
      #[zeroize(skip)]
      their_public: PublicKey,
      psk: [u8; 32],
  }
  
  /// Create a handshake initiation message (std convenience — uses OsRng and system clock).
  #[cfg(feature = "std")]
  pub fn create_initiation(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
  ) -> (Initiation, InitiatorHandshake) {
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  ```
- **encrypt_and_hash** in `rustguard-kmod/src/noise.rs` (score: 0.755)
  Signature: fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8], ct_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
  Complexity: 1/5
  Calls: seal, mix_hash
  Source:
  ```rust
  fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8], ct_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
      let ct_len = seal(key, 0, h, plaintext, ct_buf)?;
      let new_h = mix_hash(h, &ct_buf[..ct_len]);
      Some((new_h, ct_len))
  }
  
  /// Decrypt-and-hash: AEAD decrypt, mix ciphertext into hash.
  fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8], pt_buf: &mut [u8]) -> Option<([u8; 32], usize)> {
      let pt_len = open(key, 0, h, ciphertext, pt_buf)?;
      let new_h = mix_hash(h, ciphertext);
      Some((new_h, pt_len))
  }
  
  /// Compute MAC1 over a message.
  fn compute_mac1(responder_public: &[u8; 32], msg_bytes: &[u8]) -> [u8; 16] {
      let key = hash(&[LABEL_MAC1, responder_public]);
      let full = mac(&key, msg_bytes);
      let mut mac1 = [0u8; 16];
      mac1.copy_from_slice(&full[..16]);
      mac1
  }
  
  // ── TAI64N timestamp ──────────────────────────────────────────────────
  
  /// Generate a TAI64N timestamp from kernel wall clock.
  fn tai64n_now() -> [u8; 12] {
      extern "C" {
          fn wg_ktime_get_real(secs: *mut i64, nsecs: *mut i64);
      }
      let mut secs: i64 = 0;
      let mut nsecs: i64 = 0;
      unsafe { wg_ktime_get_real(&mut secs, &mut nsecs) };
  
      let tai_secs = (secs as u64) + TAI64_EPOCH_OFFSET;
      let nanos = nsecs as u32;
      let mut buf = [0u8; 12];
      buf[..8].copy_from_slice(&tai_secs.to_be_bytes());
      buf[8..].copy_from_slice(&nanos.to_be_bytes());
      buf
  }
  
  // ── Wire format constants ─────────────────────────────────────────────
  
  /// Message type 1: Handshake Initiation (148 bytes).
  pub(crate) const MSG_INITIATION: u32 = 1;
  /// Message type 2: Handshake Response (92 bytes).
  pub(crate) const MSG_RESPONSE: u32 = 2;
  /// Message type 4: Transport Data.
  pub(crate) const MSG_TRANSPORT: u32 = 4;
  
  ```

### compute_mac1 (L59–L65)
Similar functions found:
- **compute_mac1** in `rustguard-kmod/src/noise.rs` (score: 0.960)
  Signature: fn compute_mac1(responder_public: &[u8; 32], msg_bytes: &[u8]) -> [u8; 16] {
  Complexity: 1/5
  Calls: hash, mac
  Source:
  ```rust
  fn compute_mac1(responder_public: &[u8; 32], msg_bytes: &[u8]) -> [u8; 16] {
      let key = hash(&[LABEL_MAC1, responder_public]);
      let full = mac(&key, msg_bytes);
      let mut mac1 = [0u8; 16];
      mac1.copy_from_slice(&full[..16]);
      mac1
  }
  
  // ── TAI64N timestamp ──────────────────────────────────────────────────
  
  /// Generate a TAI64N timestamp from kernel wall clock.
  fn tai64n_now() -> [u8; 12] {
      extern "C" {
          fn wg_ktime_get_real(secs: *mut i64, nsecs: *mut i64);
      }
      let mut secs: i64 = 0;
      let mut nsecs: i64 = 0;
      unsafe { wg_ktime_get_real(&mut secs, &mut nsecs) };
  
      let tai_secs = (secs as u64) + TAI64_EPOCH_OFFSET;
      let nanos = nsecs as u32;
      let mut buf = [0u8; 12];
      buf[..8].copy_from_slice(&tai_secs.to_be_bytes());
      buf[8..].copy_from_slice(&nanos.to_be_bytes());
      buf
  }
  
  // ── Wire format constants ─────────────────────────────────────────────
  
  /// Message type 1: Handshake Initiation (148 bytes).
  pub(crate) const MSG_INITIATION: u32 = 1;
  /// Message type 2: Handshake Response (92 bytes).
  pub(crate) const MSG_RESPONSE: u32 = 2;
  /// Message type 4: Transport Data.
  pub(crate) const MSG_TRANSPORT: u32 = 4;
  
  pub(crate) const INITIATION_SIZE: usize = 148;
  pub(crate) const RESPONSE_SIZE: usize = 92;
  
  // ── Transport session ─────────────────────────────────────────────────
  
  /// Derived transport keys from a completed handshake.
  pub(crate) struct TransportKeys {
      /// Key for encrypting outgoing packets.
      pub(crate) key_send: [u8; 32],
      /// Key for decrypting incoming packets.
      pub(crate) key_recv: [u8; 32],
      /// Our sender index.
      pub(crate) our_index: u32,
      /// Their sender index.
  ```

### create_initiation (L85–L91)
Similar functions found:
- **create_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.886)
  Signature: pub fn create_initiation_psk( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: create_initiation_with
  Source:
  ```rust
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn create_initiation_psk(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
  ) -> (Initiation, InitiatorHandshake) {
      let ephemeral = EphemeralSecret::random();
      let timestamp = Tai64n::now();
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
      (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());
  
      // DH(our_static, responder_static) -> mix into ck.
      let dh2 = our_static.diffie_hellman(their_public);
      let key2;
  ```
- **create_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.870)
  Signature: pub(crate) fn create_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 2/5
  Calls: zeroize, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1, tai64n_now
  Source:
  ```rust
  pub(crate) fn create_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      their_public: &[u8; 32],
      sender_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; INITIATION_SIZE], InitiatorState)> {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key.
      h = mix_hash(&h, their_public);
  
      // Generate ephemeral keypair.
      let (mut eph_secret, eph_public) = generate_keypair();
  
      h = mix_hash(&h, &eph_public);
  
      // DH(ephemeral, responder_static) — C2: fail on zero result.
      let mut dh1 = dh(&eph_secret, their_public)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Encrypt our static public key.
      let mut encrypted_static = [0u8; 48]; // 32 + 16 tag
      (h, _) = encrypt_and_hash(&key, &h, our_static_public, &mut encrypted_static)?;
      zeroize(&mut key);
  
      // DH(our_static, responder_static) — C2: fail on zero result.
      let mut dh2 = dh(our_static_secret, their_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Encrypt timestamp.
      let timestamp = tai64n_now();
      let mut encrypted_timestamp = [0u8; 28]; // 12 + 16 tag
      (h, _) = encrypt_and_hash(&key2, &h, &timestamp, &mut encrypted_timestamp)?;
      zeroize(&mut key2);
  
      // Build wire message.
      let mut msg = [0u8; INITIATION_SIZE];
      msg[0..4].copy_from_slice(&MSG_INITIATION.to_le_bytes());
      msg[4..8].copy_from_slice(&sender_index.to_le_bytes());
      msg[8..40].copy_from_slice(&eph_public);
      msg[40..88].copy_from_slice(&encrypted_static);
      msg[88..116].copy_from_slice(&encrypted_timestamp);
      // mac1 over everything before mac1 field (bytes 0..116).
      let mac1 = compute_mac1(their_public, &msg[..116]);
  ```
- **create_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.864)
  Signature: pub fn create_initiation_with( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1
  Source:
  ```rust
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
      (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());
  
      // DH(our_static, responder_static) -> mix into ck.
      let dh2 = our_static.diffie_hellman(their_public);
      let key2;
      (ck, key2) = mix_key(&ck, &dh2);
  
      // Encrypt timestamp.
      let encrypted_timestamp;
      (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());
  
      // Build message (mac1/mac2 computed over wire bytes).
      let mut msg = Initiation {
          sender_index,
          ephemeral: *eph_public.as_bytes(),
          encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
          encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
          mac1: [0u8; 16],
  ```
- **process_initiation** in `rustguard-core/src/handshake.rs` (score: 0.738)
  Signature: pub fn process_initiation( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_psk
  Source:
  ```rust
  pub fn process_initiation(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  ```
- **process_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.712)
  Signature: pub(crate) fn process_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 3/5
  Calls: zeroize, constant_time_eq, hkdf, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1
  Source:
  ```rust
  pub(crate) fn process_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      msg: &[u8; INITIATION_SIZE],
      responder_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; 32], [u8; 12], [u8; RESPONSE_SIZE], TransportKeys)> {
      // Verify MAC1 first — cheap check before any DH.
      let expected_mac1 = compute_mac1(our_static_public, &msg[..116]);
      if !constant_time_eq(&msg[116..132], &expected_mac1) {
          return None;
      }
  
      let init_sender = u32::from_le_bytes(msg[4..8].try_into().ok()?);
      let init_eph: [u8; 32] = msg[8..40].try_into().ok()?;
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key.
      h = mix_hash(&h, our_static_public);
  
      // Mix initiator ephemeral.
      h = mix_hash(&h, &init_eph);
  
      // DH(our_static, initiator_ephemeral).
      let mut dh1 = dh(our_static_secret, &init_eph)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Decrypt initiator's static public key.
      let mut initiator_public = [0u8; 32];
      let (new_h, _) = decrypt_and_hash(&key, &h, &msg[40..88], &mut initiator_public)?;
      h = new_h;
      zeroize(&mut key);
  
      // DH(our_static, initiator_static).
      let mut dh2 = dh(our_static_secret, &initiator_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Decrypt timestamp — caller checks for replay (H3).
      let mut timestamp = [0u8; 12];
      let (new_h, _) = decrypt_and_hash(&key2, &h, &msg[88..116], &mut timestamp)?;
      h = new_h;
      zeroize(&mut key2);
  
      // ── Build response ──
  ```
- **process_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.701)
  Signature: pub fn process_initiation_psk( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_with
  Source:
  ```rust
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  ```

### create_initiation_psk (L95–L104)
Similar functions found:
- **create_initiation** in `rustguard-core/src/handshake.rs` (score: 0.886)
  Signature: pub fn create_initiation( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: create_initiation_psk
  Source:
  ```rust
  pub fn create_initiation(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
  ) -> (Initiation, InitiatorHandshake) {
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn create_initiation_psk(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
  ) -> (Initiation, InitiatorHandshake) {
      let ephemeral = EphemeralSecret::random();
      let timestamp = Tai64n::now();
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
  ```
- **create_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.837)
  Signature: pub fn create_initiation_with( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1
  Source:
  ```rust
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
      (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());
  
      // DH(our_static, responder_static) -> mix into ck.
      let dh2 = our_static.diffie_hellman(their_public);
      let key2;
      (ck, key2) = mix_key(&ck, &dh2);
  
      // Encrypt timestamp.
      let encrypted_timestamp;
      (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());
  
      // Build message (mac1/mac2 computed over wire bytes).
      let mut msg = Initiation {
          sender_index,
          ephemeral: *eph_public.as_bytes(),
          encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
          encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
          mac1: [0u8; 16],
  ```
- **process_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.831)
  Signature: pub fn process_initiation_psk( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_with
  Source:
  ```rust
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  ```
- **create_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.807)
  Signature: pub(crate) fn create_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 2/5
  Calls: zeroize, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1, tai64n_now
  Source:
  ```rust
  pub(crate) fn create_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      their_public: &[u8; 32],
      sender_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; INITIATION_SIZE], InitiatorState)> {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key.
      h = mix_hash(&h, their_public);
  
      // Generate ephemeral keypair.
      let (mut eph_secret, eph_public) = generate_keypair();
  
      h = mix_hash(&h, &eph_public);
  
      // DH(ephemeral, responder_static) — C2: fail on zero result.
      let mut dh1 = dh(&eph_secret, their_public)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Encrypt our static public key.
      let mut encrypted_static = [0u8; 48]; // 32 + 16 tag
      (h, _) = encrypt_and_hash(&key, &h, our_static_public, &mut encrypted_static)?;
      zeroize(&mut key);
  
      // DH(our_static, responder_static) — C2: fail on zero result.
      let mut dh2 = dh(our_static_secret, their_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Encrypt timestamp.
      let timestamp = tai64n_now();
      let mut encrypted_timestamp = [0u8; 28]; // 12 + 16 tag
      (h, _) = encrypt_and_hash(&key2, &h, &timestamp, &mut encrypted_timestamp)?;
      zeroize(&mut key2);
  
      // Build wire message.
      let mut msg = [0u8; INITIATION_SIZE];
      msg[0..4].copy_from_slice(&MSG_INITIATION.to_le_bytes());
      msg[4..8].copy_from_slice(&sender_index.to_le_bytes());
      msg[8..40].copy_from_slice(&eph_public);
      msg[40..88].copy_from_slice(&encrypted_static);
      msg[88..116].copy_from_slice(&encrypted_timestamp);
      // mac1 over everything before mac1 field (bytes 0..116).
      let mac1 = compute_mac1(their_public, &msg[..116]);
  ```
- **process_initiation** in `rustguard-core/src/handshake.rs` (score: 0.710)
  Signature: pub fn process_initiation( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_psk
  Source:
  ```rust
  pub fn process_initiation(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  ```

### create_initiation_with (L108–L170)
Similar functions found:
- **create_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.898)
  Signature: pub(crate) fn create_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 2/5
  Calls: zeroize, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1, tai64n_now
  Source:
  ```rust
  pub(crate) fn create_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      their_public: &[u8; 32],
      sender_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; INITIATION_SIZE], InitiatorState)> {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key.
      h = mix_hash(&h, their_public);
  
      // Generate ephemeral keypair.
      let (mut eph_secret, eph_public) = generate_keypair();
  
      h = mix_hash(&h, &eph_public);
  
      // DH(ephemeral, responder_static) — C2: fail on zero result.
      let mut dh1 = dh(&eph_secret, their_public)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Encrypt our static public key.
      let mut encrypted_static = [0u8; 48]; // 32 + 16 tag
      (h, _) = encrypt_and_hash(&key, &h, our_static_public, &mut encrypted_static)?;
      zeroize(&mut key);
  
      // DH(our_static, responder_static) — C2: fail on zero result.
      let mut dh2 = dh(our_static_secret, their_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Encrypt timestamp.
      let timestamp = tai64n_now();
      let mut encrypted_timestamp = [0u8; 28]; // 12 + 16 tag
      (h, _) = encrypt_and_hash(&key2, &h, &timestamp, &mut encrypted_timestamp)?;
      zeroize(&mut key2);
  
      // Build wire message.
      let mut msg = [0u8; INITIATION_SIZE];
      msg[0..4].copy_from_slice(&MSG_INITIATION.to_le_bytes());
      msg[4..8].copy_from_slice(&sender_index.to_le_bytes());
      msg[8..40].copy_from_slice(&eph_public);
      msg[40..88].copy_from_slice(&encrypted_static);
      msg[88..116].copy_from_slice(&encrypted_timestamp);
      // mac1 over everything before mac1 field (bytes 0..116).
      let mac1 = compute_mac1(their_public, &msg[..116]);
  ```
- **create_initiation** in `rustguard-core/src/handshake.rs` (score: 0.864)
  Signature: pub fn create_initiation( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: create_initiation_psk
  Source:
  ```rust
  pub fn create_initiation(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
  ) -> (Initiation, InitiatorHandshake) {
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn create_initiation_psk(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
  ) -> (Initiation, InitiatorHandshake) {
      let ephemeral = EphemeralSecret::random();
      let timestamp = Tai64n::now();
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
  ```
- **create_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.837)
  Signature: pub fn create_initiation_psk( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: create_initiation_with
  Source:
  ```rust
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn create_initiation_psk(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
  ) -> (Initiation, InitiatorHandshake) {
      let ephemeral = EphemeralSecret::random();
      let timestamp = Tai64n::now();
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
      (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());
  
      // DH(our_static, responder_static) -> mix into ck.
      let dh2 = our_static.diffie_hellman(their_public);
      let key2;
  ```
- **process_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.827)
  Signature: pub fn process_initiation_with( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 2/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, constant_time_eq
  Source:
  ```rust
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Decrypt initiator's static public key.
      let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
      h = new_h;
  
      let initiator_public = PublicKey::from_bytes(
          static_bytes
              .as_slice()
              .try_into()
              .ok()?,
      );
  
      // DH(our_static, initiator_static)
  ```
- **process_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.779)
  Signature: pub(crate) fn process_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 3/5
  Calls: zeroize, constant_time_eq, hkdf, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1
  Source:
  ```rust
  pub(crate) fn process_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      msg: &[u8; INITIATION_SIZE],
      responder_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; 32], [u8; 12], [u8; RESPONSE_SIZE], TransportKeys)> {
      // Verify MAC1 first — cheap check before any DH.
      let expected_mac1 = compute_mac1(our_static_public, &msg[..116]);
      if !constant_time_eq(&msg[116..132], &expected_mac1) {
          return None;
      }
  
      let init_sender = u32::from_le_bytes(msg[4..8].try_into().ok()?);
      let init_eph: [u8; 32] = msg[8..40].try_into().ok()?;
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key.
      h = mix_hash(&h, our_static_public);
  
      // Mix initiator ephemeral.
      h = mix_hash(&h, &init_eph);
  
      // DH(our_static, initiator_ephemeral).
      let mut dh1 = dh(our_static_secret, &init_eph)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Decrypt initiator's static public key.
      let mut initiator_public = [0u8; 32];
      let (new_h, _) = decrypt_and_hash(&key, &h, &msg[40..88], &mut initiator_public)?;
      h = new_h;
      zeroize(&mut key);
  
      // DH(our_static, initiator_static).
      let mut dh2 = dh(our_static_secret, &initiator_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Decrypt timestamp — caller checks for replay (H3).
      let mut timestamp = [0u8; 12];
      let (new_h, _) = decrypt_and_hash(&key2, &h, &msg[88..116], &mut timestamp)?;
      h = new_h;
      zeroize(&mut key2);
  
      // ── Build response ──
  ```
- **process_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.727)
  Signature: pub fn process_initiation_psk( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_with
  Source:
  ```rust
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  ```
- **process_initiation** in `rustguard-core/src/handshake.rs` (score: 0.712)
  Signature: pub fn process_initiation( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_psk
  Source:
  ```rust
  pub fn process_initiation(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  ```

### process_response (L173–L221)
Similar functions found:
- **process_response** in `rustguard-kmod/src/noise.rs` (score: 0.846)
  Signature: pub(crate) fn process_response( mut state: InitiatorState, our_static_secret: &[u8; 32],
  Complexity: 3/5
  Calls: zeroize, constant_time_eq, hkdf, dh, mix_hash, mix_key, decrypt_and_hash, compute_mac1
  Source:
  ```rust
  pub(crate) fn process_response(
      mut state: InitiatorState,
      our_static_secret: &[u8; 32],
      resp: &[u8; RESPONSE_SIZE],
  ) -> Option<TransportKeys> {
      // H6: Verify MAC1 before any DH operations.
      // The responder computed MAC1 as compute_mac1(&initiator_public, &resp[..60]).
      // We are the initiator — derive our public key from our secret to verify.
      let mut our_static_public = [0u8; 32];
      unsafe { wg_curve25519_generate_public(our_static_public.as_mut_ptr(), our_static_secret.as_ptr()) };
      let expected_mac1 = compute_mac1(&our_static_public, &resp[..60]);
      if !constant_time_eq(&resp[60..76], &expected_mac1) {
          zeroize(&mut state.ck);
          zeroize(&mut state.h);
          zeroize(&mut state.eph_secret);
          return None;
      }
  
      let mut ck = state.ck;
      let mut h = state.h;
  
      // Parse response.
      let resp_sender = u32::from_le_bytes(resp[4..8].try_into().ok()?);
      let resp_receiver = u32::from_le_bytes(resp[8..12].try_into().ok()?);
      if resp_receiver != state.sender_index {
          zeroize(&mut ck);
          zeroize(&mut h);
          zeroize(&mut state.eph_secret);
          return None;
      }
  
      let resp_eph: [u8; 32] = resp[12..44].try_into().ok()?;
      let encrypted_empty: &[u8] = &resp[44..60]; // 16 bytes (0 + 16 tag)
  
      // Mix responder ephemeral.
      h = mix_hash(&h, &resp_eph);
  
      // DH(our_ephemeral, responder_ephemeral).
      let mut dh1 = dh(&state.eph_secret, &resp_eph)?;
      (ck, _) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // DH(our_static, responder_ephemeral).
      let mut dh2 = dh(our_static_secret, &resp_eph)?;
      (ck, _) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // PSK phase.
      let (new_ck, mut t, key) = hkdf(&ck, &state.psk);
      ck = new_ck;
  ```
- **process_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.757)
  Signature: pub fn process_initiation_with( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 2/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, constant_time_eq
  Source:
  ```rust
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Decrypt initiator's static public key.
      let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
      h = new_h;
  
      let initiator_public = PublicKey::from_bytes(
          static_bytes
              .as_slice()
              .try_into()
              .ok()?,
      );
  
      // DH(our_static, initiator_static)
  ```
- **process_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.747)
  Signature: pub(crate) fn process_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 3/5
  Calls: zeroize, constant_time_eq, hkdf, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1
  Source:
  ```rust
  pub(crate) fn process_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      msg: &[u8; INITIATION_SIZE],
      responder_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; 32], [u8; 12], [u8; RESPONSE_SIZE], TransportKeys)> {
      // Verify MAC1 first — cheap check before any DH.
      let expected_mac1 = compute_mac1(our_static_public, &msg[..116]);
      if !constant_time_eq(&msg[116..132], &expected_mac1) {
          return None;
      }
  
      let init_sender = u32::from_le_bytes(msg[4..8].try_into().ok()?);
      let init_eph: [u8; 32] = msg[8..40].try_into().ok()?;
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key.
      h = mix_hash(&h, our_static_public);
  
      // Mix initiator ephemeral.
      h = mix_hash(&h, &init_eph);
  
      // DH(our_static, initiator_ephemeral).
      let mut dh1 = dh(our_static_secret, &init_eph)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Decrypt initiator's static public key.
      let mut initiator_public = [0u8; 32];
      let (new_h, _) = decrypt_and_hash(&key, &h, &msg[40..88], &mut initiator_public)?;
      h = new_h;
      zeroize(&mut key);
  
      // DH(our_static, initiator_static).
      let mut dh2 = dh(our_static_secret, &initiator_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Decrypt timestamp — caller checks for replay (H3).
      let mut timestamp = [0u8; 12];
      let (new_h, _) = decrypt_and_hash(&key2, &h, &msg[88..116], &mut timestamp)?;
      h = new_h;
      zeroize(&mut key2);
  
      // ── Build response ──
  ```
- **process_initiation** in `rustguard-core/src/handshake.rs` (score: 0.747)
  Signature: pub fn process_initiation( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_psk
  Source:
  ```rust
  pub fn process_initiation(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  ```
- **handle_response** in `rustguard-kmod/src/lib.rs` (score: 0.726)
  Signature: unsafe fn handle_response(state: *mut DeviceState, pkt: &[u8], pkt_len: usize) {
  Complexity: 1/5
  Source:
  ```rust
                  handle_response(state, pkt, pkt_len);
                  wg_kfree_skb(skb);
              }
              noise::MSG_TRANSPORT => {
                  handle_transport(state, skb, pkt, pkt_len);
                  // skb consumed by handle_transport
              }
              _ => {
                  wg_kfree_skb(skb);
              }
          }
  
          0
      }
  }
  
  /// Handle handshake initiation (type 1) — we are the responder.
  /// SAFETY: Called only from do_rx with socket lock held (single-threaded RX).
  unsafe fn handle_initiation(state: *mut DeviceState, pkt: &[u8], pkt_len: usize) {
      unsafe {
          if pkt_len < noise::INITIATION_SIZE { return; }
  
          let msg: &[u8; noise::INITIATION_SIZE] =
              pkt[..noise::INITIATION_SIZE].try_into().unwrap_or(&[0u8; noise::INITIATION_SIZE]);
  
          let mut idx_bytes = [0u8; 4];
          wg_get_random_bytes(idx_bytes.as_mut_ptr(), 4);
          let responder_index = u32::from_le_bytes(idx_bytes);
  
          let psk = (*state).peers[0].as_ref().map(|p| p.psk).unwrap_or([0u8; 32]);
  
          let result = noise::process_initiation(
              &(*state).static_secret,
              &(*state).static_public,
              msg,
              responder_index,
              &psk,
          );
  
          if let Some((initiator_public, timestamp, resp, keys)) = result {
              // H3: Timestamp replay protection — only accept strictly increasing timestamps.
              // TAI64N is big-endian encoded, so byte-wise comparison gives correct ordering.
              if let Some(ref peer) = (*state).peers[0] {
                  if peer.last_timestamp != [0u8; 12] && timestamp <= peer.last_timestamp {
                      pr_info!("rustguard: rejecting replayed handshake initiation\n");
                      return;
                  }
              }
  
              pr_info!("rustguard: handshake from {:02x}{:02x}{:02x}{:02x}...\n",
  ```
- **process_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.718)
  Signature: pub fn process_initiation_psk( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_with
  Source:
  ```rust
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  ```

### process_initiation (L227–L233)
Similar functions found:
- **process_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.855)
  Signature: pub fn process_initiation_with( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 2/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, constant_time_eq
  Source:
  ```rust
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Decrypt initiator's static public key.
      let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
      h = new_h;
  
      let initiator_public = PublicKey::from_bytes(
          static_bytes
              .as_slice()
              .try_into()
              .ok()?,
      );
  
      // DH(our_static, initiator_static)
  ```
- **process_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.855)
  Signature: pub fn process_initiation_psk( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_with
  Source:
  ```rust
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  ```
- **process_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.843)
  Signature: pub(crate) fn process_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 3/5
  Calls: zeroize, constant_time_eq, hkdf, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1
  Source:
  ```rust
  pub(crate) fn process_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      msg: &[u8; INITIATION_SIZE],
      responder_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; 32], [u8; 12], [u8; RESPONSE_SIZE], TransportKeys)> {
      // Verify MAC1 first — cheap check before any DH.
      let expected_mac1 = compute_mac1(our_static_public, &msg[..116]);
      if !constant_time_eq(&msg[116..132], &expected_mac1) {
          return None;
      }
  
      let init_sender = u32::from_le_bytes(msg[4..8].try_into().ok()?);
      let init_eph: [u8; 32] = msg[8..40].try_into().ok()?;
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key.
      h = mix_hash(&h, our_static_public);
  
      // Mix initiator ephemeral.
      h = mix_hash(&h, &init_eph);
  
      // DH(our_static, initiator_ephemeral).
      let mut dh1 = dh(our_static_secret, &init_eph)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Decrypt initiator's static public key.
      let mut initiator_public = [0u8; 32];
      let (new_h, _) = decrypt_and_hash(&key, &h, &msg[40..88], &mut initiator_public)?;
      h = new_h;
      zeroize(&mut key);
  
      // DH(our_static, initiator_static).
      let mut dh2 = dh(our_static_secret, &initiator_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Decrypt timestamp — caller checks for replay (H3).
      let mut timestamp = [0u8; 12];
      let (new_h, _) = decrypt_and_hash(&key2, &h, &msg[88..116], &mut timestamp)?;
      h = new_h;
      zeroize(&mut key2);
  
      // ── Build response ──
  ```
- **process_response** in `rustguard-core/src/handshake.rs` (score: 0.747)
  Signature: pub fn process_response( state: InitiatorHandshake, our_static: &StaticSecret,
  Complexity: 1/5
  Calls: mix_hash, mix_key, decrypt_and_hash
  Source:
  ```rust
  pub fn process_response(
      state: InitiatorHandshake,
      our_static: &StaticSecret,
      msg: &Response,
  ) -> Option<TransportSession> {
      // Access fields by reference — struct will zeroize on drop.
      let mut ck = state.ck;
      let mut h = state.h;
      let sender_index = state.sender_index;
      let psk = state.psk;
  
      // Verify receiver_index matches our sender_index.
      if msg.receiver_index != sender_index {
          return None;
      }
  
      let resp_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix responder ephemeral into hash.
      h = mix_hash(&h, resp_eph.as_ref());
  
      // DH(our_ephemeral, responder_ephemeral)
      let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
      (ck, _) = mix_key(&ck, &dh1);
  
      // DH(our_static, responder_ephemeral)
      let dh2 = our_static.diffie_hellman(&resp_eph);
      (ck, _) = mix_key(&ck, &dh2);
  
      // PSK phase (Noise_IKpsk2): mix PSK into chain.
      let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
      ck = new_ck;
      h = mix_hash(&h, &t);
  
      // Decrypt empty payload.
      let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
      let (h, _empty) = result?;
      let _ = h;
  
      // Derive transport keys: initiator sends with first, receives with second.
      let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);
  
      Some(TransportSession::new(
          sender_index,
          msg.sender_index,
          send_key,
          recv_key,
      ))
  }
  
  ```
- **create_initiation** in `rustguard-core/src/handshake.rs` (score: 0.738)
  Signature: pub fn create_initiation( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: create_initiation_psk
  Source:
  ```rust
  pub fn create_initiation(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
  ) -> (Initiation, InitiatorHandshake) {
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn create_initiation_psk(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
  ) -> (Initiation, InitiatorHandshake) {
      let ephemeral = EphemeralSecret::random();
      let timestamp = Tai64n::now();
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
  ```
- **handle_initiation** in `rustguard-kmod/src/lib.rs` (score: 0.728)
  Signature: unsafe fn handle_initiation(state: *mut DeviceState, pkt: &[u8], pkt_len: usize) {
  Complexity: 1/5
  Source:
  ```rust
                  handle_initiation(state, pkt, pkt_len);
                  wg_kfree_skb(skb);
              }
              noise::MSG_RESPONSE => {
                  handle_response(state, pkt, pkt_len);
                  wg_kfree_skb(skb);
              }
              noise::MSG_TRANSPORT => {
                  handle_transport(state, skb, pkt, pkt_len);
                  // skb consumed by handle_transport
              }
              _ => {
                  wg_kfree_skb(skb);
              }
          }
  
          0
      }
  }
  
  /// Handle handshake initiation (type 1) — we are the responder.
  /// SAFETY: Called only from do_rx with socket lock held (single-threaded RX).
  unsafe fn handle_initiation(state: *mut DeviceState, pkt: &[u8], pkt_len: usize) {
      unsafe {
          if pkt_len < noise::INITIATION_SIZE { return; }
  
          let msg: &[u8; noise::INITIATION_SIZE] =
              pkt[..noise::INITIATION_SIZE].try_into().unwrap_or(&[0u8; noise::INITIATION_SIZE]);
  
          let mut idx_bytes = [0u8; 4];
          wg_get_random_bytes(idx_bytes.as_mut_ptr(), 4);
          let responder_index = u32::from_le_bytes(idx_bytes);
  
          let psk = (*state).peers[0].as_ref().map(|p| p.psk).unwrap_or([0u8; 32]);
  
          let result = noise::process_initiation(
              &(*state).static_secret,
              &(*state).static_public,
              msg,
              responder_index,
              &psk,
          );
  
          if let Some((initiator_public, timestamp, resp, keys)) = result {
              // H3: Timestamp replay protection — only accept strictly increasing timestamps.
              // TAI64N is big-endian encoded, so byte-wise comparison gives correct ordering.
              if let Some(ref peer) = (*state).peers[0] {
                  if peer.last_timestamp != [0u8; 12] && timestamp <= peer.last_timestamp {
                      pr_info!("rustguard: rejecting replayed handshake initiation\n");
                      return;
  ```
- **create_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.715)
  Signature: pub(crate) fn create_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 2/5
  Calls: zeroize, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1, tai64n_now
  Source:
  ```rust
  pub(crate) fn create_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      their_public: &[u8; 32],
      sender_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; INITIATION_SIZE], InitiatorState)> {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key.
      h = mix_hash(&h, their_public);
  
      // Generate ephemeral keypair.
      let (mut eph_secret, eph_public) = generate_keypair();
  
      h = mix_hash(&h, &eph_public);
  
      // DH(ephemeral, responder_static) — C2: fail on zero result.
      let mut dh1 = dh(&eph_secret, their_public)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Encrypt our static public key.
      let mut encrypted_static = [0u8; 48]; // 32 + 16 tag
      (h, _) = encrypt_and_hash(&key, &h, our_static_public, &mut encrypted_static)?;
      zeroize(&mut key);
  
      // DH(our_static, responder_static) — C2: fail on zero result.
      let mut dh2 = dh(our_static_secret, their_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Encrypt timestamp.
      let timestamp = tai64n_now();
      let mut encrypted_timestamp = [0u8; 28]; // 12 + 16 tag
      (h, _) = encrypt_and_hash(&key2, &h, &timestamp, &mut encrypted_timestamp)?;
      zeroize(&mut key2);
  
      // Build wire message.
      let mut msg = [0u8; INITIATION_SIZE];
      msg[0..4].copy_from_slice(&MSG_INITIATION.to_le_bytes());
      msg[4..8].copy_from_slice(&sender_index.to_le_bytes());
      msg[8..40].copy_from_slice(&eph_public);
      msg[40..88].copy_from_slice(&encrypted_static);
      msg[88..116].copy_from_slice(&encrypted_timestamp);
      // mac1 over everything before mac1 field (bytes 0..116).
      let mac1 = compute_mac1(their_public, &msg[..116]);
  ```
- **create_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.712)
  Signature: pub fn create_initiation_with( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1
  Source:
  ```rust
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
      (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());
  
      // DH(our_static, responder_static) -> mix into ck.
      let dh2 = our_static.diffie_hellman(their_public);
      let key2;
      (ck, key2) = mix_key(&ck, &dh2);
  
      // Encrypt timestamp.
      let encrypted_timestamp;
      (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());
  
      // Build message (mac1/mac2 computed over wire bytes).
      let mut msg = Initiation {
          sender_index,
          ephemeral: *eph_public.as_bytes(),
          encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
          encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
          mac1: [0u8; 16],
  ```

### process_initiation_psk (L237–L246)
Similar functions found:
- **process_initiation** in `rustguard-core/src/handshake.rs` (score: 0.855)
  Signature: pub fn process_initiation( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_psk
  Source:
  ```rust
  pub fn process_initiation(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  ```
- **create_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.831)
  Signature: pub fn create_initiation_psk( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: create_initiation_with
  Source:
  ```rust
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn create_initiation_psk(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
  ) -> (Initiation, InitiatorHandshake) {
      let ephemeral = EphemeralSecret::random();
      let timestamp = Tai64n::now();
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
      (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());
  
      // DH(our_static, responder_static) -> mix into ck.
      let dh2 = our_static.diffie_hellman(their_public);
      let key2;
  ```
- **process_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.817)
  Signature: pub fn process_initiation_with( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 2/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, constant_time_eq
  Source:
  ```rust
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Decrypt initiator's static public key.
      let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
      h = new_h;
  
      let initiator_public = PublicKey::from_bytes(
          static_bytes
              .as_slice()
              .try_into()
              .ok()?,
      );
  
      // DH(our_static, initiator_static)
  ```
- **process_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.768)
  Signature: pub(crate) fn process_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 3/5
  Calls: zeroize, constant_time_eq, hkdf, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1
  Source:
  ```rust
  pub(crate) fn process_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      msg: &[u8; INITIATION_SIZE],
      responder_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; 32], [u8; 12], [u8; RESPONSE_SIZE], TransportKeys)> {
      // Verify MAC1 first — cheap check before any DH.
      let expected_mac1 = compute_mac1(our_static_public, &msg[..116]);
      if !constant_time_eq(&msg[116..132], &expected_mac1) {
          return None;
      }
  
      let init_sender = u32::from_le_bytes(msg[4..8].try_into().ok()?);
      let init_eph: [u8; 32] = msg[8..40].try_into().ok()?;
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key.
      h = mix_hash(&h, our_static_public);
  
      // Mix initiator ephemeral.
      h = mix_hash(&h, &init_eph);
  
      // DH(our_static, initiator_ephemeral).
      let mut dh1 = dh(our_static_secret, &init_eph)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Decrypt initiator's static public key.
      let mut initiator_public = [0u8; 32];
      let (new_h, _) = decrypt_and_hash(&key, &h, &msg[40..88], &mut initiator_public)?;
      h = new_h;
      zeroize(&mut key);
  
      // DH(our_static, initiator_static).
      let mut dh2 = dh(our_static_secret, &initiator_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Decrypt timestamp — caller checks for replay (H3).
      let mut timestamp = [0u8; 12];
      let (new_h, _) = decrypt_and_hash(&key2, &h, &msg[88..116], &mut timestamp)?;
      h = new_h;
      zeroize(&mut key2);
  
      // ── Build response ──
  ```
- **create_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.727)
  Signature: pub fn create_initiation_with( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1
  Source:
  ```rust
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
      (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());
  
      // DH(our_static, responder_static) -> mix into ck.
      let dh2 = our_static.diffie_hellman(their_public);
      let key2;
      (ck, key2) = mix_key(&ck, &dh2);
  
      // Encrypt timestamp.
      let encrypted_timestamp;
      (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());
  
      // Build message (mac1/mac2 computed over wire bytes).
      let mut msg = Initiation {
          sender_index,
          ephemeral: *eph_public.as_bytes(),
          encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
          encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
          mac1: [0u8; 16],
  ```
- **process_response** in `rustguard-core/src/handshake.rs` (score: 0.718)
  Signature: pub fn process_response( state: InitiatorHandshake, our_static: &StaticSecret,
  Complexity: 1/5
  Calls: mix_hash, mix_key, decrypt_and_hash
  Source:
  ```rust
  pub fn process_response(
      state: InitiatorHandshake,
      our_static: &StaticSecret,
      msg: &Response,
  ) -> Option<TransportSession> {
      // Access fields by reference — struct will zeroize on drop.
      let mut ck = state.ck;
      let mut h = state.h;
      let sender_index = state.sender_index;
      let psk = state.psk;
  
      // Verify receiver_index matches our sender_index.
      if msg.receiver_index != sender_index {
          return None;
      }
  
      let resp_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix responder ephemeral into hash.
      h = mix_hash(&h, resp_eph.as_ref());
  
      // DH(our_ephemeral, responder_ephemeral)
      let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
      (ck, _) = mix_key(&ck, &dh1);
  
      // DH(our_static, responder_ephemeral)
      let dh2 = our_static.diffie_hellman(&resp_eph);
      (ck, _) = mix_key(&ck, &dh2);
  
      // PSK phase (Noise_IKpsk2): mix PSK into chain.
      let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
      ck = new_ck;
      h = mix_hash(&h, &t);
  
      // Decrypt empty payload.
      let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
      let (h, _empty) = result?;
      let _ = h;
  
      // Derive transport keys: initiator sends with first, receives with second.
      let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);
  
      Some(TransportSession::new(
          sender_index,
          msg.sender_index,
          send_key,
          recv_key,
      ))
  }
  
  ```
- **create_initiation** in `rustguard-core/src/handshake.rs` (score: 0.701)
  Signature: pub fn create_initiation( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: create_initiation_psk
  Source:
  ```rust
  pub fn create_initiation(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
  ) -> (Initiation, InitiatorHandshake) {
      create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
  }
  
  /// Create a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn create_initiation_psk(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
  ) -> (Initiation, InitiatorHandshake) {
      let ephemeral = EphemeralSecret::random();
      let timestamp = Tai64n::now();
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
  ```

### process_initiation_with (L249–L357)
Similar functions found:
- **process_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.878)
  Signature: pub(crate) fn process_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 3/5
  Calls: zeroize, constant_time_eq, hkdf, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1
  Source:
  ```rust
  pub(crate) fn process_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      msg: &[u8; INITIATION_SIZE],
      responder_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; 32], [u8; 12], [u8; RESPONSE_SIZE], TransportKeys)> {
      // Verify MAC1 first — cheap check before any DH.
      let expected_mac1 = compute_mac1(our_static_public, &msg[..116]);
      if !constant_time_eq(&msg[116..132], &expected_mac1) {
          return None;
      }
  
      let init_sender = u32::from_le_bytes(msg[4..8].try_into().ok()?);
      let init_eph: [u8; 32] = msg[8..40].try_into().ok()?;
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key.
      h = mix_hash(&h, our_static_public);
  
      // Mix initiator ephemeral.
      h = mix_hash(&h, &init_eph);
  
      // DH(our_static, initiator_ephemeral).
      let mut dh1 = dh(our_static_secret, &init_eph)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Decrypt initiator's static public key.
      let mut initiator_public = [0u8; 32];
      let (new_h, _) = decrypt_and_hash(&key, &h, &msg[40..88], &mut initiator_public)?;
      h = new_h;
      zeroize(&mut key);
  
      // DH(our_static, initiator_static).
      let mut dh2 = dh(our_static_secret, &initiator_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Decrypt timestamp — caller checks for replay (H3).
      let mut timestamp = [0u8; 12];
      let (new_h, _) = decrypt_and_hash(&key2, &h, &msg[88..116], &mut timestamp)?;
      h = new_h;
      zeroize(&mut key2);
  
      // ── Build response ──
  ```
- **process_initiation** in `rustguard-core/src/handshake.rs` (score: 0.855)
  Signature: pub fn process_initiation( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_psk
  Source:
  ```rust
  pub fn process_initiation(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  ```
- **create_initiation_with** in `rustguard-core/src/handshake.rs` (score: 0.827)
  Signature: pub fn create_initiation_with( our_static: &StaticSecret, their_public: &PublicKey,
  Complexity: 1/5
  Calls: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1
  Source:
  ```rust
      create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
  }
  
  /// Create a handshake initiation with explicit ephemeral key and timestamp.
  /// This is the core function — usable from both std and no_std.
  pub fn create_initiation_with(
      our_static: &StaticSecret,
      their_public: &PublicKey,
      sender_index: u32,
      psk: &[u8; 32],
      ephemeral: EphemeralSecret,
      timestamp: Tai64n,
  ) -> (Initiation, InitiatorHandshake) {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key into hash — both sides know this.
      h = mix_hash(&h, their_public.as_ref());
  
      let eph_public = ephemeral.public_key();
  
      // Mix ephemeral public into hash.
      h = mix_hash(&h, eph_public.as_ref());
  
      // DH(ephemeral, responder_static) -> mix into ck.
      let dh1 = ephemeral.diffie_hellman(their_public);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  
      // Encrypt our static public key.
      let our_public = our_static.public_key();
      let encrypted_static;
      (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());
  
      // DH(our_static, responder_static) -> mix into ck.
      let dh2 = our_static.diffie_hellman(their_public);
      let key2;
      (ck, key2) = mix_key(&ck, &dh2);
  
      // Encrypt timestamp.
      let encrypted_timestamp;
      (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());
  
      // Build message (mac1/mac2 computed over wire bytes).
      let mut msg = Initiation {
          sender_index,
          ephemeral: *eph_public.as_bytes(),
          encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
          encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
          mac1: [0u8; 16],
  ```
- **process_initiation_psk** in `rustguard-core/src/handshake.rs` (score: 0.817)
  Signature: pub fn process_initiation_psk( our_static: &StaticSecret, msg: &Initiation,
  Complexity: 1/5
  Calls: process_initiation_with
  Source:
  ```rust
      process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
  }
  
  /// Process a handshake initiation with PSK (std convenience).
  #[cfg(feature = "std")]
  pub fn process_initiation_psk(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let resp_eph = EphemeralSecret::random();
      process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
  }
  
  /// Process a handshake initiation with explicit ephemeral (no_std compatible).
  pub fn process_initiation_with(
      our_static: &StaticSecret,
      msg: &Initiation,
      responder_index: u32,
      psk: &[u8; 32],
      last_timestamp: Option<&Tai64n>,
      resp_eph: EphemeralSecret,
  ) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
      let our_public = our_static.public_key();
  
      // ── Verify MAC1 FIRST — cheap check before any DH. ──
      let wire = msg.to_bytes();
      let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
      if !constant_time_eq(&msg.mac1, &expected_mac1) {
          return None;
      }
      // TODO: MAC2 / cookie validation under load.
  
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix our public key into hash (responder's identity).
      h = mix_hash(&h, our_public.as_ref());
  
      let init_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix initiator ephemeral into hash.
      h = mix_hash(&h, init_eph.as_ref());
  
      // DH(our_static, initiator_ephemeral)
      let dh1 = our_static.diffie_hellman(&init_eph);
      let key;
      (ck, key) = mix_key(&ck, &dh1);
  ```
- **process_response** in `rustguard-kmod/src/noise.rs` (score: 0.759)
  Signature: pub(crate) fn process_response( mut state: InitiatorState, our_static_secret: &[u8; 32],
  Complexity: 3/5
  Calls: zeroize, constant_time_eq, hkdf, dh, mix_hash, mix_key, decrypt_and_hash, compute_mac1
  Source:
  ```rust
  pub(crate) fn process_response(
      mut state: InitiatorState,
      our_static_secret: &[u8; 32],
      resp: &[u8; RESPONSE_SIZE],
  ) -> Option<TransportKeys> {
      // H6: Verify MAC1 before any DH operations.
      // The responder computed MAC1 as compute_mac1(&initiator_public, &resp[..60]).
      // We are the initiator — derive our public key from our secret to verify.
      let mut our_static_public = [0u8; 32];
      unsafe { wg_curve25519_generate_public(our_static_public.as_mut_ptr(), our_static_secret.as_ptr()) };
      let expected_mac1 = compute_mac1(&our_static_public, &resp[..60]);
      if !constant_time_eq(&resp[60..76], &expected_mac1) {
          zeroize(&mut state.ck);
          zeroize(&mut state.h);
          zeroize(&mut state.eph_secret);
          return None;
      }
  
      let mut ck = state.ck;
      let mut h = state.h;
  
      // Parse response.
      let resp_sender = u32::from_le_bytes(resp[4..8].try_into().ok()?);
      let resp_receiver = u32::from_le_bytes(resp[8..12].try_into().ok()?);
      if resp_receiver != state.sender_index {
          zeroize(&mut ck);
          zeroize(&mut h);
          zeroize(&mut state.eph_secret);
          return None;
      }
  
      let resp_eph: [u8; 32] = resp[12..44].try_into().ok()?;
      let encrypted_empty: &[u8] = &resp[44..60]; // 16 bytes (0 + 16 tag)
  
      // Mix responder ephemeral.
      h = mix_hash(&h, &resp_eph);
  
      // DH(our_ephemeral, responder_ephemeral).
      let mut dh1 = dh(&state.eph_secret, &resp_eph)?;
      (ck, _) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // DH(our_static, responder_ephemeral).
      let mut dh2 = dh(our_static_secret, &resp_eph)?;
      (ck, _) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // PSK phase.
      let (new_ck, mut t, key) = hkdf(&ck, &state.psk);
      ck = new_ck;
  ```
- **process_response** in `rustguard-core/src/handshake.rs` (score: 0.757)
  Signature: pub fn process_response( state: InitiatorHandshake, our_static: &StaticSecret,
  Complexity: 1/5
  Calls: mix_hash, mix_key, decrypt_and_hash
  Source:
  ```rust
  pub fn process_response(
      state: InitiatorHandshake,
      our_static: &StaticSecret,
      msg: &Response,
  ) -> Option<TransportSession> {
      // Access fields by reference — struct will zeroize on drop.
      let mut ck = state.ck;
      let mut h = state.h;
      let sender_index = state.sender_index;
      let psk = state.psk;
  
      // Verify receiver_index matches our sender_index.
      if msg.receiver_index != sender_index {
          return None;
      }
  
      let resp_eph = PublicKey::from_bytes(msg.ephemeral);
  
      // Mix responder ephemeral into hash.
      h = mix_hash(&h, resp_eph.as_ref());
  
      // DH(our_ephemeral, responder_ephemeral)
      let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
      (ck, _) = mix_key(&ck, &dh1);
  
      // DH(our_static, responder_ephemeral)
      let dh2 = our_static.diffie_hellman(&resp_eph);
      (ck, _) = mix_key(&ck, &dh2);
  
      // PSK phase (Noise_IKpsk2): mix PSK into chain.
      let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
      ck = new_ck;
      h = mix_hash(&h, &t);
  
      // Decrypt empty payload.
      let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
      let (h, _empty) = result?;
      let _ = h;
  
      // Derive transport keys: initiator sends with first, receives with second.
      let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);
  
      Some(TransportSession::new(
          sender_index,
          msg.sender_index,
          send_key,
          recv_key,
      ))
  }
  
  ```
- **create_initiation** in `rustguard-kmod/src/noise.rs` (score: 0.756)
  Signature: pub(crate) fn create_initiation( our_static_secret: &[u8; 32], our_static_public: &[u8; 32],
  Complexity: 2/5
  Calls: zeroize, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, compute_mac1, tai64n_now
  Source:
  ```rust
  pub(crate) fn create_initiation(
      our_static_secret: &[u8; 32],
      our_static_public: &[u8; 32],
      their_public: &[u8; 32],
      sender_index: u32,
      psk: &[u8; 32],
  ) -> Option<([u8; INITIATION_SIZE], InitiatorState)> {
      let mut ck = initial_chain_key();
      let mut h = initial_hash(&ck);
  
      // Mix responder's public key.
      h = mix_hash(&h, their_public);
  
      // Generate ephemeral keypair.
      let (mut eph_secret, eph_public) = generate_keypair();
  
      h = mix_hash(&h, &eph_public);
  
      // DH(ephemeral, responder_static) — C2: fail on zero result.
      let mut dh1 = dh(&eph_secret, their_public)?;
      let mut key;
      (ck, key) = mix_key(&ck, &dh1);
      zeroize(&mut dh1);
  
      // Encrypt our static public key.
      let mut encrypted_static = [0u8; 48]; // 32 + 16 tag
      (h, _) = encrypt_and_hash(&key, &h, our_static_public, &mut encrypted_static)?;
      zeroize(&mut key);
  
      // DH(our_static, responder_static) — C2: fail on zero result.
      let mut dh2 = dh(our_static_secret, their_public)?;
      let mut key2;
      (ck, key2) = mix_key(&ck, &dh2);
      zeroize(&mut dh2);
  
      // Encrypt timestamp.
      let timestamp = tai64n_now();
      let mut encrypted_timestamp = [0u8; 28]; // 12 + 16 tag
      (h, _) = encrypt_and_hash(&key2, &h, &timestamp, &mut encrypted_timestamp)?;
      zeroize(&mut key2);
  
      // Build wire message.
      let mut msg = [0u8; INITIATION_SIZE];
      msg[0..4].copy_from_slice(&MSG_INITIATION.to_le_bytes());
      msg[4..8].copy_from_slice(&sender_index.to_le_bytes());
      msg[8..40].copy_from_slice(&eph_public);
      msg[40..88].copy_from_slice(&encrypted_static);
      msg[88..116].copy_from_slice(&encrypted_timestamp);
      // mac1 over everything before mac1 field (bytes 0..116).
      let mac1 = compute_mac1(their_public, &msg[..116]);
  ```
- **handle_initiation** in `rustguard-kmod/src/lib.rs` (score: 0.751)
  Signature: unsafe fn handle_initiation(state: *mut DeviceState, pkt: &[u8], pkt_len: usize) {
  Complexity: 1/5
  Source:
  ```rust
                  handle_initiation(state, pkt, pkt_len);
                  wg_kfree_skb(skb);
              }
              noise::MSG_RESPONSE => {
                  handle_response(state, pkt, pkt_len);
                  wg_kfree_skb(skb);
              }
              noise::MSG_TRANSPORT => {
                  handle_transport(state, skb, pkt, pkt_len);
                  // skb consumed by handle_transport
              }
              _ => {
                  wg_kfree_skb(skb);
              }
          }
  
          0
      }
  }
  
  /// Handle handshake initiation (type 1) — we are the responder.
  /// SAFETY: Called only from do_rx with socket lock held (single-threaded RX).
  unsafe fn handle_initiation(state: *mut DeviceState, pkt: &[u8], pkt_len: usize) {
      unsafe {
          if pkt_len < noise::INITIATION_SIZE { return; }
  
          let msg: &[u8; noise::INITIATION_SIZE] =
              pkt[..noise::INITIATION_SIZE].try_into().unwrap_or(&[0u8; noise::INITIATION_SIZE]);
  
          let mut idx_bytes = [0u8; 4];
          wg_get_random_bytes(idx_bytes.as_mut_ptr(), 4);
          let responder_index = u32::from_le_bytes(idx_bytes);
  
          let psk = (*state).peers[0].as_ref().map(|p| p.psk).unwrap_or([0u8; 32]);
  
          let result = noise::process_initiation(
              &(*state).static_secret,
              &(*state).static_public,
              msg,
              responder_index,
              &psk,
          );
  
          if let Some((initiator_public, timestamp, resp, keys)) = result {
              // H3: Timestamp replay protection — only accept strictly increasing timestamps.
              // TAI64N is big-endian encoded, so byte-wise comparison gives correct ordering.
              if let Some(ref peer) = (*state).peers[0] {
                  if peer.last_timestamp != [0u8; 12] && timestamp <= peer.last_timestamp {
                      pr_info!("rustguard: rejecting replayed handshake initiation\n");
                      return;
  ```

### constant_time_eq (L360–L363)
Similar functions found:
- **constant_time_eq_16** in `rustguard-core/src/cookie.rs` (score: 0.854)
  Signature: fn constant_time_eq_16(a: &[u8; 16], b: &[u8]) -> bool {
  Complexity: 1/5
  Source:
  ```rust
          constant_time_eq_16(mac1, &expected[..16])
      }
  
      /// Verify MAC2 from pre-encoded address bytes.
      pub fn verify_mac2_from_bytes(
          &mut self,
          msg_with_mac1: &[u8],
          mac2: &[u8; 16],
          addr_bytes: &[u8],
      ) -> bool {
          let cookie = self.make_cookie_from_bytes(addr_bytes);
          let expected = crypto::mac(&cookie, &[msg_with_mac1]);
          constant_time_eq_16(mac2, &expected[..16])
      }
  
      /// Verify MAC2 on an incoming message (std convenience).
      #[cfg(feature = "std")]
      pub fn verify_mac2(
          &mut self,
          msg_with_mac1: &[u8],
          mac2: &[u8; 16],
          src: &std::net::SocketAddr,
      ) -> bool {
          let addr_bytes = encode_addr(src);
          self.verify_mac2_from_bytes(msg_with_mac1, mac2, &addr_bytes)
      }
  }
  
  impl CookieState {
      pub fn new() -> Self {
          Self {
              cookie: None,
              received: None,
          }
      }
  
      /// Process a Cookie Reply and store the decrypted cookie.
      ///
      /// `their_public`: the responder's public key.
      /// `mac1`: our MAC1 from the original message.
      pub fn process_reply(
          &mut self,
          reply: &CookieReply,
          their_public: &PublicKey,
          mac1: &[u8; 16],
      ) -> bool {
          let key = crypto::hash(&[LABEL_COOKIE, their_public.as_ref()]);
          match crypto::xopen(&key, &reply.nonce, mac1, &reply.encrypted_cookie) {
              Some(plaintext) if plaintext.len() == COOKIE_LEN => {
                  let mut cookie = [0u8; COOKIE_LEN];
  ```
- **constant_time_eq** in `rustguard-kmod/src/cookie.rs` (score: 0.853)
  Signature: fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
  Complexity: 1/5
  Source:
  ```rust
          constant_time_eq(&mac1[..16], &expected[..16])
      }
  
      /// Verify MAC2 from pre-encoded address bytes.
      pub(crate) fn verify_mac2(&mut self, msg_with_mac1: &[u8], mac2: &[u8], addr_bytes: &[u8]) -> bool {
          if mac2.len() < 16 { return false; }
          let cookie = self.make_cookie(addr_bytes);
          let expected = mac(&cookie, msg_with_mac1);
          constant_time_eq(&mac2[..16], &expected[..16])
      }
  
      /// Create a Cookie Reply message (type 3, 64 bytes).
      pub(crate) fn create_reply(
          &mut self, receiver_index: u32, mac1: &[u8; 16], addr_bytes: &[u8],
      ) -> [u8; 64] {
          let cookie = self.make_cookie(addr_bytes);
          let key = hash(&[LABEL_COOKIE, &self.our_public]);
          let nonce: [u8; 24] = random_bytes();
  
          let mut encrypted_cookie = [0u8; 32]; // 16 cookie + 16 tag
          unsafe {
              wg_xchacha20poly1305_encrypt(
                  key.as_ptr(), nonce.as_ptr(),
                  cookie.as_ptr(), COOKIE_LEN as u32,
                  mac1.as_ptr(), 16,
                  encrypted_cookie.as_mut_ptr(),
              );
          }
  
          let mut reply = [0u8; 64];
          reply[0..4].copy_from_slice(&3u32.to_le_bytes()); // MSG_COOKIE_REPLY
          reply[4..8].copy_from_slice(&receiver_index.to_le_bytes());
          reply[8..32].copy_from_slice(&nonce);
          reply[32..64].copy_from_slice(&encrypted_cookie);
          reply
      }
  }
  
  impl CookieState {
      pub(crate) fn new() -> Self {
          Self { cookie: None, received: 0 }
      }
  
      /// Process a Cookie Reply and store the decrypted cookie.
      pub(crate) fn process_reply(
          &mut self, reply: &[u8; 64], their_public: &[u8; 32], our_mac1: &[u8; 16],
      ) -> bool {
          let nonce: [u8; 24] = reply[8..32].try_into().unwrap_or([0; 24]);
          let encrypted = &reply[32..64];
  
  ```
- **constant_time_eq** in `rustguard-kmod/src/noise.rs` (score: 0.851)
  Signature: fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
  Complexity: 1/5
  Source:
  ```rust
  fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
      if a.len() != b.len() {
          return false;
      }
      unsafe { wg_crypto_memneq(a.as_ptr(), b.as_ptr(), a.len()) == 0 }
  }
  
  // ── WireGuard protocol constants ──────────────────────────────────────
  
  const CONSTRUCTION: &[u8] = b"Noise_IKpsk2_25519_ChaChaPoly_BLAKE2s";
  const IDENTIFIER: &[u8] = b"WireGuard v1 zx2c4 Jason@zx2c4.com";
  const LABEL_MAC1: &[u8] = b"mac1----";
  
  /// TAI64N epoch offset (2^62 + 10 leap seconds).
  const TAI64_EPOCH_OFFSET: u64 = 0x4000_0000_0000_000a;
  
  // ── Crypto helpers (calling C shim) ───────────────────────────────────
  
  /// BLAKE2s-256 hash of concatenated chunks.
  fn hash(chunks: &[&[u8]]) -> [u8; 32] {
      let ptrs: [*const u8; 8] = {
          let mut p = [core::ptr::null(); 8];
          for (i, c) in chunks.iter().enumerate().take(8) {
              p[i] = c.as_ptr();
          }
          p
      };
      let lens: [u32; 8] = {
          let mut l = [0u32; 8];
          for (i, c) in chunks.iter().enumerate().take(8) {
              l[i] = c.len() as u32;
          }
          l
      };
      let mut out = [0u8; 32];
      unsafe {
          wg_blake2s_hash(ptrs.as_ptr(), lens.as_ptr(), chunks.len() as u32, out.as_mut_ptr());
      }
      out
  }
  
  /// Keyed BLAKE2s MAC (for MAC1/MAC2).
  fn mac(key: &[u8], data: &[u8]) -> [u8; 32] {
      let mut out = [0u8; 32];
      unsafe {
          wg_blake2s_256_mac(
              key.as_ptr(), key.len() as u32,
              data.as_ptr(), data.len() as u32,
              out.as_mut_ptr(),
          );
  ```

Evaluate the duplication of each symbol and output the JSON.

## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "initial_chain_key",
      "line_start": 23,
      "line_end": 25,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Trivial 2-line utility function with no external duplicates found"
    },
    {
      "name": "initial_hash",
      "line_start": 27,
      "line_end": 29,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Trivial 2-line utility function with no external duplicates found"
    },
    {
      "name": "mix_hash",
      "line_start": 32,
      "line_end": 34,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Trivial 2-line utility function with no external duplicates found"
    },
    {
      "name": "mix_key",
      "line_start": 38,
      "line_end": 41,
      "duplication": "DUPLICATE",
      "confidence": 92,
      "detail": "Identical logic to mix_key in rustguard-kmod/src/noise.rs; both perform hkdf and return (new_ck, key) tuple. Parameter type differs (SharedSecret wrapper vs raw bytes) but semantic behavior is equivalent.",
      "duplicate_target": {
        "file": "rustguard-kmod/src/noise.rs",
        "symbol": "mix_key",
        "similarity": "96% identical logic — both call hkdf(ck, dh_result) and return the key pair, differing only in parameter wrapper types"
      }
    },
    {
      "name": "encrypt_and_hash",
      "line_start": 44,
      "line_end": 48,
      "duplication": "UNIQUE",
      "confidence": 72,
      "detail": "Similar structure to kmod version but different semantic contracts: returns owned Vec<u8> with immediate ciphertext vs Option<usize> with buffer-based output. Different memory models (allocation vs pre-allocated buffer) make them non-interchangeable despite 92% similarity score."
    },
    {
      "name": "decrypt_and_hash",
      "line_start": 51,
      "line_end": 55,
      "duplication": "UNIQUE",
      "confidence": 72,
      "detail": "Similar structure to kmod version but different semantic contracts: returns owned Vec<u8> plaintext vs Option<usize> with buffer-based output. Different memory models and return semantics make them non-interchangeable despite 92% similarity score."
    },
    {
      "name": "compute_mac1",
      "line_start": 59,
      "line_end": 65,
      "duplication": "DUPLICATE",
      "confidence": 91,
      "detail": "Identical logic to compute_mac1 in rustguard-kmod/src/noise.rs; both hash LABEL_MAC1||responder_public, compute MAC, and extract first 16 bytes. Parameter type differs (PublicKey wrapper vs raw [u8; 32]) but implementation is equivalent.",
      "duplicate_target": {
        "file": "rustguard-kmod/src/noise.rs",
        "symbol": "compute_mac1",
        "similarity": "95% identical implementation — both compute BLAKE2s MAC and slice first 16 bytes, differing only in parameter wrapper type"
      }
    },
    {
      "name": "InitiatorHandshake",
      "line_start": 72,
      "line_end": 81,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Struct definition with no duplicate struct definitions found in RAG results or codebase"
    },
    {
      "name": "create_initiation",
      "line_start": 85,
      "line_end": 91,
      "duplication": "UNIQUE",
      "confidence": 90,
      "detail": "Convenience wrapper that delegates to create_initiation_psk with default PSK parameter. Distinct function with clear caller-callee relationship rather than true duplication."
    },
    {
      "name": "create_initiation_psk",
      "line_start": 95,
      "line_end": 104,
      "duplication": "UNIQUE",
      "confidence": 90,
      "detail": "Convenience wrapper that generates ephemeral key and timestamp, then delegates to create_initiation_with. Distinct function serving specific purpose."
    },
    {
      "name": "create_initiation_with",
      "line_start": 108,
      "line_end": 170,
      "duplication": "UNIQUE",
      "confidence": 78,
      "detail": "Core handshake initiation logic similar to kmod version (0.898 score) but context-specific: uses PublicKey/StaticSecret wrapper types, struct-based message format, and different error handling. Non-interchangeable due to different abstraction levels."
    },
    {
      "name": "process_response",
      "line_start": 173,
      "line_end": 221,
      "duplication": "UNIQUE",
      "confidence": 76,
      "detail": "Similar protocol logic to kmod version (0.846 score) but different semantic contracts: takes InitiatorHandshake struct and Response type instead of raw state arrays. Different input/output abstractions make functions non-interchangeable."
    },
    {
      "name": "process_initiation",
      "line_start": 227,
      "line_end": 233,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Convenience wrapper that delegates to process_initiation_psk with default PSK and no last_timestamp. Distinct function with clear delegating relationship."
    },
    {
      "name": "process_initiation_psk",
      "line_start": 237,
      "line_end": 246,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "Convenience wrapper that generates random ephemeral key and delegates to process_initiation_with. Distinct function serving specific purpose."
    },
    {
      "name": "process_initiation_with",
      "line_start": 249,
      "line_end": 357,
      "duplication": "UNIQUE",
      "confidence": 77,
      "detail": "Core responder-side handshake logic similar to kmod version (0.878 score) but context-specific: uses PublicKey/StaticSecret wrappers, struct-based messages (Initiation/Response), and Option-based timestamp validation. Different abstraction levels and input/output handling make non-interchangeable."
    },
    {
      "name": "constant_time_eq",
      "line_start": 360,
      "line_end": 363,
      "duplication": "UNIQUE",
      "confidence": 85,
      "detail": "Uses subtle crate's ConstantTimeEq trait wrapper, while kmod version uses unsafe wg_crypto_memneq. Different implementation approaches (library vs raw unsafe code) despite similar purpose."
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.0992 | **Duration:** 49.2s


---

# Axis: correction

## User

## File: `rustguard-core/src/handshake.rs`
## Language: rust

```rust
//! WireGuard Noise_IKpsk2 handshake.
//!
//! The handshake is a 1-RTT key exchange:
//!   Initiator -> Responder: ephemeral, encrypted static, encrypted timestamp
//!   Responder -> Initiator: ephemeral, encrypted empty
//!
//! After this exchange both sides derive symmetric transport keys.
//! The chaining key (ck) and handshake hash (h) are threaded through
//! every operation — this is the Noise pattern in action.

use alloc::vec::Vec;
use rustguard_crypto::{
    self as crypto, EphemeralSecret, PublicKey, SharedSecret, StaticSecret, Tai64n,
    CONSTRUCTION, IDENTIFIER, LABEL_MAC1,
};
use zeroize::{Zeroize, ZeroizeOnDrop};

use crate::messages::{Initiation, Response};
use crate::session::TransportSession;

/// Precomputed initial handshake state from the protocol constants.
/// These are the same for every handshake — compute once.
fn initial_chain_key() -> [u8; 32] {
    crypto::hash(&[CONSTRUCTION])
}

fn initial_hash(ck: &[u8; 32]) -> [u8; 32] {
    crypto::hash(&[ck, IDENTIFIER])
}

/// Mix a public key into the hash. Used to bind the responder's identity.
fn mix_hash(h: &[u8; 32], data: &[u8]) -> [u8; 32] {
    crypto::hash(&[h, data])
}

/// Mix the result of a DH into the chaining key.
/// Returns (new_ck, key) where key is for AEAD encryption.
fn mix_key(ck: &[u8; 32], dh_result: &SharedSecret) -> ([u8; 32], [u8; 32]) {
    let (new_ck, key, _) = crypto::hkdf(ck, dh_result.as_bytes());
    (new_ck, key)
}

/// Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.
fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
    let ciphertext = crypto::seal(key, 0, h, plaintext);
    let new_h = mix_hash(h, &ciphertext);
    (new_h, ciphertext)
}

/// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
    let plaintext = crypto::open(key, 0, h, ciphertext)?;
    let new_h = mix_hash(h, ciphertext);
    Some((new_h, plaintext))
}

/// Compute MAC1 over a message using the responder's public key.
/// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
    let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
    let full_mac = crypto::mac(&key, &[msg_bytes]);
    let mut mac1 = [0u8; 16];
    mac1.copy_from_slice(&full_mac[..16]);
    mac1
}

// ── Initiator side ──────────────────────────────────────────────────

/// State held by the initiator between sending msg1 and receiving msg2.
/// Zeroized on drop to prevent key material from lingering in memory.
#[derive(Zeroize, ZeroizeOnDrop)]
pub struct InitiatorHandshake {
    ck: [u8; 32],
    h: [u8; 32],
    ephemeral: EphemeralSecret,
    #[zeroize(skip)]
    sender_index: u32,
    #[zeroize(skip)]
    their_public: PublicKey,
    psk: [u8; 32],
}

/// Create a handshake initiation message (std convenience — uses OsRng and system clock).
#[cfg(feature = "std")]
pub fn create_initiation(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
) -> (Initiation, InitiatorHandshake) {
    create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
}

/// Create a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn create_initiation_psk(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
) -> (Initiation, InitiatorHandshake) {
    let ephemeral = EphemeralSecret::random();
    let timestamp = Tai64n::now();
    create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
}

/// Create a handshake initiation with explicit ephemeral key and timestamp.
/// This is the core function — usable from both std and no_std.
pub fn create_initiation_with(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
    ephemeral: EphemeralSecret,
    timestamp: Tai64n,
) -> (Initiation, InitiatorHandshake) {
    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix responder's public key into hash — both sides know this.
    h = mix_hash(&h, their_public.as_ref());

    let eph_public = ephemeral.public_key();

    // Mix ephemeral public into hash.
    h = mix_hash(&h, eph_public.as_ref());

    // DH(ephemeral, responder_static) -> mix into ck.
    let dh1 = ephemeral.diffie_hellman(their_public);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Encrypt our static public key.
    let our_public = our_static.public_key();
    let encrypted_static;
    (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());

    // DH(our_static, responder_static) -> mix into ck.
    let dh2 = our_static.diffie_hellman(their_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Encrypt timestamp.
    let encrypted_timestamp;
    (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());

    // Build message (mac1/mac2 computed over wire bytes).
    let mut msg = Initiation {
        sender_index,
        ephemeral: *eph_public.as_bytes(),
        encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
        encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16], // No cookie mechanism yet.
    };

    // MAC1 is computed over everything before mac1 field.
    let wire = msg.to_bytes();
    msg.mac1 = compute_mac1(their_public, &wire[..116]);

    let state = InitiatorHandshake {
        ck,
        h,
        ephemeral,
        sender_index,
        their_public: their_public.clone(),
        psk: *psk,
    };

    (msg, state)
}

/// Process a handshake response and derive transport keys.
pub fn process_response(
    state: InitiatorHandshake,
    our_static: &StaticSecret,
    msg: &Response,
) -> Option<TransportSession> {
    // Access fields by reference — struct will zeroize on drop.
    let mut ck = state.ck;
    let mut h = state.h;
    let sender_index = state.sender_index;
    let psk = state.psk;

    // Verify receiver_index matches our sender_index.
    if msg.receiver_index != sender_index {
        return None;
    }

    let resp_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph.as_ref());

    // DH(our_ephemeral, responder_ephemeral)
    let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh1);

    // DH(our_static, responder_ephemeral)
    let dh2 = our_static.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh2);

    // PSK phase (Noise_IKpsk2): mix PSK into chain.
    let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Decrypt empty payload.
    let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
    let (h, _empty) = result?;
    let _ = h;

    // Derive transport keys: initiator sends with first, receives with second.
    let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);

    Some(TransportSession::new(
        sender_index,
        msg.sender_index,
        send_key,
        recv_key,
    ))
}

// ── Responder side ──────────────────────────────────────────────────

/// Process a handshake initiation on the responder side (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
}

/// Process a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation_psk(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let resp_eph = EphemeralSecret::random();
    process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
}

/// Process a handshake initiation with explicit ephemeral (no_std compatible).
pub fn process_initiation_with(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
    resp_eph: EphemeralSecret,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let our_public = our_static.public_key();

    // ── Verify MAC1 FIRST — cheap check before any DH. ──
    let wire = msg.to_bytes();
    let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
    if !constant_time_eq(&msg.mac1, &expected_mac1) {
        return None;
    }
    // TODO: MAC2 / cookie validation under load.

    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix our public key into hash (responder's identity).
    h = mix_hash(&h, our_public.as_ref());

    let init_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix initiator ephemeral into hash.
    h = mix_hash(&h, init_eph.as_ref());

    // DH(our_static, initiator_ephemeral)
    let dh1 = our_static.diffie_hellman(&init_eph);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Decrypt initiator's static public key.
    let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
    h = new_h;

    let initiator_public = PublicKey::from_bytes(
        static_bytes
            .as_slice()
            .try_into()
            .ok()?,
    );

    // DH(our_static, initiator_static)
    let dh2 = our_static.diffie_hellman(&initiator_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Decrypt timestamp.
    let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
    h = new_h;

    // Validate timestamp is newer than last seen — prevents replay.
    let timestamp = Tai64n::from_bytes(
        timestamp_bytes.as_slice().try_into().ok()?,
    );
    if let Some(last) = last_timestamp {
        if !timestamp.is_after(last) {
            return None; // Replayed or stale initiation.
        }
    }

    // ── Build response ──

    let resp_eph_public = resp_eph.public_key();

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph_public.as_ref());

    // DH(resp_ephemeral, initiator_ephemeral)
    let dh3 = resp_eph.diffie_hellman(&init_eph);
    (ck, _) = mix_key(&ck, &dh3);

    // DH(resp_ephemeral, initiator_static)
    let dh4 = resp_eph.diffie_hellman(&initiator_public);
    (ck, _) = mix_key(&ck, &dh4);

    // PSK phase.
    let (new_ck, t, key3) = crypto::hkdf(&ck, psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Encrypt empty.
    let encrypted_empty;
    (h, encrypted_empty) = encrypt_and_hash(&key3, &h, &[]);
    let _ = h; // final hash

    // Derive transport keys: responder sends with second, receives with first.
    let (recv_key, send_key, _) = crypto::hkdf(&ck, &[]);

    let mut resp = Response {
        sender_index: responder_index,
        receiver_index: msg.sender_index,
        ephemeral: *resp_eph_public.as_bytes(),
        encrypted_empty: encrypted_empty.try_into().expect("empty encrypted is 16 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16],
    };

    // MAC1 on response.
    let resp_wire = resp.to_bytes();
    resp.mac1 = compute_mac1(&initiator_public, &resp_wire[..60]);

    let session = TransportSession::new(responder_index, msg.sender_index, send_key, recv_key);

    Some((initiator_public, timestamp, resp, session))
}

/// Constant-time comparison for MACs using the `subtle` crate.
fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
    use subtle::ConstantTimeEq;
    a.ct_eq(b).into()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn full_handshake_and_transport() {
        // Setup: both sides have static keys, initiator knows responder's public key.
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        // Step 1: Initiator creates handshake initiation.
        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        // Step 2: Responder processes initiation, creates response.
        let (peer_pubkey, _ts, resp_msg, mut resp_session) =
            process_initiation(&responder_static, &init_msg, 2)
                .expect("responder should accept initiation");

        // Responder correctly identifies the initiator.
        assert_eq!(peer_pubkey, initiator_static.public_key());

        // Step 3: Initiator processes response, gets transport session.
        let mut init_session =
            process_response(init_state, &initiator_static, &resp_msg)
                .expect("initiator should accept response");

        // Step 4: Both sides can now exchange encrypted data.
        let plaintext = b"hello from the trenches";
        let (counter, ciphertext) = init_session.encrypt(plaintext).unwrap();
        let decrypted = resp_session
            .decrypt(counter, &ciphertext)
            .expect("responder should decrypt");
        assert_eq!(&decrypted, plaintext);

        // And the other direction.
        let reply = b"copy that, over";
        let (counter, ciphertext) = resp_session.encrypt(reply).unwrap();
        let decrypted = init_session
            .decrypt(counter, &ciphertext)
            .expect("initiator should decrypt");
        assert_eq!(&decrypted, reply);
    }

    #[test]
    fn wrong_responder_key_rejects() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let wrong_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // A different responder (wrong key) tries to process the initiation.
        let result = process_initiation(&wrong_static, &init_msg, 2);
        assert!(result.is_none(), "wrong responder key should fail");
    }

    #[test]
    fn tampered_initiation_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (mut init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // Tamper with the encrypted static field.
        init_msg.encrypted_static[0] ^= 0xff;

        let result = process_initiation(&responder_static, &init_msg, 2);
        assert!(result.is_none(), "tampered initiation should fail");
    }

    #[test]
    fn tampered_response_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Tamper with the encrypted empty field.
        resp_msg.encrypted_empty[0] ^= 0xff;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "tampered response should fail");
    }

    #[test]
    fn mismatched_receiver_index_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Change receiver_index to wrong value.
        resp_msg.receiver_index = 999;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "wrong receiver index should fail");
    }
}

```

## Symbols to evaluate

- function initial_chain_key (L23–L25)
- function initial_hash (L27–L29)
- function mix_hash (L32–L34)
- function mix_key (L38–L41)
- function encrypt_and_hash (L44–L48)
- function decrypt_and_hash (L51–L55)
- export function compute_mac1 (L59–L65)
- export class InitiatorHandshake (L72–L81)
- export function create_initiation (L85–L91)
- export function create_initiation_psk (L95–L104)
- export function create_initiation_with (L108–L170)
- export function process_response (L173–L221)
- export function process_initiation (L227–L233)
- export function process_initiation_psk (L237–L246)
- export function process_initiation_with (L249–L357)
- function constant_time_eq (L360–L363)

Identify any bugs, logic errors, or correctness issues and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "initial_chain_key",
      "line_start": 23,
      "line_end": 25,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly computes HASH(CONSTRUCTION) as the Noise protocol's initial chaining key. No issues found."
    },
    {
      "name": "initial_hash",
      "line_start": 27,
      "line_end": 29,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly computes HASH(ck || IDENTIFIER) for the initial handshake hash. No issues found."
    },
    {
      "name": "mix_hash",
      "line_start": 32,
      "line_end": 34,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correct Noise MixHash operation: h = HASH(h || data). No issues found."
    },
    {
      "name": "mix_key",
      "line_start": 38,
      "line_end": 41,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly implements HKDF2(ck, dh_result) returning (new_ck, key) and discarding the unused third output. Matches the Noise MixKey primitive."
    },
    {
      "name": "encrypt_and_hash",
      "line_start": 44,
      "line_end": 48,
      "correction": "OK",
      "confidence": 95,
      "detail": "AEAD seal with nonce 0 and h as AAD, followed by MixHash(ciphertext) is the correct Noise EncryptAndHash operation."
    },
    {
      "name": "decrypt_and_hash",
      "line_start": 51,
      "line_end": 55,
      "correction": "OK",
      "confidence": 95,
      "detail": "AEAD open with nonce 0 and h as AAD, followed by MixHash(ciphertext) on success is the correct Noise DecryptAndHash operation. Hash is updated with the ciphertext (not the plaintext), which is correct."
    },
    {
      "name": "compute_mac1",
      "line_start": 59,
      "line_end": 65,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly computes MAC1 = HMAC-BLAKE2s(HASH(LABEL_MAC1 || S_pub_r), msg)[:16] per WireGuard spec. Truncation to 16 bytes is correct."
    },
    {
      "name": "InitiatorHandshake",
      "line_start": 72,
      "line_end": 81,
      "correction": "OK",
      "confidence": 90,
      "detail": "Sensitive fields ck, h, ephemeral, and psk are correctly zeroized on drop. Non-sensitive sender_index and their_public are correctly skipped with #[zeroize(skip)]."
    },
    {
      "name": "create_initiation",
      "line_start": 85,
      "line_end": 91,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly delegates to create_initiation_psk with a zero PSK, matching WireGuard's no-PSK behavior."
    },
    {
      "name": "create_initiation_psk",
      "line_start": 95,
      "line_end": 104,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly generates a fresh random ephemeral secret and current TAI64n timestamp before delegating to the core function."
    },
    {
      "name": "create_initiation_with",
      "line_start": 108,
      "line_end": 170,
      "correction": "OK",
      "confidence": 90,
      "detail": "Correctly implements Noise_IKpsk2 initiation: MixHash(S_pub_r), MixHash(e_pub_i), MixKey(DH(e_i, S_r)), EncryptAndHash(S_pub_i), MixKey(DH(S_i, S_r)), EncryptAndHash(timestamp). MAC1 is computed over the first 116 wire bytes (1+3+4+32+48+28), which is correct for WireGuard's Initiation message layout."
    },
    {
      "name": "process_response",
      "line_start": 173,
      "line_end": 221,
      "correction": "NEEDS_FIX",
      "confidence": 92,
      "detail": "The function performs no MAC1 verification of the Response message before executing two expensive DH operations. The initiator's sender_index is transmitted in plaintext in the Initiation message, so an observer can craft a fake Response with the correct receiver_index, bypassing the only cheap early-exit check (line 183) and forcing full DH computation. compute_mac1(&our_static.public_key(), &wire[..60]) should be verified against msg.mac1 before any DH work, mirroring the responder's check in process_initiation_with."
    },
    {
      "name": "process_initiation",
      "line_start": 227,
      "line_end": 233,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly delegates to process_initiation_psk with a zero PSK and no prior timestamp, which is the correct WireGuard default behavior."
    },
    {
      "name": "process_initiation_psk",
      "line_start": 237,
      "line_end": 246,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly generates a fresh random responder ephemeral before delegating to the core function. No issues found."
    },
    {
      "name": "process_initiation_with",
      "line_start": 249,
      "line_end": 357,
      "correction": "OK",
      "confidence": 90,
      "detail": "Correctly implements the responder side: MAC1 verified first via constant_time_eq, then full Noise_IKpsk2 state evolution (DH(S_r,e_i), decrypt static, DH(S_r,S_i), decrypt timestamp, replay check), response construction (MixHash(e_r), DH(e_r,e_i), DH(e_r,S_i), PSK phase, EncryptAndHash(empty)), and transport key derivation with correct key ordering (recv=first, send=second)."
    },
    {
      "name": "constant_time_eq",
      "line_start": 360,
      "line_end": 363,
      "correction": "OK",
      "confidence": 98,
      "detail": "Correctly uses the subtle crate's ConstantTimeEq trait for timing-safe MAC comparison. ct_eq on fixed-size arrays and conversion via Into<bool> is the idiomatic correct usage."
    }
  ],
  "actions": [
    {
      "description": "Add MAC1 verification in process_response before any DH operations: compute expected_mac1 = compute_mac1(&our_static.public_key(), &msg.to_bytes()[..60]) and return None if it does not constant-time-equal msg.mac1. Without this check, an attacker who observed the plaintext sender_index in the Initiation can craft fake Response messages that survive the receiver_index guard and force two DH exponentiations per fake packet.",
      "severity": "MAJOR",
      "line": 183
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.8404 | **Duration:** 459.3s


---

# Axis: overengineering

## User

## File: `rustguard-core/src/handshake.rs`
## Language: rust

```rust
//! WireGuard Noise_IKpsk2 handshake.
//!
//! The handshake is a 1-RTT key exchange:
//!   Initiator -> Responder: ephemeral, encrypted static, encrypted timestamp
//!   Responder -> Initiator: ephemeral, encrypted empty
//!
//! After this exchange both sides derive symmetric transport keys.
//! The chaining key (ck) and handshake hash (h) are threaded through
//! every operation — this is the Noise pattern in action.

use alloc::vec::Vec;
use rustguard_crypto::{
    self as crypto, EphemeralSecret, PublicKey, SharedSecret, StaticSecret, Tai64n,
    CONSTRUCTION, IDENTIFIER, LABEL_MAC1,
};
use zeroize::{Zeroize, ZeroizeOnDrop};

use crate::messages::{Initiation, Response};
use crate::session::TransportSession;

/// Precomputed initial handshake state from the protocol constants.
/// These are the same for every handshake — compute once.
fn initial_chain_key() -> [u8; 32] {
    crypto::hash(&[CONSTRUCTION])
}

fn initial_hash(ck: &[u8; 32]) -> [u8; 32] {
    crypto::hash(&[ck, IDENTIFIER])
}

/// Mix a public key into the hash. Used to bind the responder's identity.
fn mix_hash(h: &[u8; 32], data: &[u8]) -> [u8; 32] {
    crypto::hash(&[h, data])
}

/// Mix the result of a DH into the chaining key.
/// Returns (new_ck, key) where key is for AEAD encryption.
fn mix_key(ck: &[u8; 32], dh_result: &SharedSecret) -> ([u8; 32], [u8; 32]) {
    let (new_ck, key, _) = crypto::hkdf(ck, dh_result.as_bytes());
    (new_ck, key)
}

/// Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.
fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
    let ciphertext = crypto::seal(key, 0, h, plaintext);
    let new_h = mix_hash(h, &ciphertext);
    (new_h, ciphertext)
}

/// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
    let plaintext = crypto::open(key, 0, h, ciphertext)?;
    let new_h = mix_hash(h, ciphertext);
    Some((new_h, plaintext))
}

/// Compute MAC1 over a message using the responder's public key.
/// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
    let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
    let full_mac = crypto::mac(&key, &[msg_bytes]);
    let mut mac1 = [0u8; 16];
    mac1.copy_from_slice(&full_mac[..16]);
    mac1
}

// ── Initiator side ──────────────────────────────────────────────────

/// State held by the initiator between sending msg1 and receiving msg2.
/// Zeroized on drop to prevent key material from lingering in memory.
#[derive(Zeroize, ZeroizeOnDrop)]
pub struct InitiatorHandshake {
    ck: [u8; 32],
    h: [u8; 32],
    ephemeral: EphemeralSecret,
    #[zeroize(skip)]
    sender_index: u32,
    #[zeroize(skip)]
    their_public: PublicKey,
    psk: [u8; 32],
}

/// Create a handshake initiation message (std convenience — uses OsRng and system clock).
#[cfg(feature = "std")]
pub fn create_initiation(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
) -> (Initiation, InitiatorHandshake) {
    create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
}

/// Create a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn create_initiation_psk(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
) -> (Initiation, InitiatorHandshake) {
    let ephemeral = EphemeralSecret::random();
    let timestamp = Tai64n::now();
    create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
}

/// Create a handshake initiation with explicit ephemeral key and timestamp.
/// This is the core function — usable from both std and no_std.
pub fn create_initiation_with(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
    ephemeral: EphemeralSecret,
    timestamp: Tai64n,
) -> (Initiation, InitiatorHandshake) {
    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix responder's public key into hash — both sides know this.
    h = mix_hash(&h, their_public.as_ref());

    let eph_public = ephemeral.public_key();

    // Mix ephemeral public into hash.
    h = mix_hash(&h, eph_public.as_ref());

    // DH(ephemeral, responder_static) -> mix into ck.
    let dh1 = ephemeral.diffie_hellman(their_public);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Encrypt our static public key.
    let our_public = our_static.public_key();
    let encrypted_static;
    (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());

    // DH(our_static, responder_static) -> mix into ck.
    let dh2 = our_static.diffie_hellman(their_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Encrypt timestamp.
    let encrypted_timestamp;
    (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());

    // Build message (mac1/mac2 computed over wire bytes).
    let mut msg = Initiation {
        sender_index,
        ephemeral: *eph_public.as_bytes(),
        encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
        encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16], // No cookie mechanism yet.
    };

    // MAC1 is computed over everything before mac1 field.
    let wire = msg.to_bytes();
    msg.mac1 = compute_mac1(their_public, &wire[..116]);

    let state = InitiatorHandshake {
        ck,
        h,
        ephemeral,
        sender_index,
        their_public: their_public.clone(),
        psk: *psk,
    };

    (msg, state)
}

/// Process a handshake response and derive transport keys.
pub fn process_response(
    state: InitiatorHandshake,
    our_static: &StaticSecret,
    msg: &Response,
) -> Option<TransportSession> {
    // Access fields by reference — struct will zeroize on drop.
    let mut ck = state.ck;
    let mut h = state.h;
    let sender_index = state.sender_index;
    let psk = state.psk;

    // Verify receiver_index matches our sender_index.
    if msg.receiver_index != sender_index {
        return None;
    }

    let resp_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph.as_ref());

    // DH(our_ephemeral, responder_ephemeral)
    let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh1);

    // DH(our_static, responder_ephemeral)
    let dh2 = our_static.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh2);

    // PSK phase (Noise_IKpsk2): mix PSK into chain.
    let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Decrypt empty payload.
    let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
    let (h, _empty) = result?;
    let _ = h;

    // Derive transport keys: initiator sends with first, receives with second.
    let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);

    Some(TransportSession::new(
        sender_index,
        msg.sender_index,
        send_key,
        recv_key,
    ))
}

// ── Responder side ──────────────────────────────────────────────────

/// Process a handshake initiation on the responder side (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
}

/// Process a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation_psk(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let resp_eph = EphemeralSecret::random();
    process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
}

/// Process a handshake initiation with explicit ephemeral (no_std compatible).
pub fn process_initiation_with(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
    resp_eph: EphemeralSecret,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let our_public = our_static.public_key();

    // ── Verify MAC1 FIRST — cheap check before any DH. ──
    let wire = msg.to_bytes();
    let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
    if !constant_time_eq(&msg.mac1, &expected_mac1) {
        return None;
    }
    // TODO: MAC2 / cookie validation under load.

    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix our public key into hash (responder's identity).
    h = mix_hash(&h, our_public.as_ref());

    let init_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix initiator ephemeral into hash.
    h = mix_hash(&h, init_eph.as_ref());

    // DH(our_static, initiator_ephemeral)
    let dh1 = our_static.diffie_hellman(&init_eph);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Decrypt initiator's static public key.
    let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
    h = new_h;

    let initiator_public = PublicKey::from_bytes(
        static_bytes
            .as_slice()
            .try_into()
            .ok()?,
    );

    // DH(our_static, initiator_static)
    let dh2 = our_static.diffie_hellman(&initiator_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Decrypt timestamp.
    let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
    h = new_h;

    // Validate timestamp is newer than last seen — prevents replay.
    let timestamp = Tai64n::from_bytes(
        timestamp_bytes.as_slice().try_into().ok()?,
    );
    if let Some(last) = last_timestamp {
        if !timestamp.is_after(last) {
            return None; // Replayed or stale initiation.
        }
    }

    // ── Build response ──

    let resp_eph_public = resp_eph.public_key();

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph_public.as_ref());

    // DH(resp_ephemeral, initiator_ephemeral)
    let dh3 = resp_eph.diffie_hellman(&init_eph);
    (ck, _) = mix_key(&ck, &dh3);

    // DH(resp_ephemeral, initiator_static)
    let dh4 = resp_eph.diffie_hellman(&initiator_public);
    (ck, _) = mix_key(&ck, &dh4);

    // PSK phase.
    let (new_ck, t, key3) = crypto::hkdf(&ck, psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Encrypt empty.
    let encrypted_empty;
    (h, encrypted_empty) = encrypt_and_hash(&key3, &h, &[]);
    let _ = h; // final hash

    // Derive transport keys: responder sends with second, receives with first.
    let (recv_key, send_key, _) = crypto::hkdf(&ck, &[]);

    let mut resp = Response {
        sender_index: responder_index,
        receiver_index: msg.sender_index,
        ephemeral: *resp_eph_public.as_bytes(),
        encrypted_empty: encrypted_empty.try_into().expect("empty encrypted is 16 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16],
    };

    // MAC1 on response.
    let resp_wire = resp.to_bytes();
    resp.mac1 = compute_mac1(&initiator_public, &resp_wire[..60]);

    let session = TransportSession::new(responder_index, msg.sender_index, send_key, recv_key);

    Some((initiator_public, timestamp, resp, session))
}

/// Constant-time comparison for MACs using the `subtle` crate.
fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
    use subtle::ConstantTimeEq;
    a.ct_eq(b).into()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn full_handshake_and_transport() {
        // Setup: both sides have static keys, initiator knows responder's public key.
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        // Step 1: Initiator creates handshake initiation.
        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        // Step 2: Responder processes initiation, creates response.
        let (peer_pubkey, _ts, resp_msg, mut resp_session) =
            process_initiation(&responder_static, &init_msg, 2)
                .expect("responder should accept initiation");

        // Responder correctly identifies the initiator.
        assert_eq!(peer_pubkey, initiator_static.public_key());

        // Step 3: Initiator processes response, gets transport session.
        let mut init_session =
            process_response(init_state, &initiator_static, &resp_msg)
                .expect("initiator should accept response");

        // Step 4: Both sides can now exchange encrypted data.
        let plaintext = b"hello from the trenches";
        let (counter, ciphertext) = init_session.encrypt(plaintext).unwrap();
        let decrypted = resp_session
            .decrypt(counter, &ciphertext)
            .expect("responder should decrypt");
        assert_eq!(&decrypted, plaintext);

        // And the other direction.
        let reply = b"copy that, over";
        let (counter, ciphertext) = resp_session.encrypt(reply).unwrap();
        let decrypted = init_session
            .decrypt(counter, &ciphertext)
            .expect("initiator should decrypt");
        assert_eq!(&decrypted, reply);
    }

    #[test]
    fn wrong_responder_key_rejects() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let wrong_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // A different responder (wrong key) tries to process the initiation.
        let result = process_initiation(&wrong_static, &init_msg, 2);
        assert!(result.is_none(), "wrong responder key should fail");
    }

    #[test]
    fn tampered_initiation_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (mut init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // Tamper with the encrypted static field.
        init_msg.encrypted_static[0] ^= 0xff;

        let result = process_initiation(&responder_static, &init_msg, 2);
        assert!(result.is_none(), "tampered initiation should fail");
    }

    #[test]
    fn tampered_response_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Tamper with the encrypted empty field.
        resp_msg.encrypted_empty[0] ^= 0xff;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "tampered response should fail");
    }

    #[test]
    fn mismatched_receiver_index_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Change receiver_index to wrong value.
        resp_msg.receiver_index = 999;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "wrong receiver index should fail");
    }
}

```

## Symbols to evaluate

- function initial_chain_key (L23–L25)
- function initial_hash (L27–L29)
- function mix_hash (L32–L34)
- function mix_key (L38–L41)
- function encrypt_and_hash (L44–L48)
- function decrypt_and_hash (L51–L55)
- export function compute_mac1 (L59–L65)
- export class InitiatorHandshake (L72–L81)
- export function create_initiation (L85–L91)
- export function create_initiation_psk (L95–L104)
- export function create_initiation_with (L108–L170)
- export function process_response (L173–L221)
- export function process_initiation (L227–L233)
- export function process_initiation_psk (L237–L246)
- export function process_initiation_with (L249–L357)
- function constant_time_eq (L360–L363)

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Use the project structure to detect excessive fragmentation:
- Directory with only 1 file → potential fragmentation
- More than 5 nesting levels → excessive structural complexity
- Factory/adapter directories with ≤ 2 files → likely over-engineering
These heuristics may increase a symbol's rating from LEAN to OVER.

Evaluate the complexity of each symbol and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "initial_chain_key",
      "line_start": 23,
      "line_end": 25,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Single-call wrapper that computes the Noise protocol constant exactly once. Naming it clearly communicates intent and isolates the magic constant."
    },
    {
      "name": "initial_hash",
      "line_start": 27,
      "line_end": 29,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "One-liner companion to initial_chain_key. The two helpers together mirror the Noise spec's initialisation step precisely."
    },
    {
      "name": "mix_hash",
      "line_start": 32,
      "line_end": 34,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Trivial but semantically important: naming this 'mix_hash' makes every call site read like the Noise protocol spec, which is the right trade-off in cryptographic code."
    },
    {
      "name": "mix_key",
      "line_start": 38,
      "line_end": 41,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Wraps HKDF to produce (ck, key). Discarding the third HKDF output explicitly via '_' is correct and intentional per WireGuard spec."
    },
    {
      "name": "encrypt_and_hash",
      "line_start": 44,
      "line_end": 48,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Direct encoding of the Noise 'EncryptAndHash' operation. Two steps, no hidden logic."
    },
    {
      "name": "decrypt_and_hash",
      "line_start": 51,
      "line_end": 55,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Symmetric counterpart to encrypt_and_hash. Returns Option to propagate AEAD tag failures, which is idiomatic Rust for fallible decryption."
    },
    {
      "name": "compute_mac1",
      "line_start": 59,
      "line_end": 65,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Faithfully implements the WireGuard MAC1 derivation. The temporary 'key' variable and slice copy are necessary since BLAKE2s-MAC produces 32 bytes and MAC1 is only 16."
    },
    {
      "name": "InitiatorHandshake",
      "line_start": 72,
      "line_end": 81,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Appropriately uses ZeroizeOnDrop for key material and zeroize(skip) for non-sensitive index/pubkey fields. Exactly the fields needed to bridge msg1 and msg2 — nothing extraneous."
    },
    {
      "name": "create_initiation",
      "line_start": 85,
      "line_end": 91,
      "overengineering": "LEAN",
      "confidence": 90,
      "detail": "Minimal std-only shim that forwards to the PSK variant with a zero PSK. Provides the common no-PSK use case without duplicating logic."
    },
    {
      "name": "create_initiation_psk",
      "line_start": 95,
      "line_end": 104,
      "overengineering": "ACCEPTABLE",
      "confidence": 85,
      "detail": "Middle tier in a three-layer delegation: plain → psk → with. The split between this and create_initiation_with is justified: this layer injects OS entropy and the system clock (std-only concerns), while the _with variant is deterministic for no_std and testing. Slight verbosity but the separation of concerns is sound for a crypto library."
    },
    {
      "name": "create_initiation_with",
      "line_start": 108,
      "line_end": 170,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Core no_std-compatible function. Length is proportional to the Noise_IKpsk2 spec: every statement maps to a named protocol step. No unnecessary abstraction."
    },
    {
      "name": "process_response",
      "line_start": 173,
      "line_end": 221,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Implements the initiator's response-processing path step-by-step per spec. Consuming InitiatorHandshake by value ensures the ephemeral is single-use and the struct is zeroized after use — idiomatic and correct."
    },
    {
      "name": "process_initiation",
      "line_start": 227,
      "line_end": 233,
      "overengineering": "LEAN",
      "confidence": 90,
      "detail": "Thin std convenience that delegates to the PSK variant with zero PSK and no timestamp check. Provides clean API for the common case."
    },
    {
      "name": "process_initiation_psk",
      "line_start": 237,
      "line_end": 246,
      "overengineering": "ACCEPTABLE",
      "confidence": 85,
      "detail": "Same three-tier delegation rationale as create_initiation_psk. This layer owns the std-specific random ephemeral generation, keeping process_initiation_with testable and no_std-compatible. Justified, if slightly verbose."
    },
    {
      "name": "process_initiation_with",
      "line_start": 249,
      "line_end": 357,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Long but not complex: each block maps directly to a Noise protocol step. MAC1 is checked first (cheap before DH), timestamp replay protection is inline. No gratuitous abstractions."
    },
    {
      "name": "constant_time_eq",
      "line_start": 360,
      "line_end": 363,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Correct use of the 'subtle' crate for constant-time MAC comparison. The thin wrapper gives a readable call site without hiding what it does."
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.1476 | **Duration:** 58.7s


---

# Axis: tests

## User

## File: `rustguard-core/src/handshake.rs`
## Language: rust

```rust
//! WireGuard Noise_IKpsk2 handshake.
//!
//! The handshake is a 1-RTT key exchange:
//!   Initiator -> Responder: ephemeral, encrypted static, encrypted timestamp
//!   Responder -> Initiator: ephemeral, encrypted empty
//!
//! After this exchange both sides derive symmetric transport keys.
//! The chaining key (ck) and handshake hash (h) are threaded through
//! every operation — this is the Noise pattern in action.

use alloc::vec::Vec;
use rustguard_crypto::{
    self as crypto, EphemeralSecret, PublicKey, SharedSecret, StaticSecret, Tai64n,
    CONSTRUCTION, IDENTIFIER, LABEL_MAC1,
};
use zeroize::{Zeroize, ZeroizeOnDrop};

use crate::messages::{Initiation, Response};
use crate::session::TransportSession;

/// Precomputed initial handshake state from the protocol constants.
/// These are the same for every handshake — compute once.
fn initial_chain_key() -> [u8; 32] {
    crypto::hash(&[CONSTRUCTION])
}

fn initial_hash(ck: &[u8; 32]) -> [u8; 32] {
    crypto::hash(&[ck, IDENTIFIER])
}

/// Mix a public key into the hash. Used to bind the responder's identity.
fn mix_hash(h: &[u8; 32], data: &[u8]) -> [u8; 32] {
    crypto::hash(&[h, data])
}

/// Mix the result of a DH into the chaining key.
/// Returns (new_ck, key) where key is for AEAD encryption.
fn mix_key(ck: &[u8; 32], dh_result: &SharedSecret) -> ([u8; 32], [u8; 32]) {
    let (new_ck, key, _) = crypto::hkdf(ck, dh_result.as_bytes());
    (new_ck, key)
}

/// Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.
fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
    let ciphertext = crypto::seal(key, 0, h, plaintext);
    let new_h = mix_hash(h, &ciphertext);
    (new_h, ciphertext)
}

/// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
    let plaintext = crypto::open(key, 0, h, ciphertext)?;
    let new_h = mix_hash(h, ciphertext);
    Some((new_h, plaintext))
}

/// Compute MAC1 over a message using the responder's public key.
/// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
    let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
    let full_mac = crypto::mac(&key, &[msg_bytes]);
    let mut mac1 = [0u8; 16];
    mac1.copy_from_slice(&full_mac[..16]);
    mac1
}

// ── Initiator side ──────────────────────────────────────────────────

/// State held by the initiator between sending msg1 and receiving msg2.
/// Zeroized on drop to prevent key material from lingering in memory.
#[derive(Zeroize, ZeroizeOnDrop)]
pub struct InitiatorHandshake {
    ck: [u8; 32],
    h: [u8; 32],
    ephemeral: EphemeralSecret,
    #[zeroize(skip)]
    sender_index: u32,
    #[zeroize(skip)]
    their_public: PublicKey,
    psk: [u8; 32],
}

/// Create a handshake initiation message (std convenience — uses OsRng and system clock).
#[cfg(feature = "std")]
pub fn create_initiation(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
) -> (Initiation, InitiatorHandshake) {
    create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
}

/// Create a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn create_initiation_psk(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
) -> (Initiation, InitiatorHandshake) {
    let ephemeral = EphemeralSecret::random();
    let timestamp = Tai64n::now();
    create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
}

/// Create a handshake initiation with explicit ephemeral key and timestamp.
/// This is the core function — usable from both std and no_std.
pub fn create_initiation_with(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
    ephemeral: EphemeralSecret,
    timestamp: Tai64n,
) -> (Initiation, InitiatorHandshake) {
    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix responder's public key into hash — both sides know this.
    h = mix_hash(&h, their_public.as_ref());

    let eph_public = ephemeral.public_key();

    // Mix ephemeral public into hash.
    h = mix_hash(&h, eph_public.as_ref());

    // DH(ephemeral, responder_static) -> mix into ck.
    let dh1 = ephemeral.diffie_hellman(their_public);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Encrypt our static public key.
    let our_public = our_static.public_key();
    let encrypted_static;
    (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());

    // DH(our_static, responder_static) -> mix into ck.
    let dh2 = our_static.diffie_hellman(their_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Encrypt timestamp.
    let encrypted_timestamp;
    (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());

    // Build message (mac1/mac2 computed over wire bytes).
    let mut msg = Initiation {
        sender_index,
        ephemeral: *eph_public.as_bytes(),
        encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
        encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16], // No cookie mechanism yet.
    };

    // MAC1 is computed over everything before mac1 field.
    let wire = msg.to_bytes();
    msg.mac1 = compute_mac1(their_public, &wire[..116]);

    let state = InitiatorHandshake {
        ck,
        h,
        ephemeral,
        sender_index,
        their_public: their_public.clone(),
        psk: *psk,
    };

    (msg, state)
}

/// Process a handshake response and derive transport keys.
pub fn process_response(
    state: InitiatorHandshake,
    our_static: &StaticSecret,
    msg: &Response,
) -> Option<TransportSession> {
    // Access fields by reference — struct will zeroize on drop.
    let mut ck = state.ck;
    let mut h = state.h;
    let sender_index = state.sender_index;
    let psk = state.psk;

    // Verify receiver_index matches our sender_index.
    if msg.receiver_index != sender_index {
        return None;
    }

    let resp_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph.as_ref());

    // DH(our_ephemeral, responder_ephemeral)
    let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh1);

    // DH(our_static, responder_ephemeral)
    let dh2 = our_static.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh2);

    // PSK phase (Noise_IKpsk2): mix PSK into chain.
    let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Decrypt empty payload.
    let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
    let (h, _empty) = result?;
    let _ = h;

    // Derive transport keys: initiator sends with first, receives with second.
    let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);

    Some(TransportSession::new(
        sender_index,
        msg.sender_index,
        send_key,
        recv_key,
    ))
}

// ── Responder side ──────────────────────────────────────────────────

/// Process a handshake initiation on the responder side (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
}

/// Process a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation_psk(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let resp_eph = EphemeralSecret::random();
    process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
}

/// Process a handshake initiation with explicit ephemeral (no_std compatible).
pub fn process_initiation_with(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
    resp_eph: EphemeralSecret,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let our_public = our_static.public_key();

    // ── Verify MAC1 FIRST — cheap check before any DH. ──
    let wire = msg.to_bytes();
    let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
    if !constant_time_eq(&msg.mac1, &expected_mac1) {
        return None;
    }
    // TODO: MAC2 / cookie validation under load.

    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix our public key into hash (responder's identity).
    h = mix_hash(&h, our_public.as_ref());

    let init_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix initiator ephemeral into hash.
    h = mix_hash(&h, init_eph.as_ref());

    // DH(our_static, initiator_ephemeral)
    let dh1 = our_static.diffie_hellman(&init_eph);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Decrypt initiator's static public key.
    let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
    h = new_h;

    let initiator_public = PublicKey::from_bytes(
        static_bytes
            .as_slice()
            .try_into()
            .ok()?,
    );

    // DH(our_static, initiator_static)
    let dh2 = our_static.diffie_hellman(&initiator_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Decrypt timestamp.
    let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
    h = new_h;

    // Validate timestamp is newer than last seen — prevents replay.
    let timestamp = Tai64n::from_bytes(
        timestamp_bytes.as_slice().try_into().ok()?,
    );
    if let Some(last) = last_timestamp {
        if !timestamp.is_after(last) {
            return None; // Replayed or stale initiation.
        }
    }

    // ── Build response ──

    let resp_eph_public = resp_eph.public_key();

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph_public.as_ref());

    // DH(resp_ephemeral, initiator_ephemeral)
    let dh3 = resp_eph.diffie_hellman(&init_eph);
    (ck, _) = mix_key(&ck, &dh3);

    // DH(resp_ephemeral, initiator_static)
    let dh4 = resp_eph.diffie_hellman(&initiator_public);
    (ck, _) = mix_key(&ck, &dh4);

    // PSK phase.
    let (new_ck, t, key3) = crypto::hkdf(&ck, psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Encrypt empty.
    let encrypted_empty;
    (h, encrypted_empty) = encrypt_and_hash(&key3, &h, &[]);
    let _ = h; // final hash

    // Derive transport keys: responder sends with second, receives with first.
    let (recv_key, send_key, _) = crypto::hkdf(&ck, &[]);

    let mut resp = Response {
        sender_index: responder_index,
        receiver_index: msg.sender_index,
        ephemeral: *resp_eph_public.as_bytes(),
        encrypted_empty: encrypted_empty.try_into().expect("empty encrypted is 16 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16],
    };

    // MAC1 on response.
    let resp_wire = resp.to_bytes();
    resp.mac1 = compute_mac1(&initiator_public, &resp_wire[..60]);

    let session = TransportSession::new(responder_index, msg.sender_index, send_key, recv_key);

    Some((initiator_public, timestamp, resp, session))
}

/// Constant-time comparison for MACs using the `subtle` crate.
fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
    use subtle::ConstantTimeEq;
    a.ct_eq(b).into()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn full_handshake_and_transport() {
        // Setup: both sides have static keys, initiator knows responder's public key.
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        // Step 1: Initiator creates handshake initiation.
        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        // Step 2: Responder processes initiation, creates response.
        let (peer_pubkey, _ts, resp_msg, mut resp_session) =
            process_initiation(&responder_static, &init_msg, 2)
                .expect("responder should accept initiation");

        // Responder correctly identifies the initiator.
        assert_eq!(peer_pubkey, initiator_static.public_key());

        // Step 3: Initiator processes response, gets transport session.
        let mut init_session =
            process_response(init_state, &initiator_static, &resp_msg)
                .expect("initiator should accept response");

        // Step 4: Both sides can now exchange encrypted data.
        let plaintext = b"hello from the trenches";
        let (counter, ciphertext) = init_session.encrypt(plaintext).unwrap();
        let decrypted = resp_session
            .decrypt(counter, &ciphertext)
            .expect("responder should decrypt");
        assert_eq!(&decrypted, plaintext);

        // And the other direction.
        let reply = b"copy that, over";
        let (counter, ciphertext) = resp_session.encrypt(reply).unwrap();
        let decrypted = init_session
            .decrypt(counter, &ciphertext)
            .expect("initiator should decrypt");
        assert_eq!(&decrypted, reply);
    }

    #[test]
    fn wrong_responder_key_rejects() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let wrong_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // A different responder (wrong key) tries to process the initiation.
        let result = process_initiation(&wrong_static, &init_msg, 2);
        assert!(result.is_none(), "wrong responder key should fail");
    }

    #[test]
    fn tampered_initiation_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (mut init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // Tamper with the encrypted static field.
        init_msg.encrypted_static[0] ^= 0xff;

        let result = process_initiation(&responder_static, &init_msg, 2);
        assert!(result.is_none(), "tampered initiation should fail");
    }

    #[test]
    fn tampered_response_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Tamper with the encrypted empty field.
        resp_msg.encrypted_empty[0] ^= 0xff;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "tampered response should fail");
    }

    #[test]
    fn mismatched_receiver_index_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Change receiver_index to wrong value.
        resp_msg.receiver_index = 999;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "wrong receiver index should fail");
    }
}

```

## Symbols to evaluate

- function initial_chain_key (L23–L25)
- function initial_hash (L27–L29)
- function mix_hash (L32–L34)
- function mix_key (L38–L41)
- function encrypt_and_hash (L44–L48)
- function decrypt_and_hash (L51–L55)
- export function compute_mac1 (L59–L65)
- export class InitiatorHandshake (L72–L81)
- export function create_initiation (L85–L91)
- export function create_initiation_psk (L95–L104)
- export function create_initiation_with (L108–L170)
- export function process_response (L173–L221)
- export function process_initiation (L227–L233)
- export function process_initiation_psk (L237–L246)
- export function process_initiation_with (L249–L357)
- function constant_time_eq (L360–L363)

## Test File

No test file found for this source file.

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Evaluate the test quality for each symbol and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "initial_chain_key",
      "line_start": 23,
      "line_end": 25,
      "tests": "WEAK",
      "confidence": 85,
      "detail": "Private helper exercised only indirectly through the full handshake test. No isolated unit test verifying the output against a known constant derived from CONSTRUCTION, and no test that catches a regression if the constant changes."
    },
    {
      "name": "initial_hash",
      "line_start": 27,
      "line_end": 29,
      "tests": "WEAK",
      "confidence": 85,
      "detail": "Private helper covered only transitively via full_handshake_and_transport. No direct test with known-good vectors for the CONSTRUCTION+IDENTIFIER hash, making silent regressions possible."
    },
    {
      "name": "mix_hash",
      "line_start": 32,
      "line_end": 34,
      "tests": "WEAK",
      "confidence": 85,
      "detail": "Called many times throughout the handshake but never tested in isolation. All coverage is implicit via higher-level tests; no edge-case input (empty slice, full 32-byte slice) is exercised directly."
    },
    {
      "name": "mix_key",
      "line_start": 38,
      "line_end": 41,
      "tests": "WEAK",
      "confidence": 85,
      "detail": "Indirect coverage only. The function discards the third HKDF output silently; no test verifies that the returned (ck, key) pair is derived correctly against a reference vector."
    },
    {
      "name": "encrypt_and_hash",
      "line_start": 44,
      "line_end": 48,
      "tests": "WEAK",
      "confidence": 85,
      "detail": "Covered transitively by tampered_initiation_rejected and tampered_response_rejected (which confirm AEAD authentication), but no unit test verifies ciphertext length, the hash update step, or encrypt-then-hash ordering in isolation."
    },
    {
      "name": "decrypt_and_hash",
      "line_start": 51,
      "line_end": 55,
      "tests": "WEAK",
      "confidence": 85,
      "detail": "Exercises the None path indirectly via tampered message tests, but no unit test confirms the hash is updated from the *ciphertext* (not plaintext) or checks behavior on zero-length input independently."
    },
    {
      "name": "compute_mac1",
      "line_start": 59,
      "line_end": 65,
      "tests": "WEAK",
      "confidence": 82,
      "detail": "MAC1 rejection is exercised by wrong_responder_key_rejects (MAC1 mismatch causes None) but no test directly checks the 16-byte output value against a known vector, nor are there tests for tampered mac1 bytes specifically."
    },
    {
      "name": "InitiatorHandshake",
      "line_start": 72,
      "line_end": 81,
      "tests": "WEAK",
      "confidence": 80,
      "detail": "The struct is instantiated and consumed in every test, confirming basic lifecycle. However, the ZeroizeOnDrop guarantee is never tested, and the psk field interaction is only tested with the all-zero default."
    },
    {
      "name": "create_initiation",
      "line_start": 85,
      "line_end": 91,
      "tests": "WEAK",
      "confidence": 88,
      "detail": "Used in all five inline tests covering happy path and several rejection scenarios. However it is only tested with the implicit zero PSK; no test distinguishes it from create_initiation_psk or verifies the OsRng / system-clock path specifically."
    },
    {
      "name": "create_initiation_psk",
      "line_start": 95,
      "line_end": 104,
      "tests": "WEAK",
      "confidence": 88,
      "detail": "Only reached through create_initiation (zero PSK). No test passes a non-zero PSK through this wrapper, so the psk parameter branch is untested. The core matching-PSK / mismatched-PSK scenarios are absent."
    },
    {
      "name": "create_initiation_with",
      "line_start": 108,
      "line_end": 170,
      "tests": "WEAK",
      "confidence": 85,
      "detail": "Core initiator logic exercised transitively by all handshake tests, covering the full happy path. Missing: test with non-zero PSK, test with a controlled deterministic ephemeral (no_std path), and no test verifying the mac1 slice boundary (wire[..116])."
    },
    {
      "name": "process_response",
      "line_start": 173,
      "line_end": 221,
      "tests": "WEAK",
      "confidence": 87,
      "detail": "Three tests cover the happy path, tampered encrypted_empty, and mismatched receiver_index. Missing: PSK mismatch (different PSK on each side), wrong static key for DH2, and verifying the initiator-sends/responder-receives key assignment."
    },
    {
      "name": "process_initiation",
      "line_start": 227,
      "line_end": 233,
      "tests": "WEAK",
      "confidence": 88,
      "detail": "Convenience wrapper tested in all five inline tests. Because it always passes zero PSK and no last_timestamp, the replay-protection branch and non-zero PSK paths inside process_initiation_with are never exercised through this entry point."
    },
    {
      "name": "process_initiation_psk",
      "line_start": 237,
      "line_end": 246,
      "tests": "WEAK",
      "confidence": 88,
      "detail": "Only called transitively with psk=[0u8;32] and last_timestamp=None. No test verifies PSK matching, PSK mismatch rejection, or the replay-protection branch activated by a non-None last_timestamp."
    },
    {
      "name": "process_initiation_with",
      "line_start": 249,
      "line_end": 357,
      "tests": "WEAK",
      "confidence": 90,
      "detail": "Covers MAC1 rejection (wrong key), AEAD failure (tampered encrypted_static), and happy path. Critically absent: timestamp replay protection (last_timestamp branch never tested), PSK mismatch scenario, tampered mac1 field, and tampered ephemeral or encrypted_timestamp fields."
    },
    {
      "name": "constant_time_eq",
      "line_start": 360,
      "line_end": 363,
      "tests": "WEAK",
      "confidence": 82,
      "detail": "Indirectly exercised by wrong_responder_key_rejects (MAC1 values differ). No direct unit test with equal arrays, one-bit-different arrays, or any assertion that the subtle crate's ct_eq is being used rather than a naive comparison."
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.1751 | **Duration:** 73.4s


---

# Axis: best_practices

## User

## File: `rustguard-core/src/handshake.rs`
## Language: rust
## Context: general

```rust
//! WireGuard Noise_IKpsk2 handshake.
//!
//! The handshake is a 1-RTT key exchange:
//!   Initiator -> Responder: ephemeral, encrypted static, encrypted timestamp
//!   Responder -> Initiator: ephemeral, encrypted empty
//!
//! After this exchange both sides derive symmetric transport keys.
//! The chaining key (ck) and handshake hash (h) are threaded through
//! every operation — this is the Noise pattern in action.

use alloc::vec::Vec;
use rustguard_crypto::{
    self as crypto, EphemeralSecret, PublicKey, SharedSecret, StaticSecret, Tai64n,
    CONSTRUCTION, IDENTIFIER, LABEL_MAC1,
};
use zeroize::{Zeroize, ZeroizeOnDrop};

use crate::messages::{Initiation, Response};
use crate::session::TransportSession;

/// Precomputed initial handshake state from the protocol constants.
/// These are the same for every handshake — compute once.
fn initial_chain_key() -> [u8; 32] {
    crypto::hash(&[CONSTRUCTION])
}

fn initial_hash(ck: &[u8; 32]) -> [u8; 32] {
    crypto::hash(&[ck, IDENTIFIER])
}

/// Mix a public key into the hash. Used to bind the responder's identity.
fn mix_hash(h: &[u8; 32], data: &[u8]) -> [u8; 32] {
    crypto::hash(&[h, data])
}

/// Mix the result of a DH into the chaining key.
/// Returns (new_ck, key) where key is for AEAD encryption.
fn mix_key(ck: &[u8; 32], dh_result: &SharedSecret) -> ([u8; 32], [u8; 32]) {
    let (new_ck, key, _) = crypto::hkdf(ck, dh_result.as_bytes());
    (new_ck, key)
}

/// Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.
fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
    let ciphertext = crypto::seal(key, 0, h, plaintext);
    let new_h = mix_hash(h, &ciphertext);
    (new_h, ciphertext)
}

/// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
    let plaintext = crypto::open(key, 0, h, ciphertext)?;
    let new_h = mix_hash(h, ciphertext);
    Some((new_h, plaintext))
}

/// Compute MAC1 over a message using the responder's public key.
/// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
    let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
    let full_mac = crypto::mac(&key, &[msg_bytes]);
    let mut mac1 = [0u8; 16];
    mac1.copy_from_slice(&full_mac[..16]);
    mac1
}

// ── Initiator side ──────────────────────────────────────────────────

/// State held by the initiator between sending msg1 and receiving msg2.
/// Zeroized on drop to prevent key material from lingering in memory.
#[derive(Zeroize, ZeroizeOnDrop)]
pub struct InitiatorHandshake {
    ck: [u8; 32],
    h: [u8; 32],
    ephemeral: EphemeralSecret,
    #[zeroize(skip)]
    sender_index: u32,
    #[zeroize(skip)]
    their_public: PublicKey,
    psk: [u8; 32],
}

/// Create a handshake initiation message (std convenience — uses OsRng and system clock).
#[cfg(feature = "std")]
pub fn create_initiation(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
) -> (Initiation, InitiatorHandshake) {
    create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
}

/// Create a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn create_initiation_psk(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
) -> (Initiation, InitiatorHandshake) {
    let ephemeral = EphemeralSecret::random();
    let timestamp = Tai64n::now();
    create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
}

/// Create a handshake initiation with explicit ephemeral key and timestamp.
/// This is the core function — usable from both std and no_std.
pub fn create_initiation_with(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
    ephemeral: EphemeralSecret,
    timestamp: Tai64n,
) -> (Initiation, InitiatorHandshake) {
    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix responder's public key into hash — both sides know this.
    h = mix_hash(&h, their_public.as_ref());

    let eph_public = ephemeral.public_key();

    // Mix ephemeral public into hash.
    h = mix_hash(&h, eph_public.as_ref());

    // DH(ephemeral, responder_static) -> mix into ck.
    let dh1 = ephemeral.diffie_hellman(their_public);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Encrypt our static public key.
    let our_public = our_static.public_key();
    let encrypted_static;
    (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());

    // DH(our_static, responder_static) -> mix into ck.
    let dh2 = our_static.diffie_hellman(their_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Encrypt timestamp.
    let encrypted_timestamp;
    (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());

    // Build message (mac1/mac2 computed over wire bytes).
    let mut msg = Initiation {
        sender_index,
        ephemeral: *eph_public.as_bytes(),
        encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
        encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16], // No cookie mechanism yet.
    };

    // MAC1 is computed over everything before mac1 field.
    let wire = msg.to_bytes();
    msg.mac1 = compute_mac1(their_public, &wire[..116]);

    let state = InitiatorHandshake {
        ck,
        h,
        ephemeral,
        sender_index,
        their_public: their_public.clone(),
        psk: *psk,
    };

    (msg, state)
}

/// Process a handshake response and derive transport keys.
pub fn process_response(
    state: InitiatorHandshake,
    our_static: &StaticSecret,
    msg: &Response,
) -> Option<TransportSession> {
    // Access fields by reference — struct will zeroize on drop.
    let mut ck = state.ck;
    let mut h = state.h;
    let sender_index = state.sender_index;
    let psk = state.psk;

    // Verify receiver_index matches our sender_index.
    if msg.receiver_index != sender_index {
        return None;
    }

    let resp_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph.as_ref());

    // DH(our_ephemeral, responder_ephemeral)
    let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh1);

    // DH(our_static, responder_ephemeral)
    let dh2 = our_static.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh2);

    // PSK phase (Noise_IKpsk2): mix PSK into chain.
    let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Decrypt empty payload.
    let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
    let (h, _empty) = result?;
    let _ = h;

    // Derive transport keys: initiator sends with first, receives with second.
    let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);

    Some(TransportSession::new(
        sender_index,
        msg.sender_index,
        send_key,
        recv_key,
    ))
}

// ── Responder side ──────────────────────────────────────────────────

/// Process a handshake initiation on the responder side (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
}

/// Process a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation_psk(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let resp_eph = EphemeralSecret::random();
    process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
}

/// Process a handshake initiation with explicit ephemeral (no_std compatible).
pub fn process_initiation_with(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
    resp_eph: EphemeralSecret,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let our_public = our_static.public_key();

    // ── Verify MAC1 FIRST — cheap check before any DH. ──
    let wire = msg.to_bytes();
    let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
    if !constant_time_eq(&msg.mac1, &expected_mac1) {
        return None;
    }
    // TODO: MAC2 / cookie validation under load.

    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix our public key into hash (responder's identity).
    h = mix_hash(&h, our_public.as_ref());

    let init_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix initiator ephemeral into hash.
    h = mix_hash(&h, init_eph.as_ref());

    // DH(our_static, initiator_ephemeral)
    let dh1 = our_static.diffie_hellman(&init_eph);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Decrypt initiator's static public key.
    let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
    h = new_h;

    let initiator_public = PublicKey::from_bytes(
        static_bytes
            .as_slice()
            .try_into()
            .ok()?,
    );

    // DH(our_static, initiator_static)
    let dh2 = our_static.diffie_hellman(&initiator_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Decrypt timestamp.
    let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
    h = new_h;

    // Validate timestamp is newer than last seen — prevents replay.
    let timestamp = Tai64n::from_bytes(
        timestamp_bytes.as_slice().try_into().ok()?,
    );
    if let Some(last) = last_timestamp {
        if !timestamp.is_after(last) {
            return None; // Replayed or stale initiation.
        }
    }

    // ── Build response ──

    let resp_eph_public = resp_eph.public_key();

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph_public.as_ref());

    // DH(resp_ephemeral, initiator_ephemeral)
    let dh3 = resp_eph.diffie_hellman(&init_eph);
    (ck, _) = mix_key(&ck, &dh3);

    // DH(resp_ephemeral, initiator_static)
    let dh4 = resp_eph.diffie_hellman(&initiator_public);
    (ck, _) = mix_key(&ck, &dh4);

    // PSK phase.
    let (new_ck, t, key3) = crypto::hkdf(&ck, psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Encrypt empty.
    let encrypted_empty;
    (h, encrypted_empty) = encrypt_and_hash(&key3, &h, &[]);
    let _ = h; // final hash

    // Derive transport keys: responder sends with second, receives with first.
    let (recv_key, send_key, _) = crypto::hkdf(&ck, &[]);

    let mut resp = Response {
        sender_index: responder_index,
        receiver_index: msg.sender_index,
        ephemeral: *resp_eph_public.as_bytes(),
        encrypted_empty: encrypted_empty.try_into().expect("empty encrypted is 16 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16],
    };

    // MAC1 on response.
    let resp_wire = resp.to_bytes();
    resp.mac1 = compute_mac1(&initiator_public, &resp_wire[..60]);

    let session = TransportSession::new(responder_index, msg.sender_index, send_key, recv_key);

    Some((initiator_public, timestamp, resp, session))
}

/// Constant-time comparison for MACs using the `subtle` crate.
fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
    use subtle::ConstantTimeEq;
    a.ct_eq(b).into()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn full_handshake_and_transport() {
        // Setup: both sides have static keys, initiator knows responder's public key.
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        // Step 1: Initiator creates handshake initiation.
        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        // Step 2: Responder processes initiation, creates response.
        let (peer_pubkey, _ts, resp_msg, mut resp_session) =
            process_initiation(&responder_static, &init_msg, 2)
                .expect("responder should accept initiation");

        // Responder correctly identifies the initiator.
        assert_eq!(peer_pubkey, initiator_static.public_key());

        // Step 3: Initiator processes response, gets transport session.
        let mut init_session =
            process_response(init_state, &initiator_static, &resp_msg)
                .expect("initiator should accept response");

        // Step 4: Both sides can now exchange encrypted data.
        let plaintext = b"hello from the trenches";
        let (counter, ciphertext) = init_session.encrypt(plaintext).unwrap();
        let decrypted = resp_session
            .decrypt(counter, &ciphertext)
            .expect("responder should decrypt");
        assert_eq!(&decrypted, plaintext);

        // And the other direction.
        let reply = b"copy that, over";
        let (counter, ciphertext) = resp_session.encrypt(reply).unwrap();
        let decrypted = init_session
            .decrypt(counter, &ciphertext)
            .expect("initiator should decrypt");
        assert_eq!(&decrypted, reply);
    }

    #[test]
    fn wrong_responder_key_rejects() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let wrong_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // A different responder (wrong key) tries to process the initiation.
        let result = process_initiation(&wrong_static, &init_msg, 2);
        assert!(result.is_none(), "wrong responder key should fail");
    }

    #[test]
    fn tampered_initiation_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (mut init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // Tamper with the encrypted static field.
        init_msg.encrypted_static[0] ^= 0xff;

        let result = process_initiation(&responder_static, &init_msg, 2);
        assert!(result.is_none(), "tampered initiation should fail");
    }

    #[test]
    fn tampered_response_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Tamper with the encrypted empty field.
        resp_msg.encrypted_empty[0] ^= 0xff;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "tampered response should fail");
    }

    #[test]
    fn mismatched_receiver_index_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Change receiver_index to wrong value.
        resp_msg.receiver_index = 999;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "wrong receiver index should fail");
    }
}

```

## File stats
- Lines: 476
- Symbols: 16
- Exported symbols: 9

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Use the project structure above to evaluate rule 11 (import organization) and detect file placement inconsistencies. If the file is in an unexpected directory given the project conventions, flag it as WARN for the most relevant rule.

Evaluate all 17 rules and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "score": 9.5,
  "rules": [
    {
      "rule_id": 1,
      "rule_name": "No unwrap in production code",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No .unwrap() calls exist in production paths. All fallible conversions use .expect() with descriptive messages (e.g., 'static is 48 bytes'), which is explicitly permitted by this rule. In-test .unwrap() calls are in #[cfg(test)] and are exempt."
    },
    {
      "rule_id": 2,
      "rule_name": "No unsafe blocks without justification",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No unsafe blocks are present anywhere in this file."
    },
    {
      "rule_id": 3,
      "rule_name": "Proper error handling with Result/Option",
      "status": "PASS",
      "severity": "HIGH",
      "detail": "All fallible paths that depend on external input return Option<T> and propagate failure via ?. The decrypt_and_hash return is properly threaded. Intentional discards like 'let _ = h;' and 'let (ck, _) = mix_key(...)' are protocol-defined (Noise pattern) and documented inline, not silent error ignores."
    },
    {
      "rule_id": 4,
      "rule_name": "Derive common traits on public types",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "InitiatorHandshake (pub) derives only Zeroize and ZeroizeOnDrop, omitting Debug, Clone, and PartialEq. While intentional omission of Clone (prevents accidental key duplication) and PartialEq (avoids non-constant-time comparison) is a sound security decision, the absence of a custom Debug impl means users cannot inspect the struct during development at all. A redacting Debug impl (masking key fields) would satisfy the rule's intent without leaking key material.",
      "lines": "L73-L81"
    },
    {
      "rule_id": 5,
      "rule_name": "Lifetime annotations explicit where needed",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "All functions return owned types ([u8; 32], Vec<u8>, Option<(...)>, tuples), so lifetime elision on input references is unambiguous and correct throughout. No hidden lifetime coupling exists between inputs and outputs."
    },
    {
      "rule_id": 6,
      "rule_name": "Clippy idioms",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "The single .clone() call (their_public.clone() at L161) is necessary: the struct takes ownership and the caller holds a reference, so a clone is required. No iterator chains are needed. No redundant borrowing patterns detected.",
      "lines": "L161"
    },
    {
      "rule_id": 7,
      "rule_name": "No panic! in library/production code",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No panic! macro invocations are present. The three .expect() calls in production paths ('static is 48 bytes', 'timestamp is 28 bytes', 'empty encrypted is 16 bytes') are on try_into() conversions from fixed-size cryptographic outputs whose sizes are enforced by the crypto layer — these are invariant assertions rather than user-input-dependent panics, and .expect() is explicitly permitted by rule 1."
    },
    {
      "rule_id": 8,
      "rule_name": "Module organization",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "The file is correctly placed in rustguard-core/src/handshake.rs. Internal helpers (initial_chain_key, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, constant_time_eq) are private. Public surface (compute_mac1, InitiatorHandshake, create_initiation*, process_response, process_initiation*) is cleanly separated. The initiator/responder split is clearly delineated with comments."
    },
    {
      "rule_id": 9,
      "rule_name": "Documentation comments on public items",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "All 9 exported symbols have /// doc comments. The module-level //! doc block explains the Noise_IKpsk2 pattern, the 1-RTT exchange, and the role of ck/h threading. Private helpers also have inline // comments explaining cryptographic purpose."
    },
    {
      "rule_id": 10,
      "rule_name": "No hardcoded secrets or credentials",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No hardcoded secrets are present. The &[0u8; 32] zero PSK in create_initiation and process_initiation is the WireGuard-specified 'no PSK' sentinel value (protocol-defined constant), not a credential."
    },
    {
      "rule_id": 11,
      "rule_name": "Memory safety",
      "status": "WARN",
      "severity": "HIGH",
      "detail": "InitiatorHandshake correctly derives ZeroizeOnDrop, ensuring ck, h, ephemeral, and psk are cleared on drop. However, in process_response the fields are copied out of the struct into local variables (let mut ck = state.ck; let psk = state.psk;). Because [u8; 32] is Copy, these stack locals hold live key material but are not wrapped in Zeroizing<T>, so they will not be scrubbed when the function returns. The original struct fields will still be zeroized via ZeroizeOnDrop, but the copies persist on the stack frame until overwritten by the OS/allocator.",
      "lines": "L176-L181"
    },
    {
      "rule_id": 12,
      "rule_name": "Concurrency safety",
      "status": "PASS",
      "severity": "HIGH",
      "detail": "No shared mutable state, no Arc/Mutex, no thread spawning. All functions are pure transforms over owned or immutably borrowed data. InitiatorHandshake is a single-use struct consumed by process_response (moved, not shared). No Send/Sync bounds are needed or violated."
    }
  ],
  "suggestions": [
    {
      "description": "Wrap sensitive local stack copies in Zeroizing<T> inside process_response to ensure they are cleared on scope exit, matching the intent of ZeroizeOnDrop on the parent struct.",
      "before": "let mut ck = state.ck;\nlet mut h = state.h;\nlet psk = state.psk;",
      "after": "use zeroize::Zeroizing;\nlet mut ck = Zeroizing::new(state.ck);\nlet mut h = Zeroizing::new(state.h);\nlet psk = Zeroizing::new(state.psk);"
    },
    {
      "description": "Add a custom Debug impl for InitiatorHandshake that redacts key material, preserving debuggability without leaking secrets.",
      "before": "#[derive(Zeroize, ZeroizeOnDrop)]\npub struct InitiatorHandshake { .. }",
      "after": "#[derive(Zeroize, ZeroizeOnDrop)]\npub struct InitiatorHandshake { .. }\n\nimpl core::fmt::Debug for InitiatorHandshake {\n    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {\n        f.debug_struct(\"InitiatorHandshake\")\n            .field(\"sender_index\", &self.sender_index)\n            .field(\"ck\", &\"[redacted]\")\n            .field(\"h\", &\"[redacted]\")\n            .field(\"psk\", &\"[redacted]\")\n            .finish()\n    }\n}"
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.2651 | **Duration:** 145.1s


---

# Axis: documentation

## User

## File: `rustguard-core/src/handshake.rs`
## Language: rust

```rust
//! WireGuard Noise_IKpsk2 handshake.
//!
//! The handshake is a 1-RTT key exchange:
//!   Initiator -> Responder: ephemeral, encrypted static, encrypted timestamp
//!   Responder -> Initiator: ephemeral, encrypted empty
//!
//! After this exchange both sides derive symmetric transport keys.
//! The chaining key (ck) and handshake hash (h) are threaded through
//! every operation — this is the Noise pattern in action.

use alloc::vec::Vec;
use rustguard_crypto::{
    self as crypto, EphemeralSecret, PublicKey, SharedSecret, StaticSecret, Tai64n,
    CONSTRUCTION, IDENTIFIER, LABEL_MAC1,
};
use zeroize::{Zeroize, ZeroizeOnDrop};

use crate::messages::{Initiation, Response};
use crate::session::TransportSession;

/// Precomputed initial handshake state from the protocol constants.
/// These are the same for every handshake — compute once.
fn initial_chain_key() -> [u8; 32] {
    crypto::hash(&[CONSTRUCTION])
}

fn initial_hash(ck: &[u8; 32]) -> [u8; 32] {
    crypto::hash(&[ck, IDENTIFIER])
}

/// Mix a public key into the hash. Used to bind the responder's identity.
fn mix_hash(h: &[u8; 32], data: &[u8]) -> [u8; 32] {
    crypto::hash(&[h, data])
}

/// Mix the result of a DH into the chaining key.
/// Returns (new_ck, key) where key is for AEAD encryption.
fn mix_key(ck: &[u8; 32], dh_result: &SharedSecret) -> ([u8; 32], [u8; 32]) {
    let (new_ck, key, _) = crypto::hkdf(ck, dh_result.as_bytes());
    (new_ck, key)
}

/// Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.
fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
    let ciphertext = crypto::seal(key, 0, h, plaintext);
    let new_h = mix_hash(h, &ciphertext);
    (new_h, ciphertext)
}

/// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
    let plaintext = crypto::open(key, 0, h, ciphertext)?;
    let new_h = mix_hash(h, ciphertext);
    Some((new_h, plaintext))
}

/// Compute MAC1 over a message using the responder's public key.
/// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
    let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
    let full_mac = crypto::mac(&key, &[msg_bytes]);
    let mut mac1 = [0u8; 16];
    mac1.copy_from_slice(&full_mac[..16]);
    mac1
}

// ── Initiator side ──────────────────────────────────────────────────

/// State held by the initiator between sending msg1 and receiving msg2.
/// Zeroized on drop to prevent key material from lingering in memory.
#[derive(Zeroize, ZeroizeOnDrop)]
pub struct InitiatorHandshake {
    ck: [u8; 32],
    h: [u8; 32],
    ephemeral: EphemeralSecret,
    #[zeroize(skip)]
    sender_index: u32,
    #[zeroize(skip)]
    their_public: PublicKey,
    psk: [u8; 32],
}

/// Create a handshake initiation message (std convenience — uses OsRng and system clock).
#[cfg(feature = "std")]
pub fn create_initiation(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
) -> (Initiation, InitiatorHandshake) {
    create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
}

/// Create a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn create_initiation_psk(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
) -> (Initiation, InitiatorHandshake) {
    let ephemeral = EphemeralSecret::random();
    let timestamp = Tai64n::now();
    create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
}

/// Create a handshake initiation with explicit ephemeral key and timestamp.
/// This is the core function — usable from both std and no_std.
pub fn create_initiation_with(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
    ephemeral: EphemeralSecret,
    timestamp: Tai64n,
) -> (Initiation, InitiatorHandshake) {
    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix responder's public key into hash — both sides know this.
    h = mix_hash(&h, their_public.as_ref());

    let eph_public = ephemeral.public_key();

    // Mix ephemeral public into hash.
    h = mix_hash(&h, eph_public.as_ref());

    // DH(ephemeral, responder_static) -> mix into ck.
    let dh1 = ephemeral.diffie_hellman(their_public);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Encrypt our static public key.
    let our_public = our_static.public_key();
    let encrypted_static;
    (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());

    // DH(our_static, responder_static) -> mix into ck.
    let dh2 = our_static.diffie_hellman(their_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Encrypt timestamp.
    let encrypted_timestamp;
    (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());

    // Build message (mac1/mac2 computed over wire bytes).
    let mut msg = Initiation {
        sender_index,
        ephemeral: *eph_public.as_bytes(),
        encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
        encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16], // No cookie mechanism yet.
    };

    // MAC1 is computed over everything before mac1 field.
    let wire = msg.to_bytes();
    msg.mac1 = compute_mac1(their_public, &wire[..116]);

    let state = InitiatorHandshake {
        ck,
        h,
        ephemeral,
        sender_index,
        their_public: their_public.clone(),
        psk: *psk,
    };

    (msg, state)
}

/// Process a handshake response and derive transport keys.
pub fn process_response(
    state: InitiatorHandshake,
    our_static: &StaticSecret,
    msg: &Response,
) -> Option<TransportSession> {
    // Access fields by reference — struct will zeroize on drop.
    let mut ck = state.ck;
    let mut h = state.h;
    let sender_index = state.sender_index;
    let psk = state.psk;

    // Verify receiver_index matches our sender_index.
    if msg.receiver_index != sender_index {
        return None;
    }

    let resp_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph.as_ref());

    // DH(our_ephemeral, responder_ephemeral)
    let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh1);

    // DH(our_static, responder_ephemeral)
    let dh2 = our_static.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh2);

    // PSK phase (Noise_IKpsk2): mix PSK into chain.
    let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Decrypt empty payload.
    let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
    let (h, _empty) = result?;
    let _ = h;

    // Derive transport keys: initiator sends with first, receives with second.
    let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);

    Some(TransportSession::new(
        sender_index,
        msg.sender_index,
        send_key,
        recv_key,
    ))
}

// ── Responder side ──────────────────────────────────────────────────

/// Process a handshake initiation on the responder side (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
}

/// Process a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation_psk(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let resp_eph = EphemeralSecret::random();
    process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
}

/// Process a handshake initiation with explicit ephemeral (no_std compatible).
pub fn process_initiation_with(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
    resp_eph: EphemeralSecret,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let our_public = our_static.public_key();

    // ── Verify MAC1 FIRST — cheap check before any DH. ──
    let wire = msg.to_bytes();
    let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
    if !constant_time_eq(&msg.mac1, &expected_mac1) {
        return None;
    }
    // TODO: MAC2 / cookie validation under load.

    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix our public key into hash (responder's identity).
    h = mix_hash(&h, our_public.as_ref());

    let init_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix initiator ephemeral into hash.
    h = mix_hash(&h, init_eph.as_ref());

    // DH(our_static, initiator_ephemeral)
    let dh1 = our_static.diffie_hellman(&init_eph);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Decrypt initiator's static public key.
    let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
    h = new_h;

    let initiator_public = PublicKey::from_bytes(
        static_bytes
            .as_slice()
            .try_into()
            .ok()?,
    );

    // DH(our_static, initiator_static)
    let dh2 = our_static.diffie_hellman(&initiator_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Decrypt timestamp.
    let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
    h = new_h;

    // Validate timestamp is newer than last seen — prevents replay.
    let timestamp = Tai64n::from_bytes(
        timestamp_bytes.as_slice().try_into().ok()?,
    );
    if let Some(last) = last_timestamp {
        if !timestamp.is_after(last) {
            return None; // Replayed or stale initiation.
        }
    }

    // ── Build response ──

    let resp_eph_public = resp_eph.public_key();

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph_public.as_ref());

    // DH(resp_ephemeral, initiator_ephemeral)
    let dh3 = resp_eph.diffie_hellman(&init_eph);
    (ck, _) = mix_key(&ck, &dh3);

    // DH(resp_ephemeral, initiator_static)
    let dh4 = resp_eph.diffie_hellman(&initiator_public);
    (ck, _) = mix_key(&ck, &dh4);

    // PSK phase.
    let (new_ck, t, key3) = crypto::hkdf(&ck, psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Encrypt empty.
    let encrypted_empty;
    (h, encrypted_empty) = encrypt_and_hash(&key3, &h, &[]);
    let _ = h; // final hash

    // Derive transport keys: responder sends with second, receives with first.
    let (recv_key, send_key, _) = crypto::hkdf(&ck, &[]);

    let mut resp = Response {
        sender_index: responder_index,
        receiver_index: msg.sender_index,
        ephemeral: *resp_eph_public.as_bytes(),
        encrypted_empty: encrypted_empty.try_into().expect("empty encrypted is 16 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16],
    };

    // MAC1 on response.
    let resp_wire = resp.to_bytes();
    resp.mac1 = compute_mac1(&initiator_public, &resp_wire[..60]);

    let session = TransportSession::new(responder_index, msg.sender_index, send_key, recv_key);

    Some((initiator_public, timestamp, resp, session))
}

/// Constant-time comparison for MACs using the `subtle` crate.
fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
    use subtle::ConstantTimeEq;
    a.ct_eq(b).into()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn full_handshake_and_transport() {
        // Setup: both sides have static keys, initiator knows responder's public key.
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        // Step 1: Initiator creates handshake initiation.
        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        // Step 2: Responder processes initiation, creates response.
        let (peer_pubkey, _ts, resp_msg, mut resp_session) =
            process_initiation(&responder_static, &init_msg, 2)
                .expect("responder should accept initiation");

        // Responder correctly identifies the initiator.
        assert_eq!(peer_pubkey, initiator_static.public_key());

        // Step 3: Initiator processes response, gets transport session.
        let mut init_session =
            process_response(init_state, &initiator_static, &resp_msg)
                .expect("initiator should accept response");

        // Step 4: Both sides can now exchange encrypted data.
        let plaintext = b"hello from the trenches";
        let (counter, ciphertext) = init_session.encrypt(plaintext).unwrap();
        let decrypted = resp_session
            .decrypt(counter, &ciphertext)
            .expect("responder should decrypt");
        assert_eq!(&decrypted, plaintext);

        // And the other direction.
        let reply = b"copy that, over";
        let (counter, ciphertext) = resp_session.encrypt(reply).unwrap();
        let decrypted = init_session
            .decrypt(counter, &ciphertext)
            .expect("initiator should decrypt");
        assert_eq!(&decrypted, reply);
    }

    #[test]
    fn wrong_responder_key_rejects() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let wrong_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // A different responder (wrong key) tries to process the initiation.
        let result = process_initiation(&wrong_static, &init_msg, 2);
        assert!(result.is_none(), "wrong responder key should fail");
    }

    #[test]
    fn tampered_initiation_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (mut init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // Tamper with the encrypted static field.
        init_msg.encrypted_static[0] ^= 0xff;

        let result = process_initiation(&responder_static, &init_msg, 2);
        assert!(result.is_none(), "tampered initiation should fail");
    }

    #[test]
    fn tampered_response_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Tamper with the encrypted empty field.
        resp_msg.encrypted_empty[0] ^= 0xff;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "tampered response should fail");
    }

    #[test]
    fn mismatched_receiver_index_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Change receiver_index to wrong value.
        resp_msg.receiver_index = 999;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "wrong receiver index should fail");
    }
}

```

## Symbols to evaluate

- function initial_chain_key (L23–L25)
- function initial_hash (L27–L29)
- function mix_hash (L32–L34)
- function mix_key (L38–L41)
- function encrypt_and_hash (L44–L48)
- function decrypt_and_hash (L51–L55)
- export function compute_mac1 (L59–L65)
- export class InitiatorHandshake (L72–L81)
- export function create_initiation (L85–L91)
- export function create_initiation_psk (L95–L104)
- export function create_initiation_with (L108–L170)
- export function process_response (L173–L221)
- export function process_initiation (L227–L233)
- export function process_initiation_psk (L237–L246)
- export function process_initiation_with (L249–L357)
- function constant_time_eq (L360–L363)

## Documentation Directory

No /docs/ directory found — evaluate JSDoc only, skip concept coverage.

Evaluate documentation for each symbol and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "initial_chain_key",
      "line_start": 23,
      "line_end": 25,
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "Has a `///` doc comment describing purpose and rationale ('compute once'), but no return value description or `# Examples`. Tolerable for a private helper, but the comment is shared ambiguously with `initial_hash` above it."
    },
    {
      "name": "initial_hash",
      "line_start": 27,
      "line_end": 29,
      "documentation": "UNDOCUMENTED",
      "confidence": 95,
      "detail": "No `///` doc comment at all. The preceding doc block belongs to `initial_chain_key`, leaving `initial_hash` entirely undocumented. Even as a private function, some description of the `ck` parameter and the Noise protocol role would be helpful."
    },
    {
      "name": "mix_hash",
      "line_start": 32,
      "line_end": 34,
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "Has a one-line `///` comment describing purpose, but the description says 'public key' while the parameter is `&[u8]` (generic data). No parameter or return documentation. Private function, so no `# Examples` required, but the inaccuracy reduces quality."
    },
    {
      "name": "mix_key",
      "line_start": 38,
      "line_end": 41,
      "documentation": "PARTIAL",
      "confidence": 88,
      "detail": "Two-line `///` comment describes purpose and the tuple return value `(new_ck, key)`. Good for a private helper, but the `dh_result` parameter and the dropped third HKDF output are not explained. No `# Examples`."
    },
    {
      "name": "encrypt_and_hash",
      "line_start": 44,
      "line_end": 48,
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "Single-line `///` comment describes the combined operation. No parameter descriptions (key, h, plaintext), no return tuple explanation, no `# Examples`. Adequate for private use but incomplete."
    },
    {
      "name": "decrypt_and_hash",
      "line_start": 51,
      "line_end": 55,
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "Single-line `///` comment mirrors `encrypt_and_hash`. The `Option` return indicating authentication failure is not documented. No parameter or return details, no `# Examples`. Adequate for private use but incomplete."
    },
    {
      "name": "compute_mac1",
      "line_start": 59,
      "line_end": 65,
      "documentation": "PARTIAL",
      "confidence": 90,
      "detail": "Public function with two-line `///` comment that includes an algorithmic formula — helpful. However, no `# Parameters`, `# Returns`, or `# Examples` sections. The `msg_bytes` parameter's expected content (pre-mac1 wire bytes) is not documented."
    },
    {
      "name": "InitiatorHandshake",
      "line_start": 72,
      "line_end": 81,
      "documentation": "PARTIAL",
      "confidence": 88,
      "detail": "Public struct with a two-line `///` comment describing its lifecycle role and the zeroize security property. All fields are private so field-level docs are not strictly required, but there is no `# Examples` section showing how to obtain or use this state, and no mention of which functions consume it."
    },
    {
      "name": "create_initiation",
      "line_start": 85,
      "line_end": 91,
      "documentation": "PARTIAL",
      "confidence": 90,
      "detail": "Public function with a single-line `///` comment noting it is a std convenience wrapper. No parameter descriptions, no return value explanation (`(Initiation, InitiatorHandshake)` tuple), and no `# Examples` section. The PSK defaulting to all-zeros is not documented."
    },
    {
      "name": "create_initiation_psk",
      "line_start": 95,
      "line_end": 104,
      "documentation": "PARTIAL",
      "confidence": 90,
      "detail": "Public function with a minimal one-line `///` comment. No documentation for the `psk` parameter semantics, no return description, and no `# Examples`. The PSK length constraint (32 bytes) is implied by the type but not stated."
    },
    {
      "name": "create_initiation_with",
      "line_start": 108,
      "line_end": 170,
      "documentation": "PARTIAL",
      "confidence": 90,
      "detail": "Most complex public function; has a two-line `///` comment noting it is the core no_std-compatible entry point. None of the six parameters are documented, the return tuple is not described, and there is no `# Examples` section. The inline comments inside the body are helpful but do not substitute for API-level documentation."
    },
    {
      "name": "process_response",
      "line_start": 173,
      "line_end": 221,
      "documentation": "PARTIAL",
      "confidence": 90,
      "detail": "Public function with a single-line `///` comment. The `Option` return (indicating handshake failure) is not documented, nor are the conditions that cause `None` (index mismatch, AEAD failure). No `# Parameters`, `# Returns`, or `# Examples` sections."
    },
    {
      "name": "process_initiation",
      "line_start": 227,
      "line_end": 233,
      "documentation": "PARTIAL",
      "confidence": 90,
      "detail": "Public function with a single-line `///` comment. The four-element return tuple `(PublicKey, Tai64n, Response, TransportSession)` is not explained, failure conditions for `None` are not listed, and there is no `# Examples` section."
    },
    {
      "name": "process_initiation_psk",
      "line_start": 237,
      "line_end": 246,
      "documentation": "PARTIAL",
      "confidence": 90,
      "detail": "Public function with a one-line `///` comment. The `last_timestamp` replay-protection parameter is significant but undocumented. No parameter, return, or `# Examples` sections."
    },
    {
      "name": "process_initiation_with",
      "line_start": 249,
      "line_end": 357,
      "documentation": "PARTIAL",
      "confidence": 90,
      "detail": "Largest public function in the file with a single-line `///` comment. None of the six parameters (including the critical `last_timestamp` replay guard and `resp_eph`) are documented. The complex four-element return tuple and all `None` failure conditions are undescribed. No `# Examples` section."
    },
    {
      "name": "constant_time_eq",
      "line_start": 360,
      "line_end": 363,
      "documentation": "UNDOCUMENTED",
      "confidence": 80,
      "detail": "Private function with no `///` doc comment. The name is self-explanatory and it delegates to `subtle::ConstantTimeEq`, but the security rationale (MAC comparison must not short-circuit) is worth documenting even for private helpers in a cryptographic codebase."
    }
  ],
  "docs_coverage": {
    "concepts": [],
    "score_pct": 42
  }
}

## rate_limit_event

## Result (success)

**Cost:** $0.2372 | **Duration:** 95.8s


---

# Deliberation Pass

## User

## Merged ReviewFile (from 7 axis evaluators)

```json
{
  "version": 2,
  "file": "rustguard-core/src/handshake.rs",
  "is_generated": false,
  "language": "rust",
  "parse_method": "ast",
  "verdict": "NEEDS_REFACTOR",
  "symbols": [
    {
      "name": "initial_chain_key",
      "kind": "function",
      "exported": false,
      "line_start": 23,
      "line_end": 25,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "[USED] Called in create_initiation_with (line 119) and process_initiation_with (line 275) | [UNIQUE] Trivial 2-line utility function with no external duplicates found | [OK] Correctly computes HASH(CONSTRUCTION) as the Noise protocol's initial chaining key. No issues found. | [LEAN] Single-call wrapper that computes the Noise protocol constant exactly once. Naming it clearly communicates intent and isolates the magic constant. | [WEAK] Private helper exercised only indirectly through the full handshake test. No isolated unit test verifying the output against a known constant derived from CONSTRUCTION, and no test that catches a regression if the constant changes. | [PARTIAL] Has a `///` doc comment describing purpose and rationale ('compute once'), but no return value description or `# Examples`. Tolerable for a private helper, but the comment is shared ambiguously with `initial_hash` above it."
    },
    {
      "name": "initial_hash",
      "kind": "function",
      "exported": false,
      "line_start": 27,
      "line_end": 29,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "UNDOCUMENTED",
      "confidence": 85,
      "detail": "[USED] Called in create_initiation_with (line 120) and process_initiation_with (line 276) | [UNIQUE] Trivial 2-line utility function with no external duplicates found | [OK] Correctly computes HASH(ck || IDENTIFIER) for the initial handshake hash. No issues found. | [LEAN] One-liner companion to initial_chain_key. The two helpers together mirror the Noise spec's initialisation step precisely. | [WEAK] Private helper covered only transitively via full_handshake_and_transport. No direct test with known-good vectors for the CONSTRUCTION+IDENTIFIER hash, making silent regressions possible. | [UNDOCUMENTED] No `///` doc comment at all. The preceding doc block belongs to `initial_chain_key`, leaving `initial_hash` entirely undocumented. Even as a private function, some description of the `ck` parameter and the Noise protocol role would be helpful."
    },
    {
      "name": "mix_hash",
      "kind": "function",
      "exported": false,
      "line_start": 32,
      "line_end": 34,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "[USED] Called in encrypt_and_hash (line 46), decrypt_and_hash (line 54), and multiple times in both initiator and responder paths | [UNIQUE] Trivial 2-line utility function with no external duplicates found | [OK] Correct Noise MixHash operation: h = HASH(h || data). No issues found. | [LEAN] Trivial but semantically important: naming this 'mix_hash' makes every call site read like the Noise protocol spec, which is the right trade-off in cryptographic code. | [WEAK] Called many times throughout the handshake but never tested in isolation. All coverage is implicit via higher-level tests; no edge-case input (empty slice, full 32-byte slice) is exercised directly. | [PARTIAL] Has a one-line `///` comment describing purpose, but the description says 'public key' while the parameter is `&[u8]` (generic data). No parameter or return documentation. Private function, so no `# Examples` required, but the inaccuracy reduces quality."
    },
    {
      "name": "mix_key",
      "kind": "function",
      "exported": false,
      "line_start": 38,
      "line_end": 41,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "DUPLICATE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "[USED] Called multiple times in create_initiation_with (lines 132, 145) and process_initiation_with (lines 296, 318, 337, 341) | [DUPLICATE] Identical logic to mix_key in rustguard-kmod/src/noise.rs; both perform hkdf and return (new_ck, key) tuple. Parameter type differs (SharedSecret wrapper vs raw bytes) but semantic behavior is equivalent. | [OK] Correctly implements HKDF2(ck, dh_result) returning (new_ck, key) and discarding the unused third output. Matches the Noise MixKey primitive. | [LEAN] Wraps HKDF to produce (ck, key). Discarding the third HKDF output explicitly via '_' is correct and intentional per WireGuard spec. | [WEAK] Indirect coverage only. The function discards the third HKDF output silently; no test verifies that the returned (ck, key) pair is derived correctly against a reference vector. | [PARTIAL] Two-line `///` comment describes purpose and the tuple return value `(new_ck, key)`. Good for a private helper, but the `dh_result` parameter and the dropped third HKDF output are not explained. No `# Examples`.",
      "duplicate_target": {
        "file": "rustguard-kmod/src/noise.rs",
        "symbol": "mix_key",
        "similarity": "96% identical logic — both call hkdf(ck, dh_result) and return the key pair, differing only in parameter wrapper types"
      }
    },
    {
      "name": "encrypt_and_hash",
      "kind": "function",
      "exported": false,
      "line_start": 44,
      "line_end": 48,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 72,
      "detail": "[USED] Called in create_initiation_with (lines 139, 148) and process_initiation_with (line 351) | [UNIQUE] Similar structure to kmod version but different semantic contracts: returns owned Vec<u8> with immediate ciphertext vs Option<usize> with buffer-based output. Different memory models (allocation vs pre-allocated buffer) make them non-interchangeable despite 92% similarity score. | [OK] AEAD seal with nonce 0 and h as AAD, followed by MixHash(ciphertext) is the correct Noise EncryptAndHash operation. | [LEAN] Direct encoding of the Noise 'EncryptAndHash' operation. Two steps, no hidden logic. | [WEAK] Covered transitively by tampered_initiation_rejected and tampered_response_rejected (which confirm AEAD authentication), but no unit test verifies ciphertext length, the hash update step, or encrypt-then-hash ordering in isolation. | [PARTIAL] Single-line `///` comment describes the combined operation. No parameter descriptions (key, h, plaintext), no return tuple explanation, no `# Examples`. Adequate for private use but incomplete."
    },
    {
      "name": "decrypt_and_hash",
      "kind": "function",
      "exported": false,
      "line_start": 51,
      "line_end": 55,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 72,
      "detail": "[USED] Called in process_response (line 222) and process_initiation_with (lines 308, 319) | [UNIQUE] Similar structure to kmod version but different semantic contracts: returns owned Vec<u8> plaintext vs Option<usize> with buffer-based output. Different memory models and return semantics make them non-interchangeable despite 92% similarity score. | [OK] AEAD open with nonce 0 and h as AAD, followed by MixHash(ciphertext) on success is the correct Noise DecryptAndHash operation. Hash is updated with the ciphertext (not the plaintext), which is correct. | [LEAN] Symmetric counterpart to encrypt_and_hash. Returns Option to propagate AEAD tag failures, which is idiomatic Rust for fallible decryption. | [WEAK] Exercises the None path indirectly via tampered message tests, but no unit test confirms the hash is updated from the *ciphertext* (not plaintext) or checks behavior on zero-length input independently. | [PARTIAL] Single-line `///` comment mirrors `encrypt_and_hash`. The `Option` return indicating authentication failure is not documented. No parameter or return details, no `# Examples`. Adequate for private use but incomplete."
    },
    {
      "name": "compute_mac1",
      "kind": "function",
      "exported": true,
      "line_start": 59,
      "line_end": 65,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "DUPLICATE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 82,
      "detail": "[USED] Called in create_initiation_with (line 162), process_initiation_with (lines 278, 356) | [DUPLICATE] Identical logic to compute_mac1 in rustguard-kmod/src/noise.rs; both hash LABEL_MAC1||responder_public, compute MAC, and extract first 16 bytes. Parameter type differs (PublicKey wrapper vs raw [u8; 32]) but implementation is equivalent. | [OK] Correctly computes MAC1 = HMAC-BLAKE2s(HASH(LABEL_MAC1 || S_pub_r), msg)[:16] per WireGuard spec. Truncation to 16 bytes is correct. | [LEAN] Faithfully implements the WireGuard MAC1 derivation. The temporary 'key' variable and slice copy are necessary since BLAKE2s-MAC produces 32 bytes and MAC1 is only 16. | [WEAK] MAC1 rejection is exercised by wrong_responder_key_rejects (MAC1 mismatch causes None) but no test directly checks the 16-byte output value against a known vector, nor are there tests for tampered mac1 bytes specifically. | [PARTIAL] Public function with two-line `///` comment that includes an algorithmic formula — helpful. However, no `# Parameters`, `# Returns`, or `# Examples` sections. The `msg_bytes` parameter's expected content (pre-mac1 wire bytes) is not documented.",
      "duplicate_target": {
        "file": "rustguard-kmod/src/noise.rs",
        "symbol": "compute_mac1",
        "similarity": "95% identical implementation — both compute BLAKE2s MAC and slice first 16 bytes, differing only in parameter wrapper type"
      }
    },
    {
      "name": "InitiatorHandshake",
      "kind": "class",
      "exported": true,
      "line_start": 72,
      "line_end": 81,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 80,
      "detail": "[USED] Returned by create_initiation_with (line 163) and accepted by process_response (line 174); used extensively in tests | [UNIQUE] Struct definition with no duplicate struct definitions found in RAG results or codebase | [OK] Sensitive fields ck, h, ephemeral, and psk are correctly zeroized on drop. Non-sensitive sender_index and their_public are correctly skipped with #[zeroize(skip)]. | [LEAN] Appropriately uses ZeroizeOnDrop for key material and zeroize(skip) for non-sensitive index/pubkey fields. Exactly the fields needed to bridge msg1 and msg2 — nothing extraneous. | [WEAK] The struct is instantiated and consumed in every test, confirming basic lifecycle. However, the ZeroizeOnDrop guarantee is never tested, and the psk field interaction is only tested with the all-zero default. | [PARTIAL] Public struct with a two-line `///` comment describing its lifecycle role and the zeroize security property. All fields are private so field-level docs are not strictly required, but there is no `# Examples` section showing how to obtain or use this state, and no mention of which functions consume it."
    },
    {
      "name": "create_initiation",
      "kind": "function",
      "exported": true,
      "line_start": 85,
      "line_end": 91,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 88,
      "detail": "[USED] Called in full_handshake_and_transport test (line 389) and other tests (lines 406, 427, 441) | [UNIQUE] Convenience wrapper that delegates to create_initiation_psk with default PSK parameter. Distinct function with clear caller-callee relationship rather than true duplication. | [OK] Correctly delegates to create_initiation_psk with a zero PSK, matching WireGuard's no-PSK behavior. | [LEAN] Minimal std-only shim that forwards to the PSK variant with a zero PSK. Provides the common no-PSK use case without duplicating logic. | [WEAK] Used in all five inline tests covering happy path and several rejection scenarios. However it is only tested with the implicit zero PSK; no test distinguishes it from create_initiation_psk or verifies the OsRng / system-clock path specifically. | [PARTIAL] Public function with a single-line `///` comment noting it is a std convenience wrapper. No parameter descriptions, no return value explanation (`(Initiation, InitiatorHandshake)` tuple), and no `# Examples` section. The PSK defaulting to all-zeros is not documented."
    },
    {
      "name": "create_initiation_psk",
      "kind": "function",
      "exported": true,
      "line_start": 95,
      "line_end": 104,
      "correction": "OK",
      "overengineering": "ACCEPTABLE",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "[USED] Called by create_initiation (line 91) and used as entry point for PSK-based handshakes | [UNIQUE] Convenience wrapper that generates ephemeral key and timestamp, then delegates to create_initiation_with. Distinct function serving specific purpose. | [OK] Correctly generates a fresh random ephemeral secret and current TAI64n timestamp before delegating to the core function. | [ACCEPTABLE] Middle tier in a three-layer delegation: plain → psk → with. The split between this and create_initiation_with is justified: this layer injects OS entropy and the system clock (std-only concerns), while the _with variant is deterministic for no_std and testing. Slight verbosity but the separation of concerns is sound for a crypto library. | [WEAK] Only reached through create_initiation (zero PSK). No test passes a non-zero PSK through this wrapper, so the psk parameter branch is untested. The core matching-PSK / mismatched-PSK scenarios are absent. | [PARTIAL] Public function with a minimal one-line `///` comment. No documentation for the `psk` parameter semantics, no return description, and no `# Examples`. The PSK length constraint (32 bytes) is implied by the type but not stated."
    },
    {
      "name": "create_initiation_with",
      "kind": "function",
      "exported": true,
      "line_start": 108,
      "line_end": 170,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 78,
      "detail": "[USED] Called by create_initiation_psk (line 103); core function implementing initiator handshake message creation | [UNIQUE] Core handshake initiation logic similar to kmod version (0.898 score) but context-specific: uses PublicKey/StaticSecret wrapper types, struct-based message format, and different error handling. Non-interchangeable due to different abstraction levels. | [OK] Correctly implements Noise_IKpsk2 initiation: MixHash(S_pub_r), MixHash(e_pub_i), MixKey(DH(e_i, S_r)), EncryptAndHash(S_pub_i), MixKey(DH(S_i, S_r)), EncryptAndHash(timestamp). MAC1 is computed over the first 116 wire bytes (1+3+4+32+48+28), which is correct for WireGuard's Initiation message layout. | [LEAN] Core no_std-compatible function. Length is proportional to the Noise_IKpsk2 spec: every statement maps to a named protocol step. No unnecessary abstraction. | [WEAK] Core initiator logic exercised transitively by all handshake tests, covering the full happy path. Missing: test with non-zero PSK, test with a controlled deterministic ephemeral (no_std path), and no test verifying the mac1 slice boundary (wire[..116]). | [PARTIAL] Most complex public function; has a two-line `///` comment noting it is the core no_std-compatible entry point. None of the six parameters are documented, the return tuple is not described, and there is no `# Examples` section. The inline comments inside the body are helpful but do not substitute for API-level documentation."
    },
    {
      "name": "process_response",
      "kind": "function",
      "exported": true,
      "line_start": 173,
      "line_end": 221,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 76,
      "detail": "[USED] Called in full_handshake_and_transport test (line 392) and failure scenario tests (lines 450, 465) | [UNIQUE] Similar protocol logic to kmod version (0.846 score) but different semantic contracts: takes InitiatorHandshake struct and Response type instead of raw state arrays. Different input/output abstractions make functions non-interchangeable. | [NEEDS_FIX] The function performs no MAC1 verification of the Response message before executing two expensive DH operations. The initiator's sender_index is transmitted in plaintext in the Initiation message, so an observer can craft a fake Response with the correct receiver_index, bypassing the only cheap early-exit check (line 183) and forcing full DH computation. compute_mac1(&our_static.public_key(), &wire[..60]) should be verified against msg.mac1 before any DH work, mirroring the responder's check in process_initiation_with. | [LEAN] Implements the initiator's response-processing path step-by-step per spec. Consuming InitiatorHandshake by value ensures the ephemeral is single-use and the struct is zeroized after use — idiomatic and correct. | [WEAK] Three tests cover the happy path, tampered encrypted_empty, and mismatched receiver_index. Missing: PSK mismatch (different PSK on each side), wrong static key for DH2, and verifying the initiator-sends/responder-receives key assignment. | [PARTIAL] Public function with a single-line `///` comment. The `Option` return (indicating handshake failure) is not documented, nor are the conditions that cause `None` (index mismatch, AEAD failure). No `# Parameters`, `# Returns`, or `# Examples` sections."
    },
    {
      "name": "process_initiation",
      "kind": "function",
      "exported": true,
      "line_start": 227,
      "line_end": 233,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 88,
      "detail": "[USED] Called in tests (lines 390, 408, 428, 442); convenience wrapper for responder initiation processing | [UNIQUE] Convenience wrapper that delegates to process_initiation_psk with default PSK and no last_timestamp. Distinct function with clear delegating relationship. | [OK] Correctly delegates to process_initiation_psk with a zero PSK and no prior timestamp, which is the correct WireGuard default behavior. | [LEAN] Thin std convenience that delegates to the PSK variant with zero PSK and no timestamp check. Provides clean API for the common case. | [WEAK] Convenience wrapper tested in all five inline tests. Because it always passes zero PSK and no last_timestamp, the replay-protection branch and non-zero PSK paths inside process_initiation_with are never exercised through this entry point. | [PARTIAL] Public function with a single-line `///` comment. The four-element return tuple `(PublicKey, Tai64n, Response, TransportSession)` is not explained, failure conditions for `None` are not listed, and there is no `# Examples` section."
    },
    {
      "name": "process_initiation_psk",
      "kind": "function",
      "exported": true,
      "line_start": 237,
      "line_end": 246,
      "correction": "OK",
      "overengineering": "ACCEPTABLE",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "[USED] Called by process_initiation (line 232); PSK-based responder handshake processing | [UNIQUE] Convenience wrapper that generates random ephemeral key and delegates to process_initiation_with. Distinct function serving specific purpose. | [OK] Correctly generates a fresh random responder ephemeral before delegating to the core function. No issues found. | [ACCEPTABLE] Same three-tier delegation rationale as create_initiation_psk. This layer owns the std-specific random ephemeral generation, keeping process_initiation_with testable and no_std-compatible. Justified, if slightly verbose. | [WEAK] Only called transitively with psk=[0u8;32] and last_timestamp=None. No test verifies PSK matching, PSK mismatch rejection, or the replay-protection branch activated by a non-None last_timestamp. | [PARTIAL] Public function with a one-line `///` comment. The `last_timestamp` replay-protection parameter is significant but undocumented. No parameter, return, or `# Examples` sections."
    },
    {
      "name": "process_initiation_with",
      "kind": "function",
      "exported": true,
      "line_start": 249,
      "line_end": 357,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "PARTIAL",
      "confidence": 77,
      "detail": "[USED] Called by process_initiation_psk (line 245); core function implementing responder initiation processing | [UNIQUE] Core responder-side handshake logic similar to kmod version (0.878 score) but context-specific: uses PublicKey/StaticSecret wrappers, struct-based messages (Initiation/Response), and Option-based timestamp validation. Different abstraction levels and input/output handling make non-interchangeable. | [OK] Correctly implements the responder side: MAC1 verified first via constant_time_eq, then full Noise_IKpsk2 state evolution (DH(S_r,e_i), decrypt static, DH(S_r,S_i), decrypt timestamp, replay check), response construction (MixHash(e_r), DH(e_r,e_i), DH(e_r,S_i), PSK phase, EncryptAndHash(empty)), and transport key derivation with correct key ordering (recv=first, send=second). | [LEAN] Long but not complex: each block maps directly to a Noise protocol step. MAC1 is checked first (cheap before DH), timestamp replay protection is inline. No gratuitous abstractions. | [WEAK] Covers MAC1 rejection (wrong key), AEAD failure (tampered encrypted_static), and happy path. Critically absent: timestamp replay protection (last_timestamp branch never tested), PSK mismatch scenario, tampered mac1 field, and tampered ephemeral or encrypted_timestamp fields. | [PARTIAL] Largest public function in the file with a single-line `///` comment. None of the six parameters (including the critical `last_timestamp` replay guard and `resp_eph`) are documented. The complex four-element return tuple and all `None` failure conditions are undescribed. No `# Examples` section."
    },
    {
      "name": "constant_time_eq",
      "kind": "function",
      "exported": false,
      "line_start": 360,
      "line_end": 363,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "WEAK",
      "documentation": "UNDOCUMENTED",
      "confidence": 80,
      "detail": "[USED] Called in process_initiation_with (line 279) for constant-time MAC comparison; encapsulates subtle::ConstantTimeEq conversion | [UNIQUE] Uses subtle crate's ConstantTimeEq trait wrapper, while kmod version uses unsafe wg_crypto_memneq. Different implementation approaches (library vs raw unsafe code) despite similar purpose. | [OK] Correctly uses the subtle crate's ConstantTimeEq trait for timing-safe MAC comparison. ct_eq on fixed-size arrays and conversion via Into<bool> is the idiomatic correct usage. | [LEAN] Correct use of the 'subtle' crate for constant-time MAC comparison. The thin wrapper gives a readable call site without hiding what it does. | [WEAK] Indirectly exercised by wrong_responder_key_rejects (MAC1 values differ). No direct unit test with equal arrays, one-bit-different arrays, or any assertion that the subtle crate's ct_eq is being used rather than a naive comparison. | [UNDOCUMENTED] Private function with no `///` doc comment. The name is self-explanatory and it delegates to `subtle::ConstantTimeEq`, but the security rationale (MAC comparison must not short-circuit) is worth documenting even for private helpers in a cryptographic codebase."
    }
  ],
  "actions": [
    {
      "id": 1,
      "description": "Add MAC1 verification in process_response before any DH operations: compute expected_mac1 = compute_mac1(&our_static.public_key(), &msg.to_bytes()[..60]) and return None if it does not constant-time-equal msg.mac1. Without this check, an attacker who observed the plaintext sender_index in the Initiation can craft fake Response messages that survive the receiver_index guard and force two DH exponentiations per fake packet.",
      "severity": "medium",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L183",
      "source": "correction"
    },
    {
      "id": 2,
      "description": "Complete JSDoc documentation for: `initial_chain_key`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "initial_chain_key",
      "target_lines": "L23-L25"
    },
    {
      "id": 3,
      "description": "Complete JSDoc documentation for: `mix_hash`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "mix_hash",
      "target_lines": "L32-L34"
    },
    {
      "id": 4,
      "description": "Deduplicate: `mix_key` duplicates `mix_key` in `rustguard-kmod/src/noise.rs`",
      "severity": "high",
      "effort": "small",
      "category": "refactor",
      "source": "duplication",
      "target_symbol": "mix_key",
      "target_lines": "L38-L41"
    },
    {
      "id": 5,
      "description": "Complete JSDoc documentation for: `mix_key`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "mix_key",
      "target_lines": "L38-L41"
    },
    {
      "id": 6,
      "description": "Complete JSDoc documentation for: `encrypt_and_hash`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "encrypt_and_hash",
      "target_lines": "L44-L48"
    },
    {
      "id": 7,
      "description": "Complete JSDoc documentation for: `decrypt_and_hash`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "decrypt_and_hash",
      "target_lines": "L51-L55"
    },
    {
      "id": 8,
      "description": "Deduplicate: `compute_mac1` duplicates `compute_mac1` in `rustguard-kmod/src/noise.rs`",
      "severity": "high",
      "effort": "small",
      "category": "refactor",
      "source": "duplication",
      "target_symbol": "compute_mac1",
      "target_lines": "L59-L65"
    },
    {
      "id": 9,
      "description": "Complete JSDoc documentation for: `compute_mac1`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "compute_mac1",
      "target_lines": "L59-L65"
    },
    {
      "id": 10,
      "description": "Complete JSDoc documentation for: `InitiatorHandshake`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "InitiatorHandshake",
      "target_lines": "L72-L81"
    },
    {
      "id": 11,
      "description": "Complete JSDoc documentation for: `create_initiation`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "create_initiation",
      "target_lines": "L85-L91"
    },
    {
      "id": 12,
      "description": "Complete JSDoc documentation for: `create_initiation_psk`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "create_initiation_psk",
      "target_lines": "L95-L104"
    },
    {
      "id": 13,
      "description": "Complete JSDoc documentation for: `create_initiation_with`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "create_initiation_with",
      "target_lines": "L108-L170"
    },
    {
      "id": 14,
      "description": "Complete JSDoc documentation for: `process_response`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "process_response",
      "target_lines": "L173-L221"
    },
    {
      "id": 15,
      "description": "Complete JSDoc documentation for: `process_initiation`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "process_initiation",
      "target_lines": "L227-L233"
    },
    {
      "id": 16,
      "description": "Complete JSDoc documentation for: `process_initiation_psk`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "process_initiation_psk",
      "target_lines": "L237-L246"
    },
    {
      "id": 17,
      "description": "Complete JSDoc documentation for: `process_initiation_with`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "process_initiation_with",
      "target_lines": "L249-L357"
    }
  ],
  "file_level": {
    "unused_imports": [],
    "circular_dependencies": [],
    "general_notes": "Best practices score: 9.5/10 (0 FAIL, 2 WARN, 10 PASS)"
  },
  "best_practices": {
    "score": 9.5,
    "rules": [
      {
        "rule_id": 1,
        "rule_name": "No unwrap in production code",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No .unwrap() calls exist in production paths. All fallible conversions use .expect() with descriptive messages (e.g., 'static is 48 bytes'), which is explicitly permitted by this rule. In-test .unwrap() calls are in #[cfg(test)] and are exempt."
      },
      {
        "rule_id": 2,
        "rule_name": "No unsafe blocks without justification",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No unsafe blocks are present anywhere in this file."
      },
      {
        "rule_id": 3,
        "rule_name": "Proper error handling with Result/Option",
        "status": "PASS",
        "severity": "HIGH",
        "detail": "All fallible paths that depend on external input return Option<T> and propagate failure via ?. The decrypt_and_hash return is properly threaded. Intentional discards like 'let _ = h;' and 'let (ck, _) = mix_key(...)' are protocol-defined (Noise pattern) and documented inline, not silent error ignores."
      },
      {
        "rule_id": 4,
        "rule_name": "Derive common traits on public types",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "InitiatorHandshake (pub) derives only Zeroize and ZeroizeOnDrop, omitting Debug, Clone, and PartialEq. While intentional omission of Clone (prevents accidental key duplication) and PartialEq (avoids non-constant-time comparison) is a sound security decision, the absence of a custom Debug impl means users cannot inspect the struct during development at all. A redacting Debug impl (masking key fields) would satisfy the rule's intent without leaking key material.",
        "lines": "L73-L81"
      },
      {
        "rule_id": 5,
        "rule_name": "Lifetime annotations explicit where needed",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "All functions return owned types ([u8; 32], Vec<u8>, Option<(...)>, tuples), so lifetime elision on input references is unambiguous and correct throughout. No hidden lifetime coupling exists between inputs and outputs."
      },
      {
        "rule_id": 6,
        "rule_name": "Clippy idioms",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "The single .clone() call (their_public.clone() at L161) is necessary: the struct takes ownership and the caller holds a reference, so a clone is required. No iterator chains are needed. No redundant borrowing patterns detected.",
        "lines": "L161"
      },
      {
        "rule_id": 7,
        "rule_name": "No panic! in library/production code",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No panic! macro invocations are present. The three .expect() calls in production paths ('static is 48 bytes', 'timestamp is 28 bytes', 'empty encrypted is 16 bytes') are on try_into() conversions from fixed-size cryptographic outputs whose sizes are enforced by the crypto layer — these are invariant assertions rather than user-input-dependent panics, and .expect() is explicitly permitted by rule 1."
      },
      {
        "rule_id": 8,
        "rule_name": "Module organization",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "The file is correctly placed in rustguard-core/src/handshake.rs. Internal helpers (initial_chain_key, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, constant_time_eq) are private. Public surface (compute_mac1, InitiatorHandshake, create_initiation*, process_response, process_initiation*) is cleanly separated. The initiator/responder split is clearly delineated with comments."
      },
      {
        "rule_id": 9,
        "rule_name": "Documentation comments on public items",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "All 9 exported symbols have /// doc comments. The module-level //! doc block explains the Noise_IKpsk2 pattern, the 1-RTT exchange, and the role of ck/h threading. Private helpers also have inline // comments explaining cryptographic purpose."
      },
      {
        "rule_id": 10,
        "rule_name": "No hardcoded secrets or credentials",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No hardcoded secrets are present. The &[0u8; 32] zero PSK in create_initiation and process_initiation is the WireGuard-specified 'no PSK' sentinel value (protocol-defined constant), not a credential."
      },
      {
        "rule_id": 11,
        "rule_name": "Memory safety",
        "status": "WARN",
        "severity": "HIGH",
        "detail": "InitiatorHandshake correctly derives ZeroizeOnDrop, ensuring ck, h, ephemeral, and psk are cleared on drop. However, in process_response the fields are copied out of the struct into local variables (let mut ck = state.ck; let psk = state.psk;). Because [u8; 32] is Copy, these stack locals hold live key material but are not wrapped in Zeroizing<T>, so they will not be scrubbed when the function returns. The original struct fields will still be zeroized via ZeroizeOnDrop, but the copies persist on the stack frame until overwritten by the OS/allocator.",
        "lines": "L176-L181"
      },
      {
        "rule_id": 12,
        "rule_name": "Concurrency safety",
        "status": "PASS",
        "severity": "HIGH",
        "detail": "No shared mutable state, no Arc/Mutex, no thread spawning. All functions are pure transforms over owned or immutably borrowed data. InitiatorHandshake is a single-use struct consumed by process_response (moved, not shared). No Send/Sync bounds are needed or violated."
      }
    ],
    "suggestions": [
      {
        "description": "Wrap sensitive local stack copies in Zeroizing<T> inside process_response to ensure they are cleared on scope exit, matching the intent of ZeroizeOnDrop on the parent struct.",
        "before": "let mut ck = state.ck;\nlet mut h = state.h;\nlet psk = state.psk;",
        "after": "use zeroize::Zeroizing;\nlet mut ck = Zeroizing::new(state.ck);\nlet mut h = Zeroizing::new(state.h);\nlet psk = Zeroizing::new(state.psk);"
      },
      {
        "description": "Add a custom Debug impl for InitiatorHandshake that redacts key material, preserving debuggability without leaking secrets.",
        "before": "#[derive(Zeroize, ZeroizeOnDrop)]\npub struct InitiatorHandshake { .. }",
        "after": "#[derive(Zeroize, ZeroizeOnDrop)]\npub struct InitiatorHandshake { .. }\n\nimpl core::fmt::Debug for InitiatorHandshake {\n    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {\n        f.debug_struct(\"InitiatorHandshake\")\n            .field(\"sender_index\", &self.sender_index)\n            .field(\"ck\", &\"[redacted]\")\n            .field(\"h\", &\"[redacted]\")\n            .field(\"psk\", &\"[redacted]\")\n            .finish()\n    }\n}"
      }
    ]
  },
  "docs_coverage": {
    "concepts": [],
    "score_pct": 42
  },
  "axis_meta": {
    "utility": {
      "model": "claude-haiku-4-5-20251001",
      "cost_usd": 0.061555799999999994,
      "duration_ms": 66582
    },
    "duplication": {
      "model": "claude-haiku-4-5-20251001",
      "cost_usd": 0.09922810000000001,
      "duration_ms": 49241
    },
    "correction": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.8403514999999999,
      "duration_ms": 459286
    },
    "overengineering": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.14762025,
      "duration_ms": 58692
    },
    "tests": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.17509525000000004,
      "duration_ms": 73441
    },
    "best_practices": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.26505775,
      "duration_ms": 145060
    },
    "documentation": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.237239,
      "duration_ms": 95786
    }
  }
}
```

## Source code of `rustguard-core/src/handshake.rs`

```typescript
//! WireGuard Noise_IKpsk2 handshake.
//!
//! The handshake is a 1-RTT key exchange:
//!   Initiator -> Responder: ephemeral, encrypted static, encrypted timestamp
//!   Responder -> Initiator: ephemeral, encrypted empty
//!
//! After this exchange both sides derive symmetric transport keys.
//! The chaining key (ck) and handshake hash (h) are threaded through
//! every operation — this is the Noise pattern in action.

use alloc::vec::Vec;
use rustguard_crypto::{
    self as crypto, EphemeralSecret, PublicKey, SharedSecret, StaticSecret, Tai64n,
    CONSTRUCTION, IDENTIFIER, LABEL_MAC1,
};
use zeroize::{Zeroize, ZeroizeOnDrop};

use crate::messages::{Initiation, Response};
use crate::session::TransportSession;

/// Precomputed initial handshake state from the protocol constants.
/// These are the same for every handshake — compute once.
fn initial_chain_key() -> [u8; 32] {
    crypto::hash(&[CONSTRUCTION])
}

fn initial_hash(ck: &[u8; 32]) -> [u8; 32] {
    crypto::hash(&[ck, IDENTIFIER])
}

/// Mix a public key into the hash. Used to bind the responder's identity.
fn mix_hash(h: &[u8; 32], data: &[u8]) -> [u8; 32] {
    crypto::hash(&[h, data])
}

/// Mix the result of a DH into the chaining key.
/// Returns (new_ck, key) where key is for AEAD encryption.
fn mix_key(ck: &[u8; 32], dh_result: &SharedSecret) -> ([u8; 32], [u8; 32]) {
    let (new_ck, key, _) = crypto::hkdf(ck, dh_result.as_bytes());
    (new_ck, key)
}

/// Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.
fn encrypt_and_hash(key: &[u8; 32], h: &[u8; 32], plaintext: &[u8]) -> ([u8; 32], Vec<u8>) {
    let ciphertext = crypto::seal(key, 0, h, plaintext);
    let new_h = mix_hash(h, &ciphertext);
    (new_h, ciphertext)
}

/// Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.
fn decrypt_and_hash(key: &[u8; 32], h: &[u8; 32], ciphertext: &[u8]) -> Option<([u8; 32], Vec<u8>)> {
    let plaintext = crypto::open(key, 0, h, ciphertext)?;
    let new_h = mix_hash(h, ciphertext);
    Some((new_h, plaintext))
}

/// Compute MAC1 over a message using the responder's public key.
/// MAC1 = BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)
pub fn compute_mac1(responder_public: &PublicKey, msg_bytes: &[u8]) -> [u8; 16] {
    let key = crypto::hash(&[LABEL_MAC1, responder_public.as_ref()]);
    let full_mac = crypto::mac(&key, &[msg_bytes]);
    let mut mac1 = [0u8; 16];
    mac1.copy_from_slice(&full_mac[..16]);
    mac1
}

// ── Initiator side ──────────────────────────────────────────────────

/// State held by the initiator between sending msg1 and receiving msg2.
/// Zeroized on drop to prevent key material from lingering in memory.
#[derive(Zeroize, ZeroizeOnDrop)]
pub struct InitiatorHandshake {
    ck: [u8; 32],
    h: [u8; 32],
    ephemeral: EphemeralSecret,
    #[zeroize(skip)]
    sender_index: u32,
    #[zeroize(skip)]
    their_public: PublicKey,
    psk: [u8; 32],
}

/// Create a handshake initiation message (std convenience — uses OsRng and system clock).
#[cfg(feature = "std")]
pub fn create_initiation(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
) -> (Initiation, InitiatorHandshake) {
    create_initiation_psk(our_static, their_public, sender_index, &[0u8; 32])
}

/// Create a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn create_initiation_psk(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
) -> (Initiation, InitiatorHandshake) {
    let ephemeral = EphemeralSecret::random();
    let timestamp = Tai64n::now();
    create_initiation_with(our_static, their_public, sender_index, psk, ephemeral, timestamp)
}

/// Create a handshake initiation with explicit ephemeral key and timestamp.
/// This is the core function — usable from both std and no_std.
pub fn create_initiation_with(
    our_static: &StaticSecret,
    their_public: &PublicKey,
    sender_index: u32,
    psk: &[u8; 32],
    ephemeral: EphemeralSecret,
    timestamp: Tai64n,
) -> (Initiation, InitiatorHandshake) {
    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix responder's public key into hash — both sides know this.
    h = mix_hash(&h, their_public.as_ref());

    let eph_public = ephemeral.public_key();

    // Mix ephemeral public into hash.
    h = mix_hash(&h, eph_public.as_ref());

    // DH(ephemeral, responder_static) -> mix into ck.
    let dh1 = ephemeral.diffie_hellman(their_public);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Encrypt our static public key.
    let our_public = our_static.public_key();
    let encrypted_static;
    (h, encrypted_static) = encrypt_and_hash(&key, &h, our_public.as_ref());

    // DH(our_static, responder_static) -> mix into ck.
    let dh2 = our_static.diffie_hellman(their_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Encrypt timestamp.
    let encrypted_timestamp;
    (h, encrypted_timestamp) = encrypt_and_hash(&key2, &h, timestamp.as_bytes());

    // Build message (mac1/mac2 computed over wire bytes).
    let mut msg = Initiation {
        sender_index,
        ephemeral: *eph_public.as_bytes(),
        encrypted_static: encrypted_static.try_into().expect("static is 48 bytes"),
        encrypted_timestamp: encrypted_timestamp.try_into().expect("timestamp is 28 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16], // No cookie mechanism yet.
    };

    // MAC1 is computed over everything before mac1 field.
    let wire = msg.to_bytes();
    msg.mac1 = compute_mac1(their_public, &wire[..116]);

    let state = InitiatorHandshake {
        ck,
        h,
        ephemeral,
        sender_index,
        their_public: their_public.clone(),
        psk: *psk,
    };

    (msg, state)
}

/// Process a handshake response and derive transport keys.
pub fn process_response(
    state: InitiatorHandshake,
    our_static: &StaticSecret,
    msg: &Response,
) -> Option<TransportSession> {
    // Access fields by reference — struct will zeroize on drop.
    let mut ck = state.ck;
    let mut h = state.h;
    let sender_index = state.sender_index;
    let psk = state.psk;

    // Verify receiver_index matches our sender_index.
    if msg.receiver_index != sender_index {
        return None;
    }

    let resp_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph.as_ref());

    // DH(our_ephemeral, responder_ephemeral)
    let dh1 = state.ephemeral.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh1);

    // DH(our_static, responder_ephemeral)
    let dh2 = our_static.diffie_hellman(&resp_eph);
    (ck, _) = mix_key(&ck, &dh2);

    // PSK phase (Noise_IKpsk2): mix PSK into chain.
    let (new_ck, t, key) = crypto::hkdf(&ck, &psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Decrypt empty payload.
    let result = decrypt_and_hash(&key, &h, &msg.encrypted_empty);
    let (h, _empty) = result?;
    let _ = h;

    // Derive transport keys: initiator sends with first, receives with second.
    let (send_key, recv_key, _) = crypto::hkdf(&ck, &[]);

    Some(TransportSession::new(
        sender_index,
        msg.sender_index,
        send_key,
        recv_key,
    ))
}

// ── Responder side ──────────────────────────────────────────────────

/// Process a handshake initiation on the responder side (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    process_initiation_psk(our_static, msg, responder_index, &[0u8; 32], None)
}

/// Process a handshake initiation with PSK (std convenience).
#[cfg(feature = "std")]
pub fn process_initiation_psk(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let resp_eph = EphemeralSecret::random();
    process_initiation_with(our_static, msg, responder_index, psk, last_timestamp, resp_eph)
}

/// Process a handshake initiation with explicit ephemeral (no_std compatible).
pub fn process_initiation_with(
    our_static: &StaticSecret,
    msg: &Initiation,
    responder_index: u32,
    psk: &[u8; 32],
    last_timestamp: Option<&Tai64n>,
    resp_eph: EphemeralSecret,
) -> Option<(PublicKey, Tai64n, Response, TransportSession)> {
    let our_public = our_static.public_key();

    // ── Verify MAC1 FIRST — cheap check before any DH. ──
    let wire = msg.to_bytes();
    let expected_mac1 = compute_mac1(&our_public, &wire[..116]);
    if !constant_time_eq(&msg.mac1, &expected_mac1) {
        return None;
    }
    // TODO: MAC2 / cookie validation under load.

    let mut ck = initial_chain_key();
    let mut h = initial_hash(&ck);

    // Mix our public key into hash (responder's identity).
    h = mix_hash(&h, our_public.as_ref());

    let init_eph = PublicKey::from_bytes(msg.ephemeral);

    // Mix initiator ephemeral into hash.
    h = mix_hash(&h, init_eph.as_ref());

    // DH(our_static, initiator_ephemeral)
    let dh1 = our_static.diffie_hellman(&init_eph);
    let key;
    (ck, key) = mix_key(&ck, &dh1);

    // Decrypt initiator's static public key.
    let (new_h, static_bytes) = decrypt_and_hash(&key, &h, &msg.encrypted_static)?;
    h = new_h;

    let initiator_public = PublicKey::from_bytes(
        static_bytes
            .as_slice()
            .try_into()
            .ok()?,
    );

    // DH(our_static, initiator_static)
    let dh2 = our_static.diffie_hellman(&initiator_public);
    let key2;
    (ck, key2) = mix_key(&ck, &dh2);

    // Decrypt timestamp.
    let (new_h, timestamp_bytes) = decrypt_and_hash(&key2, &h, &msg.encrypted_timestamp)?;
    h = new_h;

    // Validate timestamp is newer than last seen — prevents replay.
    let timestamp = Tai64n::from_bytes(
        timestamp_bytes.as_slice().try_into().ok()?,
    );
    if let Some(last) = last_timestamp {
        if !timestamp.is_after(last) {
            return None; // Replayed or stale initiation.
        }
    }

    // ── Build response ──

    let resp_eph_public = resp_eph.public_key();

    // Mix responder ephemeral into hash.
    h = mix_hash(&h, resp_eph_public.as_ref());

    // DH(resp_ephemeral, initiator_ephemeral)
    let dh3 = resp_eph.diffie_hellman(&init_eph);
    (ck, _) = mix_key(&ck, &dh3);

    // DH(resp_ephemeral, initiator_static)
    let dh4 = resp_eph.diffie_hellman(&initiator_public);
    (ck, _) = mix_key(&ck, &dh4);

    // PSK phase.
    let (new_ck, t, key3) = crypto::hkdf(&ck, psk);
    ck = new_ck;
    h = mix_hash(&h, &t);

    // Encrypt empty.
    let encrypted_empty;
    (h, encrypted_empty) = encrypt_and_hash(&key3, &h, &[]);
    let _ = h; // final hash

    // Derive transport keys: responder sends with second, receives with first.
    let (recv_key, send_key, _) = crypto::hkdf(&ck, &[]);

    let mut resp = Response {
        sender_index: responder_index,
        receiver_index: msg.sender_index,
        ephemeral: *resp_eph_public.as_bytes(),
        encrypted_empty: encrypted_empty.try_into().expect("empty encrypted is 16 bytes"),
        mac1: [0u8; 16],
        mac2: [0u8; 16],
    };

    // MAC1 on response.
    let resp_wire = resp.to_bytes();
    resp.mac1 = compute_mac1(&initiator_public, &resp_wire[..60]);

    let session = TransportSession::new(responder_index, msg.sender_index, send_key, recv_key);

    Some((initiator_public, timestamp, resp, session))
}

/// Constant-time comparison for MACs using the `subtle` crate.
fn constant_time_eq(a: &[u8; 16], b: &[u8; 16]) -> bool {
    use subtle::ConstantTimeEq;
    a.ct_eq(b).into()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn full_handshake_and_transport() {
        // Setup: both sides have static keys, initiator knows responder's public key.
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        // Step 1: Initiator creates handshake initiation.
        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        // Step 2: Responder processes initiation, creates response.
        let (peer_pubkey, _ts, resp_msg, mut resp_session) =
            process_initiation(&responder_static, &init_msg, 2)
                .expect("responder should accept initiation");

        // Responder correctly identifies the initiator.
        assert_eq!(peer_pubkey, initiator_static.public_key());

        // Step 3: Initiator processes response, gets transport session.
        let mut init_session =
            process_response(init_state, &initiator_static, &resp_msg)
                .expect("initiator should accept response");

        // Step 4: Both sides can now exchange encrypted data.
        let plaintext = b"hello from the trenches";
        let (counter, ciphertext) = init_session.encrypt(plaintext).unwrap();
        let decrypted = resp_session
            .decrypt(counter, &ciphertext)
            .expect("responder should decrypt");
        assert_eq!(&decrypted, plaintext);

        // And the other direction.
        let reply = b"copy that, over";
        let (counter, ciphertext) = resp_session.encrypt(reply).unwrap();
        let decrypted = init_session
            .decrypt(counter, &ciphertext)
            .expect("initiator should decrypt");
        assert_eq!(&decrypted, reply);
    }

    #[test]
    fn wrong_responder_key_rejects() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let wrong_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // A different responder (wrong key) tries to process the initiation.
        let result = process_initiation(&wrong_static, &init_msg, 2);
        assert!(result.is_none(), "wrong responder key should fail");
    }

    #[test]
    fn tampered_initiation_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (mut init_msg, _) = create_initiation(&initiator_static, &responder_public, 1);

        // Tamper with the encrypted static field.
        init_msg.encrypted_static[0] ^= 0xff;

        let result = process_initiation(&responder_static, &init_msg, 2);
        assert!(result.is_none(), "tampered initiation should fail");
    }

    #[test]
    fn tampered_response_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Tamper with the encrypted empty field.
        resp_msg.encrypted_empty[0] ^= 0xff;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "tampered response should fail");
    }

    #[test]
    fn mismatched_receiver_index_rejected() {
        let initiator_static = StaticSecret::random();
        let responder_static = StaticSecret::random();
        let responder_public = responder_static.public_key();

        let (init_msg, init_state) =
            create_initiation(&initiator_static, &responder_public, 1);

        let (_, _ts, mut resp_msg, _) =
            process_initiation(&responder_static, &init_msg, 2).unwrap();

        // Change receiver_index to wrong value.
        resp_msg.receiver_index = 999;

        let result = process_response(init_state, &initiator_static, &resp_msg);
        assert!(result.is_none(), "wrong receiver index should fail");
    }
}

```

## Instructions

Deliberate ONLY symbols with findings — skip clean symbols. For each symbol, address ALL its axis findings together in one entry (e.g., if a symbol is DEAD + NEEDS_FIX, deliberate both in the same entry). Recompute the verdict.

Rules:
- Only include axes with findings in original/deliberated (omit clean axes)
- Do NOT add new findings, symbols, or actions
- Protect ERROR (require ≥ 95 confidence to downgrade)
- Reclassification requires confidence ≥ 85
- Copy original values when you agree with the finding

## System (init)

**Model:** claude-opus-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{"verdict":"NEEDS_REFACTOR","symbols":[{"name":"initial_chain_key","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":85},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":85},"reasoning":"Tests WEAK is fair — private crypto primitive with no known-answer test vectors, only exercised transitively. Documentation PARTIAL is accurate — the doc comment is present but ambiguously shared with initial_hash. Both findings stand as-is."},{"name":"initial_hash","original":{"tests":"WEAK","documentation":"UNDOCUMENTED","confidence":85},"deliberated":{"tests":"WEAK","documentation":"UNDOCUMENTED","confidence":85},"reasoning":"Tests WEAK is valid — no isolated test with known Noise protocol test vectors. UNDOCUMENTED is correct — the preceding doc block belongs to initial_chain_key, not this function. Both findings confirmed."},{"name":"mix_hash","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":85},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":85},"reasoning":"Tests WEAK valid — heavily used but never tested in isolation. Documentation PARTIAL correct — the comment inaccurately says 'public key' when it accepts generic &[u8]. Both findings confirmed."},{"name":"mix_key","original":{"duplication":"DUPLICATE","tests":"WEAK","documentation":"PARTIAL","confidence":85},"deliberated":{"duplication":"UNIQUE","tests":"WEAK","documentation":"PARTIAL","confidence":90},"reasoning":"Duplication reclassified DUPLICATE → UNIQUE: rustguard-core (userspace, x25519-dalek types, SharedSecret wrapper, alloc-based) and rustguard-kmod (kernel module, raw byte arrays, C FFI) are fundamentally different compilation targets. Sharing code between a userspace Rust crate and a kernel module is architecturally infeasible — the type differences (SharedSecret vs raw bytes) reflect different abstraction layers, not accidental duplication. The parallel implementations are intentional. Tests WEAK and docs PARTIAL stand as originally assessed."},{"name":"encrypt_and_hash","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":72},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":72},"reasoning":"Tests WEAK valid — encrypt-then-hash ordering never verified in isolation. Documentation PARTIAL correct — single-line comment without parameter or return docs. Confidence stays at 72 due to limited visibility into full test suite coverage."},{"name":"decrypt_and_hash","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":72},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":72},"reasoning":"Tests WEAK valid — no test confirms hash is updated from ciphertext (not plaintext), which is a subtle correctness property. Documentation PARTIAL correct — Option return for auth failure undocumented. Both findings confirmed."},{"name":"compute_mac1","original":{"duplication":"DUPLICATE","tests":"WEAK","documentation":"PARTIAL","confidence":82},"deliberated":{"duplication":"UNIQUE","tests":"WEAK","documentation":"PARTIAL","confidence":88},"reasoning":"Duplication reclassified DUPLICATE → UNIQUE for the same reason as mix_key: rustguard-core and rustguard-kmod target fundamentally different environments (userspace vs kernel). The parameter type difference (PublicKey wrapper vs raw [u8; 32]) is not superficial — it reflects different type systems mandated by the compilation target. Cross-crate deduplication into a shared dependency would force the kernel module to depend on x25519-dalek types, which is architecturally wrong. Tests WEAK valid — no known-vector test for the 16-byte output. Documentation PARTIAL correct — public function lacks #Parameters/#Returns sections."},{"name":"InitiatorHandshake","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":80},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":80},"reasoning":"Tests WEAK valid — ZeroizeOnDrop guarantee untested, PSK field only tested with all-zeros. Documentation PARTIAL correct — lifecycle described but no usage examples or field docs. Both findings confirmed."},{"name":"create_initiation","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":88},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":88},"reasoning":"Tests WEAK valid — only tested with implicit zero PSK, never distinguished from create_initiation_psk. Documentation PARTIAL correct — the zero-PSK defaulting behavior is undocumented. Both findings confirmed."},{"name":"create_initiation_psk","original":{"overengineering":"ACCEPTABLE","tests":"WEAK","documentation":"PARTIAL","confidence":85},"deliberated":{"overengineering":"ACCEPTABLE","tests":"WEAK","documentation":"PARTIAL","confidence":85},"reasoning":"Overengineering ACCEPTABLE is correct — the three-tier delegation (plain → psk → with) separates std concerns (OsRng, system clock) from the deterministic core, which is sound design for a crypto library needing testability and no_std support. Tests WEAK valid — no non-zero PSK test path. Documentation PARTIAL correct — PSK semantics undocumented."},{"name":"create_initiation_with","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":78},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":78},"reasoning":"Tests WEAK valid — core function missing non-zero PSK test, deterministic ephemeral test, and mac1 boundary verification. Documentation PARTIAL correct — most complex public function with six undocumented parameters and no examples. Both findings confirmed."},{"name":"process_response","original":{"correction":"NEEDS_FIX","tests":"WEAK","documentation":"PARTIAL","confidence":76},"deliberated":{"correction":"NEEDS_FIX","tests":"WEAK","documentation":"PARTIAL","confidence":76},"reasoning":"Correction NEEDS_FIX is legitimate: the Response message carries a mac1 field (computed in process_initiation_with at line 354 over resp_wire[..60]), but process_response never verifies it. Since the Initiation's sender_index is transmitted in cleartext, an observer can forge Response messages with matching receiver_index, bypassing the only cheap guard (line 183) and forcing two DH operations. The responder side (process_initiation_with) correctly verifies MAC1 first — the asymmetry is a real oversight. This is DoS-grade, not authentication-breaking (AEAD would still reject), hence medium severity is appropriate. Tests WEAK valid — missing PSK mismatch and wrong-static-key scenarios. Documentation PARTIAL correct — Option return conditions undocumented."},{"name":"process_initiation","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":88},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":88},"reasoning":"Tests WEAK valid — always passes zero PSK and no last_timestamp, leaving replay-protection and PSK branches untested. Documentation PARTIAL correct — four-element return tuple unexplained. Both findings confirmed."},{"name":"process_initiation_psk","original":{"overengineering":"ACCEPTABLE","tests":"WEAK","documentation":"PARTIAL","confidence":85},"deliberated":{"overengineering":"ACCEPTABLE","tests":"WEAK","documentation":"PARTIAL","confidence":85},"reasoning":"Overengineering ACCEPTABLE correct — same three-tier rationale as create_initiation_psk, justified for std/no_std separation. Tests WEAK valid — PSK matching/mismatch and replay protection untested. Documentation PARTIAL correct — critical last_timestamp parameter undocumented."},{"name":"process_initiation_with","original":{"tests":"WEAK","documentation":"PARTIAL","confidence":77},"deliberated":{"tests":"WEAK","documentation":"PARTIAL","confidence":77},"reasoning":"Tests WEAK valid — timestamp replay protection (last_timestamp branch) is never exercised, PSK mismatch absent, tampered mac1/ephemeral/encrypted_timestamp fields untested. These are critical gaps for security-sensitive code. Documentation PARTIAL correct — largest public function with six undocumented parameters including the critical last_timestamp replay guard. Both findings confirmed."},{"name":"constant_time_eq","original":{"tests":"WEAK","documentation":"UNDOCUMENTED","confidence":80},"deliberated":{"tests":"WEAK","documentation":"UNDOCUMENTED","confidence":80},"reasoning":"Tests WEAK valid — only exercised via MAC1 mismatch path, no direct test with equal/near-equal arrays. UNDOCUMENTED correct — private crypto function with security rationale worth documenting (MAC comparison must not short-circuit). Both findings confirmed."}],"removed_actions":[4,8],"reasoning":"Overall assessment: This is a well-structured WireGuard Noise_IKpsk2 handshake implementation with correct cryptographic operations and good separation of concerns (std/no_std layers). Two key reclassifications: (1) mix_key and compute_mac1 DUPLICATE → UNIQUE — the parallel implementations in rustguard-core (userspace, x25519-dalek types) and rustguard-kmod (kernel module, raw bytes/C FFI) are architecturally necessary, not accidental duplication. Cross-crate deduplication would force inappropriate dependencies between fundamentally different compilation targets. Actions 4 and 8 removed accordingly. (2) process_response NEEDS_FIX confirmed — missing MAC1 verification on Response messages is a real DoS vector, as sender_index is plaintext-observable. The responder side correctly checks MAC1 first, making the asymmetry a clear oversight. Test coverage is consistently WEAK across all symbols — the tests cover happy paths and basic failure modes but lack known-answer vectors, PSK testing, replay-protection testing, and isolated primitive verification, which are important gaps for a cryptographic library. Documentation is consistently PARTIAL/UNDOCUMENTED — acceptable for private helpers but insufficient for the public API surface of a security-critical crate. Verdict remains NEEDS_REFACTOR driven by the process_response MAC1 gap and systematic test coverage weakness."}

## rate_limit_event

## Result (success)

**Cost:** $0.2267 | **Duration:** 85.7s


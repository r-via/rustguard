# Axis: utility

## User

## File: `rustguard-tun/src/xdp.rs`
## Language: rust

```rust
//! AF_XDP zero-copy UDP I/O for Linux.
//!
//! Replaces the standard UDP socket for receiving/sending WireGuard
//! encrypted packets. The plaintext side still uses TUN.
//!
//! Architecture:
//!   NIC -> XDP BPF filter (UDP:51820) -> AF_XDP socket -> decrypt -> TUN
//!   TUN -> encrypt -> AF_XDP TX ring -> NIC
//!
//! The unsafe is contained in this module. The ring buffer protocol is
//! lock-free (atomic producer/consumer indices on shared mmap'd memory).

#![cfg(target_os = "linux")]

use std::io;
use std::sync::atomic::{AtomicU32, Ordering};

// ── Constants ───────────────────────────────────────────────────────

const AF_XDP: i32 = 44;
const SOL_XDP: i32 = 283;

const XDP_UMEM_REG: i32 = 4;
const XDP_UMEM_FILL_RING: i32 = 5;
const XDP_UMEM_COMPLETION_RING: i32 = 6;
const XDP_RX_RING: i32 = 2;
const XDP_TX_RING: i32 = 3;
const XDP_MMAP_OFFSETS: i32 = 1;

const XDP_PGOFF_RX_RING: i64 = 0;
const XDP_PGOFF_TX_RING: i64 = 0x0_8000_0000;
const XDP_UMEM_PGOFF_FILL_RING: i64 = 0x1_0000_0000;
const XDP_UMEM_PGOFF_COMPLETION_RING: i64 = 0x1_8000_0000;

const XDP_USE_NEED_WAKEUP: u16 = 1 << 3;
const XDP_RING_NEED_WAKEUP: u32 = 1;

// ── Struct layouts (matching kernel uapi) ───────────────────────────

#[repr(C)]
struct XdpUmemReg {
    addr: u64,
    len: u64,
    chunk_size: u32,
    headroom: u32,
    flags: u32,
    tx_metadata_len: u32,
}

#[repr(C)]
#[derive(Clone, Copy)]
struct SockaddrXdp {
    family: u16,
    flags: u16,
    ifindex: u32,
    queue_id: u32,
    shared_umem_fd: u32,
}

#[repr(C)]
#[derive(Clone, Copy, Default)]
pub struct XdpDesc {
    pub addr: u64,
    pub len: u32,
    pub options: u32,
}

#[repr(C)]
#[derive(Default)]
struct XdpRingOffset {
    producer: u64,
    consumer: u64,
    desc: u64,
    flags: u64,
}

#[repr(C)]
#[derive(Default)]
struct XdpMmapOffsets {
    rx: XdpRingOffset,
    tx: XdpRingOffset,
    fr: XdpRingOffset,
    cr: XdpRingOffset,
}

// ── Ring buffer ─────────────────────────────────────────────────────

/// A single-producer/single-consumer ring over mmap'd memory.
struct Ring {
    producer: *const AtomicU32,
    consumer: *const AtomicU32,
    flags: *const AtomicU32,
    descs: *mut u8, // Either *mut XdpDesc or *mut u64 depending on ring type.
    mask: u32,
}

// Safety: Ring pointers come from mmap and are valid for the socket lifetime.
unsafe impl Send for Ring {}
unsafe impl Sync for Ring {}

impl Ring {
    fn producer(&self) -> u32 {
        unsafe { (*self.producer).load(Ordering::Acquire) }
    }

    fn consumer(&self) -> u32 {
        unsafe { (*self.consumer).load(Ordering::Acquire) }
    }

    fn set_producer(&self, val: u32) {
        unsafe { (*self.producer).store(val, Ordering::Release) }
    }

    fn set_consumer(&self, val: u32) {
        unsafe { (*self.consumer).store(val, Ordering::Release) }
    }

    fn needs_wakeup(&self) -> bool {
        unsafe { (*self.flags).load(Ordering::Relaxed) & XDP_RING_NEED_WAKEUP != 0 }
    }
}

// ── XDP Socket ──────────────────────────────────────────────────────

/// Configuration for creating an XDP socket.
pub struct XdpConfig {
    pub ifname: String,
    pub queue_id: u32,
    pub frame_size: u32,
    pub num_frames: u32,
    pub ring_size: u32,
}

impl Default for XdpConfig {
    fn default() -> Self {
        Self {
            ifname: "eth0".into(),
            queue_id: 0,
            frame_size: 4096,
            num_frames: 4096,
            ring_size: 2048,
        }
    }
}

/// An AF_XDP socket with UMEM and ring buffers.
/// Provides zero-copy packet I/O for the encrypted UDP side.
pub struct XdpSocket {
    fd: i32,
    umem: *mut u8,
    umem_size: usize,
    frame_size: u32,
    rx_ring: Ring,
    tx_ring: Ring,
    fill_ring: Ring,
    comp_ring: Ring,
    ring_size: u32,
    /// Free frame indices for TX.
    free_frames: Vec<u64>,
}

// Safety: XdpSocket owns its fd and mmap regions.
unsafe impl Send for XdpSocket {}

impl XdpSocket {
    /// Create and configure an AF_XDP socket.
    ///
    /// # Safety
    /// This function uses mmap to create shared memory regions with the kernel.
    /// The returned XdpSocket must not outlive the process.
    pub fn create(config: &XdpConfig) -> io::Result<Self> {
        unsafe { Self::create_inner(config) }
    }

    unsafe fn create_inner(config: &XdpConfig) -> io::Result<Self> {
        let umem_size = (config.frame_size * config.num_frames) as usize;

        // 1. Allocate UMEM.
        let umem = libc::mmap(
            std::ptr::null_mut(),
            umem_size,
            libc::PROT_READ | libc::PROT_WRITE,
            libc::MAP_PRIVATE | libc::MAP_ANONYMOUS | libc::MAP_POPULATE,
            -1,
            0,
        );
        if umem == libc::MAP_FAILED {
            return Err(io::Error::last_os_error());
        }
        let umem = umem as *mut u8;

        // 2. Create socket.
        let fd = libc::socket(AF_XDP, libc::SOCK_RAW, 0);
        if fd < 0 {
            libc::munmap(umem as *mut _, umem_size);
            return Err(io::Error::last_os_error());
        }

        // 3. Register UMEM.
        let reg = XdpUmemReg {
            addr: umem as u64,
            len: umem_size as u64,
            chunk_size: config.frame_size,
            headroom: 0,
            flags: 0,
            tx_metadata_len: 0,
        };
        if setsockopt_raw(fd, SOL_XDP, XDP_UMEM_REG, &reg) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 4. Set ring sizes.
        let rs = config.ring_size as i32;
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);

        // 5. Query mmap offsets.
        let mut offsets = XdpMmapOffsets::default();
        let mut optlen = std::mem::size_of::<XdpMmapOffsets>() as u32;
        if libc::getsockopt(
            fd,
            SOL_XDP,
            XDP_MMAP_OFFSETS,
            &mut offsets as *mut _ as *mut _,
            &mut optlen,
        ) < 0
        {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 6. mmap rings.
        let rx_map = mmap_ring(fd, &offsets.rx, config.ring_size, XDP_PGOFF_RX_RING, std::mem::size_of::<XdpDesc>())?;
        let tx_map = mmap_ring(fd, &offsets.tx, config.ring_size, XDP_PGOFF_TX_RING, std::mem::size_of::<XdpDesc>())?;
        let fill_map = mmap_ring(fd, &offsets.fr, config.ring_size, XDP_UMEM_PGOFF_FILL_RING, 8)?;
        let comp_map = mmap_ring(fd, &offsets.cr, config.ring_size, XDP_UMEM_PGOFF_COMPLETION_RING, 8)?;

        let mask = config.ring_size - 1;
        let rx_ring = make_ring(rx_map, &offsets.rx, mask);
        let tx_ring = make_ring(tx_map, &offsets.tx, mask);
        let fill_ring = make_ring(fill_map, &offsets.fr, mask);
        let comp_ring = make_ring(comp_map, &offsets.cr, mask);

        // 7. Pre-populate fill ring (first half of frames for RX).
        let rx_frames = config.num_frames / 2;
        let mut prod = fill_ring.producer();
        for i in 0..rx_frames {
            let addr_ptr = fill_ring.descs.add(((prod & mask) as usize) * 8) as *mut u64;
            *addr_ptr = (i * config.frame_size) as u64;
            prod += 1;
        }
        fill_ring.set_producer(prod);

        // Second half of frames are free for TX.
        let mut free_frames = Vec::with_capacity((config.num_frames - rx_frames) as usize);
        for i in rx_frames..config.num_frames {
            free_frames.push((i * config.frame_size) as u64);
        }

        // 8. Bind.
        let ifindex = if_nametoindex(&config.ifname)?;
        let sxdp = SockaddrXdp {
            family: AF_XDP as u16,
            flags: XDP_USE_NEED_WAKEUP,
            ifindex,
            queue_id: config.queue_id,
            shared_umem_fd: 0,
        };
        if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        Ok(Self {
            fd,
            umem,
            umem_size,
            frame_size: config.frame_size,
            rx_ring,
            tx_ring,
            fill_ring,
            comp_ring,
            ring_size: config.ring_size,
            free_frames,
        })
    }

    /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
    /// Caller must call `rx_release` after processing to return frames to the fill ring.
    pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
        let prod = self.rx_ring.producer();
        let cons = self.rx_ring.consumer();
        let available = prod.wrapping_sub(cons);

        let mut packets = Vec::with_capacity(available as usize);
        for i in 0..available {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let desc = unsafe {
                &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
            };
            let data = unsafe {
                std::slice::from_raw_parts(
                    self.umem.add(desc.addr as usize),
                    desc.len as usize,
                )
            };
            packets.push((desc.addr, data));
        }
        packets
    }

    /// Release consumed RX frames back to the fill ring.
    pub fn rx_release(&mut self, frames: &[u64]) {
        // Update RX consumer.
        let cons = self.rx_ring.consumer().wrapping_add(frames.len() as u32);
        self.rx_ring.set_consumer(cons);

        // Add frames back to fill ring.
        let mut prod = self.fill_ring.producer();
        for &addr in frames {
            let idx = (prod & (self.ring_size - 1)) as usize;
            unsafe {
                let ptr = self.fill_ring.descs.add(idx * 8) as *mut u64;
                *ptr = addr;
            }
            prod = prod.wrapping_add(1);
        }
        self.fill_ring.set_producer(prod);
    }

    /// Send a packet via the TX ring. Returns false if no frames available.
    pub fn tx_send(&mut self, data: &[u8]) -> bool {
        let Some(frame_addr) = self.free_frames.pop() else {
            return false;
        };

        // Copy data into UMEM frame.
        unsafe {
            std::ptr::copy_nonoverlapping(
                data.as_ptr(),
                self.umem.add(frame_addr as usize),
                data.len(),
            );
        }

        // Write TX descriptor.
        let prod = self.tx_ring.producer();
        let idx = (prod & (self.ring_size - 1)) as usize;
        unsafe {
            let desc = &mut *(self.tx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *mut XdpDesc);
            desc.addr = frame_addr;
            desc.len = data.len() as u32;
            desc.options = 0;
        }
        self.tx_ring.set_producer(prod.wrapping_add(1));

        // Kick kernel if needed.
        if self.tx_ring.needs_wakeup() {
            unsafe {
                libc::sendto(self.fd, std::ptr::null(), 0, libc::MSG_DONTWAIT, std::ptr::null(), 0);
            }
        }

        true
    }

    /// Reclaim completed TX frames.
    pub fn tx_complete(&mut self) {
        let prod = self.comp_ring.producer();
        let cons = self.comp_ring.consumer();
        let done = prod.wrapping_sub(cons);

        for i in 0..done {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let addr = unsafe { *(self.comp_ring.descs.add(idx * 8) as *const u64) };
            self.free_frames.push(addr);
        }
        self.comp_ring.set_consumer(cons.wrapping_add(done));
    }

    /// Raw file descriptor (for poll/epoll).
    pub fn fd(&self) -> i32 {
        self.fd
    }
}

impl Drop for XdpSocket {
    fn drop(&mut self) {
        unsafe {
            libc::close(self.fd);
            libc::munmap(self.umem as *mut _, self.umem_size);
        }
    }
}

// ── Helpers ─────────────────────────────────────────────────────────

unsafe fn setsockopt_raw<T>(fd: i32, level: i32, optname: i32, val: &T) -> i32 {
    libc::setsockopt(
        fd,
        level,
        optname,
        val as *const _ as *const _,
        std::mem::size_of::<T>() as u32,
    )
}

unsafe fn mmap_ring(
    fd: i32,
    off: &XdpRingOffset,
    ring_size: u32,
    pgoff: i64,
    desc_size: usize,
) -> io::Result<*mut u8> {
    let size = off.desc as usize + (ring_size as usize) * desc_size;
    let ptr = libc::mmap(
        std::ptr::null_mut(),
        size,
        libc::PROT_READ | libc::PROT_WRITE,
        libc::MAP_SHARED | libc::MAP_POPULATE,
        fd,
        pgoff,
    );
    if ptr == libc::MAP_FAILED {
        return Err(io::Error::last_os_error());
    }
    Ok(ptr as *mut u8)
}

unsafe fn make_ring(base: *mut u8, off: &XdpRingOffset, mask: u32) -> Ring {
    Ring {
        producer: base.add(off.producer as usize) as *const AtomicU32,
        consumer: base.add(off.consumer as usize) as *const AtomicU32,
        flags: base.add(off.flags as usize) as *const AtomicU32,
        descs: base.add(off.desc as usize),
        mask,
    }
}

pub fn if_nametoindex(name: &str) -> io::Result<u32> {
    let cname = std::ffi::CString::new(name).map_err(|_| {
        io::Error::new(io::ErrorKind::InvalidInput, "invalid interface name")
    })?;
    let idx = unsafe { libc::if_nametoindex(cname.as_ptr()) };
    if idx == 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(idx)
}

```

## Symbols to evaluate

- constant AF_XDP (L20–L20)
- constant SOL_XDP (L21–L21)
- constant XDP_UMEM_REG (L23–L23)
- constant XDP_UMEM_FILL_RING (L24–L24)
- constant XDP_UMEM_COMPLETION_RING (L25–L25)
- constant XDP_RX_RING (L26–L26)
- constant XDP_TX_RING (L27–L27)
- constant XDP_MMAP_OFFSETS (L28–L28)
- constant XDP_PGOFF_RX_RING (L30–L30)
- constant XDP_PGOFF_TX_RING (L31–L31)
- constant XDP_UMEM_PGOFF_FILL_RING (L32–L32)
- constant XDP_UMEM_PGOFF_COMPLETION_RING (L33–L33)
- constant XDP_USE_NEED_WAKEUP (L35–L35)
- constant XDP_RING_NEED_WAKEUP (L36–L36)
- class XdpUmemReg (L41–L48)
- class SockaddrXdp (L52–L58)
- export class XdpDesc (L62–L66)
- class XdpRingOffset (L70–L75)
- class XdpMmapOffsets (L79–L84)
- class Ring (L89–L95)
- export class XdpConfig (L126–L132)
- export class XdpSocket (L148–L160)
- function setsockopt_raw (L407–L415)
- function mmap_ring (L417–L437)
- function make_ring (L439–L447)
- export function if_nametoindex (L449–L458)

## Pre-computed Import Analysis

- AF_XDP (not exported): internal only — check local usage in file
- SOL_XDP (not exported): internal only — check local usage in file
- XDP_UMEM_REG (not exported): internal only — check local usage in file
- XDP_UMEM_FILL_RING (not exported): internal only — check local usage in file
- XDP_UMEM_COMPLETION_RING (not exported): internal only — check local usage in file
- XDP_RX_RING (not exported): internal only — check local usage in file
- XDP_TX_RING (not exported): internal only — check local usage in file
- XDP_MMAP_OFFSETS (not exported): internal only — check local usage in file
- XDP_PGOFF_RX_RING (not exported): internal only — check local usage in file
- XDP_PGOFF_TX_RING (not exported): internal only — check local usage in file
- XDP_UMEM_PGOFF_FILL_RING (not exported): internal only — check local usage in file
- XDP_UMEM_PGOFF_COMPLETION_RING (not exported): internal only — check local usage in file
- XDP_USE_NEED_WAKEUP (not exported): internal only — check local usage in file
- XDP_RING_NEED_WAKEUP (not exported): internal only — check local usage in file
- XdpUmemReg (not exported): internal only — check local usage in file
- SockaddrXdp (not exported): internal only — check local usage in file
- XdpDesc (exported): imported by 0 files — LIKELY DEAD
- XdpRingOffset (not exported): internal only — check local usage in file
- XdpMmapOffsets (not exported): internal only — check local usage in file
- Ring (not exported): internal only — check local usage in file
- XdpConfig (exported): imported by 0 files — LIKELY DEAD
- XdpSocket (exported): imported by 0 files — LIKELY DEAD
- setsockopt_raw (not exported): internal only — check local usage in file
- mmap_ring (not exported): internal only — check local usage in file
- make_ring (not exported): internal only — check local usage in file
- if_nametoindex (exported): imported by 0 files — LIKELY DEAD

Evaluate the utility of each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] TransportSession: utility DEAD → USED**: Deliberation reclassified: UTILITY DEAD→USED: This is a `pub struct` in a `-core` library crate (`rustguard-core`). Library crate public types are consumed by downstream crates in the workspace (e.g., `rustguard`). The utility evaluator likely only searched within the same crate and missed cross-crate imports, a classic false positive for workspace library crates. Reclassified to USED. TESTS NONE→WEAK: The merged field says NONE but the detail text itself says WEAK, and the source clearly contains 6 test functions in a `#[cfg(test)]` module covering encrypt/decrypt roundtrip, counter monotonicity, wrong-counter rejection, replay rejection, out-of-order delivery, and bidirectional use. The WEAK assessment is accurate: `encrypt_to` and `decrypt_in_place` (separate code paths using `seal_to`/`open_to`) have zero coverage, and nonce exhaustion / edge cases are untested. DOCUMENTATION UNDOCUMENTED→DOCUMENTED: The detail text itself concludes DOCUMENTED. The struct has a thorough `///` block (lines 7–11) explaining purpose, derivation, and key-role convention. Both public fields and all private fields carry `///` comments. Four of six methods have doc comments. Two public items (`new`, `send_counter`) lack doc comments (noted in best_practices rule 9), but the overall coverage is well above UNDOCUMENTED. The struct-level and field-level documentation is comprehensive.
- **[deliberation] COOKIE_LEN: utility DEAD → USED**: Deliberation reclassified: Utility DEAD reclassified → USED: this is a `pub const` in a library crate (`rustguard-core`). Downstream crates (e.g., message layout code) depend on it; pre-computed analysis likely doesn't capture cross-crate workspace imports. The entire cookie module is clearly used infrastructure for a WireGuard implementation. Tests NONE contradicts the detail text which says '[GOOD] Pure constant. Used throughout and indirectly validated in every roundtrip test.' For a pure constant with no runtime behavior, indirect validation is sufficient — reclassified to GOOD. Documentation UNDOCUMENTED is wrong — line 23 has `/// Cookie size.` which IS a doc comment, making this PARTIAL (brief but present).
- **[deliberation] CookieChecker: utility DEAD → USED**: Deliberation reclassified: Utility DEAD reclassified → USED: CookieChecker is the primary server-side public API of this module in a library crate. Pre-computed cross-crate analysis likely misses workspace-level imports. The entire module exists to support WireGuard cookie DoS protection — this struct is clearly intended for use by consuming crates. Tests NONE contradicts the detail text which says 'happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests' — reclassified to WEAK (tested but with gaps in verify_mac1, rotation, new_with). Documentation UNDOCUMENTED contradicts detail: 'Struct has a /// summary and all four fields have field-level /// comments.' Line 49 has a doc comment. Reclassified to PARTIAL (docs exist but missing examples and constructor docs).
- **[deliberation] CookieState: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX kept: unlike elapsed_since, the consequence here is that process_reply successfully decrypts a cookie but never marks it as fresh, making compute_mac2 silently return zeros on no_std. While the overall no_std design is intentional, this specific behavior is more insidious — a successful decryption followed by silent failure to use the cookie is a subtle trap with no comment explaining it. Utility DEAD → USED: same reasoning as CookieChecker — primary client-side public API in a library crate. Tests NONE → WEAK: detail says 'Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie.' Documentation UNDOCUMENTED → PARTIAL: detail says 'Struct has a /// summary and both private fields have doc comments.'
- **[deliberation] ReplayWindow: utility DEAD → USED**: Deliberation reclassified: CORRECTION ERROR confirmed at high confidence: shift_window's bit-shift block (L112-116) iterates in reverse with right-shift (>>), moving bits toward lower ages instead of higher ages. Verified with concrete example: top=0, bitmap[0]=1, then shift_window(1) right-shifts bitmap[0] to 0, losing the counter-0 mark. A subsequent check(0) would incorrectly accept the replay. The word-level copy_within is correct; only the sub-word bit-shift is inverted. Tests don't catch this because they never re-check a previously-seen counter after a shift has occurred. UTILITY reclassified DEAD→USED: this is a pub struct in a library crate (rustguard-core). Library public API types are not dead simply because no in-tree consumer is visible — the struct is the module's primary export and is extensively exercised in tests. TESTS reclassified NONE→WEAK: the detail text itself says '[WEAK]' and describes solid behavioral coverage via check_and_update (sequential, duplicate rejection, out-of-order, boundary, stress). However, check() and update() are never tested independently, and the critical shift_window bit-direction bug goes undetected due to test design gaps. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the struct's two fields have /// doc comments (L15, L17), and all three public methods have /// doc comments. Only the struct-level definition and new() constructor lack /// docs. This is clearly partial, not absent.
- **[deliberation] hash: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: this is a library crate's public API. Exported functions in a crypto library ARE the product — absence of in-workspace importers does not make them dead. The crate is designed to be consumed externally (rustguard-crypto). Tests NONE contradicts the detail text which describes three existing tests (hash_deterministic, hash_different_inputs, hash_multi_chunk); reclassified to WEAK since tests exist but lack reference vectors. Documentation UNDOCUMENTED contradicts the detail and source: line 8 has `/// BLAKE2s-256 hash.` — it's PARTIAL (brief but present, missing params/examples).
- **[deliberation] mac: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive for the same library-crate reason as hash. Tests NONE contradicts the detail text which describes mac_differs_with_key and indirect coverage via hmac_differs_from_keyed_blake2s; reclassified to WEAK (tests exist but no reference vectors). Documentation UNDOCUMENTED contradicts the source: lines 17-20 have a multi-line /// block explaining keyed MAC vs HMAC distinction. Reclassified to PARTIAL per the detail's own assessment.
- **[deliberation] hkdf: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: hkdf is a core public function of a crypto library crate, essential for the WireGuard KDF. Tests NONE contradicts the detail which describes two tests (hkdf_produces_different_outputs, hkdf_deterministic); reclassified to WEAK since no RFC 5869/WireGuard spec test vectors are present. Documentation UNDOCUMENTED contradicts source: lines 31-36 have a substantial /// block describing the algorithm and return semantics. Reclassified to PARTIAL.
- **[deliberation] AEAD_TAG_LEN: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: This is a `pub const` in a library crate (`rustguard-crypto`). It is the fundamental tag length constant meant for downstream consumers. The evaluator itself noted it matches Known False Positives. Zero in-crate importers is expected for a library public API constant. Tests NONE→GOOD: Pure compile-time constant with no runtime behavior; indirectly validated in `seal_then_open` assertion (`ciphertext.len() == plaintext.len() + AEAD_TAG_LEN`). No dedicated test needed. Documentation UNDOCUMENTED confirmed: line 7 has no `///` comment, so UNDOCUMENTED is correct.
- **[deliberation] MAX_PACKET_SIZE: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the doc comment on L9 says 'MTU 1420 + tag' but the expression computes `1500 + AEAD_TAG_LEN = 1516`, a clear mismatch (~80 bytes difference). Either the doc or the value is wrong. Utility DEAD→USED: library public constant for downstream consumption, same false positive pattern as AEAD_TAG_LEN. Tests NONE→GOOD: compile-time constant with no runtime behavior, no dedicated test required. Documentation UNDOCUMENTED→PARTIAL: line 9 has a `///` doc comment present (`/// Maximum WireGuard transport payload (MTU 1420 + tag).`), so UNDOCUMENTED is incorrect. Reclassified to PARTIAL due to the inaccurate MTU value in the doc.
- **[deliberation] seal: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: primary public encryption API of the library crate, consumed by downstream crates. Tests NONE→WEAK: `seal` is directly called in all 5 test functions (seal_then_open, wrong_key_fails, wrong_counter_fails, tampered_ciphertext_fails, aad_mismatch_fails), so NONE is factually incorrect. Reclassified to WEAK since edge cases (empty plaintext, large payloads, non-zero counter as primary input) are missing. Documentation UNDOCUMENTED→PARTIAL: has a multi-line `///` doc block on L12-15 describing purpose, nonce construction, and AAD semantics. Missing param-level docs and examples, hence PARTIAL not DOCUMENTED.
- **[deliberation] open: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: primary public decryption API of the library crate. Tests NONE→GOOD: five tests thoroughly exercise `open` — happy path, wrong key, wrong counter, tampered ciphertext, and AAD mismatch. All four primary authentication-failure modes are covered. NONE is clearly wrong. Documentation UNDOCUMENTED→PARTIAL: has a `///` block on L31-33 describing purpose and three failure conditions for `None` return. Missing param-level docs and examples, hence PARTIAL.
- **[deliberation] xseal: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: public API variant for WireGuard cookie encryption, consumed by downstream crates. Tests NONE confirmed: no test covers xseal — this is a genuine gap on a security-critical path. Documentation UNDOCUMENTED→PARTIAL: has a single-line `///` on L48 noting algorithm, nonce size, and use case. Minimal but present, so UNDOCUMENTED is incorrect.
- **[deliberation] xopen: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: public API variant for WireGuard cookie decryption, consumed by downstream crates. Tests NONE confirmed: no test covers xopen — roundtrip, wrong key, tampered data, and nonce mismatch all untested. Documentation UNDOCUMENTED→PARTIAL: has a single-line `///` on L57 noting algorithm, nonce size, and use case. Present but lacks param docs and return semantics.
- **[deliberation] seal_to: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: zero-allocation public API for hot-path transport encryption, consumed by downstream crates. Tests NONE confirmed: no test covers seal_to — output length, correct tag placement, and buffer semantics all unverified. Documentation UNDOCUMENTED→PARTIAL: has a three-line `///` block on L66-68 describing purpose, buffer precondition, and zero-allocation property. Missing `# Panics` section for `.expect()` and param-level docs.
- **[deliberation] open_to: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the function guards `ct_len < AEAD_TAG_LEN` (defensive intent) but never checks `ct_len > buf.len()`. Since `buf[..ct_len]` on L91 will panic if ct_len exceeds buf length, and this function processes potentially adversarial input while advertising Option return, the missing upper-bound guard is a genuine inconsistency. Utility DEAD→USED: zero-allocation public API for hot-path transport decryption. Tests NONE confirmed: completely untested. Documentation UNDOCUMENTED→PARTIAL: has a three-line `///` block on L81-83 describing purpose, buffer contract, and zero-allocation guarantee.
- **[deliberation] Tai64n: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: This is a `pub struct` in `rustguard-crypto`, a library crate whose entire purpose is to export types for cross-crate consumption. The evaluator itself noted this false-positive pattern and reduced confidence. Cross-crate imports were outside analysis scope, and similar symbols (TransportSession, CookieChecker) were previously misclassified. Reclassified with high confidence. Tests NONE → WEAK: The symbol field says NONE but the detail section itself begins with '[WEAK] Three inline tests cover monotonicity...' — there are clearly three tests in the `mod tests` block (L50-L80). They exist but only check roundtrip equality, never assert against known-correct byte encodings. WEAK is the accurate classification. Documentation UNDOCUMENTED → PARTIAL: Lines 4-7 contain four `///` doc comments on the struct covering purpose, byte layout, WireGuard use-case, and replay-monotonicity invariant. The detail section even states 'Struct carries four lines of /// doc comments — solid struct-level narrative.' The struct is documented; only `as_bytes` and `from_bytes` methods lack docs. PARTIAL is correct.
- **[deliberation] KEEPALIVE_TIMEOUT: utility DEAD → LOW_VALUE**: Deliberation reclassified: Utility DEAD → LOW_VALUE: this is an exported WireGuard protocol constant (§6.3). The needs_keepalive method deliberately takes keepalive_interval as an Option<Duration> parameter, making KEEPALIVE_TIMEOUT the canonical default value for external callers to pass in. For a library crate, exported protocol constants serve as public API even without internal usage — downstream consumers may use it. Removing it would break the API contract. Tests NONE: stands — no test references it. Documentation: the source clearly has '/// Keepalive interval when configured.' on L51, and the detail text itself says '[DOCUMENTED]'. The UNDOCUMENTED classification contradicts the evaluator's own detail — reclassify to DOCUMENTED.
- **[deliberation] Peer: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: The detail itself acknowledges this matches a known false-positive pattern for exported structs in workspace crates. `Peer` is the fundamental domain struct for a WireGuard daemon — it is inconceivable that a daemon named `rustguard-daemon` does not use its own Peer representation. The pre-analysis hedged with 'LIKELY DEAD', and the analysis simply lacked visibility into sibling modules (device.rs, handshake.rs, etc.) that inevitably consume this type. Reclassified with high confidence. Tests NONE: Agreed — no tests exist for this module or its methods (from_config, allows_ip, has_active_session). Kept as-is. Documentation UNDOCUMENTED → PARTIAL: The source clearly shows a struct-level `///` doc comment at L15, plus field-level `///` docs on `session` (L22) and `timers` (L24). Five fields remain undocumented and `from_config` lacks a doc comment, so coverage is incomplete but not absent. PARTIAL is the correct classification per the detail's own description.
- **[deliberation] run: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX confirmed: (1) MSG_RESPONSE peer-matching at L296-301 uses a heuristic that breaks with multiple peers — the first peer without an active session is selected regardless of which peer the handshake was for. (2) Timer keepalive nonce exhaustion at ~L340 continues without clearing the session, preventing rekeying. Both are real bugs visible in the source. Utility reclassified DEAD→USED: this is `pub fn run()`, the sole public entry point of the daemon tunnel module. It is almost certainly called from main.rs or a binary crate entry point. The pre-computed analysis likely did not trace binary crate call graphs. A pub fn named 'run' in a daemon module being dead code would be nonsensical. Overengineering ACCEPTABLE kept — large but justified orchestration function. Tests NONE accurate. Documentation reclassified UNDOCUMENTED→PARTIAL: the function does have a /// doc comment ('Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.') as visible at L48, so it is not fully undocumented; it is partial (missing parameter/error docs).
- **[deliberation] is_open: utility DEAD → USED**: Deliberation reclassified: Utility reclassified DEAD → USED. This is a library crate, and `is_open` is a natural part of the enrollment window API (`open_window`, `close_window`, `remaining`, `is_open`). The same zero-importer pattern applies to `send_command`, `start_listener`, and `cleanup` which are all classified USED under the library-crate pattern. Removing `is_open` would leave the API incomplete — callers would have to call `remaining() > 0` instead. Tests NONE and documentation PARTIAL kept as-is.
- **[deliberation] JoinConfig: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: JoinConfig is a public API struct serving as the sole parameter to the crate's primary entry point `run()`. The DEAD classification is a systematic false positive from cross-crate analysis not capturing workspace-internal consumers. This matches the documented false-positive pattern for library crate public types. Tests NONE → GOOD: The evaluator's own detail text says 'Per rule 6, types/interfaces/structs with no runtime behavior are GOOD by default' — this is a plain data struct with two fields and no methods, so GOOD is correct. Documentation UNDOCUMENTED kept: valid finding, public config type lacks /// doc comments and field descriptions.
- **[deliberation] ParsedUdp: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: This is a pub struct in a library crate returned by the public parse_eth_udp function. The original detail itself acknowledges this matches the Known False Positives pattern for library crate exports consumed by downstream crates. Tests NONE → WEAK: The detail text itself states 'The struct's two fields (src_addr, payload) are exercised by the single inline test parse_ipv4_udp_frame, which asserts both field values.' This is partial coverage (IPv4 path only, no IPv6), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Struct has a /// doc comment on L8' — the struct-level doc exists on line 8. Only the per-field docs are missing. That is PARTIAL, not UNDOCUMENTED.
- **[deliberation] parse_eth_udp: utility DEAD → USED**: Deliberation reclassified: Utility DEAD → USED: Public function in a library crate serving as the main entry point for Ethernet frame parsing. The original detail itself notes it matches the Known False Positives pattern for library crate public APIs consumed by downstream crates not visible in same-crate import analysis. Tests NONE → WEAK: The detail states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path through parse_eth_udp.' This is exercised but with limited coverage (no IPv6 branch, no error paths), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Has a two-line /// block (L14–L15) describing purpose and the None return condition.' A doc comment exists; it merely lacks #Examples and parameter descriptions. That is PARTIAL.
- **[deliberation] BATCH_SIZE: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: BATCH_SIZE is actively used within this file in RecvBatch struct field types (L20-22), for-loop bounds, and array initializations in recv_batch. It is part of the module's coherent public API. Static analysis missed intra-module usage. Documentation UNDOCUMENTED→PARTIAL: Source clearly shows `/// Maximum batch size for recvmmsg/sendmmsg.` at L13; the detail text itself states PARTIAL. The top-level UNDOCUMENTED classification contradicts the detail. Tests NONE kept: factual, though low concern for a compile-time constant.
- **[deliberation] PKT_BUF_SIZE: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: PKT_BUF_SIZE is used in RecvBatch struct definition (L20) and recv_batch buffer allocation (L50, L113). Actively consumed within the file. Documentation UNDOCUMENTED→PARTIAL: Source shows `/// Per-packet buffer size.` at L15; detail text confirms PARTIAL. Tests NONE kept: factual, low concern for a constant.
- **[deliberation] RecvBatch: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: RecvBatch is used as parameter type in both recv_batch function signatures (L40, L113). It is the core data structure of this module's API. Documentation UNDOCUMENTED→PARTIAL: Source has `/// A batch of received packets.` at L18; detail text confirms PARTIAL (struct doc exists but fields lack individual docs). Tests NONE kept: struct and constructor have zero coverage, which is a legitimate gap for a public type with unsafe-adjacent usage patterns.
- **[deliberation] recv_batch: utility DEAD → USED**: Deliberation reclassified: This is the macOS recv_batch at L112-L126. Utility DEAD→USED: macOS fallback is the platform-specific implementation consumers will call; cfg-gating makes static import analysis unreliable. Documentation UNDOCUMENTED→PARTIAL: Source shows `/// macOS fallback: receive one packet at a time.` at L110; detail text confirms PARTIAL. Tests NONE kept: WouldBlock and single-packet paths need coverage.
- **[deliberation] send_packet: utility DEAD → USED**: Deliberation reclassified: This is the macOS send_packet at L129-L131. Utility DEAD→USED: macOS platform counterpart, same reasoning as other cfg-gated functions. Overengineering ACCEPTABLE kept: API symmetry justified. Documentation UNDOCUMENTED kept: this is the one send_packet variant that truly has no doc comment (L128 is just #[cfg], no /// above it). Tests NONE kept: trivial wrapper.
- **[deliberation] PersistedPeer: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: PersistedPeer is a pub struct in a library crate (rustguard-enroll) intended to be consumed by other workspace crates. Zero in-file importers is expected for a public API type — the evaluator only searched within the crate boundary. Tests reclassified from NONE to GOOD: the detail text itself states 'Both inline tests directly construct and assert on PersistedPeer instances, exercising both public fields' and concludes GOOD for a plain data struct — the NONE field value contradicts the detail. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 11 clearly shows `/// A persisted peer — just enough to re-enroll on restart.` and best_practices rule 9 confirms 'All public items carry /// doc comments'. Field-level docs are absent but struct-level doc exists.
- **[deliberation] default_state_path: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate, designed for external consumers to call for the default path. No in-crate callers is expected — tests use temp dirs instead, and the binary crate would call this. Tests NONE is accurate: no test directly invokes this function, though for a trivial one-liner the risk is minimal. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 19 shows `/// Default state file path.` — the doc exists but is minimal (no examples, no platform notes).
- **[deliberation] save: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate meant for external consumers. Tests reclassified from NONE to WEAK: the detail text itself says 'Covered only by the save_and_load_roundtrip happy-path test with two peers' — the function IS called in the test at line 108, so NONE is factually incorrect. WEAK is appropriate given only the happy path is covered and error paths are untested. Documentation reclassified from UNDOCUMENTED to PARTIAL: lines 24-25 show a two-line /// doc comment describing purpose and format. It exists but lacks #Errors and parameter docs.
- **[deliberation] load: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate for external consumption. Tests reclassified from NONE to WEAK: both test functions call load() — save_and_load_roundtrip (line 109) and load_nonexistent_returns_empty (line 116). The detail text itself describes WEAK coverage with specific gaps (error paths untested). NONE is factually wrong. Documentation reclassified from UNDOCUMENTED to PARTIAL: line 48 shows `/// Load peers from the state file.` — the doc exists but is minimal, missing #Errors section and the important NotFound → Ok(vec![]) behavior.
- **[deliberation] icmp_checksum: utility LOW_VALUE → USED**: Deliberation reclassified: overengineering reclassified OVER → ACCEPTABLE: in an example file meant to teach, named wrappers like icmp_checksum provide semantic clarity at the call site (icmp_checksum(&reply[20..n]) is immediately understandable vs checksum_fold(...)). This is pedagogical naming, not premature abstraction. utility reclassified LOW_VALUE → USED: the wrapper is actively called and serves a clear readability purpose—it communicates the intent of the checksum computation to readers of the example. The semantic naming IS the value. tests NONE is factual for an example binary helper. documentation UNDOCUMENTED kept: no doc comment, though the name is self-descriptive and it's a private helper in an example file. Confidence raised to 85 due to clear reasoning about example-file conventions.
- **[deliberation] ip_checksum: utility LOW_VALUE → USED**: Deliberation reclassified: Same reasoning as icmp_checksum. overengineering reclassified OVER → ACCEPTABLE: the named wrapper ip_checksum communicates that this checksum is specifically for the IP header, improving call-site readability in the example. utility reclassified LOW_VALUE → USED: actively called and provides clear semantic value. tests NONE factual. documentation UNDOCUMENTED kept: no doc comment present, private helper, name is self-descriptive. The two wrappers together (icmp_checksum + ip_checksum) form a clean naming pattern that makes the main function's packet manipulation logic easier to follow.
- **[deliberation] rustguard_dev_uninit: utility LOW_VALUE → USED**: Deliberation reclassified: Utility reclassified LOW_VALUE → USED: this is a required #[no_mangle] extern C callback that must exist to satisfy the C net_device_ops callback table. The overengineering evaluator itself noted 'Required FFI callback stub. Real teardown is handled in Drop. The stub is necessary to satisfy the C callback table.' Removing it would break the kernel module. Tests NONE kept — empty body means nothing to test but technically no tests exist. Documentation PARTIAL confirmed.
- **[deliberation] rustguard_genl_get: utility LOW_VALUE → USED**: Deliberation reclassified: Utility reclassified LOW_VALUE → USED: required FFI callback stub for ABI compatibility with wg_genl.c. The evaluator noted 'Required genetlink callback stub returning 0... the stub must exist for ABI compatibility.' Tests NONE and documentation PARTIAL confirmed as-is.
- **[deliberation] rustguard_genl_set: utility LOW_VALUE → USED**: Deliberation reclassified: Utility reclassified LOW_VALUE → USED: same as rustguard_genl_get — required ABI-compatible FFI callback stub for the genetlink SET operation. Must exist even as a stub. Tests NONE and documentation PARTIAL confirmed.
- **[deliberation] MSG_TRANSPORT: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: MSG_TRANSPORT is a pub(crate) protocol constant for WireGuard transport data messages (type 4). It is almost certainly imported by other modules handling the data path (e.g., transport/data handling). The 0-importer count reflects incomplete cross-module analysis. Tests reclassified to GOOD per rule 6 (compile-time constant). Documentation reclassified to DOCUMENTED: source line 247 clearly shows `/// Message type 4: Transport Data.` preceding the constant definition. The merged JSON had an internal inconsistency where the field said UNDOCUMENTED but the detail text said DOCUMENTED.
- **[deliberation] create_initiation: utility DEAD → USED**: Deliberation reclassified: Utility DEAD is a false positive: this is the primary pub(crate) API for creating handshake initiation messages. It is the entry point for the initiator handshake flow and must be called by other crate modules (e.g., peer/device management). The 0-importer count reflects incomplete cross-crate analysis. Documentation reclassified UNDOCUMENTED→PARTIAL: source lines 284-287 show a multi-line `///` block describing purpose, return semantics, and DH failure condition. The merged JSON field contradicts its own detail text which says PARTIAL.
- **[deliberation] process_response: utility DEAD → USED**: Deliberation reclassified: Correction NEEDS_FIX is valid: the PSK-derived AEAD key at line 407 (`let (new_ck, mut t, key) = hkdf(...)`) is not declared mut and never zeroed, unlike the equivalent `key3` in process_initiation which is properly zeroed. This leaves sensitive key material on the stack. Utility DEAD→USED: this is the primary pub(crate) API for processing handshake responses, necessarily called by peer management code. Documentation UNDOCUMENTED→PARTIAL: source line 350 has `/// Process a handshake response (type 2) and derive transport keys.` — the merged field contradicts the detail text. Confidence raised since findings are now coherent.
- **[deliberation] process_initiation: utility DEAD → USED**: Deliberation reclassified: Utility DEAD→USED: this is the primary pub(crate) API for the responder handshake path, necessarily called by device/peer management modules. Documentation UNDOCUMENTED→PARTIAL: source lines 434-439 show a multi-line `///` block describing return tuple elements and requiring caller replay protection. The merged JSON field contradicts the detail text. Tests NONE is accurate — no test coverage for this critical path.
- **[deliberation] SessionTimers: utility DEAD → USED**: Deliberation reclassified: NEEDS_FIX confirmed: (1) is_dead() checks session_established age but the constant's doc says 'inactivity' — an actively-used session >240s old would be wrongly zeroed; baseline should be max(last_received, last_sent). (2) needs_keepalive() when last_sent==0 sets since_last_send = since_last_recv, making the condition `x >= interval && x < interval` always false — keepalive never fires for fresh sessions. Both are genuine logic bugs. DEAD→USED reclassified: SessionTimers is `pub(crate)` and forms the entire public API of this timer module in a kernel crate. The 'pre-computed analysis' likely has incomplete cross-file coverage; a WireGuard kernel module's timer state struct is fundamentally necessary for peer management. NONE tests confirmed: 9 methods with substantial branching logic and time-based comparisons are completely untested. UNDOCUMENTED→DOCUMENTED reclassified: source line 38 has `/// Per-peer timer state.` and all six fields carry individual `///` doc comments. The detail text itself concludes 'coverage meets the DOCUMENTED bar.'
- **[deliberation] create: utility DEAD → USED**: Deliberation reclassified: Utility reclassified DEAD→USED: this is the primary public API entry point for macOS utun creation. It's in a platform-specific module (macos.rs) that is conditionally compiled — the '0 runtime importers' finding reflects analysis on a non-macOS platform or incomplete crate analysis, not actual dead code. Correction NEEDS_FIX retained: num+1 overflow for u32::MAX is technically UB in debug and silently wrong in release; checked_add is the correct fix. Tests NONE retained: name-parsing and error-path logic is testable. Documentation UNDOCUMENTED retained: as the primary public entry point, this function genuinely needs /// documentation covering parameters, return value, and error conditions.
- **[deliberation] read: utility DEAD → USED**: Deliberation reclassified: Utility reclassified DEAD→USED: same reasoning as create — this is a public API function in a platform-specific module, not dead code. Tests NONE retained: AF-header stripping logic is testable. Documentation reclassified UNDOCUMENTED→PARTIAL: the source clearly shows a `///` doc comment on line 245 describing purpose and AF-header stripping. The evaluator field was inconsistent with its own detail text which correctly identified PARTIAL.
- **[deliberation] write: utility DEAD → USED**: Deliberation reclassified: Utility reclassified DEAD→USED: public API in platform-specific module, conditionally compiled for macOS. Tests NONE retained: IP version detection, empty-packet rejection, and AF-header prepending are all testable pure logic. Documentation reclassified UNDOCUMENTED→PARTIAL: line 266 has `/// Write an IP packet to the utun fd, prepending the 4-byte AF header.` The evaluator field contradicted its own detail which correctly identified PARTIAL.


## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "AF_XDP",
      "line_start": 20,
      "line_end": 20,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used in socket creation and SockaddrXdp struct"
    },
    {
      "name": "SOL_XDP",
      "line_start": 21,
      "line_end": 21,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used in six setsockopt/getsockopt socket option calls"
    },
    {
      "name": "XDP_UMEM_REG",
      "line_start": 23,
      "line_end": 23,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used in UMEM registration socket option call"
    },
    {
      "name": "XDP_UMEM_FILL_RING",
      "line_start": 24,
      "line_end": 24,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used to set fill ring size via setsockopt"
    },
    {
      "name": "XDP_UMEM_COMPLETION_RING",
      "line_start": 25,
      "line_end": 25,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used to set completion ring size"
    },
    {
      "name": "XDP_RX_RING",
      "line_start": 26,
      "line_end": 26,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used to set RX ring size socket option"
    },
    {
      "name": "XDP_TX_RING",
      "line_start": 27,
      "line_end": 27,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used to set TX ring size socket option"
    },
    {
      "name": "XDP_MMAP_OFFSETS",
      "line_start": 28,
      "line_end": 28,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used in getsockopt to query mmap offsets"
    },
    {
      "name": "XDP_PGOFF_RX_RING",
      "line_start": 30,
      "line_end": 30,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used as page offset for RX ring mmap"
    },
    {
      "name": "XDP_PGOFF_TX_RING",
      "line_start": 31,
      "line_end": 31,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used as page offset for TX ring mmap"
    },
    {
      "name": "XDP_UMEM_PGOFF_FILL_RING",
      "line_start": 32,
      "line_end": 32,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used as page offset for fill ring mmap"
    },
    {
      "name": "XDP_UMEM_PGOFF_COMPLETION_RING",
      "line_start": 33,
      "line_end": 33,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used as page offset for completion ring"
    },
    {
      "name": "XDP_USE_NEED_WAKEUP",
      "line_start": 35,
      "line_end": 35,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used in SockaddrXdp flags for wakeup"
    },
    {
      "name": "XDP_RING_NEED_WAKEUP",
      "line_start": 36,
      "line_end": 36,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported constant used in Ring::needs_wakeup() flag check"
    },
    {
      "name": "XdpUmemReg",
      "line_start": 41,
      "line_end": 48,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported struct constructed and used for UMEM registration"
    },
    {
      "name": "SockaddrXdp",
      "line_start": 52,
      "line_end": 58,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported struct used for socket bind address construction"
    },
    {
      "name": "XdpDesc",
      "line_start": 62,
      "line_end": 66,
      "utility": "USED",
      "confidence": 85,
      "detail": "Exported struct; pub type in library crate, matches false-positive pattern"
    },
    {
      "name": "XdpRingOffset",
      "line_start": 70,
      "line_end": 75,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported struct used in XdpMmapOffsets and mmap_ring calls"
    },
    {
      "name": "XdpMmapOffsets",
      "line_start": 79,
      "line_end": 84,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported struct constructed to query and store ring offsets"
    },
    {
      "name": "Ring",
      "line_start": 89,
      "line_end": 95,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported struct used as field type for all ring buffers"
    },
    {
      "name": "XdpConfig",
      "line_start": 126,
      "line_end": 132,
      "utility": "USED",
      "confidence": 85,
      "detail": "Exported struct; pub type in library crate, parameter to create()"
    },
    {
      "name": "XdpSocket",
      "line_start": 148,
      "line_end": 160,
      "utility": "USED",
      "confidence": 85,
      "detail": "Exported struct; main pub API for AF_XDP zero-copy UDP I/O"
    },
    {
      "name": "setsockopt_raw",
      "line_start": 407,
      "line_end": 415,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported helper function called five times in ring configuration"
    },
    {
      "name": "mmap_ring",
      "line_start": 417,
      "line_end": 437,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported function called four times to map ring buffers"
    },
    {
      "name": "make_ring",
      "line_start": 439,
      "line_end": 447,
      "utility": "USED",
      "confidence": 95,
      "detail": "Non-exported function called four times to construct Ring objects"
    },
    {
      "name": "if_nametoindex",
      "line_start": 449,
      "line_end": 458,
      "utility": "USED",
      "confidence": 85,
      "detail": "Exported function in library crate, called in create_inner setup"
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.1051 | **Duration:** 113.1s


---

# Axis: duplication

## User

## File: `rustguard-tun/src/xdp.rs`
## Language: rust

```rust
//! AF_XDP zero-copy UDP I/O for Linux.
//!
//! Replaces the standard UDP socket for receiving/sending WireGuard
//! encrypted packets. The plaintext side still uses TUN.
//!
//! Architecture:
//!   NIC -> XDP BPF filter (UDP:51820) -> AF_XDP socket -> decrypt -> TUN
//!   TUN -> encrypt -> AF_XDP TX ring -> NIC
//!
//! The unsafe is contained in this module. The ring buffer protocol is
//! lock-free (atomic producer/consumer indices on shared mmap'd memory).

#![cfg(target_os = "linux")]

use std::io;
use std::sync::atomic::{AtomicU32, Ordering};

// ── Constants ───────────────────────────────────────────────────────

const AF_XDP: i32 = 44;
const SOL_XDP: i32 = 283;

const XDP_UMEM_REG: i32 = 4;
const XDP_UMEM_FILL_RING: i32 = 5;
const XDP_UMEM_COMPLETION_RING: i32 = 6;
const XDP_RX_RING: i32 = 2;
const XDP_TX_RING: i32 = 3;
const XDP_MMAP_OFFSETS: i32 = 1;

const XDP_PGOFF_RX_RING: i64 = 0;
const XDP_PGOFF_TX_RING: i64 = 0x0_8000_0000;
const XDP_UMEM_PGOFF_FILL_RING: i64 = 0x1_0000_0000;
const XDP_UMEM_PGOFF_COMPLETION_RING: i64 = 0x1_8000_0000;

const XDP_USE_NEED_WAKEUP: u16 = 1 << 3;
const XDP_RING_NEED_WAKEUP: u32 = 1;

// ── Struct layouts (matching kernel uapi) ───────────────────────────

#[repr(C)]
struct XdpUmemReg {
    addr: u64,
    len: u64,
    chunk_size: u32,
    headroom: u32,
    flags: u32,
    tx_metadata_len: u32,
}

#[repr(C)]
#[derive(Clone, Copy)]
struct SockaddrXdp {
    family: u16,
    flags: u16,
    ifindex: u32,
    queue_id: u32,
    shared_umem_fd: u32,
}

#[repr(C)]
#[derive(Clone, Copy, Default)]
pub struct XdpDesc {
    pub addr: u64,
    pub len: u32,
    pub options: u32,
}

#[repr(C)]
#[derive(Default)]
struct XdpRingOffset {
    producer: u64,
    consumer: u64,
    desc: u64,
    flags: u64,
}

#[repr(C)]
#[derive(Default)]
struct XdpMmapOffsets {
    rx: XdpRingOffset,
    tx: XdpRingOffset,
    fr: XdpRingOffset,
    cr: XdpRingOffset,
}

// ── Ring buffer ─────────────────────────────────────────────────────

/// A single-producer/single-consumer ring over mmap'd memory.
struct Ring {
    producer: *const AtomicU32,
    consumer: *const AtomicU32,
    flags: *const AtomicU32,
    descs: *mut u8, // Either *mut XdpDesc or *mut u64 depending on ring type.
    mask: u32,
}

// Safety: Ring pointers come from mmap and are valid for the socket lifetime.
unsafe impl Send for Ring {}
unsafe impl Sync for Ring {}

impl Ring {
    fn producer(&self) -> u32 {
        unsafe { (*self.producer).load(Ordering::Acquire) }
    }

    fn consumer(&self) -> u32 {
        unsafe { (*self.consumer).load(Ordering::Acquire) }
    }

    fn set_producer(&self, val: u32) {
        unsafe { (*self.producer).store(val, Ordering::Release) }
    }

    fn set_consumer(&self, val: u32) {
        unsafe { (*self.consumer).store(val, Ordering::Release) }
    }

    fn needs_wakeup(&self) -> bool {
        unsafe { (*self.flags).load(Ordering::Relaxed) & XDP_RING_NEED_WAKEUP != 0 }
    }
}

// ── XDP Socket ──────────────────────────────────────────────────────

/// Configuration for creating an XDP socket.
pub struct XdpConfig {
    pub ifname: String,
    pub queue_id: u32,
    pub frame_size: u32,
    pub num_frames: u32,
    pub ring_size: u32,
}

impl Default for XdpConfig {
    fn default() -> Self {
        Self {
            ifname: "eth0".into(),
            queue_id: 0,
            frame_size: 4096,
            num_frames: 4096,
            ring_size: 2048,
        }
    }
}

/// An AF_XDP socket with UMEM and ring buffers.
/// Provides zero-copy packet I/O for the encrypted UDP side.
pub struct XdpSocket {
    fd: i32,
    umem: *mut u8,
    umem_size: usize,
    frame_size: u32,
    rx_ring: Ring,
    tx_ring: Ring,
    fill_ring: Ring,
    comp_ring: Ring,
    ring_size: u32,
    /// Free frame indices for TX.
    free_frames: Vec<u64>,
}

// Safety: XdpSocket owns its fd and mmap regions.
unsafe impl Send for XdpSocket {}

impl XdpSocket {
    /// Create and configure an AF_XDP socket.
    ///
    /// # Safety
    /// This function uses mmap to create shared memory regions with the kernel.
    /// The returned XdpSocket must not outlive the process.
    pub fn create(config: &XdpConfig) -> io::Result<Self> {
        unsafe { Self::create_inner(config) }
    }

    unsafe fn create_inner(config: &XdpConfig) -> io::Result<Self> {
        let umem_size = (config.frame_size * config.num_frames) as usize;

        // 1. Allocate UMEM.
        let umem = libc::mmap(
            std::ptr::null_mut(),
            umem_size,
            libc::PROT_READ | libc::PROT_WRITE,
            libc::MAP_PRIVATE | libc::MAP_ANONYMOUS | libc::MAP_POPULATE,
            -1,
            0,
        );
        if umem == libc::MAP_FAILED {
            return Err(io::Error::last_os_error());
        }
        let umem = umem as *mut u8;

        // 2. Create socket.
        let fd = libc::socket(AF_XDP, libc::SOCK_RAW, 0);
        if fd < 0 {
            libc::munmap(umem as *mut _, umem_size);
            return Err(io::Error::last_os_error());
        }

        // 3. Register UMEM.
        let reg = XdpUmemReg {
            addr: umem as u64,
            len: umem_size as u64,
            chunk_size: config.frame_size,
            headroom: 0,
            flags: 0,
            tx_metadata_len: 0,
        };
        if setsockopt_raw(fd, SOL_XDP, XDP_UMEM_REG, &reg) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 4. Set ring sizes.
        let rs = config.ring_size as i32;
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);

        // 5. Query mmap offsets.
        let mut offsets = XdpMmapOffsets::default();
        let mut optlen = std::mem::size_of::<XdpMmapOffsets>() as u32;
        if libc::getsockopt(
            fd,
            SOL_XDP,
            XDP_MMAP_OFFSETS,
            &mut offsets as *mut _ as *mut _,
            &mut optlen,
        ) < 0
        {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 6. mmap rings.
        let rx_map = mmap_ring(fd, &offsets.rx, config.ring_size, XDP_PGOFF_RX_RING, std::mem::size_of::<XdpDesc>())?;
        let tx_map = mmap_ring(fd, &offsets.tx, config.ring_size, XDP_PGOFF_TX_RING, std::mem::size_of::<XdpDesc>())?;
        let fill_map = mmap_ring(fd, &offsets.fr, config.ring_size, XDP_UMEM_PGOFF_FILL_RING, 8)?;
        let comp_map = mmap_ring(fd, &offsets.cr, config.ring_size, XDP_UMEM_PGOFF_COMPLETION_RING, 8)?;

        let mask = config.ring_size - 1;
        let rx_ring = make_ring(rx_map, &offsets.rx, mask);
        let tx_ring = make_ring(tx_map, &offsets.tx, mask);
        let fill_ring = make_ring(fill_map, &offsets.fr, mask);
        let comp_ring = make_ring(comp_map, &offsets.cr, mask);

        // 7. Pre-populate fill ring (first half of frames for RX).
        let rx_frames = config.num_frames / 2;
        let mut prod = fill_ring.producer();
        for i in 0..rx_frames {
            let addr_ptr = fill_ring.descs.add(((prod & mask) as usize) * 8) as *mut u64;
            *addr_ptr = (i * config.frame_size) as u64;
            prod += 1;
        }
        fill_ring.set_producer(prod);

        // Second half of frames are free for TX.
        let mut free_frames = Vec::with_capacity((config.num_frames - rx_frames) as usize);
        for i in rx_frames..config.num_frames {
            free_frames.push((i * config.frame_size) as u64);
        }

        // 8. Bind.
        let ifindex = if_nametoindex(&config.ifname)?;
        let sxdp = SockaddrXdp {
            family: AF_XDP as u16,
            flags: XDP_USE_NEED_WAKEUP,
            ifindex,
            queue_id: config.queue_id,
            shared_umem_fd: 0,
        };
        if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        Ok(Self {
            fd,
            umem,
            umem_size,
            frame_size: config.frame_size,
            rx_ring,
            tx_ring,
            fill_ring,
            comp_ring,
            ring_size: config.ring_size,
            free_frames,
        })
    }

    /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
    /// Caller must call `rx_release` after processing to return frames to the fill ring.
    pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
        let prod = self.rx_ring.producer();
        let cons = self.rx_ring.consumer();
        let available = prod.wrapping_sub(cons);

        let mut packets = Vec::with_capacity(available as usize);
        for i in 0..available {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let desc = unsafe {
                &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
            };
            let data = unsafe {
                std::slice::from_raw_parts(
                    self.umem.add(desc.addr as usize),
                    desc.len as usize,
                )
            };
            packets.push((desc.addr, data));
        }
        packets
    }

    /// Release consumed RX frames back to the fill ring.
    pub fn rx_release(&mut self, frames: &[u64]) {
        // Update RX consumer.
        let cons = self.rx_ring.consumer().wrapping_add(frames.len() as u32);
        self.rx_ring.set_consumer(cons);

        // Add frames back to fill ring.
        let mut prod = self.fill_ring.producer();
        for &addr in frames {
            let idx = (prod & (self.ring_size - 1)) as usize;
            unsafe {
                let ptr = self.fill_ring.descs.add(idx * 8) as *mut u64;
                *ptr = addr;
            }
            prod = prod.wrapping_add(1);
        }
        self.fill_ring.set_producer(prod);
    }

    /// Send a packet via the TX ring. Returns false if no frames available.
    pub fn tx_send(&mut self, data: &[u8]) -> bool {
        let Some(frame_addr) = self.free_frames.pop() else {
            return false;
        };

        // Copy data into UMEM frame.
        unsafe {
            std::ptr::copy_nonoverlapping(
                data.as_ptr(),
                self.umem.add(frame_addr as usize),
                data.len(),
            );
        }

        // Write TX descriptor.
        let prod = self.tx_ring.producer();
        let idx = (prod & (self.ring_size - 1)) as usize;
        unsafe {
            let desc = &mut *(self.tx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *mut XdpDesc);
            desc.addr = frame_addr;
            desc.len = data.len() as u32;
            desc.options = 0;
        }
        self.tx_ring.set_producer(prod.wrapping_add(1));

        // Kick kernel if needed.
        if self.tx_ring.needs_wakeup() {
            unsafe {
                libc::sendto(self.fd, std::ptr::null(), 0, libc::MSG_DONTWAIT, std::ptr::null(), 0);
            }
        }

        true
    }

    /// Reclaim completed TX frames.
    pub fn tx_complete(&mut self) {
        let prod = self.comp_ring.producer();
        let cons = self.comp_ring.consumer();
        let done = prod.wrapping_sub(cons);

        for i in 0..done {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let addr = unsafe { *(self.comp_ring.descs.add(idx * 8) as *const u64) };
            self.free_frames.push(addr);
        }
        self.comp_ring.set_consumer(cons.wrapping_add(done));
    }

    /// Raw file descriptor (for poll/epoll).
    pub fn fd(&self) -> i32 {
        self.fd
    }
}

impl Drop for XdpSocket {
    fn drop(&mut self) {
        unsafe {
            libc::close(self.fd);
            libc::munmap(self.umem as *mut _, self.umem_size);
        }
    }
}

// ── Helpers ─────────────────────────────────────────────────────────

unsafe fn setsockopt_raw<T>(fd: i32, level: i32, optname: i32, val: &T) -> i32 {
    libc::setsockopt(
        fd,
        level,
        optname,
        val as *const _ as *const _,
        std::mem::size_of::<T>() as u32,
    )
}

unsafe fn mmap_ring(
    fd: i32,
    off: &XdpRingOffset,
    ring_size: u32,
    pgoff: i64,
    desc_size: usize,
) -> io::Result<*mut u8> {
    let size = off.desc as usize + (ring_size as usize) * desc_size;
    let ptr = libc::mmap(
        std::ptr::null_mut(),
        size,
        libc::PROT_READ | libc::PROT_WRITE,
        libc::MAP_SHARED | libc::MAP_POPULATE,
        fd,
        pgoff,
    );
    if ptr == libc::MAP_FAILED {
        return Err(io::Error::last_os_error());
    }
    Ok(ptr as *mut u8)
}

unsafe fn make_ring(base: *mut u8, off: &XdpRingOffset, mask: u32) -> Ring {
    Ring {
        producer: base.add(off.producer as usize) as *const AtomicU32,
        consumer: base.add(off.consumer as usize) as *const AtomicU32,
        flags: base.add(off.flags as usize) as *const AtomicU32,
        descs: base.add(off.desc as usize),
        mask,
    }
}

pub fn if_nametoindex(name: &str) -> io::Result<u32> {
    let cname = std::ffi::CString::new(name).map_err(|_| {
        io::Error::new(io::ErrorKind::InvalidInput, "invalid interface name")
    })?;
    let idx = unsafe { libc::if_nametoindex(cname.as_ptr()) };
    if idx == 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(idx)
}

```

## Symbols to evaluate

- constant AF_XDP (L20–L20)
- constant SOL_XDP (L21–L21)
- constant XDP_UMEM_REG (L23–L23)
- constant XDP_UMEM_FILL_RING (L24–L24)
- constant XDP_UMEM_COMPLETION_RING (L25–L25)
- constant XDP_RX_RING (L26–L26)
- constant XDP_TX_RING (L27–L27)
- constant XDP_MMAP_OFFSETS (L28–L28)
- constant XDP_PGOFF_RX_RING (L30–L30)
- constant XDP_PGOFF_TX_RING (L31–L31)
- constant XDP_UMEM_PGOFF_FILL_RING (L32–L32)
- constant XDP_UMEM_PGOFF_COMPLETION_RING (L33–L33)
- constant XDP_USE_NEED_WAKEUP (L35–L35)
- constant XDP_RING_NEED_WAKEUP (L36–L36)
- class XdpUmemReg (L41–L48)
- class SockaddrXdp (L52–L58)
- export class XdpDesc (L62–L66)
- class XdpRingOffset (L70–L75)
- class XdpMmapOffsets (L79–L84)
- class Ring (L89–L95)
- export class XdpConfig (L126–L132)
- export class XdpSocket (L148–L160)
- function setsockopt_raw (L407–L415)
- function mmap_ring (L417–L437)
- function make_ring (L439–L447)
- export function if_nametoindex (L449–L458)

## RAG — Semantic Duplication

### setsockopt_raw (L407–L415)
No similar functions found. Mark UNIQUE.

### mmap_ring (L417–L437)
No similar functions found. Mark UNIQUE.

### make_ring (L439–L447)
No similar functions found. Mark UNIQUE.

### if_nametoindex (L449–L458)
Similar functions found:
- **ifindex_to_name** in `rustguard-tun/src/bpf_loader.rs` (score: 0.779)
  Signature: fn ifindex_to_name(ifindex: u32) -> io::Result<String> {
  Complexity: 1/5
  Source:
  ```rust
      let ifname = ifindex_to_name(ifindex)?;
      let fd_path = format!("/proc/self/fd/{prog_fd}");
      let output = std::process::Command::new("ip")
          .args(["link", "set", "dev", &ifname, "xdpgeneric", "pinned", &fd_path])
          .output();
  
      // Fallback: use the bpf netlink API via raw netlink socket.
      // If ip command doesn't support pinned fd, try bpf_xdp_attach.
      match output {
          Ok(out) if out.status.success() => Ok(()),
          _ => attach_xdp_netlink(ifindex, prog_fd),
      }
  }
  
  fn attach_xdp_netlink(ifindex: u32, prog_fd: i32) -> io::Result<()> {
      // Netlink RTM_SETLINK with IFLA_XDP attribute.
      // This is the proper way — no shelling out.
      use std::os::unix::io::FromRawFd;
  
      let sock = unsafe { libc::socket(libc::AF_NETLINK, libc::SOCK_RAW, libc::NETLINK_ROUTE) };
      if sock < 0 {
          return Err(io::Error::last_os_error());
      }
  
      // Build netlink message: RTM_SETLINK + IFLA_XDP { IFLA_XDP_FD + IFLA_XDP_FLAGS }
      const RTM_SETLINK: u16 = 19;
      const NLM_F_REQUEST: u16 = 1;
      const NLM_F_ACK: u16 = 4;
      const IFLA_XDP: u16 = 43;
      const IFLA_XDP_FD: u16 = 1;
      const IFLA_XDP_FLAGS: u16 = 3;
  
      // nlmsghdr (16) + ifinfomsg (16) + IFLA_XDP nla (4) + IFLA_XDP_FD nla (8) + IFLA_XDP_FLAGS nla (8) = 52
      let mut buf = [0u8; 64];
      let mut off = 0;
  
      // nlmsghdr
      let total_len: u32 = 52;
      buf[off..off + 4].copy_from_slice(&total_len.to_ne_bytes());
      off += 4;
      buf[off..off + 2].copy_from_slice(&RTM_SETLINK.to_ne_bytes());
      off += 2;
      buf[off..off + 2].copy_from_slice(&(NLM_F_REQUEST | NLM_F_ACK).to_ne_bytes());
      off += 2;
      off += 8; // seq + pid = 0
  
      // ifinfomsg
      buf[off] = libc::AF_UNSPEC as u8; // family
      off += 4; // family + pad + type
      let idx_bytes = ifindex.to_ne_bytes();
  ```

Evaluate the duplication of each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] random_nonce: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: Duplication DUPLICATE reclassified → UNIQUE: the duplicate target is in `rustguard-enroll/src/protocol.rs`, a completely separate crate. These are trivial 4-line functions that fill a buffer with random bytes. Deduplicating them would require creating a shared utility crate or adding a cross-crate dependency for 4 lines of code — this would increase coupling disproportionately. Two crates independently needing to generate random nonces is normal, not a code smell. Tests WEAK and documentation UNDOCUMENTED kept.
- **[deliberation] mix_key: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: Duplication reclassified DUPLICATE → UNIQUE: rustguard-core (userspace, x25519-dalek types, SharedSecret wrapper, alloc-based) and rustguard-kmod (kernel module, raw byte arrays, C FFI) are fundamentally different compilation targets. Sharing code between a userspace Rust crate and a kernel module is architecturally infeasible — the type differences (SharedSecret vs raw bytes) reflect different abstraction layers, not accidental duplication. The parallel implementations are intentional. Tests WEAK and docs PARTIAL stand as originally assessed.
- **[deliberation] compute_mac1: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: Duplication reclassified DUPLICATE → UNIQUE for the same reason as mix_key: rustguard-core and rustguard-kmod target fundamentally different environments (userspace vs kernel). The parameter type difference (PublicKey wrapper vs raw [u8; 32]) is not superficial — it reflects different type systems mandated by the compilation target. Cross-crate deduplication into a shared dependency would force the kernel module to depend on x25519-dalek types, which is architecturally wrong. Tests WEAK valid — no known-vector test for the 16-byte output. Documentation PARTIAL correct — public function lacks #Parameters/#Returns sections.
- **[deliberation] elapsed_since: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: This is the no_std variant at L26-30. Correction NEEDS_FIX: the concern is legitimate — returning Duration::ZERO makes all time-based query methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake, handshake_timed_out) silently non-functional in no_std builds. While the inline comment acknowledges the design, no _at() query variants exist for downstream kernel callers, leaving the no_std time path incomplete. Keep NEEDS_FIX. Duplication: two private cfg-gated stubs returning Duration::ZERO in different modules is a standard Rust no_std pattern, not meaningful duplication. Extracting to a shared module would create unnecessary coupling → reclassify to UNIQUE. Tests NONE: correct, no_std path is never compiled in tests. Documentation UNDOCUMENTED: fair for a private function, inline comments partially explain intent.
- **[deliberation] close_and_error: duplication DUPLICATE → UNIQUE**: Deliberation reclassified: Duplication reclassified DUPLICATE→UNIQUE: linux.rs and macos.rs are platform-specific modules compiled exclusively via cfg. Duplicating a 5-line helper across platform modules is standard practice — extracting to a shared module would create an artificial dependency for trivial code. Tests NONE is retained: the errno-before-close ordering is a subtle correctness property that could benefit from testing. Documentation PARTIAL is correct: has three /// lines covering purpose and safety.


## System (init)

**Model:** claude-haiku-4-5-20251001
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

```json
{
  "symbols": [
    {
      "name": "AF_XDP",
      "line_start": 20,
      "line_end": 20,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Linux kernel constant for AF_XDP socket family."
    },
    {
      "name": "SOL_XDP",
      "line_start": 21,
      "line_end": 21,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Linux kernel socket option level constant."
    },
    {
      "name": "XDP_UMEM_REG",
      "line_start": 23,
      "line_end": 23,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP UMEM registration constant."
    },
    {
      "name": "XDP_UMEM_FILL_RING",
      "line_start": 24,
      "line_end": 24,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP fill ring constant."
    },
    {
      "name": "XDP_UMEM_COMPLETION_RING",
      "line_start": 25,
      "line_end": 25,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP completion ring constant."
    },
    {
      "name": "XDP_RX_RING",
      "line_start": 26,
      "line_end": 26,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP RX ring constant."
    },
    {
      "name": "XDP_TX_RING",
      "line_start": 27,
      "line_end": 27,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP TX ring constant."
    },
    {
      "name": "XDP_MMAP_OFFSETS",
      "line_start": 28,
      "line_end": 28,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP mmap offsets constant."
    },
    {
      "name": "XDP_PGOFF_RX_RING",
      "line_start": 30,
      "line_end": 30,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP RX ring page offset."
    },
    {
      "name": "XDP_PGOFF_TX_RING",
      "line_start": 31,
      "line_end": 31,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP TX ring page offset."
    },
    {
      "name": "XDP_UMEM_PGOFF_FILL_RING",
      "line_start": 32,
      "line_end": 32,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP UMEM fill ring offset."
    },
    {
      "name": "XDP_UMEM_PGOFF_COMPLETION_RING",
      "line_start": 33,
      "line_end": 33,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP UMEM completion ring offset."
    },
    {
      "name": "XDP_USE_NEED_WAKEUP",
      "line_start": 35,
      "line_end": 35,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP socket flag for wakeup."
    },
    {
      "name": "XDP_RING_NEED_WAKEUP",
      "line_start": 36,
      "line_end": 36,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Kernel AF_XDP ring flag for wakeup."
    },
    {
      "name": "XdpUmemReg",
      "line_start": 41,
      "line_end": 48,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. FFI struct mapping kernel XDP UMEM registration."
    },
    {
      "name": "SockaddrXdp",
      "line_start": 52,
      "line_end": 58,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. FFI struct for XDP socket address binding."
    },
    {
      "name": "XdpDesc",
      "line_start": 62,
      "line_end": 66,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. FFI struct for XDP ring descriptor."
    },
    {
      "name": "XdpRingOffset",
      "line_start": 70,
      "line_end": 75,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. FFI struct for XDP ring buffer offsets."
    },
    {
      "name": "XdpMmapOffsets",
      "line_start": 79,
      "line_end": 84,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. FFI struct for all XDP mmap ring offsets."
    },
    {
      "name": "Ring",
      "line_start": 89,
      "line_end": 95,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Internal ring buffer wrapper for mmap'd memory."
    },
    {
      "name": "XdpConfig",
      "line_start": 126,
      "line_end": 132,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Configuration struct for XDP socket creation."
    },
    {
      "name": "XdpSocket",
      "line_start": 148,
      "line_end": 160,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Main XDP socket wrapper with UMEM and rings."
    },
    {
      "name": "setsockopt_raw",
      "line_start": 407,
      "line_end": 415,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Helper for raw libc setsockopt calls."
    },
    {
      "name": "mmap_ring",
      "line_start": 417,
      "line_end": 437,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Helper for mmap'ing XDP ring buffers."
    },
    {
      "name": "make_ring",
      "line_start": 439,
      "line_end": 447,
      "duplication": "UNIQUE",
      "confidence": 95,
      "detail": "No similar symbols found. Helper for constructing Ring from mmap regions."
    },
    {
      "name": "if_nametoindex",
      "line_start": 449,
      "line_end": 458,
      "duplication": "UNIQUE",
      "confidence": 85,
      "detail": "Score 0.779 with ifindex_to_name (reverse function) shows structural similarity in interface operations, but opposite semantic contracts (name→index vs index→name). Different callers, different invariants, not interchangeable.",
      "duplicate_target": null
    }
  ]
}
```

## rate_limit_event

## Result (success)

**Cost:** $0.0559 | **Duration:** 55.2s


---

# Axis: correction

## User

## File: `rustguard-tun/src/xdp.rs`
## Language: rust

```rust
//! AF_XDP zero-copy UDP I/O for Linux.
//!
//! Replaces the standard UDP socket for receiving/sending WireGuard
//! encrypted packets. The plaintext side still uses TUN.
//!
//! Architecture:
//!   NIC -> XDP BPF filter (UDP:51820) -> AF_XDP socket -> decrypt -> TUN
//!   TUN -> encrypt -> AF_XDP TX ring -> NIC
//!
//! The unsafe is contained in this module. The ring buffer protocol is
//! lock-free (atomic producer/consumer indices on shared mmap'd memory).

#![cfg(target_os = "linux")]

use std::io;
use std::sync::atomic::{AtomicU32, Ordering};

// ── Constants ───────────────────────────────────────────────────────

const AF_XDP: i32 = 44;
const SOL_XDP: i32 = 283;

const XDP_UMEM_REG: i32 = 4;
const XDP_UMEM_FILL_RING: i32 = 5;
const XDP_UMEM_COMPLETION_RING: i32 = 6;
const XDP_RX_RING: i32 = 2;
const XDP_TX_RING: i32 = 3;
const XDP_MMAP_OFFSETS: i32 = 1;

const XDP_PGOFF_RX_RING: i64 = 0;
const XDP_PGOFF_TX_RING: i64 = 0x0_8000_0000;
const XDP_UMEM_PGOFF_FILL_RING: i64 = 0x1_0000_0000;
const XDP_UMEM_PGOFF_COMPLETION_RING: i64 = 0x1_8000_0000;

const XDP_USE_NEED_WAKEUP: u16 = 1 << 3;
const XDP_RING_NEED_WAKEUP: u32 = 1;

// ── Struct layouts (matching kernel uapi) ───────────────────────────

#[repr(C)]
struct XdpUmemReg {
    addr: u64,
    len: u64,
    chunk_size: u32,
    headroom: u32,
    flags: u32,
    tx_metadata_len: u32,
}

#[repr(C)]
#[derive(Clone, Copy)]
struct SockaddrXdp {
    family: u16,
    flags: u16,
    ifindex: u32,
    queue_id: u32,
    shared_umem_fd: u32,
}

#[repr(C)]
#[derive(Clone, Copy, Default)]
pub struct XdpDesc {
    pub addr: u64,
    pub len: u32,
    pub options: u32,
}

#[repr(C)]
#[derive(Default)]
struct XdpRingOffset {
    producer: u64,
    consumer: u64,
    desc: u64,
    flags: u64,
}

#[repr(C)]
#[derive(Default)]
struct XdpMmapOffsets {
    rx: XdpRingOffset,
    tx: XdpRingOffset,
    fr: XdpRingOffset,
    cr: XdpRingOffset,
}

// ── Ring buffer ─────────────────────────────────────────────────────

/// A single-producer/single-consumer ring over mmap'd memory.
struct Ring {
    producer: *const AtomicU32,
    consumer: *const AtomicU32,
    flags: *const AtomicU32,
    descs: *mut u8, // Either *mut XdpDesc or *mut u64 depending on ring type.
    mask: u32,
}

// Safety: Ring pointers come from mmap and are valid for the socket lifetime.
unsafe impl Send for Ring {}
unsafe impl Sync for Ring {}

impl Ring {
    fn producer(&self) -> u32 {
        unsafe { (*self.producer).load(Ordering::Acquire) }
    }

    fn consumer(&self) -> u32 {
        unsafe { (*self.consumer).load(Ordering::Acquire) }
    }

    fn set_producer(&self, val: u32) {
        unsafe { (*self.producer).store(val, Ordering::Release) }
    }

    fn set_consumer(&self, val: u32) {
        unsafe { (*self.consumer).store(val, Ordering::Release) }
    }

    fn needs_wakeup(&self) -> bool {
        unsafe { (*self.flags).load(Ordering::Relaxed) & XDP_RING_NEED_WAKEUP != 0 }
    }
}

// ── XDP Socket ──────────────────────────────────────────────────────

/// Configuration for creating an XDP socket.
pub struct XdpConfig {
    pub ifname: String,
    pub queue_id: u32,
    pub frame_size: u32,
    pub num_frames: u32,
    pub ring_size: u32,
}

impl Default for XdpConfig {
    fn default() -> Self {
        Self {
            ifname: "eth0".into(),
            queue_id: 0,
            frame_size: 4096,
            num_frames: 4096,
            ring_size: 2048,
        }
    }
}

/// An AF_XDP socket with UMEM and ring buffers.
/// Provides zero-copy packet I/O for the encrypted UDP side.
pub struct XdpSocket {
    fd: i32,
    umem: *mut u8,
    umem_size: usize,
    frame_size: u32,
    rx_ring: Ring,
    tx_ring: Ring,
    fill_ring: Ring,
    comp_ring: Ring,
    ring_size: u32,
    /// Free frame indices for TX.
    free_frames: Vec<u64>,
}

// Safety: XdpSocket owns its fd and mmap regions.
unsafe impl Send for XdpSocket {}

impl XdpSocket {
    /// Create and configure an AF_XDP socket.
    ///
    /// # Safety
    /// This function uses mmap to create shared memory regions with the kernel.
    /// The returned XdpSocket must not outlive the process.
    pub fn create(config: &XdpConfig) -> io::Result<Self> {
        unsafe { Self::create_inner(config) }
    }

    unsafe fn create_inner(config: &XdpConfig) -> io::Result<Self> {
        let umem_size = (config.frame_size * config.num_frames) as usize;

        // 1. Allocate UMEM.
        let umem = libc::mmap(
            std::ptr::null_mut(),
            umem_size,
            libc::PROT_READ | libc::PROT_WRITE,
            libc::MAP_PRIVATE | libc::MAP_ANONYMOUS | libc::MAP_POPULATE,
            -1,
            0,
        );
        if umem == libc::MAP_FAILED {
            return Err(io::Error::last_os_error());
        }
        let umem = umem as *mut u8;

        // 2. Create socket.
        let fd = libc::socket(AF_XDP, libc::SOCK_RAW, 0);
        if fd < 0 {
            libc::munmap(umem as *mut _, umem_size);
            return Err(io::Error::last_os_error());
        }

        // 3. Register UMEM.
        let reg = XdpUmemReg {
            addr: umem as u64,
            len: umem_size as u64,
            chunk_size: config.frame_size,
            headroom: 0,
            flags: 0,
            tx_metadata_len: 0,
        };
        if setsockopt_raw(fd, SOL_XDP, XDP_UMEM_REG, &reg) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 4. Set ring sizes.
        let rs = config.ring_size as i32;
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);

        // 5. Query mmap offsets.
        let mut offsets = XdpMmapOffsets::default();
        let mut optlen = std::mem::size_of::<XdpMmapOffsets>() as u32;
        if libc::getsockopt(
            fd,
            SOL_XDP,
            XDP_MMAP_OFFSETS,
            &mut offsets as *mut _ as *mut _,
            &mut optlen,
        ) < 0
        {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 6. mmap rings.
        let rx_map = mmap_ring(fd, &offsets.rx, config.ring_size, XDP_PGOFF_RX_RING, std::mem::size_of::<XdpDesc>())?;
        let tx_map = mmap_ring(fd, &offsets.tx, config.ring_size, XDP_PGOFF_TX_RING, std::mem::size_of::<XdpDesc>())?;
        let fill_map = mmap_ring(fd, &offsets.fr, config.ring_size, XDP_UMEM_PGOFF_FILL_RING, 8)?;
        let comp_map = mmap_ring(fd, &offsets.cr, config.ring_size, XDP_UMEM_PGOFF_COMPLETION_RING, 8)?;

        let mask = config.ring_size - 1;
        let rx_ring = make_ring(rx_map, &offsets.rx, mask);
        let tx_ring = make_ring(tx_map, &offsets.tx, mask);
        let fill_ring = make_ring(fill_map, &offsets.fr, mask);
        let comp_ring = make_ring(comp_map, &offsets.cr, mask);

        // 7. Pre-populate fill ring (first half of frames for RX).
        let rx_frames = config.num_frames / 2;
        let mut prod = fill_ring.producer();
        for i in 0..rx_frames {
            let addr_ptr = fill_ring.descs.add(((prod & mask) as usize) * 8) as *mut u64;
            *addr_ptr = (i * config.frame_size) as u64;
            prod += 1;
        }
        fill_ring.set_producer(prod);

        // Second half of frames are free for TX.
        let mut free_frames = Vec::with_capacity((config.num_frames - rx_frames) as usize);
        for i in rx_frames..config.num_frames {
            free_frames.push((i * config.frame_size) as u64);
        }

        // 8. Bind.
        let ifindex = if_nametoindex(&config.ifname)?;
        let sxdp = SockaddrXdp {
            family: AF_XDP as u16,
            flags: XDP_USE_NEED_WAKEUP,
            ifindex,
            queue_id: config.queue_id,
            shared_umem_fd: 0,
        };
        if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        Ok(Self {
            fd,
            umem,
            umem_size,
            frame_size: config.frame_size,
            rx_ring,
            tx_ring,
            fill_ring,
            comp_ring,
            ring_size: config.ring_size,
            free_frames,
        })
    }

    /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
    /// Caller must call `rx_release` after processing to return frames to the fill ring.
    pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
        let prod = self.rx_ring.producer();
        let cons = self.rx_ring.consumer();
        let available = prod.wrapping_sub(cons);

        let mut packets = Vec::with_capacity(available as usize);
        for i in 0..available {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let desc = unsafe {
                &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
            };
            let data = unsafe {
                std::slice::from_raw_parts(
                    self.umem.add(desc.addr as usize),
                    desc.len as usize,
                )
            };
            packets.push((desc.addr, data));
        }
        packets
    }

    /// Release consumed RX frames back to the fill ring.
    pub fn rx_release(&mut self, frames: &[u64]) {
        // Update RX consumer.
        let cons = self.rx_ring.consumer().wrapping_add(frames.len() as u32);
        self.rx_ring.set_consumer(cons);

        // Add frames back to fill ring.
        let mut prod = self.fill_ring.producer();
        for &addr in frames {
            let idx = (prod & (self.ring_size - 1)) as usize;
            unsafe {
                let ptr = self.fill_ring.descs.add(idx * 8) as *mut u64;
                *ptr = addr;
            }
            prod = prod.wrapping_add(1);
        }
        self.fill_ring.set_producer(prod);
    }

    /// Send a packet via the TX ring. Returns false if no frames available.
    pub fn tx_send(&mut self, data: &[u8]) -> bool {
        let Some(frame_addr) = self.free_frames.pop() else {
            return false;
        };

        // Copy data into UMEM frame.
        unsafe {
            std::ptr::copy_nonoverlapping(
                data.as_ptr(),
                self.umem.add(frame_addr as usize),
                data.len(),
            );
        }

        // Write TX descriptor.
        let prod = self.tx_ring.producer();
        let idx = (prod & (self.ring_size - 1)) as usize;
        unsafe {
            let desc = &mut *(self.tx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *mut XdpDesc);
            desc.addr = frame_addr;
            desc.len = data.len() as u32;
            desc.options = 0;
        }
        self.tx_ring.set_producer(prod.wrapping_add(1));

        // Kick kernel if needed.
        if self.tx_ring.needs_wakeup() {
            unsafe {
                libc::sendto(self.fd, std::ptr::null(), 0, libc::MSG_DONTWAIT, std::ptr::null(), 0);
            }
        }

        true
    }

    /// Reclaim completed TX frames.
    pub fn tx_complete(&mut self) {
        let prod = self.comp_ring.producer();
        let cons = self.comp_ring.consumer();
        let done = prod.wrapping_sub(cons);

        for i in 0..done {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let addr = unsafe { *(self.comp_ring.descs.add(idx * 8) as *const u64) };
            self.free_frames.push(addr);
        }
        self.comp_ring.set_consumer(cons.wrapping_add(done));
    }

    /// Raw file descriptor (for poll/epoll).
    pub fn fd(&self) -> i32 {
        self.fd
    }
}

impl Drop for XdpSocket {
    fn drop(&mut self) {
        unsafe {
            libc::close(self.fd);
            libc::munmap(self.umem as *mut _, self.umem_size);
        }
    }
}

// ── Helpers ─────────────────────────────────────────────────────────

unsafe fn setsockopt_raw<T>(fd: i32, level: i32, optname: i32, val: &T) -> i32 {
    libc::setsockopt(
        fd,
        level,
        optname,
        val as *const _ as *const _,
        std::mem::size_of::<T>() as u32,
    )
}

unsafe fn mmap_ring(
    fd: i32,
    off: &XdpRingOffset,
    ring_size: u32,
    pgoff: i64,
    desc_size: usize,
) -> io::Result<*mut u8> {
    let size = off.desc as usize + (ring_size as usize) * desc_size;
    let ptr = libc::mmap(
        std::ptr::null_mut(),
        size,
        libc::PROT_READ | libc::PROT_WRITE,
        libc::MAP_SHARED | libc::MAP_POPULATE,
        fd,
        pgoff,
    );
    if ptr == libc::MAP_FAILED {
        return Err(io::Error::last_os_error());
    }
    Ok(ptr as *mut u8)
}

unsafe fn make_ring(base: *mut u8, off: &XdpRingOffset, mask: u32) -> Ring {
    Ring {
        producer: base.add(off.producer as usize) as *const AtomicU32,
        consumer: base.add(off.consumer as usize) as *const AtomicU32,
        flags: base.add(off.flags as usize) as *const AtomicU32,
        descs: base.add(off.desc as usize),
        mask,
    }
}

pub fn if_nametoindex(name: &str) -> io::Result<u32> {
    let cname = std::ffi::CString::new(name).map_err(|_| {
        io::Error::new(io::ErrorKind::InvalidInput, "invalid interface name")
    })?;
    let idx = unsafe { libc::if_nametoindex(cname.as_ptr()) };
    if idx == 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(idx)
}

```

## Symbols to evaluate

- constant AF_XDP (L20–L20)
- constant SOL_XDP (L21–L21)
- constant XDP_UMEM_REG (L23–L23)
- constant XDP_UMEM_FILL_RING (L24–L24)
- constant XDP_UMEM_COMPLETION_RING (L25–L25)
- constant XDP_RX_RING (L26–L26)
- constant XDP_TX_RING (L27–L27)
- constant XDP_MMAP_OFFSETS (L28–L28)
- constant XDP_PGOFF_RX_RING (L30–L30)
- constant XDP_PGOFF_TX_RING (L31–L31)
- constant XDP_UMEM_PGOFF_FILL_RING (L32–L32)
- constant XDP_UMEM_PGOFF_COMPLETION_RING (L33–L33)
- constant XDP_USE_NEED_WAKEUP (L35–L35)
- constant XDP_RING_NEED_WAKEUP (L36–L36)
- class XdpUmemReg (L41–L48)
- class SockaddrXdp (L52–L58)
- export class XdpDesc (L62–L66)
- class XdpRingOffset (L70–L75)
- class XdpMmapOffsets (L79–L84)
- class Ring (L89–L95)
- export class XdpConfig (L126–L132)
- export class XdpSocket (L148–L160)
- function setsockopt_raw (L407–L415)
- function mmap_ring (L417–L437)
- function make_ring (L439–L447)
- export function if_nametoindex (L449–L458)

Identify any bugs, logic errors, or correctness issues and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] elapsed_since: correction NEEDS_FIX → OK**: Deliberation reclassified: Line 43-45 is the #[cfg(not(feature = "std"))] variant returning Duration::ZERO. The NEEDS_FIX flags this as silently disabling rotation, but the CookieChecker correction evaluator explicitly states 'the code comments acknowledge the kernel module uses an independent mechanism, making this an intentional design limitation rather than an unintended bug.' The LEAN evaluator concurs: 'No_std stub returning Duration::ZERO is the minimal correct behaviour.' This is a consistent, documented intentional design — the kernel module uses its own cookie checker. Reclassified to OK. Tests NONE kept — no no_std tests exist. Documentation UNDOCUMENTED kept.
- **[deliberation] encode_addr: correction NEEDS_FIX → OK**: Deliberation reclassified: Correction NEEDS_FIX reclassified → OK: the claim that `alloc::vec::Vec` doesn't compile under std is likely wrong. In Rust crates targeting both std and no_std, it's standard practice to have `extern crate alloc;` unconditionally (or gated both ways). The function is exercised by all std tests (which create cookies using SocketAddr), proving it compiles and runs correctly. If it didn't compile, no tests would pass. Tests WEAK kept — IPv6 branch untested. Documentation PARTIAL kept.


## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

API Error: Claude's response exceeded the 32000 output token maximum. To configure this behavior, set the CLAUDE_CODE_MAX_OUTPUT_TOKENS environment variable.

## rate_limit_event

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "AF_XDP",
      "line_start": 20,
      "line_end": 20,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 44 matches the Linux kernel AF_XDP address family constant defined in linux/socket.h."
    },
    {
      "name": "SOL_XDP",
      "line_start": 21,
      "line_end": 21,
      "correction": "OK",
      "confidence": 88,
      "detail": "Value 283 matches the SOL_XDP socket-level constant in linux/if_xdp.h."
    },
    {
      "name": "XDP_UMEM_REG",
      "line_start": 23,
      "line_end": 23,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 4 matches XDP_UMEM_REG setsockopt option from linux/if_xdp.h."
    },
    {
      "name": "XDP_UMEM_FILL_RING",
      "line_start": 24,
      "line_end": 24,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 5 matches XDP_UMEM_FILL_RING from linux/if_xdp.h."
    },
    {
      "name": "XDP_UMEM_COMPLETION_RING",
      "line_start": 25,
      "line_end": 25,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 6 matches XDP_UMEM_COMPLETION_RING from linux/if_xdp.h."
    },
    {
      "name": "XDP_RX_RING",
      "line_start": 26,
      "line_end": 26,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 2 matches XDP_RX_RING from linux/if_xdp.h."
    },
    {
      "name": "XDP_TX_RING",
      "line_start": 27,
      "line_end": 27,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 3 matches XDP_TX_RING from linux/if_xdp.h."
    },
    {
      "name": "XDP_MMAP_OFFSETS",
      "line_start": 28,
      "line_end": 28,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 1 matches XDP_MMAP_OFFSETS getsockopt option from linux/if_xdp.h."
    },
    {
      "name": "XDP_PGOFF_RX_RING",
      "line_start": 30,
      "line_end": 30,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 0 matches the XDP_PGOFF_RX_RING mmap page-offset constant."
    },
    {
      "name": "XDP_PGOFF_TX_RING",
      "line_start": 31,
      "line_end": 31,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 0x80000000 matches XDP_PGOFF_TX_RING mmap page-offset constant."
    },
    {
      "name": "XDP_UMEM_PGOFF_FILL_RING",
      "line_start": 32,
      "line_end": 32,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 0x100000000 matches XDP_UMEM_PGOFF_FILL_RING mmap page-offset constant."
    },
    {
      "name": "XDP_UMEM_PGOFF_COMPLETION_RING",
      "line_start": 33,
      "line_end": 33,
      "correction": "OK",
      "confidence": 92,
      "detail": "Value 0x180000000 matches XDP_UMEM_PGOFF_COMPLETION_RING mmap page-offset constant."
    },
    {
      "name": "XDP_USE_NEED_WAKEUP",
      "line_start": 35,
      "line_end": 35,
      "correction": "OK",
      "confidence": 90,
      "detail": "Value 1<<3 = 8 matches XDP_USE_NEED_WAKEUP flag in struct sockaddr_xdp.flags from linux/if_xdp.h."
    },
    {
      "name": "XDP_RING_NEED_WAKEUP",
      "line_start": 36,
      "line_end": 36,
      "correction": "OK",
      "confidence": 90,
      "detail": "Value 1 matches XDP_RING_NEED_WAKEUP ring-flags bit from linux/if_xdp.h."
    },
    {
      "name": "XdpUmemReg",
      "line_start": 41,
      "line_end": 48,
      "correction": "NEEDS_FIX",
      "confidence": 70,
      "detail": "The tx_metadata_len field was added to struct xdp_umem_reg only in Linux 6.6. On kernels prior to 6.6, setsockopt(XDP_UMEM_REG) checks optlen strictly and returns EINVAL when optlen exceeds the expected struct size, making XdpSocket::create always fail on those kernels."
    },
    {
      "name": "SockaddrXdp",
      "line_start": 52,
      "line_end": 58,
      "correction": "OK",
      "confidence": 92,
      "detail": "Layout matches struct sockaddr_xdp from linux/if_xdp.h (family, flags, ifindex, queue_id, shared_umem_fd)."
    },
    {
      "name": "XdpDesc",
      "line_start": 62,
      "line_end": 66,
      "correction": "OK",
      "confidence": 95,
      "detail": "Layout matches struct xdp_desc from linux/if_xdp.h (addr u64, len u32, options u32)."
    },
    {
      "name": "XdpRingOffset",
      "line_start": 70,
      "line_end": 75,
      "correction": "OK",
      "confidence": 92,
      "detail": "Layout matches struct xdp_ring_offset from linux/if_xdp.h (producer, consumer, desc, flags all u64)."
    },
    {
      "name": "XdpMmapOffsets",
      "line_start": 79,
      "line_end": 84,
      "correction": "OK",
      "confidence": 92,
      "detail": "Layout matches struct xdp_mmap_offsets (rx, tx, fr, cr each an XdpRingOffset)."
    },
    {
      "name": "Ring",
      "line_start": 89,
      "line_end": 95,
      "correction": "OK",
      "confidence": 90,
      "detail": "Struct fields and SPSC atomic orderings (Acquire load, Release store) are correct for a shared ring buffer over mmap. The stored mask field is unused by Ring's own methods but harmless."
    },
    {
      "name": "XdpConfig",
      "line_start": 126,
      "line_end": 132,
      "correction": "OK",
      "confidence": 90,
      "detail": "Config struct fields are correctly typed. Default values (frame_size=4096, num_frames=4096, ring_size=2048) are all powers-of-two as required. Absence of a power-of-2 enforcement on ring_size is a validation gap in create_inner, not a struct-level bug."
    },
    {
      "name": "XdpSocket",
      "line_start": 148,
      "line_end": 160,
      "correction": "ERROR",
      "confidence": 95,
      "detail": "Four correctness bugs across the impl block: (1) tx_send copies data.len() bytes into a UMEM frame without verifying data.len() <= frame_size, causing an out-of-bounds write that corrupts adjacent UMEM frames. (2) tx_send advances the TX ring producer without checking that prod.wrapping_sub(consumer) < ring_size, allowing ring overflow that overwrites unprocessed descriptors. (3) Drop only munmaps UMEM; all four ring mmap regions (rx, tx, fill, comp) are never freed because neither Ring nor XdpSocket stores their base pointers, causing a guaranteed resource leak on every drop. (4) create_inner silently discards the return values of the four setsockopt ring-size calls, masking configuration failures."
    },
    {
      "name": "setsockopt_raw",
      "line_start": 407,
      "line_end": 415,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correct thin wrapper; returns the raw libc return code for the caller to inspect."
    },
    {
      "name": "mmap_ring",
      "line_start": 417,
      "line_end": 437,
      "correction": "OK",
      "confidence": 90,
      "detail": "Correctly computes mapping size as off.desc + ring_size * desc_size and creates a MAP_SHARED mapping at the specified page offset. Returns the base pointer, which make_ring consumes."
    },
    {
      "name": "make_ring",
      "line_start": 439,
      "line_end": 447,
      "correction": "NEEDS_FIX",
      "confidence": 88,
      "detail": "The base pointer (start of the ring mmap region) is used to derive Ring's internal pointers but is never stored in the resulting Ring. XdpSocket stores Ring values but not the mmap base or size needed for munmap, making it structurally impossible for Drop to free the ring mappings. This design omission is the direct root cause of the resource leak in XdpSocket::drop."
    },
    {
      "name": "if_nametoindex",
      "line_start": 449,
      "line_end": 458,
      "correction": "OK",
      "confidence": 95,
      "detail": "Correctly converts interface name via CString, calls libc::if_nametoindex, and maps a zero return to the last OS error."
    }
  ],
  "actions": [
    {
      "description": "tx_send copies data.len() bytes into a UMEM frame (self.umem.add(frame_addr)) without checking that data.len() <= self.frame_size. If the caller passes a slice larger than one frame, adjacent frames are silently overwritten, corrupting other live packets and causing undefined memory behaviour.",
      "severity": "CRITICAL",
      "line": 348
    },
    {
      "description": "tx_send does not check TX ring fullness before writing a descriptor. It should verify prod.wrapping_sub(self.tx_ring.consumer()) < self.ring_size; omitting this allows the producer to lap the consumer and overwrite unprocessed TX entries when the ring is saturated.",
      "severity": "MAJOR",
      "line": 356
    },
    {
      "description": "XdpSocket::drop munmaps only UMEM. The four ring mmap regions produced by mmap_ring (rx_map, tx_map, fill_map, comp_map) are consumed by make_ring but their base pointers and sizes are discarded. Neither Ring nor XdpSocket stores them, so every XdpSocket drop permanently leaks those four mappings.",
      "severity": "MAJOR",
      "line": 396
    },
    {
      "description": "create_inner ignores the return values of all four setsockopt ring-size calls (XDP_UMEM_FILL_RING, XDP_UMEM_COMPLETION_RING, XDP_RX_RING, XDP_TX_RING). If any fail, the socket proceeds with an unconfigured or default ring size, silently producing a broken XdpSocket.",
      "severity": "MAJOR",
      "line": 217
    },
    {
      "description": "In create_inner, if any mmap_ring call after the first fails (via the ? operator), previously mmapped ring regions are leaked: a tx_map failure leaves rx_map unmapped; a fill_map failure leaves rx_map and tx_map unmapped; a comp_map failure leaves all three. Additionally, if bind fails (L276), all four ring mmaps are leaked because only UMEM is freed in the error path.",
      "severity": "MAJOR",
      "line": 241
    },
    {
      "description": "XdpUmemReg includes tx_metadata_len (kernel 6.6+ only). On pre-6.6 kernels the setsockopt(XDP_UMEM_REG) call receives an optlen larger than the kernel expects and returns EINVAL, making XdpSocket::create unconditionally fail on those kernels.",
      "severity": "MINOR",
      "line": 41
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $1.5567 | **Duration:** 814.3s


---

# Axis: overengineering

## User

## File: `rustguard-tun/src/xdp.rs`
## Language: rust

```rust
//! AF_XDP zero-copy UDP I/O for Linux.
//!
//! Replaces the standard UDP socket for receiving/sending WireGuard
//! encrypted packets. The plaintext side still uses TUN.
//!
//! Architecture:
//!   NIC -> XDP BPF filter (UDP:51820) -> AF_XDP socket -> decrypt -> TUN
//!   TUN -> encrypt -> AF_XDP TX ring -> NIC
//!
//! The unsafe is contained in this module. The ring buffer protocol is
//! lock-free (atomic producer/consumer indices on shared mmap'd memory).

#![cfg(target_os = "linux")]

use std::io;
use std::sync::atomic::{AtomicU32, Ordering};

// ── Constants ───────────────────────────────────────────────────────

const AF_XDP: i32 = 44;
const SOL_XDP: i32 = 283;

const XDP_UMEM_REG: i32 = 4;
const XDP_UMEM_FILL_RING: i32 = 5;
const XDP_UMEM_COMPLETION_RING: i32 = 6;
const XDP_RX_RING: i32 = 2;
const XDP_TX_RING: i32 = 3;
const XDP_MMAP_OFFSETS: i32 = 1;

const XDP_PGOFF_RX_RING: i64 = 0;
const XDP_PGOFF_TX_RING: i64 = 0x0_8000_0000;
const XDP_UMEM_PGOFF_FILL_RING: i64 = 0x1_0000_0000;
const XDP_UMEM_PGOFF_COMPLETION_RING: i64 = 0x1_8000_0000;

const XDP_USE_NEED_WAKEUP: u16 = 1 << 3;
const XDP_RING_NEED_WAKEUP: u32 = 1;

// ── Struct layouts (matching kernel uapi) ───────────────────────────

#[repr(C)]
struct XdpUmemReg {
    addr: u64,
    len: u64,
    chunk_size: u32,
    headroom: u32,
    flags: u32,
    tx_metadata_len: u32,
}

#[repr(C)]
#[derive(Clone, Copy)]
struct SockaddrXdp {
    family: u16,
    flags: u16,
    ifindex: u32,
    queue_id: u32,
    shared_umem_fd: u32,
}

#[repr(C)]
#[derive(Clone, Copy, Default)]
pub struct XdpDesc {
    pub addr: u64,
    pub len: u32,
    pub options: u32,
}

#[repr(C)]
#[derive(Default)]
struct XdpRingOffset {
    producer: u64,
    consumer: u64,
    desc: u64,
    flags: u64,
}

#[repr(C)]
#[derive(Default)]
struct XdpMmapOffsets {
    rx: XdpRingOffset,
    tx: XdpRingOffset,
    fr: XdpRingOffset,
    cr: XdpRingOffset,
}

// ── Ring buffer ─────────────────────────────────────────────────────

/// A single-producer/single-consumer ring over mmap'd memory.
struct Ring {
    producer: *const AtomicU32,
    consumer: *const AtomicU32,
    flags: *const AtomicU32,
    descs: *mut u8, // Either *mut XdpDesc or *mut u64 depending on ring type.
    mask: u32,
}

// Safety: Ring pointers come from mmap and are valid for the socket lifetime.
unsafe impl Send for Ring {}
unsafe impl Sync for Ring {}

impl Ring {
    fn producer(&self) -> u32 {
        unsafe { (*self.producer).load(Ordering::Acquire) }
    }

    fn consumer(&self) -> u32 {
        unsafe { (*self.consumer).load(Ordering::Acquire) }
    }

    fn set_producer(&self, val: u32) {
        unsafe { (*self.producer).store(val, Ordering::Release) }
    }

    fn set_consumer(&self, val: u32) {
        unsafe { (*self.consumer).store(val, Ordering::Release) }
    }

    fn needs_wakeup(&self) -> bool {
        unsafe { (*self.flags).load(Ordering::Relaxed) & XDP_RING_NEED_WAKEUP != 0 }
    }
}

// ── XDP Socket ──────────────────────────────────────────────────────

/// Configuration for creating an XDP socket.
pub struct XdpConfig {
    pub ifname: String,
    pub queue_id: u32,
    pub frame_size: u32,
    pub num_frames: u32,
    pub ring_size: u32,
}

impl Default for XdpConfig {
    fn default() -> Self {
        Self {
            ifname: "eth0".into(),
            queue_id: 0,
            frame_size: 4096,
            num_frames: 4096,
            ring_size: 2048,
        }
    }
}

/// An AF_XDP socket with UMEM and ring buffers.
/// Provides zero-copy packet I/O for the encrypted UDP side.
pub struct XdpSocket {
    fd: i32,
    umem: *mut u8,
    umem_size: usize,
    frame_size: u32,
    rx_ring: Ring,
    tx_ring: Ring,
    fill_ring: Ring,
    comp_ring: Ring,
    ring_size: u32,
    /// Free frame indices for TX.
    free_frames: Vec<u64>,
}

// Safety: XdpSocket owns its fd and mmap regions.
unsafe impl Send for XdpSocket {}

impl XdpSocket {
    /// Create and configure an AF_XDP socket.
    ///
    /// # Safety
    /// This function uses mmap to create shared memory regions with the kernel.
    /// The returned XdpSocket must not outlive the process.
    pub fn create(config: &XdpConfig) -> io::Result<Self> {
        unsafe { Self::create_inner(config) }
    }

    unsafe fn create_inner(config: &XdpConfig) -> io::Result<Self> {
        let umem_size = (config.frame_size * config.num_frames) as usize;

        // 1. Allocate UMEM.
        let umem = libc::mmap(
            std::ptr::null_mut(),
            umem_size,
            libc::PROT_READ | libc::PROT_WRITE,
            libc::MAP_PRIVATE | libc::MAP_ANONYMOUS | libc::MAP_POPULATE,
            -1,
            0,
        );
        if umem == libc::MAP_FAILED {
            return Err(io::Error::last_os_error());
        }
        let umem = umem as *mut u8;

        // 2. Create socket.
        let fd = libc::socket(AF_XDP, libc::SOCK_RAW, 0);
        if fd < 0 {
            libc::munmap(umem as *mut _, umem_size);
            return Err(io::Error::last_os_error());
        }

        // 3. Register UMEM.
        let reg = XdpUmemReg {
            addr: umem as u64,
            len: umem_size as u64,
            chunk_size: config.frame_size,
            headroom: 0,
            flags: 0,
            tx_metadata_len: 0,
        };
        if setsockopt_raw(fd, SOL_XDP, XDP_UMEM_REG, &reg) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 4. Set ring sizes.
        let rs = config.ring_size as i32;
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);

        // 5. Query mmap offsets.
        let mut offsets = XdpMmapOffsets::default();
        let mut optlen = std::mem::size_of::<XdpMmapOffsets>() as u32;
        if libc::getsockopt(
            fd,
            SOL_XDP,
            XDP_MMAP_OFFSETS,
            &mut offsets as *mut _ as *mut _,
            &mut optlen,
        ) < 0
        {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 6. mmap rings.
        let rx_map = mmap_ring(fd, &offsets.rx, config.ring_size, XDP_PGOFF_RX_RING, std::mem::size_of::<XdpDesc>())?;
        let tx_map = mmap_ring(fd, &offsets.tx, config.ring_size, XDP_PGOFF_TX_RING, std::mem::size_of::<XdpDesc>())?;
        let fill_map = mmap_ring(fd, &offsets.fr, config.ring_size, XDP_UMEM_PGOFF_FILL_RING, 8)?;
        let comp_map = mmap_ring(fd, &offsets.cr, config.ring_size, XDP_UMEM_PGOFF_COMPLETION_RING, 8)?;

        let mask = config.ring_size - 1;
        let rx_ring = make_ring(rx_map, &offsets.rx, mask);
        let tx_ring = make_ring(tx_map, &offsets.tx, mask);
        let fill_ring = make_ring(fill_map, &offsets.fr, mask);
        let comp_ring = make_ring(comp_map, &offsets.cr, mask);

        // 7. Pre-populate fill ring (first half of frames for RX).
        let rx_frames = config.num_frames / 2;
        let mut prod = fill_ring.producer();
        for i in 0..rx_frames {
            let addr_ptr = fill_ring.descs.add(((prod & mask) as usize) * 8) as *mut u64;
            *addr_ptr = (i * config.frame_size) as u64;
            prod += 1;
        }
        fill_ring.set_producer(prod);

        // Second half of frames are free for TX.
        let mut free_frames = Vec::with_capacity((config.num_frames - rx_frames) as usize);
        for i in rx_frames..config.num_frames {
            free_frames.push((i * config.frame_size) as u64);
        }

        // 8. Bind.
        let ifindex = if_nametoindex(&config.ifname)?;
        let sxdp = SockaddrXdp {
            family: AF_XDP as u16,
            flags: XDP_USE_NEED_WAKEUP,
            ifindex,
            queue_id: config.queue_id,
            shared_umem_fd: 0,
        };
        if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        Ok(Self {
            fd,
            umem,
            umem_size,
            frame_size: config.frame_size,
            rx_ring,
            tx_ring,
            fill_ring,
            comp_ring,
            ring_size: config.ring_size,
            free_frames,
        })
    }

    /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
    /// Caller must call `rx_release` after processing to return frames to the fill ring.
    pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
        let prod = self.rx_ring.producer();
        let cons = self.rx_ring.consumer();
        let available = prod.wrapping_sub(cons);

        let mut packets = Vec::with_capacity(available as usize);
        for i in 0..available {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let desc = unsafe {
                &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
            };
            let data = unsafe {
                std::slice::from_raw_parts(
                    self.umem.add(desc.addr as usize),
                    desc.len as usize,
                )
            };
            packets.push((desc.addr, data));
        }
        packets
    }

    /// Release consumed RX frames back to the fill ring.
    pub fn rx_release(&mut self, frames: &[u64]) {
        // Update RX consumer.
        let cons = self.rx_ring.consumer().wrapping_add(frames.len() as u32);
        self.rx_ring.set_consumer(cons);

        // Add frames back to fill ring.
        let mut prod = self.fill_ring.producer();
        for &addr in frames {
            let idx = (prod & (self.ring_size - 1)) as usize;
            unsafe {
                let ptr = self.fill_ring.descs.add(idx * 8) as *mut u64;
                *ptr = addr;
            }
            prod = prod.wrapping_add(1);
        }
        self.fill_ring.set_producer(prod);
    }

    /// Send a packet via the TX ring. Returns false if no frames available.
    pub fn tx_send(&mut self, data: &[u8]) -> bool {
        let Some(frame_addr) = self.free_frames.pop() else {
            return false;
        };

        // Copy data into UMEM frame.
        unsafe {
            std::ptr::copy_nonoverlapping(
                data.as_ptr(),
                self.umem.add(frame_addr as usize),
                data.len(),
            );
        }

        // Write TX descriptor.
        let prod = self.tx_ring.producer();
        let idx = (prod & (self.ring_size - 1)) as usize;
        unsafe {
            let desc = &mut *(self.tx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *mut XdpDesc);
            desc.addr = frame_addr;
            desc.len = data.len() as u32;
            desc.options = 0;
        }
        self.tx_ring.set_producer(prod.wrapping_add(1));

        // Kick kernel if needed.
        if self.tx_ring.needs_wakeup() {
            unsafe {
                libc::sendto(self.fd, std::ptr::null(), 0, libc::MSG_DONTWAIT, std::ptr::null(), 0);
            }
        }

        true
    }

    /// Reclaim completed TX frames.
    pub fn tx_complete(&mut self) {
        let prod = self.comp_ring.producer();
        let cons = self.comp_ring.consumer();
        let done = prod.wrapping_sub(cons);

        for i in 0..done {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let addr = unsafe { *(self.comp_ring.descs.add(idx * 8) as *const u64) };
            self.free_frames.push(addr);
        }
        self.comp_ring.set_consumer(cons.wrapping_add(done));
    }

    /// Raw file descriptor (for poll/epoll).
    pub fn fd(&self) -> i32 {
        self.fd
    }
}

impl Drop for XdpSocket {
    fn drop(&mut self) {
        unsafe {
            libc::close(self.fd);
            libc::munmap(self.umem as *mut _, self.umem_size);
        }
    }
}

// ── Helpers ─────────────────────────────────────────────────────────

unsafe fn setsockopt_raw<T>(fd: i32, level: i32, optname: i32, val: &T) -> i32 {
    libc::setsockopt(
        fd,
        level,
        optname,
        val as *const _ as *const _,
        std::mem::size_of::<T>() as u32,
    )
}

unsafe fn mmap_ring(
    fd: i32,
    off: &XdpRingOffset,
    ring_size: u32,
    pgoff: i64,
    desc_size: usize,
) -> io::Result<*mut u8> {
    let size = off.desc as usize + (ring_size as usize) * desc_size;
    let ptr = libc::mmap(
        std::ptr::null_mut(),
        size,
        libc::PROT_READ | libc::PROT_WRITE,
        libc::MAP_SHARED | libc::MAP_POPULATE,
        fd,
        pgoff,
    );
    if ptr == libc::MAP_FAILED {
        return Err(io::Error::last_os_error());
    }
    Ok(ptr as *mut u8)
}

unsafe fn make_ring(base: *mut u8, off: &XdpRingOffset, mask: u32) -> Ring {
    Ring {
        producer: base.add(off.producer as usize) as *const AtomicU32,
        consumer: base.add(off.consumer as usize) as *const AtomicU32,
        flags: base.add(off.flags as usize) as *const AtomicU32,
        descs: base.add(off.desc as usize),
        mask,
    }
}

pub fn if_nametoindex(name: &str) -> io::Result<u32> {
    let cname = std::ffi::CString::new(name).map_err(|_| {
        io::Error::new(io::ErrorKind::InvalidInput, "invalid interface name")
    })?;
    let idx = unsafe { libc::if_nametoindex(cname.as_ptr()) };
    if idx == 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(idx)
}

```

## Symbols to evaluate

- constant AF_XDP (L20–L20)
- constant SOL_XDP (L21–L21)
- constant XDP_UMEM_REG (L23–L23)
- constant XDP_UMEM_FILL_RING (L24–L24)
- constant XDP_UMEM_COMPLETION_RING (L25–L25)
- constant XDP_RX_RING (L26–L26)
- constant XDP_TX_RING (L27–L27)
- constant XDP_MMAP_OFFSETS (L28–L28)
- constant XDP_PGOFF_RX_RING (L30–L30)
- constant XDP_PGOFF_TX_RING (L31–L31)
- constant XDP_UMEM_PGOFF_FILL_RING (L32–L32)
- constant XDP_UMEM_PGOFF_COMPLETION_RING (L33–L33)
- constant XDP_USE_NEED_WAKEUP (L35–L35)
- constant XDP_RING_NEED_WAKEUP (L36–L36)
- class XdpUmemReg (L41–L48)
- class SockaddrXdp (L52–L58)
- export class XdpDesc (L62–L66)
- class XdpRingOffset (L70–L75)
- class XdpMmapOffsets (L79–L84)
- class Ring (L89–L95)
- export class XdpConfig (L126–L132)
- export class XdpSocket (L148–L160)
- function setsockopt_raw (L407–L415)
- function mmap_ring (L417–L437)
- function make_ring (L439–L447)
- export function if_nametoindex (L449–L458)

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Use the project structure to detect excessive fragmentation:
- Directory with only 1 file → potential fragmentation
- More than 5 nesting levels → excessive structural complexity
- Factory/adapter directories with ≤ 2 files → likely over-engineering
These heuristics may increase a symbol's rating from LEAN to OVER.

Evaluate the complexity of each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] icmp_checksum: overengineering OVER → ACCEPTABLE**: Deliberation reclassified: overengineering reclassified OVER → ACCEPTABLE: in an example file meant to teach, named wrappers like icmp_checksum provide semantic clarity at the call site (icmp_checksum(&reply[20..n]) is immediately understandable vs checksum_fold(...)). This is pedagogical naming, not premature abstraction. utility reclassified LOW_VALUE → USED: the wrapper is actively called and serves a clear readability purpose—it communicates the intent of the checksum computation to readers of the example. The semantic naming IS the value. tests NONE is factual for an example binary helper. documentation UNDOCUMENTED kept: no doc comment, though the name is self-descriptive and it's a private helper in an example file. Confidence raised to 85 due to clear reasoning about example-file conventions.
- **[deliberation] ip_checksum: overengineering OVER → ACCEPTABLE**: Deliberation reclassified: Same reasoning as icmp_checksum. overengineering reclassified OVER → ACCEPTABLE: the named wrapper ip_checksum communicates that this checksum is specifically for the IP header, improving call-site readability in the example. utility reclassified LOW_VALUE → USED: actively called and provides clear semantic value. tests NONE factual. documentation UNDOCUMENTED kept: no doc comment present, private helper, name is self-descriptive. The two wrappers together (icmp_checksum + ip_checksum) form a clean naming pattern that makes the main function's packet manipulation logic easier to follow.


## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "AF_XDP",
      "line_start": 20,
      "line_end": 20,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Direct Linux kernel UAPI constant (44). libc does not expose AF_XDP, so manual definition is required. Single value, no abstraction."
    },
    {
      "name": "SOL_XDP",
      "line_start": 21,
      "line_end": 21,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Direct Linux kernel socket-option level constant (283). Necessary for all setsockopt/getsockopt calls on AF_XDP sockets. Not provided by libc."
    },
    {
      "name": "XDP_UMEM_REG",
      "line_start": 23,
      "line_end": 23,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel UAPI option name for UMEM registration. Required by the AF_XDP setup sequence; no installed dependency provides it."
    },
    {
      "name": "XDP_UMEM_FILL_RING",
      "line_start": 24,
      "line_end": 24,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel UAPI constant for fill-ring size option. Minimal, single-purpose, directly consumed by setsockopt call."
    },
    {
      "name": "XDP_UMEM_COMPLETION_RING",
      "line_start": 25,
      "line_end": 25,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel UAPI constant for completion-ring size option. Directly consumed by one setsockopt call, no surrounding abstraction."
    },
    {
      "name": "XDP_RX_RING",
      "line_start": 26,
      "line_end": 26,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel UAPI option name for RX ring size. Required and minimal."
    },
    {
      "name": "XDP_TX_RING",
      "line_start": 27,
      "line_end": 27,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel UAPI option name for TX ring size. Required and minimal."
    },
    {
      "name": "XDP_MMAP_OFFSETS",
      "line_start": 28,
      "line_end": 28,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel UAPI option name used in the single getsockopt call that retrieves mmap offsets. Necessary, no abstraction overhead."
    },
    {
      "name": "XDP_PGOFF_RX_RING",
      "line_start": 30,
      "line_end": 30,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel-defined mmap page offset for the RX ring (0). Named constant improves readability over a magic number in the mmap_ring call."
    },
    {
      "name": "XDP_PGOFF_TX_RING",
      "line_start": 31,
      "line_end": 31,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel-defined mmap page offset for the TX ring (0x0_8000_0000). Named constant is essential for readability of a non-obvious magic value."
    },
    {
      "name": "XDP_UMEM_PGOFF_FILL_RING",
      "line_start": 32,
      "line_end": 32,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel-defined mmap page offset for the fill ring (0x1_0000_0000). Naming it is the minimal appropriate thing to do for a non-obvious literal."
    },
    {
      "name": "XDP_UMEM_PGOFF_COMPLETION_RING",
      "line_start": 33,
      "line_end": 33,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel-defined mmap page offset for the completion ring (0x1_8000_0000). Same justification as the other PGOFF constants."
    },
    {
      "name": "XDP_USE_NEED_WAKEUP",
      "line_start": 35,
      "line_end": 35,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel UAPI flag (bit 3) passed in SockaddrXdp.flags at bind time. Single use, no wrapping logic."
    },
    {
      "name": "XDP_RING_NEED_WAKEUP",
      "line_start": 36,
      "line_end": 36,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Kernel UAPI flag checked in Ring::needs_wakeup. Single use, naming it avoids a bare magic 1 in the bitwise AND."
    },
    {
      "name": "XdpUmemReg",
      "line_start": 41,
      "line_end": 48,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Direct repr(C) mirror of the kernel xdp_umem_reg UAPI struct, including the newer tx_metadata_len field. Required for setsockopt UMEM registration; no abstraction beyond the necessary layout."
    },
    {
      "name": "SockaddrXdp",
      "line_start": 52,
      "line_end": 58,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Direct repr(C) mirror of sockaddr_xdp UAPI struct. Required by libc::bind and not provided by libc. Minimal."
    },
    {
      "name": "XdpDesc",
      "line_start": 62,
      "line_end": 66,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Direct repr(C) mirror of xdp_desc UAPI struct used in RX and TX ring slots. pub fields are appropriate since callers read addr/len directly."
    },
    {
      "name": "XdpRingOffset",
      "line_start": 70,
      "line_end": 75,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Direct repr(C) mirror of xdp_ring_offset UAPI struct. Populated by getsockopt(XDP_MMAP_OFFSETS) and then consumed by mmap_ring/make_ring. No unnecessary fields or methods."
    },
    {
      "name": "XdpMmapOffsets",
      "line_start": 79,
      "line_end": 84,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Direct repr(C) mirror of xdp_mmap_offsets UAPI struct (rx, tx, fr, cr). Used in one getsockopt call, then fields are accessed individually. Minimal wrapper."
    },
    {
      "name": "Ring",
      "line_start": 89,
      "line_end": 95,
      "overengineering": "LEAN",
      "confidence": 90,
      "detail": "Thin struct encapsulating the four raw pointers and mask for one lock-free SPSC ring. The five accessor methods (producer, consumer, set_producer, set_consumer, needs_wakeup) prevent repeated unsafe derefs and encode Acquire/Release ordering in one place. Note: the stored mask field is never used inside Ring itself—XdpSocket methods recompute ring_size-1—but this is a minor inconsistency, not overengineering."
    },
    {
      "name": "XdpConfig",
      "line_start": 126,
      "line_end": 132,
      "overengineering": "LEAN",
      "confidence": 90,
      "detail": "Five-field configuration struct with a sensible Default. Appropriate for a complex multi-step setup (UMEM + 4 rings + bind). No builder pattern, no unnecessary generics."
    },
    {
      "name": "XdpSocket",
      "line_start": 148,
      "line_end": 160,
      "overengineering": "LEAN",
      "confidence": 90,
      "detail": "Main socket struct owns fd, UMEM, four Ring abstractions, and free_frames list. All fields are directly needed by the public API (rx_poll, rx_release, tx_send, tx_complete, fd). The complexity is inherent in AF_XDP's UMEM + ring-buffer protocol, not artificially introduced."
    },
    {
      "name": "setsockopt_raw",
      "line_start": 407,
      "line_end": 415,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Generic helper that eliminates six near-identical libc::setsockopt call sites. The generic monomorphises to correctly sized calls; no additional abstraction layers are added."
    },
    {
      "name": "mmap_ring",
      "line_start": 417,
      "line_end": 437,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Encapsulates the size calculation (off.desc + ring_size * desc_size) and mmap syscall for one ring. Called four times with different offsets and desc_sizes; the factoring is appropriate."
    },
    {
      "name": "make_ring",
      "line_start": 439,
      "line_end": 447,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Constructs a Ring by computing four pointer offsets from a base address and the XdpRingOffset. Called four times with different offset structs; the factoring removes repetitive unsafe pointer arithmetic."
    },
    {
      "name": "if_nametoindex",
      "line_start": 449,
      "line_end": 458,
      "overengineering": "LEAN",
      "confidence": 95,
      "detail": "Thin safe wrapper around libc::if_nametoindex: converts &str to CString, calls the syscall, maps errno on failure. The conversion and error mapping are necessary and not over-abstracted."
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.1802 | **Duration:** 74.5s


---

# Axis: tests

## User

## File: `rustguard-tun/src/xdp.rs`
## Language: rust

```rust
//! AF_XDP zero-copy UDP I/O for Linux.
//!
//! Replaces the standard UDP socket for receiving/sending WireGuard
//! encrypted packets. The plaintext side still uses TUN.
//!
//! Architecture:
//!   NIC -> XDP BPF filter (UDP:51820) -> AF_XDP socket -> decrypt -> TUN
//!   TUN -> encrypt -> AF_XDP TX ring -> NIC
//!
//! The unsafe is contained in this module. The ring buffer protocol is
//! lock-free (atomic producer/consumer indices on shared mmap'd memory).

#![cfg(target_os = "linux")]

use std::io;
use std::sync::atomic::{AtomicU32, Ordering};

// ── Constants ───────────────────────────────────────────────────────

const AF_XDP: i32 = 44;
const SOL_XDP: i32 = 283;

const XDP_UMEM_REG: i32 = 4;
const XDP_UMEM_FILL_RING: i32 = 5;
const XDP_UMEM_COMPLETION_RING: i32 = 6;
const XDP_RX_RING: i32 = 2;
const XDP_TX_RING: i32 = 3;
const XDP_MMAP_OFFSETS: i32 = 1;

const XDP_PGOFF_RX_RING: i64 = 0;
const XDP_PGOFF_TX_RING: i64 = 0x0_8000_0000;
const XDP_UMEM_PGOFF_FILL_RING: i64 = 0x1_0000_0000;
const XDP_UMEM_PGOFF_COMPLETION_RING: i64 = 0x1_8000_0000;

const XDP_USE_NEED_WAKEUP: u16 = 1 << 3;
const XDP_RING_NEED_WAKEUP: u32 = 1;

// ── Struct layouts (matching kernel uapi) ───────────────────────────

#[repr(C)]
struct XdpUmemReg {
    addr: u64,
    len: u64,
    chunk_size: u32,
    headroom: u32,
    flags: u32,
    tx_metadata_len: u32,
}

#[repr(C)]
#[derive(Clone, Copy)]
struct SockaddrXdp {
    family: u16,
    flags: u16,
    ifindex: u32,
    queue_id: u32,
    shared_umem_fd: u32,
}

#[repr(C)]
#[derive(Clone, Copy, Default)]
pub struct XdpDesc {
    pub addr: u64,
    pub len: u32,
    pub options: u32,
}

#[repr(C)]
#[derive(Default)]
struct XdpRingOffset {
    producer: u64,
    consumer: u64,
    desc: u64,
    flags: u64,
}

#[repr(C)]
#[derive(Default)]
struct XdpMmapOffsets {
    rx: XdpRingOffset,
    tx: XdpRingOffset,
    fr: XdpRingOffset,
    cr: XdpRingOffset,
}

// ── Ring buffer ─────────────────────────────────────────────────────

/// A single-producer/single-consumer ring over mmap'd memory.
struct Ring {
    producer: *const AtomicU32,
    consumer: *const AtomicU32,
    flags: *const AtomicU32,
    descs: *mut u8, // Either *mut XdpDesc or *mut u64 depending on ring type.
    mask: u32,
}

// Safety: Ring pointers come from mmap and are valid for the socket lifetime.
unsafe impl Send for Ring {}
unsafe impl Sync for Ring {}

impl Ring {
    fn producer(&self) -> u32 {
        unsafe { (*self.producer).load(Ordering::Acquire) }
    }

    fn consumer(&self) -> u32 {
        unsafe { (*self.consumer).load(Ordering::Acquire) }
    }

    fn set_producer(&self, val: u32) {
        unsafe { (*self.producer).store(val, Ordering::Release) }
    }

    fn set_consumer(&self, val: u32) {
        unsafe { (*self.consumer).store(val, Ordering::Release) }
    }

    fn needs_wakeup(&self) -> bool {
        unsafe { (*self.flags).load(Ordering::Relaxed) & XDP_RING_NEED_WAKEUP != 0 }
    }
}

// ── XDP Socket ──────────────────────────────────────────────────────

/// Configuration for creating an XDP socket.
pub struct XdpConfig {
    pub ifname: String,
    pub queue_id: u32,
    pub frame_size: u32,
    pub num_frames: u32,
    pub ring_size: u32,
}

impl Default for XdpConfig {
    fn default() -> Self {
        Self {
            ifname: "eth0".into(),
            queue_id: 0,
            frame_size: 4096,
            num_frames: 4096,
            ring_size: 2048,
        }
    }
}

/// An AF_XDP socket with UMEM and ring buffers.
/// Provides zero-copy packet I/O for the encrypted UDP side.
pub struct XdpSocket {
    fd: i32,
    umem: *mut u8,
    umem_size: usize,
    frame_size: u32,
    rx_ring: Ring,
    tx_ring: Ring,
    fill_ring: Ring,
    comp_ring: Ring,
    ring_size: u32,
    /// Free frame indices for TX.
    free_frames: Vec<u64>,
}

// Safety: XdpSocket owns its fd and mmap regions.
unsafe impl Send for XdpSocket {}

impl XdpSocket {
    /// Create and configure an AF_XDP socket.
    ///
    /// # Safety
    /// This function uses mmap to create shared memory regions with the kernel.
    /// The returned XdpSocket must not outlive the process.
    pub fn create(config: &XdpConfig) -> io::Result<Self> {
        unsafe { Self::create_inner(config) }
    }

    unsafe fn create_inner(config: &XdpConfig) -> io::Result<Self> {
        let umem_size = (config.frame_size * config.num_frames) as usize;

        // 1. Allocate UMEM.
        let umem = libc::mmap(
            std::ptr::null_mut(),
            umem_size,
            libc::PROT_READ | libc::PROT_WRITE,
            libc::MAP_PRIVATE | libc::MAP_ANONYMOUS | libc::MAP_POPULATE,
            -1,
            0,
        );
        if umem == libc::MAP_FAILED {
            return Err(io::Error::last_os_error());
        }
        let umem = umem as *mut u8;

        // 2. Create socket.
        let fd = libc::socket(AF_XDP, libc::SOCK_RAW, 0);
        if fd < 0 {
            libc::munmap(umem as *mut _, umem_size);
            return Err(io::Error::last_os_error());
        }

        // 3. Register UMEM.
        let reg = XdpUmemReg {
            addr: umem as u64,
            len: umem_size as u64,
            chunk_size: config.frame_size,
            headroom: 0,
            flags: 0,
            tx_metadata_len: 0,
        };
        if setsockopt_raw(fd, SOL_XDP, XDP_UMEM_REG, &reg) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 4. Set ring sizes.
        let rs = config.ring_size as i32;
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);

        // 5. Query mmap offsets.
        let mut offsets = XdpMmapOffsets::default();
        let mut optlen = std::mem::size_of::<XdpMmapOffsets>() as u32;
        if libc::getsockopt(
            fd,
            SOL_XDP,
            XDP_MMAP_OFFSETS,
            &mut offsets as *mut _ as *mut _,
            &mut optlen,
        ) < 0
        {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 6. mmap rings.
        let rx_map = mmap_ring(fd, &offsets.rx, config.ring_size, XDP_PGOFF_RX_RING, std::mem::size_of::<XdpDesc>())?;
        let tx_map = mmap_ring(fd, &offsets.tx, config.ring_size, XDP_PGOFF_TX_RING, std::mem::size_of::<XdpDesc>())?;
        let fill_map = mmap_ring(fd, &offsets.fr, config.ring_size, XDP_UMEM_PGOFF_FILL_RING, 8)?;
        let comp_map = mmap_ring(fd, &offsets.cr, config.ring_size, XDP_UMEM_PGOFF_COMPLETION_RING, 8)?;

        let mask = config.ring_size - 1;
        let rx_ring = make_ring(rx_map, &offsets.rx, mask);
        let tx_ring = make_ring(tx_map, &offsets.tx, mask);
        let fill_ring = make_ring(fill_map, &offsets.fr, mask);
        let comp_ring = make_ring(comp_map, &offsets.cr, mask);

        // 7. Pre-populate fill ring (first half of frames for RX).
        let rx_frames = config.num_frames / 2;
        let mut prod = fill_ring.producer();
        for i in 0..rx_frames {
            let addr_ptr = fill_ring.descs.add(((prod & mask) as usize) * 8) as *mut u64;
            *addr_ptr = (i * config.frame_size) as u64;
            prod += 1;
        }
        fill_ring.set_producer(prod);

        // Second half of frames are free for TX.
        let mut free_frames = Vec::with_capacity((config.num_frames - rx_frames) as usize);
        for i in rx_frames..config.num_frames {
            free_frames.push((i * config.frame_size) as u64);
        }

        // 8. Bind.
        let ifindex = if_nametoindex(&config.ifname)?;
        let sxdp = SockaddrXdp {
            family: AF_XDP as u16,
            flags: XDP_USE_NEED_WAKEUP,
            ifindex,
            queue_id: config.queue_id,
            shared_umem_fd: 0,
        };
        if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        Ok(Self {
            fd,
            umem,
            umem_size,
            frame_size: config.frame_size,
            rx_ring,
            tx_ring,
            fill_ring,
            comp_ring,
            ring_size: config.ring_size,
            free_frames,
        })
    }

    /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
    /// Caller must call `rx_release` after processing to return frames to the fill ring.
    pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
        let prod = self.rx_ring.producer();
        let cons = self.rx_ring.consumer();
        let available = prod.wrapping_sub(cons);

        let mut packets = Vec::with_capacity(available as usize);
        for i in 0..available {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let desc = unsafe {
                &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
            };
            let data = unsafe {
                std::slice::from_raw_parts(
                    self.umem.add(desc.addr as usize),
                    desc.len as usize,
                )
            };
            packets.push((desc.addr, data));
        }
        packets
    }

    /// Release consumed RX frames back to the fill ring.
    pub fn rx_release(&mut self, frames: &[u64]) {
        // Update RX consumer.
        let cons = self.rx_ring.consumer().wrapping_add(frames.len() as u32);
        self.rx_ring.set_consumer(cons);

        // Add frames back to fill ring.
        let mut prod = self.fill_ring.producer();
        for &addr in frames {
            let idx = (prod & (self.ring_size - 1)) as usize;
            unsafe {
                let ptr = self.fill_ring.descs.add(idx * 8) as *mut u64;
                *ptr = addr;
            }
            prod = prod.wrapping_add(1);
        }
        self.fill_ring.set_producer(prod);
    }

    /// Send a packet via the TX ring. Returns false if no frames available.
    pub fn tx_send(&mut self, data: &[u8]) -> bool {
        let Some(frame_addr) = self.free_frames.pop() else {
            return false;
        };

        // Copy data into UMEM frame.
        unsafe {
            std::ptr::copy_nonoverlapping(
                data.as_ptr(),
                self.umem.add(frame_addr as usize),
                data.len(),
            );
        }

        // Write TX descriptor.
        let prod = self.tx_ring.producer();
        let idx = (prod & (self.ring_size - 1)) as usize;
        unsafe {
            let desc = &mut *(self.tx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *mut XdpDesc);
            desc.addr = frame_addr;
            desc.len = data.len() as u32;
            desc.options = 0;
        }
        self.tx_ring.set_producer(prod.wrapping_add(1));

        // Kick kernel if needed.
        if self.tx_ring.needs_wakeup() {
            unsafe {
                libc::sendto(self.fd, std::ptr::null(), 0, libc::MSG_DONTWAIT, std::ptr::null(), 0);
            }
        }

        true
    }

    /// Reclaim completed TX frames.
    pub fn tx_complete(&mut self) {
        let prod = self.comp_ring.producer();
        let cons = self.comp_ring.consumer();
        let done = prod.wrapping_sub(cons);

        for i in 0..done {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let addr = unsafe { *(self.comp_ring.descs.add(idx * 8) as *const u64) };
            self.free_frames.push(addr);
        }
        self.comp_ring.set_consumer(cons.wrapping_add(done));
    }

    /// Raw file descriptor (for poll/epoll).
    pub fn fd(&self) -> i32 {
        self.fd
    }
}

impl Drop for XdpSocket {
    fn drop(&mut self) {
        unsafe {
            libc::close(self.fd);
            libc::munmap(self.umem as *mut _, self.umem_size);
        }
    }
}

// ── Helpers ─────────────────────────────────────────────────────────

unsafe fn setsockopt_raw<T>(fd: i32, level: i32, optname: i32, val: &T) -> i32 {
    libc::setsockopt(
        fd,
        level,
        optname,
        val as *const _ as *const _,
        std::mem::size_of::<T>() as u32,
    )
}

unsafe fn mmap_ring(
    fd: i32,
    off: &XdpRingOffset,
    ring_size: u32,
    pgoff: i64,
    desc_size: usize,
) -> io::Result<*mut u8> {
    let size = off.desc as usize + (ring_size as usize) * desc_size;
    let ptr = libc::mmap(
        std::ptr::null_mut(),
        size,
        libc::PROT_READ | libc::PROT_WRITE,
        libc::MAP_SHARED | libc::MAP_POPULATE,
        fd,
        pgoff,
    );
    if ptr == libc::MAP_FAILED {
        return Err(io::Error::last_os_error());
    }
    Ok(ptr as *mut u8)
}

unsafe fn make_ring(base: *mut u8, off: &XdpRingOffset, mask: u32) -> Ring {
    Ring {
        producer: base.add(off.producer as usize) as *const AtomicU32,
        consumer: base.add(off.consumer as usize) as *const AtomicU32,
        flags: base.add(off.flags as usize) as *const AtomicU32,
        descs: base.add(off.desc as usize),
        mask,
    }
}

pub fn if_nametoindex(name: &str) -> io::Result<u32> {
    let cname = std::ffi::CString::new(name).map_err(|_| {
        io::Error::new(io::ErrorKind::InvalidInput, "invalid interface name")
    })?;
    let idx = unsafe { libc::if_nametoindex(cname.as_ptr()) };
    if idx == 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(idx)
}

```

## Symbols to evaluate

- constant AF_XDP (L20–L20)
- constant SOL_XDP (L21–L21)
- constant XDP_UMEM_REG (L23–L23)
- constant XDP_UMEM_FILL_RING (L24–L24)
- constant XDP_UMEM_COMPLETION_RING (L25–L25)
- constant XDP_RX_RING (L26–L26)
- constant XDP_TX_RING (L27–L27)
- constant XDP_MMAP_OFFSETS (L28–L28)
- constant XDP_PGOFF_RX_RING (L30–L30)
- constant XDP_PGOFF_TX_RING (L31–L31)
- constant XDP_UMEM_PGOFF_FILL_RING (L32–L32)
- constant XDP_UMEM_PGOFF_COMPLETION_RING (L33–L33)
- constant XDP_USE_NEED_WAKEUP (L35–L35)
- constant XDP_RING_NEED_WAKEUP (L36–L36)
- class XdpUmemReg (L41–L48)
- class SockaddrXdp (L52–L58)
- export class XdpDesc (L62–L66)
- class XdpRingOffset (L70–L75)
- class XdpMmapOffsets (L79–L84)
- class Ring (L89–L95)
- export class XdpConfig (L126–L132)
- export class XdpSocket (L148–L160)
- function setsockopt_raw (L407–L415)
- function mmap_ring (L417–L437)
- function make_ring (L439–L447)
- export function if_nametoindex (L449–L458)

## Test File

No test file found for this source file.

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Evaluate the test quality for each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] TransportSession: tests NONE → WEAK**: Deliberation reclassified: UTILITY DEAD→USED: This is a `pub struct` in a `-core` library crate (`rustguard-core`). Library crate public types are consumed by downstream crates in the workspace (e.g., `rustguard`). The utility evaluator likely only searched within the same crate and missed cross-crate imports, a classic false positive for workspace library crates. Reclassified to USED. TESTS NONE→WEAK: The merged field says NONE but the detail text itself says WEAK, and the source clearly contains 6 test functions in a `#[cfg(test)]` module covering encrypt/decrypt roundtrip, counter monotonicity, wrong-counter rejection, replay rejection, out-of-order delivery, and bidirectional use. The WEAK assessment is accurate: `encrypt_to` and `decrypt_in_place` (separate code paths using `seal_to`/`open_to`) have zero coverage, and nonce exhaustion / edge cases are untested. DOCUMENTATION UNDOCUMENTED→DOCUMENTED: The detail text itself concludes DOCUMENTED. The struct has a thorough `///` block (lines 7–11) explaining purpose, derivation, and key-role convention. Both public fields and all private fields carry `///` comments. Four of six methods have doc comments. Two public items (`new`, `send_counter`) lack doc comments (noted in best_practices rule 9), but the overall coverage is well above UNDOCUMENTED. The struct-level and field-level documentation is comprehensive.
- **[deliberation] COOKIE_LEN: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD reclassified → USED: this is a `pub const` in a library crate (`rustguard-core`). Downstream crates (e.g., message layout code) depend on it; pre-computed analysis likely doesn't capture cross-crate workspace imports. The entire cookie module is clearly used infrastructure for a WireGuard implementation. Tests NONE contradicts the detail text which says '[GOOD] Pure constant. Used throughout and indirectly validated in every roundtrip test.' For a pure constant with no runtime behavior, indirect validation is sufficient — reclassified to GOOD. Documentation UNDOCUMENTED is wrong — line 23 has `/// Cookie size.` which IS a doc comment, making this PARTIAL (brief but present).
- **[deliberation] now: tests WEAK → GOOD**: Deliberation reclassified: Tests WEAK reclassified → GOOD: a 2-line wrapper around `Instant::now()` has nothing meaningful to test directly. It is called in every test via `CookieChecker::new()`. Requiring dedicated tests for this trivial wrapper is unreasonable. Documentation UNDOCUMENTED kept — no doc comment exists, though the function is private and self-descriptive.
- **[deliberation] CookieChecker: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD reclassified → USED: CookieChecker is the primary server-side public API of this module in a library crate. Pre-computed cross-crate analysis likely misses workspace-level imports. The entire module exists to support WireGuard cookie DoS protection — this struct is clearly intended for use by consuming crates. Tests NONE contradicts the detail text which says 'happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests' — reclassified to WEAK (tested but with gaps in verify_mac1, rotation, new_with). Documentation UNDOCUMENTED contradicts detail: 'Struct has a /// summary and all four fields have field-level /// comments.' Line 49 has a doc comment. Reclassified to PARTIAL (docs exist but missing examples and constructor docs).
- **[deliberation] CookieState: tests NONE → WEAK**: Deliberation reclassified: Correction NEEDS_FIX kept: unlike elapsed_since, the consequence here is that process_reply successfully decrypts a cookie but never marks it as fresh, making compute_mac2 silently return zeros on no_std. While the overall no_std design is intentional, this specific behavior is more insidious — a successful decryption followed by silent failure to use the cookie is a subtle trap with no comment explaining it. Utility DEAD → USED: same reasoning as CookieChecker — primary client-side public API in a library crate. Tests NONE → WEAK: detail says 'Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie.' Documentation UNDOCUMENTED → PARTIAL: detail says 'Struct has a /// summary and both private fields have doc comments.'
- **[deliberation] ReplayWindow: tests NONE → WEAK**: Deliberation reclassified: CORRECTION ERROR confirmed at high confidence: shift_window's bit-shift block (L112-116) iterates in reverse with right-shift (>>), moving bits toward lower ages instead of higher ages. Verified with concrete example: top=0, bitmap[0]=1, then shift_window(1) right-shifts bitmap[0] to 0, losing the counter-0 mark. A subsequent check(0) would incorrectly accept the replay. The word-level copy_within is correct; only the sub-word bit-shift is inverted. Tests don't catch this because they never re-check a previously-seen counter after a shift has occurred. UTILITY reclassified DEAD→USED: this is a pub struct in a library crate (rustguard-core). Library public API types are not dead simply because no in-tree consumer is visible — the struct is the module's primary export and is extensively exercised in tests. TESTS reclassified NONE→WEAK: the detail text itself says '[WEAK]' and describes solid behavioral coverage via check_and_update (sequential, duplicate rejection, out-of-order, boundary, stress). However, check() and update() are never tested independently, and the critical shift_window bit-direction bug goes undetected due to test design gaps. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the struct's two fields have /// doc comments (L15, L17), and all three public methods have /// doc comments. Only the struct-level definition and new() constructor lack /// docs. This is clearly partial, not absent.
- **[deliberation] hash: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive: this is a library crate's public API. Exported functions in a crypto library ARE the product — absence of in-workspace importers does not make them dead. The crate is designed to be consumed externally (rustguard-crypto). Tests NONE contradicts the detail text which describes three existing tests (hash_deterministic, hash_different_inputs, hash_multi_chunk); reclassified to WEAK since tests exist but lack reference vectors. Documentation UNDOCUMENTED contradicts the detail and source: line 8 has `/// BLAKE2s-256 hash.` — it's PARTIAL (brief but present, missing params/examples).
- **[deliberation] mac: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive for the same library-crate reason as hash. Tests NONE contradicts the detail text which describes mac_differs_with_key and indirect coverage via hmac_differs_from_keyed_blake2s; reclassified to WEAK (tests exist but no reference vectors). Documentation UNDOCUMENTED contradicts the source: lines 17-20 have a multi-line /// block explaining keyed MAC vs HMAC distinction. Reclassified to PARTIAL per the detail's own assessment.
- **[deliberation] hkdf: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive: hkdf is a core public function of a crypto library crate, essential for the WireGuard KDF. Tests NONE contradicts the detail which describes two tests (hkdf_produces_different_outputs, hkdf_deterministic); reclassified to WEAK since no RFC 5869/WireGuard spec test vectors are present. Documentation UNDOCUMENTED contradicts source: lines 31-36 have a substantial /// block describing the algorithm and return semantics. Reclassified to PARTIAL.
- **[deliberation] AEAD_TAG_LEN: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD→USED: This is a `pub const` in a library crate (`rustguard-crypto`). It is the fundamental tag length constant meant for downstream consumers. The evaluator itself noted it matches Known False Positives. Zero in-crate importers is expected for a library public API constant. Tests NONE→GOOD: Pure compile-time constant with no runtime behavior; indirectly validated in `seal_then_open` assertion (`ciphertext.len() == plaintext.len() + AEAD_TAG_LEN`). No dedicated test needed. Documentation UNDOCUMENTED confirmed: line 7 has no `///` comment, so UNDOCUMENTED is correct.
- **[deliberation] MAX_PACKET_SIZE: tests NONE → GOOD**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the doc comment on L9 says 'MTU 1420 + tag' but the expression computes `1500 + AEAD_TAG_LEN = 1516`, a clear mismatch (~80 bytes difference). Either the doc or the value is wrong. Utility DEAD→USED: library public constant for downstream consumption, same false positive pattern as AEAD_TAG_LEN. Tests NONE→GOOD: compile-time constant with no runtime behavior, no dedicated test required. Documentation UNDOCUMENTED→PARTIAL: line 9 has a `///` doc comment present (`/// Maximum WireGuard transport payload (MTU 1420 + tag).`), so UNDOCUMENTED is incorrect. Reclassified to PARTIAL due to the inaccurate MTU value in the doc.
- **[deliberation] seal: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD→USED: primary public encryption API of the library crate, consumed by downstream crates. Tests NONE→WEAK: `seal` is directly called in all 5 test functions (seal_then_open, wrong_key_fails, wrong_counter_fails, tampered_ciphertext_fails, aad_mismatch_fails), so NONE is factually incorrect. Reclassified to WEAK since edge cases (empty plaintext, large payloads, non-zero counter as primary input) are missing. Documentation UNDOCUMENTED→PARTIAL: has a multi-line `///` doc block on L12-15 describing purpose, nonce construction, and AAD semantics. Missing param-level docs and examples, hence PARTIAL not DOCUMENTED.
- **[deliberation] open: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD→USED: primary public decryption API of the library crate. Tests NONE→GOOD: five tests thoroughly exercise `open` — happy path, wrong key, wrong counter, tampered ciphertext, and AAD mismatch. All four primary authentication-failure modes are covered. NONE is clearly wrong. Documentation UNDOCUMENTED→PARTIAL: has a `///` block on L31-33 describing purpose and three failure conditions for `None` return. Missing param-level docs and examples, hence PARTIAL.
- **[deliberation] Tai64n: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD → USED: This is a `pub struct` in `rustguard-crypto`, a library crate whose entire purpose is to export types for cross-crate consumption. The evaluator itself noted this false-positive pattern and reduced confidence. Cross-crate imports were outside analysis scope, and similar symbols (TransportSession, CookieChecker) were previously misclassified. Reclassified with high confidence. Tests NONE → WEAK: The symbol field says NONE but the detail section itself begins with '[WEAK] Three inline tests cover monotonicity...' — there are clearly three tests in the `mod tests` block (L50-L80). They exist but only check roundtrip equality, never assert against known-correct byte encodings. WEAK is the accurate classification. Documentation UNDOCUMENTED → PARTIAL: Lines 4-7 contain four `///` doc comments on the struct covering purpose, byte layout, WireGuard use-case, and replay-monotonicity invariant. The detail section even states 'Struct carries four lines of /// doc comments — solid struct-level narrative.' The struct is documented; only `as_bytes` and `from_bytes` methods lack docs. PARTIAL is correct.
- **[deliberation] JoinConfig: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD → USED: JoinConfig is a public API struct serving as the sole parameter to the crate's primary entry point `run()`. The DEAD classification is a systematic false positive from cross-crate analysis not capturing workspace-internal consumers. This matches the documented false-positive pattern for library crate public types. Tests NONE → GOOD: The evaluator's own detail text says 'Per rule 6, types/interfaces/structs with no runtime behavior are GOOD by default' — this is a plain data struct with two fields and no methods, so GOOD is correct. Documentation UNDOCUMENTED kept: valid finding, public config type lacks /// doc comments and field descriptions.
- **[deliberation] ParsedUdp: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD → USED: This is a pub struct in a library crate returned by the public parse_eth_udp function. The original detail itself acknowledges this matches the Known False Positives pattern for library crate exports consumed by downstream crates. Tests NONE → WEAK: The detail text itself states 'The struct's two fields (src_addr, payload) are exercised by the single inline test parse_ipv4_udp_frame, which asserts both field values.' This is partial coverage (IPv4 path only, no IPv6), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Struct has a /// doc comment on L8' — the struct-level doc exists on line 8. Only the per-field docs are missing. That is PARTIAL, not UNDOCUMENTED.
- **[deliberation] parse_eth_udp: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD → USED: Public function in a library crate serving as the main entry point for Ethernet frame parsing. The original detail itself notes it matches the Known False Positives pattern for library crate public APIs consumed by downstream crates not visible in same-crate import analysis. Tests NONE → WEAK: The detail states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path through parse_eth_udp.' This is exercised but with limited coverage (no IPv6 branch, no error paths), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Has a two-line /// block (L14–L15) describing purpose and the None return condition.' A doc comment exists; it merely lacks #Examples and parameter descriptions. That is PARTIAL.
- **[deliberation] PersistedPeer: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD is a false positive: PersistedPeer is a pub struct in a library crate (rustguard-enroll) intended to be consumed by other workspace crates. Zero in-file importers is expected for a public API type — the evaluator only searched within the crate boundary. Tests reclassified from NONE to GOOD: the detail text itself states 'Both inline tests directly construct and assert on PersistedPeer instances, exercising both public fields' and concludes GOOD for a plain data struct — the NONE field value contradicts the detail. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 11 clearly shows `/// A persisted peer — just enough to re-enroll on restart.` and best_practices rule 9 confirms 'All public items carry /// doc comments'. Field-level docs are absent but struct-level doc exists.
- **[deliberation] save: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate meant for external consumers. Tests reclassified from NONE to WEAK: the detail text itself says 'Covered only by the save_and_load_roundtrip happy-path test with two peers' — the function IS called in the test at line 108, so NONE is factually incorrect. WEAK is appropriate given only the happy path is covered and error paths are untested. Documentation reclassified from UNDOCUMENTED to PARTIAL: lines 24-25 show a two-line /// doc comment describing purpose and format. It exists but lacks #Errors and parameter docs.
- **[deliberation] load: tests NONE → WEAK**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate for external consumption. Tests reclassified from NONE to WEAK: both test functions call load() — save_and_load_roundtrip (line 109) and load_nonexistent_returns_empty (line 116). The detail text itself describes WEAK coverage with specific gaps (error paths untested). NONE is factually wrong. Documentation reclassified from UNDOCUMENTED to PARTIAL: line 48 shows `/// Load peers from the state file.` — the doc exists but is minimal, missing #Errors section and the important NotFound → Ok(vec![]) behavior.
- **[deliberation] MSG_TRANSPORT: tests NONE → GOOD**: Deliberation reclassified: Utility DEAD is a false positive: MSG_TRANSPORT is a pub(crate) protocol constant for WireGuard transport data messages (type 4). It is almost certainly imported by other modules handling the data path (e.g., transport/data handling). The 0-importer count reflects incomplete cross-module analysis. Tests reclassified to GOOD per rule 6 (compile-time constant). Documentation reclassified to DOCUMENTED: source line 247 clearly shows `/// Message type 4: Transport Data.` preceding the constant definition. The merged JSON had an internal inconsistency where the field said UNDOCUMENTED but the detail text said DOCUMENTED.
- **[deliberation] last_os_error: tests NONE → GOOD**: Deliberation reclassified: Tests reclassified NONE→GOOD: this is a single-line delegation to io::Error::last_os_error() with zero logic to test. Testing it would only test the stdlib. Documentation reclassified: private one-liner whose name and body are entirely self-explanatory; no doc comment needed.


## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "AF_XDP",
      "line_start": 20,
      "line_end": 20,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time constant mirroring kernel UAPI value. No runtime behavior to test directly; consistent with Known False Positives precedent (COOKIE_LEN, AEAD_TAG_LEN) — GOOD by rule 6 analogy."
    },
    {
      "name": "SOL_XDP",
      "line_start": 21,
      "line_end": 21,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time constant mirroring kernel UAPI value. No runtime behavior to test directly; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_UMEM_REG",
      "line_start": 23,
      "line_end": 23,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_UMEM_FILL_RING",
      "line_start": 24,
      "line_end": 24,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_UMEM_COMPLETION_RING",
      "line_start": 25,
      "line_end": 25,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_RX_RING",
      "line_start": 26,
      "line_end": 26,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_TX_RING",
      "line_start": 27,
      "line_end": 27,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_MMAP_OFFSETS",
      "line_start": 28,
      "line_end": 28,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_PGOFF_RX_RING",
      "line_start": 30,
      "line_end": 30,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time mmap page offset constant from kernel UAPI. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_PGOFF_TX_RING",
      "line_start": 31,
      "line_end": 31,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time mmap page offset constant from kernel UAPI. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_UMEM_PGOFF_FILL_RING",
      "line_start": 32,
      "line_end": 32,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time mmap page offset constant from kernel UAPI. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_UMEM_PGOFF_COMPLETION_RING",
      "line_start": 33,
      "line_end": 33,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time mmap page offset constant from kernel UAPI. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_USE_NEED_WAKEUP",
      "line_start": 35,
      "line_end": 35,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time flag constant (1 << 3). No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XDP_RING_NEED_WAKEUP",
      "line_start": 36,
      "line_end": 36,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Compile-time flag constant used as a bitmask. No runtime behavior; GOOD by rule 6 analogy."
    },
    {
      "name": "XdpUmemReg",
      "line_start": 41,
      "line_end": 48,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Plain #[repr(C)] data struct with no methods or runtime behavior. Per rule 6, types with no runtime behavior are GOOD by default."
    },
    {
      "name": "SockaddrXdp",
      "line_start": 52,
      "line_end": 58,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Plain #[repr(C)] data struct with no methods or runtime behavior. Per rule 6, types with no runtime behavior are GOOD by default."
    },
    {
      "name": "XdpDesc",
      "line_start": 62,
      "line_end": 66,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Public plain #[repr(C)] data struct with no methods or runtime behavior. Per rule 6, types with no runtime behavior are GOOD by default."
    },
    {
      "name": "XdpRingOffset",
      "line_start": 70,
      "line_end": 75,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Plain #[repr(C)] data struct deriving Default with no custom methods or runtime logic. Per rule 6, types with no runtime behavior are GOOD by default."
    },
    {
      "name": "XdpMmapOffsets",
      "line_start": 79,
      "line_end": 84,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Plain #[repr(C)] data struct composed of XdpRingOffset fields with no methods or runtime logic. Per rule 6, types with no runtime behavior are GOOD by default."
    },
    {
      "name": "Ring",
      "line_start": 89,
      "line_end": 95,
      "tests": "NONE",
      "confidence": 95,
      "detail": "Private struct with multiple runtime methods (producer, consumer, set_producer, set_consumer, needs_wakeup) performing atomic loads/stores via raw unsafe pointers. No test file exists for this module; zero test coverage."
    },
    {
      "name": "XdpConfig",
      "line_start": 126,
      "line_end": 132,
      "tests": "GOOD",
      "confidence": 95,
      "detail": "Public configuration struct with a trivial Default impl. Analogous to JoinConfig (reclassified GOOD in Known False Positives) — pure data carrier with no meaningful runtime logic to test. Per rule 6."
    },
    {
      "name": "XdpSocket",
      "line_start": 148,
      "line_end": 160,
      "tests": "NONE",
      "confidence": 95,
      "detail": "Core public struct with complex runtime methods (create, rx_poll, rx_release, tx_send, tx_complete, fd) involving unsafe syscalls, mmap, ring buffer manipulation, and Drop cleanup. No test file found for this module; zero test coverage for any of these critical code paths."
    },
    {
      "name": "setsockopt_raw",
      "line_start": 407,
      "line_end": 415,
      "tests": "NONE",
      "confidence": 95,
      "detail": "Private unsafe helper wrapping libc::setsockopt. Has runtime behavior (syscall invocation). No test file found for this module; zero test coverage."
    },
    {
      "name": "mmap_ring",
      "line_start": 417,
      "line_end": 437,
      "tests": "NONE",
      "confidence": 95,
      "detail": "Private unsafe function performing mmap syscall with fallibility (io::Result). Contains non-trivial size computation logic. No test file found; zero test coverage."
    },
    {
      "name": "make_ring",
      "line_start": 439,
      "line_end": 447,
      "tests": "NONE",
      "confidence": 95,
      "detail": "Private unsafe function constructing a Ring from raw pointer arithmetic. Runtime behavior with pointer offset calculations. No test file found; zero test coverage."
    },
    {
      "name": "if_nametoindex",
      "line_start": 449,
      "line_end": 458,
      "tests": "NONE",
      "confidence": 95,
      "detail": "Public function wrapping libc::if_nametoindex with CString conversion and error handling. Has meaningful runtime behavior including the null-termination check and OS error mapping. No test file found for this module; zero test coverage."
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.2224 | **Duration:** 69.9s


---

# Axis: best_practices

## User

## File: `rustguard-tun/src/xdp.rs`
## Language: rust
## Context: general

```rust
//! AF_XDP zero-copy UDP I/O for Linux.
//!
//! Replaces the standard UDP socket for receiving/sending WireGuard
//! encrypted packets. The plaintext side still uses TUN.
//!
//! Architecture:
//!   NIC -> XDP BPF filter (UDP:51820) -> AF_XDP socket -> decrypt -> TUN
//!   TUN -> encrypt -> AF_XDP TX ring -> NIC
//!
//! The unsafe is contained in this module. The ring buffer protocol is
//! lock-free (atomic producer/consumer indices on shared mmap'd memory).

#![cfg(target_os = "linux")]

use std::io;
use std::sync::atomic::{AtomicU32, Ordering};

// ── Constants ───────────────────────────────────────────────────────

const AF_XDP: i32 = 44;
const SOL_XDP: i32 = 283;

const XDP_UMEM_REG: i32 = 4;
const XDP_UMEM_FILL_RING: i32 = 5;
const XDP_UMEM_COMPLETION_RING: i32 = 6;
const XDP_RX_RING: i32 = 2;
const XDP_TX_RING: i32 = 3;
const XDP_MMAP_OFFSETS: i32 = 1;

const XDP_PGOFF_RX_RING: i64 = 0;
const XDP_PGOFF_TX_RING: i64 = 0x0_8000_0000;
const XDP_UMEM_PGOFF_FILL_RING: i64 = 0x1_0000_0000;
const XDP_UMEM_PGOFF_COMPLETION_RING: i64 = 0x1_8000_0000;

const XDP_USE_NEED_WAKEUP: u16 = 1 << 3;
const XDP_RING_NEED_WAKEUP: u32 = 1;

// ── Struct layouts (matching kernel uapi) ───────────────────────────

#[repr(C)]
struct XdpUmemReg {
    addr: u64,
    len: u64,
    chunk_size: u32,
    headroom: u32,
    flags: u32,
    tx_metadata_len: u32,
}

#[repr(C)]
#[derive(Clone, Copy)]
struct SockaddrXdp {
    family: u16,
    flags: u16,
    ifindex: u32,
    queue_id: u32,
    shared_umem_fd: u32,
}

#[repr(C)]
#[derive(Clone, Copy, Default)]
pub struct XdpDesc {
    pub addr: u64,
    pub len: u32,
    pub options: u32,
}

#[repr(C)]
#[derive(Default)]
struct XdpRingOffset {
    producer: u64,
    consumer: u64,
    desc: u64,
    flags: u64,
}

#[repr(C)]
#[derive(Default)]
struct XdpMmapOffsets {
    rx: XdpRingOffset,
    tx: XdpRingOffset,
    fr: XdpRingOffset,
    cr: XdpRingOffset,
}

// ── Ring buffer ─────────────────────────────────────────────────────

/// A single-producer/single-consumer ring over mmap'd memory.
struct Ring {
    producer: *const AtomicU32,
    consumer: *const AtomicU32,
    flags: *const AtomicU32,
    descs: *mut u8, // Either *mut XdpDesc or *mut u64 depending on ring type.
    mask: u32,
}

// Safety: Ring pointers come from mmap and are valid for the socket lifetime.
unsafe impl Send for Ring {}
unsafe impl Sync for Ring {}

impl Ring {
    fn producer(&self) -> u32 {
        unsafe { (*self.producer).load(Ordering::Acquire) }
    }

    fn consumer(&self) -> u32 {
        unsafe { (*self.consumer).load(Ordering::Acquire) }
    }

    fn set_producer(&self, val: u32) {
        unsafe { (*self.producer).store(val, Ordering::Release) }
    }

    fn set_consumer(&self, val: u32) {
        unsafe { (*self.consumer).store(val, Ordering::Release) }
    }

    fn needs_wakeup(&self) -> bool {
        unsafe { (*self.flags).load(Ordering::Relaxed) & XDP_RING_NEED_WAKEUP != 0 }
    }
}

// ── XDP Socket ──────────────────────────────────────────────────────

/// Configuration for creating an XDP socket.
pub struct XdpConfig {
    pub ifname: String,
    pub queue_id: u32,
    pub frame_size: u32,
    pub num_frames: u32,
    pub ring_size: u32,
}

impl Default for XdpConfig {
    fn default() -> Self {
        Self {
            ifname: "eth0".into(),
            queue_id: 0,
            frame_size: 4096,
            num_frames: 4096,
            ring_size: 2048,
        }
    }
}

/// An AF_XDP socket with UMEM and ring buffers.
/// Provides zero-copy packet I/O for the encrypted UDP side.
pub struct XdpSocket {
    fd: i32,
    umem: *mut u8,
    umem_size: usize,
    frame_size: u32,
    rx_ring: Ring,
    tx_ring: Ring,
    fill_ring: Ring,
    comp_ring: Ring,
    ring_size: u32,
    /// Free frame indices for TX.
    free_frames: Vec<u64>,
}

// Safety: XdpSocket owns its fd and mmap regions.
unsafe impl Send for XdpSocket {}

impl XdpSocket {
    /// Create and configure an AF_XDP socket.
    ///
    /// # Safety
    /// This function uses mmap to create shared memory regions with the kernel.
    /// The returned XdpSocket must not outlive the process.
    pub fn create(config: &XdpConfig) -> io::Result<Self> {
        unsafe { Self::create_inner(config) }
    }

    unsafe fn create_inner(config: &XdpConfig) -> io::Result<Self> {
        let umem_size = (config.frame_size * config.num_frames) as usize;

        // 1. Allocate UMEM.
        let umem = libc::mmap(
            std::ptr::null_mut(),
            umem_size,
            libc::PROT_READ | libc::PROT_WRITE,
            libc::MAP_PRIVATE | libc::MAP_ANONYMOUS | libc::MAP_POPULATE,
            -1,
            0,
        );
        if umem == libc::MAP_FAILED {
            return Err(io::Error::last_os_error());
        }
        let umem = umem as *mut u8;

        // 2. Create socket.
        let fd = libc::socket(AF_XDP, libc::SOCK_RAW, 0);
        if fd < 0 {
            libc::munmap(umem as *mut _, umem_size);
            return Err(io::Error::last_os_error());
        }

        // 3. Register UMEM.
        let reg = XdpUmemReg {
            addr: umem as u64,
            len: umem_size as u64,
            chunk_size: config.frame_size,
            headroom: 0,
            flags: 0,
            tx_metadata_len: 0,
        };
        if setsockopt_raw(fd, SOL_XDP, XDP_UMEM_REG, &reg) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 4. Set ring sizes.
        let rs = config.ring_size as i32;
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);

        // 5. Query mmap offsets.
        let mut offsets = XdpMmapOffsets::default();
        let mut optlen = std::mem::size_of::<XdpMmapOffsets>() as u32;
        if libc::getsockopt(
            fd,
            SOL_XDP,
            XDP_MMAP_OFFSETS,
            &mut offsets as *mut _ as *mut _,
            &mut optlen,
        ) < 0
        {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 6. mmap rings.
        let rx_map = mmap_ring(fd, &offsets.rx, config.ring_size, XDP_PGOFF_RX_RING, std::mem::size_of::<XdpDesc>())?;
        let tx_map = mmap_ring(fd, &offsets.tx, config.ring_size, XDP_PGOFF_TX_RING, std::mem::size_of::<XdpDesc>())?;
        let fill_map = mmap_ring(fd, &offsets.fr, config.ring_size, XDP_UMEM_PGOFF_FILL_RING, 8)?;
        let comp_map = mmap_ring(fd, &offsets.cr, config.ring_size, XDP_UMEM_PGOFF_COMPLETION_RING, 8)?;

        let mask = config.ring_size - 1;
        let rx_ring = make_ring(rx_map, &offsets.rx, mask);
        let tx_ring = make_ring(tx_map, &offsets.tx, mask);
        let fill_ring = make_ring(fill_map, &offsets.fr, mask);
        let comp_ring = make_ring(comp_map, &offsets.cr, mask);

        // 7. Pre-populate fill ring (first half of frames for RX).
        let rx_frames = config.num_frames / 2;
        let mut prod = fill_ring.producer();
        for i in 0..rx_frames {
            let addr_ptr = fill_ring.descs.add(((prod & mask) as usize) * 8) as *mut u64;
            *addr_ptr = (i * config.frame_size) as u64;
            prod += 1;
        }
        fill_ring.set_producer(prod);

        // Second half of frames are free for TX.
        let mut free_frames = Vec::with_capacity((config.num_frames - rx_frames) as usize);
        for i in rx_frames..config.num_frames {
            free_frames.push((i * config.frame_size) as u64);
        }

        // 8. Bind.
        let ifindex = if_nametoindex(&config.ifname)?;
        let sxdp = SockaddrXdp {
            family: AF_XDP as u16,
            flags: XDP_USE_NEED_WAKEUP,
            ifindex,
            queue_id: config.queue_id,
            shared_umem_fd: 0,
        };
        if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        Ok(Self {
            fd,
            umem,
            umem_size,
            frame_size: config.frame_size,
            rx_ring,
            tx_ring,
            fill_ring,
            comp_ring,
            ring_size: config.ring_size,
            free_frames,
        })
    }

    /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
    /// Caller must call `rx_release` after processing to return frames to the fill ring.
    pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
        let prod = self.rx_ring.producer();
        let cons = self.rx_ring.consumer();
        let available = prod.wrapping_sub(cons);

        let mut packets = Vec::with_capacity(available as usize);
        for i in 0..available {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let desc = unsafe {
                &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
            };
            let data = unsafe {
                std::slice::from_raw_parts(
                    self.umem.add(desc.addr as usize),
                    desc.len as usize,
                )
            };
            packets.push((desc.addr, data));
        }
        packets
    }

    /// Release consumed RX frames back to the fill ring.
    pub fn rx_release(&mut self, frames: &[u64]) {
        // Update RX consumer.
        let cons = self.rx_ring.consumer().wrapping_add(frames.len() as u32);
        self.rx_ring.set_consumer(cons);

        // Add frames back to fill ring.
        let mut prod = self.fill_ring.producer();
        for &addr in frames {
            let idx = (prod & (self.ring_size - 1)) as usize;
            unsafe {
                let ptr = self.fill_ring.descs.add(idx * 8) as *mut u64;
                *ptr = addr;
            }
            prod = prod.wrapping_add(1);
        }
        self.fill_ring.set_producer(prod);
    }

    /// Send a packet via the TX ring. Returns false if no frames available.
    pub fn tx_send(&mut self, data: &[u8]) -> bool {
        let Some(frame_addr) = self.free_frames.pop() else {
            return false;
        };

        // Copy data into UMEM frame.
        unsafe {
            std::ptr::copy_nonoverlapping(
                data.as_ptr(),
                self.umem.add(frame_addr as usize),
                data.len(),
            );
        }

        // Write TX descriptor.
        let prod = self.tx_ring.producer();
        let idx = (prod & (self.ring_size - 1)) as usize;
        unsafe {
            let desc = &mut *(self.tx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *mut XdpDesc);
            desc.addr = frame_addr;
            desc.len = data.len() as u32;
            desc.options = 0;
        }
        self.tx_ring.set_producer(prod.wrapping_add(1));

        // Kick kernel if needed.
        if self.tx_ring.needs_wakeup() {
            unsafe {
                libc::sendto(self.fd, std::ptr::null(), 0, libc::MSG_DONTWAIT, std::ptr::null(), 0);
            }
        }

        true
    }

    /// Reclaim completed TX frames.
    pub fn tx_complete(&mut self) {
        let prod = self.comp_ring.producer();
        let cons = self.comp_ring.consumer();
        let done = prod.wrapping_sub(cons);

        for i in 0..done {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let addr = unsafe { *(self.comp_ring.descs.add(idx * 8) as *const u64) };
            self.free_frames.push(addr);
        }
        self.comp_ring.set_consumer(cons.wrapping_add(done));
    }

    /// Raw file descriptor (for poll/epoll).
    pub fn fd(&self) -> i32 {
        self.fd
    }
}

impl Drop for XdpSocket {
    fn drop(&mut self) {
        unsafe {
            libc::close(self.fd);
            libc::munmap(self.umem as *mut _, self.umem_size);
        }
    }
}

// ── Helpers ─────────────────────────────────────────────────────────

unsafe fn setsockopt_raw<T>(fd: i32, level: i32, optname: i32, val: &T) -> i32 {
    libc::setsockopt(
        fd,
        level,
        optname,
        val as *const _ as *const _,
        std::mem::size_of::<T>() as u32,
    )
}

unsafe fn mmap_ring(
    fd: i32,
    off: &XdpRingOffset,
    ring_size: u32,
    pgoff: i64,
    desc_size: usize,
) -> io::Result<*mut u8> {
    let size = off.desc as usize + (ring_size as usize) * desc_size;
    let ptr = libc::mmap(
        std::ptr::null_mut(),
        size,
        libc::PROT_READ | libc::PROT_WRITE,
        libc::MAP_SHARED | libc::MAP_POPULATE,
        fd,
        pgoff,
    );
    if ptr == libc::MAP_FAILED {
        return Err(io::Error::last_os_error());
    }
    Ok(ptr as *mut u8)
}

unsafe fn make_ring(base: *mut u8, off: &XdpRingOffset, mask: u32) -> Ring {
    Ring {
        producer: base.add(off.producer as usize) as *const AtomicU32,
        consumer: base.add(off.consumer as usize) as *const AtomicU32,
        flags: base.add(off.flags as usize) as *const AtomicU32,
        descs: base.add(off.desc as usize),
        mask,
    }
}

pub fn if_nametoindex(name: &str) -> io::Result<u32> {
    let cname = std::ffi::CString::new(name).map_err(|_| {
        io::Error::new(io::ErrorKind::InvalidInput, "invalid interface name")
    })?;
    let idx = unsafe { libc::if_nametoindex(cname.as_ptr()) };
    if idx == 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(idx)
}

```

## File stats
- Lines: 458
- Symbols: 26
- Exported symbols: 4

## Project Structure

```
├── rustguard-cli/
│   └── src/
│       └── main.rs
├── rustguard-core/
│   ├── src/
│   │   ├── cookie.rs
│   │   ├── handshake.rs
│   │   ├── lib.rs
│   │   ├── messages.rs
│   │   ├── replay.rs
│   │   ├── session.rs
│   │   └── timers.rs
│   └── tests/
│       └── integration.rs
├── rustguard-crypto/
│   └── src/
│       ├── aead.rs
│       ├── blake2s.rs
│       ├── lib.rs
│       ├── tai64n.rs
│       └── x25519.rs
├── rustguard-daemon/
│   └── src/
│       ├── config.rs
│       ├── lib.rs
│       ├── peer.rs
│       └── tunnel.rs
├── rustguard-enroll/
│   └── src/
│       ├── client.rs
│       ├── control.rs
│       ├── fast_udp.rs
│       ├── lib.rs
│       ├── packet.rs
│       ├── pool.rs
│       ├── protocol.rs
│       ├── server.rs
│       └── state.rs
├── rustguard-kmod/
│   └── src/
│       ├── allowedips.rs
│       ├── cookie.rs
│       ├── lib.rs
│       ├── noise.rs
│       ├── replay.rs
│       └── timers.rs
└── rustguard-tun/
    ├── examples/
    │   └── tun_echo.rs
    └── src/
        ├── bpf_loader.rs
        ├── lib.rs
        ├── linux_mq.rs
        ├── linux.rs
        ├── macos.rs
        ├── uring.rs
        └── xdp.rs
```

Use the project structure above to evaluate rule 11 (import organization) and detect file placement inconsistencies. If the file is in an unexpected directory given the project conventions, flag it as WARN for the most relevant rule.

Evaluate all 17 rules and output the JSON.

## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "score": 6.0,
  "rules": [
    {
      "rule_id": 1,
      "rule_name": "No unwrap in production code",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No .unwrap() calls anywhere in the file. Errors are propagated via io::Error::last_os_error() with early returns, the ? operator on fallible helpers, and explicit if < 0 guards. Correct throughout."
    },
    {
      "rule_id": 2,
      "rule_name": "No unsafe blocks without clear justification comment",
      "status": "WARN",
      "severity": "CRITICAL",
      "detail": "Partial compliance. The module-level //! docstring explains the unsafe strategy, and unsafe impl Send for Ring/XdpSocket both carry // Safety: comments. XdpSocket::create has a # Safety doc section. However, six inline unsafe blocks inside safe public functions (rx_poll x2, rx_release, tx_send x3, Drop::drop) have no // Safety: comment explaining which invariants are upheld (e.g., that idx is bounded by the mask, that UMEM addresses are valid, that desc.len does not exceed frame_size).",
      "lines": "L260-L280, L295-L305, L316-L342, L348-L352"
    },
    {
      "rule_id": 3,
      "rule_name": "Proper error handling with Result/Option (no silent ignores)",
      "status": "FAIL",
      "severity": "HIGH",
      "detail": "Step 4 of create_inner calls setsockopt_raw four times (XDP_UMEM_FILL_RING, XDP_UMEM_COMPLETION_RING, XDP_RX_RING, XDP_TX_RING) and discards every return value. A kernel failure here silently produces a misconfigured socket that will appear to work but produce no traffic. Additionally, the wakeup libc::sendto call inside tx_send ignores its return value.",
      "lines": "L207-L210, L333-L337"
    },
    {
      "rule_id": 4,
      "rule_name": "Derive common traits on public types",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "XdpConfig is a public configuration struct with no derives at all — at minimum Debug and Clone are expected for usability and testability. XdpDesc is #[repr(C)] pub and derives Clone + Copy but is missing Debug and PartialEq. Internal types (Ring, SockaddrXdp, XdpRingOffset, XdpMmapOffsets) are private and not subject to this rule.",
      "lines": "L63-L67, L116-L124"
    },
    {
      "rule_id": 5,
      "rule_name": "Lifetime annotations explicit where needed",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "The single-input-reference elision on rx_poll(&self) -> Vec<(u64, &[u8])> is unambiguous under Rust's elision rules: the output &[u8] receives the lifetime of &self, which is the correct and intended constraint. No unnecessary or misleading elisions were found elsewhere."
    },
    {
      "rule_id": 6,
      "rule_name": "Use clippy idioms",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "No unnecessary .clone() calls found. Manual indexed loops over ring descriptors are appropriate here since the loop bodies dereference raw pointers — iterator adapters would not simplify the code. Vec::with_capacity is used correctly in rx_poll. No obvious clippy violations detected."
    },
    {
      "rule_id": 7,
      "rule_name": "No panic! in library/production code",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No panic!, unreachable!, todo!, or unimplemented! macros appear in the file."
    },
    {
      "rule_id": 8,
      "rule_name": "Module organization",
      "status": "PASS",
      "severity": "MEDIUM",
      "detail": "The module is correctly placed in rustguard-tun/src/xdp.rs as a Linux-only transport concern. #![cfg(target_os = \"linux\")] gates the entire module cleanly. Internal helpers (setsockopt_raw, mmap_ring, make_ring, Ring) are private. The four public exports (XdpSocket, XdpDesc, XdpConfig, if_nametoindex) are intentional and minimal. Section comments (Constants, Struct layouts, Ring buffer, XDP Socket, Helpers) give clear visual hierarchy."
    },
    {
      "rule_id": 9,
      "rule_name": "Documentation comments on public items",
      "status": "WARN",
      "severity": "MEDIUM",
      "detail": "XdpSocket and its methods are well-documented. The module-level //! block is thorough. However, XdpConfig has no /// doc comment and its fields (ifname, queue_id, frame_size, num_frames, ring_size) are undocumented. XdpDesc and its pub fields (addr, len, options) have no docs. The public function if_nametoindex has no doc comment.",
      "lines": "L63, L116, L429"
    },
    {
      "rule_id": 10,
      "rule_name": "No hardcoded secrets or credentials",
      "status": "PASS",
      "severity": "CRITICAL",
      "detail": "No secrets, API keys, tokens, or passwords found. The only hardcoded string is 'eth0' in XdpConfig::default, which is a conventional network interface default and not a credential."
    },
    {
      "rule_id": 11,
      "rule_name": "Memory safety (no leaks, proper Drop impls)",
      "status": "FAIL",
      "severity": "HIGH",
      "detail": "create_inner allocates four separate mmap regions via mmap_ring (rx_map, tx_map, fill_map, comp_map). These raw pointers are consumed by make_ring but never stored in XdpSocket — Ring only retains interior pointers (producer, consumer, descs), not the base address or mapped size needed for munmap. The Drop impl unmaps only self.umem; all four ring mmap regions are permanently leaked on every socket close. A secondary issue: if mmap_ring fails partway through step 6 (e.g., fill_map fails), previously succeeded rx_map and tx_map are not cleaned up before returning Err.",
      "lines": "L222-L228, L348-L353"
    },
    {
      "rule_id": 12,
      "rule_name": "Concurrency safety (no data races, proper Send/Sync bounds)",
      "status": "PASS",
      "severity": "HIGH",
      "detail": "Ring is Send+Sync (backed by kernel-shared mmap accessible from any thread). XdpSocket is Send but intentionally not Sync, correctly reflecting that rx_release/tx_send/tx_complete require exclusive access (&mut self). Ring producer/consumer updates use Acquire/Release atomics, appropriate for SPSC ring buffers. The needs_wakeup flag read uses Relaxed, correct for a non-gating hint. Rust's borrow checker prevents aliasing the &[u8] slices returned by rx_poll with mutable UMEM writes in tx_send."
    }
  ],
  "suggestions": [
    {
      "description": "Check return values of the four ring-size setsockopt calls in create_inner to surface configuration failures instead of silently producing a broken socket.",
      "before": "setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);\nsetsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);\nsetsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);\nsetsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);",
      "after": "for &opt in &[XDP_UMEM_FILL_RING, XDP_UMEM_COMPLETION_RING, XDP_RX_RING, XDP_TX_RING] {\n    if setsockopt_raw(fd, SOL_XDP, opt, &rs) < 0 {\n        let err = io::Error::last_os_error();\n        libc::close(fd);\n        libc::munmap(umem as *mut _, umem_size);\n        return Err(err);\n    }\n}"
    },
    {
      "description": "Store ring map base pointers and sizes in XdpSocket so Drop can unmap them, eliminating the four mmap leaks.",
      "before": "pub struct XdpSocket {\n    fd: i32,\n    umem: *mut u8,\n    umem_size: usize,\n    frame_size: u32,\n    rx_ring: Ring,\n    tx_ring: Ring,\n    fill_ring: Ring,\n    comp_ring: Ring,\n    // ...\n}\n// Drop only unmaps umem",
      "after": "pub struct XdpSocket {\n    fd: i32,\n    umem: *mut u8,\n    umem_size: usize,\n    frame_size: u32,\n    rx_ring: Ring,\n    tx_ring: Ring,\n    fill_ring: Ring,\n    comp_ring: Ring,\n    ring_maps: [(*mut u8, usize); 4], // (base, size) for rx/tx/fill/comp\n    // ...\n}\n// In Drop:\nfor &(base, sz) in &self.ring_maps {\n    libc::munmap(base as *mut libc::c_void, sz);\n}"
    },
    {
      "description": "Add // Safety: comments to inline unsafe blocks in safe public methods, documenting the invariants that justify each block.",
      "before": "let desc = unsafe {\n    &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)\n};",
      "after": "// Safety: idx is masked to (ring_size - 1), so the offset stays within the\n// mmap'd ring region. The Ring is valid for the lifetime of XdpSocket.\nlet desc = unsafe {\n    &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)\n};"
    },
    {
      "description": "Add Debug and Clone derives to XdpConfig and add Debug and PartialEq to XdpDesc for ergonomic use in tests and logging.",
      "before": "pub struct XdpConfig {\n    pub ifname: String,\n    pub queue_id: u32,\n    pub frame_size: u32,\n    pub num_frames: u32,\n    pub ring_size: u32,\n}",
      "after": "#[derive(Debug, Clone)]\npub struct XdpConfig {\n    pub ifname: String,\n    pub queue_id: u32,\n    pub frame_size: u32,\n    pub num_frames: u32,\n    pub ring_size: u32,\n}"
    }
  ]
}

## rate_limit_event

## Result (success)

**Cost:** $0.4307 | **Duration:** 229.0s


---

# Axis: documentation

## User

## File: `rustguard-tun/src/xdp.rs`
## Language: rust

```rust
//! AF_XDP zero-copy UDP I/O for Linux.
//!
//! Replaces the standard UDP socket for receiving/sending WireGuard
//! encrypted packets. The plaintext side still uses TUN.
//!
//! Architecture:
//!   NIC -> XDP BPF filter (UDP:51820) -> AF_XDP socket -> decrypt -> TUN
//!   TUN -> encrypt -> AF_XDP TX ring -> NIC
//!
//! The unsafe is contained in this module. The ring buffer protocol is
//! lock-free (atomic producer/consumer indices on shared mmap'd memory).

#![cfg(target_os = "linux")]

use std::io;
use std::sync::atomic::{AtomicU32, Ordering};

// ── Constants ───────────────────────────────────────────────────────

const AF_XDP: i32 = 44;
const SOL_XDP: i32 = 283;

const XDP_UMEM_REG: i32 = 4;
const XDP_UMEM_FILL_RING: i32 = 5;
const XDP_UMEM_COMPLETION_RING: i32 = 6;
const XDP_RX_RING: i32 = 2;
const XDP_TX_RING: i32 = 3;
const XDP_MMAP_OFFSETS: i32 = 1;

const XDP_PGOFF_RX_RING: i64 = 0;
const XDP_PGOFF_TX_RING: i64 = 0x0_8000_0000;
const XDP_UMEM_PGOFF_FILL_RING: i64 = 0x1_0000_0000;
const XDP_UMEM_PGOFF_COMPLETION_RING: i64 = 0x1_8000_0000;

const XDP_USE_NEED_WAKEUP: u16 = 1 << 3;
const XDP_RING_NEED_WAKEUP: u32 = 1;

// ── Struct layouts (matching kernel uapi) ───────────────────────────

#[repr(C)]
struct XdpUmemReg {
    addr: u64,
    len: u64,
    chunk_size: u32,
    headroom: u32,
    flags: u32,
    tx_metadata_len: u32,
}

#[repr(C)]
#[derive(Clone, Copy)]
struct SockaddrXdp {
    family: u16,
    flags: u16,
    ifindex: u32,
    queue_id: u32,
    shared_umem_fd: u32,
}

#[repr(C)]
#[derive(Clone, Copy, Default)]
pub struct XdpDesc {
    pub addr: u64,
    pub len: u32,
    pub options: u32,
}

#[repr(C)]
#[derive(Default)]
struct XdpRingOffset {
    producer: u64,
    consumer: u64,
    desc: u64,
    flags: u64,
}

#[repr(C)]
#[derive(Default)]
struct XdpMmapOffsets {
    rx: XdpRingOffset,
    tx: XdpRingOffset,
    fr: XdpRingOffset,
    cr: XdpRingOffset,
}

// ── Ring buffer ─────────────────────────────────────────────────────

/// A single-producer/single-consumer ring over mmap'd memory.
struct Ring {
    producer: *const AtomicU32,
    consumer: *const AtomicU32,
    flags: *const AtomicU32,
    descs: *mut u8, // Either *mut XdpDesc or *mut u64 depending on ring type.
    mask: u32,
}

// Safety: Ring pointers come from mmap and are valid for the socket lifetime.
unsafe impl Send for Ring {}
unsafe impl Sync for Ring {}

impl Ring {
    fn producer(&self) -> u32 {
        unsafe { (*self.producer).load(Ordering::Acquire) }
    }

    fn consumer(&self) -> u32 {
        unsafe { (*self.consumer).load(Ordering::Acquire) }
    }

    fn set_producer(&self, val: u32) {
        unsafe { (*self.producer).store(val, Ordering::Release) }
    }

    fn set_consumer(&self, val: u32) {
        unsafe { (*self.consumer).store(val, Ordering::Release) }
    }

    fn needs_wakeup(&self) -> bool {
        unsafe { (*self.flags).load(Ordering::Relaxed) & XDP_RING_NEED_WAKEUP != 0 }
    }
}

// ── XDP Socket ──────────────────────────────────────────────────────

/// Configuration for creating an XDP socket.
pub struct XdpConfig {
    pub ifname: String,
    pub queue_id: u32,
    pub frame_size: u32,
    pub num_frames: u32,
    pub ring_size: u32,
}

impl Default for XdpConfig {
    fn default() -> Self {
        Self {
            ifname: "eth0".into(),
            queue_id: 0,
            frame_size: 4096,
            num_frames: 4096,
            ring_size: 2048,
        }
    }
}

/// An AF_XDP socket with UMEM and ring buffers.
/// Provides zero-copy packet I/O for the encrypted UDP side.
pub struct XdpSocket {
    fd: i32,
    umem: *mut u8,
    umem_size: usize,
    frame_size: u32,
    rx_ring: Ring,
    tx_ring: Ring,
    fill_ring: Ring,
    comp_ring: Ring,
    ring_size: u32,
    /// Free frame indices for TX.
    free_frames: Vec<u64>,
}

// Safety: XdpSocket owns its fd and mmap regions.
unsafe impl Send for XdpSocket {}

impl XdpSocket {
    /// Create and configure an AF_XDP socket.
    ///
    /// # Safety
    /// This function uses mmap to create shared memory regions with the kernel.
    /// The returned XdpSocket must not outlive the process.
    pub fn create(config: &XdpConfig) -> io::Result<Self> {
        unsafe { Self::create_inner(config) }
    }

    unsafe fn create_inner(config: &XdpConfig) -> io::Result<Self> {
        let umem_size = (config.frame_size * config.num_frames) as usize;

        // 1. Allocate UMEM.
        let umem = libc::mmap(
            std::ptr::null_mut(),
            umem_size,
            libc::PROT_READ | libc::PROT_WRITE,
            libc::MAP_PRIVATE | libc::MAP_ANONYMOUS | libc::MAP_POPULATE,
            -1,
            0,
        );
        if umem == libc::MAP_FAILED {
            return Err(io::Error::last_os_error());
        }
        let umem = umem as *mut u8;

        // 2. Create socket.
        let fd = libc::socket(AF_XDP, libc::SOCK_RAW, 0);
        if fd < 0 {
            libc::munmap(umem as *mut _, umem_size);
            return Err(io::Error::last_os_error());
        }

        // 3. Register UMEM.
        let reg = XdpUmemReg {
            addr: umem as u64,
            len: umem_size as u64,
            chunk_size: config.frame_size,
            headroom: 0,
            flags: 0,
            tx_metadata_len: 0,
        };
        if setsockopt_raw(fd, SOL_XDP, XDP_UMEM_REG, &reg) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 4. Set ring sizes.
        let rs = config.ring_size as i32;
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);

        // 5. Query mmap offsets.
        let mut offsets = XdpMmapOffsets::default();
        let mut optlen = std::mem::size_of::<XdpMmapOffsets>() as u32;
        if libc::getsockopt(
            fd,
            SOL_XDP,
            XDP_MMAP_OFFSETS,
            &mut offsets as *mut _ as *mut _,
            &mut optlen,
        ) < 0
        {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 6. mmap rings.
        let rx_map = mmap_ring(fd, &offsets.rx, config.ring_size, XDP_PGOFF_RX_RING, std::mem::size_of::<XdpDesc>())?;
        let tx_map = mmap_ring(fd, &offsets.tx, config.ring_size, XDP_PGOFF_TX_RING, std::mem::size_of::<XdpDesc>())?;
        let fill_map = mmap_ring(fd, &offsets.fr, config.ring_size, XDP_UMEM_PGOFF_FILL_RING, 8)?;
        let comp_map = mmap_ring(fd, &offsets.cr, config.ring_size, XDP_UMEM_PGOFF_COMPLETION_RING, 8)?;

        let mask = config.ring_size - 1;
        let rx_ring = make_ring(rx_map, &offsets.rx, mask);
        let tx_ring = make_ring(tx_map, &offsets.tx, mask);
        let fill_ring = make_ring(fill_map, &offsets.fr, mask);
        let comp_ring = make_ring(comp_map, &offsets.cr, mask);

        // 7. Pre-populate fill ring (first half of frames for RX).
        let rx_frames = config.num_frames / 2;
        let mut prod = fill_ring.producer();
        for i in 0..rx_frames {
            let addr_ptr = fill_ring.descs.add(((prod & mask) as usize) * 8) as *mut u64;
            *addr_ptr = (i * config.frame_size) as u64;
            prod += 1;
        }
        fill_ring.set_producer(prod);

        // Second half of frames are free for TX.
        let mut free_frames = Vec::with_capacity((config.num_frames - rx_frames) as usize);
        for i in rx_frames..config.num_frames {
            free_frames.push((i * config.frame_size) as u64);
        }

        // 8. Bind.
        let ifindex = if_nametoindex(&config.ifname)?;
        let sxdp = SockaddrXdp {
            family: AF_XDP as u16,
            flags: XDP_USE_NEED_WAKEUP,
            ifindex,
            queue_id: config.queue_id,
            shared_umem_fd: 0,
        };
        if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        Ok(Self {
            fd,
            umem,
            umem_size,
            frame_size: config.frame_size,
            rx_ring,
            tx_ring,
            fill_ring,
            comp_ring,
            ring_size: config.ring_size,
            free_frames,
        })
    }

    /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
    /// Caller must call `rx_release` after processing to return frames to the fill ring.
    pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
        let prod = self.rx_ring.producer();
        let cons = self.rx_ring.consumer();
        let available = prod.wrapping_sub(cons);

        let mut packets = Vec::with_capacity(available as usize);
        for i in 0..available {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let desc = unsafe {
                &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
            };
            let data = unsafe {
                std::slice::from_raw_parts(
                    self.umem.add(desc.addr as usize),
                    desc.len as usize,
                )
            };
            packets.push((desc.addr, data));
        }
        packets
    }

    /// Release consumed RX frames back to the fill ring.
    pub fn rx_release(&mut self, frames: &[u64]) {
        // Update RX consumer.
        let cons = self.rx_ring.consumer().wrapping_add(frames.len() as u32);
        self.rx_ring.set_consumer(cons);

        // Add frames back to fill ring.
        let mut prod = self.fill_ring.producer();
        for &addr in frames {
            let idx = (prod & (self.ring_size - 1)) as usize;
            unsafe {
                let ptr = self.fill_ring.descs.add(idx * 8) as *mut u64;
                *ptr = addr;
            }
            prod = prod.wrapping_add(1);
        }
        self.fill_ring.set_producer(prod);
    }

    /// Send a packet via the TX ring. Returns false if no frames available.
    pub fn tx_send(&mut self, data: &[u8]) -> bool {
        let Some(frame_addr) = self.free_frames.pop() else {
            return false;
        };

        // Copy data into UMEM frame.
        unsafe {
            std::ptr::copy_nonoverlapping(
                data.as_ptr(),
                self.umem.add(frame_addr as usize),
                data.len(),
            );
        }

        // Write TX descriptor.
        let prod = self.tx_ring.producer();
        let idx = (prod & (self.ring_size - 1)) as usize;
        unsafe {
            let desc = &mut *(self.tx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *mut XdpDesc);
            desc.addr = frame_addr;
            desc.len = data.len() as u32;
            desc.options = 0;
        }
        self.tx_ring.set_producer(prod.wrapping_add(1));

        // Kick kernel if needed.
        if self.tx_ring.needs_wakeup() {
            unsafe {
                libc::sendto(self.fd, std::ptr::null(), 0, libc::MSG_DONTWAIT, std::ptr::null(), 0);
            }
        }

        true
    }

    /// Reclaim completed TX frames.
    pub fn tx_complete(&mut self) {
        let prod = self.comp_ring.producer();
        let cons = self.comp_ring.consumer();
        let done = prod.wrapping_sub(cons);

        for i in 0..done {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let addr = unsafe { *(self.comp_ring.descs.add(idx * 8) as *const u64) };
            self.free_frames.push(addr);
        }
        self.comp_ring.set_consumer(cons.wrapping_add(done));
    }

    /// Raw file descriptor (for poll/epoll).
    pub fn fd(&self) -> i32 {
        self.fd
    }
}

impl Drop for XdpSocket {
    fn drop(&mut self) {
        unsafe {
            libc::close(self.fd);
            libc::munmap(self.umem as *mut _, self.umem_size);
        }
    }
}

// ── Helpers ─────────────────────────────────────────────────────────

unsafe fn setsockopt_raw<T>(fd: i32, level: i32, optname: i32, val: &T) -> i32 {
    libc::setsockopt(
        fd,
        level,
        optname,
        val as *const _ as *const _,
        std::mem::size_of::<T>() as u32,
    )
}

unsafe fn mmap_ring(
    fd: i32,
    off: &XdpRingOffset,
    ring_size: u32,
    pgoff: i64,
    desc_size: usize,
) -> io::Result<*mut u8> {
    let size = off.desc as usize + (ring_size as usize) * desc_size;
    let ptr = libc::mmap(
        std::ptr::null_mut(),
        size,
        libc::PROT_READ | libc::PROT_WRITE,
        libc::MAP_SHARED | libc::MAP_POPULATE,
        fd,
        pgoff,
    );
    if ptr == libc::MAP_FAILED {
        return Err(io::Error::last_os_error());
    }
    Ok(ptr as *mut u8)
}

unsafe fn make_ring(base: *mut u8, off: &XdpRingOffset, mask: u32) -> Ring {
    Ring {
        producer: base.add(off.producer as usize) as *const AtomicU32,
        consumer: base.add(off.consumer as usize) as *const AtomicU32,
        flags: base.add(off.flags as usize) as *const AtomicU32,
        descs: base.add(off.desc as usize),
        mask,
    }
}

pub fn if_nametoindex(name: &str) -> io::Result<u32> {
    let cname = std::ffi::CString::new(name).map_err(|_| {
        io::Error::new(io::ErrorKind::InvalidInput, "invalid interface name")
    })?;
    let idx = unsafe { libc::if_nametoindex(cname.as_ptr()) };
    if idx == 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(idx)
}

```

## Symbols to evaluate

- constant AF_XDP (L20–L20)
- constant SOL_XDP (L21–L21)
- constant XDP_UMEM_REG (L23–L23)
- constant XDP_UMEM_FILL_RING (L24–L24)
- constant XDP_UMEM_COMPLETION_RING (L25–L25)
- constant XDP_RX_RING (L26–L26)
- constant XDP_TX_RING (L27–L27)
- constant XDP_MMAP_OFFSETS (L28–L28)
- constant XDP_PGOFF_RX_RING (L30–L30)
- constant XDP_PGOFF_TX_RING (L31–L31)
- constant XDP_UMEM_PGOFF_FILL_RING (L32–L32)
- constant XDP_UMEM_PGOFF_COMPLETION_RING (L33–L33)
- constant XDP_USE_NEED_WAKEUP (L35–L35)
- constant XDP_RING_NEED_WAKEUP (L36–L36)
- class XdpUmemReg (L41–L48)
- class SockaddrXdp (L52–L58)
- export class XdpDesc (L62–L66)
- class XdpRingOffset (L70–L75)
- class XdpMmapOffsets (L79–L84)
- class Ring (L89–L95)
- export class XdpConfig (L126–L132)
- export class XdpSocket (L148–L160)
- function setsockopt_raw (L407–L415)
- function mmap_ring (L417–L437)
- function make_ring (L439–L447)
- export function if_nametoindex (L449–L458)

## Documentation Directory

No /docs/ directory found — evaluate JSDoc only, skip concept coverage.

Evaluate documentation for each symbol and output the JSON.
## Known False Positives (previously overturned by deliberation)

The following findings were previously flagged by this axis but overturned during deliberation review. Do not re-flag them unless you have new evidence that the situation has changed.

- **[deliberation] TransportSession: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: UTILITY DEAD→USED: This is a `pub struct` in a `-core` library crate (`rustguard-core`). Library crate public types are consumed by downstream crates in the workspace (e.g., `rustguard`). The utility evaluator likely only searched within the same crate and missed cross-crate imports, a classic false positive for workspace library crates. Reclassified to USED. TESTS NONE→WEAK: The merged field says NONE but the detail text itself says WEAK, and the source clearly contains 6 test functions in a `#[cfg(test)]` module covering encrypt/decrypt roundtrip, counter monotonicity, wrong-counter rejection, replay rejection, out-of-order delivery, and bidirectional use. The WEAK assessment is accurate: `encrypt_to` and `decrypt_in_place` (separate code paths using `seal_to`/`open_to`) have zero coverage, and nonce exhaustion / edge cases are untested. DOCUMENTATION UNDOCUMENTED→DOCUMENTED: The detail text itself concludes DOCUMENTED. The struct has a thorough `///` block (lines 7–11) explaining purpose, derivation, and key-role convention. Both public fields and all private fields carry `///` comments. Four of six methods have doc comments. Two public items (`new`, `send_counter`) lack doc comments (noted in best_practices rule 9), but the overall coverage is well above UNDOCUMENTED. The struct-level and field-level documentation is comprehensive.
- **[deliberation] COOKIE_LEN: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD reclassified → USED: this is a `pub const` in a library crate (`rustguard-core`). Downstream crates (e.g., message layout code) depend on it; pre-computed analysis likely doesn't capture cross-crate workspace imports. The entire cookie module is clearly used infrastructure for a WireGuard implementation. Tests NONE contradicts the detail text which says '[GOOD] Pure constant. Used throughout and indirectly validated in every roundtrip test.' For a pure constant with no runtime behavior, indirect validation is sufficient — reclassified to GOOD. Documentation UNDOCUMENTED is wrong — line 23 has `/// Cookie size.` which IS a doc comment, making this PARTIAL (brief but present).
- **[deliberation] CookieChecker: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD reclassified → USED: CookieChecker is the primary server-side public API of this module in a library crate. Pre-computed cross-crate analysis likely misses workspace-level imports. The entire module exists to support WireGuard cookie DoS protection — this struct is clearly intended for use by consuming crates. Tests NONE contradicts the detail text which says 'happy-path methods (create_reply, verify_mac2) are reasonably well tested across four tests' — reclassified to WEAK (tested but with gaps in verify_mac1, rotation, new_with). Documentation UNDOCUMENTED contradicts detail: 'Struct has a /// summary and all four fields have field-level /// comments.' Line 49 has a doc comment. Reclassified to PARTIAL (docs exist but missing examples and constructor docs).
- **[deliberation] CookieState: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX kept: unlike elapsed_since, the consequence here is that process_reply successfully decrypts a cookie but never marks it as fresh, making compute_mac2 silently return zeros on no_std. While the overall no_std design is intentional, this specific behavior is more insidious — a successful decryption followed by silent failure to use the cookie is a subtle trap with no comment explaining it. Utility DEAD → USED: same reasoning as CookieChecker — primary client-side public API in a library crate. Tests NONE → WEAK: detail says 'Happy path covered: process_reply success, wrong-MAC1 failure, compute_mac2 with/without cookie.' Documentation UNDOCUMENTED → PARTIAL: detail says 'Struct has a /// summary and both private fields have doc comments.'
- **[deliberation] ReplayWindow: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: CORRECTION ERROR confirmed at high confidence: shift_window's bit-shift block (L112-116) iterates in reverse with right-shift (>>), moving bits toward lower ages instead of higher ages. Verified with concrete example: top=0, bitmap[0]=1, then shift_window(1) right-shifts bitmap[0] to 0, losing the counter-0 mark. A subsequent check(0) would incorrectly accept the replay. The word-level copy_within is correct; only the sub-word bit-shift is inverted. Tests don't catch this because they never re-check a previously-seen counter after a shift has occurred. UTILITY reclassified DEAD→USED: this is a pub struct in a library crate (rustguard-core). Library public API types are not dead simply because no in-tree consumer is visible — the struct is the module's primary export and is extensively exercised in tests. TESTS reclassified NONE→WEAK: the detail text itself says '[WEAK]' and describes solid behavioral coverage via check_and_update (sequential, duplicate rejection, out-of-order, boundary, stress). However, check() and update() are never tested independently, and the critical shift_window bit-direction bug goes undetected due to test design gaps. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the struct's two fields have /// doc comments (L15, L17), and all three public methods have /// doc comments. Only the struct-level definition and new() constructor lack /// docs. This is clearly partial, not absent.
- **[deliberation] do_handshake: documentation PARTIAL → DOCUMENTED**: Deliberation reclassified: Documentation PARTIAL is too strict for a private test helper. The doc comment '/// Helper: run a full handshake between two peers, return their sessions.' clearly states purpose and return semantics. The return type signature `(TransportSession, TransportSession)` is self-documenting — the variable names at every call site (`(init, resp)` or `(init_session, resp_session)`) make tuple ordering unambiguous. All other test symbols received DOCUMENTED under test-module leniency; a helper used exclusively by those tests deserves the same leniency. Requiring `# Examples` or field-by-field tuple documentation for a 20-line private test helper is unreasonable. Reclassified to DOCUMENTED.
- **[deliberation] BLAKE2S_BLOCK_SIZE: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Documentation UNDOCUMENTED reclassified to DOCUMENTED. This is a private constant named BLAKE2S_BLOCK_SIZE with value 64 — the name is self-documenting for anyone with basic cryptographic knowledge. The original evaluator even acknowledged it is 'tolerable.' For a private constant whose name IS the specification reference, requiring a doc comment is excessive.
- **[deliberation] hash: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: this is a library crate's public API. Exported functions in a crypto library ARE the product — absence of in-workspace importers does not make them dead. The crate is designed to be consumed externally (rustguard-crypto). Tests NONE contradicts the detail text which describes three existing tests (hash_deterministic, hash_different_inputs, hash_multi_chunk); reclassified to WEAK since tests exist but lack reference vectors. Documentation UNDOCUMENTED contradicts the detail and source: line 8 has `/// BLAKE2s-256 hash.` — it's PARTIAL (brief but present, missing params/examples).
- **[deliberation] mac: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive for the same library-crate reason as hash. Tests NONE contradicts the detail text which describes mac_differs_with_key and indirect coverage via hmac_differs_from_keyed_blake2s; reclassified to WEAK (tests exist but no reference vectors). Documentation UNDOCUMENTED contradicts the source: lines 17-20 have a multi-line /// block explaining keyed MAC vs HMAC distinction. Reclassified to PARTIAL per the detail's own assessment.
- **[deliberation] hkdf: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: hkdf is a core public function of a crypto library crate, essential for the WireGuard KDF. Tests NONE contradicts the detail which describes two tests (hkdf_produces_different_outputs, hkdf_deterministic); reclassified to WEAK since no RFC 5869/WireGuard spec test vectors are present. Documentation UNDOCUMENTED contradicts source: lines 31-36 have a substantial /// block describing the algorithm and return semantics. Reclassified to PARTIAL.
- **[deliberation] MAX_PACKET_SIZE: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the doc comment on L9 says 'MTU 1420 + tag' but the expression computes `1500 + AEAD_TAG_LEN = 1516`, a clear mismatch (~80 bytes difference). Either the doc or the value is wrong. Utility DEAD→USED: library public constant for downstream consumption, same false positive pattern as AEAD_TAG_LEN. Tests NONE→GOOD: compile-time constant with no runtime behavior, no dedicated test required. Documentation UNDOCUMENTED→PARTIAL: line 9 has a `///` doc comment present (`/// Maximum WireGuard transport payload (MTU 1420 + tag).`), so UNDOCUMENTED is incorrect. Reclassified to PARTIAL due to the inaccurate MTU value in the doc.
- **[deliberation] seal: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: primary public encryption API of the library crate, consumed by downstream crates. Tests NONE→WEAK: `seal` is directly called in all 5 test functions (seal_then_open, wrong_key_fails, wrong_counter_fails, tampered_ciphertext_fails, aad_mismatch_fails), so NONE is factually incorrect. Reclassified to WEAK since edge cases (empty plaintext, large payloads, non-zero counter as primary input) are missing. Documentation UNDOCUMENTED→PARTIAL: has a multi-line `///` doc block on L12-15 describing purpose, nonce construction, and AAD semantics. Missing param-level docs and examples, hence PARTIAL not DOCUMENTED.
- **[deliberation] open: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: primary public decryption API of the library crate. Tests NONE→GOOD: five tests thoroughly exercise `open` — happy path, wrong key, wrong counter, tampered ciphertext, and AAD mismatch. All four primary authentication-failure modes are covered. NONE is clearly wrong. Documentation UNDOCUMENTED→PARTIAL: has a `///` block on L31-33 describing purpose and three failure conditions for `None` return. Missing param-level docs and examples, hence PARTIAL.
- **[deliberation] xseal: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: public API variant for WireGuard cookie encryption, consumed by downstream crates. Tests NONE confirmed: no test covers xseal — this is a genuine gap on a security-critical path. Documentation UNDOCUMENTED→PARTIAL: has a single-line `///` on L48 noting algorithm, nonce size, and use case. Minimal but present, so UNDOCUMENTED is incorrect.
- **[deliberation] xopen: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: public API variant for WireGuard cookie decryption, consumed by downstream crates. Tests NONE confirmed: no test covers xopen — roundtrip, wrong key, tampered data, and nonce mismatch all untested. Documentation UNDOCUMENTED→PARTIAL: has a single-line `///` on L57 noting algorithm, nonce size, and use case. Present but lacks param docs and return semantics.
- **[deliberation] seal_to: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: zero-allocation public API for hot-path transport encryption, consumed by downstream crates. Tests NONE confirmed: no test covers seal_to — output length, correct tag placement, and buffer semantics all unverified. Documentation UNDOCUMENTED→PARTIAL: has a three-line `///` block on L66-68 describing purpose, buffer precondition, and zero-allocation property. Missing `# Panics` section for `.expect()` and param-level docs.
- **[deliberation] open_to: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX confirmed: the function guards `ct_len < AEAD_TAG_LEN` (defensive intent) but never checks `ct_len > buf.len()`. Since `buf[..ct_len]` on L91 will panic if ct_len exceeds buf length, and this function processes potentially adversarial input while advertising Option return, the missing upper-bound guard is a genuine inconsistency. Utility DEAD→USED: zero-allocation public API for hot-path transport decryption. Tests NONE confirmed: completely untested. Documentation UNDOCUMENTED→PARTIAL: has a three-line `///` block on L81-83 describing purpose, buffer contract, and zero-allocation guarantee.
- **[deliberation] Tai64n: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD → USED: This is a `pub struct` in `rustguard-crypto`, a library crate whose entire purpose is to export types for cross-crate consumption. The evaluator itself noted this false-positive pattern and reduced confidence. Cross-crate imports were outside analysis scope, and similar symbols (TransportSession, CookieChecker) were previously misclassified. Reclassified with high confidence. Tests NONE → WEAK: The symbol field says NONE but the detail section itself begins with '[WEAK] Three inline tests cover monotonicity...' — there are clearly three tests in the `mod tests` block (L50-L80). They exist but only check roundtrip equality, never assert against known-correct byte encodings. WEAK is the accurate classification. Documentation UNDOCUMENTED → PARTIAL: Lines 4-7 contain four `///` doc comments on the struct covering purpose, byte layout, WireGuard use-case, and replay-monotonicity invariant. The detail section even states 'Struct carries four lines of /// doc comments — solid struct-level narrative.' The struct is documented; only `as_bytes` and `from_bytes` methods lack docs. PARTIAL is correct.
- **[deliberation] TAI64_EPOCH_OFFSET: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Tests WEAK: Kept as-is. The constant is only exercised through roundtrip tests that would pass even with an incorrect value. A test asserting `from_unix(0,0).as_bytes()[..8]` against the known hex value would be the minimal correctness check but is absent. However, as a private constant this is lower severity. Documentation UNDOCUMENTED → DOCUMENTED: Line 11 contains `/// TAI is ahead of UTC by 10 seconds (1970 epoch) + 2^62 offset.` — a `///` doc comment directly above the constant that clearly explains the composition of the hex literal (10-second TAI-UTC offset + 2^62 base). The evaluator's own detail acknowledges the name is 'moderately self-descriptive' and the struct-level docs provide indirect context. With an explicit doc comment present, UNDOCUMENTED is incorrect. Confidence raised from 60 to 88 since the doc comment is plainly visible in the source.
- **[deliberation] KEEPALIVE_TIMEOUT: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Utility DEAD → LOW_VALUE: this is an exported WireGuard protocol constant (§6.3). The needs_keepalive method deliberately takes keepalive_interval as an Option<Duration> parameter, making KEEPALIVE_TIMEOUT the canonical default value for external callers to pass in. For a library crate, exported protocol constants serve as public API even without internal usage — downstream consumers may use it. Removing it would break the API contract. Tests NONE: stands — no test references it. Documentation: the source clearly has '/// Keepalive interval when configured.' on L51, and the detail text itself says '[DOCUMENTED]'. The UNDOCUMENTED classification contradicts the evaluator's own detail — reclassify to DOCUMENTED.
- **[deliberation] Peer: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD → USED: The detail itself acknowledges this matches a known false-positive pattern for exported structs in workspace crates. `Peer` is the fundamental domain struct for a WireGuard daemon — it is inconceivable that a daemon named `rustguard-daemon` does not use its own Peer representation. The pre-analysis hedged with 'LIKELY DEAD', and the analysis simply lacked visibility into sibling modules (device.rs, handshake.rs, etc.) that inevitably consume this type. Reclassified with high confidence. Tests NONE: Agreed — no tests exist for this module or its methods (from_config, allows_ip, has_active_session). Kept as-is. Documentation UNDOCUMENTED → PARTIAL: The source clearly shows a struct-level `///` doc comment at L15, plus field-level `///` docs on `session` (L22) and `timers` (L24). Five fields remain undocumented and `from_config` lacks a doc comment, so coverage is incomplete but not absent. PARTIAL is the correct classification per the detail's own description.
- **[deliberation] run: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX confirmed: (1) MSG_RESPONSE peer-matching at L296-301 uses a heuristic that breaks with multiple peers — the first peer without an active session is selected regardless of which peer the handshake was for. (2) Timer keepalive nonce exhaustion at ~L340 continues without clearing the session, preventing rekeying. Both are real bugs visible in the source. Utility reclassified DEAD→USED: this is `pub fn run()`, the sole public entry point of the daemon tunnel module. It is almost certainly called from main.rs or a binary crate entry point. The pre-computed analysis likely did not trace binary crate call graphs. A pub fn named 'run' in a daemon module being dead code would be nonsensical. Overengineering ACCEPTABLE kept — large but justified orchestration function. Tests NONE accurate. Documentation reclassified UNDOCUMENTED→PARTIAL: the function does have a /// doc comment ('Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.') as visible at L48, so it is not fully undocumented; it is partial (missing parameter/error docs).
- **[deliberation] ParsedUdp: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD → USED: This is a pub struct in a library crate returned by the public parse_eth_udp function. The original detail itself acknowledges this matches the Known False Positives pattern for library crate exports consumed by downstream crates. Tests NONE → WEAK: The detail text itself states 'The struct's two fields (src_addr, payload) are exercised by the single inline test parse_ipv4_udp_frame, which asserts both field values.' This is partial coverage (IPv4 path only, no IPv6), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Struct has a /// doc comment on L8' — the struct-level doc exists on line 8. Only the per-field docs are missing. That is PARTIAL, not UNDOCUMENTED.
- **[deliberation] parse_eth_udp: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD → USED: Public function in a library crate serving as the main entry point for Ethernet frame parsing. The original detail itself notes it matches the Known False Positives pattern for library crate public APIs consumed by downstream crates not visible in same-crate import analysis. Tests NONE → WEAK: The detail states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path through parse_eth_udp.' This is exercised but with limited coverage (no IPv6 branch, no error paths), which is WEAK not NONE. Documentation UNDOCUMENTED → PARTIAL: The detail confirms 'Has a two-line /// block (L14–L15) describing purpose and the None return condition.' A doc comment exists; it merely lacks #Examples and parameter descriptions. That is PARTIAL.
- **[deliberation] ENROLL_REQUEST_MAGIC: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant with a self-descriptive name and an inline comment showing the ASCII interpretation ('RGE\x01'). The evaluator itself noted 'tolerated under leniency rules' for private items. Reclassifying to DOCUMENTED — private items with clear naming and inline comments meet the bar.
- **[deliberation] ENROLL_RESPONSE_MAGIC: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Same rationale as ENROLL_REQUEST_MAGIC: private constant with self-descriptive name and inline ASCII comment. Evaluator acknowledged leniency applies. Reclassifying to DOCUMENTED.
- **[deliberation] random_nonce: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Tests WEAK: only tested indirectly through build functions, acceptable for a private thin wrapper around getrandom but still technically weak. Documentation reclassified to DOCUMENTED: private 4-line function with a self-descriptive name and trivial body (getrandom fill + expect). The evaluator explicitly noted 'tolerated under leniency rules' for private items. No `///` needed here.
- **[deliberation] BATCH_SIZE: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: BATCH_SIZE is actively used within this file in RecvBatch struct field types (L20-22), for-loop bounds, and array initializations in recv_batch. It is part of the module's coherent public API. Static analysis missed intra-module usage. Documentation UNDOCUMENTED→PARTIAL: Source clearly shows `/// Maximum batch size for recvmmsg/sendmmsg.` at L13; the detail text itself states PARTIAL. The top-level UNDOCUMENTED classification contradicts the detail. Tests NONE kept: factual, though low concern for a compile-time constant.
- **[deliberation] PKT_BUF_SIZE: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: PKT_BUF_SIZE is used in RecvBatch struct definition (L20) and recv_batch buffer allocation (L50, L113). Actively consumed within the file. Documentation UNDOCUMENTED→PARTIAL: Source shows `/// Per-packet buffer size.` at L15; detail text confirms PARTIAL. Tests NONE kept: factual, low concern for a constant.
- **[deliberation] RecvBatch: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: RecvBatch is used as parameter type in both recv_batch function signatures (L40, L113). It is the core data structure of this module's API. Documentation UNDOCUMENTED→PARTIAL: Source has `/// A batch of received packets.` at L18; detail text confirms PARTIAL (struct doc exists but fields lack individual docs). Tests NONE kept: struct and constructor have zero coverage, which is a legitimate gap for a public type with unsafe-adjacent usage patterns.
- **[deliberation] recv_batch: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: This is the macOS recv_batch at L112-L126. Utility DEAD→USED: macOS fallback is the platform-specific implementation consumers will call; cfg-gating makes static import analysis unreliable. Documentation UNDOCUMENTED→PARTIAL: Source shows `/// macOS fallback: receive one packet at a time.` at L110; detail text confirms PARTIAL. Tests NONE kept: WouldBlock and single-packet paths need coverage.
- **[deliberation] PersistedPeer: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: PersistedPeer is a pub struct in a library crate (rustguard-enroll) intended to be consumed by other workspace crates. Zero in-file importers is expected for a public API type — the evaluator only searched within the crate boundary. Tests reclassified from NONE to GOOD: the detail text itself states 'Both inline tests directly construct and assert on PersistedPeer instances, exercising both public fields' and concludes GOOD for a plain data struct — the NONE field value contradicts the detail. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 11 clearly shows `/// A persisted peer — just enough to re-enroll on restart.` and best_practices rule 9 confirms 'All public items carry /// doc comments'. Field-level docs are absent but struct-level doc exists.
- **[deliberation] default_state_path: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate, designed for external consumers to call for the default path. No in-crate callers is expected — tests use temp dirs instead, and the binary crate would call this. Tests NONE is accurate: no test directly invokes this function, though for a trivial one-liner the risk is minimal. Documentation reclassified from UNDOCUMENTED to PARTIAL: source line 19 shows `/// Default state file path.` — the doc exists but is minimal (no examples, no platform notes).
- **[deliberation] save: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate meant for external consumers. Tests reclassified from NONE to WEAK: the detail text itself says 'Covered only by the save_and_load_roundtrip happy-path test with two peers' — the function IS called in the test at line 108, so NONE is factually incorrect. WEAK is appropriate given only the happy path is covered and error paths are untested. Documentation reclassified from UNDOCUMENTED to PARTIAL: lines 24-25 show a two-line /// doc comment describing purpose and format. It exists but lacks #Errors and parameter docs.
- **[deliberation] load: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: pub function in a library crate for external consumption. Tests reclassified from NONE to WEAK: both test functions call load() — save_and_load_roundtrip (line 109) and load_nonexistent_returns_empty (line 116). The detail text itself describes WEAK coverage with specific gaps (error paths untested). NONE is factually wrong. Documentation reclassified from UNDOCUMENTED to PARTIAL: line 48 shows `/// Load peers from the state file.` — the doc exists but is minimal, missing #Errors section and the important NotFound → Ok(vec![]) behavior.
- **[deliberation] WINDOW_SIZE: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Documentation reclassified: private constant with a perfectly self-descriptive name (`WINDOW_SIZE`) whose value (2048) is directly referenced in the module-level `//!` doc comment ('2048-bit bitmap'). Under private-item leniency, this qualifies as DOCUMENTED. The original evaluator itself noted it is 'tolerated under private-item leniency rules,' contradicting its own UNDOCUMENTED classification.
- **[deliberation] BITMAP_LEN: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Documentation reclassified: private constant with a self-descriptive name and an inline comment (`// 32 u64s`) explaining the derived value. The derivation expression `(WINDOW_SIZE / 64) as usize` is itself self-documenting. Under private-item leniency for non-exported constants, this is adequately documented.
- **[deliberation] MSG_TRANSPORT: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Utility DEAD is a false positive: MSG_TRANSPORT is a pub(crate) protocol constant for WireGuard transport data messages (type 4). It is almost certainly imported by other modules handling the data path (e.g., transport/data handling). The 0-importer count reflects incomplete cross-module analysis. Tests reclassified to GOOD per rule 6 (compile-time constant). Documentation reclassified to DOCUMENTED: source line 247 clearly shows `/// Message type 4: Transport Data.` preceding the constant definition. The merged JSON had an internal inconsistency where the field said UNDOCUMENTED but the detail text said DOCUMENTED.
- **[deliberation] create_initiation: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD is a false positive: this is the primary pub(crate) API for creating handshake initiation messages. It is the entry point for the initiator handshake flow and must be called by other crate modules (e.g., peer/device management). The 0-importer count reflects incomplete cross-crate analysis. Documentation reclassified UNDOCUMENTED→PARTIAL: source lines 284-287 show a multi-line `///` block describing purpose, return semantics, and DH failure condition. The merged JSON field contradicts its own detail text which says PARTIAL.
- **[deliberation] process_response: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Correction NEEDS_FIX is valid: the PSK-derived AEAD key at line 407 (`let (new_ck, mut t, key) = hkdf(...)`) is not declared mut and never zeroed, unlike the equivalent `key3` in process_initiation which is properly zeroed. This leaves sensitive key material on the stack. Utility DEAD→USED: this is the primary pub(crate) API for processing handshake responses, necessarily called by peer management code. Documentation UNDOCUMENTED→PARTIAL: source line 350 has `/// Process a handshake response (type 2) and derive transport keys.` — the merged field contradicts the detail text. Confidence raised since findings are now coherent.
- **[deliberation] process_initiation: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility DEAD→USED: this is the primary pub(crate) API for the responder handshake path, necessarily called by device/peer management modules. Documentation UNDOCUMENTED→PARTIAL: source lines 434-439 show a multi-line `///` block describing return tuple elements and requiring caller replay protection. The merged JSON field contradicts the detail text. Tests NONE is accurate — no test coverage for this critical path.
- **[deliberation] KEEPALIVE_TIMEOUT_NS: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: DEAD is correct: private constant with zero references anywhere in the file. NONE tests is technically correct but moot for a dead compile-time constant. UNDOCUMENTED is wrong — source line 19 clearly shows `/// Keepalive interval (10 seconds).` directly above the constant; the detail text itself says '[DOCUMENTED]'. Reclassifying documentation to DOCUMENTED. The primary action is to remove the dead constant.
- **[deliberation] SessionTimers: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: NEEDS_FIX confirmed: (1) is_dead() checks session_established age but the constant's doc says 'inactivity' — an actively-used session >240s old would be wrongly zeroed; baseline should be max(last_received, last_sent). (2) needs_keepalive() when last_sent==0 sets since_last_send = since_last_recv, making the condition `x >= interval && x < interval` always false — keepalive never fires for fresh sessions. Both are genuine logic bugs. DEAD→USED reclassified: SessionTimers is `pub(crate)` and forms the entire public API of this timer module in a kernel crate. The 'pre-computed analysis' likely has incomplete cross-file coverage; a WireGuard kernel module's timer state struct is fundamentally necessary for peer management. NONE tests confirmed: 9 methods with substantial branching logic and time-based comparisons are completely untested. UNDOCUMENTED→DOCUMENTED reclassified: source line 38 has `/// Per-peer timer state.` and all six fields carry individual `///` doc comments. The detail text itself concludes 'coverage meets the DOCUMENTED bar.'
- **[deliberation] TUNSETIFF: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant with universally recognized Linux kernel ABI name. The evaluator acknowledged 'tolerated under private-item leniency.' Reclassifying to DOCUMENTED: for private kernel ioctl constants, the standard name IS the documentation. No action references this finding.
- **[deliberation] SIOCSIFADDR: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Same rationale as TUNSETIFF — private constant with canonical POSIX ioctl name that any Linux systems programmer recognizes. Tolerated under private-item leniency per the evaluator's own detail.
- **[deliberation] SIOCSIFDSTADDR: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant, standard Linux kernel name. Self-documenting by convention. Reclassified under private-item leniency.
- **[deliberation] SIOCSIFNETMASK: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant, standard Linux kernel name. Self-documenting. Reclassified under private-item leniency.
- **[deliberation] SIOCSIFMTU: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant, standard Linux kernel name. Self-documenting. Reclassified under private-item leniency.
- **[deliberation] SIOCSIFFLAGS: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant, standard Linux kernel name. Self-documenting. Reclassified under private-item leniency.
- **[deliberation] SIOCGIFFLAGS: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant, standard Linux kernel name. Self-documenting. Reclassified under private-item leniency.
- **[deliberation] IFF_TUN: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant, well-known Linux TUN flag. The preceding group comment ('// TUN flags.') provides category context. Self-documenting name. Reclassified under private-item leniency.
- **[deliberation] IFF_NO_PI: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant, well-known Linux TUN flag alongside IFF_TUN. Self-documenting name with group comment. Reclassified under private-item leniency.
- **[deliberation] IFF_UP: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant, universally known interface flag. Self-documenting name with group comment. Reclassified under private-item leniency.
- **[deliberation] IFNAMSIZ: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant matching the canonical Linux kernel IFNAMSIZ (16). Any systems programmer recognizes this immediately. Reclassified under private-item leniency.
- **[deliberation] IfreqFlags: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private repr(C) FFI struct with self-descriptive field names (ifr_name, ifr_flags, _pad). Section header comment ('Structs matching Linux kernel layout') provides context. Private-item leniency applies; the struct's purpose is clear from its name and fields.
- **[deliberation] IfreqAddr: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Correction NEEDS_FIX confirmed: struct is 32 bytes but Linux ifreq is 40. The kernel's copy_from_user reads sizeof(ifreq)=40 bytes, reading 8 bytes past the struct allocation. While functionally harmless for SIOCSIFADDR/SIOCSIFDSTADDR/SIOCSIFNETMASK (only first 32 bytes consumed), this is a real ABI violation reading potentially uninitialized stack memory. Adding _pad: [u8; 8] is the correct fix. Documentation reclassified to DOCUMENTED: private repr(C) struct with self-descriptive fields under private-item leniency.
- **[deliberation] IfreqMtu: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private repr(C) FFI struct with clear field names and inline padding comment. Section header provides context. Reclassified under private-item leniency.
- **[deliberation] last_os_error: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Tests NONE confirmed — no tests exist, factually correct. However, this is a one-line private wrapper around io::Error::last_os_error() in a privileged FFI module; testing burden is minimal but absence is not critical. Documentation reclassified to DOCUMENTED: single-line private function whose name perfectly describes its behavior.
- **[deliberation] close_and_error: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Duplication DUPLICATE confirmed: byte-for-byte identical implementation exists in linux_mq.rs. Both modules are in the same crate and could share a common linux_util module. Tests NONE confirmed factually. Documentation reclassified to DOCUMENTED: private 4-line helper with perfectly descriptive name (closes fd, returns the error).
- **[deliberation] make_sockaddr_in: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Duplication DUPLICATE confirmed: identical sockaddr_in constructor in linux_mq.rs, both in same crate. Reasonable refactoring target. Tests NONE confirmed. Documentation reclassified to DOCUMENTED: private helper with self-descriptive name and clear parameter type (Ipv4Addr → sockaddr_in).
- **[deliberation] set_name: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Duplication DUPLICATE confirmed: identical implementation in linux_mq.rs. Tests NONE confirmed. Documentation reclassified to DOCUMENTED: private helper with clear name and obvious semantics (copies name into fixed-size buffer).
- **[deliberation] configure_interface: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Correction NEEDS_FIX confirmed: this function uses IfreqAddr which is undersized (32 vs 40 bytes). The root cause is in IfreqAddr's definition (action 1 addresses it), but the usage site is where the ABI violation manifests. Duplication DUPLICATE confirmed: 98% identical to linux_mq.rs version — same ioctl sequence, same structs, same error handling. Tests NONE confirmed. Documentation reclassified to DOCUMENTED: private function with descriptive name; private-item leniency applies.
- **[deliberation] AF_SYSTEM: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant whose name is an exact Darwin header identifier (AF_SYSTEM=32). Self-descriptive for any systems programmer; `///` doc would be redundant. Reclassified under private-item leniency.
- **[deliberation] AF_SYS_CONTROL: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant matching Darwin AF_SYS_CONTROL. Name is self-documenting for the target audience. Reclassified under private-item leniency.
- **[deliberation] SYSPROTO_CONTROL: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant matching Darwin SYSPROTO_CONTROL. Standard kernel constant name serves as documentation. Reclassified under private-item leniency.
- **[deliberation] UTUN_OPT_IFNAME: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant matching Darwin UTUN_OPT_IFNAME. Name communicates purpose clearly. Reclassified under private-item leniency.
- **[deliberation] UTUN_CONTROL_NAME: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private constant containing the literal Darwin kernel control name string. The byte literal is self-documenting. Reclassified under private-item leniency.
- **[deliberation] CTLIOCGINFO: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private ioctl constant with an inline // comment showing the _IOWR macro expansion. Name matches Darwin header exactly. Reclassified under private-item leniency; the inline comment provides sufficient context.
- **[deliberation] SIOCAIFADDR: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Standard BSD ioctl name for adding interface address. Self-descriptive. Reclassified under private-item leniency.
- **[deliberation] AF_INET: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Universally known socket constant. Private u8 variant justified by Darwin struct layout needs. No doc comment needed.
- **[deliberation] AF_INET6: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Universally known socket constant. Same reasoning as AF_INET. Reclassified under private-item leniency.
- **[deliberation] CtlInfo: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private repr(C) struct that exactly mirrors Darwin's struct ctl_info. Field names match kernel headers. Section comment above provides context. Reclassified under private-item leniency.
- **[deliberation] SockaddrCtl: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private repr(C) struct mirroring Darwin sockaddr_ctl. Field names are self-documenting and match kernel headers exactly. Reclassified under private-item leniency.
- **[deliberation] SockaddrIn: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private repr(C) struct mirroring Darwin sockaddr_in. Field names match BSD socket conventions universally. Reclassified under private-item leniency.
- **[deliberation] IfAliasReq: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Private repr(C) struct mirroring Darwin ifaliasreq. Field names match kernel headers. Reclassified under private-item leniency.
- **[deliberation] configure_address: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Correction NEEDS_FIX is valid and confirmed: libc::close(sock) is called before last_os_error(), allowing close() to clobber errno. The existing close_and_error helper in the same file solves exactly this problem but isn't used here. This is a real bug. Tests NONE retained. Documentation reclassified: private function whose name and signature clearly communicate its purpose; private-item leniency applies.
- **[deliberation] set_mtu: documentation UNDOCUMENTED → DOCUMENTED**: Deliberation reclassified: Correction NEEDS_FIX is valid: same errno-clobbering defect as configure_address, plus the IfreqMtu struct padding issue means the kernel reads past allocated memory. Both are real bugs. Tests NONE retained. Documentation reclassified: private function with self-descriptive name; private-item leniency applies.
- **[deliberation] read: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility reclassified DEAD→USED: same reasoning as create — this is a public API function in a platform-specific module, not dead code. Tests NONE retained: AF-header stripping logic is testable. Documentation reclassified UNDOCUMENTED→PARTIAL: the source clearly shows a `///` doc comment on line 245 describing purpose and AF-header stripping. The evaluator field was inconsistent with its own detail text which correctly identified PARTIAL.
- **[deliberation] write: documentation UNDOCUMENTED → PARTIAL**: Deliberation reclassified: Utility reclassified DEAD→USED: public API in platform-specific module, conditionally compiled for macOS. Tests NONE retained: IP version detection, empty-packet rejection, and AF-header prepending are all testable pure logic. Documentation reclassified UNDOCUMENTED→PARTIAL: line 266 has `/// Write an IP packet to the utun fd, prepending the 4-byte AF header.` The evaluator field contradicted its own detail which correctly identified PARTIAL.


## System (init)

**Model:** claude-sonnet-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{
  "symbols": [
    {
      "name": "AF_XDP",
      "line_start": 20,
      "line_end": 20,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching canonical Linux kernel AF_XDP address-family value (44). Self-documenting by convention for any XDP-aware systems programmer; private-item leniency applies."
    },
    {
      "name": "SOL_XDP",
      "line_start": 21,
      "line_end": 21,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching canonical Linux SOL_XDP socket-option level (283). Standard kernel name is self-documenting; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_REG",
      "line_start": 23,
      "line_end": 23,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP setsockopt option name. Standard uapi name is self-documenting; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_FILL_RING",
      "line_start": 24,
      "line_end": 24,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP setsockopt option name for fill ring size. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_COMPLETION_RING",
      "line_start": 25,
      "line_end": 25,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP setsockopt option for completion ring. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_RX_RING",
      "line_start": 26,
      "line_end": 26,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP RX ring setsockopt option. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_TX_RING",
      "line_start": 27,
      "line_end": 27,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP TX ring setsockopt option. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_MMAP_OFFSETS",
      "line_start": 28,
      "line_end": 28,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP getsockopt option for querying mmap offsets. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_PGOFF_RX_RING",
      "line_start": 30,
      "line_end": 30,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP mmap page-offset for the RX ring. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_PGOFF_TX_RING",
      "line_start": 31,
      "line_end": 31,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP mmap page-offset for the TX ring (0x08000000). Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_PGOFF_FILL_RING",
      "line_start": 32,
      "line_end": 32,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP UMEM fill-ring mmap page-offset. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_PGOFF_COMPLETION_RING",
      "line_start": 33,
      "line_end": 33,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private constant matching Linux kernel XDP UMEM completion-ring mmap page-offset. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_USE_NEED_WAKEUP",
      "line_start": 35,
      "line_end": 35,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private flag constant matching Linux kernel XDP_USE_NEED_WAKEUP bind flag (bit 3). Standard uapi flag name; private-item leniency applies."
    },
    {
      "name": "XDP_RING_NEED_WAKEUP",
      "line_start": 36,
      "line_end": 36,
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "Private flag constant matching Linux kernel ring-flags NEED_WAKEUP bit. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XdpUmemReg",
      "line_start": 41,
      "line_end": 48,
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "Private repr(C) struct mirroring Linux kernel xdp_umem_reg uapi layout. Section header comment 'Struct layouts (matching kernel uapi)' provides explicit context. Field names match kernel headers exactly. Private-item leniency applies."
    },
    {
      "name": "SockaddrXdp",
      "line_start": 52,
      "line_end": 58,
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "Private repr(C) struct mirroring Linux kernel sockaddr_xdp. Field names (family, flags, ifindex, queue_id, shared_umem_fd) match kernel uapi exactly. Private-item leniency with section header context applies."
    },
    {
      "name": "XdpDesc",
      "line_start": 62,
      "line_end": 66,
      "documentation": "UNDOCUMENTED",
      "confidence": 95,
      "detail": "Public struct (pub) with three public fields (addr, len, options) and no /// doc comment whatsoever. As a public type returned and consumed by XdpSocket's rx_poll/tx_send, it requires at minimum a struct-level /// explaining that it is a kernel XDP ring descriptor, plus field-level docs."
    },
    {
      "name": "XdpRingOffset",
      "line_start": 70,
      "line_end": 75,
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "Private repr(C) struct mirroring Linux kernel xdp_ring_offset. Field names (producer, consumer, desc, flags) match kernel uapi. Private-item leniency with section header context applies."
    },
    {
      "name": "XdpMmapOffsets",
      "line_start": 79,
      "line_end": 84,
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "Private repr(C) struct mirroring Linux kernel xdp_mmap_offsets. Aggregates rx/tx/fr/cr XdpRingOffset fields matching kernel layout. Private-item leniency with section header context applies."
    },
    {
      "name": "Ring",
      "line_start": 89,
      "line_end": 95,
      "documentation": "DOCUMENTED",
      "confidence": 75,
      "detail": "Private struct with an explicit /// doc comment 'A single-producer/single-consumer ring over mmap'd memory.' and an explanatory note on the descs field ('Either *mut XdpDesc or *mut u64 depending on ring type'). Adequate for a private internal type under private-item leniency."
    },
    {
      "name": "XdpConfig",
      "line_start": 126,
      "line_end": 132,
      "documentation": "PARTIAL",
      "confidence": 88,
      "detail": "Public struct with a struct-level /// doc ('Configuration for creating an XDP socket.') but no individual field-level /// comments on any of its five public fields (ifname, queue_id, frame_size, num_frames, ring_size). Missing an # Examples section and field descriptions documenting valid ranges or defaults."
    },
    {
      "name": "XdpSocket",
      "line_start": 148,
      "line_end": 160,
      "documentation": "PARTIAL",
      "confidence": 88,
      "detail": "Public struct with a two-line /// doc block describing purpose and zero-copy role. Only the free_frames field carries a /// comment; the remaining eight fields (fd, umem, umem_size, frame_size, rx_ring, tx_ring, fill_ring, comp_ring, ring_size) have no documentation. No # Examples section. Partial coverage for a public type."
    },
    {
      "name": "setsockopt_raw",
      "line_start": 407,
      "line_end": 415,
      "documentation": "DOCUMENTED",
      "confidence": 68,
      "detail": "Private unsafe generic helper wrapping libc::setsockopt. Name is perfectly self-descriptive (setsockopt, raw pointer variant). Private-item leniency applies; no /// required."
    },
    {
      "name": "mmap_ring",
      "line_start": 417,
      "line_end": 437,
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "Private unsafe helper that mmaps a single XDP ring region. Name clearly communicates intent. Parameters (fd, off, ring_size, pgoff, desc_size) mirror the kernel mmap contract. Private-item leniency applies."
    },
    {
      "name": "make_ring",
      "line_start": 439,
      "line_end": 447,
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "Private unsafe constructor that builds a Ring from a raw mmap base pointer and kernel offsets. Name and parameter types are self-descriptive. Private-item leniency applies."
    },
    {
      "name": "if_nametoindex",
      "line_start": 449,
      "line_end": 458,
      "documentation": "UNDOCUMENTED",
      "confidence": 95,
      "detail": "Public function (pub) with no /// doc comment. Returns io::Result<u32> and wraps libc::if_nametoindex, but does not document the string format expected for name, the error condition when the interface does not exist, or provide an # Examples section. Public API with no documentation."
    }
  ],
  "docs_coverage": {
    "concepts": [],
    "score_pct": 75
  }
}

## rate_limit_event

## Result (success)

**Cost:** $0.3663 | **Duration:** 120.6s


---

# Deliberation Pass

## User

## Merged ReviewFile (from 7 axis evaluators)

```json
{
  "version": 2,
  "file": "rustguard-tun/src/xdp.rs",
  "is_generated": false,
  "language": "rust",
  "parse_method": "ast",
  "verdict": "CRITICAL",
  "symbols": [
    {
      "name": "AF_XDP",
      "kind": "constant",
      "exported": false,
      "line_start": 20,
      "line_end": 20,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used in socket creation and SockaddrXdp struct | [UNIQUE] No similar symbols found. Linux kernel constant for AF_XDP socket family. | [OK] Value 44 matches the Linux kernel AF_XDP address family constant defined in linux/socket.h. | [LEAN] Direct Linux kernel UAPI constant (44). libc does not expose AF_XDP, so manual definition is required. Single value, no abstraction. | [GOOD] Compile-time constant mirroring kernel UAPI value. No runtime behavior to test directly; consistent with Known False Positives precedent (COOKIE_LEN, AEAD_TAG_LEN) — GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching canonical Linux kernel AF_XDP address-family value (44). Self-documenting by convention for any XDP-aware systems programmer; private-item leniency applies."
    },
    {
      "name": "SOL_XDP",
      "kind": "constant",
      "exported": false,
      "line_start": 21,
      "line_end": 21,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used in six setsockopt/getsockopt socket option calls | [UNIQUE] No similar symbols found. Linux kernel socket option level constant. | [OK] Value 283 matches the SOL_XDP socket-level constant in linux/if_xdp.h. | [LEAN] Direct Linux kernel socket-option level constant (283). Necessary for all setsockopt/getsockopt calls on AF_XDP sockets. Not provided by libc. | [GOOD] Compile-time constant mirroring kernel UAPI value. No runtime behavior to test directly; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching canonical Linux SOL_XDP socket-option level (283). Standard kernel name is self-documenting; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_REG",
      "kind": "constant",
      "exported": false,
      "line_start": 23,
      "line_end": 23,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used in UMEM registration socket option call | [UNIQUE] No similar symbols found. Kernel AF_XDP UMEM registration constant. | [OK] Value 4 matches XDP_UMEM_REG setsockopt option from linux/if_xdp.h. | [LEAN] Kernel UAPI option name for UMEM registration. Required by the AF_XDP setup sequence; no installed dependency provides it. | [GOOD] Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP setsockopt option name. Standard uapi name is self-documenting; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_FILL_RING",
      "kind": "constant",
      "exported": false,
      "line_start": 24,
      "line_end": 24,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used to set fill ring size via setsockopt | [UNIQUE] No similar symbols found. Kernel AF_XDP fill ring constant. | [OK] Value 5 matches XDP_UMEM_FILL_RING from linux/if_xdp.h. | [LEAN] Kernel UAPI constant for fill-ring size option. Minimal, single-purpose, directly consumed by setsockopt call. | [GOOD] Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP setsockopt option name for fill ring size. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_COMPLETION_RING",
      "kind": "constant",
      "exported": false,
      "line_start": 25,
      "line_end": 25,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used to set completion ring size | [UNIQUE] No similar symbols found. Kernel AF_XDP completion ring constant. | [OK] Value 6 matches XDP_UMEM_COMPLETION_RING from linux/if_xdp.h. | [LEAN] Kernel UAPI constant for completion-ring size option. Directly consumed by one setsockopt call, no surrounding abstraction. | [GOOD] Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP setsockopt option for completion ring. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_RX_RING",
      "kind": "constant",
      "exported": false,
      "line_start": 26,
      "line_end": 26,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used to set RX ring size socket option | [UNIQUE] No similar symbols found. Kernel AF_XDP RX ring constant. | [OK] Value 2 matches XDP_RX_RING from linux/if_xdp.h. | [LEAN] Kernel UAPI option name for RX ring size. Required and minimal. | [GOOD] Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP RX ring setsockopt option. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_TX_RING",
      "kind": "constant",
      "exported": false,
      "line_start": 27,
      "line_end": 27,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used to set TX ring size socket option | [UNIQUE] No similar symbols found. Kernel AF_XDP TX ring constant. | [OK] Value 3 matches XDP_TX_RING from linux/if_xdp.h. | [LEAN] Kernel UAPI option name for TX ring size. Required and minimal. | [GOOD] Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP TX ring setsockopt option. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_MMAP_OFFSETS",
      "kind": "constant",
      "exported": false,
      "line_start": 28,
      "line_end": 28,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used in getsockopt to query mmap offsets | [UNIQUE] No similar symbols found. Kernel AF_XDP mmap offsets constant. | [OK] Value 1 matches XDP_MMAP_OFFSETS getsockopt option from linux/if_xdp.h. | [LEAN] Kernel UAPI option name used in the single getsockopt call that retrieves mmap offsets. Necessary, no abstraction overhead. | [GOOD] Compile-time kernel UAPI constant. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP getsockopt option for querying mmap offsets. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_PGOFF_RX_RING",
      "kind": "constant",
      "exported": false,
      "line_start": 30,
      "line_end": 30,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used as page offset for RX ring mmap | [UNIQUE] No similar symbols found. Kernel AF_XDP RX ring page offset. | [OK] Value 0 matches the XDP_PGOFF_RX_RING mmap page-offset constant. | [LEAN] Kernel-defined mmap page offset for the RX ring (0). Named constant improves readability over a magic number in the mmap_ring call. | [GOOD] Compile-time mmap page offset constant from kernel UAPI. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP mmap page-offset for the RX ring. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_PGOFF_TX_RING",
      "kind": "constant",
      "exported": false,
      "line_start": 31,
      "line_end": 31,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used as page offset for TX ring mmap | [UNIQUE] No similar symbols found. Kernel AF_XDP TX ring page offset. | [OK] Value 0x80000000 matches XDP_PGOFF_TX_RING mmap page-offset constant. | [LEAN] Kernel-defined mmap page offset for the TX ring (0x0_8000_0000). Named constant is essential for readability of a non-obvious magic value. | [GOOD] Compile-time mmap page offset constant from kernel UAPI. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP mmap page-offset for the TX ring (0x08000000). Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_PGOFF_FILL_RING",
      "kind": "constant",
      "exported": false,
      "line_start": 32,
      "line_end": 32,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used as page offset for fill ring mmap | [UNIQUE] No similar symbols found. Kernel AF_XDP UMEM fill ring offset. | [OK] Value 0x100000000 matches XDP_UMEM_PGOFF_FILL_RING mmap page-offset constant. | [LEAN] Kernel-defined mmap page offset for the fill ring (0x1_0000_0000). Naming it is the minimal appropriate thing to do for a non-obvious literal. | [GOOD] Compile-time mmap page offset constant from kernel UAPI. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP UMEM fill-ring mmap page-offset. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_UMEM_PGOFF_COMPLETION_RING",
      "kind": "constant",
      "exported": false,
      "line_start": 33,
      "line_end": 33,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used as page offset for completion ring | [UNIQUE] No similar symbols found. Kernel AF_XDP UMEM completion ring offset. | [OK] Value 0x180000000 matches XDP_UMEM_PGOFF_COMPLETION_RING mmap page-offset constant. | [LEAN] Kernel-defined mmap page offset for the completion ring (0x1_8000_0000). Same justification as the other PGOFF constants. | [GOOD] Compile-time mmap page offset constant from kernel UAPI. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private constant matching Linux kernel XDP UMEM completion-ring mmap page-offset. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XDP_USE_NEED_WAKEUP",
      "kind": "constant",
      "exported": false,
      "line_start": 35,
      "line_end": 35,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used in SockaddrXdp flags for wakeup | [UNIQUE] No similar symbols found. Kernel AF_XDP socket flag for wakeup. | [OK] Value 1<<3 = 8 matches XDP_USE_NEED_WAKEUP flag in struct sockaddr_xdp.flags from linux/if_xdp.h. | [LEAN] Kernel UAPI flag (bit 3) passed in SockaddrXdp.flags at bind time. Single use, no wrapping logic. | [GOOD] Compile-time flag constant (1 << 3). No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private flag constant matching Linux kernel XDP_USE_NEED_WAKEUP bind flag (bit 3). Standard uapi flag name; private-item leniency applies."
    },
    {
      "name": "XDP_RING_NEED_WAKEUP",
      "kind": "constant",
      "exported": false,
      "line_start": 36,
      "line_end": 36,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 70,
      "detail": "[USED] Non-exported constant used in Ring::needs_wakeup() flag check | [UNIQUE] No similar symbols found. Kernel AF_XDP ring flag for wakeup. | [OK] Value 1 matches XDP_RING_NEED_WAKEUP ring-flags bit from linux/if_xdp.h. | [LEAN] Kernel UAPI flag checked in Ring::needs_wakeup. Single use, naming it avoids a bare magic 1 in the bitwise AND. | [GOOD] Compile-time flag constant used as a bitmask. No runtime behavior; GOOD by rule 6 analogy. | [DOCUMENTED] Private flag constant matching Linux kernel ring-flags NEED_WAKEUP bit. Standard uapi name; private-item leniency applies."
    },
    {
      "name": "XdpUmemReg",
      "kind": "class",
      "exported": false,
      "line_start": 41,
      "line_end": 48,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "[USED] Non-exported struct constructed and used for UMEM registration | [UNIQUE] No similar symbols found. FFI struct mapping kernel XDP UMEM registration. | [NEEDS_FIX] The tx_metadata_len field was added to struct xdp_umem_reg only in Linux 6.6. On kernels prior to 6.6, setsockopt(XDP_UMEM_REG) checks optlen strictly and returns EINVAL when optlen exceeds the expected struct size, making XdpSocket::create always fail on those kernels. | [LEAN] Direct repr(C) mirror of the kernel xdp_umem_reg UAPI struct, including the newer tx_metadata_len field. Required for setsockopt UMEM registration; no abstraction beyond the necessary layout. | [GOOD] Plain #[repr(C)] data struct with no methods or runtime behavior. Per rule 6, types with no runtime behavior are GOOD by default. | [DOCUMENTED] Private repr(C) struct mirroring Linux kernel xdp_umem_reg uapi layout. Section header comment 'Struct layouts (matching kernel uapi)' provides explicit context. Field names match kernel headers exactly. Private-item leniency applies."
    },
    {
      "name": "SockaddrXdp",
      "kind": "class",
      "exported": false,
      "line_start": 52,
      "line_end": 58,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "[USED] Non-exported struct used for socket bind address construction | [UNIQUE] No similar symbols found. FFI struct for XDP socket address binding. | [OK] Layout matches struct sockaddr_xdp from linux/if_xdp.h (family, flags, ifindex, queue_id, shared_umem_fd). | [LEAN] Direct repr(C) mirror of sockaddr_xdp UAPI struct. Required by libc::bind and not provided by libc. Minimal. | [GOOD] Plain #[repr(C)] data struct with no methods or runtime behavior. Per rule 6, types with no runtime behavior are GOOD by default. | [DOCUMENTED] Private repr(C) struct mirroring Linux kernel sockaddr_xdp. Field names (family, flags, ifindex, queue_id, shared_umem_fd) match kernel uapi exactly. Private-item leniency with section header context applies."
    },
    {
      "name": "XdpDesc",
      "kind": "class",
      "exported": true,
      "line_start": 62,
      "line_end": 66,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "UNDOCUMENTED",
      "confidence": 85,
      "detail": "[USED] Exported struct; pub type in library crate, matches false-positive pattern | [UNIQUE] No similar symbols found. FFI struct for XDP ring descriptor. | [OK] Layout matches struct xdp_desc from linux/if_xdp.h (addr u64, len u32, options u32). | [LEAN] Direct repr(C) mirror of xdp_desc UAPI struct used in RX and TX ring slots. pub fields are appropriate since callers read addr/len directly. | [GOOD] Public plain #[repr(C)] data struct with no methods or runtime behavior. Per rule 6, types with no runtime behavior are GOOD by default. | [UNDOCUMENTED] Public struct (pub) with three public fields (addr, len, options) and no /// doc comment whatsoever. As a public type returned and consumed by XdpSocket's rx_poll/tx_send, it requires at minimum a struct-level /// explaining that it is a kernel XDP ring descriptor, plus field-level docs."
    },
    {
      "name": "XdpRingOffset",
      "kind": "class",
      "exported": false,
      "line_start": 70,
      "line_end": 75,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "[USED] Non-exported struct used in XdpMmapOffsets and mmap_ring calls | [UNIQUE] No similar symbols found. FFI struct for XDP ring buffer offsets. | [OK] Layout matches struct xdp_ring_offset from linux/if_xdp.h (producer, consumer, desc, flags all u64). | [LEAN] Direct repr(C) mirror of xdp_ring_offset UAPI struct. Populated by getsockopt(XDP_MMAP_OFFSETS) and then consumed by mmap_ring/make_ring. No unnecessary fields or methods. | [GOOD] Plain #[repr(C)] data struct deriving Default with no custom methods or runtime logic. Per rule 6, types with no runtime behavior are GOOD by default. | [DOCUMENTED] Private repr(C) struct mirroring Linux kernel xdp_ring_offset. Field names (producer, consumer, desc, flags) match kernel uapi. Private-item leniency with section header context applies."
    },
    {
      "name": "XdpMmapOffsets",
      "kind": "class",
      "exported": false,
      "line_start": 79,
      "line_end": 84,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "[USED] Non-exported struct constructed to query and store ring offsets | [UNIQUE] No similar symbols found. FFI struct for all XDP mmap ring offsets. | [OK] Layout matches struct xdp_mmap_offsets (rx, tx, fr, cr each an XdpRingOffset). | [LEAN] Direct repr(C) mirror of xdp_mmap_offsets UAPI struct (rx, tx, fr, cr). Used in one getsockopt call, then fields are accessed individually. Minimal wrapper. | [GOOD] Plain #[repr(C)] data struct composed of XdpRingOffset fields with no methods or runtime logic. Per rule 6, types with no runtime behavior are GOOD by default. | [DOCUMENTED] Private repr(C) struct mirroring Linux kernel xdp_mmap_offsets. Aggregates rx/tx/fr/cr XdpRingOffset fields matching kernel layout. Private-item leniency with section header context applies."
    },
    {
      "name": "Ring",
      "kind": "class",
      "exported": false,
      "line_start": 89,
      "line_end": 95,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "DOCUMENTED",
      "confidence": 75,
      "detail": "[USED] Non-exported struct used as field type for all ring buffers | [UNIQUE] No similar symbols found. Internal ring buffer wrapper for mmap'd memory. | [OK] Struct fields and SPSC atomic orderings (Acquire load, Release store) are correct for a shared ring buffer over mmap. The stored mask field is unused by Ring's own methods but harmless. | [LEAN] Thin struct encapsulating the four raw pointers and mask for one lock-free SPSC ring. The five accessor methods (producer, consumer, set_producer, set_consumer, needs_wakeup) prevent repeated unsafe derefs and encode Acquire/Release ordering in one place. Note: the stored mask field is never used inside Ring itself—XdpSocket methods recompute ring_size-1—but this is a minor inconsistency, not overengineering. | [NONE] Private struct with multiple runtime methods (producer, consumer, set_producer, set_consumer, needs_wakeup) performing atomic loads/stores via raw unsafe pointers. No test file exists for this module; zero test coverage. | [DOCUMENTED] Private struct with an explicit /// doc comment 'A single-producer/single-consumer ring over mmap'd memory.' and an explanatory note on the descs field ('Either *mut XdpDesc or *mut u64 depending on ring type'). Adequate for a private internal type under private-item leniency."
    },
    {
      "name": "XdpConfig",
      "kind": "class",
      "exported": true,
      "line_start": 126,
      "line_end": 132,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "GOOD",
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "[USED] Exported struct; pub type in library crate, parameter to create() | [UNIQUE] No similar symbols found. Configuration struct for XDP socket creation. | [OK] Config struct fields are correctly typed. Default values (frame_size=4096, num_frames=4096, ring_size=2048) are all powers-of-two as required. Absence of a power-of-2 enforcement on ring_size is a validation gap in create_inner, not a struct-level bug. | [LEAN] Five-field configuration struct with a sensible Default. Appropriate for a complex multi-step setup (UMEM + 4 rings + bind). No builder pattern, no unnecessary generics. | [GOOD] Public configuration struct with a trivial Default impl. Analogous to JoinConfig (reclassified GOOD in Known False Positives) — pure data carrier with no meaningful runtime logic to test. Per rule 6. | [PARTIAL] Public struct with a struct-level /// doc ('Configuration for creating an XDP socket.') but no individual field-level /// comments on any of its five public fields (ifname, queue_id, frame_size, num_frames, ring_size). Missing an # Examples section and field descriptions documenting valid ranges or defaults."
    },
    {
      "name": "XdpSocket",
      "kind": "class",
      "exported": true,
      "line_start": 148,
      "line_end": 160,
      "correction": "ERROR",
      "overengineering": "ACCEPTABLE",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "PARTIAL",
      "confidence": 85,
      "detail": "[USED] Exported struct; main pub API for AF_XDP zero-copy UDP I/O | [UNIQUE] No similar symbols found. Main XDP socket wrapper with UMEM and rings. | [ERROR] Four correctness bugs across the impl block: (1) tx_send copies data.len() bytes into a UMEM frame without verifying data.len() <= frame_size, causing an out-of-bounds write that corrupts adjacent UMEM frames. (2) tx_send advances the TX ring producer without checking that prod.wrapping_sub(consumer) < ring_size, allowing ring overflow that overwrites unprocessed descriptors. (3) Drop only munmaps UMEM; all four ring mmap regions (rx, tx, fill, comp) are never freed because neither Ring nor XdpSocket stores their base pointers, causing a guaranteed resource leak on every drop. (4) create_inner silently discards the return values of the four setsockopt ring-size calls, masking configuration failures. | [LEAN] Main socket struct owns fd, UMEM, four Ring abstractions, and free_frames list. All fields are directly needed by the public API (rx_poll, rx_release, tx_send, tx_complete, fd). The complexity is inherent in AF_XDP's UMEM + ring-buffer protocol, not artificially introduced. | [NONE] Core public struct with complex runtime methods (create, rx_poll, rx_release, tx_send, tx_complete, fd) involving unsafe syscalls, mmap, ring buffer manipulation, and Drop cleanup. No test file found for this module; zero test coverage for any of these critical code paths. | [PARTIAL] Public struct with a two-line /// doc block describing purpose and zero-copy role. Only the free_frames field carries a /// comment; the remaining eight fields (fd, umem, umem_size, frame_size, rx_ring, tx_ring, fill_ring, comp_ring, ring_size) have no documentation. No # Examples section. Partial coverage for a public type."
    },
    {
      "name": "setsockopt_raw",
      "kind": "function",
      "exported": false,
      "line_start": 407,
      "line_end": 415,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "DOCUMENTED",
      "confidence": 68,
      "detail": "[USED] Non-exported helper function called five times in ring configuration | [UNIQUE] No similar symbols found. Helper for raw libc setsockopt calls. | [OK] Correct thin wrapper; returns the raw libc return code for the caller to inspect. | [LEAN] Generic helper that eliminates six near-identical libc::setsockopt call sites. The generic monomorphises to correctly sized calls; no additional abstraction layers are added. | [NONE] Private unsafe helper wrapping libc::setsockopt. Has runtime behavior (syscall invocation). No test file found for this module; zero test coverage. | [DOCUMENTED] Private unsafe generic helper wrapping libc::setsockopt. Name is perfectly self-descriptive (setsockopt, raw pointer variant). Private-item leniency applies; no /// required."
    },
    {
      "name": "mmap_ring",
      "kind": "function",
      "exported": false,
      "line_start": 417,
      "line_end": 437,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "[USED] Non-exported function called four times to map ring buffers | [UNIQUE] No similar symbols found. Helper for mmap'ing XDP ring buffers. | [OK] Correctly computes mapping size as off.desc + ring_size * desc_size and creates a MAP_SHARED mapping at the specified page offset. Returns the base pointer, which make_ring consumes. | [LEAN] Encapsulates the size calculation (off.desc + ring_size * desc_size) and mmap syscall for one ring. Called four times with different offsets and desc_sizes; the factoring is appropriate. | [NONE] Private unsafe function performing mmap syscall with fallibility (io::Result). Contains non-trivial size computation logic. No test file found; zero test coverage. | [DOCUMENTED] Private unsafe helper that mmaps a single XDP ring region. Name clearly communicates intent. Parameters (fd, off, ring_size, pgoff, desc_size) mirror the kernel mmap contract. Private-item leniency applies."
    },
    {
      "name": "make_ring",
      "kind": "function",
      "exported": false,
      "line_start": 439,
      "line_end": 447,
      "correction": "NEEDS_FIX",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "DOCUMENTED",
      "confidence": 65,
      "detail": "[USED] Non-exported function called four times to construct Ring objects | [UNIQUE] No similar symbols found. Helper for constructing Ring from mmap regions. | [NEEDS_FIX] The base pointer (start of the ring mmap region) is used to derive Ring's internal pointers but is never stored in the resulting Ring. XdpSocket stores Ring values but not the mmap base or size needed for munmap, making it structurally impossible for Drop to free the ring mappings. This design omission is the direct root cause of the resource leak in XdpSocket::drop. | [LEAN] Constructs a Ring by computing four pointer offsets from a base address and the XdpRingOffset. Called four times with different offset structs; the factoring removes repetitive unsafe pointer arithmetic. | [NONE] Private unsafe function constructing a Ring from raw pointer arithmetic. Runtime behavior with pointer offset calculations. No test file found; zero test coverage. | [DOCUMENTED] Private unsafe constructor that builds a Ring from a raw mmap base pointer and kernel offsets. Name and parameter types are self-descriptive. Private-item leniency applies."
    },
    {
      "name": "if_nametoindex",
      "kind": "function",
      "exported": true,
      "line_start": 449,
      "line_end": 458,
      "correction": "OK",
      "overengineering": "LEAN",
      "utility": "USED",
      "duplication": "UNIQUE",
      "tests": "NONE",
      "documentation": "UNDOCUMENTED",
      "confidence": 85,
      "detail": "[USED] Exported function in library crate, called in create_inner setup | [UNIQUE] Score 0.779 with ifindex_to_name (reverse function) shows structural similarity in interface operations, but opposite semantic contracts (name→index vs index→name). Different callers, different invariants, not interchangeable. | [OK] Correctly converts interface name via CString, calls libc::if_nametoindex, and maps a zero return to the last OS error. | [LEAN] Thin safe wrapper around libc::if_nametoindex: converts &str to CString, calls the syscall, maps errno on failure. The conversion and error mapping are necessary and not over-abstracted. | [NONE] Public function wrapping libc::if_nametoindex with CString conversion and error handling. Has meaningful runtime behavior including the null-termination check and OS error mapping. No test file found for this module; zero test coverage. | [UNDOCUMENTED] Public function (pub) with no /// doc comment. Returns io::Result<u32> and wraps libc::if_nametoindex, but does not document the string format expected for name, the error condition when the interface does not exist, or provide an # Examples section. Public API with no documentation."
    }
  ],
  "actions": [
    {
      "id": 1,
      "description": "tx_send copies data.len() bytes into a UMEM frame (self.umem.add(frame_addr)) without checking that data.len() <= self.frame_size. If the caller passes a slice larger than one frame, adjacent frames are silently overwritten, corrupting other live packets and causing undefined memory behaviour.",
      "severity": "high",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L348",
      "source": "correction"
    },
    {
      "id": 2,
      "description": "tx_send does not check TX ring fullness before writing a descriptor. It should verify prod.wrapping_sub(self.tx_ring.consumer()) < self.ring_size; omitting this allows the producer to lap the consumer and overwrite unprocessed TX entries when the ring is saturated.",
      "severity": "medium",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L356",
      "source": "correction"
    },
    {
      "id": 3,
      "description": "XdpSocket::drop munmaps only UMEM. The four ring mmap regions produced by mmap_ring (rx_map, tx_map, fill_map, comp_map) are consumed by make_ring but their base pointers and sizes are discarded. Neither Ring nor XdpSocket stores them, so every XdpSocket drop permanently leaks those four mappings.",
      "severity": "medium",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L396",
      "source": "correction"
    },
    {
      "id": 4,
      "description": "create_inner ignores the return values of all four setsockopt ring-size calls (XDP_UMEM_FILL_RING, XDP_UMEM_COMPLETION_RING, XDP_RX_RING, XDP_TX_RING). If any fail, the socket proceeds with an unconfigured or default ring size, silently producing a broken XdpSocket.",
      "severity": "medium",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L217",
      "source": "correction"
    },
    {
      "id": 5,
      "description": "In create_inner, if any mmap_ring call after the first fails (via the ? operator), previously mmapped ring regions are leaked: a tx_map failure leaves rx_map unmapped; a fill_map failure leaves rx_map and tx_map unmapped; a comp_map failure leaves all three. Additionally, if bind fails (L276), all four ring mmaps are leaked because only UMEM is freed in the error path.",
      "severity": "medium",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L241",
      "source": "correction"
    },
    {
      "id": 6,
      "description": "XdpUmemReg includes tx_metadata_len (kernel 6.6+ only). On pre-6.6 kernels the setsockopt(XDP_UMEM_REG) call receives an optlen larger than the kernel expects and returns EINVAL, making XdpSocket::create unconditionally fail on those kernels.",
      "severity": "low",
      "effort": "small",
      "category": "quickwin",
      "target_symbol": null,
      "target_lines": "L41",
      "source": "correction"
    },
    {
      "id": 7,
      "description": "Add JSDoc documentation for exported symbol: `XdpDesc`",
      "severity": "medium",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "XdpDesc",
      "target_lines": "L62-L66"
    },
    {
      "id": 8,
      "description": "Complete JSDoc documentation for: `XdpConfig`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "XdpConfig",
      "target_lines": "L126-L132"
    },
    {
      "id": 9,
      "description": "Complete JSDoc documentation for: `XdpSocket`",
      "severity": "low",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "XdpSocket",
      "target_lines": "L148-L160"
    },
    {
      "id": 10,
      "description": "Add JSDoc documentation for exported symbol: `if_nametoindex`",
      "severity": "medium",
      "effort": "trivial",
      "category": "hygiene",
      "source": "documentation",
      "target_symbol": "if_nametoindex",
      "target_lines": "L449-L458"
    }
  ],
  "file_level": {
    "unused_imports": [],
    "circular_dependencies": [],
    "general_notes": "Best practices score: 6/10 (2 FAIL, 3 WARN, 7 PASS)"
  },
  "best_practices": {
    "score": 6,
    "rules": [
      {
        "rule_id": 1,
        "rule_name": "No unwrap in production code",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No .unwrap() calls anywhere in the file. Errors are propagated via io::Error::last_os_error() with early returns, the ? operator on fallible helpers, and explicit if < 0 guards. Correct throughout."
      },
      {
        "rule_id": 2,
        "rule_name": "No unsafe blocks without clear justification comment",
        "status": "WARN",
        "severity": "CRITICAL",
        "detail": "Partial compliance. The module-level //! docstring explains the unsafe strategy, and unsafe impl Send for Ring/XdpSocket both carry // Safety: comments. XdpSocket::create has a # Safety doc section. However, six inline unsafe blocks inside safe public functions (rx_poll x2, rx_release, tx_send x3, Drop::drop) have no // Safety: comment explaining which invariants are upheld (e.g., that idx is bounded by the mask, that UMEM addresses are valid, that desc.len does not exceed frame_size).",
        "lines": "L260-L280, L295-L305, L316-L342, L348-L352"
      },
      {
        "rule_id": 3,
        "rule_name": "Proper error handling with Result/Option (no silent ignores)",
        "status": "FAIL",
        "severity": "HIGH",
        "detail": "Step 4 of create_inner calls setsockopt_raw four times (XDP_UMEM_FILL_RING, XDP_UMEM_COMPLETION_RING, XDP_RX_RING, XDP_TX_RING) and discards every return value. A kernel failure here silently produces a misconfigured socket that will appear to work but produce no traffic. Additionally, the wakeup libc::sendto call inside tx_send ignores its return value.",
        "lines": "L207-L210, L333-L337"
      },
      {
        "rule_id": 4,
        "rule_name": "Derive common traits on public types",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "XdpConfig is a public configuration struct with no derives at all — at minimum Debug and Clone are expected for usability and testability. XdpDesc is #[repr(C)] pub and derives Clone + Copy but is missing Debug and PartialEq. Internal types (Ring, SockaddrXdp, XdpRingOffset, XdpMmapOffsets) are private and not subject to this rule.",
        "lines": "L63-L67, L116-L124"
      },
      {
        "rule_id": 5,
        "rule_name": "Lifetime annotations explicit where needed",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "The single-input-reference elision on rx_poll(&self) -> Vec<(u64, &[u8])> is unambiguous under Rust's elision rules: the output &[u8] receives the lifetime of &self, which is the correct and intended constraint. No unnecessary or misleading elisions were found elsewhere."
      },
      {
        "rule_id": 6,
        "rule_name": "Use clippy idioms",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "No unnecessary .clone() calls found. Manual indexed loops over ring descriptors are appropriate here since the loop bodies dereference raw pointers — iterator adapters would not simplify the code. Vec::with_capacity is used correctly in rx_poll. No obvious clippy violations detected."
      },
      {
        "rule_id": 7,
        "rule_name": "No panic! in library/production code",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No panic!, unreachable!, todo!, or unimplemented! macros appear in the file."
      },
      {
        "rule_id": 8,
        "rule_name": "Module organization",
        "status": "PASS",
        "severity": "MEDIUM",
        "detail": "The module is correctly placed in rustguard-tun/src/xdp.rs as a Linux-only transport concern. #![cfg(target_os = \"linux\")] gates the entire module cleanly. Internal helpers (setsockopt_raw, mmap_ring, make_ring, Ring) are private. The four public exports (XdpSocket, XdpDesc, XdpConfig, if_nametoindex) are intentional and minimal. Section comments (Constants, Struct layouts, Ring buffer, XDP Socket, Helpers) give clear visual hierarchy."
      },
      {
        "rule_id": 9,
        "rule_name": "Documentation comments on public items",
        "status": "WARN",
        "severity": "MEDIUM",
        "detail": "XdpSocket and its methods are well-documented. The module-level //! block is thorough. However, XdpConfig has no /// doc comment and its fields (ifname, queue_id, frame_size, num_frames, ring_size) are undocumented. XdpDesc and its pub fields (addr, len, options) have no docs. The public function if_nametoindex has no doc comment.",
        "lines": "L63, L116, L429"
      },
      {
        "rule_id": 10,
        "rule_name": "No hardcoded secrets or credentials",
        "status": "PASS",
        "severity": "CRITICAL",
        "detail": "No secrets, API keys, tokens, or passwords found. The only hardcoded string is 'eth0' in XdpConfig::default, which is a conventional network interface default and not a credential."
      },
      {
        "rule_id": 11,
        "rule_name": "Memory safety (no leaks, proper Drop impls)",
        "status": "FAIL",
        "severity": "HIGH",
        "detail": "create_inner allocates four separate mmap regions via mmap_ring (rx_map, tx_map, fill_map, comp_map). These raw pointers are consumed by make_ring but never stored in XdpSocket — Ring only retains interior pointers (producer, consumer, descs), not the base address or mapped size needed for munmap. The Drop impl unmaps only self.umem; all four ring mmap regions are permanently leaked on every socket close. A secondary issue: if mmap_ring fails partway through step 6 (e.g., fill_map fails), previously succeeded rx_map and tx_map are not cleaned up before returning Err.",
        "lines": "L222-L228, L348-L353"
      },
      {
        "rule_id": 12,
        "rule_name": "Concurrency safety (no data races, proper Send/Sync bounds)",
        "status": "PASS",
        "severity": "HIGH",
        "detail": "Ring is Send+Sync (backed by kernel-shared mmap accessible from any thread). XdpSocket is Send but intentionally not Sync, correctly reflecting that rx_release/tx_send/tx_complete require exclusive access (&mut self). Ring producer/consumer updates use Acquire/Release atomics, appropriate for SPSC ring buffers. The needs_wakeup flag read uses Relaxed, correct for a non-gating hint. Rust's borrow checker prevents aliasing the &[u8] slices returned by rx_poll with mutable UMEM writes in tx_send."
      }
    ],
    "suggestions": [
      {
        "description": "Check return values of the four ring-size setsockopt calls in create_inner to surface configuration failures instead of silently producing a broken socket.",
        "before": "setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);\nsetsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);\nsetsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);\nsetsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);",
        "after": "for &opt in &[XDP_UMEM_FILL_RING, XDP_UMEM_COMPLETION_RING, XDP_RX_RING, XDP_TX_RING] {\n    if setsockopt_raw(fd, SOL_XDP, opt, &rs) < 0 {\n        let err = io::Error::last_os_error();\n        libc::close(fd);\n        libc::munmap(umem as *mut _, umem_size);\n        return Err(err);\n    }\n}"
      },
      {
        "description": "Store ring map base pointers and sizes in XdpSocket so Drop can unmap them, eliminating the four mmap leaks.",
        "before": "pub struct XdpSocket {\n    fd: i32,\n    umem: *mut u8,\n    umem_size: usize,\n    frame_size: u32,\n    rx_ring: Ring,\n    tx_ring: Ring,\n    fill_ring: Ring,\n    comp_ring: Ring,\n    // ...\n}\n// Drop only unmaps umem",
        "after": "pub struct XdpSocket {\n    fd: i32,\n    umem: *mut u8,\n    umem_size: usize,\n    frame_size: u32,\n    rx_ring: Ring,\n    tx_ring: Ring,\n    fill_ring: Ring,\n    comp_ring: Ring,\n    ring_maps: [(*mut u8, usize); 4], // (base, size) for rx/tx/fill/comp\n    // ...\n}\n// In Drop:\nfor &(base, sz) in &self.ring_maps {\n    libc::munmap(base as *mut libc::c_void, sz);\n}"
      },
      {
        "description": "Add // Safety: comments to inline unsafe blocks in safe public methods, documenting the invariants that justify each block.",
        "before": "let desc = unsafe {\n    &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)\n};",
        "after": "// Safety: idx is masked to (ring_size - 1), so the offset stays within the\n// mmap'd ring region. The Ring is valid for the lifetime of XdpSocket.\nlet desc = unsafe {\n    &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)\n};"
      },
      {
        "description": "Add Debug and Clone derives to XdpConfig and add Debug and PartialEq to XdpDesc for ergonomic use in tests and logging.",
        "before": "pub struct XdpConfig {\n    pub ifname: String,\n    pub queue_id: u32,\n    pub frame_size: u32,\n    pub num_frames: u32,\n    pub ring_size: u32,\n}",
        "after": "#[derive(Debug, Clone)]\npub struct XdpConfig {\n    pub ifname: String,\n    pub queue_id: u32,\n    pub frame_size: u32,\n    pub num_frames: u32,\n    pub ring_size: u32,\n}"
      }
    ]
  },
  "docs_coverage": {
    "concepts": [],
    "score_pct": 75
  },
  "axis_meta": {
    "utility": {
      "model": "claude-haiku-4-5-20251001",
      "cost_usd": 0.10513229999999998,
      "duration_ms": 113100
    },
    "duplication": {
      "model": "claude-haiku-4-5-20251001",
      "cost_usd": 0.05587835,
      "duration_ms": 55211
    },
    "correction": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 1.55672425,
      "duration_ms": 814307
    },
    "overengineering": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.18015175,
      "duration_ms": 74516
    },
    "tests": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.22244599999999998,
      "duration_ms": 69880
    },
    "best_practices": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.4306775,
      "duration_ms": 228968
    },
    "documentation": {
      "model": "claude-sonnet-4-6",
      "cost_usd": 0.36627625,
      "duration_ms": 120600
    }
  }
}
```

## Source code of `rustguard-tun/src/xdp.rs`

```typescript
//! AF_XDP zero-copy UDP I/O for Linux.
//!
//! Replaces the standard UDP socket for receiving/sending WireGuard
//! encrypted packets. The plaintext side still uses TUN.
//!
//! Architecture:
//!   NIC -> XDP BPF filter (UDP:51820) -> AF_XDP socket -> decrypt -> TUN
//!   TUN -> encrypt -> AF_XDP TX ring -> NIC
//!
//! The unsafe is contained in this module. The ring buffer protocol is
//! lock-free (atomic producer/consumer indices on shared mmap'd memory).

#![cfg(target_os = "linux")]

use std::io;
use std::sync::atomic::{AtomicU32, Ordering};

// ── Constants ───────────────────────────────────────────────────────

const AF_XDP: i32 = 44;
const SOL_XDP: i32 = 283;

const XDP_UMEM_REG: i32 = 4;
const XDP_UMEM_FILL_RING: i32 = 5;
const XDP_UMEM_COMPLETION_RING: i32 = 6;
const XDP_RX_RING: i32 = 2;
const XDP_TX_RING: i32 = 3;
const XDP_MMAP_OFFSETS: i32 = 1;

const XDP_PGOFF_RX_RING: i64 = 0;
const XDP_PGOFF_TX_RING: i64 = 0x0_8000_0000;
const XDP_UMEM_PGOFF_FILL_RING: i64 = 0x1_0000_0000;
const XDP_UMEM_PGOFF_COMPLETION_RING: i64 = 0x1_8000_0000;

const XDP_USE_NEED_WAKEUP: u16 = 1 << 3;
const XDP_RING_NEED_WAKEUP: u32 = 1;

// ── Struct layouts (matching kernel uapi) ───────────────────────────

#[repr(C)]
struct XdpUmemReg {
    addr: u64,
    len: u64,
    chunk_size: u32,
    headroom: u32,
    flags: u32,
    tx_metadata_len: u32,
}

#[repr(C)]
#[derive(Clone, Copy)]
struct SockaddrXdp {
    family: u16,
    flags: u16,
    ifindex: u32,
    queue_id: u32,
    shared_umem_fd: u32,
}

#[repr(C)]
#[derive(Clone, Copy, Default)]
pub struct XdpDesc {
    pub addr: u64,
    pub len: u32,
    pub options: u32,
}

#[repr(C)]
#[derive(Default)]
struct XdpRingOffset {
    producer: u64,
    consumer: u64,
    desc: u64,
    flags: u64,
}

#[repr(C)]
#[derive(Default)]
struct XdpMmapOffsets {
    rx: XdpRingOffset,
    tx: XdpRingOffset,
    fr: XdpRingOffset,
    cr: XdpRingOffset,
}

// ── Ring buffer ─────────────────────────────────────────────────────

/// A single-producer/single-consumer ring over mmap'd memory.
struct Ring {
    producer: *const AtomicU32,
    consumer: *const AtomicU32,
    flags: *const AtomicU32,
    descs: *mut u8, // Either *mut XdpDesc or *mut u64 depending on ring type.
    mask: u32,
}

// Safety: Ring pointers come from mmap and are valid for the socket lifetime.
unsafe impl Send for Ring {}
unsafe impl Sync for Ring {}

impl Ring {
    fn producer(&self) -> u32 {
        unsafe { (*self.producer).load(Ordering::Acquire) }
    }

    fn consumer(&self) -> u32 {
        unsafe { (*self.consumer).load(Ordering::Acquire) }
    }

    fn set_producer(&self, val: u32) {
        unsafe { (*self.producer).store(val, Ordering::Release) }
    }

    fn set_consumer(&self, val: u32) {
        unsafe { (*self.consumer).store(val, Ordering::Release) }
    }

    fn needs_wakeup(&self) -> bool {
        unsafe { (*self.flags).load(Ordering::Relaxed) & XDP_RING_NEED_WAKEUP != 0 }
    }
}

// ── XDP Socket ──────────────────────────────────────────────────────

/// Configuration for creating an XDP socket.
pub struct XdpConfig {
    pub ifname: String,
    pub queue_id: u32,
    pub frame_size: u32,
    pub num_frames: u32,
    pub ring_size: u32,
}

impl Default for XdpConfig {
    fn default() -> Self {
        Self {
            ifname: "eth0".into(),
            queue_id: 0,
            frame_size: 4096,
            num_frames: 4096,
            ring_size: 2048,
        }
    }
}

/// An AF_XDP socket with UMEM and ring buffers.
/// Provides zero-copy packet I/O for the encrypted UDP side.
pub struct XdpSocket {
    fd: i32,
    umem: *mut u8,
    umem_size: usize,
    frame_size: u32,
    rx_ring: Ring,
    tx_ring: Ring,
    fill_ring: Ring,
    comp_ring: Ring,
    ring_size: u32,
    /// Free frame indices for TX.
    free_frames: Vec<u64>,
}

// Safety: XdpSocket owns its fd and mmap regions.
unsafe impl Send for XdpSocket {}

impl XdpSocket {
    /// Create and configure an AF_XDP socket.
    ///
    /// # Safety
    /// This function uses mmap to create shared memory regions with the kernel.
    /// The returned XdpSocket must not outlive the process.
    pub fn create(config: &XdpConfig) -> io::Result<Self> {
        unsafe { Self::create_inner(config) }
    }

    unsafe fn create_inner(config: &XdpConfig) -> io::Result<Self> {
        let umem_size = (config.frame_size * config.num_frames) as usize;

        // 1. Allocate UMEM.
        let umem = libc::mmap(
            std::ptr::null_mut(),
            umem_size,
            libc::PROT_READ | libc::PROT_WRITE,
            libc::MAP_PRIVATE | libc::MAP_ANONYMOUS | libc::MAP_POPULATE,
            -1,
            0,
        );
        if umem == libc::MAP_FAILED {
            return Err(io::Error::last_os_error());
        }
        let umem = umem as *mut u8;

        // 2. Create socket.
        let fd = libc::socket(AF_XDP, libc::SOCK_RAW, 0);
        if fd < 0 {
            libc::munmap(umem as *mut _, umem_size);
            return Err(io::Error::last_os_error());
        }

        // 3. Register UMEM.
        let reg = XdpUmemReg {
            addr: umem as u64,
            len: umem_size as u64,
            chunk_size: config.frame_size,
            headroom: 0,
            flags: 0,
            tx_metadata_len: 0,
        };
        if setsockopt_raw(fd, SOL_XDP, XDP_UMEM_REG, &reg) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 4. Set ring sizes.
        let rs = config.ring_size as i32;
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_FILL_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_UMEM_COMPLETION_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_RX_RING, &rs);
        setsockopt_raw(fd, SOL_XDP, XDP_TX_RING, &rs);

        // 5. Query mmap offsets.
        let mut offsets = XdpMmapOffsets::default();
        let mut optlen = std::mem::size_of::<XdpMmapOffsets>() as u32;
        if libc::getsockopt(
            fd,
            SOL_XDP,
            XDP_MMAP_OFFSETS,
            &mut offsets as *mut _ as *mut _,
            &mut optlen,
        ) < 0
        {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        // 6. mmap rings.
        let rx_map = mmap_ring(fd, &offsets.rx, config.ring_size, XDP_PGOFF_RX_RING, std::mem::size_of::<XdpDesc>())?;
        let tx_map = mmap_ring(fd, &offsets.tx, config.ring_size, XDP_PGOFF_TX_RING, std::mem::size_of::<XdpDesc>())?;
        let fill_map = mmap_ring(fd, &offsets.fr, config.ring_size, XDP_UMEM_PGOFF_FILL_RING, 8)?;
        let comp_map = mmap_ring(fd, &offsets.cr, config.ring_size, XDP_UMEM_PGOFF_COMPLETION_RING, 8)?;

        let mask = config.ring_size - 1;
        let rx_ring = make_ring(rx_map, &offsets.rx, mask);
        let tx_ring = make_ring(tx_map, &offsets.tx, mask);
        let fill_ring = make_ring(fill_map, &offsets.fr, mask);
        let comp_ring = make_ring(comp_map, &offsets.cr, mask);

        // 7. Pre-populate fill ring (first half of frames for RX).
        let rx_frames = config.num_frames / 2;
        let mut prod = fill_ring.producer();
        for i in 0..rx_frames {
            let addr_ptr = fill_ring.descs.add(((prod & mask) as usize) * 8) as *mut u64;
            *addr_ptr = (i * config.frame_size) as u64;
            prod += 1;
        }
        fill_ring.set_producer(prod);

        // Second half of frames are free for TX.
        let mut free_frames = Vec::with_capacity((config.num_frames - rx_frames) as usize);
        for i in rx_frames..config.num_frames {
            free_frames.push((i * config.frame_size) as u64);
        }

        // 8. Bind.
        let ifindex = if_nametoindex(&config.ifname)?;
        let sxdp = SockaddrXdp {
            family: AF_XDP as u16,
            flags: XDP_USE_NEED_WAKEUP,
            ifindex,
            queue_id: config.queue_id,
            shared_umem_fd: 0,
        };
        if libc::bind(fd, &sxdp as *const _ as *const _, std::mem::size_of::<SockaddrXdp>() as u32) < 0 {
            let err = io::Error::last_os_error();
            libc::close(fd);
            libc::munmap(umem as *mut _, umem_size);
            return Err(err);
        }

        Ok(Self {
            fd,
            umem,
            umem_size,
            frame_size: config.frame_size,
            rx_ring,
            tx_ring,
            fill_ring,
            comp_ring,
            ring_size: config.ring_size,
            free_frames,
        })
    }

    /// Receive packets. Returns an iterator over (frame_addr, packet_data) pairs.
    /// Caller must call `rx_release` after processing to return frames to the fill ring.
    pub fn rx_poll(&self) -> Vec<(u64, &[u8])> {
        let prod = self.rx_ring.producer();
        let cons = self.rx_ring.consumer();
        let available = prod.wrapping_sub(cons);

        let mut packets = Vec::with_capacity(available as usize);
        for i in 0..available {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let desc = unsafe {
                &*(self.rx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *const XdpDesc)
            };
            let data = unsafe {
                std::slice::from_raw_parts(
                    self.umem.add(desc.addr as usize),
                    desc.len as usize,
                )
            };
            packets.push((desc.addr, data));
        }
        packets
    }

    /// Release consumed RX frames back to the fill ring.
    pub fn rx_release(&mut self, frames: &[u64]) {
        // Update RX consumer.
        let cons = self.rx_ring.consumer().wrapping_add(frames.len() as u32);
        self.rx_ring.set_consumer(cons);

        // Add frames back to fill ring.
        let mut prod = self.fill_ring.producer();
        for &addr in frames {
            let idx = (prod & (self.ring_size - 1)) as usize;
            unsafe {
                let ptr = self.fill_ring.descs.add(idx * 8) as *mut u64;
                *ptr = addr;
            }
            prod = prod.wrapping_add(1);
        }
        self.fill_ring.set_producer(prod);
    }

    /// Send a packet via the TX ring. Returns false if no frames available.
    pub fn tx_send(&mut self, data: &[u8]) -> bool {
        let Some(frame_addr) = self.free_frames.pop() else {
            return false;
        };

        // Copy data into UMEM frame.
        unsafe {
            std::ptr::copy_nonoverlapping(
                data.as_ptr(),
                self.umem.add(frame_addr as usize),
                data.len(),
            );
        }

        // Write TX descriptor.
        let prod = self.tx_ring.producer();
        let idx = (prod & (self.ring_size - 1)) as usize;
        unsafe {
            let desc = &mut *(self.tx_ring.descs.add(idx * std::mem::size_of::<XdpDesc>()) as *mut XdpDesc);
            desc.addr = frame_addr;
            desc.len = data.len() as u32;
            desc.options = 0;
        }
        self.tx_ring.set_producer(prod.wrapping_add(1));

        // Kick kernel if needed.
        if self.tx_ring.needs_wakeup() {
            unsafe {
                libc::sendto(self.fd, std::ptr::null(), 0, libc::MSG_DONTWAIT, std::ptr::null(), 0);
            }
        }

        true
    }

    /// Reclaim completed TX frames.
    pub fn tx_complete(&mut self) {
        let prod = self.comp_ring.producer();
        let cons = self.comp_ring.consumer();
        let done = prod.wrapping_sub(cons);

        for i in 0..done {
            let idx = (cons.wrapping_add(i) & (self.ring_size - 1)) as usize;
            let addr = unsafe { *(self.comp_ring.descs.add(idx * 8) as *const u64) };
            self.free_frames.push(addr);
        }
        self.comp_ring.set_consumer(cons.wrapping_add(done));
    }

    /// Raw file descriptor (for poll/epoll).
    pub fn fd(&self) -> i32 {
        self.fd
    }
}

impl Drop for XdpSocket {
    fn drop(&mut self) {
        unsafe {
            libc::close(self.fd);
            libc::munmap(self.umem as *mut _, self.umem_size);
        }
    }
}

// ── Helpers ─────────────────────────────────────────────────────────

unsafe fn setsockopt_raw<T>(fd: i32, level: i32, optname: i32, val: &T) -> i32 {
    libc::setsockopt(
        fd,
        level,
        optname,
        val as *const _ as *const _,
        std::mem::size_of::<T>() as u32,
    )
}

unsafe fn mmap_ring(
    fd: i32,
    off: &XdpRingOffset,
    ring_size: u32,
    pgoff: i64,
    desc_size: usize,
) -> io::Result<*mut u8> {
    let size = off.desc as usize + (ring_size as usize) * desc_size;
    let ptr = libc::mmap(
        std::ptr::null_mut(),
        size,
        libc::PROT_READ | libc::PROT_WRITE,
        libc::MAP_SHARED | libc::MAP_POPULATE,
        fd,
        pgoff,
    );
    if ptr == libc::MAP_FAILED {
        return Err(io::Error::last_os_error());
    }
    Ok(ptr as *mut u8)
}

unsafe fn make_ring(base: *mut u8, off: &XdpRingOffset, mask: u32) -> Ring {
    Ring {
        producer: base.add(off.producer as usize) as *const AtomicU32,
        consumer: base.add(off.consumer as usize) as *const AtomicU32,
        flags: base.add(off.flags as usize) as *const AtomicU32,
        descs: base.add(off.desc as usize),
        mask,
    }
}

pub fn if_nametoindex(name: &str) -> io::Result<u32> {
    let cname = std::ffi::CString::new(name).map_err(|_| {
        io::Error::new(io::ErrorKind::InvalidInput, "invalid interface name")
    })?;
    let idx = unsafe { libc::if_nametoindex(cname.as_ptr()) };
    if idx == 0 {
        return Err(io::Error::last_os_error());
    }
    Ok(idx)
}

```

## Instructions

Deliberate ONLY symbols with findings — skip clean symbols. For each symbol, address ALL its axis findings together in one entry (e.g., if a symbol is DEAD + NEEDS_FIX, deliberate both in the same entry). Recompute the verdict.

Rules:
- Only include axes with findings in original/deliberated (omit clean axes)
- Do NOT add new findings, symbols, or actions
- Protect ERROR (require ≥ 95 confidence to downgrade)
- Reclassification requires confidence ≥ 85
- Copy original values when you agree with the finding

## System (init)

**Model:** claude-opus-4-6
**Mode:** axis evaluator (single-turn, no tools)

## Assistant



## Assistant

{"verdict":"CRITICAL","symbols":[{"name":"XdpUmemReg","original":{"correction":"NEEDS_FIX","confidence":65},"deliberated":{"correction":"NEEDS_FIX","confidence":70},"reasoning":"Correction NEEDS_FIX is valid: tx_metadata_len field (kernel 6.6+) causes setsockopt to fail with EINVAL on older kernels due to strict optlen checking. The code should either conditionally include this field or size-check at runtime. Confidence bumped slightly since the struct layout mismatch is clearly visible in the code, though target kernel version is unknown."},{"name":"XdpDesc","original":{"documentation":"UNDOCUMENTED","confidence":85},"deliberated":{"documentation":"UNDOCUMENTED","confidence":90},"reasoning":"Documentation UNDOCUMENTED is correct. XdpDesc is a public exported struct with three public fields (addr, len, options) and no /// doc comment. As a public API type consumed and returned by XdpSocket methods, it needs at minimum a struct-level doc explaining it mirrors kernel xdp_desc, plus field-level docs. The #[repr(C)] nature and kernel UAPI origin make documentation even more important for users."},{"name":"Ring","original":{"tests":"NONE","confidence":75},"deliberated":{"tests":"NONE","confidence":75},"reasoning":"Tests NONE is correct. Ring has five runtime methods performing atomic operations via unsafe raw pointer dereferences (producer, consumer, set_producer, set_consumer, needs_wakeup). These encode critical Acquire/Release ordering semantics. While testing requires AF_XDP kernel support making unit tests impractical, the zero coverage on these safety-critical methods is a legitimate gap."},{"name":"XdpConfig","original":{"documentation":"PARTIAL","confidence":85},"deliberated":{"documentation":"PARTIAL","confidence":88},"reasoning":"Documentation PARTIAL is correct. Has struct-level /// doc but all five public fields (ifname, queue_id, frame_size, num_frames, ring_size) lack individual /// comments documenting valid ranges, defaults, or constraints (e.g., ring_size must be power-of-two). For a public configuration struct this is incomplete."},{"name":"XdpSocket","original":{"correction":"ERROR","overengineering":"ACCEPTABLE","tests":"NONE","documentation":"PARTIAL","confidence":85},"deliberated":{"correction":"ERROR","overengineering":"ACCEPTABLE","tests":"NONE","documentation":"PARTIAL","confidence":92},"reasoning":"Correction ERROR is strongly confirmed with four distinct bugs visible in source: (1) L340-347: tx_send copies data.len() bytes without checking data.len() <= frame_size — out-of-bounds UMEM write is clearly present. (2) L352-360: tx_send advances TX producer without checking ring fullness (prod - consumer < ring_size) — ring overflow possible when free_frames count exceeds ring_size. (3) L396-400: Drop only munmaps UMEM; four ring mmap regions are never freed because base pointers/sizes are discarded after make_ring. (4) L207-210: four setsockopt return values silently discarded — clearly visible as bare statements. All four are confirmed in the source code. Overengineering ACCEPTABLE is correct — complexity is inherent to AF_XDP protocol. Tests NONE is correct — core public API with complex unsafe syscalls has zero test coverage. Documentation PARTIAL is correct — struct-level doc exists but field-level docs are missing for 8 of 9 fields. Confidence raised to 92 since all bugs are directly verifiable."},{"name":"setsockopt_raw","original":{"tests":"NONE","confidence":68},"deliberated":{"tests":"NONE","confidence":68},"reasoning":"Tests NONE is technically correct — this private unsafe function wrapping libc::setsockopt has no test coverage. However, as a thin FFI wrapper, the practical value of unit testing is low since it would require actual kernel socket operations. Keeping NONE at same confidence."},{"name":"mmap_ring","original":{"tests":"NONE","confidence":65},"deliberated":{"tests":"NONE","confidence":65},"reasoning":"Tests NONE is correct. Private unsafe function performing mmap with non-trivial size computation (off.desc + ring_size * desc_size). Contains fallible logic but requires kernel AF_XDP support to test. Zero coverage is a legitimate gap."},{"name":"make_ring","original":{"correction":"NEEDS_FIX","tests":"NONE","confidence":65},"deliberated":{"correction":"NEEDS_FIX","tests":"NONE","confidence":85},"reasoning":"Correction NEEDS_FIX is confirmed and directly related to XdpSocket's ERROR finding. The function consumes the mmap base pointer to derive Ring's internal pointers but never stores it in Ring, making it structurally impossible for XdpSocket::drop to munmap ring regions. This is the root cause of the resource leak. The fix requires storing base and size in Ring or returning them alongside. Tests NONE also correct. Confidence raised since the omission is clearly visible in the code at L439-447."},{"name":"if_nametoindex","original":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":85},"deliberated":{"tests":"NONE","documentation":"UNDOCUMENTED","confidence":88},"reasoning":"Documentation UNDOCUMENTED is correct — public function with no /// doc comment. Should document parameter format, error conditions (invalid name, nonexistent interface), and return value semantics. Tests NONE is correct — public function with CString conversion, FFI call, and error mapping has zero coverage. Both findings are straightforward for a public API function."}],"removed_actions":[],"reasoning":"Overall assessment confirms CRITICAL verdict. The XdpSocket ERROR finding is robust — all four bugs (OOB write in tx_send, TX ring overflow, ring mmap resource leak, silently ignored setsockopt errors) are directly verifiable in the source code. The make_ring NEEDS_FIX is the structural root cause of the ring mmap leak and coherently supports the XdpSocket ERROR. The XdpUmemReg NEEDS_FIX for kernel compatibility is a valid secondary concern. Documentation gaps on public types (XdpDesc, if_nametoindex fully undocumented; XdpConfig, XdpSocket partially documented) are legitimate. Test coverage is zero across the entire module — while kernel-dependent testing is challenging, the absence is still a real gap for safety-critical unsafe code. No actions need removal; all 10 are well-supported by the source code. Note: actions 7-10 reference 'JSDoc' which should be 'rustdoc' for Rust code, but the intent is clear and the actions themselves are valid."}

## rate_limit_event

## Result (success)

**Cost:** $0.2165 | **Duration:** 70.9s

